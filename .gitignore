# Dependencies
node_modules/
.pnpm-store/

# Build outputs (generated, never committed)
dist/
build/
.next/
out/
.turbo/
*.tsbuildinfo

# Env — secrets never committed; .env.example documents every var
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store

# IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# React Native
apps/mobile/ios/Pods/
apps/mobile/ios/build/
apps/mobile/android/build/
apps/mobile/android/app/build/
apps/mobile/android/.gradle/
apps/mobile/android/local.properties
apps/mobile/vendor/
apps/mobile/.bundle/config
*.xcuserdata
*.xcworkspace/xcuserdata/

# Playwright / test artifacts
playwright-report/
test-results/

# Subagent-driven-development workspace (ledger, briefs, review packages)
.superpowers/

# QA runs — plan/report/triage are COMMITTED history; only evidence blobs are ignored.
# Do NOT widen this to `.qa`: that excludes every plan, report and triage the /qa skill
# requires to be kept, and the loss is silent — the files simply stop appearing in commits.
.qa/*/artifacts/

# Playwright MCP scratch (console logs, traces), written during /qa runs
.playwright-mcp/

# graphify — generated knowledge-graph cache, never committed
graphify-out/
