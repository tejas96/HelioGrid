# Extraction ledger — Rulings, design-system rulings, owner directives, decision logs

**Source (single):** `docs/15-spec-resolutions.md` (Status: Binding · 2026-07-24 · Owner: product) — the overlay itself. All rows are therefore already post-overlay by definition; where a later amendment inside docs/15 changes an earlier ruling, the **amended (post-overlay) state is recorded first** and the superseded wording is marked in `notes`.

**Scope of this file (per Task 2 brief):** §1 R1–R18 (lines 12–118) · §3 design-system owner rulings R19-A…E + §3 canon paragraph (line 170+) · §4 owner directives OD-1…OD-10 (line 186+) · R20 (line 234) · "Earlier owner decisions (still binding)" EOD-1…EOD-7 (line 261) · "User decisions log" UD-1…UD-9 (line 272).
**Out of scope here (owned by `d-census.md`):** §2 D1–D39 conformance table (lines 120–166) and its Reading rule.

**Standing law from the source's own preamble (line 6, verbatim):** "Every ambiguity in the POC spec is ruled here, once. Agents implement the ruling; they do not re-litigate it. If code and a ruling disagree, the ruling wins until the product owner changes it **in this file**."

**Reading notes for PRD authors:**
- Quotes are verbatim from docs/15, including its bold markers. Newlines inside a cell are rendered `<br>`; nothing else is altered. Where a cell compresses, the row says so in `notes`.
- Many rulings carry **implementation content** (table names, enums, ports, packages). Global Constraint §14/DD4 bars that from PRD bodies — authors must carry the *product law* and drop the mechanism. The verbatim text is kept here so nothing is lost in translation.
- `target_doc` follows the Task 2 initial mapping unless the ruling text points elsewhere; every correction/extension is stated in `notes`.
- Rows that are pure engineering carry `registers/traceability.md (excluded: implementation)` so no row is left without a destination.
- **Source-availability gap:** every `./research/*.md` pointer in docs/15 (journey.md, phases710.md, voice.md, verify-billing.md, sync.md, ds-reconciliation.md, ds-usage.md) resolves to `docs/research/`, which **does not exist in this repo**; `./archive/BLUEPRINT.md` is stated as archived and non-binding. Detail behind those pointers is unrecoverable — record as a source gap (Global Constraint §3.5).

---

## 1. §1 — The 18 rulings (docs/15 lines 12–118)

| key | verbatim ruling/directive | consequence (from source) | target_doc | notes |
|---|---|---|---|---|
| R1 | **R1 — Naming: Proposal vs Quotation vs Quote.** "**RULING: "Proposal" everywhere — entity, schema, code identifiers, UI copy, and customer-facing documents in all three locales (EN/HI/MR).** The words "quote"/"quotation" are banned from identifiers and UI strings. One exception: global search treats "quotation"/"quote" as query aliases for proposals, because that is what users will type." | "**Consequence:** table `proposals`, i18n keys `proposal.*`, customer link says Proposal; Lingui translators render it as प्रस्ताव / प्रस्ताव with "Proposal" retained where the transliteration is more natural in the field. No dual vocabulary anywhere." | F7 + M04 · search-alias clause → F6 · customer-link wording → F5 | Ambiguity ruled (verbatim): "The spec uses all three; Indian EPCs say "quotation"." · Mapping extension: the initial table has no vocabulary row; R1 is a product-wide naming law that binds every module, so it is filed to the i18n/localization foundation (docs/10 → F3 + F7) with the search-alias half to F6 (notifications/search) and the customer-facing half to F5. · **Possible source defect, NOT resolved:** the consequence gives the same Devanagari string "प्रस्ताव / प्रस्ताव" for both HI and MR — either an intentional identical rendering or a typo; recorded, not corrected. |
| R2 | **R2 — Project state machine: short vs full chain.** POST-OVERLAY (amendment stated first, per §4 preamble): "**AMENDED 2026-08-02 (global-backend ruling):** two stages carry market-neutral enum names — `UTILITY_INSPECTION` (was DISCOM_INSPECTION) and `INCENTIVE_CLAIMED` (was SUBSIDY_CLAIMED); the blocker party `utility` replaces `discom`. Stage LABELS are market-pack data ("DISCOM inspection" / "Subsidy claimed" are the IN pack's labels); skippable stages are pack data. The machine itself — one canonical chain, same transitions — is unchanged."<br>"**RULING: the FULL 9-stage chain is the canonical state machine; the D9 shorthand is deprecated and appears nowhere.** `WON → MATERIAL_ORDERED → DISPATCHED → INSTALLATION → ELECTRICAL_METERING → UTILITY_INSPECTION → COMMISSIONED → INCENTIVE_CLAIMED → HANDED_OVER`, plus `CANCELLED` (reason required; revenue stops counting immediately). Blocker sub-states (waiting on utility/customer/material/us) ride on any stage." | "**Consequence:** enum in docs/04-data-model.md; projects board columns, tranche `due_on_stage` mapping, customer-link stage display, and days-in-stage metrics all use the 9-stage chain. Residential deals may pass through stages quickly; they still pass through them." | M08 · tranche `due_on_stage` → M11 · customer-link stage display → F5 · IN stage labels + skippable stages → F1 · days-in-stage metrics → M13 | Supersedes: D9's 5-state shorthand (Won → Ordered → Installed → Commissioned → Handed over) is **deprecated and appears nowhere**. · The RULING's chain text is already written in post-amendment enum names, so no rewrite was needed. · Market-neutrality: labels/skippable stages are explicitly market-pack data → F1 India pack keys, module body stays neutral (Global Constraint §6). |
| R3 | **R3 — Voice vendor.** "**RULING: Exotel (telephony, AgentStream bidirectional WS, DLT/TRAI ops) + Sarvam AI (Saarika STT / Bulbul TTS / Sarvam LLM) behind our `TelephonyProvider`/`SpeechProvider` ports; thin NestJS orchestrator in `apps/voice`. Bolna is the documented Plan B behind the same ports; LiveKit self-host is v2.** Decided — see ./research/voice.md ([Exotel AgentStream](https://docs.exotel.com/exotel-agentstream/bidirectional-streaming), [Sarvam pricing](https://www.sarvam.ai/api-pricing))." | "**Consequence:** agent languages (6) remain independent of UI languages (3) — the sets never converge by accident. ≈₹2.5–4/min all-in metered to the tenant usage ledger. Number provisioning + IVR per BLUEPRINT directive 7 and docs/07. (IN-market ruling: the vendors stand behind their ports; another market adds adapters — the ruling is unchanged.)" | M07 · per-minute usage metering → M12 · agent-vs-UI language sets → F7 · IN vendor/rails → F1 | Vendor→capability translation required (Global Constraint §6): Exotel/Sarvam become capability requirements with v1 vendor as reference implementation. · Amended in effect by OD-7 (telephony capability framework, ADR-0019) — read R3 together with OD-7. · Source-gap: `./research/voice.md` missing. |
| R4 | **R4 — Payment gateway.** "**RULING: Razorpay, both roles, decided.** Platform SaaS billing = Razorpay Subscriptions (UPI AutoPay primary, card e-mandate fallback, native trials); tenant customer-collections = BYO-Razorpay payment links per tenant via `PaymentLinkPort` — platform never touches tenant funds (no PA licence needed). Detail in docs/16-billing-and-entitlements.md; verification in ./research/verify-billing.md ([Subscriptions webhooks](https://razorpay.com/docs/webhooks/subscriptions/))." | "**Consequence:** D38's deferral is dead (see §2); pricing model is defined in docs/01-business-model.md; RBI payment-data localization satisfied because Razorpay holds instruments. (IN-market ruling: Razorpay is the IN rail behind `SubscriptionBillingPort`/`PaymentLinkPort`; billing schema is provider-neutral per docs/04 §9. Selling outside India needs a supplier-of-record decision — owner-blocked.)" | M12 + 04-business-model · tenant customer-collections link → M11 · Razorpay-as-IN-rail + RBI localization → F1 | Supersedes: **D38** ("Billing & subscription deferred") — "D38's deferral is dead". · Open/blocked item for `registers/open-questions.md`: "Selling outside India needs a supplier-of-record decision — owner-blocked." · Source-gap: `./research/verify-billing.md` missing. |
| R5 | **R5 — PVGIS vs Google Solar source of truth.** "**RULING: phase-10 wins. PVGIS is the energy source of record (SARAH3 → ERA5 ladder, labelled "Real · PVGIS"); built-in latitude-fit fallback always labelled "±10%". Google Solar is an enhancement — Building Insights cross-checks and roof-detection input — never a dependency; Gemini is the roof-detect photo fallback (shapes only, schema-enforced, temp 0).** India coverage gaps make any Google dependency a product-breaking bet." | "**Consequence:** energy figures never silently switch source; the provenance line names the database. All three are server-proxied with per-tenant metering (docs/07). The journey doc's phrasing is corrected wherever quoted." | M05 · provenance/source labelling → F8 · per-tenant metering of the three services → M12 | Corrects the journey doc: "Journey implies Google Solar drives the roof; phase-10 says PVGIS drives energy and Google Solar is an enhancement" → **phase-10 wins**; any journey-stage extraction quoting the Google-Solar-drives-roof phrasing is superseded here. · Mapping extension: the labelling half is provenance/honesty → F8 (per the R18 mapping row). |
| R6 | **R6 — D33 single customer link (C&I risk).** "**RULING (amended by the 20-day everything-in directive, 2026-07-24): named links + OTP-at-accept SHIP IN the 20-day build** (Track B, docs/14) — per-contact labelled links, per-link open attribution, and an OTP challenge on Accept (via `OtpPort` — MSG91 is the IN rail) above a tenant-set value threshold denominated in the tenant's currency. The `customer_links` entity carries `label` and nullable `contact_id` (docs/08); D33's accepted risk is closed at launch, not later." | "**Consequence:** acceptance records capture full attribution (link id, contact, OTP verification, IP, user agent) from day one; UXG-11 in docs/13 is in-scope, not deferred." | F5 · accept/acceptance record → M04 · OTP rail (MSG91) + currency threshold → F1 | Supersedes: **D33** ("C&I same single link, no per-contact links/OTP"). · Also flips **UXG-11** from deferred to in-scope — cross-check `ux-gaps.md`. · Risk statement being closed (verbatim): "Anyone holding the link can accept a ₹92L order." · "20-day build" calendar framing is dead per OD-5 (track structure survives, day numbers do not) — the *scope* commitment ("ship in v1") is what carries. |
| R7 | **R7 — Phantom step 5.** "**RULING: folded into the Panel layout step.** The wizard presents **9 visible steps**; arrival at Panel layout offers Place manually / Auto-fill to target / Use maximum capacity (exactly as the phase-10 census describes). Internal step IDs remain stable so ported code and tests do not renumber." | "**Consequence:** step indicator shows "n / 9"; docs/05-domain-migration.md maps old step IDs → new positions; no user-visible "step 5" ever exists." | M05 | Ambiguity ruled (verbatim): "The studio wizard counts a step 5 that has no screen (auto panel placement)." · Cross-check against `studio-census-checklist.md`: wizard step count must read 9 visible steps everywhere. |
| R8 | **R8 — Customer merge (same person, two numbers).** "**RULING (amended by the 20-day everything-in directive, 2026-07-24): the merge flow SHIPS IN the 20-day build** (Track A CRM, docs/14). The customers/contacts split (docs/04) makes merge = re-point contact and lead references to the survivor, mark the loser merged (never deleted), keep the audit trail." | "**Consequence:** merge UI lands with CRM core (UXG-05 in-scope); the merge operation provably touches no money tables (schema review in docs/04)." | M02 | Ambiguity ruled (verbatim): "Dedupe by phone cannot catch it; "offer merge later" is undesigned." · Flips **UXG-05** to in-scope. · Product law to carry: merge never deletes (archive-never-delete family) and never touches money. |
| R9 | **R9 — Snooze / dormant / reopen: consolidated state machine.** "**RULING: this table is the single definition.** All timers run as BullMQ repeatable jobs; all wake-ups land at 09:00 tenant-local time."<br>(the seven-state table is reproduced verbatim in §1a below, keyed `R9.<state>`) | "**Consequence:** the lead `stage` enum and side-state fields in docs/04 implement exactly this; no screen invents a new timer." | M02 · "needs you" owner dashboard surface → M13 · escalation notification → F6 | Ambiguity ruled (verbatim): "Triggers and timers were scattered across Stages 2/3/7." — therefore any timer text found in journey S2/S3/S7 is **superseded by R9** where they disagree; `journey-stages.md` rows must defer to this table. · Binding cross-cutting facts: wake-ups at 09:00 **tenant-local**; no state ever deletes. |
| R10 | **R10 — Agent correction training.** "**RULING: review-queue only, no auto-training, ever in v1.** A human correction updates the call record (outcome/summary) and emits a review-queue item; an owner explicitly promotes an answer into the knowledge base (same one-tap loop as unanswered questions). Nothing a rep types reaches the agent's behaviour without that explicit promotion." | "**Consequence:** no feedback pipeline to the LLM; knowledge base remains the only behaviour input besides versioned config (D36). Keeps agent behaviour auditable per call (config version + KB version)." | M07 | Ambiguity ruled (verbatim): ""Corrections train nothing automatically" — mechanism undesigned." · Non-goal to record in M07 §5: no auto-training / no feedback pipeline in v1. |
| R11 | **R11 — Quick mode status.** "**RULING: committed. Quick mode ships in the 20-day build (Track B proposal builder)** — steps 1, 3, 8, 10 visible; AI auto-fill for 4/5; tenant defaults for 6/7/9/11; loss-free expansion to the full builder." | "**Consequence:** UXG-09 designs it; duplicate-proposal (the fastest path) and Quick mode are the two speed paths and must not diverge in validation behaviour (see R12)." | M04 · tenant defaults for steps 6/7/9/11 → M01 | Status change: "Proposed (steps 1/3/8/10) but never locked" → **committed / ships in v1**. · Binding pairing: Quick mode + duplicate-proposal must share validation behaviour with R12. |
| R12 | **R12 — Chip-rail free navigation vs step gating.** "**RULING: free navigation everywhere in the proposal builder; validation runs at Generate ONLY. The Next-disabled rule is killed.** Generate lists every failure as a tappable jump ("Fix 2 issues to share"). The mandatory-components gate (D22) and the payable ≤ ₹0 block (D34) are Generate-time checks." | "**Consequence:** applies to the proposal builder. The **studio's electrical hard gate stays** — an invalid string design still blocks the layout step's Next (phase-10 census; it protects a physical system, not form completeness)." | M04 · studio electrical hard gate → M05 | Kills: the "Next disabled until valid" rule wherever it appears in the journey/source screens — supersede on sight. · Deliberate asymmetry, do NOT normalize: builder = validate-at-Generate; studio = hard gate at layout step. · Re-homes D22 and D34 enforcement to Generate-time. |
| R13 | **R13 — Catalog duplication in tenant config.** POST-OVERLAY (amendment stated first): "**AMENDED 2026-08-02 (global-backend ruling):** ALMM/DCR are the IN entries of a scheme-keyed `certifications` structure on catalog specs; the market pack declares which schemes a market requires. The two-tier resolution is unchanged."<br>"**RULING: the two-tier catalog resolves it.** One catalog surface: platform master catalog (curated, ALMM/DCR-flagged) + tenant own catalog + tenant catalog overrides; resolution order tenant-override → tenant-item → platform-item. Catalog rate history is the versioned rate history on tenant items/overrides; **`price_book_versions` exists as a separate table for non-catalog rates** (docs/04 is canon); sent proposals keep the rate version they were built with." | "**Consequence:** tables `platform_catalog_items` (with `kind` column), `tenant_catalog_*`, `tenant_catalog_overrides`, plus `price_book_versions` for non-catalog rates (docs/04 is canon); the settings screen shows one catalog with a rates panel; the duplicate row in the old spec table is void." | M01 · scheme-keyed certifications (ALMM/DCR as IN entries) → F1 · sent-proposal rate pinning → M04 | Voids: "the duplicate row in the old spec table" — the tenant-config catalog listed twice collapses to ONE catalog surface. · Market-neutrality correction: module body says "certification schemes declared by the market pack"; ALMM/DCR are F1 IN-pack keys. · Product law to carry: sent proposals keep the rate version they were built with (pinning). |
| R14 | **R14 — Offline scope boundary.** "**RULING: the exact boundary lives in docs/06-offline-and-sync.md; summary here is normative.**<br>- **Offline-full (read + write, durable queue):** physical survey capture (all groups, versioned-append), survey photos (PowerSync Attachments → Tigris, resumable), quick-add lead, activity/visit logging, task ticks, My Day + assigned leads/read cache.<br>- **Online-only (fail fast, honest message, never queued):** proposal generate/share, any money mutation (BOM edits, discounts, tranches, payments), design mutations (single-editor LWW + server version check), billing, agent/tenant config, imports/exports, PDF render.<br>- **Reads everywhere degrade to cache with a staleness banner** — money figures shown from cache carry the provisional label (money-never-stale rule)." | "**Consequence:** UXG-10 designs the status system; the NestJS PowerSync write connector accepts only the offline-full mutation set (idempotent, tenant-checked, versioned)." | F4 · per-surface offline notes → M02 (quick-add lead, activity logging, My Day), M03/M04 (survey capture + photos — see notes), M11 (money-never-stale), M05 (design mutations online-only) | Replaces the unspecified "Everything else degrades gracefully". · This summary is declared **normative** even though detail lives in docs/06 → F4 owns it; module authors reference F4, never restate a different boundary. · Timing caveat from OD-10: "Until offline lands, mobile is online-first behind a repository abstraction" — the boundary is the target state. · Survey module id is not fixed by the mapping table (survey is brief-only scope M03/M09/M10 territory); author must confirm which module owns physical survey capture. |
| R15 | **R15 — Referral model.** "**RULING: the referral tag + "came from" chip ship in Track A (CRM core); the credits LEDGER is the spec-locked exclusion.** A referral row links referrer customer → referred lead (source=referral), visible on both records and in win/loss analytics. No monetary credit, no redemption, no balance in v1." | "**Consequence:** UXG-19; when a credits ledger arrives it references the existing referral rows — no backfill problem." | M02 · win/loss analytics surface → M13 | Ambiguity ruled (verbatim): ""Tagged and credited" with no credit model." — the "credited" half is an explicit **non-goal** for M02 §5 (no monetary credit / redemption / balance in v1). |
| R16 | **R16 — Installer role gap.** "**RULING: confirmed — the coordinator (Manager role) runs the checklist in v1; crew login/role is v2.** Ticks are attributed to the coordinator; an optional free-text "done by" per step captures the crew member's name. Crew sees no money because crew sees no screen." | "**Consequence:** UXG-20; the six preset roles stay untouched (D27/D29); v2 adds an Installer preset without schema change (roles are already M:N)." | F2 · InstallationSheet / checklist behaviour → M08 | Ambiguity ruled (verbatim): "InstallationSheet's "crew ticks" have no crew user in v1." · Confirms D27/D29: exactly six preset roles, no Installer role in v1 (F2 role-name list is authoritative per DD3). |
| R17 | **R17 — OPEX/PPA scope.** "**RULING: OPEX/PPA is a proposal document type only in v1. A PPA billing/metering engine is an explicit non-goal**, recorded in docs/00-vision-and-scope.md. The proposal renders per-unit terms; the project after Won tracks the same stages and document checklist; no recurring invoicing, no meter ingestion." | "**Consequence:** the proposal `type` enum carries CAPEX/OPEX_PPA; nothing downstream branches on it except the rendered document and the honesty label on financial projections." | M04 · post-Won identical stage/checklist behaviour → M08 · honesty label on financial projections → F8 · non-goal record → vision/scope doc (docs/00 lineage) | Explicit non-goal to carry verbatim into the scope doc + M04 §5: "A PPA billing/metering engine is an explicit non-goal" — no recurring invoicing, no meter ingestion. · Mapping note: the initial table has no docs/00 → PRD row; the non-goal's stated home is docs/00-vision-and-scope.md, so it also belongs to whichever PRD doc inherits vision/scope (see `docs-rules.md` DOC00.*). |
| R18 | **R18 — N7 provenance definition location.** "**RULING: the phase-10 definition is canonical and is adopted into `packages/domain` as a first-class type:** `measured` (on site) · `derived` (computed from model/imagery/BOM geometry) · `estimated` (heuristic from capacity+location, incl. Path B AI fill) · `assumed` (catalog defaults without design). Energy additionally carries source labelling: "Real · PVGIS ({database})" vs "Built-in estimate ±10%" (R5)." | "**Consequence:** every user-visible number's type includes a provenance tier; the BOM's per-line confidence, the proposal's honesty labels, and the customer link's disclaimers all read from the same enum. No screen invents a fifth tier." | F8 (explicit in mapping) · BOM per-line confidence → M04/M11 · customer-link disclaimers → F5 · studio-derived numbers → M05 | Ambiguity ruled (verbatim): "Referenced everywhere, defined elsewhere." · Hard closure: exactly FOUR tiers — "No screen invents a fifth tier." · Binds to R5 for the energy source label. |

### 1a. R9 state table — reproduced verbatim (docs/15 lines 57–65)

Ruling text that governs this table: "**RULING: this table is the single definition.** All timers run as BullMQ repeatable jobs; all wake-ups land at 09:00 tenant-local time."

| key | State | Entry trigger | Timer / rule | Exit → target | Surfaced where | target_doc |
|---|---|---|---|---|---|---|
| R9.unassigned | Unassigned | Lead created, no owner | >24 h unassigned → escalate to owner (notification; state unchanged) | Assign → Assigned (stage New) | Lead inbox; owner dashboard "needs you" | M02 · escalation notification → F6 · owner dashboard "needs you" → M13 |
| R9.snoozed | Snoozed | User action + wake-up date (mandatory) | Hidden from My Day until wake date | Wake at 09:00 on date → prior stage + follow-up task; "Wake up now" manual | Lead detail chip "Snoozed till …" | M02 |
| R9.dormant | Dormant | 30 days with zero activity on any open stage | Nightly sweep flags; never deletes | Any activity → back to its stage; explicit Reopen | Leads filter; excluded from My Day | M02 |
| R9.disqualified | Disqualified | User action, reason mandatory (6 reasons) | None | Reopen (returns to prior stage, history kept) | Win/loss "disqualified early" list | M02 · win/loss list → M13 |
| R9.lost | Lost | Mark lost, reason mandatory (6 reasons) | Reason = postponed → auto-resurface on the given date; reason = not interested → no-call task suppressed 6 months | Reopen → prior stage | Win/loss "lost late" list | M02 · win/loss list → M13 |
| R9.junk | Junk | User action | Leaves all queues; never deleted | Reopen (rare) | Search only | M02 · search surface → F6 |
| R9.reopened | Reopened | Action on Lost/Disqualified/Dormant/Junk | — | Enters at prior funnel stage | Timeline records the reopen | M02 |

**Gap in source, not resolved:** the six mandatory reasons for Disqualified and for Lost are referenced ("6 reasons") but never enumerated in docs/15 — record in `registers/open-questions.md` unless another source lists them.

---

## 2. §3 — Design-system owner rulings (docs/15 lines 170–182, dated 2026-07-24, "final")

| key | verbatim ruling/directive | consequence (from source) | target_doc | notes |
|---|---|---|---|---|
| R19-CTX | §3 canon paragraph: "The canonical visual system is the vendored UX package at `design/ds-source/` (tokens/*.css, readme brand law, `_adherence.oxlintrc.json`, 21-component manifest, Geist fonts). The 22-point conflict list and its binding resolutions live in the DS reconciliation research; the owner ruled the four open items as follows. Agents implement these; they do not re-litigate them." | — (framing paragraph; no separate consequence line) | F3 | Counting discrepancy in the source, NOT resolved: the paragraph says "the owner ruled the **four** open items as follows" but **five** rulings follow (R19-A…R19-E) — R19-E is framed as a formal retirement recorded in the D3 row, which may be why it is not counted. Recorded as-is. · Source-gap: the DS reconciliation research (the 22-point conflict list) is missing from this repo — its binding resolutions are unavailable. · `design/` is read-only source (Global Constraint §1). |
| R19-A | "**R19-A — Light-only v1.** Dark mode is struck from the definition of done. The DS is light-only by law and by fact (`color-scheme:light`, zero dark tokens; the readme's "(+ dark mode)" index line is false). The semantic-alias indirection (`--bg-page`, `--surface-card`, `--text-body`, …) is kept so a dark value-set can drop in later. The old "studio canvas stays dark" doctrine is dead — the mockups show a light studio (`--canvas` `#F6F7F9` / `--canvas-sunken` `#EEF0F3` wells)." | — (no separate Consequence line in §3; the ruling states its own effects) | F3 · light studio canvas → M05 | Supersedes: the "studio canvas stays dark" doctrine **and** the DS readme's "(+ dark mode)" line (declared false). · Non-goal for v1: dark mode. · Any journey/census text describing a dark studio canvas is superseded here. |
| R19-B | "**R19-B — The overline is a NAMED EXCEPTION to the 12px floor (N3).** The signature overline micro-label — **11px / 700 / uppercase / 0.12em tracking** — is the single sanctioned sub-12px use. Micro-labels only; never body, data, or interactive text. N3 otherwise stands unchanged." | — (no separate Consequence line) | F3 | N3 = the 12px minimum type-size product law (defined in the POC DESIGN-SYSTEM / docs/10 layer, retained per D3 row as the interaction/a11y/product-law contract). Exactly ONE sanctioned exception. |
| R19-C | "**R19-C — AA-failing DS colours keep their exact hex but get RESTRICTED ROLES.** `--text-tertiary` `#A1A5AC` (~2.5:1 on white) = decorative/timestamps only, never load-bearing text — meaning-bearing overlines render in `--text-secondary` `#74787E`. `--warning` `#E9A23B` (~2.2:1 as bare text) always sits on its tinted chip `--warning-bg` `#FDF4E6`, never as bare foreground text. The N4 contrast build gate stays; the pairs file is regenerated from ds-source values with these role constraints encoded." | — (no separate Consequence line) | F3 | Deliberate, owner-accepted a11y trade-off recorded rather than fixed: AA-failing hexes are kept, constrained by role. N4 (contrast gate) stays. Interacts with R19-B: a meaning-bearing overline must use `--text-secondary`, not `--text-tertiary`. |
| R19-D | "**R19-D — Weight 600 is SANCTIONED.** `--fw-semibold:600` becomes a token (mockups use 600 ×210 on dense desktop screens); the sanctioned weight set is **400 / 500 / 600 / 700**. The readme's "500 restricted to buttons/tabs/table-headers" clause is dead (usage overrules it — 500 ×1213)." | — (no separate Consequence line) | F3 | Kills the DS readme's "500 restricted to buttons/tabs/table-headers" clause. · Precedent stated in the source: observed usage overruled written readme law. |
| R19-E | "**R19-E — "Instrument" is formally retired** (recorded in the D3 row above): graphite+brass is not the product's visual identity anywhere, for any tenant. Every visual fact now comes from ds-source; the POC DESIGN-SYSTEM.md survives only as the interaction/a11y/product-law contract layer." | — (no separate Consequence line; the D3 row in §2 carries the detail) | F3 · brass-centre FAB half of D31 → F3/M-navigation authors | Supersedes: **D3** ("Instrument" brand: graphite + brass) and the "brass centre" half of **D31** (per the §2 D31 row: centre FAB is near-black `--action-primary` `#0A0A0B` on the white arc). "No brass/graphite token survives anywhere." · Split of authority to carry: ds-source = every visual fact; POC DESIGN-SYSTEM.md = interaction/a11y/product-law contracts ONLY (touch targets, no-hover-only, provenance, states, focus visibility). · Cross-check with `d-census.md` D3/D31 rows. |

---

## 3. §4 — Owner directives (docs/15 lines 186–232, absorbed from the archived BLUEPRINT, 2026-07-30)

Source preamble (verbatim, binds how these rows are read): "The binding directives from the original planning session, preserved here because `BLUEPRINT.md` is archived. **Where a directive was later amended, the amendment is stated FIRST and the superseded wording is marked as such** — the archived file interleaved them, which let an agent quote stale law from above a correction."

| key | verbatim ruling/directive | consequence (from source) | target_doc | notes |
|---|---|---|---|---|
| OD-1 · backend-nestjs | "1. **Backend: NestJS.**" | — (no consequence stated in source) | registers/traceability.md (excluded: implementation) | Pure implementation (Global Constraint §14/DD4 bars it from PRD bodies). Record disposition `excluded`. Surfaces indirectly in R3 ("thin NestJS orchestrator"), R14 ("NestJS PowerSync write connector"). |
| OD-2 · fly-native-no-aws | "2. **Fly-native storage/services only initially — no AWS.** (ADR-0007/0008.)" | — (no consequence stated; ADR-0007/0008 named as the binding form) | registers/traceability.md (excluded: implementation) · data-residency residue → F1 | Product-visible residue only: hosting/residency claims (RBI localization is separately satisfied via Razorpay per R4; DPDP residency rules live in docs/08 → F2/F1). Named storage in UD-7: Tigris/Upstash. |
| OD-3 · mobile-bare-rn-no-expo | "3. **Mobile: bare React Native, NO Expo**, iOS + Android from day one. (ADR-0011.)" | — (no consequence stated; ADR-0011 named) | registers/traceability.md (excluded: implementation) · product commitment "iOS + Android from day one" → every module §2 Personas & surfaces | Split row: the stack half is implementation (excluded); the **surface commitment** (both platforms at launch, no phased platform rollout) is a product scope fact and must appear wherever surfaces are declared. |
| OD-4 · billing-in-v1 | "4. **Payments, subscriptions, billing, entitlements, usage tracking and gateway integration are IN the initial product** — supersedes D38. Trial-only, no free tier. (docs/16, ADR-0013.)" | — (no separate consequence line; "supersedes D38" is the stated effect) | M12 + 04-business-model · usage tracking of metered services → M12 · trial state UX → M12 | Supersedes: **D38** (and, with it, the "billing deferred" half of D11 and the "billing screens are mocks" D26 — see §2 rows). Duplicates/reinforces R4 and UD-7. · Pre-commitment carried in the §2 D38 row: "read + export always work regardless of billing state" — a P0 product law for M12. |
| OD-5 · timeline-superseded | "5. **Timeline: SUPERSEDED.** The original directive was "the entire product ships in ONE 20-day build". Reality overtook it — foundation work ran its own path and store accounts plus billable Fly infrastructure are owner-blocked (`docs/ops/company-registration-blockers.md`). What survives is the *track dependency structure*, not the calendar: docs/14. The scope commitment (no Launch-2, no "later" buckets) stands; the day numbers do not." | — (no separate consequence line) | registers/traceability.md (scope law binding every module §5 Non-goals) · blockers → registers/open-questions.md | **Critical cross-cutting correction:** every "ships in the 20-day build" phrase elsewhere (R6, R8, R11, D23 row, docs/14 Track/Day references) must be read as **"in v1 scope"** — the day numbers are dead. Track dependency structure survives. · Surviving product law: no Launch-2, no "later" buckets. · Owner-blocked items to register: store accounts, billable Fly infrastructure (`docs/ops/company-registration-blockers.md` — file not verified present in this repo). |
| OD-6 · scale-global-cleancode | "6. **Long-term scalability, global expansion, clean code, AI-assisted development, maintainability and production ops — without unnecessary complexity.**" | — (no consequence stated in source) | F1 (global expansion → market-neutral bodies + market packs) · remainder → registers/traceability.md (excluded: engineering principle) | The "global expansion" clause is the root of the market-neutrality Global Constraint (§6) and of the 2026-08-02 global-backend amendments in R2/R13/D36 and UD-9. "Without unnecessary complexity" is a standing counterweight to REC/enhancement inflation. |
| OD-7 · telephony-capability-framework | "7. **Telephony — AMENDED 2026-07-26; binding form is ADR-0019.** Telephony is a provider-agnostic **capability framework**: a capability-negotiated port family, a provider-agnostic call-control plane (warm/cold transfer with pinned AI context, tenant-configurable escalation chains, callback queues, defined degradation ladders), and routing policies as versioned tenant data. Corrections from spike S5, which are the operative facts: **BYO = inbound forwarding to the platform ExoPhone — outbound CLI is NOT portable**; **DTMF-send is a declared capability that Exotel AgentStream does NOT provide**, so IVR traversal degrades honestly until a capable adapter exists; **the 1600-series is closed to non-BFSI**, so promotional outbound uses the 140-series RTM route. Launch builds launch scope only; advanced capabilities are seams, not code." | "*SUPERSEDED wording from the original directive: "BYO = hosted/ported with KYC" and "`sendDtmf()`/`onDtmf()` over AgentStream" — both contradicted by S5. Do not implement.*" | M07 · number-series rules (1600 non-BFSI / 140-series RTM), BYO forwarding, DLT/TRAI ops → F1 · honest IVR degradation → F8 | Supersedes explicitly: "BYO = hosted/ported with KYC" and "`sendDtmf()`/`onDtmf()` over AgentStream" — **do not implement**. · Amends R3's vendor framing: vendors sit behind a capability-negotiated framework; capabilities are *declared* and may be absent. · Product-visible law: **IVR traversal degrades honestly** when DTMF-send is unavailable (honesty/provenance family → F8; cross-check UXG-18 IVR-traversal markers). · "Launch builds launch scope only; advanced capabilities are seams, not code" → keep advanced call-control out of v1 requirements. |
| OD-8 · no-feature-flags | "8. **No feature-flag system.** Features ship enabled when merged; no flag infrastructure, no dark launches. The ONLY runtime gating is billing entitlements. Incomplete work does not merge." | — (no consequence stated in source) | M12 (entitlements are the only runtime gate) · remainder → registers/traceability.md (excluded: delivery process) | Product-visible law worth carrying: a user's feature availability is determined **only** by billing entitlements — no per-tenant feature toggles, no beta flags, no dark launches. Constrains any requirement that assumes a toggle. |
| OD-9 · studio-is-flagship | "9. **The 3D Design Studio is THE flagship — nothing is compromised against it.** The tool census is the binding acceptance checklist for the port: every tool and every computed output survives, refactored to the design system, touch-first. The scale program is investment INTO the studio moat, never a reason to cut studio capability. Mobile presents the full-parity touch studio through an authenticated WebView; no studio feature is dropped on any surface. **Canonical census: `prd/modules/M05-studio/studio-census.md`** (promoted 2026-07-30; the POC's `phase-10-prompts.md` is NOT canonical). " | — (no separate consequence line; named as binding form) | M05 · docs/11 scale program → M05 · WebView/full-parity surface statement → M05 §2 | Canon correction, binding on other ledger files: **`prd/modules/M05-studio/studio-census.md` is canonical; `phase-10-prompts.md` is NOT** — any phase-10-prompts citation is superseded for census purposes (note that R5/R7/R12 still cite "the phase-10 census" for substance; the *canonical artifact* is now studio-census.md). · Reinforces Global Constraint §3.2: the census never shrinks. · "No studio feature is dropped on any surface" = full mobile parity at 375px (D2). |
| OD-10 · studio-and-offline-last | "10. **Studio port and the offline layer are the LAST build phases.** The studio moves last because a validated implementation already exists in the POC (re-honouring D23); offline moves last because nothing at launch depends on it. This is a move in TIME only — never in scope or quality: the census gate, DS-refactor targets and scale program are unchanged. Until offline lands, mobile is online-first behind a repository abstraction." | — (no consequence stated in source) | M05 + F4 · build-ordering half → registers/traceability.md (excluded: build sequencing per §14/DD4) | Sequencing is barred from PRD bodies; the surviving product facts are (a) **no scope or quality reduction** for studio or offline, and (b) the interim state "mobile is online-first" until offline lands — which qualifies R14's boundary as a target state. · Re-honours D23 (studio lowest build priority) after the same-day "studio-first" reversal recorded in the §2 D23 row. |

---

## 4. R20 — Auth removed to greenfield (docs/15 lines 234–259, 2026-08-01)

| key | verbatim ruling/directive | consequence (from source) | target_doc | notes |
|---|---|---|---|---|
| R20 | "**R20 — Auth removed to greenfield, overriding append-only migrations (2026-08-01)**<br>**Asked:** how far "remove all backend integration" reaches, given that migrations are append-only by hard rule (CLAUDE.md §6, enforced by a sha256 lock in the runner and a CI diff check), and that the identity spine cannot be removed surgically — `files`, `audit_log`, `usage_events`, `tenant_phone_numbers` and `sync_mutations` all carry `tenant_id` foreign keys to `tenants`, and `files.uploaded_by` references `users`.<br>**Ruled:** full database reset. Delete migrations `0001`–`0006` and the entire Drizzle schema; `packages/db` keeps only `client.ts`, `migrate.ts`, `uuid.ts`. The auth + tenancy module authors a fresh `0001` when its slice begins (Law 9), reading the `auth/tenancy` row of `docs/forward-compat.md` first.<br>**Not precedent.** This is the only sanctioned override of the append-only rule. A future schema change adds a new numbered file. Detail: the auth teardown." | "**Cost, accepted knowingly:** all four invariants have nothing left to compare. They now report that out loud — the runner prints `INVARIANTS VACUOUS: 0 application tables` and each invariant states what it did and did not verify — so a green run can never again be read as "tenancy is proven". `oasdiff` reports 9 breaking API removals; the enforced freshness half of `check:openapi` passes. Both login flows run on a deliberate walkthrough stub." | registers/traceability.md (excluded: implementation) · product-visible residue (auth/tenancy is unbuilt; both login flows are stubs) → F2 | **Renumbering (verbatim, docs/15 lines 236–239):** "**Renumbered 2026-08-03.** This ruling was authored as "R19", colliding with the design-system rulings R19-A…E above. Ids are stable and never reused (`.claude/rules/CLAUDE.md` §2), so the LATER ruling moved to the next free number. Citations of "docs/15 R19" written before this date and referring to the auth teardown mean R20." — **any other source citing "R19" for the auth teardown must be read as R20; "R19" for visual/DS matters means R19-A…E.** · Entirely implementation (migrations, schema, packages) — barred from PRD bodies by §14/DD4; recorded here for traceability completeness. · Product-visible residue: "Both login flows run on a deliberate walkthrough stub" — auth (Mobile OTP + Google Login, Global Constraint §6) exists as requirement in F2, not as built behaviour. · Confirms `docs/forward-compat.md` `auth/tenancy` row as a required input for F2 authors. |

---

## 5. Earlier owner decisions (still binding) — docs/15 line 261+

Source heading verbatim: "### Earlier owner decisions (still binding)". The paragraph is a single `·`-separated list; each clause is keyed below in document order.

| key | verbatim ruling/directive | consequence (from source) | target_doc | notes |
|---|---|---|---|---|
| EOD-1 · thin-test-net | "Thin test safety net only — strict typecheck/lint gates plus the locked invariant set; no routine unit-test authoring (**reaffirmed and hardened 2026-07-29: no `.test.*`/`.spec.*` files at all until a testing program is explicitly commissioned**)" | — (no consequence stated in source) | registers/traceability.md (excluded: engineering process) | Hardened 2026-07-29 (post-overlay state recorded): from "no routine unit-test authoring" to "**no `.test.*`/`.spec.*` files at all**" until a testing program is commissioned. · Interacts with R20's "INVARIANTS VACUOUS" cost. · Not product content, but it constrains what "acceptance criteria" can assume about automated verification. |
| EOD-2 · pricing-benchmark | "benchmark pricing below Reslink/ARKA/Aurora with healthy margin (final tiers: docs/01)" | — (no consequence stated; final tiers named as docs/01) | 04-business-model (+ M12 for entitlement tiers) | Pricing position is competitive-relative; final tiers live in docs/01 → cross-check `docs-rules.md` DOC01.* and `competitive-verdicts.md`. |
| EOD-3 · design-range | "1 kW → 100 MW design range with a credible scale path" | — (no consequence stated in source) | M05 (docs/11 scale program → M05) · segment range also cited in the §2 D1 row (docs/11) | Same range appears in the §2 D1 row: "1 kW→100 MW range (docs/11)" — consistent, no contradiction. |
| EOD-4 · gaps-first-class | "competitive gap analysis first-class, UX gaps registered for implementation-time design" | — (no consequence stated in source) | registers/enhancements.md + registers/open-questions.md · UX gaps → `ux-gaps.md` ledger (UXG-01…27) · competitive gaps → `competitive-verdicts.md` (CG-1…19) | This is the authority for keeping UXG items as *registered design-time gaps* rather than dropping them, and for treating docs/12 verdicts as first-class PRD input. |
| EOD-5 · two-tier-catalog | "two-tier catalog (platform master + tenant own + tenant overrides; resolution: tenant-override → tenant-item → platform-item)" | — (no consequence stated in source) | M01 | Identical in substance to R13 and to UD-4; three independent statements agree — no contradiction. |
| EOD-6 · consistency-over-cleverness | "consistency and stability over cleverness" | — (no consequence stated in source) | registers/traceability.md (product principle; no single owner doc) · nearest home F3 (design/interaction consistency) | Principle with no natural module home; recorded so it is not lost. Useful as the tie-breaker when a REC proposes novelty. |
| EOD-7 · agent-rules-top-deliverable | "**the AI-agent rules are the most important deliverable**" | — (no consequence stated in source) | registers/traceability.md (excluded: development process) — **ambiguous**, see notes | **Ambiguity NOT resolved:** "AI-agent rules" may mean (a) the repo's rules governing AI-assisted development (reading with OD-6's "AI-assisted development" clause and the `.claude/rules/CLAUDE.md` citation in R20), or (b) the voice-agent behaviour rules (→ M07, reading with D10/D24/D36/R10). Both readings are supported by nearby text; recorded for the owner to disambiguate → `registers/open-questions.md`. |

---

## 6. User decisions log — docs/15 line 272+

Source heading verbatim: "### User decisions log". Single `·`-separated paragraph; each clause keyed in document order.

| key | verbatim ruling/directive | consequence (from source) | target_doc | notes |
|---|---|---|---|---|
| UD-1 | "Docs home `/Volumes/works-space/heliogrid`" | — (no consequence stated in source) | registers/traceability.md (excluded: process/environment) | **Contradiction with this workspace, recorded not resolved:** the stated docs home is `/Volumes/works-space/heliogrid`, but this PRD project reads sources from `/Volumes/works-space/heliogrid_v2_prd/docs/`. Whether v2_prd is a copy or a successor is unstated. |
| UD-2 | "competitors ARKA 360, Aurora, OpenSolar, Reslink" | — (no consequence stated in source) | 04-business-model · verdict detail → `competitive-verdicts.md` (docs/12) | Four named competitors; docs/12 additionally carries the Reslink tier-coverage table and the moat list. Consistent with EOD-2's pricing benchmark set (Reslink/ARKA/Aurora — OpenSolar is not in the pricing benchmark, only in the competitor set; recorded, not reconciled). |
| UD-3 | "tests: thin safety net" | — (no consequence stated in source) | registers/traceability.md (excluded: engineering process) | Same decision as EOD-1 (which carries the 2026-07-29 hardening). Read EOD-1 as the live form. |
| UD-4 | "two-tier catalog: yes" | — (no consequence stated in source) | M01 | Confirms R13 / EOD-5. |
| UD-5 | "pricing: trial-only, no free tier" | — (no consequence stated in source) | 04-business-model + M12 | Confirms R4 / OD-4. Product law: a trial exists; a permanently free tier does not. |
| UD-6 | "voice v1: Exotel + Sarvam" | — (no consequence stated in source) | M07 (+ F1 for the IN rails) | Confirms R3; must still be expressed as capability requirements with v1 vendor as reference implementation (Global Constraint §6), and read under OD-7's capability framework. |
| UD-7 | "final review: NestJS, Fly-native storage (Tigris/Upstash, no AWS), bare React Native (no Expo), billing/entitlements/payments in v1" | — (no consequence stated in source) | billing/entitlements/payments half → M12 + 04-business-model · stack half → registers/traceability.md (excluded: implementation) | Restates OD-1/OD-2/OD-3/OD-4 as the "final review" decision set; names the concrete Fly-native services (Tigris object storage, Upstash) which OD-2 leaves unnamed. |
| UD-8 | "DB: Fly unmanaged postgres-flex (owner choice; deprecation risk accepted with mandatory mitigations and documented escape hatches)" | — (no consequence stated in source) | registers/traceability.md (excluded: implementation) · accepted risk → registers/open-questions.md | Knowingly accepted risk with "mandatory mitigations and documented escape hatches" — the mitigations/hatches are not enumerated in docs/15 (source gap). |
| UD-9 | "global backend 2026-08-02: global-ready with India-only launch; one currency per tenant; global-safe billing schema, India-only rails; target regions Gulf/MENA, SEA/Africa, EU/UK/AU, US (global-backend ruling, 2026-08-02)." | — (no consequence stated in source; this is the authority behind the 2026-08-02 amendments to R2, R13 and D36) | F1 (market packs + India-only launch) · one currency per tenant + global-safe billing schema → M12 + 04-business-model · market-neutral module bodies → all modules (Global Constraint §6) | **Latest-dated ruling in the file (2026-08-02) and the source of the R2 / R13 / D36 amendments** — it is the authority for market-neutral enums, scheme-keyed certifications and the per-market ComplianceGate ruleset. · Product laws to carry: launch is **India-only** despite global-ready structure; **one currency per tenant** (binds R6's tenant-set OTP threshold "denominated in the tenant's currency"); billing schema global-safe but rails India-only (R4's supplier-of-record question stays owner-blocked). · Target regions named: Gulf/MENA, SEA/Africa, EU/UK/AU, US — future-facing, not v1 scope (OD-5's "no later buckets" applies to *v1 features*, not to these named expansion regions). |
