# Extraction ledger — Tenant configuration, agent performance, billing (deferred-era), multilingual

**Source:** `docs/product/product-journey.md` L1165–1413 — § "TENANT CONFIGURATION — everything an EPC can make their own" (L1165–1285), § "AGENT PERFORMANCE — proving the voice agent is worth keeping" (L1289–1352), § "BILLING & SUBSCRIPTION — DEFERRED, planned separately (D38)" (L1355–1362), § "MULTILINGUAL — English · Hindi · Marathi (D25)" (L1366–1410). The next section, § "ROLES & PERMISSIONS" (L1414+), is outside this file's range (roles matrix → F2 per the brief's mapping).
**Overlay applied:** `docs/15-spec-resolutions.md` read in full first — §1 rulings R1, R3, R4, R10, R12, R13; §2 D-rows D3 (with §3 R19-A…E), D10, D13, D18, D20, D24, D25, D26, D27/D28/D29, D32, D34, D36 (PARTIAL, AMENDED 2026-08-02), D37, D38 (SUPERSEDED); §4 owner directives 4, 7, 8; global-backend ruling 2026-08-02.
**Reading rule:** every row records the POST-overlay state. Superseded source wording is quoted and ~~struck~~ with the superseding authority named; where source and overlay pull against each other the tension is **recorded, not resolved**.
**Columns:** `key` · `content` · `target_doc` · `notes`. Default mapping per the brief: tenant configuration → **M01** · agent performance → **M07 + M13** · billing → **M12 / 04-business-model** · multilingual → **F3**. Cross-referenced docs in parentheses, reason stated in the cell.

## 1 · Tenant configuration — principle (TC.principle.*)

| key | content | target_doc | notes |
|---|---|---|---|
| TC.principle.1 | **Config is a first-class product surface** (L1165–1169). "Every company on the platform is different: different brands, different warranties, different pitch, different service area. **Configuration is a first-class product surface, not a settings dumping ground.**" | M01 | Frames every row below. |
| TC.principle.2 | **The governing principle (D36, post-overlay)** (L1171–1176, L1185–1186). Source: "The **agent is fully the tenant's** — nothing about it is locked. The app ships **safe, India-legal defaults** and ~~the owner can change or switch off any of them~~; ~~the tenant owns compliance~~. Config is kept **simple**: everything pre-filled, plain questions, plus a free-text box so they're never boxed in." … "The owner decides how their agent talks, what it discusses (including price), and when it calls. ~~They own the responsibility; we surface it once, plainly, not as a wall.~~" POST-OVERLAY (docs/15 §2 D36 PARTIAL, AMENDED 2026-08-02): config is fully tenant-owned, **but the statutory floor is ENFORCED by the non-swappable `ComplianceGate`, not merely surfaced** — the gate's MECHANISM is non-swappable; its statutory RULESET is per-market pack data (IN = TRAI/DLT: DND scrub, 9am–9pm, DLT series, opt-out, recording retention). "Tenants configure within the law, not around it"; a market with no voice ruleset cannot enable outbound voice. | M01 (+M07, +F1) | The struck clauses are exactly what the overlay kills. Above-floor items (tone, price talk, hand-over shape, a narrower-than-floor schedule) stay tenant-editable. |
| TC.principle.3 | **The five shipped safe defaults** (L1178–1183): the agent introduces itself as an automated assistant *(default; editable)* · "Talk to a person" is offered *(default on; the owner shapes hand-over)* · hand-over on price questions, an angry customer, "stop" *(default; add / edit / remove)* · calls run 9am–9pm and skip DND numbers ~~*(default; the owner can change it)*~~ · recording consent captured *(default; a customer may decline and still be served)*. POST-OVERLAY: the 9am–9pm window, DND skip and "stop"/opt-out are IN statutory-ruleset items enforced by `ComplianceGate` — no longer owner-switchable; the remaining defaults stay tenant-editable per D36. | M01 (+M07, +F1) | Recording retention is also in the IN ruleset; the consent-capture *default* itself is not named as statutory — recorded, not resolved. |
| TC.principle.4 | **Platform-behaviour carve-out (N7 aside)** (L1188–1190): "*Separate from the agent, and still platform behaviour: the product's own number-honesty — provenance labels, and estimates never printed as calculations — because that governs the proposal and design **output**, not the agent's speech. See N7.*" Number-honesty is never a tenant config surface. | M01 (+F8) | R18 makes the provenance enum canonical; no screen invents a fifth tier. |

## 2 · A — Voice agent configuration (TC.agent-setup.*)

| key | content | target_doc | notes |
|---|---|---|---|
| TC.agent-setup.1 | **Design approach** (L1196–1198): "a solar business owner shouldn't have to write a prompt, so the default is **guided questions with everything pre-filled** — plus a free-text box for anything else, so they're never limited. Simple by default, open when they want more." | M01 (+M07) | D24's guided-config half is HONORED; its "locked by platform" half is superseded by D36. |
| TC.agent-setup.2 | **Screen: Agent setup — guided** (L1202): plain-language steps, all pre-filled — name · voice · languages · tone · opening line · what to say when it doesn't know · when to hand to a human · when it may call (hours, days, holidays) — plus a free-text "anything else". ~~"Everything editable; nothing locked."~~ POST-OVERLAY: everything editable *within* the `ComplianceGate` statutory floor (D36 amended). | M01 (+M07) | Agent languages (6) are independent of UI languages (3) per R3 — the sets never converge by accident. |
| TC.agent-setup.3 | **Screen: Opening line** (L1203): pre-filled with an AI disclosure — "Namaste, this is *Asha* from *Suryodaya Solar*. I'm an automated assistant — is now a good time?" — that the owner can keep or change. "All of it is theirs." | M01 (+M07) | The disclosure default is tenant-editable as written (it is not in D36's listed IN statutory ruleset). TENSION recorded, not resolved: docs/12 CG-moat.1 names "disclosure ≤30 s" *inside* `ComplianceGate`. |
| TC.agent-setup.4 | **Screen: Hand-over rules** (L1204): a list the owner edits, adds to or removes — price questions · angry customer · asks for the owner · a question it can't answer · asks to stop — each with what the agent says as it hands over. "Sensible defaults, none forced." POST-OVERLAY: "asks to stop" is the statutory opt-out (enforced by `ComplianceGate`); the rest stays owner-shaped. | M01 (+M07) | "Talk to a person" ships default-on (C7 family). |
| TC.agent-setup.5 | **Screen: Calling window** (L1205): days, hours, holiday calendar. "Defaults to 9am–9pm and skips DND; ~~the owner can change any of it (and owns that choice — D36)~~." POST-OVERLAY: in a market with a statutory voice ruleset the window and DND scrub are floor-enforced; the owner configures only within the law (narrower windows, holidays). | M01 (+M07, +F1) | IN ruleset = F1 market-pack data; M07 references pack keys per Global Constraints §6. |
| TC.agent-setup.6 | **Screen: Test the agent** (L1206): "**The most important screen here.** Call yourself, or run a typed conversation. Hear exactly what a customer hears, before anyone else does." | M01 (+M07) | — |
| TC.agent-setup.7 | **Screen: Change history** (L1207): "Kept quietly in the background — each call records which settings answered it, so a dispute is answerable. Not a chore the owner manages." | M01 (+M07) | Matches the D18 overlay addition (agent-config version per call) and R10's auditability (config version + KB version). |

## 3 · B — Business knowledge base (TC.kb.*)

| key | content | target_doc | notes |
|---|---|---|---|
| TC.kb.1 | **Structured, not an upload** (L1211): "Not a document upload. A **structured, reviewable knowledge base** in the owner's own words." Eight sections (TC.kb.2–9). | M01 (+M07) | R10: the KB is the only agent-behaviour input besides versioned config; it changes only by explicit owner promotion — no auto-training, ever in v1. |
| TC.kb.2 | KB section **About us** (L1215): years in business, installations completed, certifications, service area. | M01 (+M07) | — |
| TC.kb.3 | KB section **Products** (L1216): panel brands offered, inverter brands, why we chose them. | M01 (+M07) | Brand/model names are never translated (MULTI.4). |
| TC.kb.4 | KB section **Warranty** (L1217): panel 25yr performance / 12yr product, inverter 5yr, workmanship 2yr. | M01 (+M07) | Example values, not platform defaults. |
| TC.kb.5 | KB section **Process & timeline** (L1218): survey in 2 days · design in 3 · install 1–2 days · net-metering 3–6 weeks. | M01 (+M07) | — |
| TC.kb.6 | KB section **Pricing & offers** (L1219): "What's included, what's extra, and whatever the owner wants the agent to say about price — their call (D36)." | M01 (+M07) | D10's "may never discuss discounts" was superseded by D36 — price talk is owner-configurable, above the statutory floor. |
| TC.kb.7 | KB section **Subsidy** (L1220): how PM Surya Ghar works, who qualifies, who applies, typical timeline. | M01 (+M07, +F1) | PM Surya Ghar is IN market-pack colour; the KB section itself is market-neutral. |
| TC.kb.8 | KB section **Financing** (L1221): which banks/NBFCs, typical EMI, documents needed. | M01 (+M07) | Bank/NBFC lists are F1 pack colour (cf. CG-4). |
| TC.kb.9 | KB section **Common objections** (L1222): "Too expensive" · "I'll wait for prices to drop" · "Does it work in monsoon?" · "What about cleaning?" — with the answer the owner wants given. | M01 (+M07) | — |
| TC.kb.10 | **The unanswered-questions loop** (L1224–1231): "When a customer asks something the agent could not answer, it is captured as an **unanswered question**. The owner sees a short list: *'3 customers asked about hail damage this week.'* One tap to answer it, and the agent knows it from the next call." "The knowledge base grows from real calls instead of a blank page. This is the difference between a config screen people fill in once and abandon, and one that gets better weekly." | M01 (+M07) | R10 pins the mechanism: review-queue only; nothing a rep types reaches agent behaviour without explicit owner promotion. Same list surfaces in AP.screen.2. |
| TC.kb.11 | **Seeded, not empty** (L1233–1235): "Every new tenant starts with a solar-industry default pack — generic but correct answers for subsidy, warranty, monsoon, cleaning, net metering. The owner reviews and personalises. Day one it works; week four it sounds like them." | M01 (+M07, +F1) | The seed pack's subsidy/net-metering content is market-pack material for IN. |

## 4 · C — Everything else configurable per tenant (TC.<area>.*)

| key | content | target_doc | notes |
|---|---|---|---|
| TC.branding.1 | **Branding** (L1241): logo, letterhead, colours on customer documents, company details. | M01 (+F7) | Post-overlay the visual base is `design/ds-source` (D3 SUPERSEDED, R19-A…E); tenant branding rides on it, for customer documents. |
| TC.catalog.1 | **Catalog** (L1242, duplicated at L1244): "Which panels/inverters/BOS this company actually sells — the list step 8 picks from." NOTE: the source table lists **Catalog twice** (L1242 and L1244, the second without the step-8 clause). POST-OVERLAY R13: the two-tier catalog resolves it — platform master catalog + tenant own catalog + tenant overrides, resolution order tenant-override → tenant-item → platform-item; "the duplicate row in the old spec table is void"; the settings screen shows ONE catalog with a rates panel. | M01 | R13 AMENDED 2026-08-02: ALMM/DCR are the IN entries of a scheme-keyed `certifications` structure; the market pack declares which schemes a market requires. |
| TC.pricebook.1 | **Price book** (L1243): "Rates per component, versioned so old quotes keep their prices." POST-OVERLAY R13: catalog rate history is the versioned rate history on tenant items/overrides; `price_book_versions` is a separate structure for non-catalog rates; "sent proposals keep the rate version they were built with." | M01 | R1 vocabulary correction required downstream: "old quotes" must be written *proposals* in the PRD (quote/quotation banned from UI strings and identifiers; search aliases excepted). |
| TC.templates.1 | **Proposal templates** (L1245): cover, sections included, default T&C, bank details. | M01 (+M06) | Document is named "Proposal" in all three locales (R1). |
| TC.payment-terms.1 | **Payment terms** (L1246): "Named tranche templates — 10/60/20/10, 30/60/10." | M01 (+M11) | Tranche templates feed the money path; tranche `due_on_stage` maps to the 9-stage chain (R2). |
| TC.timeline.1 | **Project timeline** (L1247): default phases and descriptions (Stage 6B, step 6). | M01 (+M06) | Cross-ref the S6B ledger rows in `journey-stages.md`. |
| TC.discount-limits.1 | **Discount limits** (L1248): "Not in this release — no approval, no ceiling (D34). Reserved for when a tenant asks for per-rep limits." | M01 (§Non-goals) | D34 HONORED post-overlay — the only guard is arithmetic at Generate: payable ≤ ₹0 blocks, below-cost warns (R12). |
| TC.lead-sources.1 | **Lead sources** (L1249): "Which channels are live." | M01 (+M02) | D13 HONORED: v1 sources = manual, CSV, inbound voice; website/WhatsApp remain "later" cards (UXG-03). |
| TC.roles.1 | **Roles** (L1250): "Who sees what, who approves what." POST-OVERLAY: six fixed presets, OR-across-roles, no custom roles and no per-person exceptions (D27/D28/D29 HONORED) — so the v1 config surface is role *assignment* (stacking presets), never a role editor. | M01 (+F2) | "Who approves what" recorded as written; tension held, not resolved: no discount-approval flow exists (D34). The roles matrix itself (L1414+) is F2's ledger material, outside this file. |
| TC.message-templates.1 | **Message templates** (L1251): "WhatsApp proposal message, follow-up nudge, reminder." POST-OVERLAY D32: templates are composed by the product and manually copy-pasted by the rep (`MessagingPort` ManualCopyAdapter) — the app never sends on the customer's behalf in v1. | M01 (+M06, +F6) | Template translation is F3 material (MULTI.4: "Notifications and WhatsApp templates" are translated). |

## 5 · D — Making configuration not feel like work (TC.config-ux.*)

| key | content | target_doc | notes |
|---|---|---|---|
| TC.config-ux.1 | **Nothing is required on day one** (L1255–1256): "Every setting has a working default. A tenant can sign up and send a real proposal without opening settings once." | M01 | Pairs with TC.wrong.6 (zero-config fallback). |
| TC.config-ux.2 | **Configure in context, not in a settings maze** (L1257–1259): "The moment a rep needs a component product they do not stock, offer to add it to the catalog *there*. Settings screens exist for revisiting, not for setup." | M01 (+M05) | In-context catalog add crosses into the builder/studio surfaces. |
| TC.config-ux.3 | **Show the effect** (L1260–1262): "Every config screen shows a live preview — the proposal with your logo, the agent's opening line spoken aloud, the payment tranches as the customer sees them." | M01 | Pairs with TC.wrong.7 (preview catches tone/knowledge mismatch). |
| TC.config-ux.4 | **One "Business profile" screen that feeds many places** (L1263–1265): "Company name, logo, address and GSTIN are asked once and used by the proposal, the agent's script, the customer link and the invoice." | M01 (+F1) | GSTIN is IN-pack colour. "The invoice" is now a real surface post-overlay (billing in v1, D38 superseded). |

## 6 · Tenant configuration — what goes wrong (TC.wrong.*)

| key | content | target_doc | notes |
|---|---|---|---|
| TC.wrong.1 | "**Owner sets the agent to do something legally risky** (calls outside hours, edits out the disclosure) → ~~allowed (D36); the one honest note has already told them they own that choice~~" (L1268–1269). POST-OVERLAY: SUPERSEDED for statutory items — calls outside the statutory window (and DND/opt-out violations) are BLOCKED by `ComplianceGate`; "Tenants configure within the law, not around it." Editing the disclosure is not in D36's listed IN ruleset and stays owner-editable as written. | M01 (+M07, +F1) | Same disclosure tension as TC.agent-setup.3 (docs/12 CG-moat.1 puts "disclosure ≤30 s" inside the gate) — recorded, not resolved. |
| TC.wrong.2 | "**Knowledge base contradicts itself** (two different warranty answers) → flagged on save" (L1270). | M01 (+M07) | — |
| TC.wrong.3 | "**Agent config changed mid-campaign** → versioned; calls already scheduled use the version they were queued with, and the owner is told" (L1271–1272). | M01 (+M07) | Config version per call = D18 overlay + R10 auditability. |
| TC.wrong.4 | "**Tenant removes a catalog product still used by a draft proposal** → the draft keeps its components; the product is archived, not destroyed" (L1273–1274). | M01 (+M06) | Archive-never-delete is also a docs/04 product rule (see `docs-rules` ledger). |
| TC.wrong.5 | "**Price book updated after quotes were sent** → sent quotes keep original prices, always" (L1275). | M01 (+M06) | R13 restates this as canon (sent proposals keep their rate version). R1 vocabulary: *proposals*, not quotes. |
| TC.wrong.6 | "**A tenant with no config at all** → everything falls back to platform defaults and nothing breaks" (L1276–1277). | M01 | — |
| TC.wrong.7 | "**Agent's tone set to 'Direct' but the knowledge is verbose** → preview shows the mismatch before it goes live" (L1278–1279). | M01 (+M07) | Depends on TC.config-ux.3 and TC.agent-setup.6. |

## 7 · Tenant configuration — recommendation (TC.rec.*)

| key | content | target_doc | notes |
|---|---|---|---|
| TC.rec.1 | **Build the unanswered-questions loop early, even before the agent is live** (L1281–1285): "It is the mechanism that keeps the whole system honest and improving, and it costs almost nothing: capture what the agent couldn't handle, show the owner, make answering it one tap. Without it, every tenant's agent decays into a script nobody maintains." | M01 (+M07) | Source recommendation (SRC, not REC-class enhancement — it is in the journey). R10 is the pinned mechanism. |

## 8 · Agent performance (AP.*)

| key | content | target_doc | notes |
|---|---|---|---|
| AP.retention.1 | **The retention problem** (L1291–1293): "An owner paying for automated calls who cannot see what they bought will cancel within a month. This dashboard is not analytics garnish; it is the reason the agent survives its first invoice." | M07 (+M13) | "Its first invoice" is literal post-overlay — billing is in v1 (D38 SUPERSEDED). |
| AP.dashboard.1 | **Screen: Agent performance — the monthly block** (L1297–1315). THIS MONTH vs last month: calls attempted (412, ↑18%) · connected (246, 60%, ↑4%) · callbacks booked (38) · site visits booked (17) · handed to a human (29) · questions it could not answer (11 → review). OUTCOMES: Interested 94 · Not interested 61 · Callback requested 38 · No answer 166 · Asked to stop 9. WHAT IT SAVED YOU: "246 conversations your team did not have to start · ≈ 20 hours of calling time". | M07 (+M13) | Numbers are the source's illustrative sample, not targets. Dashboards are honest periodic decision tools (D37 HONORED — read-only). |
| AP.dashboard.2 | **"Deals it touched"** (L1317–1321): "31 proposals were quiet, the agent called, and the customer responded within 3 days · ₹ 1.4 Cr of pipeline in those deals" — marked "(see note)" pointing at the honesty rule (AP.honesty.1). | M07 (+M13) | ₹/Cr formatting is IN-pack number format (global-backend ruling). |
| AP.honesty.1 | **The honesty rule: correlation, not attribution — stated on the screen** (L1323–1333): "**'Deals it touched' is correlation, not attribution — and the screen must say so.**" On-screen caption: "*The agent called and the customer responded within 3 days. We cannot prove the call caused it.*" "Claiming an agent 'generated ₹1.4 Cr' when it made one follow-up call is exactly the dishonesty this product exists to avoid — and an owner who catches you inflating it stops trusting every other number you show them. Being the one product that states the limit is consistent with everything else here (N7, D21)." | M07 (+M13, +F8) | D37 HONORED: "correlation-not-attribution" is platform law; docs/12 CG-moat.2 cites the same rule. |
| AP.screen.1 | **Supporting screen: Call log** (L1338): every call — customer, duration, outcome, language, which config version, transcript, recording. Filterable. | M07 (+M13) | D18 HONORED: outcome + summary + interest signal, transcript/recording on tap, plus agent-config version per call and IVR-traversal markers (UXG-18). |
| AP.screen.2 | **Supporting screen: Unanswered questions** (L1339): "The list from D24 — what customers asked that the agent could not handle. One tap to answer. **This is where the dashboard turns into improvement.**" | M07 (+M13) | Same loop as TC.kb.10; R10: promotion is explicit, owner-only. |
| AP.screen.3 | **Supporting screen: Usage** (L1340): "Calls made and minutes used this period — a plain activity stat, ~~**no plan cap or limit** (billing deferred, D38)~~." POST-OVERLAY: D38 is **SUPERSEDED** — usage tracking and entitlements ARE in v1; agent minutes are metered to the tenant usage ledger (R3: ≈₹2.5–4/min all-in) and billing entitlements are the ONLY runtime gating (§4 directive 8). The Usage screen reads the real usage ledger; whether a cap applies is docs/16 entitlement data, no longer "none by design". | M07 (+M12, +M13) | The struck clause is the deferred-era claim; do not carry it into any PRD as live. |
| AP.screen.4 | **Supporting screen: Per-rep view** (L1341): "Which reps lean on the agent, whose leads it rescued. Manager-only." | M07 (+M13) | Visibility per D20 role scoping (F2 matrix). |
| AP.wrong.1 | "**Connect rate collapses** (wrong numbers, bad timing) → surfaced as a warning with the likely cause, not left for the owner to notice" (L1344–1345). | M07 (+M13) | — |
| AP.wrong.2 | "**Agent escalating almost everything** → its knowledge is too thin; link straight to the unanswered-questions list" (L1346–1347). | M07 (+M13) | — |
| AP.wrong.3 | "**Owner sees a big 'deals touched' number and over-trusts it** → the caveat is on the screen, not in a tooltip" (L1348–1349). | M07 (+M13, +F8) | Acceptance-level restatement of AP.honesty.1. |
| AP.wrong.4 | "**Nobody opens this screen** → a monthly summary is pushed to the owner in-app, where they actually read things" (L1350–1351). | M07 (+M13, +F6) | In-app push is within the v1 notification scope (push + in-app, staff surfaces). |

## 9 · Billing & subscription — deferred-era section, post-overlay (BILL.*)

| key | content | target_doc | notes |
|---|---|---|---|
| BILL.1 | **The deferred-era ruling, superseded whole** (L1355–1362). Source: ~~"**Billing and subscription are OUT of the current plan.** No billing screen, and — the point — **no subscription restriction gates any feature**: no plan/usage limits, no trial-expiry or suspension read-only, no seat caps, no PRO-tier locks, no 'upgrade' prompts. Every user can use every built feature. The pricing model and the whole billing/subscription surface will be designed in a **separate plan** (D38)."~~ POST-OVERLAY: **D38 is SUPERSEDED** (product-owner override 2026-07-24; docs/15 §2 D38 + §4 directive 4) — payments, subscriptions, billing, entitlements, usage tracking and gateway integration are **IN v1**: Razorpay both roles (R4), **trial-only, no free tier** (docs/16); billing entitlements are the ONLY runtime gating (§4 directive 8); D26's mock billing screens are redesigned as real (UXG-13). | M12 (+04-business-model) | This section predates the overlay; nothing in the struck text survives as scope. Recorded so the section can never be quoted as live law. |
| BILL.2 | **The surviving pre-commitment** (L1361–1362): "when that lands, one principle is pre-committed: **never hold a customer's data hostage** (read + export always work, likely required under DPDP)." The overlay keeps it verbatim in the D38 row: "read + export always work regardless of billing state." | M12 (+04-business-model) | The soft-block law for suspended/expired tenants; DPDP tie-in is docs/08 / F2 material. |

## 10 · Multilingual — EN/HI/MR (MULTI.*)

| key | content | target_doc | notes |
|---|---|---|---|
| MULTI.1 | **The font — a design-system change, not a translation task** (L1368–1373): "Our design system specifies ~~**Inter**~~, which has no Devanagari coverage. Hindi and Marathi both need it. Pair ~~Inter~~ with **Noto Sans Devanagari**, matched for optical size and weight, or Devanagari text will render in a system fallback that looks broken beside the Latin." POST-OVERLAY CONFLICT (recorded, not resolved): D3 is SUPERSEDED — docs/15 §3 makes `design/ds-source` with **Geist** typography canonical, so the Latin face named here is stale. The rule that survives: the Latin face has no Devanagari coverage and must be paired with a matched Devanagari face (D25 HONORED — "Lingui v5, per-user re-render, Devanagari chain (docs/10)"). | F3 (+F7) | Whether Noto Sans Devanagari remains the pairing against Geist is docs/10's call — not asserted here. |
| MULTI.2 | **Text expansion** (L1375–1378): "Hindi and Marathi run roughly 15–30% longer than English, and Devanagari needs more line height for its headline stroke and matras. **Any layout tuned to English string lengths will break.** Buttons, chips, table headers and the 11 proposal step titles are the usual casualties." | F3 (+F7) | The 11-step proposal builder is the referent (Quick mode shows 4 of them, R11). |
| MULTI.3 | **Line height** (L1380–1381): "Devanagari needs more than the Latin scale allows. The type scale keeps its sizes; line heights get a per-script adjustment." | F3 (+F7) | Per-script adjustment must land inside the ds-source type scale (docs/15 §3). |
| MULTI.4 | **What is translated, and what is not** (L1383–1389). TRANSLATED: all UI labels, buttons, navigation · empty states, errors, help text · notifications and WhatsApp templates · voice agent speech. NOT translated: customer names, addresses · brand and model names (panels, inverters) · technical units — kW, kWh, kWp · "₹ formatting stays Indian in every language." | F3 | POST-OVERLAY notes: WhatsApp templates are copy-paste artifacts (D32 — composed, never sent); "voice agent speech" belongs to the agent language set (6), independent of UI (3) per R3; "₹ … Indian" is IN-pack number formatting under the global-backend ruling — the capability is F3's, the format is F1 pack data. |
| MULTI.5 | **Screen: Language picker** (L1394): in onboarding (first run) and in **Profile & preferences**, reachable by every user from More → Profile (mobile) and the sidebar (desktop). Shows each language *in its own script* — English · हिंदी · मराठी — never translated names. Defaults to the device locale; changing it re-renders the whole app immediately, no reload. | F3 | D25 HONORED — per-user re-render is the overlay's own phrase. |
| MULTI.6 | **Per-user, not per-tenant** (L1395): "One company can have an English-speaking owner and a Marathi-speaking surveyor. Language is a user setting." | F3 | — |
| MULTI.7 | Goes wrong: "**Missing translation** → falls back to English, never shows a raw key" (L1398). | F3 | — |
| MULTI.8 | Goes wrong: "**Long string breaks a button** → buttons wrap or truncate with the full text available; they never overflow" (L1399–1400). | F3 (+F7) | — |
| MULTI.9 | Goes wrong: "**Mixed script in one line** ('8.2 kWp सिस्टम') → normal and must look deliberate; test it" (L1401). | F3 | — |
| MULTI.10 | Goes wrong: "**Agent language ≠ app language** → they are independent; a Marathi-speaking rep may call a Hindi-speaking customer" (L1402–1403). | F3 (+M07) | R3 consequence verbatim: "the sets never converge by accident." |
| MULTI.11 | Goes wrong: "**Numbers** → Indian grouping in all three languages, always" (L1404). | F3 (+F1) | POST-OVERLAY: lakh/crore grouping is the IN pack's number format (global-backend ruling 2026-08-02) — "always" holds per market, not globally (same narrowing recorded on CG-moat.4). |
| MULTI.12 | **Recommendation: design every screen in Hindi at least once, early** (L1406–1410): "English-only design that gets translated later always breaks — and the breakage is invisible until a real user opens it. Building one screen in Devanagari now surfaces the font, spacing and line-height issues while they are cheap to fix." | F3 (+F7) | Source recommendation (SRC — in the journey). |
