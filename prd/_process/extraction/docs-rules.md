# docs-rules — product rules from docs 00–16, forward-compat, architecture (post-overlay)

(Assembled from part A: docs 00–07+FC, and part B: docs 08–16+ARCH. Column counts differ per part; each table carries its own header.)

# Extraction ledger — docs rules, part A (docs 00, 01, 02, 03, 04, 05, 06, 07, forward-compat)

Task 2 · Source: `docs/00-vision-and-scope.md`, `docs/01-business-model.md`, `docs/02-system-architecture.md` (product-level), `docs/03-tech-stack.md` (product-relevant constraints), `docs/04-data-model.md` (product rules riding on the model), `docs/05-domain-migration.md` (domain concepts that surface in product), `docs/06-offline-and-sync.md`, `docs/07-integrations.md` (capability level), `docs/forward-compat.md`.
All rows are POST-overlay (`docs/15-spec-resolutions.md` applied). Contradictions and superseded wording are flagged in `notes`, never silently resolved. Pure engineering (infra sizing, package layout, CI, pins) is excluded.

## DOC00 — Vision & scope

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC00.product-definition | HelioGrid is "a multi-tenant SaaS that runs the selling engine of an Indian solar EPC company — from a lead captured in under 30 seconds to a signed project with money collected", around one flagship asset: a full 3D design studio producing "engineering-grade layouts, shading analysis, electrical sizing and a priced bill of materials from 1 kW rooftops to 100 MW plants". | 01-product-overview | V2 brief globalizes framing (BRIEF); source text is India-worded — record both. |
| DOC00.one-record | Replaces the "spreadsheet-and-rekeying pipeline" with "one record that travels: CRM → survey → 3D design → proposal → no-login customer link → voice-agent follow-up → light project tracking → payments." | 01-product-overview | |
| DOC00.market-moment | Market moment: PM Surya Ghar — "1 crore households targeted, ~24% done as of Dec 2025"; most Indian EPCs run on Excel and WhatsApp; the funnel is "enormous and under-digitised". | 04-business-model | Cited source `./research/market.md` is missing (known gap — registers/conflicts.md). |
| DOC00.studio-flagship | Conviction 1: "The 3D Design Studio is the flagship. Nothing is compromised against it." Every studio tool and computed output survives the port; the tool census is the acceptance gate; touch-first, full parity on every surface including 375 px mobile. | M05 | Canonical census is `prd/modules/M05-studio/studio-census.md` (docs/15 §4 directive 9); POC phase-10-prompts is NOT canonical. |
| DOC00.honesty-conviction | Conviction 2: "Honesty is a feature." Every user-visible number carries a provenance tier (measured / derived / estimated / assumed); "money never renders while stale"; "structural adequacy is never computed — an engineer signs off". Competitors sell certainty; we sell trustworthy numbers. | F8 | Provenance tiers canonically defined in R18. |
| DOC00.india-first-global-ready | Conviction 3: India-first, global-ready — GST-native money path, PM Surya Ghar subsidy slabs, DISCOM-aware project states, TRAI-compliant voice calling, ₹ in lakh/crore grouping, EN/HI/MR UI — "built on a market-config layer that is injected, not hard-coded, so new markets are configuration". | F1 | Binding since 2026-08-02 global-backend ruling; see DOCFC rows. |
| DOC00.buyer-and-auth | The buyer is the EPC organisation; the owner signs up self-serve (phone + OTP). Residential AND C&I, both high volume (D1); deal values span "₹4.5L to ₹92L+" (1 kW → 100 MW range per docs/11). | 01-product-overview | Global constraint adds Google Login as BRIEF-origin auth alongside Mobile OTP (SRC). |
| DOC00.nine-stage-backbone | The v1 backbone is the nine-stage journey (Stages 0–8): company onboarding → user onboarding → lead capture → qualify & assign → site survey (two modes, D30) → design (10-step studio) → proposal (11-step builder, two paths) → follow-up/voice agent/close → projects (light). | 03-journey-map | Stage detail is extracted in the journey-stages ledger; this row is the backbone commitment. |
| DOC00.customer-journey-parallel | Parallel customer journey (C1–C13): "the EPC's customer never logs in. One tokenised link" carries them from proposal (with the 3D roof) through acceptance, advance payment, the long DISCOM wait — "made visible and attributable, not faster" — to installation, commissioning and handover. | F5 | |
| DOC00.tenant-config-scope | Tenant configuration surface: agent setup (guided + knowledge base + unanswered-questions loop), branding, two-tier catalog with price-book versions, proposal templates, payment terms, roles, message templates. | M01 | |
| DOC00.v1-entire-product | Scope commitment: "v1 = the ENTIRE product … no Launch-2, no v1.1, no 'later' bucket." | 01-product-overview | The 20-day calendar attached to this in docs/00 is SUPERSEDED (docs/15 §4 directive 5): the scope commitment stands; the day numbers do not. PRD carries no timelines (DD4). |
| DOC00.outside-window | The only honest exceptions to shipped-at-launch: (a) third-party approval clocks (DLT, telephony KYC, store review — code ships, activation follows the clocks), (b) spec-locked exclusions (D32/D29/D35/D9), (c) utility-scale studio enhancements (GPU shading, trackers, terrain) continuing immediately after as studio-moat investment. "1 kW → 100 MW remains a v1 commitment." | 01-product-overview | |
| DOC00.nongoal-projects-light | Non-goal: inventory, purchase orders, crew scheduling, O&M, generation monitoring (D9). "Projects are status + documents + money." The data model retains commissioning artefacts so a future O&M surface can attach. | M08 | |
| DOC00.nongoal-whatsapp-send | Non-goal: WhatsApp sending (D32). The app renders the message and the rep pastes it into their own WhatsApp. "Link opens are tracked; delivery is not." Messaging capability ships manual-copy only; BYO-WABA is v2. | M06 | Tension to record: M03 (marketing, BRIEF new scope) adds WhatsApp campaign channels — brief-mandated scope vs SRC non-goal; do not silently resolve. |
| DOC00.nongoal-custom-roles | Non-goal: custom roles (D29). Six fixed presets, stackable, OR-across-roles, widest visibility; "no per-person permission exceptions ever (D28)". | F2 | V2 brief expands to ~12 fixed preset roles (F2, Task 5) — preset-only principle unchanged. |
| DOC00.nongoal-measurement | Non-goal: LiDAR / photo measurement / AR capture (D35). Survey photos — any source including drone — are "*reference* for the designer, never measurement". | M04 | |
| DOC00.nongoal-discount-approval | Non-goal: discount approval workflows (D34). "The only guard is arithmetic: payable ≤ ₹0 blocks Generate." | M06 | D19 (owner approves every discount) is superseded by D34. |
| DOC00.nongoal-feature-flags | Non-goal: feature flags (owner directive). Features ship enabled when merged; "The only runtime gating in the product is billing entitlements." | M12 | |
| DOC00.nongoal-ppa-billing | Non-goal: PPA/OPEX operational billing — "the proposal type exists; metering/billing behind it does not" (R17). No recurring invoicing, no meter ingestion; post-Won the project tracks the same stages and document checklist. | M06 | Explicit non-goal recorded in docs/00 per R17; also bounds M11 scope. |
| DOC00.nongoal-lead-channels | Non-goal: website / inbound-WhatsApp lead channels (D13) — lead sources v1 are manual, CSV import and inbound voice only; website/WhatsApp remain "later" cards. | M02 | Tension: M03 (BRIEF new scope) adds campaign-driven lead capture channels; record, don't resolve. |
| DOC00.three-audiences | Three audiences: Owner (full login, widest visibility — pipeline, assignment, agent controls/queue, billing, tenant config, sign-offs); Employees (login, role-scoped: role decides home screen; reps see own leads, managers team, owner everything, D20); the EPC's customer (no login — one tokenised link, D5). | F2 | Customer-link specifics split to DOC00.customer-link-audience. |
| DOC00.customer-link-audience | Customer link contents: proposal with their actual 3D roof, accept/negotiate, payment status, project progress with honest wait attribution, handover pack. Lifecycle: "proposal → progress → handover." "Never blocked over unpaid money." | F5 | |
| DOC00.voice-touchpoint | Voice-agent calls are the fourth touchpoint: the agent serves the customer in their language (Hindi/Marathi/Gujarati/Tamil/Telugu/English), "discloses it is automated within 30 seconds, and hands to a human on request". | M07 | Agent language set (6) is independent of UI language set (3) — R3 consequence. |
| DOC00.demo-rooftop | Stage-0 promise: "demo Pune rooftop ready on day one" for every new tenant — makes every first session concrete (also GTM demo asset). | M01 | |

## DOC01 — Business model

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC01.in-price-book | Market scope: every price, benchmark and mandate mechanic in docs/01 is "the IN market's price book (INR)"; global pricing arrives per-market as `plan_prices` rows; "nothing here is the generic model". | 04-business-model | Matches PRD split: tier architecture generic, India book as source-derived baseline. |
| DOC01.org-pays | "The EPC organisation pays. Nobody else." One subscription per tenant, owner-administered, covering every employee — "no per-seat pricing, ever" (per-seat punishes whole-company adoption, which is the moat). | 04-business-model | |
| DOC01.byo-collections | The EPC's customer never pays the platform anything: tranche collections run through the tenant's own gateway account (BYO credentials) — "funds settle EPC-direct, the platform never touches money, no RBI payment-aggregator exposure". | M11 | Razorpay = IN reference implementation behind PaymentLinkPort (vendor-neutral rule). |
| DOC01.trial | 14-day full-feature trial: every feature, every tier capability, no card at signup (signup stays phone + OTP + company name, per D11). Trial caps to bound COGS: 25 AI roof detections, 15 voice-agent minutes, 5 GB storage. | 04-business-model | D11's "billing deferred" half is superseded (billing in v1, trial-only). |
| DOC01.mandate-at-conversion | Mandate collected at conversion, not signup: UPI AutoPay primary (monthly tier prices sit under the ₹15,000/debit cap), card e-mandate fallback (required for annual plans, which exceed the UPI cap). Pre-debit notifications are the gateway's job. | M12 | IN-rail mechanics; provider-neutral capability per SubscriptionBillingPort. |
| DOC01.trial-soft-block | Trial expiry = soft block: creating new leads/designs/proposals pauses; "read and export always work regardless of billing state — we never hold an EPC's customer data hostage (pre-committed rule, DPDP-aligned)". | M12 | |
| DOC01.no-free-tier | No perpetual free tier (user decision, final). Rationale: OpenSolar's free tier is funded by distributor placement and a payments cut — "a transaction-layer business we deliberately do not enter"; a free tier without that engine is COGS with no revenue. | 04-business-model | |
| DOC01.reslink-calibration | Benchmark calibration (owner-supplied): Reslink India INR page is authoritative — Basic ₹60,000/yr (up to 50 kW), Pro ₹85,000/yr (up to 500 kW, 1,000 proposals), Premium ₹1,20,000/yr (up to 5 MW), Enterprise custom. Capacity axis = "single-design kW ceiling"; we adopt the same axis and undercut every rung. | 04-business-model | Replaces the earlier cumulative-kWp/month meter; research/market.md USD figures are NOT the benchmark. |
| DOC01.gates-capacity-not-features | Tier gates are "capacity ceilings + usage counts + metered bundles — never features" (owner-confirmed). Every feature is in every tier: CRM & projects, full studio (shadow analysis, all 11 obstruction types, tin-shed/metal-roof, ground mount, structures, SLD + AC/DC & earthing layouts, industrial drawing sheets, PV/energy reports, DXF/SVG/PDF export), offline field app, customer links, all languages. The pricing page says competitors ransom these. | 04-business-model | |
| DOC01.billing-cadence | Every tier bills monthly OR yearly; "Yearly = pay for 10 months, get 12 — 2 months free (~17% off), one upfront payment. All prices exclude 18% GST." Owner-set price anchors: ~₹2k / ~₹4k / ~₹10k monthly, custom Enterprise. | 04-business-model | |
| DOC01.tier-table | IN tier book: Starter ₹1,999/mo (₹19,990/yr) — 50 kW ceiling, 30 proposals/mo, 10 active projects, 30 detections/mo bundled then ₹10 each, voice PAYG ₹6/min, 10 GB. Growth ₹3,999/mo (₹39,990/yr) — 500 kW, 300 proposals, unlimited projects, 100 detections, 50 GB. Pro ₹9,999/mo (₹99,999/yr) — 5 MW, 1,500 proposals, 400 detections, 400 voice min bundled, 250 GB. Enterprise custom (anchor ₹24,999+/mo) — 100 MW (utility: blocks/zones, trackers, terrain), unlimited, custom bundles + BYO number, white-label (custom domain + unbranded customer links), custom integrations / public API. Unlimited users on every tier. | 04-business-model | |
| DOC01.tier-positioning | Positioning laws: Starter is the "every EPC" tier (1–5 person residential shop; outgrowing caps IS the upgrade signal); Growth is the default past ~15 installs/month or small C&I; Pro is the C&I tier (voice bundle "no competitor has at any price"); Enterprise for ≥5 MW single designs / open-access / utility, sales-assisted annual contracts. | 04-business-model | |
| DOC01.cap-soft-block | Cap enforcement is soft-block UX: at 100% of a ceiling/count a banner appears, 7 days of grace follow, then "*new* creations of that type pause until upgrade or the next cycle. Reading, editing existing records and exporting never pause." Caps reset monthly on the tenant's billing anchor; counts are plain COUNT(*) over the cycle window. | M12 | |
| DOC01.metered-bundles | Three metered COGS lines sold with bundle allowances: voice-agent minutes sold at ₹6/min (COGS ₹2.5–4.0/min all-in); AI roof detection at ₹10/detection beyond bundle (COGS ≈₹7 worst case); OTP absorbed — "Not billed — absorbed, fair-use capped". PVGIS is free; Google Solar/Gemini are server-proxied with per-tenant metering and quotas "so a runaway tenant cannot torch margin". | 04-business-model | |
| DOC01.rate-card-caution | CAUTION carried verbatim: AI-detection COGS rests on "Unverified vendor-rate estimates — confirm official rate cards" (Google Solar dataLayers ≈ ₹6.5/call est. at $0.075; Gemini fallback ≈ ₹0.4/image). | 04-business-model | The confirm-rate-card spike lives in docs/03; do not present these COGS figures as verified. |
| DOC01.margin-ruling | Ruling: "gross margin ≥ 80% at typical utilisation on every self-serve tier — met." Worst-case floors accepted; overage pricing sits "≥40% above worst-case unit COGS" on every tier; "Bundles are sized so no tier can go margin-negative." Trial COGS bounded ≈₹300–500/trial by trial caps. | 04-business-model | |
| DOC01.gtm | Go-to-market: (1) ride PM Surya Ghar — subsidy slabs computed in the domain layer (state × capacity × DCR), not manually configured, "demo this in the first 5 minutes"; (2) replace Excel + WhatsApp, not another SaaS (30-second lead add, <10-minute remote survey-to-proposal, one record without rekeying); (3) India-native compliance is the wedge (ALMM/DCR at design time, GST-native BOM→proposal→tranche money path, DISCOM-aware states, DLT messaging, TRAI calling); (4) the voice agent is the demo that closes; (5) founder-led sales into EPC clusters; trial-to-paid is the only launch conversion metric. | 04-business-model | |
| DOC01.price-under-incumbents | Pricing principle 1: "Price under ARKA and Reslink at equivalent capacity, always" — benchmarked against Reslink's INR page; margin comes from COGS discipline, not list price. | 04-business-model | |
| DOC01.yearly-payment-rail | Yearly collection mechanics: yearly is a single payment link/invoice or card e-mandate/e-NACH because "every annual total incl. 18% GST exceeds the ₹15k UPI AutoPay per-debit cap — even Starter: ₹19,990 + GST = ₹23,588". Monthly rides UPI AutoPay on all tiers. | M12 | IN-rail specifics. |
| DOC01.supplier-of-record | "Prices ex-GST; we are supplier of record" — GST-compliant invoice per cycle (our GSTIN/SAC); IRN/e-invoicing obligations are ours. | M12 | |
| DOC01.grandfather | "Grandfather generously." Early tenants keep launch pricing for 24 months minimum; "repricing is a trust event in a WhatsApp-connected market". | 04-business-model | |
| DOC01.price-for-every-epc | "A price for every EPC (owner directive)." Starter exists so price is never the reason a small EPC stays on Excel; entry ≈₹2,000/mo (owner-set anchor). | 04-business-model | |
| DOC01.creation-caps | Proposal/project creation is capped per tier (owner directive) — visible, generous counts (30 / 300 / 1,500 / unlimited proposals per month; Starter additionally 10 active projects). "Caps are upgrade signals and abuse bounds, never feature ransoms"; enforcement is soft-block with read + export always working. | 04-business-model | Enforcement UX detail in M12 (DOC01.cap-soft-block). |

## DOC02 — System architecture (product-level only)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC02.link-grant | The public customer link is a "stateless HMAC-signed grant (scope + proposal/project id + expiry), never a session" — outside the login/role system entirely. Grants: view proposal, accept, download PDF, pay tranche via the tenant's BYO payment link. Rate-limited per token+IP; "Every access is audit-logged." | F5 | |
| DOC02.roles-mechanism | Tenancy/visibility mechanism: 6 stackable preset roles (owner, manager, sales_rep, surveyor, designer, engineer), "OR-across-roles, widest visibility wins". Platform-admin operations use a separate role and are audit-logged. | F2 | Normative capability matrix lives in docs/08 (part B); V2 expands presets to ~12 (F2). |
| DOC02.tz-schedules | "User-facing schedules are tenant-timezone-aware by law"; v1 tenants are all IST so stated times read as IST. Platform-internal sweeps stay fixed-clock. | F1 | Same law as DOCFC.tz-scheduling. |
| DOC02.trigger-schedule | Product-visible automation timers: proposal-unopened-3d follow-up queue daily 09:15; task-overdue-2d nudges daily 09:05; snooze-wake sweep every 15 min; dormant sweep nightly; DND scrub refreshed daily before the 09:00 calling window; recording-retention purge (90-day) nightly; trial-expiry sweep flips entitlements to soft-block (read+export always work); billing reconciliation every 6 h. | M07 | Snooze/dormant semantics themselves are R9 (rulings ledger); trial sweep → M12. |
| DOC02.money-never-stale-api | The fingerprint mechanism "IS the 'money never renders while stale' rule: a quote whose designFp no longer matches reads as provisional, enforced at the API, not in the UI". Catalog-version / rules-pack / engine-version changes self-stale older outputs. | F8 | |
| DOC02.server-identifiers | "Server assigns all business identifiers" (proposal/project numbers via tenant counters) — never client-generated. | M06 | Applies equally to project numbers (M08). |
| DOC02.scale-editable-unit | Above rooftop scale the editable unit is the "block/table/zone — panels become derived instances"; the payload carries blocks alongside the per-panel rooftop model from day one so 100 MW needs no migration. | M05 | |
| DOC02.market-pack-unit | "Market pack = rules + catalog + templates + locale data, versioned as one unit": electrical ladders, design-temp bands, setbacks, wind zones, subsidy/incentive model, tax model (per_line_rate vs document_level; per-line GST is the IN instance), net-metering conventions, project-stage labels + skippable stages, document checklists, payment-mode vocabulary, phone spec, certification schemes (ALMM/DCR for IN), compliance ruleset + calendar (calling hours, DND semantics), catalog scope + price-book currency, proposal/PDF/SLD templates + standards labels, locale data (currency display incl. lakh/crore, holiday calendar, default units). "No market fact is ever a module-level constant." | F1 | |
| DOC02.market-invariants | What stays invariant across markets: geometry/electrical kernels ("ladders are data, math is not"), the provenance/honesty system, the one money path, the tenancy model, the design system. What swaps per market: rules data, catalog/price book, tax + subsidy models, locale, telephony adapter, payment adapter. | F1 | |

## DOC03 — Tech stack (product-relevant constraints only)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC03.webview-parity | Mobile studio: "the studio opens as an authenticated WebView at full parity (D2: responsive web at 375px) — native canvas editing is NOT rebuilt". No studio feature dropped on any surface. | M05 | |
| DOC03.mobile-scope | The mobile app is field-first: My Day, leads, quick-add, offline surveys, visits, notifications; studio via WebView. Mobile is built in lockstep with web ("mobile is never a follow-up"). | 01-product-overview | Surface emphasis feeds each module's Personas & surfaces section. |
| DOC03.currency-units-law | Product law travelling with tokens: "₹ uses Indian grouping (lakh/crore) in every locale; kW/kWh/kWp are never translated"; Devanagari renders via a dedicated font chain, never OS fallback. | F3 | Per-market grouping generalised by market pack (F1). |
| DOC03.devanagari-documents | Proposals in Hindi/Marathi are commercial documents — "broken conjuncts are not acceptable"; PDF rendering must shape Devanagari correctly (the vendor choice exists solely to guarantee this). | F3 | Capability: DocumentRenderPort (DOC07.pdf-artifact). |
| DOC03.light-only | Light-only v1 (owner ruling 2026-07-24): no dark variants shipped; semantic aliases keep a dark value-set droppable later. | F7 | Duplicate of R19-A (rulings ledger). |
| DOC03.dpdp-residency | Compliance rulings: DPDP Rules 2025 use a negative-list model — cross-border object storage is permitted by default while the DB holding primary PII stays in India; RBI payment-data localisation is satisfied because a licensed Indian PA holds payment instruments — "we never do". | F1 | IN determination; new markets need their own determination (DOCFC.new-market-gate). |

## DOC04 — Data model (product rules riding on the model)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC04.currency-stamp | Money-bearing document roots stamp `currency_code` at creation; line items inherit it; sums reconcile to the currency's minor unit. One currency per tenant, server-assigned from the market at tenant creation (v1: INR only). | F1 | |
| DOC04.tenant-onboarding-fields | Tenant record carries Stage-0 declarations: segment ("what do you sell": residential / ci / both), typical system kW ("seeds first-quote defaults"), address, bank details (used by proposal step 11), logo; tax registrations empty until first proposal (v1 accepts only IN_GST). | M01 | |
| DOC04.user-lifecycle | Users: phone is the login identity (E.164, unique global); status invited / active / deactivated — "deactivate, never delete". Service invariants: always ≥1 owner, always ≥1 user with team management. | M01 | |
| DOC04.user-language-units | Per-user UI language en/hi/mr (D25) and units preference m/ft — "procurement stays metric regardless". | F3 | |
| DOC04.roles-no-exceptions | Permission = OR across held roles; lead visibility = widest scope held. "No per-user permission override table exists — deliberately (D28)." Custom roles are v2 (D29). | F2 | |
| DOC04.invites-phone | Team invites are phone-based with roles attached; invite states pending/accepted/expired/revoked with expiry; accepting creates the user + roles in one step. | M01 | |
| DOC04.tenant-settings | Tenant settings sections: branding ("colours within token constraints", letterhead), proposal defaults (T&C pages, timeline template, default tranche template), which lead sources are live, capture config ("deferred sources parked here"), locale (default language, holiday calendar). | M01 | |
| DOC04.phone-identity-dedupe | "Phone is identity" for customers: unique per tenant on phone number — "dedupe on capture from every channel"; dedupe also checks contacts' phone numbers. | M02 | |
| DOC04.contacts | A customer can carry additional contacts (name, phone, role label — decision-maker / landlord / spouse, tenant-extendable) with one primary. | M02 | |
| DOC04.compliance-flags | Per-customer calling-compliance state: voice consent (granted/denied/unknown), DND status with daily scrub cache, do-not-call flag ("'stop calling' — irreversible without the customer's say-so"), complaint-triggered permanent quiet flag. The gate reads one row per dial. | M07 | Fields live on the CRM customer record (M02) from day 1 (forward-compat). |
| DOC04.merge-tombstone | Customer merge (R8): merged rows point at the survivor and "are tombstones, never deleted"; merge = re-point contact and lead references, keep the audit trail; "the merge operation provably touches no money tables". | M02 | |
| DOC04.lead-sources | Lead source set: manual / csv_import / inbound_call / website / whatsapp / referral — docs/04 marks "last three dormant v1, D13". | M02 | CONTRADICTION with R15: the referral tag + "came from" chip SHIP in v1 (Track A) — post-overlay, `referral` is live; only website/whatsapp stay dormant. Record in conflicts register. |
| DOC04.lead-machine | Lead state machine — funnel: new → contacted → qualified → survey → design → proposal → negotiating → won; parking/terminal: lost, disqualified, junk, dormant. Reason mandatory entering lost/disqualified (6 reasons each); dormant set only by the 30-day-silent sweep; reopen restores the pre-close stage and counts reopens; "postponed" losses auto-resurface on the given date; "entering won creates the project in the same transaction". Snooze is orthogonal to stage (hidden from My Day until wake). | M02 | Consolidated timer semantics are R9 (rulings ledger). |
| DOC04.unassigned-inbox | A lead with no owner is the inbox/unassigned state; ">24 h unassigned escalates to owner". | M02 | |
| DOC04.qualification | Qualification captures: monthly bill (tenant currency), roof ownership, roof type, shading, timeline, decision-maker — a form snapshot; monthly bill is sortable/filterable. | M02 | |
| DOC04.forecast-not-revenue | Lead estimated value is a "weighted-pipeline input (forecast ≠ revenue)" — forecast never mixes into won totals (D37). | M13 | |
| DOC04.assignment-history | Assignment history is append-only and records the rep's open-lead count snapshot at assign time (D14: "rep load visible"); current owner is separate from the audit trail. | M02 | |
| DOC04.timeline | One polymorphic timeline (activities) per lead/project/customer rendered as a single stream; kinds include note, call logged, agent call, stage change, assignment, proposal sent, link opened, survey submitted, design event, sign-off event, payment, document, task event, system; actor may be user / agent / system / customer. Append-only. | M02 | Cross-module: every module writes here. |
| DOC04.tasks | Tasks carry assignee, kind (follow_up / site_visit / call / custom), due date, status; "Overdue is derived, never a stored state"; auto-created tasks record their provenance rule (e.g. proposal_sent_plus_2d). The task-overdue-2d agent trigger reads this. | M07 | |
| DOC04.survey-versioned | "Surveys are versioned-append: a revisit inserts a new version row; nothing mutates." Statuses draft → in_progress → submitted → superseded; submit notifies the designer. This is also the offline conflict story — "offline captures never fight over one row". | M04 | |
| DOC04.survey-provenance | Survey-level provenance default: remote → derived, physical → measured (R18/D30 split); captured sections (roof/electrical/shading/access/structural) carry "per-field skipped-but-flagged markers and per-field provenance"; remote detection artefacts carry per-detection confidence + source (dataLayers/gemini) + prompt version. | M04 | |
| DOC04.sanctioned-load | Sanctioned load (from the meter photo) is captured per site; "design overrun = real approval blocker" — the sanctioned-load warning is fed from survey capture. | M04 | Auto-design treats it as a soft cap (DOC05.auto-design-soft-cap). |
| DOC04.survey-gaps | Remote-survey gaps ("what remote couldn't determine") are first-class: each gap resolves as ask_customer / capture_on_site / resolved / waived; "Physical-visit booking pulls the open capture_on_site set into the guided flow." | M04 | |
| DOC04.visits | Site visits are scheduled assignments (scheduled / in_progress / done / cancelled) linked to the survey version they produce; booking a visit for open on-site gaps schedules here. | M04 | |
| DOC04.photos-reference | Survey photos are tagged (roof corner, obstruction, meter, DB board, structure, access, other), carry source (on_site / customer_sent / drone) and can pin to a specific obstruction; they "travel to the designer attached to the survey". Reference only, never measurement (D35). | M04 | |
| DOC04.design-variants-recommended | Design variants are sibling designs on the same lead (duplication lineage kept); exactly one may be recommended per lead — "customer sees exactly one recommendation (D16)". | M05 | |
| DOC04.design-freshness-pins | Every design pins its inputs (catalog version, price-book version, rules-pack version) into the fingerprint so an external change self-stales it; "staleness = compare, not flag-flipping"; energy figures surface their irradiance source (pvgis / estimate). | F8 | |
| DOC04.signoff-append | Engineer sign-offs are append-only decisions pinning exactly what was reviewed (design version + fingerprint); decision approved/returned with comments "pinned to the fault"; "A design edit after approval (fingerprint mismatch) drops signoff_status back to draft". "Structural adequacy is never computed — this table records who signed and when", and the disclaimer travels with every structure-bearing output. | M05 | Honesty law shared with F8. |
| DOC04.proposal-paths | One proposal object, two entry paths (D21): with_design (derived numbers) / without_design ("estimated/assumed + mandatory 'Indicative proposal' disclaimer"). Proposal type: capex / opex_ppa (PPA billing beyond the document is out of v1 scope — R17); nothing downstream branches on type except the rendered document and the honesty label. | M06 | |
| DOC04.proposal-status-machine | Proposal status: draft → shared → accepted / declined; "superseded when a newer proposal replaces it on the lead". | M06 | |
| DOC04.proposal-versions-immutable | Proposal versions are immutable, append-only, server-numbered; each version snapshots the full 11-step field set + computed money block and pins catalog/price-book versions — "sent proposals keep original prices forever"; each version carries a change note ("what changed and why"). Staleness is "derived, never stored" (pinned design fingerprint vs live). | M06 | |
| DOC04.proposal-tax-model | The proposal money block is scheme-generic: currency code, cost ex/incl tax, per-line tax %, document-level taxes[] breakdown, battery + battery tax %, incentive amount, discount %⇄amount, payable. "Tax strategy comes from the market pack: per_line_rate (IN) or document_level" (reserved, e.g. US sales tax). | M06 | |
| DOC04.components-gate | Components are mandatory on every proposal (D22), enforced at Generate: all five categories present (panel, inverter, structure, electrical, BOS) "+battery mandatory for offgrid/hybrid — no-battery hard block". Each line carries resolution provenance (tenant_override / tenant_item / platform_item / custom) and a provenance tier; component rows are immutable with their version. | M06 | |
| DOC04.payable-guard | Money guard at Generate: "payable ≤ 0 (tenant currency) blocks — the only hard discount guard (D34); no approval tables exist". | M06 | docs/15 D34 row adds: below-cost warns. |
| DOC04.tranche-templates | Payment tranche templates: named splits with percentages summing to exactly 100.00 and a stage each tranche is due on; "Platform seeds 10/60/20/10 and 30/60/10 at tenant creation"; a tenant default exists. | M11 | Template management surface is tenant config (M01). |
| DOC04.tranches-money-path | One money path: the quoted tranche schedule on a proposal version becomes the project's collection schedule at Won (same rows). Σpct = 100.00 per version; Σ amounts = payable to the currency's minor unit; "BOM ↔ proposal ↔ tranches ↔ payments reconcile". Stage completion makes the matching tranche due; tranche status upcoming → due → part_received → received, waived terminal, recomputed from payments. | M11 | |
| DOC04.link-lifecycle | Customer links are tokenised, no login ever (D5); "one URL advances in place through the lifecycle" proposal → progress → handover; status active / revoked / expired; the raw token is never stored. "Never revoked over unpaid money (product law: chase the person, don't punish the view)." | F5 | |
| DOC04.link-named-otp | Named links + OTP-at-accept ship in v1 (R6-amended): a deal may carry multiple labelled per-contact links; an OTP challenge gates Accept above "a tenant-set value threshold" (threshold lives in tenant settings, denominated in the tenant's currency). Acceptance records capture full attribution (link id, contact, OTP verification, IP, user agent) from day one. | F5 | D33's accepted risk is closed at launch — D33 superseded. |
| DOC04.link-events | Per-link open attribution is append-only: opened / section_viewed / accepted / negotiate_requested / declined, with section and view duration; "Shared → opened → viewed duration" derives from events. Opens tracked, delivery never (D32). No PII in URLs or logs. | F5 | |
| DOC04.accepted-human-confirms | Customer "accepted" notifies the rep — "the rep still marks Won (human confirms, then the project row is born)". | F5 | Cross-contract with M02 (Won transition) and M08 (project creation). |
| DOC04.project-machine | The full 9-stage project chain is canonical (R2): won → material_ordered → dispatched → installation → electrical_and_metering → utility_inspection → commissioned → incentive_claimed → handed_over, plus terminal cancelled (reason mandatory) reachable from any stage; "cancelled projects stop counting as revenue immediately". Stage LABELS are market-pack data ("DISCOM inspection" / "Subsidy claimed" are the IN pack's labels); incentive_claimed is skippable per pack rule and for no-incentive projects. Days-in-stage is the primary board metric. The D9 5-stage shorthand is a display grouping at most, deprecated as a model. | M08 | R2 amended 2026-08-02: market-neutral enum names. |
| DOC04.document-checklist | The project document checklist is market-pack data, seeded at project creation (IN pack: signed proposal, advance receipt, net-metering application, DISCOM approval, subsidy application & sanction, commissioning certificate, warranty documents, handover pack — 8 rows; "incentive row omitted for commercial"). Statuses pending / uploaded / verified. "Handover = all rows past pending + pack shared on the link." | M08 | |
| DOC04.blockers | Project blockers record who is waited on: utility / customer / material / us ("DISCOM" is the IN pack's label for utility), with reason, start and expected-until — the customer link renders honest wait framing ("applied 15 Aug, typically 3–6 weeks"). "The product's job is to make waiting visible and attributable, not faster." | M08 | |
| DOC04.payments-append-only | Project payments are an append-only receipts ledger: mode validated against the market pack's payment-mode list (IN: upi / neft / cheque / cash / payment_link), reference + receipt file, "append-only with reversal rows (negative amount + pointer), never edited". BYO-gateway payment-link receipts arrive via webhooks and insert here. | M11 | |
| DOC04.installation-checklist | Installation checklist steps are generated from the design's structural dependency graph (foundation → legs → rafters → purlins → modules → stringing → BOS) with manual additions; "v1: the coordinator ticks (no installer role, D29)" with an optional free-text "done by" per step (R16). | M08 | |
| DOC04.catalog-two-tier | Two-tier catalog, resolution implemented once: "tenant_catalog_overrides → tenant_catalog_items → platform_catalog_items". "Archive, never delete: removed products keep serving old proposals." Tenant overrides are sparse (null field falls through to the platform value) and unique per platform item; overrides carry visibility (visible/hidden) and preferred flags. | M01 | R13: one catalog surface; the old spec's duplicate row is void. |
| DOC04.catalog-certifications | Platform master catalog is curated with typed per-kind specs; certifications are scheme-keyed on specs — "IN: ALMM, DCR; the pack declares which schemes a market requires" (R13-amended). Brand+model are never translated. Platform catalog is read-only to tenants. | M01 | |
| DOC04.catalog-provenance | Catalog provenance enum: manufacturer_datasheet / installer_pricebook / mock_representative — data honesty about where a spec came from. | M01 | Provenance surfacing is an F8 concern. |
| DOC04.catalog-release-stale | Catalog releases are labelled and append-only; the release label rides into designs and proposal versions — "a release publish self-stales every design fingerprint that pinned an older label". | M01 | Staleness mechanics → F8. |
| DOC04.pricebook-versions | Price-book versions (rates for everything that isn't a catalog item — R13) are immutable snapshots: "Price updates create a new version, never mutate rates in place; sent proposals pin version_no." One active version per tenant; default margin % rides the version. | M01 | |
| DOC04.agent-config-versions | Agent config is versioned-append (D36): config covers agent name, voice, tone, languages (6-language agent set), opening line (AI disclosure default), allowed topics, hand-over rules, max attempts, calling window (9am–9pm default, timezone, holiday calendar). "Configs are never edited in place: publishing a change inserts version n+1 — queued calls keep the version they were queued with." A change-history screen reads the versions. | M07 | |
| DOC04.kb-unanswered-loop | Knowledge base: 8 sections (about us, products, warranty, process, pricing, subsidy, financing, objections), tenant-editable; the unanswered-questions loop (D24) tracks question + asked count, and a "One-tap answer writes the answer into the named knowledge section". KB is mutable — calls pin agent-config version, not KB content. | M07 | R10: review-queue only; nothing a rep types reaches agent behaviour without explicit promotion. |
| DOC04.call-queue-compliance | Queued agent calls: triggers proposal_unopened_3d / task_overdue_2d / failed_attempts_3 / manual / callback_requested; scheduling is window-shifted ("11 pm capture ⇒ not before 9 am"); attempts counted; a blocked queue item persists the pre-dial compliance verdict (dnd / no_consent / do_not_call / outside_window / quiet_flag); owner can cancel queued calls. | M07 | |
| DOC04.call-ledger | Every call — human and agent, inbound and outbound — is ledgered: outcome (interested / callback_requested / not_interested / no_answer / busy / wrong_number / voicemail / escalated / transferred / opted_out), interest signal (hot/warm/cold/none), one-line summary on the timeline with "transcript on tap" (D18), language, recording-consent flag ("may decline, still served"), AI "disclosure_played ≤30 s", the agent-config version actually used, per-call cost breakdown. Recording purged at 90 days; "transcript retained". | M07 | |
| DOC04.byo-number | Tenant phone numbers: platform-provisioned default or BYO; CLI series distinguishes transactional vs promotional vs standard inbound. | M07 | STALE WORDING in docs/04: "BYO is a hosting/porting flow with KYC" + a `porting` status — SUPERSEDED by directive 7 as amended (S5/ADR-0019): "BYO = inbound forwarding to the platform ExoPhone — outbound CLI is NOT portable". Post-overlay state = forwarding only; record conflict. |
| DOC04.ivr-flows | Inbound IVR is a per-tenant flow: greeting → menu → routes (AI agent / ring group [users] / voicemail) with business-hours branches; versioned, published whole. | M07 | STALE WORDING in docs/04: outbound DTMF traversal "sendDtmf/onDtmf on TelephonyProvider" is the superseded directive-7 wording — post-overlay, DTMF-send is a declared capability NO current adapter provides; traversal degrades honestly (ivr_blocked). Record conflict. |
| DOC04.routing-policies | Call routing/escalation policies are versioned-append tenant data: ordered when(conditions) → then(action) rules — conditions include AI confidence, customer-requests-human, intent/department, priority, business hours, VIP/existing-project; actions continue_ai / warm_transfer / cold_transfer / escalate(chain) / enqueue_callback / voicemail; escalation chains are ordered levels with ring timeouts and a "mandatory terminal fallback (callback queue or voicemail)". "In-flight calls keep the version they started with." | M07 | |
| DOC04.handoff-ledger | Every handoff (warm/cold transfer, escalation) is ledgered append-only with "the PINNED AI-generated context (summary text, intent, sentiment, collected fields, transcript pointer) generated once at handoff decision time", the target, the outcome, and whether the whisper summary played. | M07 | |
| DOC04.user-presence | Per-user routing availability: available / busy / off with optional until-time; "v1 = manual toggle". | M07 | |
| DOC04.plans-bundles | Plans (starter/growth/pro/enterprise) carry trial days and included bundles: voice_minutes, ai_detections, otp_sms ("fair-use cap — not billed v1"), storage_gb, seats ("reserved — always unlimited in v1, no per-seat pricing"); plan capacity ceilings (e.g. single-design kW) live in the plan definition, not as metered entitlement keys. | M12 | |
| DOC04.plan-prices-per-currency | Plan prices are a per-currency price list with provider-neutral gateway refs; "v1 rows are INR/Razorpay only — a new market adds rows, zero schema change". | 04-business-model | Market price-book architecture. |
| DOC04.subscription-states | Subscription lifecycle: trialing → active → past_due → halted; cancelled / expired terminal; one non-terminal subscription per tenant; the successful-charge event is the entitlement grant trigger; mandate types validated per provider/market (IN: upi_autopay / card_emandate); cancel-at-period-end supported. | M12 | |
| DOC04.entitlements-current | Entitlements are the current effective limits per metered key, recomputed on every charge/plan change; source plan / trial / manual_grant; queried on the hot path for the soft-block check. "No schema mechanism may hold data hostage" — read + export always work regardless of billing state. | M12 | |
| DOC04.usage-ledger | Usage is an append-only metered ledger: billable metrics voice_minutes / ai_detections / otp_sms (not billed v1) / storage_gb (daily gauge), plus non-billable observability metrics (solar/map/DEM fetches, push sent, documents rendered); every event carries provenance (the call, detection, OTP send, document) and an idempotency key ("duplicate emits are no-ops"); internal cost estimates are "never customer-facing". Rollups serve the usage screen; "the ledger is the truth". | M12 | |
| DOC04.invoices | Platform invoices (we are supplier of record): per-cycle, scheme-generic tax breakdown (IN: the single GST line), scheme-tagged statutory extras (IN: IRN/ack/QR + SAC — "ours to validate"), statuses issued/paid/failed/refunded, PDF attached. | M12 | |
| DOC04.byo-credentials | Tenant BYO integration credentials (payment gateway now, WABA later) are encrypted at rest, decrypted only inside the owning adapter, "never logged, never in contracts"; "Platform never touches tenant funds (no RBI PA licence needed)". | M11 | |
| DOC04.notification-types | Notification type set: proposal_opened, agent_escalation, follow_up_due, survey_submitted, design_returned, signoff_requested, payment_due, lead_unassigned_24h, system — each deep-links to its subject; read state and push-sent marker tracked. | F6 | Full enum exists from day 1 (forward-compat). |
| DOC04.notification-language | Notification title/body are "rendered in the user's language at emit time — notifications are not re-translated". | F6 | |
| DOC04.message-templates | Message templates per key (proposal_share, follow_up_nudge, payment_reminder, visit_confirmation, handover — tenant-extendable) × 3 UI languages with placeholders; platform seeds a starter set. "Copy-paste supply for the rep's manual WhatsApp flow (D32 — the app never sends)." | F6 | Consumed by M06/M07/M08 share flows. |
| DOC04.targets | Monthly targets at tenant or per-user scope (one per scope+month): "Stores the goal only; actuals derive from proposals/payments at read time." Consumed by dashboards ("set a monthly target"). | M13 | |
| DOC04.audit-log | Append-only audit log spanning tenant and platform actions (actor: user/agent/system/webhook/admin; before/after diffs); platform-admin reads are themselves audited; nothing can update or delete audit rows. | F2 | |
| DOC04.referral-no-credit | Referral (R15): a referral row links referrer customer → referred lead (source=referral), "visible on both records and in win/loss analytics. No monetary credit, no redemption, no balance in v1" — the credits LEDGER is the spec-locked exclusion; future credits reference existing referral rows, no backfill problem. | M02 | See DOC04.lead-sources contradiction note. |

## DOC05 — Domain migration (domain concepts that surface in product)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC05.fingerprint-self-stale | The 5-layer nested fingerprint graph (site ⊂ geometry ⊂ layout ⊂ electrical ⊂ design) plus the engine-versioned shading fingerprint port intact; catalog version, structure-model version and shading-engine version join the fingerprints "so a price-book or engine change self-stales older outputs"; staleness UX carries over unchanged. | F8 | |
| DOC05.wizard-9-steps | The studio wizard presents 9 visible steps (R7 — phantom step 5 folded into Panel layout, which offers Place manually / Auto-fill to target / Use maximum capacity); step indicator shows "n / 9"; "no user-visible 'step 5' ever exists" (internal IDs stay stable). | M05 | |
| DOC05.no-kw-clamp | The freemium design-capacity gate is deleted: "plan gating is a billing concern … never a design-capacity cap. No kW clamp exists anywhere in domain." | M12 | Tier single-design ceilings (DOC01.tier-table) are therefore enforced via billing entitlements, not inside the design engine — enforcement location, not contradiction. |
| DOC05.ai-doorway | AI roof detection enters a design only through a validated artifact (version → pin → geometry → bounds → confidence, per-entity drop-with-reason); applying it stamps entity provenance "so the UI can say 'N AI-detected entities — dimensions are detector estimates'". | M04 | Same doorway rule stated in docs/07 §3. |
| DOC05.shading-validation-banner | The shading engine's "ENGINEER VALIDATION banner travels" with the ported module — shading outputs carry their validation caveat. | M05 | |
| DOC05.electrical-locked-rules | Locked electrical behaviours: autostring's "unstrung-over-illegal rule" (never produce an illegal string; leave panels unstrung instead); an empty voltage window "stays an explicit fault", never silently passed. | M05 | Complements R12: the studio's electrical hard gate stays. |
| DOC05.bom-money-locked | Locked BOM money math: "margin-below-GST, pre-GST pro-rata discount"; money invariants (GST/discount/margin/subsidy) are release-gated. | M05 | Surfaces in proposal money (M06) and the money path (M11). |
| DOC05.bom-overrides | BOM per-field overrides are preserved with stale-field tracking; "override=measured" — a user-entered override takes measured provenance. | M05 | |
| DOC05.review-read-only | Design review output is "read-only, never a second gate" — review surfaces findings without blocking. | M05 | |
| DOC05.narrative-facts | Proposal narrative generation is fact-traceable — every narrative claim maps to a facts[] entry (traceability test travels with the module). | M06 | |
| DOC05.auto-design-soft-cap | Auto-design honours the sanctioned-load soft cap (design can exceed it, with warning — the survey-fed approval-blocker warning). | M05 | |
| DOC05.share-links-replaced | The POC's local share-viewer links are replaced by tokenised customer links (signed, scope + expiry, server-rendered) — no local-only share path survives. | F5 | |

## DOC06 — Offline & sync

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC06.reads-local | "Reads are local, always." Every synced entity reads from the on-device store; "No screen blocks on connectivity; no spinner walls." | F4 | |
| DOC06.writes-queued | "Writes are queued, never lost." All offline mutations enter a durable upload queue; the server is the only writer of record. | F4 | |
| DOC06.server-owns-money | "The server owns truth and money." Version checks, tenant checks, business identifiers and "every rupee are computed server-side. Offline output is always labelled provisional." | F4 | Provisional labelling is F8 law. |
| DOC06.offline-full-set | Offline-full (read + write, durable queue) — the R14 boundary: physical survey capture (all groups, versioned-append), survey photos (resumable upload), quick-add lead, activity/visit logging, task ticks, My Day + assigned leads/read cache. | F4 | |
| DOC06.online-only-set | Online-only ("fail fast, honest message, never queued"): proposal generate/share, any money mutation (BOM edits, discounts, tranches, payments), design mutations, billing, agent/tenant config, imports/exports, PDF render, external intelligence (irradiance, imagery, AI detection). | F4 | |
| DOC06.stale-reads | "Reads everywhere degrade to cache with a staleness banner"; money figures shown from cache carry the provisional label (money-never-stale rule). | F4 | |
| DOC06.designs-not-mobile-offline | Design editing on mobile requires connectivity (studio WebView); designs are not in the mobile offline set. The web studio stays fully usable through connectivity blips (geometry, layout, electrical are local); only server recomputes and saves queue; a stale save conflict rolls back optimistically-applied state and prompts reload. | F4 | Also bounds M05 offline behaviour. |
| DOC06.conflict-matrix | Conflict rules by entity: survey — versioned-append, "a revisit NEVER overwrites v1"; design — single-editor + version check, "No merge, ever"; lead field edits — per-field last-writer-wins with an activity entry per applied change "so a 'lost' concurrent edit is always visible and recoverable from the log"; visit — status only moves forward; catalog — read-only on device; notifications — read-state up only. "LWW is resolved by server apply order, never device clocks"; capture time is display/audit only. | F4 | |
| DOC06.rejects-ack | Every mutation response — including rejections — acknowledges the mutation; server truth replaces local state on next sync; "Rejected-but-user-visible outcomes surface in the attention tray." | F4 | |
| DOC06.attachment-pipeline | Photo pipeline: capture offline to device storage; upload when connected (default "Wi-Fi-or-charging", user can force "upload now on mobile data" per batch; small mutations always upload immediately); the local full-resolution copy is deleted "only after" the server confirms ownership; device keeps the thumbnail; 2 GB local cache cap with LRU eviction of acked originals. | F4 | |
| DOC06.sync-status-ux | Sync status UX contract: a global chip ("3 surveys waiting · 47 photos · will upload on Wi-Fi") with last sync time, opening a per-item sync tray; "Never blocking" — no modal, no spinner wall, no disabled primary actions because of connectivity; per-record badges queued → syncing → synced plus an attention state with a one-line reason and retry/discard; all copy localized EN/HI/MR. | F4 | |
| DOC06.token-expiry | Token expiry while offline: local reads/writes continue indefinitely; on reconnect, re-auth releases the preserved queue only when "the same user on the same tenant signs back in. A different user signing in wipes the local DB and queue first — tenant isolation beats convenience". | F4 | |
| DOC06.update-required | A breaking sync change gives older apps "a soft 'update required' screen with reads still working". | F4 | |
| DOC06.quarantine | Corrupt payloads: "Quarantine, never crash, never silently drop." Client-side quarantined rows are badged for attention; server-side invalid mutations are preserved for recovery — "nothing a field user captured is ever unrecoverable". | F4 | |
| DOC06.entitlement-grace | Entitlement state is cached on-device with a 72 h grace "so a paid tenant is never soft-blocked by a dead zone; read + export always work regardless". | F4 | Enforcement rules live in M12. |
| DOC06.no-local-price | "No device ever prints a customer-facing price computed locally." Offline, a user can queue a proposal-draft request; every locally-shown figure renders provisional until the server recompute lands. | F4 | Also binds M06. |
| DOC06.online-first-until-offline | Until the offline layer lands, mobile is online-first — "a move in TIME only — never in scope or quality" (directive 10); surveys are versioned-append and the idempotency ledger exists from the start regardless. | F4 | Sequencing language; no calendar dates enter the PRD (DD4). |

## DOC07 — Integrations (capability level, vendor-neutral)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC07.ports-vendor-neutral | Every external capability sits behind a port owned by the product; vendor names are v1 reference implementations; swapping a vendor is an adapter change, never a product change. Per-tenant BYO credentials are encrypted; no third-party call happens from the client (one exception: the intentionally-public, referrer-locked maps JS key). | 01-product-overview | Matches the global constraint "vendor names become capability requirements". |
| DOC07.metering-entitlement-order | Billable usage (voice minutes, AI detections) checks entitlements BEFORE the action runs; metering itself "never blocks or fails the request". Non-billable metrics are still metered for quotas, fairness and cost dashboards. | M12 | |
| DOC07.pvgis-energy-of-record | Energy source of record: PVGIS only — "PVGIS is the ONLY energy source"; solar-imagery flux layers are never requested; upstream unavailable → built-in latitude estimate with provenance 'estimate' and the estimate badge on energy and finance outputs; "design is never blocked. Money produced from an estimate carries the provisional provenance chain." | F8 | R5 detail; provenance line names the database ("Real · PVGIS ({database})" vs "Built-in estimate ±10%"). |
| DOC07.google-enhancement-only | Building insights (satellite roof data) is an "enhancement, never dependency": any failure "simply means the studio starts from manual tracing"; "No feature depends on Google Solar existing — India coverage is patchy and that is fine." | M04 | |
| DOC07.roof-detect-honesty | AI roof detection: DSM plane-fit primary, vision-model fallback under binding honesty rules — deterministic (temp 0), schema-enforced output, versioned prompt recorded as provenance, "no-guessing instructions (empty result beats invented roof)"; detection runs against the exact stored tile the canvas shows (1:1 pixel mapping); geometry cross-check floors confidence where a detected roof barely overlaps the building mask; both paths exit through artifact validation. Detection failure → user traces manually; missing key → feature hidden gracefully; "Never a hard error in the studio." | M04 | |
| DOC07.detect-billing-outcome | Detection billing: 1 unit per successful detect, either path — "the tenant buys the outcome, not the vendor"; entitlement check before dispatch; bundled per plan, metered beyond. | M12 | |
| DOC07.telephony-capabilities | Telephony is a capability-negotiated port family (ADR-0019): every adapter declares what it truly supports and "business logic branches on the declaration — never on the vendor name". Binding degradation ladder: a feature needing a missing capability "degrades on a defined path — never throws, never silently no-ops": warm transfer unavailable → cold transfer + push; IVR traversal without DTMF-send → "step skipped, call flagged ivr_blocked for human follow-up"; no agent available → callback queue → voicemail. | M07 | S5 operative facts: DTMF-send is declared but unprovided by the v1 adapter; 1600-series closed to non-BFSI → promotional outbound uses the 140-series route. |
| DOC07.routing-tenant-data | Routing and escalation are tenant DATA, not code: condition→action rules (AI confidence, customer-requests-human, intent, priority, business hours, VIP); escalation chains ring level-by-level with timeouts and an always-reachable final fallback; every handoff writes the pinned AI summary/context; "warm transfers deliver a TTS whisper summary before bridging, cold transfers push summary + deep-link". | M07 | |
| DOC07.byo-forwarding | Number provisioning: platform-provisioned number is the default and "the only outbound origin"; "BYO = the tenant's existing number forwarding inbound to their ExoPhone — outbound identity remains the ExoPhone. There is no porting/hosting of outbound CLI … and product copy must say 'forwarding'." | M07 | Supersedes docs/04's porting/KYC wording (see DOC04.byo-number). |
| DOC07.inbound-ivr | Inbound IVR is per-tenant flow config: greeting → menu → AI agent / ring group / voicemail, business-hours aware; the flow definition is ours, so a provider swap re-compiles rather than rebuilds. | M07 | |
| DOC07.compliance-gate | ComplianceGate is "ours, non-swappable" — "One concrete implementation, no alternate adapter, ever", because regulator penalties (₹25,000 per upheld complaint) land on the tenant. Enforced before EVERY dial: daily-refreshed DND scrub + tenant opt-out list; 9 am–9 pm promotional window + holiday calendar; transactional vs promotional series routing; keypress opt-out honoured ≤24 h; 90-day recording retention. Fail-closed: scrub data older than 24 h pauses promotional dialing (transactional continues); "There is no override flag." | M07 | D36-amended 2026-08-02: the MECHANISM is non-swappable; the statutory RULESET is per-market pack data (the above is the IN/TRAI-DLT ruleset). "A market with no voice ruleset in its pack cannot enable outbound voice." |
| DOC07.call-failure-honesty | Call failure behaviour: setup failure retries then reschedules per campaign rules; mid-call media drop → attempt one apology TTS, else hang up with outcome dropped — "call record always written"; provider outage pauses outbound and falls inbound to voicemail. | M07 | |
| DOC07.agent-orchestration | The call orchestrator — never vendor code — owns turn-taking, barge-in, "AI-disclosure ≤30 s", escalate-to-human, and outcome classification; STT/LLM stalls follow a defined ladder (filler → retry once → escalate-to-human or voicemail per tenant config). Agent config is versioned; queued calls use the queue-time version (D36). | M07 | |
| DOC07.messaging-manual | Messaging v1 is manual copy (D32): the app renders the localized message; the rep copies it (copy-to-clipboard + wa.me deep link); no send capability exists. v2 is BYO-WABA — the tenant owns their WhatsApp Business account and number, "assets survive a provider switch"; v2 send failure falls back to manual rendering ("the v1 path never leaves the codebase"). | M06 | Templates supply → F6 (DOC04.message-templates). M03 (BRIEF) may widen scope — record, don't resolve. |
| DOC07.payment-link-fallback | Tenant collections: BYO gateway per tenant, funds settle customer → EPC direct. Missing/invalid credentials → the tranche "falls back to manual-collection mode (record payment by hand — always available, cash is still king in EPC)". Missed webhooks are healed by status-poll reconciliation whenever a user views the tranche, plus a periodic sweep. | M11 | |
| DOC07.pdf-artifact | Document rendering: "The customer link always renders the proposal as web — PDF is an artifact, never the only path to the number." Render failure retries once, then fails with a user notification. | F5 | Also binds M06 (proposal PDF). |
| DOC07.push-best-effort | Push is best-effort by contract: the notification record is the source of truth; "the in-app inbox and badge derive from it, so a dropped push never loses information." | F6 | |
| DOC07.otp-delivery | Login OTP delivery: SMS primary with automatic WhatsApp-channel fallback on failure or 30 s timeout; rate limits 3 sends/number/15 min, 8/number/day, per-IP throttle; both channels fail → "login fails loudly with a retry-later message" — "No silent degradation on the front door." OTP is fair-use capped, never billed in v1. | M01 | |
| DOC07.map-tile-pinned | Satellite tiles are fetched once per site capture and stored: "the tile a design was traced on never changes under it" (stable 1:1 mapping for detection, reproducible provenance). Tile fetch failure → studio still opens with blank canvas + manual calibration (known-distance rescale); "Never blocks." | M04 | Also serves M05 (studio canvas). |
| DOC07.dem-honesty | Terrain data unavailable → "flat-terrain assumption with provenance 'assumed' and a visible warning on ground-mount outputs. Rooftop work never touches this port." | M05 | |
| DOC07.quotas | Per-tenant daily quotas on non-billable external fetches (solar data default 500/day, map tiles, DEM) — courtesy limits and margin protection. | M12 | |

## DOCFC — Forward-compatibility register (product-level rows)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOCFC.money-generic | Money is market-generic on every surface: amounts are generic `*_amount` values (never rupee/paise-named), the money-bearing document root stamps its currency, and "sums reconcile to the currency's minor unit". | F1 | Binding on every module (global-backend ruling 2026-08-02). |
| DOCFC.tax-generic | Tax is scheme-generic: `tax_pct`, `taxes[]`/`tax_breakdown`, `tax_registrations` — "never GST-named columns"; statutory extras (IRN/SAC) live in scheme-tagged structures. | F1 | |
| DOCFC.market-vocab | Market vocabularies (document checklists, payment modes, mandate types) are open sets validated against the tenant market's pack — "never closed pg enums". Canonical state machines keep market-neutral value names; "labels come from the pack". | F1 | |
| DOCFC.tz-scheduling | "User-facing repeatable jobs are tenant-timezone-aware, never fixed IST" (platform-internal sweeps may stay fixed-clock). | F1 | |
| DOCFC.new-market-gate | New market onboarding requires "a privacy/residency determination for that jurisdiction BEFORE tenants are created there", and selling subscriptions there needs "a supplier-of-record decision (owner-blocked)". | F1 | docs/08 §9 is the IN determination (part B). |
| DOCFC.rbac-deny-default | Roles are stackable M:N; RBAC runs through ONE deny-by-default guard — a surface declares the capability it needs and the guard resolves it from roles (D27 OR-across, D28 no per-person grants) — so a new role never requires a product-wide sweep. Identity: E.164 phone; deactivate-never-delete. | F2 | |
| DOCFC.read-export-exemption | The entitlement guard carries the read+export exemption by construction; proposal/project caps are counted over the billing-cycle window. | M12 | |
| DOCFC.crm-compliance-day1 | Customer records carry consent/DND/do-not-call/preferred-language from day 1 (the compliance gate reads them); lead sources include inbound_call; snooze/dormant timestamps exist; customers support multiple contacts and are merge-ready ("survivor re-pointing touches no money tables"). | M02 | |
| DOCFC.link-full-lifecycle | The customer link ships its full lifecycle plus label, contact attribution and OTP-at-accept from the start — "named links now in scope, used at launch". | F5 | Register says "Full A–F lifecycle" — the A–F stage letters are defined in the customer-journey ledger (C1–C13 framing). |

## Self-verification

- Row counts by section (grep-verified): DOC00 = 24 · DOC01 = 23 · DOC02 = 9 · DOC03 = 6 · DOC04 = 72 · DOC05 = 12 · DOC06 = 17 · DOC07 = 21 · DOCFC = 9. **Total = 193 rows.**
- Every row has a non-empty `target_doc`; targets use the prd/00-README.md document map (`01-product-overview`, `03-journey-map`, `04-business-model`, `F1`–`F8`, `M01`–`M13`).
- Overlay applied: all rows reflect docs/15 post-overlay state (D38 billing-in-v1, R6 named links + OTP in v1, R2 market-neutral stage names, D36 amended gate, directive 5 calendar supersession, directive 7/S5 telephony corrections).
- Contradictions recorded, not resolved: DOC04.byo-number (porting/KYC wording vs S5 forwarding-only), DOC04.ivr-flows (sendDtmf wording vs S5), DOC04.lead-sources (referral dormant vs R15 ships-v1), DOC00.nongoal-whatsapp-send and DOC00.nongoal-lead-channels (SRC non-goals vs M03 BRIEF scope).

<!-- docs-rules PART B — sources: docs/08, 09, 10, 11, 14, 16, architecture.md.
     Read through the docs/15 overlay (read fully before extraction): post-overlay state recorded;
     superseded material flagged in notes. Column convention: | key | rule | target_doc | notes |
     target_doc uses the Task-2 mapping tokens (F1–F8, M01–M13, 04-business-model, 00-README);
     for docs/10 the mapping "F3 + F7" is applied as: i18n rules → F3, design-system rules → F7. -->

## docs/08 — Security & Tenancy (product-visible rules)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC08.auth-phone-otp | Sign-in is phone + 6-digit OTP (SMS primary; WhatsApp-OTP retry channel for opt-in/failed delivery). OTP is single-use, 5-min TTL. No passwords exist anywhere in the product. | M01 | Post-overlay (R20): auth was removed to greenfield — this section is the DESIGN the rebuild implements, not a running system. Task brief adds Google Login (BRIEF origin); docs/08 specifies phone-OTP only — not a contradiction, but Google Login is absent from source docs. |
| DOC08.invite-attach | Invited employee: invite is keyed by phone; OTP verification attaches tenant membership + roles atomically. | M01 | |
| DOC08.session-lifetimes | Sessions: web 30 days rolling; mobile 90 days (field crews offline for long windows). Deactivating a user (or "sign out everywhere") kills every device within ≤10 minutes. | M01 | Revocation surface lives on the Team screen (F2). |
| DOC08.matrix.lead-visibility | Lead visibility scope: Owner = All · Manager = Team · Sales rep = Own · Surveyor = Assigned · Designer = Assigned · Engineer = Assigned. | F2 | |
| DOC08.matrix.add-edit-leads | Add and edit leads: Owner, Manager, Sales rep. | F2 | |
| DOC08.matrix.assign-leads | Assign leads to others: Owner, Manager. | F2 | |
| DOC08.matrix.delete-leads | Delete leads: Owner only. | F2 | |
| DOC08.matrix.capture-surveys | Capture site surveys: Owner, Manager, Sales rep, Surveyor. | F2 | |
| DOC08.matrix.create-edit-designs | Create and edit designs: Owner, Designer. | F2 | |
| DOC08.matrix.approve-designs | Approve designs (sign-off): Owner, Engineer. | F2 | |
| DOC08.matrix.create-edit-proposals | Create and edit proposals (incl. discounts, per D34): Owner, Manager, Sales rep, Designer. | F2 | |
| DOC08.matrix.send-proposals | Send proposals to customers: Owner, Manager, Sales rep. | F2 | |
| DOC08.matrix.update-project-stages | Update project stages: Owner, Manager. | F2 | |
| DOC08.matrix.record-payments | Record payments, upload documents: Owner, Manager. | F2 | |
| DOC08.matrix.configure-agent | Configure the agent and its knowledge: Owner only. | F2 | |
| DOC08.matrix.agent-performance | See agent performance: Owner, Manager. | F2 | |
| DOC08.matrix.manage-team | Manage team and roles: Owner only. | F2 | |
| DOC08.matrix.manage-catalog | Manage catalog and price book: Owner only. | F2 | |
| DOC08.matrix.manage-billing | Manage billing: Owner only. | F2 | Restored capability — D38 superseded (billing is v1). |
| DOC08.matrix.company-reports | See company reports: Owner, Manager. | F2 | |
| DOC08.six-roles | Six fixed stackable preset roles, 16 capabilities. | F2 | Role preset set is defined in F2 (Task 5); all tasks use those exact names. |
| DOC08.roles-or | OR across roles: permission granted if any held role grants it; visibility = WIDEST scope among held roles. | F2 | |
| DOC08.no-per-user-exceptions | No per-user permission exceptions, ever (D28). Permissions derive purely from roles; there is deliberately no per-user override in the data model. | F2 | |
| DOC08.deactivate-never-delete | Users are deactivated, never deleted. Deactivation revokes sessions and hides the user from assignment pickers; history keeps attribution. | F2 | |
| DOC08.min-owner | Always ≥1 Owner; always ≥1 person with Manage team. Enforced as guarded transitions, not UI-only. | F2 | |
| DOC08.midtask-permission | Mid-task permission loss: the current in-flight action completes; the restriction applies from the next action (no mid-flight error storms). | F2 | |
| DOC08.link-token | Customer never logs in (D5): tokenised links are unguessable, scoped, expiring, and revocable — a revoked/regenerated link dies instantly regardless of its own expiry. | F5 | |
| DOC08.link-named-otp | Named per-contact labelled links, per-link open attribution, and an OTP challenge on Accept above a tenant-set value threshold (default ₹10L) ship at launch. | F5 | Post-overlay: R6-amended — D33 (single link, accepted risk) is SUPERSEDED; the risk is closed at launch, not later. |
| DOC08.link-scopes | Link scopes: proposal.view, proposal.respond (Accept / Negotiate / Decline), progress.view, handover.view. One link per deal migrates through the lifecycle (journey C1–C13); effective rights = token scopes ∩ current link-row stage. | F5 | |
| DOC08.link-expiry | Link expiry: 12 months on view scopes. Re-minting from the Share screen issues a fresh token for the same link; the old token keeps working until its own expiry unless regenerate-with-revoke is chosen. | F5 | |
| DOC08.accept-revalidates | Accept is never trusted from the token alone: the server re-checks the proposal is the latest version and not stale (money-never-stale), the lead is not Won/Lost, and runs OTP-at-accept above the tenant threshold. Acceptance records full attribution (link, contact, OTP verification, IP, user agent) from day one. | F5 | |
| DOC08.open-tracking | Link-open tracking without PII: opens logged as link + timestamp + device class only; IP never persisted on the open event; public pages carry zero third-party scripts/fonts/analytics; no customer PII in any URL. There is NO "delivered" state — D32 honesty stands. | F5 | |
| DOC08.link-rate-limits | Public link rate limits: 60 views/hour per link; 5 respond-actions/hour; global public-route ceiling with backoff. | F5 | |
| DOC08.link-never-billing-blocked | Customer links are never blocked over the tenant's unpaid platform bill — links stay live in every billing state ("chase the person, don't punish the view"). | F5 | Restated in docs/16 §3. |
| DOC08.otp-limits | OTP anti-abuse (product-visible): requests 3 per 15 min and 8/day per phone; resend cooldown 45 s; 5 verify attempts then the OTP is invalidated; 3 consecutive invalidations lock the phone 15 min; invite sends capped 50/day per tenant. | M01 | |
| DOC08.otp-destination | Phone destination: +91 allowlist by default; other country codes enabled per market rollout — a global-ready switch, not a code change. | F1 | |
| DOC08.otp-copy | Every OTP SMS states the app name and "we never call to ask for this code" (anti-vishing); support never asks for an OTP. | M01 | |
| DOC08.dlt-templates | SMS traffic uses only DLT-registered entity/header/templates (IN statutory; unregistered traffic is carrier-blocked). | F1 | IN market pack rule. |
| DOC08.agent-lead-scope | The voice agent's context is scoped to the single lead matched by the verified caller number — no cross-customer retrieval capability exists; the knowledge base is per-tenant and read-only to the agent; the agent writes nothing beyond the call-outcome record. | M07 | |
| DOC08.compliance-gate | ComplianceGate is non-swappable and runs on every leg: DND scrub, 9am–9pm calling window + holiday calendar, AI disclosure within 30 s, keypress opt-out. | M07 | Post-overlay (D36 AMENDED 2026-08-02): the gate's MECHANISM is non-swappable; the statutory RULESET is per-market pack data (the listed rules are the IN ruleset). A market with no voice ruleset cannot enable outbound voice. Tenants configure within the law, not around it. |
| DOC08.recording-retention | Voice recording retention: 90 days, then hard delete; the transcript is retained on the timeline. | M07 | Also F1 (IN ruleset item). |
| DOC08.dpdp-roles | DPDP posture (IN market determination): platform is Data Fiduciary for tenant users' PII and Data Processor for the EPC's customer data (the tenant is fiduciary for their customers); DPA terms ride in the subscription agreement. Onboarding any new market requires that jurisdiction's own privacy/residency determination BEFORE tenants exist there. | F1 | |
| DOC08.residency | Residency: all PII/primary data in India; payment instruments never touch the platform (the gateway, an Indian PA, holds them — RBI localisation satisfied); object storage sits in Singapore under DPDP's negative-list transfer model, with a documented migration path if the list changes. | F1 | |
| DOC08.consent-records | Per-customer voice-call consent, recording consent, and DND/do-not-call flags with timestamps and source are stored, surfaced pre-dial, and exported on request. | F1 | Also M07 (surfaced pre-dial). |
| DOC08.data-rights-export | Access/export rights: tenant-level export always works regardless of billing state (product law). Individual data-principal export is a support-backed workflow — JSON/CSV of all rows keyed to the principal, 30-day SLA. | F1 | |
| DOC08.data-rights-correction | Correction happens in-app (lead/customer edit). | F1 | |
| DOC08.erasure-anonymise | Erasure = anonymisation, never row deletion: PII fields overwritten (name becomes "Erased", phone becomes a keyed hash preserving dedupe integrity) while financial/tax records (proposals, invoices, payments) are retained for the market pack's statutory period (IN: GST, 6+ years). User accounts follow deactivate-never-delete with the same anonymisation. | F1 | |
| DOC08.breach-grievance | Breach duty: notify the Data Protection Board and affected principals; a grievance contact is published in-app. | F1 | |
| DOC08.audit-coverage | Append-only audit log; the covered-events list is the acceptance checklist: auth events; team invite/role changes (old→new) incl. blocked last-Owner / last-Manage-team attempts; tenant settings, branding, catalog and price-book changes; money events (proposal generate/send/version, discount applied with amount + who, tranche edits, payment recorded, Won/Lost/Cancelled-after-Won); customer-link mint/re-mint/revoke/open/Accept-Negotiate-Decline with attribution; design sign-off approve/return with who + when (the engineer-led structural safety record); agent config changes (version id), knowledge-base edits, queue changes, DND/consent changes, escalations; billing plan changes, subscription transitions, entitlement overrides; credential lifecycle + every decrypt; data-rights requests and completions; admin/back-office access to tenant data. | F2 | Written server-side in the same transaction as the mutation. |
| DOC08.audit-tenant-export | Audit log retention 24 months hot, then archived; the log is tenant-scoped and tenants can export their own. | F2 | |
| DOC08.admin-impersonation | Admin/back-office impersonation is read-only and always audited. | F2 | |
| DOC08.credentials-last4 | Per-tenant BYO credentials are write-only from the tenant's side — the UI shows last-4 only; every decrypt is audit-logged. | M01 | Product-visible slice of the §7 encryption design. |

## docs/09 — Observability & Ops (product-visible only; the rest is engineering and skipped)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC09.credential-probe-nag | Tenant-facing integration credentials (BYO gateway / WABA) are probed on a schedule; invalid credentials surface as an alert plus a settings nag — never silent failure. Per-tenant credentials are the tenant's to rotate. | M01 | Status-honesty rule; the alerting plumbing itself is engineering. |
| DOC09.compliance-fail-closed | If the DND scrub goes stale, promotional dialing pauses FAIL-CLOSED (and the pause is alarmed) — compliance outranks revenue-generating dialing. | M07 | |
| DOC09.cogs-honesty | Per-tenant usage (voice legs, AI detections, OTP, storage) rolls up from the usage ledger next to plan revenue — "the margin check that keeps pricing honest". | 04-business-model | Internal-facing; included because it anchors the pricing-honesty posture of docs/01 and 16. |

## docs/10 — i18n & Design System (product-level rulings; build tooling skipped)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC10.canon | The vendored UX design-system package (design/ds-source) is the pixel-perfect source of truth: light, warm-neutral, Geist typography, near-black actions, iridescent atmosphere. "Instrument" graphite+brass is retired as the product identity. | F7 | Post-overlay: D3 SUPERSEDED; R19-E final. The POC design doc survives only as the interaction/a11y contract layer (now §11 = N-rules). |
| DOC10.light-only | v1 is LIGHT-ONLY (R19-A, final). "Light AND dark correct" is struck from the Definition of Done. The old "studio canvas stays dark" doctrine is dead — the studio is light. Dark mode later is a value-set drop into the alias layer, not a redesign. | F7 | |
| DOC10.mono-numerics | IDs, kWh readings, ₹ amounts, coordinates, invoice numbers, and phone numbers render in the mono face; numeric data is tabular; currency/quantities right-align in tables. | F7 | |
| DOC10.weights | Sanctioned font-weight set: 400 / 500 / 600 / 700 (600 sanctioned by ruling R19-D; the old "500 restricted to buttons/tabs/table-headers" clause is dead). | F7 | |
| DOC10.accent-systems | Two accent systems, never conflated: interactive accent (violet — focus rings, links, selected states, active tabs; NEVER a button fill) and the iridescent trio (atmosphere only — gradient/glow/icon-wash/AI cues). The primary action button is near-black, never coloured — the strongest identity marker. | F7 | |
| DOC10.no-borders | No structural 1px borders anywhere; hierarchy comes from luminance + soft shadow. Exceptions: dashed file-upload drop zones, and an opt-in HIGH-CONTRAST FIELD MODE — the sanctioned escape hatch for sunlight visibility on site. | F7 | Field mode is a product-visible capability. |
| DOC10.restricted-colours | Restricted colour roles (R19-C): text-tertiary is decorative/timestamps only, never load-bearing text; warning tone always sits on its tinted chip, never bare foreground text; disabled state is never the only signal. | F7 | Post-overlay 2026-07-30: TWO of ruling C's restrictions were RETIRED (not relaxed) — danger darkened to #D34247 and text-secondary to #686C71, both now AA; the caveats on those two tokens no longer describe anything. |
| DOC10.overline-exception | The overline (11px / 700 / uppercase / 0.12em tracking) is the SINGLE named exception to the 12px type floor (R19-B) — micro-labels only, never body, data, or interactive text. | F7 | |
| DOC10.status-not-colour-alone | Status is never conveyed by colour alone — always label + dot (domain status → semantic colour map). | F7 | |
| DOC10.data-colour-law | UI colour and DATA colour are separate systems (N6): never style a button with a data colour, never chart with the UI accent. Studio data palettes (roof identity, strings, irradiance) must be deuteranopia-distinguishable within each set, and every data-colour encoding pairs with a non-colour channel (label, pattern, or position). Data colours are never tenant-overridable — they carry engineering meaning. | F7 | |
| DOC10.brand-restraint | Neutrals are the product (95%); iridescence is atmosphere only — it never fills a button, row, chip or field. Brand/AI features use a gradient-filled object, not an outlined icon. Restraint is the premium signal: one accent gesture per screen. | F7 | |
| DOC10.overlay-scrim | Overlays blur the layer behind and fade it toward white — never a dark scrim. | F7 | |
| DOC10.focus-ring | Focus is always a single 2px accent ring at 2px offset, never removed; inputs are borderless and focus via elevation treatment; error state is an inset danger ring. | F7 | |
| DOC10.density | Two densities: Expressive (mobile, onboarding, dashboards, empty states) vs Functional (data tables, long forms, kanban, settings, admin) — same colours, type and rules; only spacing and radius change. | F7 | |
| DOC10.iconography | Icons: Lucide, outlined, 1.5px stroke; never mix filled and outlined in one context; filled variants only for the active bottom-nav item. No icon font, no emoji, no unicode-as-icon. | F7 | |
| DOC10.no-logo | No logo exists — the wordmark is plain Geist Bold. Do not invent a mark. | F7 | |
| DOC10.content-voice | Content rules: sentence case, plain, direct, short. Buttons are verbs ("Schedule survey") — never "Submit", "OK", "Click here". Errors state problem and fix. Dates as "12 Mar 2026"; numbers always carry units; ~68-char max prose line; no emoji. | F7 | |
| DOC10.photos | Photos are masked to 16–24px radius, never colour-filtered; no repeating patterns, no full-bleed photos. | F7 | |
| DOC10.numberfield-blur | NumberField commits on blur (explicit Done/Cancel on mobile keyboards) — never on keystroke. Numeric entry is the always-available precise path beside every gesture. | F7 | |
| DOC10.table-caption | Every data table requires a caption — the BOM and quote are commercial documents; no anonymous tables. | F7 | |
| DOC10.icon-label | Every icon-only control requires an accessible label — a missing label is a build failure, not a warning. | F7 | |
| DOC10.focus-management | Modals/sheets: focus moves in, wraps, and restores on dismiss — on both platforms. | F7 | |
| DOC10.tenant-branding | Per-tenant branding applies to CUSTOMER DOCUMENTS ONLY: proposal PDF + tokenised customer-link pages (tenant logo + primary brand colour). The operator app (web and mobile) is never restyled per tenant — no tenant CSS, no theme upload, no per-tenant palette. On palette save, contrast is re-verified computationally; palettes are never rejected — compliant shades are derived and previewed live. | F7 | The branding settings surface itself belongs to tenant config (M01). |
| DOC10.dod | Per-screen Definition of Done (a screen violating any single item is not done): works at 375px and 1536px with no horizontal scroll; all four states (loading/empty/error/offline); keyboard-operable with visible focus; contrast verified incl. restricted roles; every target ≥44×44 px; LIGHT theme correct; rendered in Hindi and checked (layout survives Devanagari + expansion); every user-visible number carries its provenance tier; density correct for the surface; zero raw hex / off-scale values; tested at realistic volume (200-lead list, 40-line BOM, 221-panel design); wired into the flows that reach it — no orphan screens. | F7 | Provenance item cross-refs F8 (R18/N7). |
| DOC10.studio-dod | The DoD applies unreduced to the 3D studio: it is the flagship, it is light, and full parity at 375px is "the hardest and least negotiable commitment in the system". | M05 | Also F7. |
| DOC10.n-rules | N1–N10 interaction/accessibility law survives unchanged as product law (numbering fixed — never renumber, never reword): N1 no hover-only meaning; N2 targets ≥44×44; N3 12px floor (overline exception); N4 contrast verified not eyeballed; N5 accessible names + focus trap/restore; N6 UI vs data colour; N7 provenance tier on every number (tiers canonical in R18 — no screen invents a fifth); N8 destructive actions confirmed AND undoable, undo thumb-reachable on mobile; N9 no layout tuned to a fixed viewport; N10 loading/empty/error/offline states are part of "done". | F7 | N7 → F8. Promoted verbatim from the POC brief 2026-07-30; cite docs/10 §11, never the POC file. |
| DOC10.touch-contract | Touch contract: build for pointer events, branch on capability, never on screen width. One canvas gesture vocabulary across satellite canvas, layout editor and 3D scene (1-finger pan, pinch zoom, 2-finger rotate, tap select, long-press 350ms contextual, drag with snap, 2-finger tap undo); never require wheel, middle-click or keyboard to reach a function. Precision under fingertip: loupe, offset dragging, snap-first-then-nudge, numeric entry always available, explicit Done/Cancel commit. Reachability: primary actions in the bottom third; destructive never adjacent to primary; undo persistently reachable while any canvas tool is active. | F7 | Also M05 (studio canvases). |
| DOC10.i18n-locales | UI languages EN/HI/MR from one catalog; language is per-USER, not per-tenant; switching re-renders the whole app immediately with no reload. A Marathi surveyor and an English owner coexist in one tenant. | F3 | |
| DOC10.i18n-fallback | Missing translations fall back to English at runtime — never a bare key, never a crash. | F3 | |
| DOC10.templates-are-data | Voice-agent and WhatsApp message templates are tenant DATA in all three languages — not translation-catalog messages. | F3 | Also M07/M01 (template management). |
| DOC10.devanagari | Hindi/Marathi render through a bundled Devanagari face (the brand face has zero Devanagari coverage; OS-fallback rendering is unacceptable). Layouts must survive ~20–30% Hindi/Marathi expansion: no fixed-width labels, no truncation of amounts or units, buttons size to content. | F3 | |
| DOC10.money-format | Money: ONE formatting function everywhere — Indian grouping (₹4,52,471; compact ₹92L, ₹1.4 Cr) in EVERY locale, on web, mobile, PDFs and voice-agent text alike. Money strings also obey provenance and never-stale rendering; ₹ amounts render mono, tabular, right-aligned in tables. | F3 | Cross-refs F8 (provenance/never-stale). |
| DOC10.units-not-translated | Units are never translated: kW, kWh, kWp, brand/model names, DISCOM names stay as-is in all locales; value+unit is unbreakable. m/ft follows user preference — EXCEPT procurement quantities, which stay metric. | F3 | |
| DOC10.latin-digits | Digits are always Latin 0-9 — never Devanagari numerals, in any locale, including documents. | F3 | |
| DOC10.dates-tz | Dates in "12 Mar 2026" style; default timezone Asia/Kolkata per tenant; no hand-rolled date strings. | F3 | |
| DOC10.add-language | Adding a language is a defined playbook (add locale, translate with English fallback, font/script check at the four weights, plural-rule check, money formatter unchanged by design, expansion check on the five densest screens) — no token change, no component change, no schema change beyond the locale enum. | F3 | |

## docs/11 — Scale Program (utility-scale product capabilities and phase commitments; no dates)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC11.range | Design range 1 kW → 100 MW without a rewrite. Every scale phase is investment INTO the studio moat, never a reason to cut studio capability. | M05 | |
| DOC11.regimes | Three scale regimes: Rooftop 1–500 kW (≤ ~1,100 modules, per-panel editing, flat ground); Large C&I 0.5–10 MW (≤ ~20k modules, block/table editing); Utility 10–100 MW (≤ ~175k modules, zone → block → tracker table, terrain-aware). | M05 | |
| DOC11.block-unit | Above rooftop scale the editable unit is the block / table / zone — NEVER the panel; panels become derived instances of a table (industry paradigm; per-panel at 150k was rejected as the wrong abstraction). | M05 | |
| DOC11.regime-per-design | Regime is a property of the DESIGN, not the tenant: a 100 kW rooftop and a 40 MW park can coexist in one tenant. | M05 | |
| DOC11.paradigm-switch | The studio switches editing paradigm at the block boundary: a design with blocks presents block/table tools; pure-rooftop designs keep the FULL per-panel tool census — every tool survives. | M05 | |
| DOC11.per-panel-in-table | Per-panel editing remains available INSIDE a table (remove-map pattern) — no census tool is lost; it is scoped, not deleted. | M05 | |
| DOC11.phase-a-in-build | The build ships the full studio plus scale Phase A (performance foundation, per-site projection, blocks/tables in the design payload from day one — large C&I designs work); Phases B/C continue afterwards as studio-moat investment. | M05 | Scope commitment; day numbers excluded per task rules (docs/14 is dateless anyway). |
| DOC11.phase-b-capabilities | Phase B commitments (Large C&I): draw a zone polygon → auto-fill with tables → drag/rotate/split blocks, per-block GCR and tilt, per-table delete/nudge, keepout subtraction — on-object direct manipulation, touch-first, works at 375px (design DoD unchanged); GPU-accelerated shading with a CPU fallback path retained permanently; server-side full simulation for batch/over-budget designs, with results stamped and staleness cleared on completion. | M05 | GPU/CPU paths pinned within ±2% on a golden scene — one engine version, two executors (honesty-relevant). |
| DOC11.phase-c-trackers | Phase C commitment: single-axis trackers with closed-form GCR backtracking (rows never self-shade at low sun), validated against the pvlib reference. | M05 | |
| DOC11.phase-c-terrain | Phase C commitment: terrain import from a free global 30 m DEM (GLO-30 baseline, SRTM fallback), draped mesh ground; tables sit on the terrain surface; flat ground remains the default when no DEM is loaded. | M05 | |
| DOC11.terrain-spacing | Terrain-aware row spacing: the winter-solstice shadow-free solver generalises to sloped ground at block granularity (N–S grade changes shadow-free distance). Cross-slope tracker articulation stays deferred with an explicit trigger. | M05 | |
| DOC11.block-electrical | Block-level electrical: the existing combiner architecture is extended (never replaced) with an inverter-block tier — central-inverter blocks; string inverters remain for ≤10 MW distributed designs; MV collection is stubbed as a LABELLED ASSUMPTION — no MV engineering claim. Reconciliation gates: Σ combiner inputs = total strings; Σ blocks = project total. | M05 | |
| DOC11.dxf-at-scale | Permit/DXF outputs at scale: zone plan, table rows with pitch dimensions, per-block electrical single-line, DEM contour underlay. Provenance tiers print on every sheet, unchanged. | M05 | |
| DOC11.deferrals | Explicit deferrals, each with a re-evaluation trigger: terrain-following tracker articulation (trigger: won utility deal with >3° cross-slope in a block, or ≥2 lost bids citing it); server GPU farm; WASM shading kernel; utility-grade electrical autorouting (MV/trenching — detailed-engineering territory, revisit only on demand for IFC-grade deliverables); WebXR/photoreal — "Never, absent a paying driver". | M05 | Non-goals with triggers. |
| DOC11.perf-budgets | Performance budgets are RELEASE CRITERIA per regime — a phase does not ship over budget. Headlines: full in-browser shading recompute ≤5 s rooftop / ≤30 s C&I (GPU) / ≤90 s utility; incremental edit → heatmap first paint ≤1 s / ≤3 s / ≤5 s; orbit ≥30 fps floors (mobile utility ≥24, view-prioritised); server sim ≤10 min p95 at utility. Measured on stated mid-range reference devices. | M05 | |
| DOC11.provenance-at-scale | Scale changes resolution, never honesty: provenance tiers go block-level, not away; a block aggregate inherits the WEAKEST tier of its members; DEM-draped ground is "derived" (public 30 m DEM), never "measured". | M05 | Cross-refs F8. |
| DOC11.shading-limits-printed | The shading model's documented limits stay printed at every scale: beam-only, linear-in-unshaded-area, no bypass-diode cliff, no string mismatch — partial-shade losses read OPTIMISTIC at 1 kW and at 100 MW alike. GPU acceleration changes speed, not the physics claims. | M05 | Cross-refs F8. |
| DOC11.structural-never-computed | Structural adequacy is NEVER computed — at any scale. Tracker foundations, pile embedment and wind loading on a 100 MW park are engineer-led exactly as a 3 kW rooftop is; structural verification remains a recorded human sign-off (who + when), and the disclaimer travels with every structure-bearing output, including block-level DXF sheets and MV assumptions. | M05 | Cross-refs F8. |
| DOC11.money-stale-at-scale | Money never renders while stale — and long recomputes make this bite: a utility recompute can take minutes as a server job; for that entire window the quote and BOM render as provisional, and proposal issue is blocked until shading and the money path reconcile. No express lane at scale. | M05 | Cross-refs M11 (money path) and F8. |

## docs/14 — Build roadmap (SCOPE COMMITMENTS ONLY; timelines/tracks/day numbers excluded by rule)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC14.scope-commitment | Everything in the product journey is committed scope: CRM, billing, catalog, proposals, customer links, remote + physical survey, voice agent, projects, dashboards, notifications, search, the bare-RN mobile app (iOS + Android), the 3D Design Studio port, and the offline layer. | 00-README | The doc is DATELESS by design (overlay §4 directive 5: timeline superseded; scope commitment stands). |
| DOC14.no-later-bucket | No Launch-2, no v1.1, no "later" bucket — the scope commitment survives the retired calendar. | 00-README | |
| DOC14.billing-v1 | Billing IS in v1 — trial-only, no free tier. | 04-business-model | Post-overlay: D38 superseded by owner override. |
| DOC14.activation-vs-build | Third-party approval clocks (SMS/DLT registration, telephony KYC/number provisioning, gateway live-key KYC, app-store review) gate ACTIVATION, not scope: all code ships regardless, and every gated capability has a documented fallback (test route / WhatsApp-OTP / internal distribution). | 00-README | Durations omitted per no-timelines rule. |
| DOC14.spec-locked-exclusions | Spec-locked exclusions (product decisions, not deferrals): WhatsApp sending integration (manual copy IS the design, D32); custom-role builder (D29); photo-derived measurement (D35); inventory/PO/O&M (D9); the referral credits LEDGER (R15 — the referral tag + "came from" chip DO ship); post-handover generation monitoring (C13). | 00-README | Mirror into module non-goals. |
| DOC14.studio-scale-scope | The build ships the full studio + scale Phase A (large C&I designs work); utility-scale enhancements (GPU shading, trackers, terrain) continue afterwards as studio-moat investment. | M05 | Duplicates DOC11.phase-a-in-build from the roadmap side. |
| DOC14.lockstep | Web + mobile lockstep (owner directive): a module with a mobile surface is not done until BOTH its web and RN screens ship, in the same slice, from the same contract. Mobile is never a follow-up task. | 00-README | |
| DOC14.merge-included | The customer merge flow is committed CRM scope (R8 pulled in per the no-v1.1 directive): re-point references to the survivor, mark the loser merged (never deleted), money tables untouched. | M02 | Post-overlay: R8-amended — merge SHIPS, not "later". |
| DOC14.csv-import | CSV lead import with column mapping + duplicate preview is committed scope. | M02 | |
| DOC14.referral-ships | Referral tag + "came from" chip are committed CRM scope (no monetary credits ledger — R15). | M02 | |
| DOC14.snooze-jobs | Leads ship with the full state machine plus snooze/dormant repeatable timers, dedupe at quick-add, assignment with rep load visible, qualification, timeline, tasks. | M02 | Restates R9/D14 from the roadmap side. |
| DOC14.coach-demo-seed | First-run coach marks (≤3, dismissible) and a demo Pune project seed are committed onboarding scope. | M01 | |
| DOC14.message-templates | Tenant message templates in 3 languages are committed scope. | M01 | |
| DOC14.quick-mode | Proposal Quick mode is committed scope. | M06 | Post-overlay: R11 committed. |
| DOC14.named-links-otp | Named per-contact links + OTP-at-accept above a tenant-set threshold are committed launch scope — closes the D33 ₹92L risk at launch. | F5 | Post-overlay: R6-amended. |
| DOC14.link-3g | The customer link obeys a 3G text-first rule (link pages usable on slow connections, text before weight). | F5 | |
| DOC14.byo-payment-links | BYO-gateway payment links (tenant's own account) are live in projects scope for tranche collection. | M11 | |
| DOC14.voice-capabilities | Committed voice capabilities: inbound IVR flows; number provisioning (platform number, or BYO as inbound forwarding — outbound caller-ID is NOT portable); cold transfer + pinned-AI-context handoff + callback queue live; warm transfer live only if provider-verified, else auto-degrades; single-level escalation live with chains as data; DTMF-send / IVR traversal degrades HONESTLY (the launch provider lacks the capability). | M07 | Post-overlay: §4 directive 7 AMENDED — "BYO = hosted/ported with KYC" and DTMF-over-stream wording SUPERSEDED; do not implement. |
| DOC14.notifications-search | Notification bell centre, push wiring, and app-wide global search are committed scope. | F6 | |
| DOC14.dashboards | Owner + rep dashboards and funnel/win-loss analytics are committed scope, under the D37 honesty rules (read-only, forecast never in won totals, correlation-not-attribution). | M13 | |
| DOC14.offline-scope | The offline layer is committed scope (last by dependency — nothing at launch depends on it): physical survey fully offline, photo attachments, sync-status UX; until it lands, mobile is online-first behind a repository abstraction. | F4 | Also §4 directive 10: a move in TIME only, never in scope or quality. |
| DOC14.launch-gate | Launch gate is all-or-nothing; product-visible gate items: money invariants (GST/discount/subsidy/tranche sums; proposal total = Σ tranches; BOM total = proposal total; money-never-stale badge verified); a full end-to-end walk in production (signup → lead → remote survey → design → proposal → link opened on a phone → OTP accept → project created → tranche received); billing webhook dedupe proven and read+export verified while entitlement-blocked; customer-link states A–F + edge states render; Hindi proposal PDF correct; 375px pass on every shipped screen; light theme correct; provenance tiers + structural disclaimer + Path-B label on every number-bearing surface; cross-tenant isolation proven; studio tool census 100%; offline survey round-trip proven on a real device in airplane mode; named-link OTP accept verified; voice agent live-call demo (or documented pending state with fallback evidence). | 00-README | |
| DOC14.census-quality-gate | The studio census gate is a QUALITY gate and does not move for the calendar; if the studio track runs long, the release valve is dashboards/polish scope elsewhere — NEVER census rows. The census never shrinks. | M05 | |
| DOC14.release-valves | The only sanctioned scope release valves are dashboard polish, funnel-analytics depth, and agent-performance screens — NEVER money invariants, tenancy isolation, census rows, offline round-trip, or provenance/honesty surfaces. | 00-README | Scope-protection rule (process framing stripped). |

## docs/16 — Billing & Entitlements (ALL product rules)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOC16.billing-in-v1 | Billing is in v1: trial-only, NO free tier. | 04-business-model | Post-overlay: D38 SUPERSEDED by product-owner override 2026-07-24. |
| DOC16.two-money-systems | Two money systems that never mix: (1) platform SaaS billing — the tenant pays us, our merchant account; (2) tenant customer-collections — the homeowner/factory pays the EPC via the tenant's own gateway account. The platform never touches tenant funds. | 04-business-model | Collections detail rows target M11. |
| DOC16.pricing-single-source | Prices, caps and bundle sizes are defined ONCE in docs/01 (business model); docs/16 fixes billing MECHANICS only — one definition per fact. | 04-business-model | |
| DOC16.org-pricing | Pricing is org-level and capacity-tiered — NEVER per-seat (Indian EPCs employ many low-cost designers; priced below both benchmarked competitors). | 04-business-model | |
| DOC16.unlimited-users | Unlimited users on every tier. Capacity/usage differentiates tiers — never features (owner-confirmed). | 04-business-model | |
| DOC16.capacity-levers | Tier levers: single-design kW ceiling; proposal-creation cap per month (30/300/1,500/unlimited); Starter active-project cap (10); voice-minute bundle; AI-detection bundle; storage GB — values live in docs/01. | 04-business-model | |
| DOC16.mandate-routes | Mandate route per tier band (IN-market policy, in the gateway adapter layer, never generic domain): all monthly tiers fit under the ₹15k UPI AutoPay debit cap → UPI AutoPay; Enterprise (custom) → e-NACH / invoice; ANY yearly variant (totals incl. 18% GST exceed ₹15k) → single payment link/invoice per year, no mandate; renewal is a fresh invoice. | M12 | Also F1 (IN rails). |
| DOC16.mandate-ladder | IN mandate ladder: UPI AutoPay primary → card e-mandate fallback → e-NACH for Enterprise. Annual = one-shot invoice, no mandate. Pre-debit notifications before each charge are the gateway's job — we build nothing, but dunning copy references them. | M12 | F1 for the rails. |
| DOC16.plan-objects | Every tier exists as two gateway plan objects (monthly + yearly) mirrored 1:1 by per-currency plan-price rows; OUR tables are the source of truth for entitlements, the gateway's for money. Cycle switches (monthly → yearly) follow the same mechanics as tier upgrades: immediate entitlements, prorated delta, new subscription at boundary. | M12 | Post-overlay (global-backend ruling): billing schema is provider-neutral, per-currency; one currency per tenant. |
| DOC16.trial | Trial: 14 days, EVERY tier capability, within the Trial caps defined in docs/01 (25 AI detections / 15 voice minutes / 5 GB storage). No card or mandate required to start — signup stays phone+OTP+company. One 7-day extension available to support. Trial is modelled in-app only; the gateway subscription is created at conversion. | M12 | Caps restated from docs/01 (the single source). |
| DOC16.lifecycle-states | Subscription lifecycle: trialing → (pay) active; trialing → (14d unconverted) expired [terminal]; active → (charge fails) past_due → (7 days unpaid) halted → (payment) active; active → (owner cancels) cancelled (runs to paid period end, then behaves as halted). History is append-only. | M12 | |
| DOC16.entitled-buffer | `active` means mandate live and current period paid; entitlement extends to period end + a 3-day buffer for webhook lag. | M12 | |
| DOC16.past-due-grace | past_due grace is 7 days: days 0–3 full function + banner; days 4–7 metered features pause. Only features that cost per-use money pause; core selling continues through the whole grace window. | M12 | |
| DOC16.trial-expired-terminal | `expired` (trial ended unconverted) is terminal and behaves exactly as halted: soft-block, data retained indefinitely. | M12 | |
| DOC16.reactivation | Reactivation (halted/cancelled/expired → active): owner pays from the always-available billing screen; a NEW gateway subscription is created (halted subscriptions are never resumed — one live mandate at a time); active immediately on confirmation. All data intact regardless of how long the tenant was halted. Reactivation is offered permanently. | M12 | |
| DOC16.soft-block-never-hard | Soft-block, never hard-block: blocked mutations return a typed entitlement-blocked error; the UI renders the state banner + "Reactivate" path. No data is EVER deleted for non-payment — deletion happens only via the DPDP erasure workflow. | M12 | |
| DOC16.softblock.always-on | Available in EVERY billing state (incl. halted, expired, cancelled): read everything, search, dashboards; export (CSV, data export, existing proposal PDFs); customer links (view AND respond) + customer progress pages; billing screens and pay/upgrade/reactivate. | M12 | Product law: read + export ALWAYS work; never punish the tenant's customer. |
| DOC16.softblock.core-gated | Gated only at halted/expired/cancelled(post-period): create/edit leads, tasks, activities, surveys; studio create/edit designs (read-only open ALWAYS works); generate/send proposals, mark won/lost, project updates; file/photo uploads. All of these work through the entire past_due grace window. | M12 | |
| DOC16.softblock.metered-pause | Voice agent (outbound + AI inbound) and AI roof detections: within plan/trial allowance while trialing/active/past_due days 0–3; PAUSED days 4–7; blocked from halted. | M12 | |
| DOC16.softblock.invites | Team invites (OTP spend) are blocked from past_due day 4 onward. | M12 | |
| DOC16.halted-inbound-degrade | When halted, inbound agent calls degrade to missed-call log + voicemail (no AI minutes burned). | M12 | Also M07. |
| DOC16.offline-drain-never-blocked | Offline-captured survey data still syncs and reads while blocked — the block is on NEW mutations from the UI, never on draining a field crew's already-captured queue. | M12 | Also F4. |
| DOC16.entitlement-truth | A successful charge is the source of truth for entitlement (extends the entitled window, writes the payment, triggers the tax invoice, clears dunning). Stale/out-of-order gateway events cannot regress state; a reconcile-by-poll backstop repairs any drift and every repair raises an alert — reconciliation should be boring. | M12 | Compressed from the webhook map + at-least-once pipeline; wire mechanics are engineering. |
| DOC16.pause-resume-not-offered | Subscription pause/resume is not offered in v1 (such gateway events are logged + alerted if ever received). | M12 | |
| DOC16.hosted-checkout | Checkout is the gateway's hosted flow from the billing screen; the platform never sees payment instruments (RBI localisation stays the gateway's problem). | M12 | Also F1. |
| DOC16.no-feature-flags | There are no feature flags in this product; features ship enabled when merged. The SINGLE runtime gate is billing entitlements: plan entitlements (ceilings/booleans) + usage allowances (metered bundles). | 00-README | Owner directive 8; also M12. |
| DOC16.gate.state-guard | Every UI mutation is gated by the billing-state matrix; on deny the user gets the typed blocked error + a reactivate CTA. | M12 | |
| DOC16.gate.design-kw | Design kW ceiling is enforced at design save/creation and proposal Generate — NEVER mid-edit in the studio ("the flagship is never interrupted per-keystroke"). On deny: save blocked with upgrade prompt; existing designs always open. | M12 | Also M05. |
| DOC16.gate.proposal-count | Proposal count/month is enforced at proposal create (plain count over the billing-cycle window): banner at 80%, 7-day grace at 100%; then new proposal creation pauses. Editing, sharing and duplicating EXISTING proposals — and all reads/exports — never pause. | M12 | Also M06. |
| DOC16.gate.active-projects | Starter's 10-active-project cap is enforced at mark-won → project create; "active" = not handed over/cancelled. On deny: mark-won blocked with upgrade prompt; existing projects stay fully workable — never strand a live installation. | M12 | Also M08. |
| DOC16.gate.voice | Voice outbound allowance is checked before queue insert AND again before dial (queue entries can outlive allowance); a blocked entry is marked and the owner notified. AI inbound over-allowance falls back to the human ring-group/voicemail per the tenant's IVR config. | M12 | Also M07. |
| DOC16.gate.ai-detection | AI roof detection allowance is checked server-side before the call; on deny, detection is blocked but MANUAL outline is always available (it costs nothing). | M12 | Also M04. |
| DOC16.gate.storage | Storage is gated at upload issuance only when the gauge exceeds ceiling × 1.1 (10% soft headroom); reads/exports never. | M12 | |
| DOC16.never-gated | Never gated, by law: reads, search, exports, customer links, billing screens, engineer sign-off on already-submitted designs (safety workflow), and offline sync drain of already-captured data. | M12 | |
| DOC16.usage-ledger | Usage metering is an append-only ledger — THE LEDGER IS THE BILL; no other counter exists. Every write carries an idempotency key so a retried job or duplicate webhook can never double-meter. Rollups are always reproducible from the ledger (same discipline as the money path). | M12 | |
| DOC16.metering-rules | Metering rules: voice minutes = one event per completed call (every call ledgered with cost breakdown); AI detections bill only when a result was returned — failures don't bill; OTP SMS is tracked for cost visibility but fair-use capped and NOT billed in v1; storage is a nightly gauge snapshot, not a counter. | M12 | |
| DOC16.usage-honesty | Usage-screen honesty: the tenant-visible usage screen shows exactly the rollups we enforce and bill from — same query, same numbers, no smoothing — labelled with the period and "measured" provenance. If a bundle is 80% consumed, the screen says so BEFORE the gate ever fires. | M12 | Cross-refs F8. |
| DOC16.overage | Overage (voice minutes / AI detections beyond bundle) is billed at published per-unit rates as add-ons on the next subscription invoice; the usage screen shows accruing overage in the tenant's currency with its market grouping (₹ lakh/crore for IN) as it happens. | M12 | |
| DOC16.gst-supplier-of-record | The platform is the supplier of record (the gateway is a gateway, not merchant-of-record): our GSTIN, our GST remittance, our liability. | 04-business-model | Also F1 (IN tax scheme). |
| DOC16.gst-invoice | A GST-compliant invoice is generated per billing cycle: our GSTIN, the tenant's GSTIN (captured at conversion — B2B tenants need it for input tax credit), place-of-supply logic (intra-state CGST+SGST / inter-state IGST), SAC 998434 (cloud/SaaS, 18% GST) for all tiers and overage add-ons. | M12 | F1: GST is the IN market's tax scheme; the canonical invoice row is scheme-neutral (currency, subtotal/tax/total, tax breakdown, scheme-tagged e-invoicing data). |
| DOC16.e-invoicing-threshold | e-invoicing (IRN) is not applicable until turnover crosses the ₹5-crore threshold; the gateway does NOT file IRNs for us; when crossed, IRN data attaches to the invoice record — nothing backfilled. | F1 | CA validates the threshold at each FY close. |
| DOC16.invoices-exportable | Invoices are exportable by the tenant in every billing state (read+export law). | M12 | |
| DOC16.byo-collections | Tenant customer-collections (advance + project tranches): each tenant connects THEIR OWN gateway account; payment links are minted on the tenant's account and funds settle directly to the tenant's bank; webhooks are verified with the tenant's own secret, idempotent, mark the tranche received and attach the receipt. The platform never touches funds — the load-bearing regulatory line (no payment-aggregator licence required). | M11 | Credentials stored encrypted per docs/08 §7; UI shows last-4 only. |
| DOC16.route-rejected | Split-settlement with the platform as master merchant (gateway "Route") is REJECTED for v1 — it would make us the KYC-bearing master merchant with escrow obligations; documented only as an alternate adapter. Revisit only if a marketplace revenue model appears. | M11 | Non-goal with trigger. |
| DOC16.manual-payment-modes | Tenants without a gateway account: manual payment modes stand — record a market payment mode (IN pack: UPI/NEFT/cheque) and attach the receipt. The payment-link port is an accelerator, not a dependency. Tranche math stays on the one money path (BOM ↔ proposal ↔ tranches ↔ project payments, to the currency's minor unit). | M11 | |
| DOC16.dunning-ladder | Dunning ladder from first failed charge: day 0 → past_due, banner + push + SMS ("payment failed, we'll retry — update method here"); day 2 reminder; day 4 metered features pause and the message states EXACTLY what paused and what still works; day 6 final warning with one-tap pay link; day 7 → halted, message confirms read+export+customer links still work; post-halt weekly ×4, then monthly — reactivation always one payment away. | M12 | |
| DOC16.dunning-channels | Dunning channels are the IN market pack's stack: in-app banner (owner + managers), push, SMS (DLT-registered templates), WhatsApp utility template where the owner opted in. Platform→tenant messaging is ours — D32 only constrains tenant→customer messaging. A future market's pack names its own channel stack. | M12 | Also F1. |
| DOC16.trial-nudges | Trial nudges reuse the dunning pipeline: day 7 ("half way"), day 12, day 14 (expiry). | M12 | |
| DOC16.dunning-honesty | All dunning copy is honest about state — no "your data will be deleted" threats, because it won't be. | M12 | Cross-refs F8. |
| DOC16.refunds | Refunds: 7-day money-back on the FIRST paid cycle only (removes post-trial conversion risk), refund-to-source, GST credit note auto-issued against the cycle invoice. Renewal cycles: no refunds — cancellation runs to period end. | M12 | |
| DOC16.upgrade | Upgrade: entitlements apply IMMEDIATELY (never make a paying customer wait); the prorated delta for the remaining cycle is charged as a one-time invoice; the plan swaps at the next cycle boundary. | M12 | |
| DOC16.downgrade | Downgrade takes effect at the next cycle; no mid-cycle refund. If current usage exceeds the lower tier's ceilings, the downgrade screen shows EXACTLY what will be blocked before confirming (honesty rule) — existing over-ceiling designs remain readable/exportable forever. | M12 | Cross-refs F8. |
| DOC16.cancellation | Cancellation: owner-initiated from the billing screen; reason captured (product signal, not a gate); service runs to the paid period end; data retained; reactivation always offered. | M12 | |
| DOC16.trial-no-conversion | Trial → no conversion: no charge ever occurred, nothing to refund; state becomes expired. | M12 | |
| DOC16.goodwill-credits | Support-issued goodwill credits are entitlement-override records (audited) — never manual DB edits. | M12 | |

## architecture.md (product-level only — the file is almost entirely engineering; two rows extracted)

| key | rule | target_doc | notes |
|---|---|---|---|
| DOCARCH.platform-parity | Every shared visual component exists as a web + native pair satisfying ONE shared API contract, shipped in the same change — cross-platform feature/visual parity is structural law, not aspiration. | F7 | Product-level reading of Law 7 / the ui + ui-api pairing. |
| DOCARCH.money-grouping | Money never renders with locale-default number formats — tenant-currency grouping always (₹ lakh/crore for IN). | F3 | Architecture restates the CLAUDE.md §7 rule as a hard boundary on the i18n layer. |
