# Extraction ledger — D-census (D1–D39)

**Source of the verbatim text:** `prd/_process/product-journey.md` § "Decisions locked" (lines 33–89, incl. the two table blocks and the D36 calling-rules block).
**Overlay applied:** `docs/15-spec-resolutions.md` §2 (D1–D39 conformance table), §3 (design-system rulings R19-A…E) and §4 (owner directives 1–10, R20, earlier owner decisions, user-decisions log).
**Reading rule (docs/15 §2):** a SUPERSEDED decision is dead — superseded D-text never enters a PRD as a live requirement. For superseded rows the text cell shows the original census wording struck through, followed by **POST-OVERLAY:** the operative state. For amended rows the census text is quoted verbatim and the status cell says exactly how the overlay amends it.
**Status vocabulary:** `live` · `superseded-by-R<n>` · `superseded-by-D<n>` (census-internal supersession, confirmed by docs/15 §2) · `superseded-by-owner-directive` (named) · `amended` (how, and by what).

| key | verbatim post-overlay requirement text | status | target_doc | notes |
|---|---|---|---|---|
| D1 | Residential **and** C&I, both high volume | live (docs/15: HONORED — segment flag through lead/proposal; 1 kW→100 MW range, docs/11) | M02 | Segment flag rides lead→proposal, so also touches M06; the 1 kW→100 MW design-range commitment lands with the studio scope (M05/docs/11) and §4 "earlier owner decisions". |
| D2 | Full mobile parity — every screen works at 375px, including the design studio | live (docs/15: HONORED — responsive web studio; RN app presents it via authenticated WebView) | F7 | Owner directive 9 restates: mobile presents the full-parity touch studio through an authenticated WebView; no studio feature dropped on any surface. |
| D3 | ~~Brand: "Instrument" — warm graphite + brass, ink label on brass fills~~ **POST-OVERLAY:** visual identity is the vendored UX design-system package `design/ds-source` — Geist typography, near-black primary `#0A0A0B`, violet accent `#5A4BFF`, iridescent atmosphere (`#7B5CFF`/`#3B82F6`/`#E85CBE`), light-only. No brass/graphite token survives anywhere, for any tenant. POC DESIGN-SYSTEM.md retained for interaction/a11y/product-law contracts ONLY (touch targets, no-hover-only, provenance, states, focus visibility). | superseded-by-owner-directive (vendored design-system package ruling, owner-confirmed pixel-perfect directive, 2026-07-24; docs/15 §3 R19-A…E — R19-E formally retires "Instrument") | F7 | §3 detail also binding: light-only v1 (R19-A, dark-studio doctrine dead), 11px overline named exception (R19-B), restricted roles for AA-failing hexes (R19-C), weight 600 sanctioned — set 400/500/600/700 (R19-D). |
| D4 | ~~WhatsApp is the primary customer channel; email secondary~~ **POST-OVERLAY:** no WhatsApp integration v1; manual copy-paste per D32 (Download PDF + Copy link, pasted into the rep's own WhatsApp; opens tracked via our link, delivery not). | superseded-by-D32 (docs/15 §2 confirms: SUPERSEDED — by D32, POC spec) | M06 | Already struck in the census itself. WhatsApp remains the channel customers actually use — the app just does not send on their behalf. |
| D5 | Customer never logs in — tokenised link only | live (docs/15: HONORED — stateless signed tokens, docs/08; named links + OTP ship v1 per R6-amended) | F5 | R6 ADDS per-contact labelled links + OTP-at-accept without breaking D5: still no customer login, no portal accounts. |
| D6 | Tailwind + Radix in code; Claude Design for screens | live (docs/15: HONORED — Tailwind v4 + Radix in `packages/ui`; Claude-Design mockup phase complete, gaps via docs/13) | F7 | Implementation-stack content — Global Constraints keep stack detail out of PRD bodies; record as design-system provenance only. Remaining screen gaps are designed at implementation time via docs/13 (UXG register). |
| D7 | Three audiences: company **owner**, **employees**, and the **EPC's customer** | live (docs/15: HONORED — two authenticated persona classes + anonymous link persona) | F2 | |
| D8 | A **voice agent** calls customers for follow-ups and answers inbound questions | live (docs/15: HONORED — Exotel+Sarvam per R3; inbound IVR per owner directive 7) | M07 | Directive 7 was AMENDED 2026-07-26 (ADR-0019): DTMF-send is a declared capability Exotel AgentStream does NOT provide — IVR traversal degrades honestly; BYO = inbound forwarding only, outbound CLI not portable; promotional outbound uses the 140-series RTM route. |
| D9 | v1 = **Sell + light project tracking**. Won deal → Ordered → Installed → Commissioned → Handed over, plus a document checklist and customer-visible progress. **No** inventory, POs, scheduling engine or O&M. | amended — scope half live (no inventory/POs/scheduling/O&M; docs/15: PARTIAL); the 5-state shorthand is superseded-by-R2: the FULL 9-stage chain is canonical (`WON → MATERIAL_ORDERED → DISPATCHED → INSTALLATION → ELECTRICAL_METERING → UTILITY_INSPECTION → COMMISSIONED → INCENTIVE_CLAIMED → HANDED_OVER` + `CANCELLED`, reason required) and the shorthand appears nowhere | M08 | R2 further AMENDED 2026-08-02 (global-backend): `UTILITY_INSPECTION`/`INCENTIVE_CLAIMED` are market-neutral enum names; "DISCOM inspection"/"Subsidy claimed" are IN market-pack LABELS; skippable stages are pack data; blocker party `utility` replaces `discom`. |
| D10 | Voice agent's **default** behaviour: follow up, answer FAQ, book callbacks/visits, gauge interest; by default it offers a human for price/discount rather than negotiating. ~~It may never discuss discounts or negotiate.~~ **The "never" is SUPERSEDED by D36** — discount/negotiation are now owner-configurable defaults, not hard rules. (Actual deal *acceptance* still happens when the customer taps Accept on the proposal link — C8 — not by a verbal agent agreement.) Every call is transcribed onto the lead timeline. | live (docs/15: HONORED — as tenant-editable defaults under D36; the struck "never" clause was already superseded inside the census) | M07 | Acceptance-only-via-link (C8) cross-references F5. |
| D11 | **Self-serve signup.** ~~Free trial; billing prompted later.~~ Billing is DEFERRED (D38) — signup asks only for the company + the owner; there is no trial gate or billing prompt anywhere in the current plan. | amended — signup half live; the billing-deferral half is superseded-by-owner-directive 4 (product-owner override 2026-07-24: payments/subscriptions/billing/entitlements/usage/gateway IN v1, trial-only, no free tier; docs/15 §2 D11: PARTIAL) | M01 | CONTRADICTION noted, not resolved here: the census's post-strike text still says "no trial gate… anywhere", but the post-overlay billing model is trial-only (docs/16, directive 4) — overlay wins; the billing half of this row belongs to M12. |
| D12 | ~~App UI **English**.~~ **POST-OVERLAY:** app UI is multilingual EN/HI/MR (D25). Surviving half: voice agent speaks **Hindi, Marathi, Gujarati, Tamil, Telugu + English**, chosen per customer — the agent language set (6) is unchanged and independent of UI languages (3). | superseded-by-D25 (docs/15 §2: SUPERSEDED — by D25 (UI EN/HI/MR); agent language set (6) unchanged) | F3 | R3 consequence makes the independence explicit: "the sets never converge by accident". |
| D13 | v1 lead sources: **manual quick-add, CSV import, inbound call via voice agent.** Website form and inbound WhatsApp are deferred — WhatsApp is outbound-only in v1. | live (docs/15: HONORED — website/WhatsApp remain "later" cards, UXG-03) | M02 | |
| D14 | Assignment is **manual, with each rep's open load visible** at the moment of assigning. No auto-routing rules in v1. | live (docs/15: HONORED — Assign screen designed at implementation, UXG-04; no auto-routing) | M02 | |
| D15 | Survey is a **task assignable to anyone** with the capability — rep or dedicated surveyor. One capture flow for both. | live (docs/15: HONORED) | M04 | |
| D16 | Customer sees **one recommended system** by default; the designer may add variants when the customer is price-sensitive or undecided. | live (docs/15: HONORED — `is_recommended`; variants compare screen, UXG-08) | M06 | Spans modules: variants are authored in design (M05), recommended/compare is customer-facing (F5); primary home M06 per S6 mapping. |
| D17 | Voice agent triggers **two ways**: automatically as a safety net (proposal unopened 3d · rep task overdue 2d · 3 failed manual attempts), **and** on demand when a rep hands a lead to it. | live (docs/15: HONORED — BullMQ repeatable jobs) | M07 | |
| D18 | After a call the timeline shows **outcome + one-line summary + interest signal**, with transcript and recording available on tap. | live (docs/15: HONORED — plus agent-config version per call (D36) and IVR-traversal markers (UXG-18) added by the overlay) | M07 | |
| D19 | ~~The owner approves every discount.~~ **POST-OVERLAY:** no discount approval flow at all (D34); the only guard is arithmetic — payable ≤ ₹0 blocks Generate, below-cost warns. | superseded-by-D34 (docs/15 §2 confirms: SUPERSEDED — no approval flow) | M06 | Already struck in the census itself. |
| D20 | **Reps see only their own leads.** Managers see the team's, owner sees everything. | live (docs/15: HONORED — role scoping + RLS backstop, docs/08) | F2 | |
| D21 | **Two ways to send a proposal: WITH a design, or WITHOUT one.** Both use the same 11-step proposal builder. A design pre-fills most of it; without a design the user types or AI-fills the same fields. See Stage 6B. | live (docs/15: HONORED — Path A/B, provenance differs per R18: derived vs estimated incl. Path B AI fill) | M06 | R11 commits Quick mode (steps 1/3/8/10 visible) as a sibling speed path in the same builder; R12: validation at Generate only. |
| D22 | **Components are MANDATORY on every proposal.** No lump-sum quotes. All 5 categories (Panel · Inverter · Cable · Electrical · Structure, + Battery when added) must be selected before Generate. **Component kits were considered and REMOVED (2026-07-21)** — speed comes instead from *duplicate an earlier proposal*, which carries its components, and from Path A filling them straight off the BOM. | live — amended on enforcement point by R12: the mandatory-components gate is a Generate-time check ONLY (free navigation; the Next-disabled rule is killed) (docs/15: HONORED — enforced at Generate, not per-step) | M06 | |
| D23 | **The design studio (Stage 5) and all 3D screens are LOW PRIORITY — design them last.** Everything else ships first: onboarding, CRM, survey, proposal builder, voice agent, close, project tracking. The studio already works in code; redesigning it is an improvement, not a blocker. | live (docs/15: HONORED — restored by owner directive 2026-07-24 rev 2 / directive 10: studio is the LAST track of the single build; the earlier same-day "studio-first" reversal was itself reversed by the owner; flagship status, tool-census gate and scope unchanged per directive 9) | M05 | Directive 10: last-in-TIME only, never in scope or quality. Directive 5 superseded the 20-day calendar — track dependency structure survives, day numbers do not. Build sequencing is process, not PRD scope (Global Constraints: no timelines/build phases) — carried for traceability only. |
| D24 | **The agent is configured through guided questions and a structured knowledge base, kept simple** — with a free-text box so the owner is never boxed in. Unanswered questions from real calls feed back as one-tap additions. ~~Everything about safety, honesty and compliance is locked by the platform.~~ **The "locked by platform" half is SUPERSEDED by D36** — the agent is fully tenant-configurable; the app ships safe defaults, the owner owns compliance. See "Tenant configuration". | amended — guided config + KB + unanswered-loop live; "locked by platform" half superseded by D36 (docs/15: PARTIAL). NOTE: D36 itself has since been amended — the statutory floor IS enforced (ComplianceGate), see D36 row | M07 | R10 pins the loop: review-queue only, no auto-training ever in v1; nothing a rep types reaches agent behaviour without explicit owner promotion. |
| D25 | **The app UI is multilingual: English, Hindi, Marathi.** Supersedes the English-only half of D12. Voice agent languages stay configurable per tenant, defaulting to the same three. Devanagari support is a design-system change, not just a translation task — see "Multilingual". | live (docs/15: HONORED — Lingui v5, per-user re-render, Devanagari chain, docs/10) | F3 | CONTRADICTION noted, not resolved: census says agent languages "defaulting to the same three", but docs/15 D12 row holds the agent language set at 6 and R3 keeps the sets independent — overlay wins: agent set (6) unchanged, tenant-configurable. |
| D26 | ~~Billing screens are MOCK for now.~~ **POST-OVERLAY:** real billing ships in v1; the D26-era mock is redesigned (UXG-13, docs/16). No mock billing surface exists. | superseded-by-owner-directive 4 (docs/15 §2: SUPERSEDED — by D38 (removed entirely), then by product-owner override 2026-07-24: real billing in v1) | M12 | Double supersession chain: D26 → D38 → owner override. |
| D27 | **Six fixed preset roles; one person may hold SEVERAL.** Permission granted if any held role grants it; lead visibility takes the widest. Solves the small-firm "one person does three jobs" case without a custom-role builder. | live (docs/15: HONORED) | F2 | Global Constraints: the fixed preset role set is defined once in F2; all modules use those exact names (DD3). |
| D28 | **No per-person permission exceptions, ever.** To know what someone can do, you look at their roles — one source of truth. Exceptions are how permission systems become unauditable. | live (docs/15: HONORED — permissions derive purely from roles) | F2 | |
| D29 | **Custom roles deferred to v2.** Ship the six, watch which combinations companies actually ask for, then add the presets they wanted — rather than guessing at a checkbox editor nobody fills in. | live (docs/15: HONORED; R16 confirms the installer gap: coordinator = Manager role runs the installation checklist in v1, optional free-text "done by" per step, crew login/Installer preset is v2 without schema change) | F2 | |
| D30 | **Survey has two modes: REMOTE (address → Google Solar API → AI roof detection, minutes, no travel) and PHYSICAL (on site).** Remote is the default first pass for residential and is enough to design and quote; the physical visit becomes verification before installation rather than a prerequisite for quoting. Remote data is labelled **derived from imagery**, physical data **measured on site**. | live — amended on the imagery source by R5: PVGIS is the energy source of record; Google Solar is an enhancement (Building Insights cross-checks, roof-detection input), never a dependency; Gemini is the roof-detect photo fallback (docs/15 D30: HONORED — remote-first residential; provenance split per R18) | M04 | R18 formalizes the labels as provenance tiers: remote = `derived`, physical = `measured`. |
| D31 | **Mobile navigation is an ARC BAR with an elevated centre**, not a flat five-tab rectangle. Slots: My Day · Leads · **➕ Add lead** (centre) · Projects · More. Centre is brass with an ink glyph and never changes per screen; the verb adapts by role (surveyor = Start survey). **Add this component to the Claude Design system** — see "Arc nav — addition" at the end of this file. | amended — arc-bar + role-adaptive halves live; the "brass centre" half is VOID with D3: the centre FAB is near-black `--action-primary` `#0A0A0B` on the white arc (docs/15: PARTIAL; mockup ground truth, ./research/ds-usage.md §3) | F7 | The "Add this component to the Claude Design system" sentence is historical process (mockup phase complete per D6 row; the trailing prompt library was removed from the vendored doc) — not a live requirement. |
| D32 | **No WhatsApp integration in v1.** The rep taps **Download PDF** and **Copy link**, then pastes both into their own WhatsApp. WhatsApp remains the channel customers actually use — the app just does not send on their behalf. **The link is ours, so opens ARE tracked**; delivery is not, because we do not control the sending. | live (docs/15: HONORED — `MessagingPort` ManualCopyAdapter; v2 BYO-WABA documented, docs/07) | M06 | |
| D33 | ~~C&I customers use the same single link as residential in v1. No per-contact links, no identity check, no portal accounts. Deferred deliberately — revisit when it hurts. ⚠️ Known accepted risk: anyone holding the link can tap Accept…~~ **POST-OVERLAY:** named links + OTP-at-accept SHIP in v1 — per-contact labelled links, per-link open attribution, and an OTP challenge on Accept (via `OtpPort`; MSG91 is the IN rail) above a tenant-set value threshold in the tenant's currency; `customer_links` carries `label` and nullable `contact_id`; acceptance records capture full attribution (link id, contact, OTP verification, IP, user agent) from day one. D33's accepted risk is closed at launch, not later. | superseded-by-R6 (R6 as amended by the 20-day everything-in directive, 2026-07-24; docs/15 §2: SUPERSEDED) | F5 | UXG-11 is in-scope, not deferred. Still no customer accounts/portal — D5 stands. |
| D34 | **No discount approval in this release** — supersedes D19. Anyone with permission to create a proposal can apply a discount and share it immediately. No request sheet, no approval queue, no "Pending approval" status. The only guard is arithmetic: a discount driving the client-payable figure to ₹0 or below is warned about and blocks Generate. Rationale: the approval hop was a known bottleneck and the permission to build a proposal already implies the commercial trust. Revisit if a tenant asks for per-rep discount ceilings. | live (docs/15: HONORED — payable ≤ ₹0 blocks Generate; below-cost warns) | M06 | The guard is a Generate-time check per R12. |
| D35 | **Survey photos are reference for the design, not measurement.** The survey captures and attaches photos of the roof, its obstructions and everything around the building — however taken: phone on site, customer-sent on WhatsApp, or a drone / other shot uploaded. Every photo is tagged and travels with the survey to the designer, who uses them when building the 3D proposal. What stays out of v1 is *deriving numbers* from photos (LiDAR, auto roof measurement, AR height) — a person still enters every dimension. This refines the earlier "drone out of scope" note: drone-as-imagery is fine; drone-as-automatic-measurement is not. | live (docs/15: HONORED — no LiDAR/AR/auto-measure; drone-as-imagery ok) | M04 | |
| D36 | **The voice agent is FULLY tenant-configurable — nothing is locked by the platform.** Supersedes the "compliance locked by the platform" half of D24. The owner sets and can change everything: name, voice, tone, languages, opening line, what it may discuss (including price), its whole knowledge, its hand-over rules, and its calling schedule. The app **ships India's calling rules as the starting defaults** — DND respected, 9am–9pm, and the agent disclosing it's automated — but ~~the owner can change or switch off any of them~~. ~~**The tenant owns compliance** (TRAI/DND and the rest); we provide safe defaults and one honest line, not a wall… **Accepted risk:** a tenant can configure calls that break local rules; that is their responsibility, surfaced once, plainly, not enforced.~~ **POST-OVERLAY:** the statutory floor is ENFORCED by our non-swappable `ComplianceGate`, not merely surfaced — tenants configure within the law, not around it. | amended (docs/15 §2 D36: PARTIAL — config fully tenant-owned stands; the "surfaced, not enforced"/"accepted risk" clause is dead. Further AMENDED 2026-08-02, global-backend ruling: the gate's MECHANISM is non-swappable; its statutory RULESET is per-market data from the market pack — TRAI/DLT (DND scrub, 9am–9pm, DLT series, opt-out, recording retention) is the IN ruleset; a market with no voice ruleset cannot enable outbound voice) | M07 | The IN statutory ruleset (and the "India's calling rules" defaults block, journey line 58) becomes F1 India market-pack data; M07 references pack keys per Global Constraints. Above-floor behaviour (tone, price talk, hand-over, wider-than-floor schedule within law) stays tenant-editable. |
| D37 | **Dashboards are the owner's periodic decision tool, deliberately separate from the task-driven daily screens.** My Day and the lists stay *tasks, not KPIs* (2.1) — a rep never opens a chart to know who to call. Dashboards are honest and action-oriented, not a vanity wall: every tile answers "what do I do about this?"; **forecast is a projection, never revenue** (weighted pipeline, labelled expected-not-promised); **won means signed**, and a deal cancelled after Won never silently keeps counting as revenue; **agent contribution is correlation, not attribution** (links to Agent performance, does not re-claim credit); reps see their own, managers their team, owner all (D20). No new metric is added unless it changes a decision. See "Dashboards & reports". | live (docs/15: HONORED — read-only, forecast never in won totals, correlation-not-attribution) | M13 | R2 detail: `CANCELLED` stops revenue counting immediately — the "cancelled after Won" clause is implemented via the 9-stage machine. Honesty-labelling aspects touch F8. |
| D38 | ~~**Billing & subscription are DEFERRED and planned separately — and no subscription restriction gates any feature in this plan.** Supersedes D26. There is no billing screen in the current build; every plan/usage limit, trial-expiry, suspension, seat cap, PRO-tier lock and "upgrade" prompt is removed…~~ **POST-OVERLAY:** payments, subscriptions, billing, entitlements, usage tracking and gateway integration are IN v1 — Razorpay (Subscriptions with UPI AutoPay primary, card e-mandate fallback, native trials; per R4), trial-only, no free tier (docs/16, ADR-0013). Billing entitlements are the ONLY runtime gating (directive 8 — no feature flags). Pre-commitment KEPT: "never hold a customer's data hostage" — read + export always work regardless of billing state. | superseded-by-owner-directive 4 (product-owner override 2026-07-24; docs/15 §2: SUPERSEDED) | M12 | R4's IN-market note: Razorpay is the IN rail behind provider-neutral ports; selling outside India needs a supplier-of-record decision — owner-blocked. Pricing tiers: docs/01 (benchmark below Reslink/ARKA/Aurora, §4 earlier owner decisions). |
| D39 | **The design studio is NOT being redesigned in Claude Design — it is kept and REFACTORED.** Supersedes the Phase 10 UX-redesign approach. The existing studio's geometry, engineering logic and code (energy, electrical, BOM, roof AI, structure, 3D) carry over as-is into the new production build; the work there is restyling it to the design system, adding the touch model, and hardening it to production — not designing it afresh. ~~`docs/phase-10-prompts.md` is retained as the **refactor checklist**~~ **POST-OVERLAY: canonical census is `prd/modules/M05-studio/studio-census.md`** (promoted 2026-07-30; the POC's phase-10-prompts.md is NOT canonical — directive 9). **The product also moves to a new repository, built from scratch as a real multi-tenant SaaS** (web + a real mobile app, shared TypeScript domain layer, Fly.io); this repo becomes the specification and the source of the reusable domain code. | live — amended on the checklist location by owner directive 9 (docs/15 §2: HONORED — this repo; tool census is the studio acceptance checklist) | M05 | Minor source tension, recorded not resolved: docs/15 §2 D39 row cites `./research/phases710.md §2` as the acceptance checklist while directive 9 (later, 2026-07-30) names `prd/modules/M05-studio/studio-census.md` canonical — Global Constraints adopt studio-census.md verbatim as M05's baseline, and the census never shrinks. Repo/stack detail (Fly.io, TS domain) is implementation-level, excluded from PRD bodies. |

## Self-verification

- Row count: 39 rows, keys D1…D39 (table sorted by key), no gaps, none `not-found-in-source` — all 39 are defined in the census.
- Status tally: **superseded 7** — D3 (owner-directive, §3), D4 (→D32), D12 (→D25), D19 (→D34), D26 (owner-directive 4), D33 (→R6), D38 (owner-directive 4). This exactly matches the journey header's own warning list: "D3, D4, D12, D19, D26, D33 and D38 in particular have all been overridden."
- **amended 7** — D9, D11, D24, D25, D31, D36, D39 (each names the amending authority and which half survives).
- **live 25** — the remaining rows (some carry non-supersession clarifying notes from their docs/15 rows, e.g. D22 Generate-time enforcement per R12, D30 PVGIS-of-record per R5, D23 owner-directive build-order history).
- 7 + 7 + 25 = 39.
- Every row has a non-empty `target_doc`.
