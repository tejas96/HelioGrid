# Feature inventory — step4-components (Step 4 · Components: panel / capacity / inverter selection + the compare matrix)

Pass-two inventory, 2026-08-05 (Sitting 4). Primary files (under `3d_design_studio/src/features/solar-studio/`): `screens/Step4Components.tsx` (960 ln) · `lib/capabilities.ts` (104 ln) · `data/catalog.ts` (57 ln) · `data/panels.ts` (22 ln) · `data/inverters.ts` (17 ln) · `data/profiles.ts` (111 ln).

Test run (read-only, deps from lockfile): `npx vitest run` on the 5 owned/adjacent test files → **5 files, 92 tests, all PASS** (vitest 3.2.7, 1.01 s): `catalog.test.ts` (7) · `profiles.test.ts` (32) · `bom-catalog.test.ts` (3) · `comparison.test.ts` (25) · `capabilities.test.ts` (25). Formula/recommendation values below were additionally reproduced live with `npx tsx` against the real modules (`recommendInverterFor`, `suggestKwpFromBill`, module-efficiency, catalog census).

One-hop libraries consulted but owned elsewhere: `lib/comparison.ts` (shortlist / candidate pipeline / recommendation — s8 comparison sitting; the Step-4-facing behaviours are inventoried HERE because Step 4 is their only UI), `lib/layout.ts` `estimateMaxCapacityKwp` + `fillRoofAsSegment` (s5), `lib/solar.ts` `suggestKwpFromBill` (s7), `lib/finance.ts` + `data/rules/india.ts` subsidy slab (s9), `lib/fingerprints.ts` `designFp`/`electricalFp` (s10), `lib/stringing.ts` `autoString`/`validateSystem` (s5), `components/ui.tsx` `Dialog`/`Seg`/`Sheet` (s8/shared), `store/store.tsx` `useProjectPatch` + undo stack (s10), `screens/Wizard.tsx` `nextBlocker` (s10).

**Ownership note.** `lib/capabilities.ts` (obstruction capability model) and `data/profiles.ts` (structural steel section catalog) are assigned to this area as *catalog/data* files, but their only UI carriers are Step 3 (obstructions) and Steps 5–6 (structure). Their rows are at the end (§H, §I) and are cross-referenced from `step3-obstructions.md` rows 18/32/34/35/36; the behaviours are stated here once, non-duplicatively, from the owner's side.

---

## §A — Step shell, accordion, gating

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step4-components.1 | Screen is a single 640px-max centred column (`padding 26px 20px 80px`) with three accordion sections in fixed order **PANEL → CAPACITY → INVERTER**, rendered as `SectionHead` buttons with labels "PANEL"/"CAPACITY"/"INVERTER" and step captions "Step 1 of 3"/"Step 2 of 3"/"Step 3 of 3". No Battery section exists anywhere in the screen or the type model | Step4Components.tsx:125-259,535-605; `grep -rni battery src/features/solar-studio` → 0 hits | working (census-faithful; **DD12/M05-37's Battery section is absent from the POC entirely** — no `BatterySpec`, no catalog entries, no UI) | SC.10-5.01 |
| CODE.step4-components.2 | Exactly ONE section is open at a time (`useState<Section>`, `setOpen` replaces). Initial open computed **once at mount**: `!panel ? 'panel' : targetKwp <= 0 ? 'capacity' : 'inverter'` — so a returning user lands on the first incomplete part | Step4Components.tsx:26,73-75,132,150,247 | working | SC.10-5.01 |
| CODE.step4-components.3 | Done-state per header: `state` string = `"{brand} {watt}W"` (panel) / `"{targetKwp} kWp"` (capacity) / `"{brand} {acKw}kW"` (inverter); when set the right rail shows a green `Check` (stroke 3), when null an amber dot + the words "Not selected". `aria-expanded` on each header; Chevron Up/Down | Step4Components.tsx:129,147,244,562-602 | working | SC.10-5.01 |
| CODE.step4-components.4 | Auto-advance on completion: choosing a panel opens CAPACITY; the capacity "Continue" button opens INVERTER. Sections are freely reopenable at any time by clicking any header — **the order is NOT enforced**: INVERTER can be opened with no panel and no capacity set (`onToggle={() => setOpen('inverter')}` is unconditional) | Step4Components.tsx:132,139,150,233,247 | working (§M05.6 "Behavior detail" says the accordion *enforces* the order on first pass; the POC only auto-advances) | SC.10-5.01 |
| CODE.step4-components.5 | Clicking the header of the already-open section is a **no-op** (`setOpen` to the same value) — there is no way to collapse a section back to the headers-only view, and `aria-expanded=true` never toggles to false from its own control | Step4Components.tsx:132,150,247,549-551 | POC-DEFECT(open section's disclosure control is inert; `aria-expanded` never returns to false) | SC.10-5.01 |
| CODE.step4-components.6 | Wizard Next gate for step 4 blocks with, in order: "Select a panel to continue" → "Select an inverter to continue" → "Set a target capacity to continue"; message surfaces as a 2.6 s toast. The same gate drives `allowedStep`, so steps 5+ are unreachable until all three are set. Next from step 4 jumps to **step 6** (auto-placement runs inside the editor mount), Back from 6 returns to 4 | Wizard.tsx:118-123,164-172,206-215 (one hop) | working | NONE (wizard-level; s10) |
| CODE.step4-components.7 | Every Step-4 edit except Compare→Apply is written with `patch(...)` at its **default `undoable = false`** — selecting a panel, typing a capacity, tapping Auto, selecting an inverter, changing the count, topology or MLPE push nothing onto the undo stack. Only `applyRow` passes `true` | Step4Components.tsx:79-81,84-97; store.tsx:471-474,204-207 (one hop) | working (census only requires Compare→Apply to be undoable; recorded because no undo/redo control exists on this screen either) | SC.10-5.39 |

## §B — Panel section (`PanelPicker`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step4-components.8 | Panel list source is `PANEL_DB` imported **directly from `data/panels.ts`**, bypassing the `resolveCatalog()` envelope that `data/catalog.ts` exists to be. Same for `INVERTER_DB`. The compare sheet's shortlist reads `resolveCatalog().panels`/`.inverters`, so picker and matrix read two different doors onto (today) the same array — a swapped/imported catalog would change the matrix and the BOM but not the pickers | Step4Components.tsx:18-19,120,626,803; comparison.ts:209,238,252 (one hop); catalog.ts:45-51 | POC-DEFECT(picker bypasses the catalog resolution point; M05-37 requires browsing to search the tenant's *resolved* catalog) | SC.10-5.03 |
| CODE.step4-components.9 | Selected-panel summary card (`--paper-2` background, only when a panel is set): brand bold; model 12px; a spec line `"{lengthMm} × {widthMm} mm · Voc {vocV}V · Vmp {vmpV}V · Isc {iscA}A"`; wattage large at 22px/800 with the caption "watts" right-aligned. `impA`, `tempCoeffVocPct`, price, warranty, weight, ALMM/DCR and availability are **not** shown in the summary | Step4Components.tsx:640-657 | working | SC.10-5.02 |
| CODE.step4-components.10 | Search: shared `SearchInput` (magnifier at left 11px, 9px/12px/9px/32px padding, radius 8, `aria-label="Search panels"`, placeholder "Search by brand or wattage..."). Match is a case-insensitive substring of the concatenation `brand + ' ' + model + ' ' + watt` — so "605" and "waaree top" both hit; tech, dimensions and price are not searchable | Step4Components.tsx:28-67,616,626-628,659-664 | working | SC.10-5.03 |
| CODE.step4-components.11 | Filter drawer toggled by a "Filter" button (`SlidersHorizontal`, `aria-expanded`); drawer is a `--paper-2` card holding: **Min Watt** / **Max Watt** number inputs in a 2-col grid (`aria-label` "Minimum wattage"/"Maximum wattage"), the technology chip row, then DCR and ALMM checkboxes | Step4Components.tsx:617,665-713 | working | SC.10-5.04 |
| CODE.step4-components.12 | Watt filter defaults **minW = 400, maxW = 650** — hardcoded literals, not derived from the catalog (bundled DB spans 440–625 W, so all 15 pass by default, but a future 380 W or 700 W entry would be invisible until the user notices the filter). Predicate is inclusive: `p.watt < minW \|\| p.watt > maxW` rejects | Step4Components.tsx:618-619,629 | working (hardcoded window recorded) | SC.10-5.04 |
| CODE.step4-components.13 | Emptying a watt field commits `Number('') === 0`: clearing **Min** widens to everything; clearing **Max** sets maxW = 0 and filters the list to zero rows. Negative values are accepted (no clamp, no `min` attribute on either input) | Step4Components.tsx:678-683,687-692,629 | POC-DEFECT(watt bounds unclamped; an emptied Max Watt silently empties the catalog) | SC.10-5.04 |
| CODE.step4-components.14 | Technology filter is a single-select chip row over exactly `['All Types','Mono PERC','TOPCon','Bifacial','Poly','HJT']` (`TECHS`), `aria-pressed` per chip, default "All Types" = no tech constraint; otherwise strict equality `p.tech !== tech` rejects. The six values are the census list verbatim and are the closed `CellTech` union plus the sentinel | Step4Components.tsx:607,620,630,696-705; types.ts:250 | working | SC.10-5.05 |
| CODE.step4-components.15 | **DCR** checkbox (default off): when on, keeps only `p.dcr === true` → 5 of 15 bundled panels (Adani ELAN Shine 540/545, Waaree Bi-55-550 Bifacial, Tata TP535, Premier PE-575). DCR is a plain boolean on `PanelSpec`, not scheme-keyed market-pack data | Step4Components.tsx:622,632,707-709; panels.ts:10-12,16,20; types.ts:275 | working (hardcoded IN scheme; M05-38 wants per-scheme filters declared by the market pack) | SC.10-5.06 |
| CODE.step4-components.16 | **ALMM** checkbox (default off): when on, keeps only `p.almm === true` → 12 of 15 (excludes Gautam GS-440 Poly, LONGi Hi-MO 7 625, Jinko Tiger Neo 615). Filters compose as AND with search, watt window and tech | Step4Components.tsx:621,631,710-712; panels.ts:17-19 | working (same hardcoded-scheme note) | SC.10-5.07 |
| CODE.step4-components.17 | Result count line above the list reads `"{list.length} panels"` at 11.5px `--ink-3`, recomputed from the same `useMemo` the list renders — always search + all filters, never the raw catalog size. Always plural ("1 panels") | Step4Components.tsx:624-636,715-717 | working | SC.10-5.08 |
| CODE.step4-components.18 | List rows (scroll region `maxHeight 320px`): 32×32 rounded brand mark showing `brand[0]` in violet (`#7c3aed` on `#7c3aed22`); title `"{brand} {model}"` 13px/700; sub-line `"{watt}W · {tech} · {lengthMm}×{widthMm}mm"`; then an **ALMM** badge (`badge-almm`) and/or a **DCR** badge (`badge-good`) where they apply. Selected row is tinted `#eff4ff` and carries `aria-pressed` | Step4Components.tsx:718-764; theme.css:273-303 | working | SC.10-5.09 |
| CODE.step4-components.19 | Rows render in raw `PANEL_DB` array order — there is no sort control and no default ordering rule (the array is grouped by brand, not by watt or price) | panels.ts:6-22; Step4Components.tsx:719 | working (no sort; not census-listed) | NONE |
| CODE.step4-components.20 | Choosing a row calls `setComponents({panel})` and opens CAPACITY. **No confirmation is shown even when panels are already placed** — `PlacedPanel` carries no dimensions, so every consumer reads the live `project.components.panel`; swapping the panel here silently resizes every placed module in situ with no re-fill, no setback re-check and no undo entry. The identical action taken through Compare→Apply *is* guarded by the replace-panel dialog | Step4Components.tsx:135-141; types.ts:515-529 (no dims on PlacedPanel); layout/shading/structure/sld/finance all read `project.components.panel` (17 call sites) | POC-DEFECT(direct panel swap bypasses the SC.10-5.40 guard the Compare path enforces, and is not undoable) | SC.10-5.40 |
| CODE.step4-components.21 | Zero-result state: the list area renders empty with only the "0 panels" count above it — no "no matches", no "clear filters" affordance, no reset control for the six filter inputs | Step4Components.tsx:715-764 (absence) | POC-DEFECT(no empty-result state or filter reset) | NONE |
| CODE.step4-components.22 | Filter + search state is `useState` local to `PanelPicker`, which **unmounts whenever another accordion section is open** (`{open === 'panel' && <PanelPicker/>}`) — reopening PANEL resets query to "", watt window to 400/650, tech to "All Types" and both checkboxes to off | Step4Components.tsx:134-142,616-622 | POC-DEFECT(filter state discarded on every section switch) | SC.10-5.03–SC.10-5.07 |
| CODE.step4-components.23 | **Route 2 — "enter specs manually"**: rendered as a non-interactive caption at the foot of the picker (`PencilLine` icon + the words "or enter specs manually"). It is a plain `div` — no button, no disabled control, no handler | Step4Components.tsx:765-779 | working (census-faithful "available-later" caption) — **diverges from M05-37/DD12, which rules all three entry paths present** | SC.10-5.10 |
| CODE.step4-components.24 | **Route 3 — "upload datasheet (PDF extraction)"**: same caption line, `FileUp` icon, literal trailing text "— mocked in POC". No file input, no upload handler, no extraction/review path exists anywhere in the codebase | Step4Components.tsx:765-779; no `input[type=file]` or PDF-extraction module in `solar-studio/` | working (honest available-later caption) — **no datasheet-upload path exists in code; M01-40's review-before-commit extraction is entirely absent** | SC.10-5.11 |

## §C — Capacity section

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step4-components.25 | Target capacity field: `<input type="number" min={0} step={0.1}>` with a right-inset "kWp" adornment (absolute, 12px, `--ink-3`), `aria-label="Target capacity in kilowatt peak"`, placeholder "0". `value = c.targetKwp \|\| ''` so 0 renders as the placeholder | Step4Components.tsx:154-180 | working | SC.10-5.12 |
| CODE.step4-components.26 | The field commits `Number(e.target.value)` raw: `step={0.1}` and `min={0}` are spinner/validation hints only, so a **typed** 5.37 or −3 is stored verbatim. A negative target reads back as "Not selected"/blocked (`targetKwp <= 0` everywhere) but is persisted, and each keystroke is a separate non-undoable store patch | Step4Components.tsx:163-167,79-81 | POC-DEFECT(typed value neither clamped to ≥0 nor quantised to the 0.1 grid SC.10-5.12 states) | SC.10-5.12 |
| CODE.step4-components.27 | **Auto** button (`Sparkles`, `btn-primary`): sets `targetKwp = estimateMaxCapacityKwp(project, panel).kwp` — sums `fillRoofAsSegment` over every roof with `avoidPanels: []` (raw roof capacity, ignoring what is already placed), then rounds **UP** to 0.1 kWp (`Math.ceil(count·watt/100)/10`) so `floor(kwp·1000/watt) === count` reproduces exactly in the auto-design and comparison budgets. Hint below: "Auto = maximum fit for your drawn roof after setbacks & obstructions." | Step4Components.tsx:181-195; layout.ts:677-697 (one hop) | working | SC.10-5.13 |
| CODE.step4-components.28 | Auto is `disabled` when `!c.panel \|\| project.roofs.length === 0`, but the only text on the control is `title="Estimate max capacity from your roof"` — its **purpose**, not the blocking reason. Neither "choose a panel first" nor "draw a roof first" is ever shown | Step4Components.tsx:183,188 | POC-DEFECT(disabled reason not shown; SC.10-5.13 / M05-39 require the reason) | SC.10-5.13 |
| CODE.step4-components.29 | Site-intelligence cross-check line (`--info`, 11.5px), gated on `location.solarInsights.status === 'ok' && maxPanels !== undefined`: "Cross-check: Google Solar estimates max **N panels** on this roof (at {panelCapacityWatts} W each — your module differs)." Information only — nothing downstream reads it | Step4Components.tsx:197-211 | POC-DEFECT(census/doc wording is "estimates max N panels (~X kWp)"; the POC prints the count and the reference watt but **never the kWp figure**) | SC.10-5.14 |
| CODE.step4-components.30 | Bill-based suggestion, shown when `info.monthlyBillInr && project.location`: a tappable banner (`Lightbulb`) reading "From your ₹{monthlyBillInr}/month bill we recommend ~{Y} kWp (tap to apply)"; tap or Enter sets `targetKwp = Y`. Y = `suggestKwpFromBill` = `round( (bill / max(1, tariffInrPerKwh)) / (peakSunHours · 30 · 0.8) · 10 ) / 10` — a hardcoded 0.8 performance ratio and a 30-day month (probe: ₹3,000 @ ₹8/kWh, 5 PSH → **3.1 kWp**) | Step4Components.tsx:99-106,212-229; solar.ts:247-255 (one hop) | working | SC.10-5.15 |
| CODE.step4-components.31 | Both suggestion banners (bill + recommended inverter) use `className="banner-warn"` — amber `--warn-bg`/`--warn` — so two *positive* recommendations render in the app's warning colour | Step4Components.tsx:214,813; theme.css:844-862 | working (semantic-colour divergence recorded) | SC.10-5.15, SC.10-5.17 |
| CODE.step4-components.32 | Both banners are `<div role="button" tabIndex={0}>` with `onClick` + `onKeyDown` on **Enter only** — Space does not activate them, and the ₹ amount is printed unformatted (no `en-IN` grouping, unlike every figure in the compare sheet) | Step4Components.tsx:215-228,814-824 | POC-DEFECT(Space key inert on a role=button; bill amount ungrouped) | SC.10-5.15, SC.10-5.17 |
| CODE.step4-components.33 | **Continue** (`btn-secondary btn-block`, `ArrowRight`) opens the INVERTER section and is `disabled` while `c.targetKwp <= 0` | Step4Components.tsx:230-237 | working | SC.10-5.16 |

## §D — Inverter section (`InverterPicker`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step4-components.34 | The DC numerator shared by the recommendation, the ratio read-out and the banner is `effectiveKwp`: 0 when `targetKwp <= 0`; the raw `targetKwp` when there are no roofs or no panel; otherwise the **`achievedKwp` of the comparison matrix's `isCurrent` row** (what the roofs actually hold toward the target), falling back to `targetKwp` when that is 0. One rule, deliberately shared with the matrix | Step4Components.tsx:108-116,255,797 | working | SC.10-5.41 |
| CODE.step4-components.35 | Recommendation rule (`recommendInverterFor`, called with phase = 3 when `info.connectionType === 'three'` else 1, and `maxCount = 4`): scan every catalog inverter × count 1..4, keep only DC/AC in **[0.90, 1.35]**, minimise \|ratio − 1.15\|, tie-break on lower `priceInr × count`; single-phase supply is restricted to 1φ units. Probe: 5 kWp/1φ → Growatt MIN 5000TL-X ×1 (₹52,000 beats GoodWe GW5000-NS ₹55,000 at the same ratio 1.00); 11.5 kWp/3φ → Solis S5-GC10K ×1 (ratio 1.15 exact, ₹99,000 beats Growatt MID 10KTL3-X ₹105,000); 100 kWp/3φ → Amaze 30 kVA ×3 | Step4Components.tsx:117-122; comparison.ts:141-162 (one hop); test comparison.test.ts:308-336 (3 PASS incl. the price tie-break pin) | working | SC.10-5.17 |
| CODE.step4-components.36 | Recommended banner, shown only while `recommended && !selected`: "Recommended: {brand} {model} ({acKw}kW[ ×{count}], {phases}φ) → DC/AC {ratio to 2dp} (tap to apply)". Tap/Enter applies **both** the inverter and the recommended count in two separate non-undoable patches (`onSelect` then `onCount`) | Step4Components.tsx:810-835,256-257 | working | SC.10-5.17 |
| CODE.step4-components.37 | When `recommendInverterFor` returns **null** — nothing in the catalog lands in the 0.90–1.35 band — the banner simply does not render and **no explanation is given**. Probe: 4.3 kWp on a 1φ supply → null (best available ratio 0.86 against the 3/5/6 kW single-phase steps); 40 kWp on a 1φ supply → null. The compare sheet has a `nearestInverterFit` fallback for exactly this case; Step 4's picker has none | Step4Components.tsx:117-122,810; comparison.ts:170-198 (fallback, matrix-only); tsx probe: `recommendInverterFor(4.3,1,INVERTER_DB) === null` | POC-DEFECT(silent no-recommendation state; the user sees an unannotated raw list) | SC.10-5.17 |
| CODE.step4-components.38 | Selected-inverter summary card: brand bold; model; spec line `"{mppt.count} MPPT · {mppt.minV}–{mppt.maxV}V · {phases}φ · η {efficiencyPct}%"`; kW rating large at 22px/800 with caption "kW". `maxDcV`, `mppt.maxCurrentA`, `stringsPerMppt`, price and warranty are not shown | Step4Components.tsx:836-851 | working | SC.10-5.18 |
| CODE.step4-components.39 | "No. of Inverters" row (in the summary card, above a `--line` divider): `<input type="number" min={1} max={10}>`, `aria-label="Number of inverters"`, 70px wide, right-aligned. Commit is `Math.max(1, Number(value))` — the **lower** bound is enforced but `max={10}` is a spinner hint only, so a typed 50 is stored and propagates to the ratio, the BOM and stringing | Step4Components.tsx:863-872 | POC-DEFECT(upper bound of the census's 1–10 range not enforced on typed input) | SC.10-5.19 |
| CODE.step4-components.40 | Live DC/AC read-out (only while an inverter is selected): `ratio = effectiveKwp / (acKw × count)`, printed to 2dp, with `> 1.35` → amber + `AlertTriangle` + "high (clipping risk)", `< 0.9` → amber + "low (oversized)", otherwise green + `CheckCircle2` + "healthy (0.90–1.35)". Band edges are inclusive on the healthy side (exactly 1.35 and exactly 0.90 read healthy) — the census boundaries verbatim | Step4Components.tsx:806,874-898 | working | SC.10-5.20 |
| CODE.step4-components.41 | Inverter search: shared `SearchInput` (`aria-label="Search inverters"`, placeholder "Search inverters..."), case-insensitive substring of `brand + ' ' + model + ' ' + acKw`. Result count line `"{list.length} inverters"`. List (`maxHeight 300px`): 32×32 emerald brand mark (`#059669` on `#05966922`) with `brand[0]`, title `"{brand} {model}"`, sub-line `"{acKw} kW · {phases}φ · {mppt.count} MPPT · {mppt.minV}–{mppt.maxV}V"`, and a `badge-good` **RECOMMENDED** badge on the recommended id. Selected row tinted `#eff4ff`, `aria-pressed` | Step4Components.tsx:802-805,901-957 | working | SC.10-5.21 |
| CODE.step4-components.42 | The inverter list has **no filter drawer** (search only) and is **not phase-filtered**: on a single-phase site every 3φ unit is still selectable, with no badge, no warning and no gate — the phase restriction lives only inside `recommendInverterFor`. List order is raw `INVERTER_DB` array order, which is not monotonic in kW (Amaze 30 kW is listed before Solis 25 kW) | Step4Components.tsx:803-805,913; inverters.ts:4-17 | POC-DEFECT(a 1φ supply can silently select a 3φ inverter from the picker) | NONE |

## §E — Topology & MLPE

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step4-components.43 | **DC Collection Topology** `Seg` radiogroup — "String inverters" (`'string'`) / "Central + combiners" (`'central'`), default `c.inverterTopology ?? 'string'`. Hint switches: central → "Strings parallel through fused combiner boxes (SCB) to a central DC bus — the BOM adds combiners + string fuses (C&I / ground-mount)."; string → "Strings land directly on inverter MPPTs (residential / small C&I)." Only `'central'` appends to `electricalFp`, so untouched projects keep a byte-identical fingerprint | Step4Components.tsx:262-276; types.ts:337; fingerprints.ts:145 (one hop) | working | SC.10-5.22 |
| CODE.step4-components.44 | **MLPE** `Seg` radiogroup — "None" (`'none'`) / "DC optimisers" (`'optimizer'`), default `c.mlpe ?? 'none'`. Hint switches: optimiser → "Every module tracks its own MPP — mixed orientation, tilt and shading may share a string… BOM adds one optimiser per module."; none → "Plain string wiring — a string may only hold modules of matching roof, orientation, tilt and shade exposure." Only `'optimizer'` appends to the fingerprint | Step4Components.tsx:278-291; types.ts:346; fingerprints.ts:146 (one hop) | working | SC.10-5.23 |
| CODE.step4-components.45 | Both selectors render **outside** the accordion — they sit under the INVERTER header as long as `c.inverter` is set, and stay visible while the PANEL or CAPACITY section is the open one. They belong to no section's done-state and are not summarised in any header | Step4Components.tsx:261-293 | working (structural divergence from the "once an inverter is chosen, two more choices appear" framing recorded) | SC.10-5.22, SC.10-5.23 |

## §F — Compare options (`ComparisonSheet`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step4-components.46 | "Compare options — energy, cost & payback" (`Scale`, `btn-secondary btn-block`) is `disabled` when `project.roofs.length === 0`, with `title="Draw a roof first (Step 2)"` as the reason. The reason is a native tooltip only — invisible on touch, and never rendered inline | Step4Components.tsx:296-304 | working (reason present; touch-surface gap recorded, same class as step3's `data-tip` directive gaps) | SC.10-5.24 |
| CODE.step4-components.47 | Opens a full `Sheet` (role=dialog, aria-modal, focus trap, Esc-to-close) titled "Compare options". Content = basis paragraph → warnings → the matrix → decision cards → fixed-assumptions footnote | Step4Components.tsx:305-320,355-367,527-531; ui.tsx:70-105 (one hop) | working | SC.10-5.24 |
| CODE.step4-components.48 | **Basis statement** (11.5px `--ink-3`), assembled live: objective — "Every option is auto-filled toward your {targetKwp} kWp target on the drawn roofs." vs "No target capacity set — every option is filled to maximum roof capacity."; then "Estimates assume unshaded panels, {PVGIS-measured irradiance \| estimated irradiance (±10% — PVGIS data not loaded)} and catalog prices (v{catalogVersion}{, representative data — not confirmed vendor quotes when provenance is `mock-representative`}); the placed design and your BOM edits may differ." Today that renders "v2026.07-1, representative data — not confirmed vendor quotes" | Step4Components.tsx:368-379; comparison.ts:385-391; catalog.ts:33-43 | working | SC.10-5.25 |
| CODE.step4-components.49 | Warnings list from `result.warnings`, each an amber `banner-warn`: "Draw a roof first — capacity is 0 for every option." (roofless) and "Your target capacity is smaller than a single panel — every option places 0 panels." (target > 0, roofs exist, every row placed 0) | Step4Components.tsx:380-384; comparison.ts:242,314-321 (one hop); test comparison.test.ts:188,214 | working | SC.10-5.25 |
| CODE.step4-components.50 | Candidate set = `shortlistPanels`: **ALMM-listed catalog panels ranked by ₹/W ascending, capped at 6**, with the current selection forced into slot 0 (even when non-ALMM). The rule string is returned verbatim for the decision card. Bundled ₹/W ties at 23.00 across AESOLAR 605/610/615, Adani 540/545 and Tata 535, so the shortlist is brand-clustered by construction. The **current row keeps the user's own inverter + count**; every other row gets `recommendInverterFor`, falling back to `nearestInverterFit` | comparison.ts:205-226,397-412 (one hop); tsx probe (₹/W ladder); test comparison.test.ts:270-306 (3 PASS) | working | SC.10-5.26 |
| CODE.step4-components.51 | Matrix is an 11-column `<table>` in an `overflow-x: auto` wrapper, 12px, `white-space: nowrap`, header row 10.5px `--ink-3`: OPTION · INVERTER · FITS · MODULE η · ANNUAL · NET COST · PAYBACK · 25-YR SAVINGS · WARRANTY · INSTALL · (action). The recommended row is tinted `#eff8f1` | Step4Components.tsx:385-402,406-412 | working | SC.10-5.26–SC.10-5.36 |
| CODE.step4-components.52 | **OPTION** cell: `"{brand} {watt}W"` bold, plus a `badge-good` **RECOMMENDED** when `row.key === recommendedKey` and a plain `badge` **CURRENT** when `row.isCurrent` (both can appear on one row); sub-line = model + `" · on order"` when `availability === 'on_order'` + `" · DCR"` when `dcr`. ALMM is not noted in the matrix even though the shortlist is ALMM-only | Step4Components.tsx:413-432 | working | SC.10-5.26 |
| CODE.step4-components.53 | **INVERTER** cell: `"{brand} {acKw}kW"` + `" ×{count}"` when count > 1, sub-line `"DC/AC {dcAcRatio}"` (2dp, `—` when null); `'—'` when no inverter could be fitted at all | Step4Components.tsx:433-445; comparison.ts:268 | working | SC.10-5.27 |
| CODE.step4-components.54 | **FITS** cell: `"{panelsPlaced} × {watt}W"` with sub-line `"{achievedKwp} kWp"` (2dp). Fill is budgeted `floor(targetKwp·1000 / watt)` panels spent across roofs in **array order** (deliberately not the raycast-ranked order auto-design uses — a synchronous compare must not raycast), `avoidPanels: []`, BOM overrides stripped, margin kept | Step4Components.tsx:446-449; comparison.ts:92-133,287; test comparison.test.ts:56-114 (4 PASS) | working | SC.10-5.28 |
| CODE.step4-components.55 | **MODULE η** cell: `round( watt / (lengthM × widthM × 1000) × 1000 ) / 10` % — 1 dp, STC area basis. Probe: Adani ELAN Shine 540 → 20.9 %; AESOLAR CMER-132BDS 605 → 22.4 %; Gautam GS-440 Poly → 19.9 % | Step4Components.tsx:450; comparison.ts:266,288 | working | SC.10-5.29 |
| CODE.step4-components.56 | **ANNUAL** cell: `round(report.annualMwh × 1000)` kWh, `toLocaleString('en-IN')`. Every candidate is computed with `solarAccess = 1` (unshaded) because the raycast engine never runs on a hypothetical layout — identical treatment across rows, stated in the basis line | Step4Components.tsx:451-453; comparison.ts:8-11,258,289 | working | SC.10-5.30 |
| CODE.step4-components.57 | **NET COST** cell: `₹{netCostInr}` en-IN, with a green sub-line `"after ₹{subsidyInr} subsidy"` when the subsidy is > 0. Subsidy = PM Surya Ghar slab (₹30,000/kW first 2 kW + ₹18,000 for the 3rd, capped ₹78,000 at ≥3 kW), residential-only and **DCR-gated** — a non-DCR module scores ₹0, which is what separates the DCR rows in the matrix | Step4Components.tsx:454-461; finance.ts:14-25,34-39; rules/india.ts:431-438 (one hop) | working | SC.10-5.31 |
| CODE.step4-components.58 | **PAYBACK** cell: `'—'` when `panelsPlaced === 0`, else `"{paybackYears} yr"` (1 dp). Payback iterates 25 years with 3 %/yr tariff escalation and annual degradation, interpolating within the crossing year; `netCost <= 0` pays back at 0 and a never-crossing case pins at 25 | Step4Components.tsx:462-464; finance.ts:41-57 | working | SC.10-5.32 |
| CODE.step4-components.59 | **25-YR SAVINGS** cell: `₹{savings25YrInr}` en-IN, sub-line `"{roi25Pct}% of cost"` when non-null. ROI is derived in the matrix (`round(savings25 / netCost × 100)`, null when netCost ≤ 0) because `FinancialSummary` has no ROI field | Step4Components.tsx:465-470; comparison.ts:59-60,296-297; finance.ts:58-64 | working | SC.10-5.33 |
| CODE.step4-components.60 | **WARRANTY** cell: `"{panel.warrantyYears ?? '—'}[ / {inverter.warrantyYears}] yr"`. Bundled panels carry 25/27/30 yr and inverters 3/5/10 yr, so today it always renders a pair; a user-imported spec without the optional field degrades to "— / 5 yr" | Step4Components.tsx:471-474; panels.ts, inverters.ts; test catalog.test.ts:38-39,68-69 | working | SC.10-5.34 |
| CODE.step4-components.61 | **INSTALL** cell: complexity band from panel count — **low ≤ 12 · medium ≤ 30 · high > 30** — with sub-line `"~{arrayWeightKg} kg"` = `round(panel.weightKg × panelsPlaced)`, omitted when the spec has no `weightKg` | Step4Components.tsx:475-480; comparison.ts:228-230,309-310 | working | SC.10-5.35 |
| CODE.step4-components.62 | **Action** cell, three mutually exclusive states: `!stringFeasible` → amber "⚠ check" (no Apply — an infeasible option cannot be applied at all); else `isCurrent` → green "✓ selected"; else an **Apply** button. Below it, `feasibilityNote ?? warnNote` in amber 10px/170px-wrapped. `stringFeasible = panelsPlaced > 0 && inverter != null && validateSystem has no error` | Step4Components.tsx:481-510; comparison.ts:261-265,299 | working | SC.10-5.36 |
| CODE.step4-components.63 | Feasibility/warn note taxonomy, all rendered verbatim: 0 panels + sub-panel target → "Target {X} kWp is below one {W} W module — raise the target"; 0 panels otherwise → "No panels fit the drawn roofs"; no inverter → "No inverter lands in the 0.90–1.35 DC/AC band for this capacity"; else the first `validateSystem` error. Warn side: the first `validateSystem` warn, else (for fallback fits sitting in validateSystem's 0.80–0.90 silence zone) "DC/AC {r} is outside the recommended 0.90–1.35 band — nearest catalog fit" — a warn never hides | Step4Components.tsx:497-509; comparison.ts:269-308; test comparison.test.ts:226-260 (2 PASS) | working | SC.10-5.36 |
| CODE.step4-components.64 | Recommendation rule for the matrix (distinct from the inverter picker's): eligible = `stringFeasible && (dcAcRatio == null \|\| ≤ 1.35)` — **clipping fits can never win**, because the energy model has no clipping term — sorted by lowest payback, then higher 25-yr savings, then lower net cost. Winner's key becomes `recommendedKey` | comparison.ts:323-335 (one hop); test comparison.test.ts:149-161,226-249 | working | SC.10-5.26 |
| CODE.step4-components.65 | **Decision cards** below the matrix, one `card` each with topic (12.5px/800), choice (`--ink-2`), reason (`--ink-3`), and inputs joined by " · " in `--mono` 10px. Always: *Comparison basis* (inputs `targetKwp=…`, `catalog=2026.07-1 (mock-representative)`, `irradiance=PVGIS\|estimate`, `marginPct=…`); *Which options are compared* (the verbatim shortlist rule); and, when a winner exists, *Recommended: {brand} {model}* with payback/net/annual and the next-best comparison plus the stated tie-break order | Step4Components.tsx:517-526; comparison.ts:338-379; test comparison.test.ts:162-172 | working | SC.10-5.37 |
| CODE.step4-components.66 | **Fixed-assumptions footnote** (10.5px `--ink-3`): "Fixed assumptions shared by every row: 3%/yr tariff escalation, 25-year horizon, your saved margin, PM Surya Ghar subsidy rules. Warranty shown as panel / inverter years." The 3 % and the 25 years are hardcoded in the JSX **and** independently in `finance.ts` (`const escalation = 3`, `y <= 25`) — two literals that can drift; the subsidy scheme name is hardcoded rather than pack data | Step4Components.tsx:527-530; finance.ts:45,50,59 | working (duplicated literal + hardcoded scheme name recorded; M05-41 wants the scheme to be F1 pack data) | SC.10-5.38 |
| CODE.step4-components.67 | **Apply** → `applyRow`: one `patch({components: {…c, panel, inverter, inverterCount}}, true)` — the only `undoable = true` call on the screen, so panel + inverter + count move as a single undo step. `targetKwp` is deliberately not touched. Rows with `row.inverter == null` are refused at both entry points | Step4Components.tsx:83-97,307-317 | working | SC.10-5.39 |
| CODE.step4-components.68 | **Replace-panel confirmation**: when `project.panels.length > 0 && row.panel.id !== c.panel?.id`, Apply stages the row in `pendingApply` and opens a `Dialog` "Replace the panel?" (`AlertTriangle`) reading "{N} panels are already placed for the {brand} {watt} W module. Switching to the {brand} {watt} W module outdates that layout — re-run Auto-fill in the editor afterwards." Actions: "Keep current" (discards) / "Replace panel" (applies). Applying only the inverter/count on the same panel is not gated | Step4Components.tsx:307-317,321-350 | working | SC.10-5.40 |
| CODE.step4-components.69 | The compare sheet stays open after Apply and after the confirm dialog (`onClose` is never called from the apply path). Because `memoizedComparison` re-keys on `designFp`, the sheet recomputes in place and the applied row flips to CURRENT/"selected" live | Step4Components.tsx:306-320; comparison.ts:425-432 | working | SC.10-5.41 |
| CODE.step4-components.70 | Everything derived recomputes per render from the store — module η, annual kWh, net cost, subsidy, payback, 25-yr savings, ROI %, DC/AC ratio, array weight, install complexity, `stringFeasible`, the recommended inverter + count, `effectiveKwp` and the bill suggestion. The matrix is memoised on `designFp` (last-value, single slot), which carries the catalog version since Phase 22d | Step4Components.tsx:99-122,364; comparison.ts:414-438; fingerprints.ts:131-147,219; test comparison.test.ts:337-360 (3 PASS incl. catalog-version invalidation) | working | SC.10-5.41 |
| CODE.step4-components.71 | `targetKwp` is part of `electricalFp` → `designFp`, and `effectiveKwp` calls `memoizedComparison` **during render**. Every keystroke in the capacity field therefore invalidates the memo and re-runs the full 6-candidate pipeline (fill → autoString → energy → BOM → financials) synchronously in the render path | Step4Components.tsx:111-116; fingerprints.ts:131-147; comparison.ts:233-312,425-432 | uncertain (code-analysis: cost unmeasured on large roofs; the 25-test comparison suite runs in 151 ms on small fixtures) | SC.10-5.41 |

## §G — Catalog envelope & component databases

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step4-components.72 | `CatalogEnvelope` = versioned wrapper `{catalogVersion, effectiveFrom, provenance, entryProvenance, panels, inverters, pricebook}` over the bundled DBs. Active values: **version `2026.07-1`**, **effectiveFrom `2026-07-01`**, provenance **`mock-representative`**, `entryProvenance: {}`. `resolveCatalog()` is the single resolution point (Phase 10 is to swap it for "active imported version, else bundled") | catalog.ts:19-51; test catalog.test.ts:13-22 (2 PASS: same array identity, version regex `^\d{4}\.\d{2}-\d+$`, parseable date) | working | NONE (envelope infrastructure; M01-32/33/38 side) |
| CODE.step4-components.73 | `CatalogProvenance` union = `'manufacturer-datasheet' \| 'installer-pricebook' \| 'mock-representative'`; `catalogProvenance(entryId)` falls back to the catalog-level label. **The function has zero non-test call sites**, `entryProvenance` is never populated, and `effectiveFrom` is never rendered anywhere — the only provenance the user sees is the catalog-level one in the compare basis line | catalog.ts:13-17,53-57; `grep -rn catalogProvenance src/` → only catalog.ts, its test, and the unrelated `basis.catalogProvenance` string in Step4; test catalog.test.ts:106-108 | POC-DEFECT(per-entry provenance and effective date are dead — honest-labeling surface exists in the model but not in any UI) | NONE |
| CODE.step4-components.74 | `resolveCatalog()` consumers: `lib/comparison.ts` (shortlist + candidate inverters), `lib/bom/context.ts` (pricebook) and `lib/fingerprints.ts` (`designFp` carries `catalogVersion`, so a price-book revision re-keys every design and stales its captures). Pinned by `bom-catalog.test.ts` (3 PASS: "a swapped catalog changes the quoted rate", "reaches the module-level sloped-hardware table too", "a price-book revision re-keys the design") | catalog.ts:49-51; comparison.ts:209,238; bom/context.ts:256; fingerprints.ts:219; test bom-catalog.test.ts (3 PASS) | working | NONE |
| CODE.step4-components.75 | `PANEL_DB` — **15** representative Indian-market modules: 440–625 W; tech mix TOPCon 7 · Mono PERC 4 · Bifacial 2 · Poly 1 · HJT 1; **12 ALMM**, **5 DCR** (all 5 also ALMM); availability `in_stock` ×12 / `on_order` ×3 (RenewSys DESERV 590 HJT, LONGi Hi-MO 7 625, Jinko Tiger Neo 615); prices ₹8,800–₹15,340; warranty 25/27/30 yr; weight 23.0–32.0 kg. Brands: AESOLAR ×3, Adani ×2, Waaree ×2, Vikram, RenewSys, Tata Power Solar, Gautam, LONGi, Jinko, Premier Energies, Goldi | panels.ts:6-22; tsx probe (counts verified live) | working | SC.10-5.09 |
| CODE.step4-components.76 | Every `PanelSpec` field: `id, brand, model, watt, tech, lengthMm, widthMm, vocV, vmpV, iscA, impA, tempCoeffVocPct` (required) + `tempCoeffPmaxPct?, almm, dcr, priceInr, warrantyYears?, weightKg?, availability?`. **`tempCoeffPmaxPct` is absent from all 15 bundled entries**, so string sizing falls back to the market rule and labels the voltage window "estimated" (the code explicitly forbids reusing `tempCoeffVocPct` for Vmp) | types.ts:252-290; panels.ts:7-21 | working | SC.10-5.02 |
| CODE.step4-components.77 | Panel schema gate (`catalog.test.ts`, PASS): unique ids · watt > 0 · lengthMm > widthMm · vocV > vmpV · iscA > impA · tempCoeffVocPct < 0 · priceInr > 0 · **\|Vmp·Imp − watt\|/watt < 3 %** · warrantyYears ∈ [10,40] · weightKg ∈ [15,40] · areal density 8–14 kg/m² · availability ∈ {in_stock, on_order} | catalog.test.ts:24-49 (1 PASS over all 15) | working | NONE |
| CODE.step4-components.78 | `INVERTER_DB` — **12** on-grid units: 3–30 kW; **4 single-phase** (GoodWe GW3000-NS 3 kW, GoodWe GW5000-NS 5 kW, Growatt MIN 5000TL-X 5 kW, Sungrow SG6.0RS 6 kW) and **8 three-phase** (Sungrow SG8.0RT, Growatt MID 10KTL3-X, Solis S5-GC10K, Huawei SUN2000-12KTL-M5, Sungrow SG15RT, Growatt MID 20KTL3-X, Amaze AN HKVA 30 KVA-360V, Solis S5-GC25K); MPPT count 2 except Solis 25 kW (3) and Amaze 30 kW (10); maxDcV 600 (1φ) / 1000 (Amaze) / 1100 (rest); η 97.6–98.6 %; ₹42,000–₹255,000; warranty 3/5/10 yr | inverters.ts:4-17; tsx probe | working | SC.10-5.21 |
| CODE.step4-components.79 | `InverterSpec` fields: `id, brand, model, acKw, phases (1\|3), mppt{count,minV,maxV,maxCurrentA,stringsPerMppt}, maxDcV, efficiencyPct, priceInr, warrantyYears?`. Schema gate (PASS): unique ids · acKw > 0 · phases ∈ {1,3} · mppt.count > 0 · minV > 0 · maxV > minV · **maxDcV ≥ mppt.maxV** · maxCurrentA > 0 · stringsPerMppt > 0 · 90 < η < 100 · price > 0 · warranty ∈ [2,15] | types.ts:284-304; catalog.test.ts:50-70 (1 PASS over all 12) | working | NONE |
| CODE.step4-components.80 | Pricebook integrity gates travelling with the envelope (PASS): every scalar rate finite and > 0; the by-size cable lookup tables walked entry-by-entry (no zero rate hiding inside); and **"a dearer conductor never costs less per metre"** — AC and DC cable rates must be strictly monotonic in mm² | catalog.test.ts:71-104 (2 PASS) | working | NONE (BOM side; s9) |

## §H — `lib/capabilities.ts` (obstruction capability model — UI carrier is Step 3)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step4-components.81 | `ObstructionCapabilities` BASE preset: `panelsMayCross false · legsAllowedNearby true · minVerticalClearanceM 0.3 · minHorizontalClearanceM 0 · maintenanceAccess 'perimeter' · removable false · mustRemainOpenToSky false · castsAnalyticalShadow true · supportsStructuralLoad false · requiresEngineerConfirmation false` | capabilities.ts:12-23 | working | NONE (cross-ref step3-obstructions.18) |
| CODE.step4-components.82 | `CAPABILITY_PRESETS` — 11 obstruction types, defaults only: tank & solar_wh (panelsMayCross, access 'top', requiresEngineerConfirmation) · dish (panelsMayCross, removable) · ladder (panelsMayCross, clearance 0.5, removable) · chimney/turbine_vent/windmill (mustRemainOpenToSky) · tree (access 'none') · elevated (supportsStructuralLoad) · building/other (BASE) | capabilities.ts:26-48 | working | NONE (cross-ref step3-obstructions.18) |
| CODE.step4-components.83 | `resolveCapabilities(o)` precedence: explicit per-instance `o.capabilities.*` → type preset → **legacy booleans**, with two fields keyed to the legacy canon (`panelsMayCross ?? !o.blocksPlacement`, `castsAnalyticalShadow ?? o.castsShadow`), so an untouched project resolves to exactly its pre-model behaviour and keeps a byte-identical fingerprint | capabilities.ts:55-71; test capabilities.test.ts:52-90 (4 PASS incl. the fingerprint guard) | working | NONE |
| CODE.step4-components.84 | `castsAnalyticalShadow(o)` is the **one** shadow predicate for both the analysis engine and the 3D scene — before it existed both read the raw boolean and `capabilities.castsAnalyticalShadow` was silently dead. Note: because the factory writes the full preset explicitly, this is exactly what makes Step 3's "Casts shadow" toggle inert (recorded as `step3-obstructions.32`) | capabilities.ts:73-86 | working (the resolver is correct; the Step-3 toggle defect belongs to that screen) | NONE |
| CODE.step4-components.85 | `requiredBridgeClearanceM(o) = o.heightM + resolveCapabilities(o).minVerticalClearanceM` — exact arithmetic pinned by test | capabilities.ts:88-91; test capabilities.test.ts:185-188 | working | NONE |
| CODE.step4-components.86 | `isBridgedAt(o, structureClearanceM)`: `false` when `!o.blocksPlacement` (master switch — nothing to bridge), `false` when clearance is null/undefined, `false` when `!panelsMayCross \|\| mustRemainOpenToSky`, else `structureClearanceM >= requiredBridgeClearanceM(o)`. 25 tests PASS across migration safety, fingerprint stability, bridging combinations, DRC/decision-log, shading parity and `reconcileBridgedPanels` symmetry | capabilities.ts:93-104; test capabilities.test.ts (25 PASS) | working | NONE |

## §I — `data/profiles.ts` (structural steel section catalog — UI carrier is Steps 5–6)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step4-components.87 | **8 mounting sections**, each `{key, label, kgPerM, dims, sectionMm, isGrade 'IS 2062', coating 'HDG ≥ 80 µm'}`: c_channel "C-Channel" 2.2 kg/m (80×40×8×1.6) · u_channel 2.4 (75×40×2.0) · l_angle 1.8 (45×45×2.6) · z_purlin 2.6 (85×40×2.0) · rhs "RHS / Box" 3.4 (65×45×2.0) · chs "CHS / Round tube" 3.0 (Ø63×2.0) · top_hat 2.01 (40×40×20×1.6) · c_channel_80 "C-Channel 80 (heavy)" 3.73 (80×40×15×2.5) | profiles.ts:74-104; test profiles.test.ts:26-57 (4 PASS) | working | NONE (structure; s5/s6) |
| CODE.step4-components.88 | Two mass rules, chosen by shape family: **open** sections (c/u/l/z/hat) → `developedWidthMm × t × 7850 / 1e6` (the COIL BLANK consumed, not the finished polygon — worth ~5 %); **hollow** (rhs/chs) → `crossSectionArea × 7850 / 1e6`. Blank widths: c = h + 2b + 2·lip · u/z = h + 2b · l = 2h − t · hat = b + 2h + 2·lip. CHS area = `π·t·(h − t)` with h the outside diameter | profiles.ts:7-21,24-62; test profiles.test.ts:59-87 (10 PASS, ±3 %, incl. "the heavy channel reproduces the reference 3.73 kg/m exactly" and "hollow sections use enclosed area, not a blank") | working | NONE |
| CODE.step4-components.89 | `sectionLabel(dims)` derives `sectionMm` from the dimensions so the human-readable string can never drift: CHS → `Ø{h} × {t}`, otherwise `{h} × {b} [× {lip}] × {t}` with integers unpadded and non-integers at 1 dp | profiles.ts:64-72; test profiles.test.ts:89-103 (2 PASS) | working | NONE |
| CODE.step4-components.90 | **Array order is load-bearing**: `STRUCTURE_PROFILES[0]` (c_channel) is `DEFAULT_PROFILE`, used as the default at four call sites (Scene3D, Step6Editor ×2, structure-edit); new entries must be appended. `profileByKey(key)` is the lookup. The six legacy `kgPerM` values are unchanged by design — dimensions were fitted to the masses, not the reverse, so no project's tonnage moved | profiles.ts:86-111; test profiles.test.ts:31-57 ("the two former duplicate defaults are now ONE object", "STRUCTURE_PROFILES[0] is still the c_channel four call sites default to") | working | NONE |
| CODE.step4-components.91 | This module de-duplicates a real drift hazard: `STRUCTURE_PROFILES` used to live in `lib/segment-ops.ts` with a **second hand-copied `DEFAULT_PROFILE` literal** in `lib/layout.ts`; both now re-export from here, and the test asserts object identity | profiles.ts:1-6; test profiles.test.ts:31-41 | working | NONE |
| CODE.step4-components.92 | Geometry contract enforced alongside the catalog (18 further PASS): every outline encloses real area and closes cleanly; the 2D glyph path is generated from the same outline as the 3D model; hollow sections produce a hole ring; extrusions are exactly 1 unit long on Y, centred on the extrusion axis, cross-section extents equal the declared dims in metres, scaling Y leaves every cross-section vertex untouched, and the cache hands back one geometry per profile | test profiles.test.ts:105-224 (18 PASS) | working | NONE |

---

## CENSUS-UNMATCHED

All 41 entries of `M05-design-studio.md` Appendix A §A.10-5 (`SC.10-5.01`–`SC.10-5.41`) are matched by at least one row above. **CENSUS-UNMATCHED: none.**

Nine of the matched entries are matched *with a defect or divergence* rather than cleanly, and are listed here so the gap is not lost behind the match:

| census entry | matched at | divergence |
|---|---|---|
| `SC.10-5.01` | rows 1, 4, 5 | order not enforced on reopen; open section's disclosure control inert; no Battery section (DD12/M05-37 only) |
| `SC.10-5.03`–`SC.10-5.07` | rows 8, 13, 22 | picker bypasses `resolveCatalog()`; watt bounds unclamped; all filter state discarded when the section closes |
| `SC.10-5.10`, `SC.10-5.11` | rows 23, 24 | both routes are non-interactive captions; no datasheet-upload or manual-entry path exists in code |
| `SC.10-5.12` | row 26 | typed capacity neither clamped ≥ 0 nor quantised to the 0.1 grid |
| `SC.10-5.13` | row 28 | disabled Auto shows its purpose, never its blocking reason |
| `SC.10-5.14` | row 29 | cross-check prints the panel count and reference watt but never the "~X kWp" the census names |
| `SC.10-5.17` | row 37 | no-recommendation case (band empty) is silent — no message, no fallback |
| `SC.10-5.19` | row 39 | typed inverter count is not capped at 10 |
| `SC.10-5.40` | row 20 | the replace-panel guard exists only on the Compare→Apply path; the direct picker swaps the panel under a placed layout silently |

## Beyond census (new in the POC, no `SC.10-5.*` entry)

Rows 6, 7, 19, 21, 42, 64, 71 and the whole of §G/§H/§I: the wizard-level step-4 gate and its three blocking messages; non-undoable direct edits; unsorted picker lists; the missing empty-result state; the un-phase-filtered inverter list; the matrix's *separate* clipping-excluded recommendation rule; the synchronous per-keystroke matrix recompute; the versioned catalog envelope with provenance labelling (`catalog.ts`) and its schema/monotonicity gates; the obstruction capability model (`capabilities.ts`); and the structural steel section catalog with its two mass rules (`profiles.ts`).

---

**rowCount:** 92
**defectCount:** 14 (rows 5, 8, 13, 20, 21, 22, 26, 28, 29, 32, 37, 39, 42, 73) + 1 uncertain (row 71)
**censusUnmatched:** 0 of 41 (`SC.10-5.01`–`SC.10-5.41` all matched; 9 matched with a recorded divergence — table above)
**newBeyondCensus:** 26 rows (6, 7, 19, 21, 42, 64, 71, 72–80, 81–86, 87–92)

**notes** (≤10 lines)
1. Test evidence: 5 files / 92 tests, all PASS (`catalog` 7 · `profiles` 32 · `bom-catalog` 3 · `comparison` 25 · `capabilities` 25); no failing test, so no DATA finding. Formula values re-derived live with `npx tsx`.
2. The single biggest contract gap is **catalog resolution**: the pickers import `PANEL_DB`/`INVERTER_DB` directly while the matrix, BOM and fingerprint all go through `resolveCatalog()` — M05-37's "browsing searches the tenant's resolved catalog" is structurally not met.
3. **No datasheet-upload path exists in code** — a caption reading "upload datasheet (PDF extraction) — mocked in POC" and a matching "enter specs manually" caption, neither interactive. M01-39/M01-40's add triad and review-before-commit are wholly absent.
4. **No Battery section exists** — not in the screen, not in `types.ts`, not in the catalog. The POC is census-faithful here; DD12/M05-37 is what it diverges from.
5. Scheme badges are hardcoded IN booleans (`almm`/`dcr` on `PanelSpec`), not market-pack-declared scheme data (M05-38/F1-19).
6. M05-43's "catalog state is honest in the picker" is unmet: `availability: 'on_order'` is surfaced **only** in the compare matrix, never in the panel list or the selected-panel summary; there is no discontinued/archived concept at all.
7. The two DC/AC band consumers agree exactly on 0.90–1.35 / 1.15 target, but Step 4's picker has no `nearestInverterFit` fallback and the matrix does — the same site can see "no recommendation" on the screen and a recommended row in the sheet.
8. `capabilities.ts` and `profiles.ts` are catalog/data files whose only UI carriers are Step 3 and Steps 5–6; their rows here are the owner's statement and cross-reference `step3-obstructions.md` rows 18/32/34/35/36 rather than restating that screen's defects.
9. Touch-surface gaps (native `title` as the only disabled-reason carrier on Compare; Space inert on the two `role="button"` banners) are recorded but are the shared-control class, not step-4-specific.
10. Perf: `effectiveKwp` runs `memoizedComparison` during render and `targetKwp` is inside `designFp`, so each capacity keystroke re-runs 6 full candidate pipelines synchronously — flagged uncertain, unmeasured at scale.
