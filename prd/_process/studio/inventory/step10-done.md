# Feature inventory — `step10-done` (Step 10 · Done · finalize/review · fingerprints · duplicate-as-variant)

**Sitting 9** of the HelioGrid Studio pass-two. Slug `step10-done`, keys `CODE.step10-done.<n>`.

**PRIMARY files (owned here)** — all under `3d_design_studio/src/features/solar-studio/`:
- `screens/Step10Done.tsx` (85 lines)
- `lib/review.ts` (162 lines)
- `lib/fingerprints.ts` (316 lines)
- `lib/project-duplicate.ts` (54 lines)

**Cited, NOT owned** (their sitting owns the behavior; cited here only where a primary file's
behavior is unreadable without them): `screens/Wizard.tsx` · `screens/Dashboard.tsx` ·
`store/store.tsx` (sitting 10 shell) · `screens/Step7Proposal.tsx` (sitting 6) ·
`screens/InstallationSheet.tsx` (sitting 12) · `screens/ProposalView.tsx` ·
`screens/ShareViewer.tsx` (sitting 11) · `lib/electrical/gate.ts` (sitting 7) ·
`lib/bom.ts` (sitting 8) · `lib/insights/*` (sitting 6) · `lib/structure.ts` · `lib/health.ts`
(sitting 5) · `data/catalog.ts` (sitting 4) · `lib/persistence/*` (sitting 10).

**Tests run** (2026-08-05, read-only repo):
```
npx vitest run src/features/solar-studio/lib/__tests__/{fingerprints,review,project-duplicate,persistence}.test.ts
→ 4 files passed · 51 tests passed · 0 failed (678 ms)
   fingerprints.test.ts 19 · review.test.ts 11 · project-duplicate.test.ts 9 · persistence.test.ts 12
```
No failing test in this area. Every `POC-DEFECT` below is therefore `docs-contradict` or
`visible-malfunction`, never `failing-test`.

**Census section:** `M05-design-studio.md` Appendix A §A.10-11 (`SC.10-11.01`–`SC.10-11.19`, 19 entries).

---

## A · `screens/Step10Done.tsx` — the Done screen

| Key | Behavior (code-faithful) | Evidence | Status | Census |
|---|---|---|---|---|
| CODE.step10-done.1 | **Step 10 is the wizard's terminal screen.** `Step10Done` is rendered by the `Wizard` switch's `default:` branch, i.e. for step 10 and any higher value (the route clamps 1–10 twice: `router.ts` `useRoute` and the page component). It takes no props — it reads the active project from the store. | `screens/Step10Done.tsx:7-8`; `screens/Wizard.tsx:210` (sitting 10); `router.ts:41-46`; `app/(studio)/wizard/[step]/page.tsx:8` | working | SC.10-11.01 |
| CODE.step10-done.2 | **The active project is asserted non-null** (`useActiveProject()!`). Safe only because `Wizard` returns `null` before rendering any body when there is no project; a direct render of `Step10Done` outside the wizard would throw. | `screens/Step10Done.tsx:8`; `screens/Wizard.tsx:173` | working | NONE |
| CODE.step10-done.3 | **Arriving marks the project proposal-ready.** On mount, if `project.status !== 'proposal_ready'`, the screen patches `{ status: 'proposal_ready' }`. Empty dependency array (`eslint-disable react-hooks/exhaustive-deps`) ⇒ it runs exactly once per mount, never again while mounted. This is the ONLY "finalize" act in the wizard: there is no confirm dialog, no readiness check, no undo entry. | `screens/Step10Done.tsx:12-18` | working | SC.10-11.01 |
| CODE.step10-done.4 | **Status has exactly two values** — `'in_progress' \| 'proposal_ready'`; new projects start `'in_progress'`. There is no `sent` / `approved` / `won` state in the studio model. | `types.ts:20`; `store/store.tsx:90` (sitting 10) | working | NONE |
| CODE.step10-done.5 | **The finalize patch stamps `updatedAt = Date.now()` and is NOT undoable.** `useProjectPatch()(patch, undoable = false)` dispatches `update-project`, which merges the patch into the active project and re-stamps `updatedAt`; the undo stack is only appended when `undoable` is true (capped at 25 entries, `slice(-24) + current`), so the transition to proposal-ready pushes nothing and cannot be undone from the wizard. | `screens/Step10Done.tsx:9,15`; `store/store.tsx:471-475,191-208` (sitting 10) | working | NONE |
| CODE.step10-done.6 | **Second writer of the same status:** Step 7's "Generate Proposal" also patches `{ status: 'proposal_ready' }` before navigating to `/proposal`. A project can therefore be proposal-ready without ever having reached Step 10, and Step 10's effect then no-ops. | `screens/Step7Proposal.tsx:366-373` (sitting 6); `screens/Step10Done.tsx:14` | working | NONE |
| CODE.step10-done.7 | **Proposal-ready is set with no readiness precondition of its own.** Reaching step 10 requires only that steps 1–9's `nextBlocker` gates pass (state + confirmed location, ≥1 roof, panel + inverter + target kWp > 0, ≥1 enabled panel, `electricalGate` clean). Zero captures, stale captures, an all-assumed BOM and open critical insights do NOT prevent the status flip — `preProposalReview`'s `attention` findings are advisory only (CODE.step10-done.48). | `screens/Step10Done.tsx:13-18`; `screens/Wizard.tsx:107-146,163-168`; `lib/review.ts:11-14` | working | SC.10-11.01 |
| CODE.step10-done.8 | **DISCREPANCY (in-code, unresolved):** `project-duplicate.ts` states as law that "a copy with no captures is not proposal-ready, whatever the original claimed" and forces `status: 'in_progress'` — yet Step 10 marks a project proposal-ready with zero captures. The two rules disagree about what proposal-ready means. Recorded, not resolved. | `lib/project-duplicate.ts:11-13,47`; `screens/Step10Done.tsx:13-18` | POC-DEFECT (docs-contradict: in-file rationale contradicts the screen) | NONE |
| CODE.step10-done.9 | **The share URL is `${location.origin}/share/${project.shareId}`**, recomputed on every render (no memo). Path route, resolved by `app/(studio)/share/[shareId]`. The in-file comment records that the earlier `#/share` hash form was never parsed by the Next router. | `screens/Step10Done.tsx:20-23`; `app/(studio)/share/[shareId]/page.tsx` | working | SC.10-11.05 |
| CODE.step10-done.10 | **The share link resolves on this device only.** `ShareViewer` looks the id up in the local store (`state.projects.find(p => p.shareId === shareId)`); projects live in localStorage and there is no share backend. Opening the copied link elsewhere shows "Shared design not found · This link is invalid or the design isn't on this device (POC stores locally)". Documented in-code as a POC limit. | `screens/Step10Done.tsx:20-22`; `screens/ShareViewer.tsx:6-29` (sitting 11) | working (POC limitation, self-documented) | SC.10-11.05 |
| CODE.step10-done.11 | **`shareId` is minted once at project creation** — `crypto.randomUUID()` with dashes stripped, first 12 chars; fallback `Math.random().toString(36).slice(2,12)` when `crypto.randomUUID` is unavailable. It is never rotated by Step 10. | `store/store.tsx:79-83,129` (sitting 10) | working | SC.10-11.05 |
| CODE.step10-done.12 | **Layout:** full-height flex column, centred both axes, `gap 8`, `padding 24`, `textAlign center`. No scroll region of its own (the wizard body wrapper scrolls). No responsive branch — the same layout on phone and desktop; the action row wraps (`flexWrap: 'wrap'`). | `screens/Step10Done.tsx:26-37,60` | working | SC.10-11.01 |
| CODE.step10-done.13 | **Success badge:** a 74 × 74 px circle (`borderRadius '50%'`), `background var(--good-bg)` (#e9f7ef), `color var(--good)` (#15803d), `marginBottom 8`, containing a `CheckCircle2` glyph at `size 36`, `strokeWidth 2.2`, `aria-hidden` (decorative — the heading carries the meaning). | `screens/Step10Done.tsx:38-52`; `theme.css:14-15` | working | SC.10-11.01 |
| CODE.step10-done.14 | **Heading:** `<h2>` reading exactly **"Project Complete"** (`margin 0`). | `screens/Step10Done.tsx:53` | working | SC.10-11.01 |
| CODE.step10-done.15 | **Subhead names the project:** "Your solar design for **{project.info.name}** is ready." — the name in `<b>`, line in `var(--ink-2)`. Uses the project name, not the customer name. | `screens/Step10Done.tsx:54-56` | working | SC.10-11.01 |
| CODE.step10-done.16 | **Reassurance line:** "You can return to edit this project anytime from the home screen." — `var(--ink-3)`, `fontSize 12.5`, `margin '0 0 18px'`. This is the census's "reassures that it can be reopened and edited". | `screens/Step10Done.tsx:57-59` | working | SC.10-11.01 |
| CODE.step10-done.17 | **Action row: five buttons**, `display flex`, `gap 10`, `flexWrap wrap`, `justifyContent center`; four `btn btn-secondary` and one `btn btn-primary` (Done). All icons render at `size 15`. Fixed order: View Proposal · BOM & Pricing · Installation Plan · Copy 3D Share Link · Done. | `screens/Step10Done.tsx:60-79` | working | SC.10-11.02–.06 |
| CODE.step10-done.18 | **Action 1 — "View Proposal"** (`FileText` icon) navigates to `/proposal` (the customer-facing `ProposalView`, sitting 11). No new tab, no state passed; the proposal reads the same active project. | `screens/Step10Done.tsx:61-63` | working | SC.10-11.02 |
| CODE.step10-done.19 | **Action 2 — "BOM & Pricing"** (`ReceiptText` icon) navigates back to `/wizard/9`. Backward navigation is always permitted (`allowedStep` only caps how far FORWARD one may go, and it is 10 here), and nothing is reset by going back — `status` stays `proposal_ready` while the user edits Step 9. | `screens/Step10Done.tsx:64-66`; `screens/Wizard.tsx:163-174` | working | SC.10-11.03 |
| CODE.step10-done.20 | **Action 3 — "Installation Plan"** (`HardHat` icon) sets local state `installSheet = true`, which mounts `<InstallationSheet project={project} onClose={…} />` as an overlay over the Done screen (not a route). Closing sets it back to `false`, unmounting the sheet; the sheet's own tick-off state is persisted on the project, so it survives the unmount. | `screens/Step10Done.tsx:10,67-69,80-82`; `screens/InstallationSheet.tsx:16-27` (sitting 12) | working | SC.10-11.04 |
| CODE.step10-done.21 | **Action 4 — "Copy 3D Share Link"** (`Link2` icon) calls `navigator.clipboard.writeText(shareUrl)` and nothing else. | `screens/Step10Done.tsx:70-75` | POC-DEFECT (visible-malfunction: no feedback — see CODE.step10-done.22) | SC.10-11.05 |
| CODE.step10-done.22 | **The copy action gives the user no confirmation and swallows no error.** There is no toast, no icon swap, no `.then()`, no `.catch()`. The returned Promise is unhandled ⇒ a rejection (denied permission) surfaces as an unhandled rejection, and on a non-secure origin `navigator.clipboard` is `undefined` ⇒ `TypeError` in the click handler, with the button still appearing to work. The same repo does it correctly elsewhere: Scene3D's copy-link swaps its icon to a tick and its tooltip to "Link copied" for 1800 ms. `ProposalView`'s "Copy 3D link" shares this defect. | `screens/Step10Done.tsx:72`; contrast `three/Scene3D.tsx:562-571` (sitting 5B); same defect at `screens/ProposalView.tsx:153-158` (sitting 11) | POC-DEFECT (visible-malfunction: silent action + unhandled rejection) | SC.10-11.05 |
| CODE.step10-done.23 | **Action 5 — "Done"** (`btn btn-primary`, trailing `ArrowRight` icon) navigates to `/projects` (the Dashboard = the POC's design list). No save call is needed — every patch already persisted. | `screens/Step10Done.tsx:76-78` | working | SC.10-11.06 |
| CODE.step10-done.24 | **Two "Done" controls are visible at once on step 10.** The wizard header replaces "Next" with a primary `Check`-icon "Done" button at `step === 10`, which also navigates to `/projects`; the body renders its own primary "Done" button. Same destination, duplicated primary affordance. | `screens/Wizard.tsx:320-329` (sitting 10); `screens/Step10Done.tsx:76-78` | working (redundant control) | SC.10-11.06 |
| CODE.step10-done.25 | **No copy of the proposal link, no QR, no "send" on this screen.** The only outward-facing action is the 3D share link; issuing/printing/QR live on `ProposalView` (Print / Save PDF, QR of the same `/share/<id>` URL). | `screens/Step10Done.tsx:60-79`; `screens/ProposalView.tsx:159-161,216-225` (sitting 11) | working | SC.10-11.02 |
| CODE.step10-done.26 | **DISCREPANCY vs the owner ruling:** the copied link is the read-only **3D** URL (`/share/<shareId>`), matching the census wording "Copy 3D share link" and the POC prompt ("copies the customer's read-only 3D link"). M05-75 records the 2026-08-04 ruling (Q27) that the copied link must be the customer's **proposal** link, whose page carries a "View in 3D" button, with no separate 3D URL minted. Code predates the ruling. Recorded, not resolved. | `screens/Step10Done.tsx:23`; `docs/phase-10-prompts.md:1485`; `prd/modules/M05-design-studio.md:740` | uncertain (code correct as POC; superseded by ruling Q27) | SC.10-11.05 |
| CODE.step10-done.27 | **Empty/error states: none.** The screen has no loading, empty or error branch — it renders the same five actions regardless of whether the design has panels, captures, a BOM or a proposal. The only conditional render is the installation-sheet overlay. | `screens/Step10Done.tsx:25-83` | working | NONE |

---

## B · `lib/review.ts` — the pre-proposal review (the finalize gate's honest half)

| Key | Behavior (code-faithful) | Evidence | Status | Census |
|---|---|---|---|---|
| CODE.step10-done.28 | **`preProposalReview(project) → { items, overall, issuable }`** answers "is this ready to send?" once, from four independent signals that each live on a different screen: electrical gate (Step 6) · engineering insights (Step 6) · BOM confidence (Step 9) · capture staleness (Step 7). Pure and synchronous; no I/O. | `lib/review.ts:1-19,42` · test `review.test.ts` "always reports all four" | working | SC.10-11.01 |
| CODE.step10-done.29 | **Always exactly four items, always in this order:** `electrical` → `insights` → `bom-confidence` → `captures`. No item is ever omitted — a passing check renders as a green row, so the list length never signals anything. | `lib/review.ts:45-153` · test `review.test.ts:29-38` | working | NONE |
| CODE.step10-done.30 | **Each item carries the step that can FIX it** (`step`: electrical 6 · insights 6 · bom-confidence 9 · captures 7) — the file's stated rule is "a finding with nowhere to go is noise". The test asserts every step > 0. | `lib/review.ts:23-26,52,70,104,133` · test `review.test.ts:37` | working | NONE |
| CODE.step10-done.31 | **Three statuses only:** `'blocked' \| 'attention' \| 'ready'`. There is no "unknown"/"pending" state — an unavailable signal resolves to `ready`. | `lib/review.ts:21` | working | NONE |
| CODE.step10-done.32 | **`ReviewItem` = `{ key, step, title, status, detail }`**; `ReviewResult` = `{ items, overall, issuable }` where `overall` is the worst status present and `issuable` is "nothing blocks issuing". | `lib/review.ts:23-38` | working | NONE |
| CODE.step10-done.33 | **The review DERIVES, it never re-implements.** It calls the same `electricalGate` the wizard's Next calls, the same `memoizedInsights` the tray uses, the same `bomConfidence(mergedBom())` Step 9 shows and the same `isCaptureFresh` that stamps the capture badge — so the checklist can never disagree with the screen it points at. Stated design law: "a second, subtly different gate is how two answers to one question get shipped." | `lib/review.ts:11-19,46,66,99,128` · test `review.test.ts:49-53` | working | NONE |
| CODE.step10-done.34 | **Item 1 · Electrical design — the one hard gate.** When `electricalGate(project)` returns a gate, status is `blocked` and `detail` is the gate's own message verbatim (e.g. "N modules are not in any string", "(and N more electrical errors)"); the message is NOT re-worded here. | `lib/review.ts:45-56`; `lib/electrical/gate.ts:26-57` (sitting 7) · test `review.test.ts:40-47` | working | NONE |
| CODE.step10-done.35 | **Item 1 pass copy:** "Every enabled module is wired into a string within the inverter's limits." (status `ready`). | `lib/review.ts:57-62` | working | NONE |
| CODE.step10-done.36 | **Item 1 edge — vacuous ready.** `electricalGate` returns `null` (= no blocker) when the panel spec, the inverter or every enabled panel is missing, because "earlier steps own these". A project with zero panels therefore renders Item 1 green with the sentence above, and `issuable` is `true`. Unreachable through the wizard (Step 6's Next blocks on "Place at least one panel to continue"), reachable by calling the function directly or via a deep link with stale state. | `lib/review.ts:46-62`; `lib/electrical/gate.ts:31-33`; `screens/Wizard.tsx:120-122` | uncertain (honest-by-upstream-gate only) | NONE |
| CODE.step10-done.37 | **Item 2 · Design review — "open" means NOT accepted.** `open = insights.filter(i => project.insightState?.[i.key] !== 'accepted')`. The persisted state has two values, `'accepted' \| 'ignored'` — so an insight the user explicitly **ignored** still counts as open and keeps appearing in the review. The file's own header says "insights the user has not accepted or ignored", which the code does not do. Recorded, not resolved. | `lib/review.ts:65-67`; `types.ts:982` | POC-DEFECT (docs-contradict: in-file comment vs filter) | NONE |
| CODE.step10-done.38 | **Item 2 status rule:** `attention` iff any open insight has severity `warning` or `critical`; otherwise `ready` — including when open items exist (severities `info`/`suggestion`). Severity ladder is `info \| suggestion \| warning \| critical`. | `lib/review.ts:80-83`; `lib/insights/types.ts:9` (sitting 6) | working | NONE |
| CODE.step10-done.39 | **Item 2 detail, attention wording:** `"{n} item{s} to review: {title1}; {title2}; {title3}…"` — count pluralized at n > 1, first **3** titles joined with "; ", trailing "…" only when more than 3 exist. | `lib/review.ts:86-94` | working | NONE |
| CODE.step10-done.40 | **Item 2 detail, non-blocking wording:** `"{n} optional suggestion{s}, none blocking: {titles…}"`. The in-code rationale: "A green tick beside '5 open suggestions' reads as a contradiction… say that plainly instead of letting the icon and the sentence disagree." | `lib/review.ts:84-94` | working | NONE |
| CODE.step10-done.41 | **Item 2 zero-open copy:** "No open suggestions from the design, maintenance or constructability checks." (status `ready`). | `lib/review.ts:68-76` | working | NONE |
| CODE.step10-done.42 | **Item 2 cost:** insights are read through `memoizedInsights`, whose last-value memo key is `designFp(project) + '§' + JSON(insightState) + '§' + registered analyzer ids` — so the review re-runs the analyzers only when the design fingerprint, the accept/ignore state or the registry composition changes. | `lib/review.ts:66`; `lib/insights/registry.ts:73-88` (sitting 6) | working | NONE |
| CODE.step10-done.43 | **Item 3 · Quantity confidence** runs `bomConfidence(mergedBom(project))` — the same merged BOM Step 9 prices, so the review cannot quote a different confidence than the BOM screen. | `lib/review.ts:99`; `lib/bom.ts:91-116` (sitting 8) | working | NONE |
| CODE.step10-done.44 | **Item 3 attention copy:** `"Preliminary — {n} line{s} need site verification: {item1}; {item2}; {item3}…"` — n = `needsVerification.length` (distinct item names whose effective confidence is `estimated` or `assumed`), first **3** listed, "…" when more. Status `attention` (never blocked). | `lib/review.ts:100-112` · test `review.test.ts:86-95` | working | SC.10-11.01 (proposal-honesty half; the notice itself is `SC.10-10.41`, sitting 8) |
| CODE.step10-done.45 | **Item 3 ready copy:** "Every quantity is measured or derived from the design." | `lib/review.ts:113-119` | working | NONE |
| CODE.step10-done.46 | **Item 3 inherits the BOM's two confidence rules** (owned by sitting 8, load-bearing here): an **excluded** line cannot set the quote's confidence, and a hand-**overridden** line counts as `measured`. So excluding an uncertain item, or typing a real measured value over it, clears it from this review row. | `lib/bom.ts:104-114` (sitting 8) | working | NONE |
| CODE.step10-done.47 | **Item 4 · Shadow study imagery — target is 4 captures.** `CAPTURE_TARGET = 4`; only captures with an `imageBlobId` count as taken (a defined-but-unshot capture slot does not). | `lib/review.ts:40,127` · test `review.test.ts:71-77` | working | NONE |
| CODE.step10-done.48 | **Item 4 precedence: shortfall is reported BEFORE staleness.** If fewer than 4 captures have images → `attention`, detail `"{n} of 4 captures taken."`; only when all 4 exist is staleness evaluated. A project with 2 stale captures reports "2 of 4 captures taken." and never mentions staleness. | `lib/review.ts:129-152` · test `review.test.ts:71-77` | working | NONE |
| CODE.step10-done.49 | **Item 4 stale copy:** `"{n} capture{s show\|  shows} an older version of the layout — retake before issuing."` (singular renders " shows", plural "s show"). Status `attention`. Staleness is `!isCaptureFresh(project, c)` — one definition, in `fingerprints.ts`, so this can never disagree with the stale badge on the capture itself. | `lib/review.ts:122-145` · test `review.test.ts:56-62` | working | NONE |
| CODE.step10-done.50 | **Item 4 fresh copy:** "All captures match the current layout." | `lib/review.ts:146-152` · test `review.test.ts:64-69` | working | NONE |
| CODE.step10-done.51 | **Item 4 never blocks** — "a proposal printing pictures of a design that no longer exists" is called out even though it gates nothing; a missing photo is explicitly "not an engineering fault". | `lib/review.ts:122-126` · test `review.test.ts:79-83` | working | NONE |
| CODE.step10-done.52 | **Item 4 gap — the COVER image's staleness is not reviewed.** The review checks `project.captures` only; `capturesFresh()` additionally fails when `coverImageBlobId` is set and `coverForLayoutFp !== layoutFp(p)`. A proposal can therefore print a stale cover while the review reads "All captures match the current layout" (the stale cover IS flagged separately on `ProposalView`). | `lib/review.ts:127-152`; `lib/fingerprints.ts:312-316`; `screens/ProposalView.tsx:76` (sitting 11) | POC-DEFECT (visible-malfunction: stale cover passes the readiness review) | NONE |
| CODE.step10-done.53 | **`overall` is the worst status present:** `blocked` if any item is blocked, else `attention` if any is attention, else `ready`. | `lib/review.ts:155-159` · test `review.test.ts:97-101` | working | NONE |
| CODE.step10-done.54 | **`issuable = overall !== 'blocked'`** — attention never withholds issuing. | `lib/review.ts:161` · test `review.test.ts:103-107` | working | NONE |
| CODE.step10-done.55 | **Deterministic:** identical project ⇒ deeply equal result (pinned by test). | `lib/review.ts:42-161` · test `review.test.ts:109-112` | working | NONE |
| CODE.step10-done.56 | **The review is read-only and gates nothing.** Stated law: "nothing here gates anything the wizard does not already gate". Neither `issuable` nor `overall` is consulted by the wizard's Next, by Step 7's "Generate Proposal", or by Step 10's status flip. | `lib/review.ts:11-14`; `screens/Step7Proposal.tsx:366-373`; `screens/Step10Done.tsx:13-18` | working (by design) | NONE |
| CODE.step10-done.57 | **Sole consumer surface — Step 7's "BEFORE YOU ISSUE" card** (sitting 6 owns the rendering): a `role="status" aria-live="polite"` card with a 3 px left border tinted by `overall`, header label from a tone map — `blocked` → "Not ready to issue" (bad), `attention` → "Ready — with caveats" (warn), `ready` → "Ready to issue" (good) — then one row per item: status icon (`Check` / `XCircle` / `AlertTriangle`), bold title, `sr-only` status word ("ready"/"blocked"/"needs attention"), and the detail line. | `screens/Step7Proposal.tsx:173-228` (sitting 6) | working | NONE |
| CODE.step10-done.58 | **Per-item jump button:** a ghost "Step {n}" button appears only when the item is not `ready` **and** its step ≠ 7, navigating to `/wizard/{step}`. So a stale-capture finding (step 7) offers no button — the user is already on that screen. | `screens/Step7Proposal.tsx:222-231` (sitting 6) | working | NONE |
| CODE.step10-done.59 | **The review has NO Step-10 surface.** `Step10Done` neither calls `preProposalReview` nor shows any of its four signals; the screen that flips the project to proposal-ready is the one screen that never displays the readiness answer. | `screens/Step10Done.tsx:1-85` (no import of `lib/review`) | POC-DEFECT (visible-malfunction: finalize happens where readiness is not shown) | SC.10-11.01 |

---

## C · `lib/fingerprints.ts` — the five-layer derived-state graph

### C1 · The graph and its layers

| Key | Behavior (code-faithful) | Evidence | Status | Census |
|---|---|---|---|---|
| CODE.step10-done.60 | **Five layered fingerprints over the canonical `Project`, strictly nested: `siteFp ⊂ geometryFp ⊂ layoutFp ⊂ electricalFp ⊂ designFp`.** Each is a plain string; each higher layer literally begins with the lower one (asserted by `startsWith` in the test). An edit that changes a layer changes every layer above it and no layer below it. | `lib/fingerprints.ts:1-13` · test `fingerprints.test.ts:66-76` | working | NONE |
| CODE.step10-done.61 | **Purpose (product level): "what must recalculate after this edit?" has exactly one answer.** Every derived artifact — per-panel solar access, roof heatmap, shadow captures + cover, BOM, SLD, financials, insights, health, comparison — keys its freshness on exactly one layer instead of ad-hoc effect dependencies. | `lib/fingerprints.ts:1-10` | working | NONE |
| CODE.step10-done.62 | **Deterministic:** the same project always produces byte-identical strings for all six functions (pinned for a `structuredClone`d copy). Fingerprints are compared by string equality only; they are never parsed. | `lib/fingerprints.ts:22-316` · test `fingerprints.test.ts:60-64` | working | NONE |
| CODE.step10-done.63 | **Numeric rounding helper `r(v, f) = Math.round(v * f)`** — used at two precisions: 1e5 for lat/lng (≈1.1 m at the equator) and 100 for metres (1 cm). Sub-precision jitter therefore does not re-key anything. | `lib/fingerprints.ts:22,27-28,93-94,276` | working | NONE |
| CODE.step10-done.64 | **`siteFp` — the site physics inputs.** Returns the literal `'site:none'` when there is no location. Otherwise `JSON.stringify([lat×1e5 rounded, lng×1e5 rounded, weather.fetchedAt ?? 0, weather.raddatabase ?? ''])` — i.e. the pin at ~1.1 m and the measured-weather stamp (fetch time + which PVGIS radiation database). | `lib/fingerprints.ts:24-33` | working | NONE |
| CODE.step10-done.65 | **`geometryFp` — the physical scene** (shadow casters + mounting surfaces): `siteFp` + `'\|'` + JSON of (a) `[calibration.scaleFactor ?? 1, calibration.northOffsetDeg ?? 0]` — calibration "reshapes the world" (projector scale + sun frame), read optionally because payloads from an older build can arrive mid-session via multi-tab storage events; (b) per roof `[polygon, heightM, pitchDeg, slopeAzimuthDeg, parapet, roofType, setbackM, perEdgeSetbacksM]` — the **whole** `parapet` object is serialised, so per-edge parapet data is caught; (c) per obstruction `[center, shape, lengthM, widthM, diameterM, heightM, rotationDeg, castsShadow, blocksPlacement, setbackM, roofId]`. | `lib/fingerprints.ts:35-75` | working | NONE |
| CODE.step10-done.66 | **`geometryFp` conditional suffix — obstruction capability overrides.** Per obstruction with a `capabilities` object: `\|oc:{id}:{JSON}`. Untouched objects append nothing, so fingerprints (and stored captures) from before the feature existed stay byte-identical; an explicit capability edit correctly stales downstream data. | `lib/fingerprints.ts:76-82` | working | NONE |
| CODE.step10-done.67 | **Documented maintenance hazard (geometry):** per-roof shading fields must live inside `polygon` / `heightM` / `parapet`; any NEW top-level `Roof` field that affects shading must be added to this list by hand or captures will read fresh against a changed scene. The field list is explicit, not a whole-object stringify. | `lib/fingerprints.ts:35-41,52-61` | working (documented risk) | NONE |
| CODE.step10-done.68 | **`layoutFp` — placement.** `geometryFp` + `'\|'` + JSON of: panels `[id, x×100 rounded (cm), y×100 rounded (cm), roofId, orientation, azimuthDeg, tiltDeg, enabled, segmentId ?? '']`; segments `[id, polygon, racking, orientation, azimuthDeg, moduleGapM, removed]` (`racking` stringified whole); keepouts `[id, shape, heightM, kind, roofId]`; walkways `[id, a, b, widthMm, roofId]`. | `lib/fingerprints.ts:85-105` | working | NONE |
| CODE.step10-done.69 | **`layoutFp` conditional suffix — leg plans.** Per segment with a `legPlan`: `\|lp:{segmentId}:{JSON of points}`. Rationale in code: the segment tuple is an explicit field list, so a new field there would not be picked up at all; and "moving a leg moves steel, which moves the quote". | `lib/fingerprints.ts:106-112` | working | NONE |
| CODE.step10-done.70 | **`layoutFp` UNCONDITIONAL suffix — `\|sm:` + `structureModelVersion()` (currently `'sm2'`).** Every structural field is lazy, so a project that never chose a foundation stores identical bytes before and after the built-in default moves; without this stamp its captures would keep reporting FRESH while the scene had gained a pedestal under every leg. **Accepted consequence: bumping it re-keys every project and stales every capture** — "that is exactly what the bump means". | `lib/fingerprints.ts:113-128`; `lib/structure.ts:114-119` (sitting 5C) | working | NONE |
| CODE.step10-done.71 | **`electricalFp` — components + string topology.** `layoutFp` + `'\|'` + JSON of `[panel.id ?? '', inverter.id ?? '', inverterCount, targetKwp, strings[id, inverterIndex, mpptIndex, panelIds]]`. | `lib/fingerprints.ts:131-142` | working | NONE |
| CODE.step10-done.72 | **`electricalFp` conditional suffixes:** `\|central` only when `components.inverterTopology === 'central'`, `\|mlpe` only when `components.mlpe === 'optimizer'`. The default `'string'` topology and absent MLPE serialise exactly as before those fields existed. | `lib/fingerprints.ts:143-147` | working | NONE |
| CODE.step10-done.73 | **`designFp` — everything that reaches a customer-facing output.** `electricalFp` + `'\|'` + JSON of: legacy `bomOverrides[id, qty, unitPriceInr, item, spec, auto, overridden]`; the Phase-22c `bom` block (below); `pricing.marginPct`; `derived.sldOverrides`; project info `[name, customerName, state, discom, siteType, connectionType, sanctionedLoadKw, tariffInrPerKwh, monthlyBillInr]`; `rails[id, a, b]`; `arresters[id, pos]`; `inverterPlacements[id, roofId, edgeIndex, t]`. | `lib/fingerprints.ts:150-189` | working | NONE |
| CODE.step10-done.74 | **`designFp` — per-field BOM overrides are order-normalised.** When `project.bom` exists it contributes `[overrides[lineKey, entries(fields) sorted by field name → [field, value]], custom[id, category, item, qty, unit, unitPriceInr], inputs ?? null]`; when absent it serialises `undefined`, exactly as before the field existed. The sort exists so that "key order must not matter" — the same edits in a different order produce the same fingerprint. | `lib/fingerprints.ts:157-172` | working | NONE |
| CODE.step10-done.75 | **`designFp` conditional suffixes, in order:** `\|sd:{structureDefaults JSON}` when structure defaults exist · `\|cr:{routes JSON}` only when `cableRoutes.length > 0`, each route `[id, waypoints, verticalDropM, slackPct, manual ?? false]` (routes move cable metres, therefore money) · per roof `\|so:{roofId}:{structureOverride JSON}` when that roof has an override. | `lib/fingerprints.ts:190-206` | working | NONE |
| CODE.step10-done.76 | **`designFp` UNCONDITIONAL suffix — `\|cat:` + `resolveCatalog().catalogVersion` (currently `'2026.07-1'`).** Prices and specs live OUTSIDE the project, so without it a price-book revision leaves every stored quote and proposal looking fresh while quoting last quarter's money. **Accepted consequence: bumping the catalog version re-keys every project's design fingerprint and stales their captures.** | `lib/fingerprints.ts:207-220`; `data/catalog.ts:20-21,34` (sitting 4) | working | NONE |
| CODE.step10-done.77 | **The conditional-suffix law (whole file):** every field added after a fingerprint shipped appends **nothing** when absent, so a stored project upgrades byte-identically and its captures/quotes do not go stale on deploy. The two deliberate exceptions are the structure-model stamp and the catalog version, both documented with their blast radius. | `lib/fingerprints.ts:76-82,106-128,190-220` | working | NONE |

### C2 · Shading key and freshness helpers

| Key | Behavior (code-faithful) | Evidence | Status | Census |
|---|---|---|---|---|
| CODE.step10-done.78 | **`SHADING_ENGINE_VERSION = 6`** — bumped when the shading ENGINE changes results for identical geometry. Recorded history: **v2** raycast material became `DoubleSide` (backface hits such as parapet inner walls now register; access can only fall) · **v3** sun sampling extended 5 → all 12 months · **v4** rays start at the real module-plane height (structure clearance + tilt rise) instead of a fixed 0.45 m · **v5** modules themselves became casters (Tier-2 row-on-row self-shading) · **v6** sun sampling aligned with the roof heatmap — full daylight window at 0.5 h steps with sin α·dh weights (was 08:00–17:00 at ~1.5 h with a 0.05 weight floor). Joining the key makes every stamp from an older engine read stale, so projects self-heal on next load. | `lib/fingerprints.ts:223-244` | working | NONE |
| CODE.step10-done.79 | **`shadingFp(project)` — the recompute/stamp key for per-panel solar access.** Returns `''` when the project is null or has no location (nothing to compute). Otherwise `` `e{VERSION}\|` `` + `geometryFp` + `'\|'` + JSON per panel of `[id, x cm, y cm, roofId, panelSampleHeightM(p, panel, spec) cm, tiltDeg, azimuthDeg, orientation, enabled]`. Defensive: `p.components?.panel ?? null` (hand-rolled test projects may omit components). | `lib/fingerprints.ts:246-285`; `lib/panel-pose.ts` (sitting 5A) · test `fingerprints.test.ts:74-75` | working | NONE |
| CODE.step10-done.80 | **`shadingFp` is NOT a sixth layer.** It is geometry + the panel fields the engine actually reads, so electrical and commercial edits never trigger the expensive shading pass. The v5 scope change is recorded in-code: tilt/azimuth/orientation/enabled were once deliberately EXCLUDED (they do not move the sample point) but became engine inputs when panels became casters — "omitting them would stamp stale access as fresh, the one failure the fingerprint graph exists to prevent". Accepted cost: more recomputes on tilt/enable edits, absorbed by the debounce + worker. | `lib/fingerprints.ts:12-16,246-259` · test `fingerprints.test.ts:170-188` | working | NONE |
| CODE.step10-done.81 | **`shadingFingerprint` is a legacy alias of `shadingFp`**, kept because the heatmap invalidation in `Step6Editor` and `Scene3D` keys on that name. | `lib/fingerprints.ts:287-291`; `screens/Step6Editor.tsx:86,310`; `three/Scene3D.tsx:32,280` (sitting 5) | working | NONE |
| CODE.step10-done.82 | **`isShadingFresh(p)`** — `true` when `p.panels.length === 0` (an empty design is vacuously fresh), else `p.derived.solarAccessFp === shadingFp(p)`. `false` means the numbers on screen are provisional: either the optimistic 1.0 default or values from an older scene, until `useDesignSync` finishes and re-stamps. | `lib/fingerprints.ts:293-304` · test `fingerprints.test.ts:263-274` | working | NONE |
| CODE.step10-done.83 | **`isCaptureFresh(p, cap)`** — `!!cap.forLayoutFp && cap.forLayoutFp === layoutFp(p)`. A **legacy capture with a null stamp is always stale** (it cannot prove what it shows). | `lib/fingerprints.ts:306-309` · test `fingerprints.test.ts:276-296` | working | NONE |
| CODE.step10-done.84 | **`capturesFresh(p)`** — `false` if a cover image exists and `coverForLayoutFp !== layoutFp(p)`; otherwise every capture that has an image must be fresh. **A project with no images at all returns `true`** ("nothing stale"), so this function alone cannot distinguish "all fresh" from "no imagery". | `lib/fingerprints.ts:311-316` · test `fingerprints.test.ts:298-308` | working | NONE |

### C3 · The invalidation matrix (one row per pinned edit — "what invalidates what")

Gate: for each edit, the listed layers MUST differ and every unlisted layer MUST be byte-identical (`expectInvalidates`, `fingerprints.test.ts:45-57`).

| Key | Edit → layers invalidated | Evidence | Status | Census |
|---|---|---|---|---|
| CODE.step10-done.85 | **Moving the location pin** → site · geometry · layout · electrical · design · shading (everything). | test `fingerprints.test.ts:80-86` | working | NONE |
| CODE.step10-done.86 | **New measured weather landing** (`weather.fetchedAt` changes) → everything. Measured irradiance replacing an estimate re-keys the whole design. | test `fingerprints.test.ts:88-107` | working | NONE |
| CODE.step10-done.87 | **Moving a roof vertex** → geometry, layout, electrical, design, shading; site unchanged. | test `fingerprints.test.ts:109-118` | working | NONE |
| CODE.step10-done.88 | **Toggling a parapet** → geometry and above + shading. | test `fingerprints.test.ts:120-129` | working | NONE |
| CODE.step10-done.89 | **Adding an obstruction** → geometry and above + shading. | test `fingerprints.test.ts:131-157` | working | NONE |
| CODE.step10-done.90 | **Moving a panel** (≥1 cm) → layout, electrical, design, shading; geometry unchanged. | test `fingerprints.test.ts:159-168` | working | NONE |
| CODE.step10-done.91 | **Disabling a panel** → layout, electrical, design **and shading** (it stops casting on its neighbours — v5 semantics; the test once pinned the opposite). | test `fingerprints.test.ts:170-180` | working | NONE |
| CODE.step10-done.92 | **Changing panel tilt** → layout, electrical, design **and shading** (a tilted plate casts differently). | test `fingerprints.test.ts:182-188` | working | NONE |
| CODE.step10-done.93 | **Restringing** (splitting a string) → electrical + design only. Captures and shading stay fresh. | test `fingerprints.test.ts:190-202` | working | NONE |
| CODE.step10-done.94 | **Changing inverter count** → electrical + design only. | test `fingerprints.test.ts:204-210` | working | NONE |
| CODE.step10-done.95 | **Changing the margin %** → design only. | test `fingerprints.test.ts:212-218` | working | NONE |
| CODE.step10-done.96 | **A BOM override** → design only. | test `fingerprints.test.ts:220-243` | working | NONE |
| CODE.step10-done.97 | **Renaming the project** → design only — explicitly "captures must NOT go stale" on a rename. | test `fingerprints.test.ts:245-251,294-295` | working | NONE |
| CODE.step10-done.98 | **Recomputed `solarAccess` values written back onto panels** → NO fingerprint changes at all (derived data must never re-key its own inputs, or the recompute would never converge). | test `fingerprints.test.ts:253-259` | working | NONE |

### C4 · Who consumes each layer (the freshness contract in practice)

| Key | Behavior (code-faithful) | Evidence | Status | Census |
|---|---|---|---|---|
| CODE.step10-done.99 | **`layoutFp` stamps shadow captures.** Step 7 stamps each capture's `forLayoutFp` with `layoutFp(project)` at capture time and the cover's `coverForLayoutFp` when a cover is chosen; the capture tile shows a stale badge via `isCaptureFresh`. | `screens/Step7Proposal.tsx:8,57,262,343` (sitting 6); `types.ts:609-613,954-956` | working | NONE |
| CODE.step10-done.100 | **`shadingFp` drives the solar-access recompute loop.** `useDesignSync` memoises `shadingFp(project)`, recomputes when it changes, and stamps `derived.solarAccessFp` with the fingerprint of the project it actually computed for (not the current one) — so a mid-flight edit cannot mark stale results fresh. | `store/useDesignSync.ts:12,20,39` (sitting 10) | working | NONE |
| CODE.step10-done.101 | **`layoutFp` keys the 3D structure graph;** `shadingFingerprint` keys the heatmap in both the 2D editor and the 3D scene. View-only state (hiding a panel to look at a rafter) is deliberately kept OUT of both — "if it keyed `layoutFp` it would stale captures". | `three/Scene3D.tsx:62,280,1031-1043`; `screens/Step6Editor.tsx:310`; `lib/structure-view.ts:8-9` (sitting 5) | working | NONE |
| CODE.step10-done.102 | **`designFp` keys design health.** `healthKey(p)` = `designFp(p)` + the panels' `solarAccess` stamp + `insightState` — designFp alone "lies" because per-panel access and accept/ignore state are not in it; the health snapshot is additionally marked `provisional` when `!isShadingFresh(project)`. | `lib/health.ts:18-29,154-160,291` (sitting 5A) | working | NONE |
| CODE.step10-done.103 | **`designFp` keys the insight memo and the comparison memo** (last-value memoisation, so identical design state returns the identical array instance). | `lib/insights/registry.ts:3,73-88` (sitting 6); `lib/comparison.ts:33,420-426` (sitting 6) | working | NONE |
| CODE.step10-done.104 | **Freshness reaches the customer-facing surfaces:** `ProposalView` computes `isShadingFresh` and `capturesFresh` to mark provisional numbers and stale imagery; `EnergyReportSheet` uses `isShadingFresh` the same way. This is the "money/numbers never render while stale" rule in code. | `screens/ProposalView.tsx:18,75-76` (sitting 11); `components/EnergyReportSheet.tsx:7,25` (sitting 6) | working | NONE |
| CODE.step10-done.105 | **Migration/normalisation preserves fingerprint bytes.** `persistence/normalize.ts` defaults `coverForLayoutFp` to `null` and is written so that a segment/field that never existed serialises exactly as before — the persistence layer is explicitly aware that it must not perturb `layoutFp`. | `lib/persistence/normalize.ts:47,105,165` (sitting 10) · test `persistence.test.ts` (12 passed) | working | NONE |

---

## D · `lib/project-duplicate.ts` — duplicate-as-variant

| Key | Behavior (code-faithful) | Evidence | Status | Census |
|---|---|---|---|---|
| CODE.step10-done.106 | **`duplicateProject(p, ids, existingNames = []) → Project`** — "save as a variant"; the stated use is pricing Option B against Option A on the same roof. **Pure**: ids and the clock are injected (`DuplicateIds = { id, shareId, now }`) so the caller supplies `crypto.randomUUID()` values and `Date.now()`. | `lib/project-duplicate.ts:1-13,16-40` · test `project-duplicate.test.ts:22-30` | working | SC.10-11.19 |
| CODE.step10-done.107 | **Everything not explicitly overridden is copied by spread** — roofs, panels, segments, keepouts, walkways, obstructions, components, strings, rails, arresters, inverter placements, cable routes, structure defaults/overrides, `bomOverrides` + `bom`, `pricing`, `sldParams`/`derived.sldOverrides`, `insightState`, `installation`, `structuralVerification`, `wizardStep`, `location`, and every `info` field except the name. Test pins roofs/panels/strings/components. | `lib/project-duplicate.ts:41-53` · test `project-duplicate.test.ts:23-30` | working | SC.10-11.19 |
| CODE.step10-done.108 | **Override 1 — new `id` and new `shareId`.** Stated reason: "a share link identifies ONE document. Reusing it would make the copy silently overwrite what a customer already received." | `lib/project-duplicate.ts:1-8,42-43` · test `project-duplicate.test.ts:32-38` | working | SC.10-11.19 |
| CODE.step10-done.109 | **Override 2 — `createdAt` and `updatedAt` both set to `ids.now`.** A variant is a new document, not an edit of the original. | `lib/project-duplicate.ts:44-45` · test `project-duplicate.test.ts:53-57` | working | NONE |
| CODE.step10-done.110 | **Override 3 — `status` forced to `'in_progress'`,** whatever the original claimed: "a copy with no captures is not proposal-ready". | `lib/project-duplicate.ts:11-13,47` · test `project-duplicate.test.ts:48-51` | working | NONE |
| CODE.step10-done.111 | **Override 4 — all image references dropped:** `captures: []`, `coverImageBlobId: null`, `coverImage: null`, `coverForLayoutFp: null`. Reason (load-bearing): captures/cover are IndexedDB blob **references**, and blob GC on project delete is driven by `projectBlobIds(project)` — a copy sharing those ids would have its images deleted out from under it the moment either project was removed. Dropping them is called honest because captures are fingerprint-stamped and get retaken per variant anyway. | `lib/project-duplicate.ts:5-10,50-52`; `lib/persistence/blobs.ts:92-97` (sitting 10) · test `project-duplicate.test.ts:40-46` | working | NONE |
| CODE.step10-done.112 | **Override 5 — the name is passed through `copyName`.** | `lib/project-duplicate.ts:48` | working | SC.10-11.19 |
| CODE.step10-done.113 | **The source project is never mutated** (pinned by a JSON snapshot comparison before/after). | `lib/project-duplicate.ts:41-53` · test `project-duplicate.test.ts:59-64` | working | NONE |
| CODE.step10-done.114 | **`copyName(name, existing)` — base extraction.** Strips a trailing copy suffix with `/ \(copy( \d+)?\)$/` first, so duplicating "Villa (copy)" yields "Villa (copy 2)" rather than "Villa (copy) (copy)". | `lib/project-duplicate.ts:22-24` · test `project-duplicate.test.ts:72-76` | working | NONE |
| CODE.step10-done.115 | **`copyName` — first candidate is `"{base} (copy)"`;** if that name is already taken it counts `n = 2, 3, 4 …` and returns the first `"{base} (copy {n})"` not present in `existing`. Gaps are filled (the loop returns the first free number, not max + 1). Comparison is exact-string, case-sensitive. | `lib/project-duplicate.ts:23-30` · test `project-duplicate.test.ts:67-81` | working | NONE |
| CODE.step10-done.116 | **`copyName`'s unbounded `for (let n = 2; ; n++)`** terminates only because `existing` is finite; with an empty/duplicate-free list it exits on the first iteration. No cap, no guard — safe in practice, worth knowing. | `lib/project-duplicate.ts:26-29` | working | NONE |
| CODE.step10-done.117 | **DEFECT — the copy inherits the engineer's structural sign-off.** `structuralVerification` (`{ status: 'engineer_approved' \| 'pending', notes }`) is copied by the spread and is not in the override list. A variant that no engineer has ever seen therefore renders "Engineer approved" with the original's notes on the BOM, the drawings and the proposal. The census law is that sign-off is a recorded human decision pinned to what was reviewed. | `lib/project-duplicate.ts:41-53`; `lib/structure.ts:41-52` (`engineeringStatus`, sitting 5C); `screens/ProposalView.tsx:414-419` (sitting 11); `types.ts:994` | POC-DEFECT (visible-malfunction: unreviewed variant shows engineer-approved) | SC.10-11.16 · SC.10-11.18 |
| CODE.step10-done.118 | **The copy inherits the crew's installation tick-offs.** `project.installation.stepStates` is copied, so a brand-new variant can open its Installation Plan already part-ticked from the original's site progress. No override, no test. | `lib/project-duplicate.ts:41-53`; `screens/InstallationSheet.tsx:25-26` (sitting 12) | uncertain (no ruling in code; plausible-but-unstated intent) | NONE |
| CODE.step10-done.119 | **Derived stamps survive the copy, and the fingerprint graph makes that correct:** `derived.solarAccessFp` keys on `shadingFp`, which excludes the project name — so the copy's per-panel solar access stays fresh and is not recomputed. `derived.healthSnapshot` and the insight/comparison memos key on `designFp`, which **includes `info.name`** — so the rename alone re-keys them and the copy recomputes health/insights on open. | `lib/fingerprints.ts:175-185,260-285`; `lib/health.ts:154-160` · test `fingerprints.test.ts:245-251` | working | NONE |
| CODE.step10-done.120 | **Call site (sitting 10 owns the screen): Dashboard row menu → "Duplicate"** (`Copy` icon), one of four menu items (Show Proposal · Show 3D · BOM & Pricing · Duplicate) plus a destructive Delete separated by a rule. It calls `duplicateProject(p, { id: genId('prj'), shareId: newShareId(), now: Date.now() }, allProjectNames)` and dispatches `create-project`. | `screens/Dashboard.tsx:276-281,82-89` (sitting 10); `lib/geo.ts:627-631`; `store/store.tsx:79-83` | working | SC.10-11.19 |
| CODE.step10-done.121 | **Name uniqueness is checked GLOBALLY, not per lead** — `existingNames` is every project's name in the store, so "Sharma Residence (copy)" cannot collide with an unrelated customer's identically-named design either. There is no lead/parent entity in the POC to scope it to. | `screens/Dashboard.tsx:86` (sitting 10) | working | SC.10-11.19 |
| CODE.step10-done.122 | **Duplicating silently switches the active project and wipes undo history.** The `create-project` reducer prepends the copy, sets `activeProjectId` to it, and clears both `undoStack` and `redoStack`. The user stays on the Dashboard with no indication that the active document changed; the copy appears first under the default "recently updated" sort (its `updatedAt` is `now`). No navigation into the copy, no toast. | `store/store.tsx:158-165` (sitting 10); `screens/Dashboard.tsx:82-89` | POC-DEFECT (visible-malfunction: unsignalled active-project switch + undo loss) | NONE |
| CODE.step10-done.123 | **No variant *lineage* is recorded.** The copy keeps no pointer to its source (no `parentId`, no lead id, no "duplicated from"), and there is no `recommended` flag anywhere in the model. Variants exist only as sibling rows in a flat project list. | `lib/project-duplicate.ts:36-53`; `types.ts:920-996` | working (POC scope) | SC.10-11.19 (partial) |
| CODE.step10-done.124 | **No side-by-side variant comparison.** The Dashboard card shows thumbnail · status chip ("Proposal ready" / "In progress") · name · address ("No location set" when absent) · **CAPACITY kWp** (or "—") · **CUSTOMER** (or "—") · **UPDATED** (relative time). The census's variant comparison asks for system size, **annual generation, price and payback** with one marked recommended — generation, price, payback and the recommendation are absent. | `screens/Dashboard.tsx:315-345,438-468` (sitting 10) | working (code) / census gap | SC.10-11.19 (partial) |

---

## E · Cross-cutting — how "done" interacts with the wizard and the proposal status

| Key | Behavior (code-faithful) | Evidence | Status | Census |
|---|---|---|---|---|
| CODE.step10-done.125 | **Step 10 is reachable only when steps 1–9 all pass their gates.** `allowedStep` walks `nextBlocker(1…9)` and stops at the first blocked step; `allowedStep === 10` only when none blocks. A deep link to `/wizard/10` on an incomplete project redirects to the first blocking step (effect) and renders `null` in the meantime. | `screens/Wizard.tsx:160-174` (sitting 10) | working | SC.10-11.01 |
| CODE.step10-done.126 | **Getting to Step 10 stamps `wizardStep`.** `go(next)` patches `wizardStep: max(current, clamped)` — it only ever advances — so reopening the project from the Dashboard lands on `/wizard/10`, i.e. **a finished project reopens on the Done screen**, which immediately re-asserts `proposal_ready`. | `screens/Wizard.tsx:176-180` (sitting 10); `screens/Dashboard.tsx:100-103`; `screens/Step10Done.tsx:13-18` | working | SC.10-11.01 |
| CODE.step10-done.127 | **Step 10 has no forward gate and no `nextBlocker` case** — `nextBlocker` returns `null` for step 10 (and the header shows Done instead of Next). Back from Step 10 goes to Step 9 (`go(step - 1)`); the 4→6 / 6→4 skip (no Step 5 screen) does not apply here. | `screens/Wizard.tsx:107-146,181-194,320-329` (sitting 10) | working | NONE |
| CODE.step10-done.128 | **Wizard chrome present on the Done screen:** header "Step 10 of 10 · **Final**" · Back · design-health chip (or the disabled "Health —" chip) · unit toggle · Save (patches `{}` and toasts "Project saved" for 1400 ms) · Save & go home · Help · the header Done button · a 3 px progress bar at **100 %** width (`step / 10`). Body background is `var(--paper)` (step 10 is not one of the dark steps 2/3/6). | `screens/Wizard.tsx:194,245,293-341` (sitting 10) | working | SC.10-11.01 |
| CODE.step10-done.129 | **Step 10 Help sheet copy:** does = "Issue the proposal and share it."; one tip = "The share link opens a read-only copy of this design." | `screens/Wizard.tsx:103-106` (sitting 10) | working | SC.10-11.05 |
| CODE.step10-done.130 | **`proposal_ready` is surfaced only on the Dashboard** — as a green "Proposal ready" chip on the card and as one of three filter tabs (All / In progress / Proposal ready) with live counts. Nothing else in the app reads `status`; it gates no export, no share and no proposal render. | `screens/Dashboard.tsx:73-80,195-199,318-345` (sitting 10) | working | SC.10-11.01 |
| CODE.step10-done.131 | **An unapproved design is NOT withheld from the customer.** `ProposalView` and `ShareViewer` render regardless of `structuralVerification`; the only consequence of "pending" is the label "Mounting structure — PRELIMINARY — pending engineer verification" in the proposal's structure disclaimer. The census law (`SC.10-11.18`) says an unapproved design is never shown to the customer. | `screens/ProposalView.tsx:414-419` (sitting 11); `screens/ShareViewer.tsx:6-29` (sitting 11); `lib/structure.ts:41-52` (sitting 5C) | POC-DEFECT (docs-contradict: census law not enforced in code) | SC.10-11.18 (partial) |
| CODE.step10-done.132 | **There is no engineer sign-off queue, no review screen and no return-with-comments flow anywhere in `src/`.** The only sign-off surface is a two-chip toggle on Step 8's drawings (`pending` / `engineer_approved` + free-text notes) — self-service, with **no record of who approved or when**, and no append-only history. | `screens/Step8Sld.tsx:132-140` (sitting 7); repo-wide grep for `structuralVerification` returns only Step8Sld, structure.ts, types.ts and one test | POC-DEFECT (docs-contradict: census §A.10-11 sign-off entries unimplemented) | SC.10-11.14 · .15 · .16 · .17 (partial/absent) |

---

## Census diff — `M05-design-studio.md` Appendix A §A.10-11 (`SC.10-11.01`–`SC.10-11.19`, 19 entries)

| Census entry | Disposition | Where |
|---|---|---|
| `SC.10-11.01` | **matched** — Done screen states completion, names the project, reassures it can be reopened | CODE.step10-done.1, .3, .7, .12–.16, .125–.128, .130 |
| `SC.10-11.02` | **matched** — View proposal | CODE.step10-done.18, .25 |
| `SC.10-11.03` | **matched** — BOM & pricing (→ Step 9) | CODE.step10-done.19 |
| `SC.10-11.04` | **matched** — Installation plan (opens the existing `InstallationSheet`) | CODE.step10-done.20 |
| `SC.10-11.05` | **matched, with a superseding ruling** — Copy 3D share link exists; the copied URL is the 3D `/share/<shareId>` link, which owner ruling Q27 (2026-08-04, M05-75) supersedes with the proposal link | CODE.step10-done.9–.11, .21, .22, .26, .129 |
| `SC.10-11.06` | **matched** — Done → the project list (twice: body + header) | CODE.step10-done.23, .24 |
| `SC.10-11.07`–`.12` | **covered by another sitting** — installation-plan internals (work order, progress + "done of total", tick-off items, phase headings, Print, empty state) live in `screens/InstallationSheet.tsx` + `lib/installation.ts`, **sitting 12**. Step 10 owns only the launch control (CODE.step10-done.20). Not unmatched. | sitting 12 |
| `SC.10-11.13` | **covered by another sitting** — "crew sees no money": `InstallationSheet.tsx` contains no currency, price or cost field (verified by grep). | sitting 12 |
| `SC.10-11.14` | **UNMATCHED** | see below |
| `SC.10-11.15` | **UNMATCHED** | see below |
| `SC.10-11.16` | **PARTIAL** — a `pending` / `engineer_approved` chip pair + notes exists on Step 8 (sitting 7) and its label travels onto every engineering surface, but nothing records **who** or **when**, nothing pins the approval to a design version/fingerprint, there is no append-only history, and a duplicate silently inherits the approval (CODE.step10-done.117) | CODE.step10-done.117, .132 |
| `SC.10-11.17` | **UNMATCHED** | see below |
| `SC.10-11.18` | **PARTIAL** — "the app never computes structural adequacy" IS honored (`engineeringStatus` renders "PRELIMINARY — pending engineer verification" and the structure disclaimer travels with every structure-bearing output), but "an unapproved design is never shown to the customer" is **not enforced**: the proposal and the 3D share view render regardless | CODE.step10-done.131 |
| `SC.10-11.19` | **PARTIAL** — duplicate-as-variant works and is pure/tested (CODE.step10-done.106–.124), but there is no lead entity to group variants under, no lineage pointer, no "recommended" marker, and the list card compares only capacity/customer/updated — not annual generation, price or payback | CODE.step10-done.106–.124 |

### CENSUS-UNMATCHED (regression-risk list — census entries in §A.10-11 with no code behavior)

| Census entry | What the census requires | Nearest code | Risk |
|---|---|---|---|
| `SC.10-11.14` | Engineer sign-off **queue** for the Engineer role: designs waiting, **oldest first** | none — no queue surface, no role model, no "submitted for review" state in `ProjectStatus` (`in_progress \| proposal_ready` only) | whole feature absent |
| `SC.10-11.15` | Queue entry shows customer · system size · who designed it · how long it has been waiting; open → review | none — no reviewer-facing surface; `Project` carries no author/designer field and no submitted-at timestamp | whole feature absent |
| `SC.10-11.17` | Review action **Return with comments**, each comment **pinned to the thing it refers to**, design goes back to the designer with a notification | none — no comment model, no pin/anchor model, no notification hook anywhere in `src/` | whole feature absent |
| `SC.10-11.16` (partial) | Approve **records** the structural verification (who + when), design may then go to the customer | `screens/Step8Sld.tsx:132-140` — status chip + free-text notes only | who/when/pinning/append-only all missing |
| `SC.10-11.18` (partial) | An unapproved design is **never shown to the customer** | `screens/ProposalView.tsx`, `screens/ShareViewer.tsx` — render unconditionally | customer-facing gate missing |

---

## Summary

- **rowCount:** 132 (`CODE.step10-done.1`–`.132`, contiguous, no gaps; every row has 5 filled cells)
- **status split:** 119 working (incl. 6 qualified "working (…)") · **10 POC-DEFECT** · 3 uncertain
- **defect list:** `.8` finalize-vs-duplicate contradiction · `.21`/`.22` silent copy-link (+ unhandled rejection) · `.37` "ignored" insights still count as open · `.52` stale cover passes the review · `.59` finalize happens where readiness is never shown · `.117` duplicate inherits engineer approval · `.122` duplicate silently switches active project + wipes undo · `.131` unapproved design still shown to the customer · `.132` sign-off queue/return flow absent
- **censusUnmatched:** `SC.10-11.14`, `SC.10-11.15`, `SC.10-11.17` (absent); `SC.10-11.16`, `SC.10-11.18`, `SC.10-11.19` (partial)
- **newBeyondCensus:** 92 rows carry `census: NONE` — almost all of them the fingerprint graph (`.60`–`.105`, 46 rows) and the review's internals (`.28`–`.59`), neither of which the census names at all
- **tests:** 51/51 pass (fingerprints 19 · review 11 · project-duplicate 9 · persistence 12); no failing test in this area

