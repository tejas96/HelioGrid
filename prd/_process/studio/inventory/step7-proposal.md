# Feature inventory — step7-proposal (Step 7 · Proposal captures & readiness + the studio's ENERGY and FINANCE model)

Pass-two inventory, 2026-08-05 (Sitting 6). Primary files, all under `3d_design_studio/src/features/solar-studio/` unless noted: `screens/Step7Proposal.tsx` (396 ln) · `components/EnergyReportSheet.tsx` (267 ln) · `lib/solar.ts` (261) · `lib/poa.ts` (87) · `lib/pvgis.ts` (130) · `lib/weatherApi.ts` (40) · `src/app/api/pvgis/route.ts` (83) · `lib/finance.ts` (82) · `lib/financing.ts` (122) · `lib/comparison.ts` (439) · `lib/proposal-narrative.ts` (127) · `lib/insights/{registry,types,analyzers,analyzers-access}.ts` (95/51/147/249).

**Test run** (read-only, deps from lockfile, vitest 3.2.7): `npx vitest run` over the 13 owned/adjacent test files → **13 files, 132 tests, ALL PASS** (874 ms): `comparison` 25 · `pvgis` 14 · `solar` 13 · `review` 11 · `insights-analyzers` 10 · `analyzers-access` 15 · `financing` 8 · `finance` 7 · `weatherApi` 7 · `insights` 7 · `poa` 5 · `proposal-narrative` 5 · `tariff` 5. **No failing test ⇒ no test-derived DATA finding.** Numeric claims below were additionally re-derived live with `npx tsx` against the real modules (subsidy slab, loss stack + PR, POA factors at Pune, financing options, payback/25-yr loops).

**One-hop files consulted but owned elsewhere** (cited, never re-inventoried): `lib/review.ts` + `lib/fingerprints.ts` (`layoutFp` / `isCaptureFresh` / `isShadingFresh` / `capturesFresh` / `designFp`) — sitting 9 · `lib/bom.ts` `mergedBom`/`bomTotal`/`bomConfidence` + `data/rules/india.ts` (subsidy, financing, O&M thresholds) — sitting 8 · `three/Scene3D.tsx` (`seasonDate`, capture button, energy-report toolbar button) + `lib/layout.ts` `estimateMaxCapacityKwp`/`fillRoofAsSegment` + `lib/spacing.ts` `shadowFreePitchM` — sitting 5 · `screens/Wizard.tsx` header Back/Next + health chip and `lib/persistence/blobs.ts` `putImage` — sitting 10 · `screens/ProposalView.tsx` (`/proposal`) — sitting 11 · `data/discoms.ts` tariff table (`tariff.test.ts` is claimed by this sitting but its subject is sitting 13's) — sitting 13.

**Ownership note.** `lib/comparison.ts` is owned here (§P) because it *is* an energy+finance pipeline; its only UI carrier is Step 4's compare sheet, which `step4-components.md` inventories from the screen side. `lib/__tests__/gemini-client.test.ts` is claimed by this sitting by the ledger but its subject (`lib/roof-ai/gemini-client.ts`) is sitting 2's — recorded, not inventoried.

---

## §A — Step shell & the two-phase machine

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.1 | Step 7 is a **two-phase screen inside one component**, switched by a single `activeIdx: number \| null` state: non-null ⇒ **phase A capture studio** (full-bleed `position:absolute; inset:0` `Scene3D`), null ⇒ **phase B review** (640 px-max centred column, `padding 26px 20px 90px`). There is no route change and no chip — the phase is invisible to the URL and to Back/Forward | Step7Proposal.tsx:34-36,82-171,182-183 | working | SC.10-8.01, SC.10-8.13 |
| CODE.step7-proposal.2 | Entry phase is chosen once at mount from `project.captures.length >= 4 ? null : 0` — it counts capture **records**, not records that actually carry an `imageBlobId`. Four records whose blobs all failed to save still land the user straight in review with four "Not captured" tiles | Step7Proposal.tsx:34-36 vs the `c.imageBlobId` predicate used everywhere else (41,117,127,260,290) | POC-DEFECT(entry gate counts records, not saved images — inconsistent with every other capture count on the screen) | SC.10-8.01 |
| CODE.step7-proposal.3 | Re-entering Step 7 with a partial set always resumes at **shot 1**, not at the first uncaptured shot (`0`, a literal). Auto-advance to "next un-captured" exists only *after* a successful save | Step7Proposal.tsx:35,77-78 | working (divergence: §10.8's auto-advance rule is save-scoped in the POC; entry is not) | SC.10-8.09 |
| CODE.step7-proposal.4 | The wizard header (Back · "Step 7 of 10 · Proposal" · health chip · units · Save · Home · Help · **Next**) sits ABOVE the `flex:1; overflow:auto; position:relative` body, so the capture studio's `inset:0` covers only the body — **Back → Step 6 and Next → Step 8 stay reachable in both phases** | Wizard.tsx:189-195,207,320-329,344 (one hop) | working | SC.10-8.01 (nav per §10.8 "Back → 10.6 · Next → 10.9") |
| CODE.step7-proposal.5 | `computeEnergyReport(project)` runs **unconditionally at the top of the component**, on every render, including the whole capture phase where the result is never read | Step7Proposal.tsx:38 vs 82-171 | working (dead compute in phase A; full model runs per render) | NONE |
| CODE.step7-proposal.6 | The entire screen is inline `style={{}}` with raw hex/px and `var(--x, #hex)` fallbacks (`#fef2f2`, `#dc2626`, `#7f1d1d`, `#374151`, `rgba(20,23,28,0.92)`, `#9ca3af`, `#fff`, `#111`) — the legacy studio stylesheet path, not the `.ds` token contract | Step7Proposal.tsx:85-171,175-179,242-254,270-288 vs `docs/DESIGN-SYSTEM.md` / CLAUDE.md rule 1 | working (legacy screen, area-level note; recorded once, not per row) | NONE |

## §B — Phase A · the capture studio

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.7 | Exactly **four fixed presets**, in this order and with these exact settings: `sum_am` "Summer Morning" `seasonDate('summer')` 09:00 mode `shadow` · `sum_noon` "Summer Noon" 12:00 `shadow` · `sa_summer` "Solar Access (Summer)" 12:00 `solar_access` · `sa_winter` "Solar Access (Winter)" `seasonDate('winter')` 12:00 `solar_access`. `seasonDate` returns **21 Jun** (summer) and **21 Dec** (winter) of the **current calendar year** at 12:00 local | Step7Proposal.tsx:21-28; Scene3D.tsx:77-85 (one hop) | working | SC.10-8.01–SC.10-8.05 |
| CODE.step7-proposal.8 | Each preset drives `Scene3D` via `initial={{ date, hour, solarAccess: mode === 'solar_access' }}`, which seeds `useState` **once** (`initial?.date ?? new Date()`, `initial?.hour ?? 12`, `initial?.solarAccess ?? false`). `key={preset.id}` forces a remount per shot, so switching shots genuinely re-seeds the sun/date/view | Step7Proposal.tsx:86-91; Scene3D.tsx:234-237 (one hop) | working | SC.10-8.01–SC.10-8.05 |
| CODE.step7-proposal.9 | Once seeded, the scene's date / hour / solar-access controls are fully live — the user can move the sun off the preset and still press Capture. **The stored `ShadowCapture` records the PRESET's `dateIso`/`hour`/`mode`, not the state the picture was actually taken at**, and the review tile prints those preset values under the image | Step7Proposal.tsx:57-66,311-317; Scene3D.tsx:405-409 (the capture path passes only the data URL back) | POC-DEFECT(a capture's printed date/hour can be a caption for a picture taken at a different sun position) | SC.10-8.16 |
| CODE.step7-proposal.10 | The **Capture** control is `Scene3D`'s own button (`btn-primary`, `<Camera/> Capture`, fixed at `right:16 bottom:128 z:31`), rendered only when `captureMode`. It grabs the WebGL canvas with `toDataURL('image/jpeg', 0.85)` at the canvas's current pixel size — no fixed export resolution, no crop, no aspect contract | Scene3D.tsx:405-409,682-691 (one hop); Step7Proposal.tsx:88-90 | working | SC.10-8.06 |
| CODE.step7-proposal.11 | Progress counter reads **`Shadow captures: {N}/4`** where `N = project.captures.filter(c => c.imageBlobId).length` | Step7Proposal.tsx:116-118 | working (wording: census/§10.8 specify "Shadow captures: N **of** 4"; the POC prints `N/4`) | SC.10-8.07 |
| CODE.step7-proposal.12 | Four 22×22 numbered buttons: green `var(--good)` + `<Check strokeWidth=3/>` once captured, white/`#111` when active, `#374151`/`#9ca3af` otherwise; `aria-label` = `"{label}"` or `"{label} (captured)"`; clicking any one jumps to that shot to take or retake it | Step7Proposal.tsx:119-140 | working | SC.10-8.08 |
| CODE.step7-proposal.13 | After a **successful** save the screen auto-advances to `all.findIndex(p => !captures.some(c => c.id === p.id && c.imageBlobId))`; `-1` (all four done) drops straight into review | Step7Proposal.tsx:77-78 | working | SC.10-8.09 |
| CODE.step7-proposal.14 | **The first successfully saved capture becomes the cover**: `coverImageBlobId: project.coverImageBlobId ?? blobId`, stamped with `coverForLayoutFp = layoutFp(project)` at capture time. The same blob id is shared by the capture and the cover (GC is reference-counted per project, per the in-file comment) | Step7Proposal.tsx:57,69-75 | working | SC.10-8.10 |
| CODE.step7-proposal.15 | **Skip to review** — a `chip` (`#374151`, white) that sets `activeIdx = null` at any point, no confirmation, no count check | Step7Proposal.tsx:141-147 | working | SC.10-8.11 |
| CODE.step7-proposal.16 | Save-error path: `putImage(dataUrl)` is awaited in a `try`; on throw the capture is **not** written and a `role="alert"` red banner (`#7f1d1d`) appears under the checklist reading *"{label} could not be saved — browser image storage is unavailable (private mode or storage full). Free space and capture again."* — the cause and the remedy, then `return` so `activeIdx` stays on the failed shot | Step7Proposal.tsx:43-56,149-168 | working | SC.10-8.12 |
| CODE.step7-proposal.17 | The save-error banner is rendered **only inside phase A**; `saveError` is never cleared on shot switch or on "Skip to review", so it silently disappears (and its state survives) when the user leaves the capture studio | Step7Proposal.tsx:82,149-168 (no reset on 122/144) | POC-DEFECT(error state neither cleared nor carried into review) | SC.10-8.12 |
| CODE.step7-proposal.18 | Every capture write is `patch({...})` at its **default `undoable = false`** — taking or retaking a shot, and the auto-cover assignment, push nothing onto the undo stack. Only "Set as cover" passes `true` | Step7Proposal.tsx:69-75 vs 343 | working (inconsistency recorded) | SC.10-8.10, SC.10-8.16 |

## §C — Phase B · the "Before you issue" readiness card

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.19 | Verdict banner from `preProposalReview(project).overall`, mapped to three tones/labels: `blocked` → **"Not ready to issue"** (`--bad`/`--bad-bg`) · `attention` → **"Ready — with caveats"** (`--warn`) · `ready` → **"Ready to issue"** (`--good`). Card is `role="status" aria-live="polite"` with a 3 px left border in the tone colour | Step7Proposal.tsx:174-196 | working (label wording differs from the census's NOT READY · READY WITH CAVEATS · READY TO ISSUE, semantics identical) | SC.10-8.13 |
| CODE.step7-proposal.20 | `overall` = worst of the four items (`blocked` > `attention` > `ready`); `issuable = overall !== 'blocked'` | review.ts:155-161 (one hop) + review.test.ts "blocked beats attention", "issuable is true whenever nothing is blocked" | working | SC.10-8.13 |
| CODE.step7-proposal.21 | Each item renders: a status glyph (`Check` green / `XCircle` red / `AlertTriangle` amber, `aria-hidden`) + an `sr-only` `" — ready / blocked / needs attention"` suffix on the title + a plain-language `detail` + a **`Step {n}` jump button** shown only when `status !== 'ready' && step !== 7` | Step7Proposal.tsx:197-228 | working | SC.10-8.14 |
| CODE.step7-proposal.22 | Item 1 **Electrical design** (step 6) — the ONLY item that can block. Derives from the same `electricalGate(project)` the wizard's Next uses, so the card and the gate cannot disagree; ready text: *"Every enabled module is wired into a string within the inverter's limits."* | review.ts:45-63 (one hop) + review.test.ts:49 "agrees with the wizard gate — it derives from the SAME function" | working | SC.10-8.13, SC.10-8.14 |
| CODE.step7-proposal.23 | Item 2 **Design review** (step 6) — open insights = `memoizedInsights(project)` minus those the user marked `accepted`. Status is `attention` only when an open item is `warning`/`critical`; a set of pure suggestions stays **ready** with the text *"N optional suggestions, none blocking: …"* (max 3 titles, then `…`), deliberately avoiding a green tick beside a contradiction | review.ts:65-96 (one hop) | working | SC.10-8.14 |
| CODE.step7-proposal.24 | Item 2 reads the analyzer registry **without registering anything**. Registration happens at *module scope* in `Step6Editor.tsx:125` (`registerAllAnalyzers()`) and defensively inside `health.ts:158,180` — `review.ts` has no such call. Today the Wizard statically imports `Step6Editor`, so the registry is populated by import side-effect; the guarantee is import-order, not code | review.ts:17,66; Step6Editor.tsx:125; health.ts:158,180; analyzers.ts:134-147 | uncertain (latent false-green: an empty registry makes "Design review" read *"No open suggestions…"*; only a static-import side effect prevents it) | SC.10-8.14 |
| CODE.step7-proposal.25 | Item 3 **Quantity confidence** (step 9) — `bomConfidence(mergedBom(project))`; `attention` + *"Preliminary — N lines need site verification: …"* (max 3) when any line is estimated/assumed, else *"Every quantity is measured or derived from the design."* | review.ts:98-120 (one hop) + review.test.ts:87 | working | SC.10-8.14 |
| CODE.step7-proposal.26 | Item 4 **Shadow study imagery** (step 7) — three-way: `< 4` saved captures ⇒ attention *"N of 4 captures taken."*; else any `!isCaptureFresh` ⇒ attention *"N capture(s) show an older version of the layout — retake before issuing."*; else ready. **Never blocks** — "a missing photo is not an engineering fault" | review.ts:122-153 (one hop) + review.test.ts:79 | working | SC.10-8.14, SC.10-8.20 |
| CODE.step7-proposal.27 | Item 4 checks `project.captures` only. **The cover image's own freshness (`coverForLayoutFp`) is never checked by the card** — `capturesFresh()` in fingerprints does check it, but `review.ts` uses the per-capture `isCaptureFresh` instead | review.ts:127-128 vs fingerprints.ts:312-316 (one hop) | POC-DEFECT(cover staleness invisible on the readiness card; see also row 33) | SC.10-8.20 |

## §D — Phase B · cover image, the shadow set, and the staleness law

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.28 | COVER IMAGE section: `<BlobImg>` at full column width (radius 12, `placeholderHeight 220`) when `coverImageBlobId` is set; otherwise a card with an `ImageOff` glyph and the words **"No cover captured yet"** | Step7Proposal.tsx:231-255 | working | SC.10-8.15 |
| CODE.step7-proposal.29 | SHADOW ANALYSIS section: a fixed **2×2 grid** over the same four presets. Each tile shows the image or a `--paper-3` placeholder with `ImageOff` + **"Not captured"**, then the preset label (12 px/700) and `"{d MMM yyyy} · {hour}:00"` formatted `en-IN` | Step7Proposal.tsx:257-317 | working | SC.10-8.16 |
| CODE.step7-proposal.30 | **Staleness badge**: `stale = !!blobId && !!cap && !isCaptureFresh(project, cap)`, i.e. `cap.forLayoutFp !== layoutFp(project)`. Renders an amber (`--warn`) pill top-right of the tile reading **"Outdated — retake"** with `<RefreshCw/>`, whose click jumps straight to that shot in the capture studio | Step7Proposal.tsx:262-289; fingerprints.ts:306-309 (one hop) | working | SC.10-8.16, SC.10-8.20 |
| CODE.step7-proposal.31 | The staleness definition is `layoutFp` — roof/keepout/walkway geometry + every panel's id/centre(cm)/roof/orientation/azimuth/tilt/enabled/segment + segment polygons/racking + leg plans + the structure model version. A component swap, a price edit or a tariff change does NOT stale a capture; a moved panel does | fingerprints.ts:86-150 (one hop) | working | SC.10-8.20 |
| CODE.step7-proposal.32 | Cover marker / action per tile: the tile whose blob **is** the cover shows a green `<Check/> Cover image`; every other captured tile gets a full-width **"Set as cover"** `btn-secondary`. Uncaptured tiles get neither — only a captured image can become the cover | Step7Proposal.tsx:318-348 | working | SC.10-8.16 |
| CODE.step7-proposal.33 | **"Set as cover" stamps `coverForLayoutFp: layoutFp(project)` — the CURRENT layout — regardless of the capture's own `forLayoutFp`.** Promoting a capture that is displaying "Outdated — retake" therefore marks the *cover* as fresh; `capturesFresh()` (which the built proposal at `/proposal` uses to decide whether to warn) then passes the cover while the tile beside it says the image is out of date | Step7Proposal.tsx:342-344 vs 262; fingerprints.ts:312-316; ProposalView.tsx:76,113 (one hop) | POC-DEFECT(cover freshness is stamped from the app's clock, not the image's — a stale image can be laundered into a "fresh" cover) | SC.10-8.20 |
| CODE.step7-proposal.34 | The COVER IMAGE preview itself carries **no staleness badge** — only the four tiles do. Mitigated in practice because the cover blob is always one of the four captures, whose tile does badge | Step7Proposal.tsx:231-255 (absence) | POC-DEFECT(the largest, most customer-facing image on the screen is the one with no honesty marker) | SC.10-8.15, SC.10-8.20 |
| CODE.step7-proposal.35 | Captures cannot be **deleted or cleared** — only overwritten by retaking the same preset id (`others = captures.filter(c => c.id !== preset.id)`). There is no way to un-set a cover once one exists | Step7Proposal.tsx:67-68 (absence of a delete path) | working (not census-listed) | NONE |
| CODE.step7-proposal.36 | Image bytes live in IndexedDB via `putImage`; the project stores only `imageBlobId`. Rendering goes through `<BlobImg>` (async blob → object URL) with a `placeholderHeight` so the layout does not jump | Step7Proposal.tsx:9-10,49,232-238,290-291; persistence/blobs.ts (one hop, sitting 10) | working | SC.10-8.15, SC.10-8.16 |

## §E — Phase B · system summary and the two actions

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.37 | SYSTEM SUMMARY is exactly four `Row` pairs from `computeEnergyReport`: **Capacity** `{capacityKwp} kWp` · **Panels** `{panelCount} × {components.panel?.watt}W` · **Solar Access** `{avgSolarAccessPct}%` · **Annual Generation** `{annualMwh} MWh` | Step7Proposal.tsx:354-360,389-396 | working | SC.10-8.17 |
| CODE.step7-proposal.38 | With no panel selected the Panels row renders the literal string **"0 × undefinedW"** (`project.components.panel?.watt` is not defaulted, unlike `EnergyReportSheet`'s `?? 0`) | Step7Proposal.tsx:357 vs EnergyReportSheet.tsx:56 | POC-DEFECT(undefined leaks into a customer-facing summary) | SC.10-8.17 |
| CODE.step7-proposal.39 | **The system summary carries no provenance and no freshness label.** The same four numbers in `EnergyReportSheet` sit under an explicit "Real irradiance — PVGIS {db} ({N}-yr record)" / "Built-in irradiance model (latitude fit, ±10%)" line and behind an `isShadingFresh` "provisional" banner; the screen that hands these numbers to the proposal shows them bare | Step7Proposal.tsx:354-360 (absence) vs EnergyReportSheet.tsx:32-52,86-98 | POC-DEFECT(F8-08/F8-09 provenance binding + CLAUDE.md "every user-visible number carries a provenance tier" unmet at the hand-off point) | SC.10-8.17 |
| CODE.step7-proposal.40 | **Edit Photos** (`btn-secondary`, `<Camera/>`) → `setActiveIdx(0)`, i.e. always back to shot 1 (not to the first uncaptured shot) | Step7Proposal.tsx:363-365 | working | SC.10-8.18 |
| CODE.step7-proposal.41 | **Generate Proposal** (`btn-primary`, `<FileText/>`, `flex:1`) does exactly two things: `patch({ status: 'proposal_ready' })` (non-undoable) and `navigate('/proposal')`. Nothing is copied or snapshotted — `ProposalView` re-reads the live active project, so "hands numbers + captures to the proposal" is by shared state, not by hand-off payload | Step7Proposal.tsx:366-375; ProposalView.tsx:76,254-275 (one hop) | working | SC.10-8.19 |
| CODE.step7-proposal.42 | **The Generate Proposal button is never disabled** — it is fully enabled while the card above it reads "Not ready to issue" (`review.issuable === false`). No confirmation, no toast, no reference to the verdict | Step7Proposal.tsx:366-375 vs 174-180 | working *by design* per M05-61/R12 (mandatory-component + payable-floor enforcement is re-homed to M06's Generate) — **recorded: the studio's own verdict has zero effect on the studio's own issue action** | SC.10-8.13, SC.10-8.19 |
| CODE.step7-proposal.43 | `status: 'proposal_ready'` is **not exclusive to this action** — `Step10Done` sets the same status on mount, so a project can read "Proposal ready" on the dashboard without Step 7's Generate ever being pressed | Step7Proposal.tsx:370; Step10Done.tsx:14-15; Dashboard.tsx:77-78,343 (one hop) | working (recorded) | SC.10-8.19 |

## §F — The ENERGY model (`lib/solar.ts` · product level)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.44 | **One energy function for the whole product.** `computeEnergyReport(project)` is pure `f(project)` and is the single producer of every energy number anywhere: Step 7's summary, the Energy Report sheet, `computeFinancials`, `proposalNarrative`, every comparison row, `panelEnergyShares`, the health score and the proposal/share views. There is no second model | solar.ts:115-244; consumers at Step7Proposal.tsx:38, EnergyReportSheet.tsx:22, finance.ts:29, proposal-narrative.ts:30, comparison.ts:258, solar.ts:96 | working | SC.10-8.17 |
| CODE.step7-proposal.45 | Capacity `capacityKwp = (enabled panel count × components.panel.watt) / 1000`, rounded to 2 dp. **Disabled panels are excluded from every downstream number** (capacity, energy, access, finance) but still exist in the layout | solar.ts:116-118,225 | working | SC.10-8.17 |
| CODE.step7-proposal.46 | Roof area is `roofsUnionAreaM2(project.roofs)` — a **union, not a sum**, so a mumty/stacked roof cannot double-count its footprint. Rounded to whole m²; the only consumer is the report's "Roof Area" stat and the narrative's "we modelled X of usable roof" | solar.ts:120,227; roof-topology.ts (one hop) | working | NONE (report-level) |
| CODE.step7-proposal.47 | **Loss stack (equipment)** — five fixed lines, percentages hardcoded in `equipmentLosses()`: `temperature 8.1` · `soiling 3.0` · `inverter` = `round((100 − efficiencyPct)·10)/10` when the selected inverter's datasheet efficiency is in (50,100), else **3.0** · `mismatch 2.0` · `dc_wiring 2.0` | solar.ts:42-54; probe: default set sums to **18.1 %** | working | NONE |
| CODE.step7-proposal.48 | **Losses compose MULTIPLICATIVELY** (PVWatts/PVsyst convention `PR = Π(1−lᵢ)`), never additively, and the product is clamped to `[0.005, 1]`. Probed: default stack ⇒ `PR_equip = 0.83045` (16.96 % total, not 18.1 %); with a 98.4 %-efficient inverter ⇒ inverter line 1.6 % and `PR_equip = 0.84243` | solar.ts:56-60,144-145; solar.test.ts:110 "losses compose multiplicatively — total < the arithmetic sum", :117 "inverter loss comes from the selected inverter datasheet efficiency" | working | NONE |
| CODE.step7-proposal.49 | **Shading is deliberately NOT in the equipment stack.** It applies to the BEAM component only, inside the plane-of-array term, so a fully beam-shaded module still collects the diffuse share. Inter-row self-shading is likewise not a separate derate — modules are shadow casters in the raycast engine, so row-on-row loss already lives inside each panel's measured `solarAccess` (an earlier analytical derate priced the same physics twice and was removed) | solar.ts:37-41,136-162 | working | NONE |
| CODE.step7-proposal.50 | **PVGIS path** (weather present and pinned): per month `kWh_m = capacityKwp × GHI_day[m] × days[m] × PR_equip × (Kd[m] + (1−Kd[m])·meanBeamRatio·beamAccess)`, where `days` is the FIXED calendar `[31,28,31,…]` (leap years ignored; the ÷days in the mapper and ×days here cancel exactly) and `meanBeamRatio` is the unweighted mean of `poaBeamRatio` over enabled panels. Annual = Σ months | solar.ts:163-189; pvgis.ts:15,102; solar.test.ts:58 "labels the source PVGIS and sums months to the annual total" | working | NONE |
| CODE.step7-proposal.51 | **Estimate path** (no usable weather): `annualKwh = capacityKwp × peakSunHours × 365 × PR_equip × (0.35 + 0.65·meanBeamRatio·beamAccess)`, with `peakSunHours` defaulting to **5.3** when there is no location. Months are then a pure *shape* split of that annual figure by hardcoded `MONTH_FACTORS = [0.95,1.0,1.12,1.13,1.15,0.88,0.78,0.80,0.86,1.02,0.98,0.93]` — an India monsoon curve applied at any latitude and either hemisphere | solar.ts:27-30,121,190-205 | working (India-shaped constants live in a market-neutral module — pack-law note, see row 91) | NONE |
| CODE.step7-proposal.52 | `MONSOON_MONTHS = [5,6,7,8]` (Jun–Sep, 0-indexed) is a constant of the module, surfaced as the amber bars in the report — it is not derived from the site's own Kd or GHI curve even when real PVGIS data is present | solar.ts:30,234; EnergyReportSheet.tsx:111 | working | NONE |
| CODE.step7-proposal.53 | Both paths default the site to **lat 20, lng 77** (geometric centre of India) when `project.location` is absent — a silent, market-specific fallback inside the POA/beam computation | solar.ts:98-99,127-128 | working (recorded; see row 91) | NONE |
| CODE.step7-proposal.54 | **Shading loss line.** `shadeFactor = shaded/unshaded` (GHI-weighted over the 12 months in the PVGIS path, single-ratio in the estimate path); the report appends a 6th loss line `{key:'shading', label:'Shading (beam)', pct: round((1−shadeFactor)·1000)/10}`. At **zero** beam access the line reads 30–90 %, never 100 % — the diffuse share survives | solar.ts:171-201,209-214; solar.test.ts:154 "shading loss line reflects the BEAM-only effect, never 100%" | working | NONE |
| CODE.step7-proposal.55 | **Displayed Performance Ratio** `= clamp(PR_equip × shadeFactor, 0.005, 1)`, ×100 to 0.1 %. It includes equipment + beam shading but **excludes orientation** (POA is the reference plane). `totalLossPct = round((1−PR)·1000)/10`. Gate: PR stays in (0,100] and `totalLossPct < 100` at every beam access from 0 to 1, on both paths | solar.ts:206-215,232; solar.test.ts:98 "PR stays within (0,1] even at ZERO beam access (old additive model went negative)" | working | NONE |
| CODE.step7-proposal.56 | **`avgSolarAccessPct` is a diffuse-floored comparability score, not an energy fraction**: `round((0.35 + 0.65·beamAccess)·100)` — the FIXED `DIFFUSE_SHARE`, used identically on both paths (even when the real monthly Kd is driving the energy). Consequence: a fully beam-shaded array reports **35 % solar access**, an unshaded one 100 %. Chosen so this metric is byte-identical to the heatmap's colour metric | solar.ts:185,201,238; solar.test.ts:142 "unified access metric: diffuse-floored (= heatmap definition), not raw beam" (asserts exactly 35 and 100) | working (by design; but see row 105 — the narrative sentence built on it is not) | SC.10-8.17 |
| CODE.step7-proposal.57 | **`beamAccess` is an unweighted arithmetic mean** of `p.solarAccess ?? 1` over enabled panels — not weighted by panel wattage, orientation or irradiance. A design mixing a clear main array with a few heavily shaded modules is modelled at the mean, i.e. a mean-field model, not a per-panel sum | solar.ts:122-125,146 | working (documented mean-field choice; recorded because the customer-facing wording implies a per-array measurement) | SC.10-8.17 |
| CODE.step7-proposal.58 | **The report's `poaFactor` readout is contaminated by shading.** PVGIS path: `reportPoa = annualKwh / (capacityKwp × annualGhi × 365 × PR_equip)` — the numerator already carries `beamAccess`. Estimate path: `reportPoa = poaShaded`, likewise. The Energy Report renders this as **"Tilt/Azimuth (POA) ×{poaFactor}"** with the footnote "POA = beam-weighted plane-of-array estimate", so on any shaded design the tilt/azimuth figure silently reports the shading loss a second time, next to the loss line that already reports it | solar.ts:186-188,199,228; EnergyReportSheet.tsx:81-84,97 | POC-DEFECT(a labelled orientation-only factor that includes shading; double-reads the same loss on one card) | NONE |
| CODE.step7-proposal.59 | `meanPoa` (a full per-panel `poaFactor` loop, solar.ts:129-135) is **dead in practice** — both branches overwrite `reportPoa`; it survives only as the `panels.length === 0` / zero-baseline fallback | solar.ts:127-135,188,199 | working (wasted per-panel loop on every report) | NONE |
| CODE.step7-proposal.60 | **Degradation is a hardcoded `0.0075` (0.75 %/yr)** inside `computeEnergyReport` — not read from the panel's datasheet, not from the rule config, identical for every module in the catalog, and reported to the user as `degradationPctPerYear: 0.75` | solar.ts:218,241 | POC-DEFECT(a per-product warranty parameter modelled as a global constant; the panel catalog's own degradation figures, if any, are never consulted) | NONE |
| CODE.step7-proposal.61 | **25-year projection**: `lifetimeMwh25 = Σ_{y=0..24} annual·(1−0.0075)^y` = **22.8742 × annual** (probed) and `year25Mwh = annual·(1−0.0075)^24` = **0.83470 × annual**; both rounded to 0.1 MWh. The horizon `25` is a literal in three separate loops (`solar.ts` twice, `finance.ts` twice) | solar.ts:217-223,239-240; finance.ts:48-66 | working (horizon hardcoded in four places) | NONE |
| CODE.step7-proposal.62 | `specificYield = round(annualKwh / capacityKwp)` kWh/kWp from the **unrounded** annual; `annualMwh = round(annualKwh/100)/10` (0.1 MWh = 100 kWh granularity); `annualKwh` is also returned unrounded expressly so per-panel shares and the report cannot drift by rounding | solar.ts:229-231; types.ts:632-636 | working | NONE |
| CODE.step7-proposal.63 | **Provenance flag `irradianceSource: 'PVGIS' \| 'estimate'` is decided by the actual computation path**, not by the persisted `location.dataSource` string — a rehydrated project whose weather was rejected or whose pin moved relabels itself honestly | solar.ts:152,189,204,242; solar.test.ts:76 "falls back (and relabels) when weather is for a different pin"; EnergyReportSheet.tsx:87-96 (comment states exactly this) | working | NONE (F8-08/F8-09 binding) |
| CODE.step7-proposal.64 | **Stale-pin guard**: `activeWeather(location)` returns the stored `SiteWeather` only when `latLngNear(w.forLatLng, location.latLng, 1e-4)` — ~11 m on both axes. Beyond that the measured weather is dropped and the estimate is used, rather than quoting old-pin numbers | solar.ts:66-78; solar.test.ts:45-55 | working | NONE |
| CODE.step7-proposal.65 | **The energy chain has no inverter clipping model and no AC limit** — `computeEnergyReport` never reads `inverter.acKw` or `inverterCount`; only the datasheet *efficiency* enters, as one loss line. A 2.0 DC/AC design reports the same kWh/kWp as a 1.0 one. `comparison.ts` compensates by refusing to *recommend* rows above 1.35 DC/AC, and the `dc-ac-ratio` analyzer warns, but the number itself is never derated | solar.ts:115-244 (absence of `acKw`); comparison.ts:176-180,323-334; analyzers.ts:41-69 | POC-DEFECT(no clipping term — an oversized array's annual generation is overstated wherever the guardrails do not apply, e.g. the Energy Report and the proposal) | NONE |
| CODE.step7-proposal.66 | The loss stack has **no site or climate dependence**: temperature loss is 8.1 % in Leh and in Chennai; soiling is 3.0 % on a dusty industrial roof and a coastal villa; there is no albedo, bifacial-gain, snow, availability or LID/LeTID term at all — even when the full PVGIS climatology (12 monthly GHI + 12 monthly Kd) is available to derive some of them | solar.ts:42-54 vs 163-189 | POC-DEFECT(measured climate data is used for irradiance only; every other physical term stays a constant) | NONE |
| CODE.step7-proposal.67 | `panelEnergyShares(project)` is a **split of the one report number**, not a second model: weight per enabled panel `= kd + (1−kd)·poaBeamRatio(tilt,az)·solarAccess` (with `kd` = the mean measured monthly diffuse fraction, or 0.35), and each share = `annualKwh × wᵢ / Σw`. By construction `Σ shares === report.annualKwh`, so the on-panel inspector can never quote a figure the report disagrees with | solar.ts:80-113 | working | NONE (Step-6 surface; owned here as the model) |
| CODE.step7-proposal.68 | `estimateSolarAccess(_panel)` **returns the constant `1`** — an exported "cheap analytic version used before 3D raycast runs" that computes nothing | solar.ts:257-260 | POC-DEFECT(dead exported stub that would report every panel as unshaded if any caller trusted it) | NONE |
| CODE.step7-proposal.69 | `suggestKwpFromBill(bill, tariff, psh) = round( (bill / max(1,tariff)) / (psh · 30 · 0.8) · 10 )/10` — a hardcoded 0.8 performance ratio (vs the model's own 0.83 PR) and a 30-day month. Probe: ₹3,000 @ ₹8/kWh, 5 PSH ⇒ **3.1 kWp**. Sole consumer is Step 4's bill banner | solar.ts:246-255; Step4Components.tsx:99-106 (one hop) | working (its 0.8 PR is a fourth, independent performance assumption in the codebase) | NONE (SC.10-5.15, s4) |
| CODE.step7-proposal.70 | `mockIrradiance(lat) = clamp(5.6 − \|lat−24\|·0.045, 4.6, 5.9)` kWh/m²/day — the "built-in irradiance model (latitude fit, ±10 %)" the report's fallback provenance line names. Probed: 8°N→4.88 · 18.52°N→5.35 · 24°N→5.60 · 28.6°N→5.39 · 34°N→5.15. It is a **peak centred on Rajasthan/Gujarat with a linear falloff**, valid only for India, and it is consumed by Step 1, never by `computeEnergyReport` (which reads the resulting `peakSunHours`) | solar.ts:19-24; Step1Setup.tsx:396 (one hop) | working (see row 91) | NONE |

## §G — Plane-of-array transposition (`lib/poa.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.71 | `poaBeamRatio(lat,lng,tilt,az)` = annual **beam-only** transposition ratio, computed by numeric integration over the same sun engine the shadows use: **5 sample months** (Jan, Mar, Jun, Sep, Dec, always the 21st) × **10 sample hours** (08:00–17:00 **local mean-solar** time via `solarHourDate(…,lng)`, so the result is timezone-independent), skipping samples with altitude ≤ 0.03 rad. Ratio = Σ max(0, n·s) / Σ max(0, sin alt), rounded to 3 dp | poa.ts:10-12,27-65; poa.test.ts:27 "orientation genuinely moves the factor (guards solar-time sampling)" | working | NONE |
| CODE.step7-proposal.72 | Azimuth convention is **project-wide 0 = North, 90 = East, 180 = South**; panel normal built as `(sin t·sin a, cos t, −sin t·cos a)` in the scene frame (x = east, y = up, −z = north) | poa.ts:26,37-42 | working | NONE |
| CODE.step7-proposal.73 | `poaFactor = DIFFUSE_SHARE + (1−DIFFUSE_SHARE)·poaBeamRatio` with **`DIFFUSE_SHARE = 0.35` fixed** — the fallback used when no measured Kd exists. Probed at Pune (18.52 N, 73.86 E): flat **1.000** · 10° S **1.040** (beam 1.061) · 20° S **1.059** (beam 1.090, the peak) · 30° S 1.056 · 35° S 1.047 · 10° E 0.977 · 10° W 1.005 · **20° N 0.864** (beam 0.791) | poa.ts:14,73-86; probe; poa.test.ts:9,13,21,38 | working | NONE |
| CODE.step7-proposal.74 | Model boundaries, stated in the file header and **not** re-stated in any UI: diffuse is treated as fully orientation-neutral (no sky-diffuse transposition, no Perez/Hay model), there is **no air-mass or clear-sky weighting** of the samples (an 08:00 sample counts as much as noon), **no ground-reflected/albedo term**, no bifacial rear gain, and no horizon profile | poa.ts:1-7,44-60 (absence) | working (honest in the code comment; the only user-facing label is EnergyReportSheet's "POA = beam-weighted plane-of-array estimate") | NONE |
| CODE.step7-proposal.75 | Two **module-level `Map` caches** (`beamCache`, `factorCache`) keyed on `lat(2dp),lng(2dp),round(tilt),round(az/5)*5` — ~1.1 km / 1° tilt / 5° azimuth resolution. Neither is bounded or evicted, and neither is keyed on the sample year (`new Date().getFullYear()` is read inside the loop), so entries persist for the life of the tab | poa.ts:16-17,33,46,62-63,79-85 | working (unbounded cache; negligible today, recorded) | NONE |

## §H — PVGIS ingestion, the fallback ladder and provenance

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.76 | **Server proxy** `GET /api/pvgis?lat&lng` (PVGIS sends no CORS headers). `runtime = 'nodejs'`, `dynamic = 'force-dynamic'`, upstream `cache: 'no-store'`, so a per-coordinate answer is never statically cached or served for the wrong pin. It **always replies HTTP 200** with a `{status: 'ok' \| 'unavailable' \| 'error', weather?, message?}` envelope — one simple contract for the client | app/api/pvgis/route.ts:14-15,22-82 | working | NONE |
| CODE.step7-proposal.77 | Upstream is **PVGIS v5.3 `MRcalc`** with `horirrad=1&d2g=1&outputformat=json` and an **explicit radiation-database ladder** `PVGIS-SARAH3 → PVGIS-ERA5` (never a silent server-side default). A 400 on a rung means "outside that grid" and falls to the next; a 400 on the last rung ⇒ `unavailable`. India is outside SARAH3, so the primary market lands deterministically on ERA5 — the ladder buys determinism and honest provenance, not different data | route.ts:17-19,30-54; pvgis.ts:17-25; pvgis.test.ts:105 "the ladder prefers satellite SARAH3 and falls back to global ERA5" | working | NONE |
| CODE.step7-proposal.78 | Server timeout **8 s** per rung (`AbortController`); an abort answers `{status:'error', message:'PVGIS request timed out'}`, any other throw `'PVGIS request failed'`, a non-400 non-ok `'PVGIS HTTP {code}'`. Lat/lng are validated (`finite`, \|lat\|≤90, \|lng\|≤180) before any fetch. Success carries `Cache-Control: public, max-age=86400` | route.ts:20,26-28,40-47,55-60,69-72,76-82 | working | NONE |
| CODE.step7-proposal.79 | **Client** `fetchWeather` has a **9 s** timeout (deliberately > the server's 8 s so the server's own timeout surfaces first), retries **exactly once** on `null`/`error`, and **never retries on `unavailable`** (deterministic no-coverage). It never throws — every failure path returns `null` so the built-in estimate stays the floor. On success it re-stamps `forLatLng` with the requested pin and `fetchedAt = Date.now()` | weatherApi.ts:8,16-40; weatherApi.test.ts:28 "returns null on unavailable WITHOUT retrying", :35 "retries once on a transient error then gives up" | working | NONE |
| CODE.step7-proposal.80 | **Mapper `pvgisToWeather` is all-or-nothing.** MRcalc returns one row per (year, month) across the record; each calendar month is averaged over all years to a climatological normal, then GHI is expressed as kWh/m²/day by dividing by the FIXED `DAYS_IN_MONTH` (the ÷days here and ×days in the energy model cancel exactly, so leap years never drift). `annualGhi = Σ monthlySums / 365`. Any of: fewer than 12 rows · a month with no data · a non-finite/negative `H(h)_m` · a non-finite `Kd` · a monthly mean `Kd` outside **0.10–0.90** ⇒ `return null` for the whole payload — never a partial repair | pvgis.ts:14-15,68-124; pvgis.test.ts:23,34,48-73 | working | NONE |
| CODE.step7-proposal.81 | **Provenance captured with the data**: `raddatabase` (from `inputs.meteo_data.radiation_db`) and `yearsOfRecord` (count of distinct `year` values — 16 in the bundled Pune fixture) ride on `SiteWeather` and are printed verbatim in the Energy Report's provenance line. `fetchedAt` is stamped by the caller, not the mapper (keeping the mapper pure) | pvgis.ts:110-123; pvgis.test.ts:109 "mapper surfaces the record length (fixture: 16 years, 2005–2020)"; EnergyReportSheet.tsx:90-96 | working | NONE (F8-08/F8-09) |
| CODE.step7-proposal.82 | `isValidSiteWeather` is the **one shape guard** shared by the fetch client and the persistence validator: 12-length arrays, GHI ≥ 0 and finite, diffuse fraction in **0.05–0.95**, `annualGhi > 0`, numeric `forLatLng`, `source === 'pvgis'`. Corrupt persisted weather therefore degrades to the estimate rather than driving energy | pvgis.ts:42-61; pvgis.test.ts:89 "REJECTS a negative monthly GHI (corrupt persisted data → estimate)" | working | NONE |
| CODE.step7-proposal.83 | Note the **two different Kd windows**: the mapper rejects a monthly mean outside 0.10–0.90, the shape guard accepts 0.05–0.95. A hand-edited or future payload can therefore be valid to one and not the other | pvgis.ts:56 vs 100 | working (recorded inconsistency; both are conservative) | NONE |
| CODE.step7-proposal.84 | **Nothing in Step 7 fetches or refreshes weather.** `fetchWeather` is called from Step 1 only; Step 7 (and the Energy Report opened from it) consume whatever is on the project, silently falling back to the estimate if the pin has since moved >11 m. There is no "refresh climate data" affordance anywhere in the proposal step | weatherApi.ts:32 callers (Step1Setup only); Step7Proposal.tsx (absence) | working (recorded — the provenance can change under the user with no way to act on it) | NONE |

## §I — FINANCE (`lib/finance.ts` · product level)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.85 | **One money path.** `computeFinancials(project, report).systemCostInr` is `bomTotal(mergedBom(project), project)` — literally the Step-9 quote total off the same merged lines (auto + user overrides + custom), so the proposal's headline cost cannot disagree with the BOM table. Gate: `finance.test.ts:32` asserts `fin.systemCostInr === bomTotal(...) === bomMoney(...).total`, and that overrides, custom lines and a margin change all flow through | finance.ts:33-34; finance.test.ts:28-104 | working | NONE (M05-61 hand-off honesty) |
| CODE.step7-proposal.86 | **PM Surya Ghar subsidy** `subsidyInr(kwp, residential, dcrEligible)`: zero unless `siteType === 'residential'` **and** the selected panel's `dcr === true` **and** kWp > 0; otherwise `kw = min(kwp, capKw=3)`, `first = min(kw,2)·₹30,000 + max(0,kw−2)·₹18,000`, result `min(₹78,000, round(...))`. Probed: 0.5→₹15,000 · 1→₹30,000 · 2→₹60,000 · 2.5→₹69,000 · 3→₹78,000 · **4/10/100 kWp → ₹78,000** (the cap applies to ALL residential systems ≥3 kW; it does not zero out for large ones). Non-residential or non-DCR ⇒ ₹0 | finance.ts:6-25; india.ts:431-437; finance.test.ts:9-25 | working | NONE (M05-61; F1 pack law — see row 91) |
| CODE.step7-proposal.87 | **Annual savings** `= round(annualKwh × tariffInrPerKwh)` where the tariff is `project.info.tariffInrPerKwh` (resolved per state/DISCOM/category at Step 1). The model assumes **100 % of generation offsets grid consumption at the retail tariff** — no self-consumption ratio, no net-metering/net-billing/gross-metering distinction, no export rate, no time-of-day tariff, no demand-charge component | finance.ts:41-43; tariff.test.ts (DISCOM resolution, one hop, sitting 13) | POC-DEFECT(a C&I design with low daytime load, or any state on net *billing*, is over-credited; the studio is explicitly a C&I product too) | NONE |
| CODE.step7-proposal.88 | **`computeFinancials` reads the ROUNDED annual energy**: `const annualKwh = report.annualMwh * 1000` — i.e. the 0.1 MWh-rounded figure — even though `EnergyReport.annualKwh` exists unrounded expressly so consumers "can't drift by rounding". Every finance number (savings, payback, 25-yr savings, and therefore every comparison row and the narrative) inherits up to ±50 kWh of quantisation — ~₹400/yr at ₹8/kWh, ~₹15k over 25 years | finance.ts:42 vs solar.ts:229-230 and types.ts:632-636 (the field's own doc comment) | POC-DEFECT(the unrounded field exists for exactly this reason and is not used) | NONE |
| CODE.step7-proposal.89 | **Payback with escalating tariff and degrading output.** Year 1 saving = `annualSavings`; thereafter `gen *= (1 − degradationPct/100)` and `yearly = round(gen × tariff × 1.03^y)`. Payback = the first year where cumulative ≥ net cost, **interpolated within that year** (`y−1 + (netCost − prevCum)/yearly`), rounded to 0.1 yr. `netCost ≤ 0 ⇒ payback 0`. Probed: ₹3.5 L net, 7,500 kWh @ ₹8, 0.75 %/yr degradation ⇒ **5.5 yr** (5.9 yr with escalation switched off) | finance.ts:44-59,77; probe | working | NONE |
| CODE.step7-proposal.90 | **A system that never pays back reports "25 years", not "never".** `payback` is initialised to `25` and the loop simply ends; probed ₹50 L net on a 7,500 kWh system ⇒ `paybackYears: 25`. `EnergyReportSheet` prints "25.0 years", `comparison.ts` ranks by this value (so a never-paying row sorts alongside a genuine 25-yr one), and `financing.ts`'s cash note reads *"payback in about 25 years, then free power."* Only `proposal-narrative.ts` dodges it, by gating its sentence on `paybackYears < 25` | finance.ts:48,53-55; probe; EnergyReportSheet.tsx:194; financing.ts:68; comparison.ts:330-334; proposal-narrative.ts:84 | POC-DEFECT(a sentinel value is rendered as a measurement — the one number a customer reads first) | NONE |
| CODE.step7-proposal.91 | **Tariff escalation is a hardcoded `const escalation = 3` (%/yr) inside `computeFinancials`** — unlike the subsidy slab and the financing terms, it is NOT in `data/rules/india.ts`. It is returned as `tariffEscalationPct` and nothing in the UI renders it. Same class: the 25-yr horizon (4 literals), the 0.75 %/yr degradation, `DIFFUSE_SHARE 0.35`, `MONTH_FACTORS`, `psh 5.3`, the lat 20/lng 77 fallback and `mockIrradiance`'s India latitude fit are all market/finance constants living outside the pack | finance.ts:44,80; india.ts:431-437,491-499 (what IS packed) | POC-DEFECT(F1 pack law: India-specific commercial and climate assumptions are code, not pack DATA — only the subsidy slab, financing terms and O&M thresholds were extracted) | NONE |
| CODE.step7-proposal.92 | **25-year savings** `= Σ_{y=0..24} gen_y × tariff × 1.03^y`, with `gen` degrading 0.75 %/yr — **gross lifetime bill offset, not net benefit**: it never subtracts the system cost, and there is no O&M/insurance/inverter-replacement cost anywhere in the model. Probed: 7,500 kWh @ ₹8 ⇒ **₹19,78,658** over 25 years (₹13,72,452 with no escalation). The narrative sells it as *"an estimated ₹X over 25 years"* | finance.ts:60-66,78; probe; proposal-narrative.ts:86 | POC-DEFECT(no lifecycle cost at all — inverter replacement around year 10-12 is a certainty in this market and is nowhere in the 25-year number) | NONE |
| CODE.step7-proposal.93 | `FinancialSummary.emiPerMonthInr` is a bare **5-yr / 9.5 % EMI on the FULL net cost** (`r=0.095/12, n=60`) — probed ₹8,863 on ₹4.22 L, versus the financing model's ₹7,090 loan EMI after its 20 % down payment. The field is marked `@deprecated` in `types.ts` and **nothing renders it**; it is still computed on every call and still persisted in the summary | finance.ts:67-70,79; types.ts:657-663; probe | working (dead but live-computed; the two-EMI hazard is documented and defused) | NONE |
| CODE.step7-proposal.94 | Guards: `netCost = max(0, systemCost − subsidy)`; the payback loop is skipped entirely when `netCost <= 0`; `yearly > 0` is checked before dividing (a 0-panel candidate would otherwise produce `NaN`); `comparison.test.ts:214` asserts a sub-panel target still yields a **finite** payback | finance.ts:40,48,51-55; comparison.test.ts:214 | working | NONE |

## §J — Financing options (`lib/financing.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.95 | Four modes, always in the order **cash · loan · lease · ppa**, every one derived from the SAME `fin.systemCostInr`/`netCostInr` and the same annual grid saving, so no financing view can disagree with the BOM. Each option carries `upfrontInr`, `monthlyInr`, `firstYearNetInr` (annual saving − year-1 financing outflow), `lifetimeCostInr`, a `headline` and a plain-language `note` | financing.ts:11-33,50-122; financing.test.ts:34,67 | working | NONE |
| CODE.step7-proposal.96 | `emiInr(principal, annualRatePct, months)` = standard level-payment amortisation `P·r·(1+r)^n / ((1+r)^n − 1)` with `r = APR/1200`, `0` for non-positive principal or months, and a straight `P/n` at 0 %. Gate: ₹1 L @ 12 % over 12 mo ⇒ ₹8,800–8,900 | financing.ts:37-43; financing.test.ts:22 | working | NONE |
| CODE.step7-proposal.97 | Terms come from `resolveRules().financing` — **loan 9.5 % APR / 5 yr / 20 % down · lease 11 % / 10 yr · PPA 20 % below grid / 15 yr** — all labelled ASSUMED/representative in the pack. Probed on ₹5 L system, ₹78 k subsidy, ₹4.22 L net, ₹60 k saving, 10,000 kWh @ ₹6: **cash** ₹4,22,000 up / lifetime ₹4,22,000 · **loan** ₹84,400 down + ₹7,090/mo, lifetime ₹5,09,800, yr-1 net **−₹25,080** · **lease** ₹0 down + ₹6,888/mo, lifetime ₹8,26,560, yr-1 net −₹22,656 · **PPA** ₹0 down at ₹4.80/kWh, ₹4,000/mo, lifetime ₹7,20,000, yr-1 net **+₹12,000** | financing.ts:55,72-114; india.ts:491-499; probe | working | NONE |
| CODE.step7-proposal.98 | **The lease is amortised on `systemCostInr`, the GROSS cost — the subsidy is not applied**, while cash/loan/PPA all work off `netCostInr`. Deliberate in that a developer-owned system cannot claim a residential subsidy, but it is nowhere stated: the sheet's footnote says *"every option finances the same ₹{systemCost/1000}k system"* while three of the four cards actually quote the net figure | financing.ts:73,90; EnergyReportSheet.tsx:226-229 | POC-DEFECT(the four cards are not on a common basis and the footnote asserts that they are) | NONE |
| CODE.step7-proposal.99 | **PPA reconciliation contract**: `computeFinancing(fin, annualKwh, gridTariff)` requires `annualSavings === annualKwh × gridTariff` from the caller, so PPA savings reconcile exactly. `EnergyReportSheet` honours it by passing `r.annualMwh * 1000` and `project.info.tariffInrPerKwh` — the same rounded pair `computeFinancials` used, so the two agree (both inheriting row 88's quantisation) | financing.ts:45-49,101-114; EnergyReportSheet.tsx:201 | working | NONE |
| CODE.step7-proposal.100 | Escalation is applied to **neither** side of the financing comparison: `firstYearNetInr` and `lifetimeCostInr` use flat year-1 savings and flat payments, while `computeFinancials` escalates the tariff 3 %/yr. A 15-yr PPA lifetime cost and a 10-yr lease are therefore nominal, un-escalated, and un-discounted (no NPV/IRR anywhere in the product) | financing.ts:82,96,110-111 vs finance.ts:44,58,64 | working (recorded — the two finance surfaces use different time-value assumptions) | NONE |
| CODE.step7-proposal.101 | `firstYearNetInr` can be (and usually is) **negative for loan and lease** — the model does not hide it; the sheet colours it `--warn` with a `−₹Xk` prefix | financing.ts:82,96; EnergyReportSheet.tsx:213-221 | working | NONE |
| CODE.step7-proposal.102 | Financing has **no eligibility, no credit logic, no tenure/rate choice and no lender identity** — one representative offer per mode, no UI to change any term. The honesty note ("Representative terms … confirm actual lender / PPA offers") carries the whole load | financing.ts:1-7; EnergyReportSheet.tsx:226-229 | working (POC boundary, honestly labelled) | NONE |

## §K — The insight substrate & registry (`lib/insights/{types,registry}.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.103 | **One substrate, many surfaces.** An `InsightAnalyzer` is `{id, title, analyze(project): Insight[]}`, required to be pure and non-mutating. The Copilot tray (Step 6), the Design Health score and Step 7's readiness card all consume the SAME registry, so a score computed against a different analyzer set than the tray shows is structurally impossible | insights/types.ts:1-51; registry.ts:6-20; analyzers.ts:128-137 | working | SC.10-8.14 |
| CODE.step7-proposal.104 | An `Insight` carries: a **stable dedupe/persistence key** `<analyzerId>:<subject>` (the same finding must produce the same key across recomputes or accept/ignore state would not stick), `category` (one of 8), `severity` (info/suggestion/warning/critical), **`impact` 0..1** (how much acting changes the outcome), **`confidence` 0..1** (input quality: measured = 1 … assumed ≈ 0.3), `title`, a `detail` that must cite the datum that triggered it, an `evidence: string[]`, optional `focusIds` (click-to-locate) and an optional `action` label | types.ts:21-44 | working | NONE |
| CODE.step7-proposal.105 | `computeInsights`: runs every registered analyzer with **per-analyzer isolation** (a throwing analyzer is `console.warn`ed and skipped, it cannot take the tray down), **dedupes by key keeping the higher severity**, filters out keys the user marked `ignored` (unless `includeIgnored`), and sorts by `severity rank` (critical 3 / warning 2 / suggestion 1 / info 0) → `impact × confidence` → `key` alphabetical | registry.ts:22-65; insights.test.ts:39-95 (dup ids rejected, dedupe, ordering, ignore filter, throwing-analyzer isolation) | working | NONE |
| CODE.step7-proposal.106 | `registerAnalyzer` **throws** on a duplicate id, so the two pack registrars guard themselves by probing the registry (`listAnalyzers().some(a => a.id === 'roof-utilization'/'cleaning-access')`) rather than by a module-level boolean — which is what lets tests `clearAnalyzers()` and re-register | registry.ts:8-11,17-20; analyzers.ts:141-147; analyzers-access.ts:243-249; insights-analyzers.test.ts:41, analyzers-access.test.ts:181 | working | NONE |
| CODE.step7-proposal.107 | `memoizedInsights(project)` is a **last-value memo** keyed on `designFp(project) + '§' + JSON.stringify(project.insightState) + '§' + [...analyzers.keys()].join(',')` — registry composition is part of the key, so a late-registering pack invalidates. Value-equal projects return the *identical array instance*; screens may call it every render | registry.ts:67-95; insights.test.ts:97 "returns the identical array for unchanged design state" | working | NONE |
| CODE.step7-proposal.108 | **The memo key does not include the shading fingerprint.** Safe today only because no registered analyzer reads `panel.solarAccess`; the moment one does (the obvious next analyzer — "these modules are heavily shaded"), the tray and the readiness card will go stale after a raycast without any key change | registry.ts:79-83; analyzers.ts + analyzers-access.ts (no `solarAccess` read) | uncertain (correct today by coincidence of the current pack, not by construction) | NONE |
| CODE.step7-proposal.109 | **Only 5 of the 8 declared `InsightCategory` values are ever emitted** — `energy`, `electrical`, `utilization`, `maintenance`, `constructability`. Nothing in the registry ever emits **`commercial`**, **`structure`** or **`data-quality`**: there is no analyzer anywhere that comments on price, margin, payback or subsidy eligibility, and none that flags "these numbers rest on an estimate rather than measured irradiance". The readiness card's "Design review" line is therefore silent on exactly the two things Step 7 exists to certify | types.ts:11-19 vs the 8 registered analyzers (analyzers.ts:16,41,71,98; analyzers-access.ts:51,91,152,198) | POC-DEFECT(the substrate declares commercial and data-quality review; no analyzer implements either, so the pre-proposal check cannot see a money or provenance problem) | SC.10-8.14 |
| CODE.step7-proposal.110 | `Insight.action` is a bare `{label: string}` "one-click remedy descriptor — wired by the tray in later phases". Exactly one analyzer sets it (`cleaning-access` → *"Add a walkway in Step 6"*) and no consumer renders a control for it | types.ts:42-43; analyzers-access.ts:81 | working (declared-not-wired; honest as text, inert as an action) | NONE |

## §L — Analyzer pack v1 — what the studio actually recommends (`lib/insights/analyzers.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.111 | **`roof-utilization`** (`utilization`, **suggestion**, confidence 0.9): fires when `placedKwp / estimateMaxCapacityKwp(project, panel).kwp < 0.6`, i.e. under 60 % of the valid positions used. Title *"Roofs can hold {max} kWp — you placed {placed}"*; detail suggests documenting the headroom for a future expansion; impact `1 − ratio`; evidence cites both kWp figures and the max panel count. Silent with no panel, no roof or no placed panels | analyzers.ts:16-39; insights-analyzers.test.ts:53,64 | working | SC.10-8.14 |
| CODE.step7-proposal.112 | **`dc-ac-ratio`** (`electrical`, **warning**, confidence 1.0): fires when `placedKwp / (inverter.acKw × inverterCount)` falls outside **0.90–1.35**. High ⇒ *"will clip on clear days. Consider one more inverter or a larger model."*; low ⇒ *"oversized … money spent on unused AC headroom."* Impact `min(1, \|ratio−1.15\|/1.15)`; evidence names the band verbatim. **The same 0.90–1.35 / 1.15 band is used by `comparison.ts`'s inverter recommender** — the two agree exactly | analyzers.ts:41-69; comparison.ts:141-162; insights-analyzers.test.ts:75-95 | working | SC.10-8.14 |
| CODE.step7-proposal.113 | **`orientation`** (`energy`, **suggestion**, confidence 0.8): **northern hemisphere only** (`location.latLng.lat > 0` or it returns nothing at all); flags enabled panels with `tilt > 3°` whose azimuth is more than **60° from south**. Impact = flagged/total; `focusIds` carries the panel ids for click-to-locate; evidence cites the count and the latitude | analyzers.ts:71-96; insights-analyzers.test.ts:97,106 (asserts silence in the southern hemisphere) | working (a hemisphere-gated rule that silently emits nothing rather than saying it does not apply) | SC.10-8.14 |
| CODE.step7-proposal.114 | **`row-spacing`** (`energy`, **suggestion**, confidence 0.85): per segment, skips `flush` racking, non-positive pitch and single-row tables; compares `racking.rowPitchM` against `shadowFreePitchM(lat, lng, tilt, moduleLength, azimuth)` with a **5 cm tolerance**. Detail is unusually honest — *"winter row-shading is being priced as an energy loss in the report. Widen the pitch (or accept the trade-off consciously)"* — which is true precisely because of row 49 (row shading is measured, not modelled twice) | analyzers.ts:98-126; spacing.ts (one hop); insights-analyzers.test.ts:149,161 | working | SC.10-8.14 |

## §M — Maintenance & constructability pack (`lib/insights/analyzers-access.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.115 | **Every threshold in this pack is an ASSUMED O&M convention from the rule pack, never a code minimum** — `cleaningReachM 6`, `ladderEdgeM 2.5`, `inverterClearanceM 0.9` — and every insight's `confidence` is hard-capped at **0.6** (`CONVENTION_CONFIDENCE`). The evidence strings say so in the user's own words ("ASSUMED O&M convention", "ASSUMED site convention", "ASSUMED — not a code minimum") | analyzers-access.ts:11-21,77,188,231; india.ts:524-526; analyzers-access.test.ts:78 "cites the threshold as an assumed convention, and never claims certainty" | working (the strongest honest-labelling in the area) | SC.10-8.14 |
| CODE.step7-proposal.116 | **`cleaning-access`** (`maintenance`, **warning**): per roof, flags enabled modules whose centre is more than 6 m from **both** the nearest roof-polygon edge and the nearest walkway centreline. Detail ties it back to the energy model — *"The energy report already assumes the array gets cleaned — add a walkway, or the soiling loss will be worse than modelled"* — i.e. it audits the 3.0 % soiling constant of row 47. `focusIds` = the unreachable modules; `action` label "Add a walkway in Step 6" | analyzers-access.ts:48-86; analyzers-access.test.ts:53,62,72 | working | SC.10-8.14 |
| CODE.step7-proposal.117 | **`module-replacement`** (`maintenance`, **suggestion**, deliberately not a warning — "dense arrays are normal, not defects"): only on roofs with ≥ 5 enabled modules; a module is "boxed in" when neighbours exist in all four in-plane directions within `1.35 × max(module width, height)` measured from its own projected footprint (orientation/foreshortening safe) | analyzers-access.ts:88-147; analyzers-access.test.ts:88,104,111 | working | SC.10-8.14 |
| CODE.step7-proposal.118 | **`ladder-access`** (`constructability`, **warning**, fixed impact 0.5): samples each roof edge at ~1 m intervals and asks whether ANY point on ANY edge has no module centre within 2.5 m; edges shorter than 2.5 m do not count. Fires only when no edge qualifies. Silent on an empty roof | analyzers-access.ts:149-195; analyzers-access.test.ts:128,140,146 | working | SC.10-8.14 |
| CODE.step7-proposal.119 | **`inverter-access`** (`maintenance`, **warning**, fixed impact 0.4): for each placed inverter, interpolates its position along its roof edge and flags obstructions within `0.9 m + obstruction setback` or enabled modules within 0.9 m. Evidence names each blocking obstruction by label; `focusIds` covers both blockers and crowding modules | analyzers-access.ts:197-240; analyzers-access.test.ts:153,165 | working | SC.10-8.14 |
| CODE.step7-proposal.120 | **Nothing in this pack blocks anything** — by design, per the §8.4 risk note that over-strict defaults just annoy installers. Gate: `analyzers-access.test.ts:185` asserts every access insight is at most a warning; and the readiness card maps warnings to *attention*, never *blocked* (only `electricalGate` can block). The pack also says nothing about an empty project — no noise before there is a design | analyzers-access.ts:12-13; analyzers-access.test.ts:177,185; review.ts:79-96 (one hop) | working | SC.10-8.13, SC.10-8.14 |

## §N — The Energy Report sheet (`components/EnergyReportSheet.tsx`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.121 | The sheet is `f(project)` only — it recomputes `computeEnergyReport`, `computeFinancials` and `computeFinancing` on every open, holds no state, and takes a `readOnly` flag. Its **single mount point is the 3D scene's toolbar** (`<BarChart3/>`, `aria-label="Energy report"`), which is present whenever the heatmap is off — including inside **Step 7's capture studio** | EnergyReportSheet.tsx:13-28; Scene3D.tsx:238,517,795-796 (one hop) | working | NONE (§10.7 surface, owned here) |
| CODE.step7-proposal.122 | **Freshness banner**: when `!isShadingFresh(project)` an amber `role="status"` strip with a spinning `RefreshCw` heads the sheet — *"Recalculating shading for the latest edits — energy and solar-access numbers below are provisional and will refresh in a moment."* | EnergyReportSheet.tsx:25,32-52; fingerprints.ts:301-304 (one hop) | working | NONE ("money never renders while stale") |
| CODE.step7-proposal.123 | Section order is exactly the §10.7 list: SYSTEM SUMMARY (Capacity kWp · Panels `{count}` @ `{watt ?? 0}W` · Roof Area, **unit-aware** via `M2_TO_FT2`/`areaUnit`) → ANNUAL GENERATION → MONTHLY GENERATION → LOSSES BREAKDOWN → SOLAR ACCESS → 25-YEAR PROJECTION → FINANCIALS → FINANCING OPTIONS → actions | EnergyReportSheet.tsx:53-246 vs docs/phase-10-prompts.md:1044-1067 | working | NONE |
| CODE.step7-proposal.124 | **Annual-generation hero**: `{annualMwh} MWh` at 30 px/800 with "per year (Year 1)", then three inline stats — **Specific Yield** `{specificYield} kWh/kWp`, **Performance Ratio** `{performanceRatio}%`, **Tilt/Azimuth (POA)** `×{poaFactor}` | EnergyReportSheet.tsx:60-85 | working (the POA stat is the contaminated one — row 58) | NONE |
| CODE.step7-proposal.125 | **Provenance line** under the hero, driven by `r.irradianceSource` (never the persisted `dataSource`): PVGIS ⇒ *"Real irradiance — PVGIS {raddatabase \|\| (measured)} ({N}-yr record)"*; else *"Built-in irradiance model (latitude fit, ±10%)"*; then *"· POA = beam-weighted plane-of-array estimate · shading auto-updates on edits"*. Rendered at **10 px, opacity 0.7** on a blue gradient | EnergyReportSheet.tsx:86-98 | working (content exactly satisfies F8-08/F8-09; legibility of the label is the weakest part of the honesty chain) | NONE |
| CODE.step7-proposal.126 | **Monthly bars**: 12 columns, height `(kwh / max(...monthlyKwh, 1)) × 90px`, amber `#f59e0b` for `monsoonMonths`, blue `#2563eb` otherwise, single-letter month labels, plus a Regular/Monsoon legend | EnergyReportSheet.tsx:11,101-125 | working | NONE |
| CODE.step7-proposal.127 | **A month's exact kWh is carried ONLY in a native `title` attribute** on the bar — a desktop hover tooltip. On touch, the figure §10.7 requires ("each month's exact kWh available") is unreachable; there is no value label, no tap-to-reveal, no table view | EnergyReportSheet.tsx:106 | POC-DEFECT(touch-inaccessible data; the studio's mobile contract is 375 px) | NONE |
| CODE.step7-proposal.128 | **Losses breakdown**: one row per loss line — label, a track, and the % right-aligned. Bar width is `min(100, (pct/12)·100)`, i.e. the scale **saturates at 12 %**: temperature (8.1 %) fills 67 %, and a 40 % shading loss and an 80 % one are drawn identically full. Colours cycle a hardcoded 6-hex palette by index | EnergyReportSheet.tsx:127-146 | POC-DEFECT(the one chart the customer reads as "where the energy went" is clipped at 12 %, so the dominant loss is visually indistinguishable) | NONE |
| CODE.step7-proposal.129 | Charts use **raw hex** (`#1d4ed8`, `#2563eb`, `#f59e0b`, `#84cc16`, `#3b82f6`, `#ec4899`, `#a855f7`, `#ef4444`) rather than the design system's DATA tokens that §10.7 requires ("Charts use the design system's DATA colours, never the brass accent") | EnergyReportSheet.tsx:63,111,120,123,138 vs docs/phase-10-prompts.md:1068 | working (legacy screen; recorded against the doc rule) | NONE |
| CODE.step7-proposal.130 | **Total System Loss** `{totalLossPct}%` on a bordered footer row; **Solar Access** card shows `{avgSolarAccessPct}%` with the caption *"Beam access (3D raycast) + diffuse floor — same metric as the heatmap"* — the only place the 35 % floor is explained to a user | EnergyReportSheet.tsx:147-173 | working | NONE |
| CODE.step7-proposal.131 | **25-year projection**: Lifetime Generation `{lifetimeMwh25} MWh` and Year 25 Output `{year25Mwh} MWh` with `({round(year25/max(0.01,annual)·100)}% of Year 1)` — always **83 %** given the fixed 0.75 %/yr degradation | EnergyReportSheet.tsx:175-188 | working | NONE |
| CODE.step7-proposal.132 | **FINANCIALS block** is three stats: Net Cost `₹{netCost/1000}k` with sub-caption `after ₹{subsidy/1000}k subsidy` · Yearly Savings `₹{savings/1000}k` `@₹{tariff}/kWh` · Payback `{paybackYears.toFixed(1)}` years. Two problems in one line of markup: the section label literally reads **"FINANCIALS (improvement)"** — an internal build-plan word on a customer-facing sheet — and money is rendered in **thousands (`₹422k`), not the en-IN lakh/crore convention** used by every other money surface in the app (`toLocaleString('en-IN')`) | EnergyReportSheet.tsx:190-195 vs financing.ts:35 and comparison.ts:369 | POC-DEFECT(internal vocabulary leaks into the UI; money format contradicts the rest of the product) | NONE |
| CODE.step7-proposal.133 | **FINANCING OPTIONS** render only when `fin.systemCostInr > 0`: one card per mode with `label`, `headline`, `note`, and — when `monthlyInr > 0` — a coloured `yr-1 net ±₹Xk` (green when ≥ 0, amber otherwise). Footnote: *"Representative terms — every option finances the same ₹{systemCost/1000}k system; confirm actual lender / PPA offers."* | EnergyReportSheet.tsx:197-231 | working (the footnote's "same system" claim is the row-98 defect) | NONE |
| CODE.step7-proposal.134 | Actions (hidden when `readOnly`, i.e. on the customer share link): **Customize Proposal** → `/wizard/7` and **Quick Generate** → `/proposal`. Opened from Step 7's own capture studio, "Customize Proposal" navigates the user to the step they are already on; and Quick Generate reaches `/proposal` **without** setting `status: 'proposal_ready'` — the only other path to that route does | EnergyReportSheet.tsx:233-246 vs Step7Proposal.tsx:369-372 | POC-DEFECT(self-navigation from Step 7; two routes to the proposal that leave the project in different states) | SC.10-8.19 |

## §O — Proposal narrative (`lib/proposal-narrative.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.135 | Pure `f(project, fmtArea)` returning `NarrativeSection[]`, each beat = `{text, facts: (string\|number)[]}`. **Every sentence is a template with slots filled from the project — nothing is generated or embellished** — and each beat carries the raw values it was built from so traceability is testable. Rendered by `/proposal` (`ProposalView`, sitting 11), which is where Step 7's Generate button lands | proposal-narrative.ts:1-33; ProposalView.tsx:11,70 (one hop); proposal-narrative.test.ts:41 "EVERY numeric fact in a beat appears in that beat's text (no orphan claims)" | working | SC.10-8.19 (the hand-off's content) |
| CODE.step7-proposal.136 | Returns **`[]`** (no story at all) when there is no panel spec or `panelCount === 0`; sections with no real data are omitted rather than padded | proposal-narrative.ts:31-32; proposal-narrative.test.ts:36 | working | NONE |
| CODE.step7-proposal.137 | Five sections, each conditional: **Your rooftop** (address beat only when one exists; always "we modelled {area} of usable roof and placed {n} panels of {watt} W each") · **Your system** (rating + inverter make/model/count when set; solar-access sentence when > 0) · **Energy & savings** · **Why this design** (up to 3 real `project.designLog` entries with a `reason` and a `topic` — never invented reasons) · **Room to grow** (only when `estimateMaxCapacityKwp − capacityKwp ≥ 0.5 kWp`) | proposal-narrative.ts:36-126; proposal-narrative.test.ts:82 "surfaces REAL decision-log entries, not invented reasons" | working | NONE |
| CODE.step7-proposal.138 | **Provenance is a fact only when the sentence names it**: the year-one beat appends *", using measured PVGIS climate data for your location"* and adds `'PVGIS'` to `facts` **only** when `irradianceSource === 'PVGIS'`. On the estimate path the sentence says nothing about where the number came from — no "built-in estimate ±10 %" counterpart exists in the narrative at all | proposal-narrative.ts:70-78 | POC-DEFECT(asymmetric provenance: the good case is named, the weak case is silent — the F8-09 "Built-in estimate" label does not travel into the customer document's prose) | NONE |
| CODE.step7-proposal.139 | **The solar-access sentence misstates its own metric**: *"Across the year the array receives {avgSolarAccessPct}% of available sunlight"* — but that figure is the 0.35-floored comparability score of row 56, not a fraction of available sunlight. A fully beam-shaded array tells the customer it "receives 35 % of available sunlight"; an unshaded one claims 100 % | proposal-narrative.ts:61-66 vs solar.ts:185,201,238 | POC-DEFECT(a defensible internal metric is given a plain-English meaning it does not have, in the customer-facing document) | NONE |
| CODE.step7-proposal.140 | Payback beat is gated `0 < paybackYears < 25`, which quietly sidesteps the never-pays-back sentinel of row 90 — the sentence simply vanishes, with nothing said in its place. Subsidy beat only when `subsidyInr > 0` ("never a ₹0 line") | proposal-narrative.ts:84-95; proposal-narrative.test.ts:94 | working (the omission is safe; the silence is unexplained) | NONE |
| CODE.step7-proposal.141 | The 25-year money sentence reads *"then keeps saving for the rest of its life — an estimated {savings25YrInr} over 25 years"* — the **gross, un-net, lifecycle-cost-free** figure of row 92, at 3 %/yr escalation the reader is never told about | proposal-narrative.ts:86 | POC-DEFECT(a projection presented without its assumptions; cf. M06's `F8-23` requirement that projections travel with them) | NONE |
| CODE.step7-proposal.142 | The narrative recomputes `computeEnergyReport` and `computeFinancials` itself rather than accepting them, so it cannot be fed different numbers than the rest of the app — the traceability test asserts exactly that ("the headline numbers come from the SAME engines as the rest of the app") | proposal-narrative.ts:30,33; proposal-narrative.test.ts:65 | working | NONE |

## §P — The comparison engine (`lib/comparison.ts` · UI carried by Step 4)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step7-proposal.143 | **No scoring black box.** Each candidate is run through the *existing* pure pipelines — `fillRoofAsSegment` → `reindexSegment` → `autoString` → `computeEnergyReport` → `computeFinancials` — on a deterministically constructed hypothetical design. Gate: `comparison.test.ts:117` asserts matrix cells EQUAL single-run pipeline outputs on the same constructed candidate, and the CURRENT selection goes through the same constructor so the gate binds it too | comparison.ts:86-133,232-312; comparison.test.ts:117,141 | working | NONE (SC.10-5.x, s4) |
| CODE.step7-proposal.144 | **Stated honesty contract**, surfaced verbatim in the UI basis line: candidates assume **UNSHADED** panels (the raycast never ran on a hypothetical layout) — identical treatment per row keeps it fair, absolute kWh is provisional; rows are **catalog-priced** with `bomOverrides: []` (overrides key on brand+model and would apply only to the current selection) while the saved **margin is kept**; fill is budgeted by `targetKwp` in **roof array order**, deliberately not the raycast-ranked order auto-design uses | comparison.ts:8-17,98-132,338-354; comparison.test.ts:68 "strips BOM overrides for comparability but keeps the saved margin" | working | NONE |
| CODE.step7-proposal.145 | Candidate budget `= floor(targetKwp × 1000 / panel.watt)` panels (or `Infinity` when no target), spent roof by roof in array order until exhausted | comparison.ts:98-121; comparison.test.ts:56,79,86 | working | NONE |
| CODE.step7-proposal.146 | **`recommendInverterFor(dcKwp, phase, inverters, maxCount=4)`**: DC/AC in **0.90–1.35**, closest to **1.15**, ties broken by lower total price; single-phase supply restricts to 1φ units; up to 4 identical units; `null` when nothing lands in the band. The same band the `dc-ac-ratio` analyzer and Step 4's picker use | comparison.ts:135-162; comparison.test.ts:311,322,330 | working | NONE |
| CODE.step7-proposal.147 | **`nearestInverterFit` fallback** when the band is empty: two passes — first refusing any ratio > 1.35, only then allowing clipping fits — *because the energy pipeline has no clipping model* (row 65), so an oversized-DC row would show kWh the inverter cannot deliver. The row still gets a real inverter, BOM and finance instead of a misleading ₹0 line | comparison.ts:164-198; comparison.test.ts:173 "gives every candidate a real inverter even when no catalog unit fits the band" | working | NONE |
| CODE.step7-proposal.148 | **`shortlistPanels`**: ALMM-listed catalog panels ranked by **₹/W** (cheapest capacity first), capped at 6, with the current selection **always first and always included** even when non-ALMM. The rule is returned as text so the UI can state it verbatim, and the text reports what was actually built (*"…plus your current selection (not ALMM-listed)"*) | comparison.ts:200-226; comparison.test.ts:271,287,295 | working | NONE |
| CODE.step7-proposal.149 | **A warn never hides**: `warnNote` is `validateSystem`'s first warning, or — when that is silent — an out-of-band note *"DC/AC {r} is outside the recommended 0.90–1.35 band — nearest catalog fit"*, which covers `validateSystem`'s 0.80–0.90 silence zone | comparison.ts:269-274; comparison.test.ts:250 "out-of-band fits carry a warn note; in-band fits stay clean" | working | NONE |
| CODE.step7-proposal.150 | **Feasibility notes distinguish causes**: 0 panels with a sub-module target ⇒ *"Target {k} kWp is below one {w} W module — raise the target"*; 0 panels otherwise ⇒ *"No panels fit the drawn roofs"*; no inverter ⇒ *"No inverter lands in the 0.90–1.35 DC/AC band for this capacity"*; else the first electrical error. Plus project-level warnings for a roofless project and for an all-zero comparison | comparison.ts:242,300-307,314-321; comparison.test.ts:188,214 | working | NONE |
| CODE.step7-proposal.151 | **The recommendation rule, spelled out**: among rows that are `stringFeasible` **and** not clipping-risky (`dcAcRatio == null \|\| ≤ 1.35`), rank by **lowest payback → highest 25-yr savings → lowest net cost**; the winner's `DesignDecision` names the runner-up and states the tie-breaks. Gate: clipping fits never win and always carry a visible warn note | comparison.ts:323-379; comparison.test.ts:149,226 | working | NONE |
| CODE.step7-proposal.152 | **The ranking inherits every finance defect above**: `paybackYears` is the row-90 sentinel (a never-paying row sorts as exactly 25 alongside a real 25-yr payback), `annualKwh` is `round(report.annualMwh × 1000)` — the row-88 quantisation, so two candidates within 100 kWh/yr are indistinguishable — and `roi25Pct = round(savings25 / netCost × 100)` labels a **gross-revenue-to-cost ratio "ROI"** (a system returning exactly its cost would read 100 %, not 0 %) | comparison.ts:289,296-297,330-334; finance.ts:42,48 | POC-DEFECT(the recommendation is sorted on a sentinel-bearing, quantised key and reports a mis-named ROI) | NONE |
| CODE.step7-proposal.153 | **Basis + decision cards**: `basis` carries `objective` (target_kwp \| max_roof), `targetKwp`, `catalogVersion`, `catalogProvenance` and `irradianceSource` (PVGIS \| estimate, from `activeWeather`); `decisions[]` always includes `comparison-basis` whose `inputs` list `targetKwp`, `catalog=v (provenance)`, `irradiance=…`, `marginPct=…`, plus `shortlist` and `recommendation` cards when applicable | comparison.ts:71-84,338-379,384-392; comparison.test.ts:162,261 | working | NONE |
| CODE.step7-proposal.154 | `memoizedComparison` is a last-value memo keyed on `designFp(project)` alone (which carries the catalog version as of Phase 22d). Candidate rows never read measured `solarAccess`, so no shading stamp belongs in the key — the reasoning is in the file and the memo is value-keyed (a structural clone hits it) | comparison.ts:414-438; comparison.test.ts:338,348,356 | working | NONE |
| CODE.step7-proposal.155 | `complexityOf(n)` — install complexity is **panel count alone**: ≤12 low · ≤30 medium · else high. Roof pitch, height, access, structure type and the whole constructability analyzer pack (§M) are ignored | comparison.ts:228-230,310 | working (a one-variable proxy presented as "install complexity") | NONE |
| CODE.step7-proposal.156 | `moduleEfficiencyPct = round((watt / (lengthM × widthM × 1000)) × 1000)/10` — module area from the *catalog* dimensions, i.e. gross module area, not cell area | comparison.ts:266,288 | working | NONE |

---

## §Q — Consistency / conflict vs the main-suite M06 proposal builder (DIFFERENT SURFACE — not conflated)

The studio's Step 7 is **not** M06's 11-step proposal builder, and the studio's own `/proposal` route (`ProposalView`, sitting 11, fed by `proposal-narrative.ts`) is a **third**, POC-only document. Step 7's contract with M06 is exactly `SC.10-8.19` / `M05-61`: mark the design proposal-ready and hand over numbers + captures. Everything below is a recorded cross-module note, not a Step-7 row.

| # | studio (this area, code-faithful) | M06 (`prd/modules/M06-proposals.md`) | disposition |
|---|---|---|---|
| Q1 | **Tariff escalation 3 %/yr**, hardcoded in `finance.ts:44`, never rendered, never editable | **Step 5 "Electricity inflation %", source default ≈6 %**, user-editable (`M06-11`) | **CONFLICT** — the same lifetime-savings figure is computed twice on different inflation assumptions. A Path-A pre-fill from the studio arrives at 3 % and sits in a field defaulted to 6 %; whichever wins, the studio's Energy Report and the issued document disagree |
| Q2 | **PR is derived** (`PR_equip × shadeFactor`, ≈83 % × shading) and read-only | **Step 4 "Efficiency / PR % (50–100)"** and **"seasonal generation dip % (0–50)"** are typed fields with ✦ AI Auto-fill and "Reset to AI values" (`M06-10`) | **CONFLICT (recorded, M06's to resolve)** — `M06-03` labels design-backed Step-4 generation `derived`, but the field stays editable; the studio's measured monsoon shape (`MONTH_FACTORS` / real monthly Kd) has no channel into a single "dip %" field |
| Q3 | **Four financing modes** (cash / loan / lease / PPA) with packed representative terms; `emiPerMonthInr` is `@deprecated` and dead | **"the EMI calculator is proposal-side — and it is the whole v1 financing story"**; Step 3 exposes only an EMI-interest-rate field (`M06-40`) | **GAP** — the studio computes lease and PPA economics the proposal builder has no field for. `M06-06` makes CAPEX/OPEX-PPA a *document type only* that nothing downstream branches on, so a studio-computed PPA rate (₹/kWh at 20 % below grid, 15 yr) cannot travel into an OPEX proposal |
| Q4 | **Subsidy** = PM Surya Ghar slab hardcoded in `data/rules/india.ts`, gated on `siteType === 'residential'` **and** the panel's `dcr` flag; not editable | **"incentive amount per the pack's incentive model (`F1-14`)"** in the scheme-generic money block (`M06-09`), inside its own client-payable summary | **CONFLICT** — two subsidy computations for one system, on different data (the studio's is DCR-gated at the *panel* level; M06's is an amount in a money block). The studio's is also not scheme-parameterised — see row 91 |
| Q5 | **25-year horizon** in four hardcoded loops; the 25-yr figure is **gross** bill offset (no lifecycle cost, no inverter replacement) | **"Lifetime savings (25-year horizon)"** (`M06-11`), with projections required to travel with their assumptions (`F8-23`) | **AGREES on horizon; OPEN on definition** — nothing states whether the lifetime figure is gross or net. The studio's is gross (row 92) and its narrative sells it without assumptions (row 141) |
| Q6 | **Energy provenance** is real and correct: `irradianceSource` follows the actual computation path, and the Energy Report renders *"Real irradiance — PVGIS {db} ({N}-yr record)"* vs *"Built-in irradiance model (latitude fit, ±10 %)"* | `F8-08`/`F8-09` require exactly that label to ride every generation figure (`M06-10`, `M06-51`) | **AGREES in the model, BREAKS at the hand-off** — Step 7's own SYSTEM SUMMARY drops the label (row 39) and Generate hands numbers by shared project state with no provenance payload (row 41). The label lives only in a sheet the proposal builder never opens. On the estimate path the studio's narrative says nothing at all (row 138) |
| Q7 | **Staleness** is computed from `layoutFp` / `designFp` / `shadingFp`; captures badge themselves; the readiness card reports stale imagery | `M06-46`: staleness is **derived** by comparing the version's *pinned* design fingerprint against what is live (`F8-13`) | **COMPATIBLE mechanism, MISSING pin** — Step 7's Generate flips a status and navigates; it snapshots no fingerprint, so there is nothing on the studio side for a proposal version to pin. Plus the cover's freshness can be laundered (row 33) |
| Q8 | **Narrative fact-traceability** is implemented: each beat carries the `facts` it was built from, and a test asserts every numeric fact appears in the beat's text | `M06-52`: *"every narrative claim maps to a facts entry … no narrative sentence asserts a number that is not in the version's data"* | **AGREES in law, DIFFERENT ARTIFACT** — the studio's narrative belongs to the POC's own `/proposal` page, not to M06's document template. Two documents implement one law; only M06's is the product |
| Q9 | **Money path**: `fin.systemCostInr === bomTotal(mergedBom(project), project)`, margin below tax, per-line GST | `M06-39`: the same locked invariants, *"arithmetic authored in `modules/M05`"* | **AGREES by construction** — M05 is the author; recorded as the one clean seam |
| Q10 | Subsidy caps at 3 kW; the freemium plan gate is `planLimitKw: 10`; the energy model has no clipping term at any scale | `M06-09` capacity range **0.5–7000 kW**, already recorded as diverging from `D1`'s 1 kW → 100 MW | **RECORDED** — a third scale envelope. At C&I sizes the studio's un-clipped kWh (row 65) and 100 %-offset savings (row 87) are the numbers a 7 MW proposal would inherit |

---

## Census diff — `M05-design-studio.md` Appendix A §A.10-8 (`SC.10-8.01`–`SC.10-8.20`, 20 entries)

Every one of the 20 entries is matched by at least one row above. **CENSUS-UNMATCHED: none.**

| census entry | matched at | note |
|---|---|---|
| `SC.10-8.01` two-phase step, four shots each naming name/date/hour | rows 1, 2, 3, 4, 7, 8 | matched **with defects** — entry gate counts records not images (row 2) |
| `SC.10-8.02`–`SC.10-8.05` the four fixed shots | row 7 | clean (21 Jun / 21 Dec of the current year, 9:00 / 12:00) |
| `SC.10-8.06` Capture from the current 3D view | row 10 | clean |
| `SC.10-8.07` "Shadow captures: N of 4" | row 11 | matched **with a wording divergence** — the POC prints `N/4` |
| `SC.10-8.08` four numbered shot buttons, tick + jump/retake | row 12 | clean |
| `SC.10-8.09` auto-advance to the next uncaptured shot | rows 3, 13 | matched **with a divergence** — advance is save-scoped; re-entry always resumes at shot 1 |
| `SC.10-8.10` first capture auto-becomes the cover | rows 14, 18 | clean (non-undoable — recorded) |
| `SC.10-8.11` Skip to review | row 15 | clean |
| `SC.10-8.12` honest save-error with a remedy | rows 16, 17 | matched **with a defect** — the error is never cleared and never reaches review |
| `SC.10-8.13` "Before you issue" card, three verdicts | rows 19, 20, 22, 42, 120 | matched; label wording differs; **the verdict has no effect on the Generate action** (row 42) |
| `SC.10-8.14` each check: status + meaning + jump button | rows 21–26, 103, 109, 111–120 | matched **with defects** — no `commercial` or `data-quality` analyzer exists (row 109); registry population is an import side-effect (row 24, uncertain) |
| `SC.10-8.15` cover preview or "no cover captured yet" | rows 28, 34, 36 | matched **with a defect** — the cover preview carries no staleness marker |
| `SC.10-8.16` the shadow set: image/"Not captured", name, date/hour, OUTDATED—RETAKE, cover marker / SET AS COVER | rows 9, 18, 29, 30, 32, 36 | matched **with a defect** — the printed date/hour is the preset's, not the shot's (row 9) |
| `SC.10-8.17` system summary (kWp, count × watts, avg access %, annual generation) | rows 37, 38, 39, 44, 45, 56, 57 | matched **with defects** — "0 × undefinedW" (row 38); **no provenance or freshness label** (row 39) |
| `SC.10-8.18` Edit photos → capture studio | row 40 | clean (always returns to shot 1) |
| `SC.10-8.19` Generate proposal marks proposal-ready and hands numbers + captures over | rows 41, 42, 43, 134, 135 | matched **with defects** — Quick Generate reaches `/proposal` without setting the status (row 134); Step 10 sets the same status independently (row 43) |
| `SC.10-8.20` staleness law: a stale capture must say so | rows 26, 27, 30, 31, 33, 34 | matched **with defects** — cover freshness is stamped from the app's clock, so a stale image can be promoted to a "fresh" cover (row 33) |

**CENSUS-UNMATCHED table: empty (0 of 20).**

## Beyond census (no `SC.10-8.*` entry — 99 rows)

Rows 5, 6, 35; the whole of **§F** (energy model, 46–55 & 57–70), **§G** (POA, 71–75), **§H** (PVGIS ingestion + proxy + provenance, 76–84), **§I** (finance, 85–94), **§J** (financing options, 95–102), the substrate rows of **§K** (104–108, 110), **§N** (Energy Report sheet, 121–133), **§O** (narrative, 136–142) and **§P** (comparison engine, 143–156). The census section 10-8 covers only Step 7's *screen*; the studio's entire energy and finance model — the thing that makes Step 7's four numbers and the whole proposal — has no census entry in §A.10-8. Its census home is §A.10-7 (the 3D view / energy report) and §A.10-5 (compare options), which sittings 5 and 4 carry; the **model** is inventoried here.

---

**rowCount:** 156
**defectCount:** 28 POC-DEFECT (rows 2, 9, 17, 27, 33, 34, 38, 39, 58, 60, 65, 66, 68, 87, 88, 90, 91, 92, 98, 109, 127, 128, 132, 134, 138, 139, 141, 152) + 2 uncertain (rows 24, 108)
**censusUnmatched:** 0 of 20 (`SC.10-8.01`–`SC.10-8.20` all matched; 11 matched with a recorded divergence — table above)
**newBeyondCensus:** 99 rows

**notes** (≤10 lines)
1. Test evidence: 13 files / 132 tests, **all PASS** — no failing test, so no test-derived DATA finding. Formula values re-derived live with `npx tsx` (subsidy slab, PR 0.83045, POA at Pune, financing options, payback 5.5 yr, 25-yr ₹19.79 L).
2. The energy model is genuinely good physics for a POC — multiplicative losses, beam-only shading with a diffuse floor, real PVGIS Kd, an all-or-nothing mapper, a stale-pin guard and a provenance flag that follows the *actual* computation path. Its three real gaps are **no clipping term** (row 65), **no climate dependence outside irradiance** (row 66) and a **POA readout contaminated by shading** (row 58).
3. The finance model's biggest single defect is the **payback sentinel**: a system that never pays back reports "25 years" everywhere, including as the comparison engine's primary sort key (rows 90, 152).
4. `computeFinancials` reads the **rounded** `annualMwh × 1000` while the unrounded `annualKwh` exists for exactly this purpose — every money figure in the product inherits 100 kWh quantisation (row 88).
5. **F1 pack law:** only the subsidy slab, financing terms and O&M thresholds were extracted to `data/rules/india.ts`. Tariff escalation (3 %), degradation (0.75 %/yr), the 25-yr horizon, `DIFFUSE_SHARE`, `MONTH_FACTORS`, `psh 5.3`, the lat-20/lng-77 fallback and `mockIrradiance`'s India latitude fit are all still code (row 91).
6. **Provenance (F8-08/F8-09)** is implemented correctly in the model and in the Energy Report sheet, and then **dropped at both customer-facing exits**: Step 7's own summary (row 39) and the narrative's estimate path (row 138).
7. The insight registry is a clean substrate, but **no analyzer emits `commercial` or `data-quality`** — the pre-proposal review structurally cannot flag a money or provenance problem (row 109), and its registry is populated only by `Step6Editor`'s module-scope side effect (row 24).
8. Two rows deserve immediate attention as customer-facing falsehoods: **"Set as cover" launders a stale image into a fresh cover** (row 33) and **the narrative calls the 0.35-floored access score "% of available sunlight"** (row 139).
9. Step 7 is a **thin screen over deep libraries**: 43 of 156 rows are the screen; the remaining 113 are the model. Ownership of `comparison.ts` sits here while its only UI lives in Step 4 (`step4-components.md` §F/§G) — read the two together.
10. M06 cross-module: one real numeric conflict (**3 % vs ≈6 % escalation**, Q1), one clean seam (**the money path**, Q9) and one structural gap (**lease/PPA economics with no proposal-side field**, Q3).
