# Feature inventory — shell (Studio shell: wizard frame, dashboard, login, store/undo/autosave, persistence/migration, UI kit, routing)

Pass-two inventory, 2026-08-05. Sitting 10 primary files: `screens/Wizard.tsx`, `screens/Dashboard.tsx`, `screens/Login.tsx`, `store/store.tsx` + `store/store.test.ts` + `store/useDesignSync.ts`, `lib/persistence/{schema,repository,normalize,blobs,useImage}.ts`, `lib/drawing-project.ts`, `router.ts`, `components/ui.tsx` (all under `3d_design_studio/src/features/solar-studio/`), plus `src/app/(studio)/*` (except `share/[shareId]` → s11), `src/app/layout.tsx`, `src/app/design/page.tsx`.

File-list discrepancies vs the task brief (recorded, not resolved): the brief lists "store/ (all 4)" but `store/useHealthSync.ts` is claimed by sitting 5 in file-claims.md — treated as one-hop here; the brief lists `app/page.tsx` but no such file exists (`src/app/` holds only `layout.tsx`, `(studio)/`, `api/`, `design/` — the root route is `app/(studio)/page.tsx`); file-claims assigns `components/BlobImg.tsx` to sitting 10 while the plan table and the brief give it to sitting 11 (share) — not inventoried here.

Test run (read-only, deps from lockfile): `npx vitest run store/store.test.ts lib/__tests__/persistence.test.ts lib/__tests__/drawing-project.test.ts lib/__tests__/step-help.test.ts lib/__tests__/model-version.test.ts components/__tests__/{NumberField,axe,focus}.dom.test.tsx screens/__tests__/` → **11 files, 120 tests, all PASS** (vitest 3.2.7). (`screens/__tests__/` contains only step6-erase (s5), BomRow, DiscountField (s8) — run as directed, owned elsewhere.)

One-hop libraries consulted but owned elsewhere: `lib/health.ts` + `store/useHealthSync.ts` (s5), `lib/electrical/gate.ts` (s7), `data/rules/india.ts` + `lib/bom/view.ts` + `data/pricebook.ts` + `lib/units.ts` (s8), `lib/project-duplicate.ts` (s9), `lib/solar.ts` (s6), `lib/maps.ts` + `lib/geo.ts` (s1), `lib/fingerprints.ts` (s9), `lib/shading.ts` + `lib/analysis-client.ts` (s5/s1), `types.ts` (s13), `lib/pvgis.ts` (s6), `lib/sld.ts` (s7).

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.shell.1 | Wizard step model: `STEP_NAMES` = 10 named steps (Project Setup · Roof Setup · Obstructions · Components · Panel Placement · Manual Edit · Proposal · SLD & Drawings · BOM & Pricing · Final); header always reads "Step {n} of 10 · {name}" — census post-overlay (R7/M05-02) directs 9 visible steps with no user-visible step 5; the code shows 10 | Wizard.tsx:21-32,244-246 | working (census overlay directs 9-step renumber) | SC.10-1.04 |
| CODE.shell.2 | Step 5 has no page of its own: routes 5 and 6 both render `Step6Editor`; Next from step 4 jumps straight to 6, Back from 6 returns to 4 — but a deep link `/wizard/5` still shows "Step 5 of 10 · Panel Placement" over the manual editor (step 5 is URL-reachable, never nav-reachable) | Wizard.tsx:189-195,205-206 | working | SC.10-1.04 |
| CODE.shell.3 | Step navigation `go()`: target clamped 1–10; `project.wizardStep` is a high-water mark (`max(current, target)`) patched NON-undoably, so going Back never loses resume position and step changes never enter the undo stack | Wizard.tsx:176-180 | working | NONE |
| CODE.shell.4 | Prerequisite deep-link gate: highest viewable step = first step 1…9 whose Next-gate fails (else 10); a URL beyond it triggers `navigate('/wizard/{allowedStep}')` and the wizard renders null meanwhile — a stale/deep-linked step can never crash on missing data (e.g. Step 6 with a null panel spec) | Wizard.tsx:161-174 | working | NONE |
| CODE.shell.5 | Step-1 Next gate messages, in order: no state → "Select a state to continue"; unconfirmed location → "Confirm the installation location to continue" | Wizard.tsx:112-115 | working | SC.10-1.15 |
| CODE.shell.6 | Step-2 Next gate: zero roofs → "Draw at least one roof to continue" (census's exemplar phrasing "Draw at least one roof" carried) | Wizard.tsx:116-117 | working | SC.10-1.15 |
| CODE.shell.7 | Step-4 Next gate messages, in order: "Select a panel to continue" → "Select an inverter to continue" → targetKwp ≤ 0 → "Set a target capacity to continue" | Wizard.tsx:119-122 | working | SC.10-1.15 |
| CODE.shell.8 | Step-6 hard gate: zero ENABLED panels → "Place at least one panel to continue"; else `electricalGate(project)` (s7) blocks Next with its message, appending " — use Stringing → Auto string" when auto-stringable; the same gate feeds `allowedStep`, so a design that turns invalid cannot sit on a downstream step | Wizard.tsx:123-135; electrical/gate.ts:27-35 (one hop, s7) | working | SC.10-1.16 |
| CODE.shell.9 | Steps 3, 5, 7, 8, 9 have NO Next gate (nextBlocker returns null); step 10 is terminal | Wizard.tsx:136-138 | working | SC.10-1.15 |
| CODE.shell.10 | Blocked-Next toast: gate message shown in a fixed bottom-centre `role="status"` toast for 2600 ms, then auto-clears; Next does not navigate | Wizard.tsx:182-187,372-391 | working | SC.10-1.15 |
| CODE.shell.11 | Header Back button: ghost icon button, `aria-label="Back"` + tooltip, disabled at step ≤ 1; Back from 6 goes to 4 (step-5 fold) | Wizard.tsx:234-243,193-195 | working | SC.10-1.07 |
| CODE.shell.12 | Header step title "Step {n} of 10 · {name}" — a passive read-out only: no step-list sheet, no ‹ › step arrows, no desktop step rail, and no per-step state display (not started / in progress / done / has errors) exists anywhere | Wizard.tsx:244-246 | working (census 10-1.05/10-1.06 UI absent — see CENSUS-UNMATCHED) | SC.10-1.08 |
| CODE.shell.13 | Design Health chip reads the debounce-STAMPED snapshot (`derived.healthSnapshot.current`), never live `computeHealth` — deliberate: live O(panels²) DRC in the header render path costs 10–40 ms per pointermove on large arrays; `healthKey` (~0.1 ms) alone detects "stamp still settling" | Wizard.tsx:148-158 | working | SC.10-1.09 |
| CODE.shell.14 | Health chip content: coloured band dot (Good=var(--good), Fair=var(--warn), Poor=var(--bad)) + band label + numeric total; tap opens the health sheet (`aria-haspopup="dialog"`); aria-label "Design health: {band} {total} of 100" | Wizard.tsx:249-277,399-404 | working | SC.10-1.09 SC.10-1.17 |
| CODE.shell.15 | Chip provisional state: when the snapshot is flagged provisional OR its key no longer matches the live `healthKey(project)`, the dot dims to 0.45 opacity, label/score dim to 0.6 with a "…" suffix, tooltip becomes "Design health — provisional (recalculating)", aria appends ", provisional — recalculating" | Wizard.tsx:156-158,253-274 | working | SC.10-1.20 |
| CODE.shell.16 | No-score placeholder: before any snapshot exists (no panels yet) a disabled "Health —" chip renders with tooltip "Design health appears once panels are placed" | Wizard.tsx:278-288 | working | SC.10-1.09 |
| CODE.shell.17 | Band thresholds and weights come from rules: Good ≥ 85, Fair ≥ 65, else Poor; total = weighted mean over applicable categories, energy ×40 · electrical ×40 · utilization ×20 (health engine itself is s5; rules s8) | Wizard.tsx:425,509-514; rules/india.ts:440-441 (one hop, s8) | working | SC.10-1.17 |
| CODE.shell.18 | Header units toggle m/ft: `UnitToggle` bound to `useUnits()` — the preference is stored on the USER (`user.units`), so it is global across every project and step; all stored values stay metres, only formatting converts; falls back to metric with no session (share/proposal links) | Wizard.tsx:289-292; units.ts:1-62 (one hop, s8) | working | SC.10-1.10 |
| CODE.shell.19 | Header Save: `patch({})` (bumps `updatedAt`, rides the normal debounced persist) + toast "Project saved" for 1400 ms; save is otherwise automatic — the button is reassurance, not the only save path | Wizard.tsx:293-301 | working | SC.10-1.11 |
| CODE.shell.20 | Header "Save & go home": `patch({})` then navigate to `/projects` (the dashboard). Census words this "Save & exit to lead" — the POC has no lead/CRM, so the exit lands on the project dashboard | Wizard.tsx:302-310 | working (no lead entity in POC) | SC.10-1.12 |
| CODE.shell.21 | Header Help button: `aria-label="Help with step {n}: {name}"`, `aria-haspopup="dialog"`, opens the per-step help sheet | Wizard.tsx:311-320 | working | SC.10-1.13 |
| CODE.shell.22 | Per-step help sheet: titled "Step {n} — {name}", body = one plain-language "what this step is for" paragraph + bulleted tips from `STEP_HELP[step-1]` | Wizard.tsx:346-363 | working | SC.10-1.13 |
| CODE.shell.23 | `STEP_HELP` data: exactly one `{does, tips[]}` entry per step (10), kept adjacent to STEP_NAMES so they cannot drift; step-6 tips fold the old keyboard cheat-sheet into visible help: arrow-nudge 0.1 m (Shift 0.5 m), drag semantics, refused-move rule, and shortcuts V select · P panels · E erase · W walkway · K no-build zone · R rail · L arrester · I inverter · G stringing · H heatmap · S strings; step-help.test pins length parity, non-empty does (>20 chars), ≥1 tip each, and the shortcut coverage | Wizard.tsx:39-107; step-help.test.ts:7-30 (4 tests PASS) | working | SC.10-1.13 |
| CODE.shell.24 | Header primary action: steps 1–9 show "Next ›" (btn-primary); step 10 shows "✓ Done" which navigates to `/projects` | Wizard.tsx:321-329 | working | SC.10-1.14 |
| CODE.shell.25 | Progress bar: 3 px track under the header, fill width = step/10 × 100%, 0.3 s width transition | Wizard.tsx:332-342 | working | NONE |
| CODE.shell.26 | Canvas theming: steps 2, 3, 6 render on `--editor-bg` (dark canvas); all other steps on `--paper` (light form) — matches the POC doc's "editor steps are dark canvas; the form steps are light" | Wizard.tsx:197,213-220 | working | NONE |
| CODE.shell.27 | Step body routing: 1→Step1Setup · 2→Step2Roof · 3→Step3Obstructions · 4→Step4Components · 5/6→Step6Editor · 7→Step7Proposal · 8→Step8Sld · 9→Step9Bom · default→Step10Done | Wizard.tsx:199-211 | working | SC.10-1.04 |
| CODE.shell.28 | Health sheet provisional banner: "Shading is recalculating — the energy score is provisional until it lands." (warn banner at top of sheet) | Wizard.tsx:428-432 | working | SC.10-1.20 |
| CODE.shell.29 | Health sheet category cards: Energy / Electrical / Roof utilization; each shows score "{n} / 100" coloured by its own band, or "not scored yet"; a scored category with zero deductions reads "No issues found." | Wizard.tsx:405-409,433-452 | working | SC.10-1.17 SC.10-1.18 |
| CODE.shell.30 | Deduction rows: "−{points}" (warn colour, bold) + plain-language label per named issue — the census exemplar "−8 · off-south orientation" format | Wizard.tsx:454-462 | working | SC.10-1.18 |
| CODE.shell.31 | "WHAT CHANGED" delta: per changed category "{Category}: {from} → {to}" with "New:" (red), "Resolved:" (green, excluding insight-codes the user set to 'ignored'), and "Dismissed (not fixed):" (grey, the ignored ones) — deltas render ONLY while the persisted snapshot key matches the live health key (during the stamp debounce the "to" values would contradict the cards above). NOTE: delta compares `healthSnapshot.prev` vs `.current`, i.e. the previous background STAMP (≈600 ms debounce), not literally "since last save" as the census words it | Wizard.tsx:420-424,465-502 | working (census wording "since last save" ≠ code's per-stamp delta — recorded) | SC.10-1.19 |
| CODE.shell.32 | Health sheet context line: "Context (not scored): {items}" for unscored context facts | Wizard.tsx:504-508 | working | SC.10-1.18 |
| CODE.shell.33 | Health sheet provenance footer: "Total {n} = weighted mean of the scored categories (energy ×40, electrical ×40, utilization ×20). Deterministic rule-based deductions — every point traces to a named issue above. Bands: Good ≥ 85, Fair ≥ 65." (weights/bands read live from rules, not hard-coded copy) | Wizard.tsx:509-514 | working | SC.10-1.17 |
| CODE.shell.34 | Dashboard quarantine banner (`role="alert"`): "{N} saved project(s) couldn't be read and was/were set aside without touching your other projects. The raw data is preserved in browser storage under 'solar-studio-quarantine:' keys for recovery." — shown whenever the boot load quarantined projects | Dashboard.tsx:108-125 | working | NONE |
| CODE.shell.35 | Dashboard top bar: sun-icon "SOLAR STUDIO" brand, the logged-in company name, a "v0.1-poc" version badge, Language and Logout ghost buttons (labelled + tooltipped) | Dashboard.tsx:126-168 | working | NONE |
| CODE.shell.36 | Logout: dispatches `logout` (clears user + active project, keeps projects in storage) and navigates to /login | Dashboard.tsx:155-166; store.tsx:148-149 | working | NONE |
| CODE.shell.37 | Language dialog: 4 languages listed — English (selectable), Hindi/Thai/Vietnamese VISIBLE but disabled at 0.55 opacity with "Not translated yet — the app stays in English"; deliberate honesty rule in code comment: a picker that stores a language nothing renders in "lies about what it did"; radio dispatches `set-language` (English only reachable) | Dashboard.tsx:34-42,505-551; store.tsx:150-153 | working | NONE |
| CODE.shell.38 | "My Projects" header with live counts line "{N} project(s) · {M} in progress" and the primary "+ New Project" button | Dashboard.tsx:170-189 | working | NONE |
| CODE.shell.39 | New Project: `create-project` with `newProject()` defaults, becomes active, undo/redo cleared, then navigate to /wizard/1 | Dashboard.tsx:92-95; store.tsx:158-165 | working | SC.10-1.03 |
| CODE.shell.40 | Filter chips: All / In progress / Proposal ready, each with a live count badge and `aria-pressed`; filtering is by `project.status` ('in_progress' \| 'proposal_ready') | Dashboard.tsx:25,73-80,193-210 | working | NONE |
| CODE.shell.41 | Search field ("Search name, customer, address", labelled): case-insensitive substring match over project name, customer name and address | Dashboard.tsx:56-64,211-237 | working | NONE |
| CODE.shell.42 | Sort select (labelled "Sort projects"): "Last updated" (updatedAt desc, default) or "Name" (locale compare) | Dashboard.tsx:48,65-69,238-246 | working | NONE |
| CODE.shell.43 | Empty state: home icon + "No projects yet — create your first solar design." (shown for zero matches too — a filtered-empty list shows the same copy) | Dashboard.tsx:251-257 | working | NONE |
| CODE.shell.44 | Project card is a real button (Phase 22p fix): `role="button"`, `tabIndex=0`, aria-label "Open project {name}, customer {customer}", Enter AND Space open it (Space scroll prevented) — previously the primary action of the home screen was mouse-only | Dashboard.tsx:282-306 | working | SC.10-1.03 |
| CODE.shell.45 | Opening a project RESUMES at its saved step: `open-project` (clears undo/redo + conflict flag) then navigate `/wizard/{p.wizardStep}` | Dashboard.tsx:97-100; store.tsx:173-180 | working | SC.10-1.03 |
| CODE.shell.46 | Card thumbnail: static satellite image of the confirmed pin (zoom 18, 320 px via `staticSatelliteUrl`, s1) or a dark gradient placeholder when no location | Dashboard.tsx:268-275,307-314 | working | NONE |
| CODE.shell.47 | Card status chip: "Proposal ready" (good colours) or "In progress" (white chip), with a solid state dot | Dashboard.tsx:316-344 | working | NONE |
| CODE.shell.48 | Card ⋮ actions menu (`aria-haspopup="menu"`, `aria-expanded`, role="menu"/"menuitem"): Show Proposal (open + /proposal) · Show 3D (open + /wizard/6) · BOM & Pricing (open + /wizard/9) · Duplicate · Delete (red, separated) — menu toggles per card, card-click suppressed inside it | Dashboard.tsx:276-281,345-423 | working | SC.10-1.03 |
| CODE.shell.49 | Duplicate: `duplicateProject` (s9) with a fresh project id + fresh shareId + collision-avoiding name against every existing project name; the copy becomes the active project (create-project) | Dashboard.tsx:82-90; project-duplicate.ts (one hop, s9) | working | SC.10-1.02 |
| CODE.shell.50 | Card stats grid: CAPACITY ("{kWp} kWp" from `computeEnergyReport` (s6), "—" when 0) · CUSTOMER ("—" when blank) · UPDATED (relative: "today" / "yesterday" / "{d}d ago"). Census 10-1.02 wants size AND generation, price, payback per variant plus one "recommended" — only size/capacity exists on the card; no generation/price/payback, no recommended marker, and projects are a flat list, never grouped per lead | Dashboard.tsx:27-32,425-468 | working (census 10-1.02 metrics beyond size absent — see CENSUS-UNMATCHED) | SC.10-1.02 |
| CODE.shell.51 | Delete confirm dialog: "Delete project?" / "'{name}' and its design will be permanently removed from this browser." Cancel / red Delete; deletion also removes its localStorage key and GC's its IndexedDB images on the next flush. Deletion is confirm-only and NOT undoable (the undo stack is per-active-project); product-spec v1 §8 rule 5 says "Nothing is deleted without undo" — that spec is marked historical/superseded, recorded here, not resolved | Dashboard.tsx:477-503; store.tsx:166-172,366-373 | POC-DEFECT(docs-contradict: product-spec §8.5 "nothing deleted without undo" — project delete is confirm-only, no undo) | NONE |
| CODE.shell.52 | Login is a two-phase MOCK: phase 1 phone — fixed "+91 IN" prefix chip, placeholder 9876543210, digits-only (non-digits stripped on input), maxLength 10, Continue enabled only when `/^\d{10}$/` matches, Enter advances; phase 2 password — any non-empty string logs in. The POC doc/product-spec direct phone + OTP, no password; code uses a password mock and says so on screen | Login.tsx:6-14,61-106 | working (mock; spec directs OTP — recorded) | NONE |
| CODE.shell.53 | Password phase: heading flips "Welcome" → "Welcome Back", subline "Enter your password for {phone}"; show/hide password eye toggle (labelled + tooltip); visible hint "Mock login — any password works in this POC."; autoFocus on the field; Enter submits when non-empty | Login.tsx:52-59,107-156 | working | NONE |
| CODE.shell.54 | Successful login dispatches user `{phone, companyName: 'helio grid', language: 'en', units: 'metric'}` and navigates to /projects | Login.tsx:16-22 | working | NONE |
| CODE.shell.55 | "Forgot password?" ghost button has NO onClick handler — a dead control (the class of inert control the repo's honesty audit elsewhere calls out) | Login.tsx:168 | POC-DEFECT(dead control: no handler) | NONE |
| CODE.shell.56 | Phase-2 "Back" returns to the phone phase (phone value kept); fixed footer "Solar Design Studio POC" | Login.tsx:165-167,173-182 | working | NONE |
| CODE.shell.57 | `newProject()` defaults: name "New Solar Project", country India, siteType residential, connection single-phase, sanctionedLoad 0, groundMount false, monthlyBill null, tariff = rules default (`defaults.tariffNewProjectInrPerKwh`, s8), status 'in_progress', wizardStep 1, margin = DEFAULT_MARGIN_PCT (12%), calibration {scaleFactor 1, northOffsetDeg 0, reference null}, empty entity arrays, fresh shareId | store.tsx:85-131; pricebook.ts:137 (one hop, s8) | working | NONE |
| CODE.shell.58 | Share id generation: `crypto.randomUUID()` with dashes stripped, first 12 chars; non-crypto fallback `Math.random` base-36 (10 chars); every duplicate gets a FRESH shareId | store.tsx:79-83 | working | NONE |
| CODE.shell.59 | Undo model: whole-project snapshots; `update-project {undoable:true}` pushes the PRE-patch snapshot and clears redo; stack capped at 25 snapshots (`slice(-24)` + current); default is NON-undoable (derived/form writes never pollute undo) | store.tsx:191-208,471-475; store.test.ts:87-118 (PASS) | working | NONE |
| CODE.shell.60 | Undo/redo restore re-stamps `updatedAt = Date.now()` — deliberate: multi-tab merge is last-writer-wins on that clock, so an undo persisted with its old timestamp would be ignored by other tabs and silently re-overwritten; empty-stack undo/redo is a no-op | store.tsx:226-254; store.test.ts:121-148 (PASS) | working | NONE |
| CODE.shell.61 | Undo/redo history is scoped to the ACTIVE project and cleared on create/open/close/hydrate; NOTE: no keyboard binding or button lives in the shell itself — Ctrl+Z/Ctrl+Shift+Z and undo/redo toolbar buttons are wired inside the canvas steps (s2/s3/s5 screens dispatch `{type:'undo'/'redo'}`) | store.tsx:158-190; store.test.ts:197-218 (PASS) | working | NONE |
| CODE.shell.62 | `stamp-health` action: merges the health snapshot into the CURRENT project at reduce time (a debounced closure can hold a stale `derived`; this can't), bumps updatedAt, never an undo step | store.tsx:210-225; store.test.ts:156-165 (PASS) | working | NONE |
| CODE.shell.63 | Multi-tab last-writer-wins: an external project write is adopted only when STRICTLY newer (`existing.updatedAt >= incoming` is ignored — an equal stamp is our own echo); unknown ids are adopted as new projects; external delete removes the project (and clears active if it was open) | store.tsx:255-287; persistence.test.ts:256-311 (PASS) | working | NONE |
| CODE.shell.64 | Stamp-only echo detection: an external write that is content-equal ignoring `updatedAt` + `derived` (another tab's background shading/health stamper) neither wipes the editing tab's undo history nor raises the conflict banner; a REAL foreign edit does both | store.tsx:263-279; store.test.ts:167-194 (PASS) | working | NONE |
| CODE.shell.65 | Hydration: async `loadState()` (v1→v2 migration + image hoisting) → `hydrate` action; the persisted-snapshot ref is seeded with EXACTLY what storage holds so the first flush writes nothing — a booting tab must never blanket-rewrite keys over newer data another tab saved meanwhile | store.tsx:324-348 | working | NONE |
| CODE.shell.66 | Boot-time image GC: after hydration, `pruneOrphanImages` deletes IDB blobs referenced by neither loaded projects nor quarantined payloads; runs ONLY at boot because mid-session orphans may still sit in the in-memory undo stack | store.tsx:339-344; blobs.ts:100-118 | working | NONE |
| CODE.shell.67 | Autosave: every state change schedules a 250 ms debounced flush; flush writes ONLY dirty project keys (reference inequality vs the persisted snapshot) and meta only when identity/index changed — a keystroke never rewrites every project | store.tsx:315,350-414 | working | SC.10-1.11 |
| CODE.shell.68 | Flush-on-exit: `pagehide` and `visibilitychange` force an immediate flush so the 250 ms debounce window can never be lost to a tab close/background | store.tsx:416-425 | working | SC.10-1.11 |
| CODE.shell.69 | Deletions in the flush: project keys removed AND their images GC'd; after a lost snapshot (quota Retry) the diff can't see deletions, so `sweepOrphanProjectKeys` removes keys the app no longer knows — keeping quarantined ids, whose prj: key may be the only surviving copy | store.tsx:366-380; repository.ts:296-304 | working | NONE |
| CODE.shell.70 | Persist status: any failed write sets 'quota'/'error' and the persisted snapshot does NOT advance, so the next flush retries everything still dirty; `retryPersist` forgets the snapshot entirely ⇒ next flush rewrites all keys | store.tsx:392-403,441-448 | working | NONE |
| CODE.shell.71 | Store access: `useStore()` throws "useStore outside StoreProvider"; `useActiveProject()` returns the active project or null; `useProjectPatch()` dispatches update-project with `undoable=false` default | store.tsx:457-475 | working | NONE |
| CODE.shell.72 | `set-language` / `set-units` are no-ops when logged out (guarded on `state.user`) | store.tsx:150-157 | working | SC.10-1.10 |
| CODE.shell.73 | Storage schema v2 layout: `solar-studio-meta` {schemaVersion 2, user, activeProjectId, projectIds[]} + `solar-studio-prj:<id>` one project per key + `solar-studio-quarantine:<id>` raw unparseable payloads (never auto-deleted) + `solar-studio-v1`/`-backup` legacy keys; images live in IndexedDB by id so a keystroke never re-serializes megabytes of base64 | schema.ts:1-31 | working | NONE |
| CODE.shell.74 | v1→v2 migration: the monolith is split into per-project keys, inline base64 images hoisted to IDB, user.units defaulted to metric, v1 kept as `solar-studio-v1-backup`; the v1 key is removed ONLY when every write of the new layout AND the backup succeeded | repository.ts:128-179; persistence.test.ts:82-134 (PASS) | working | NONE |
| CODE.shell.75 | Migration under quota never loses data: on any failed write the (possibly half-written) meta key is dropped so the next boot re-runs migration from the untouched v1 monolith; the running session still uses the fully-migrated in-memory state and the quota chip surfaces the failure | repository.ts:169-177; persistence.test.ts:196-217 (PASS) | working | NONE |
| CODE.shell.76 | A v1 payload unreadable as a whole becomes the backup key and the app starts fresh (if even the backup write fails, the v1 key itself stays as backup) | repository.ts:132-137; persistence.test.ts:136-142 (PASS) | working | NONE |
| CODE.shell.77 | Per-project quarantine on load: a project that fails parse/shape-check (`id` string + `roofs` array required) is copied to its quarantine key and the original prj: key removed ONLY when that copy landed — under quota the original key stays as the only copy; siblings are never touched | repository.ts:68-75,110-118; persistence.test.ts:145-194 (PASS) | working | NONE |
| CODE.shell.78 | Corrupt meta index: raw meta preserved under `solar-studio-meta-corrupt-backup` and the index REBUILT by scanning surviving prj: keys — a corrupt index never orphans intact projects; a meta that parses but has no usable projectIds takes the same path | repository.ts:191-218; persistence.test.ts:221-230 (PASS) | working | NONE |
| CODE.shell.79 | Image hoisting is idempotent: hoisted projects have their slimmed JSON written back immediately, so a second boot reuses the same blob ids instead of duplicating bytes; a failed blob write drops the image rather than the project | repository.ts:42-66,104-109; persistence.test.ts:232-253 (PASS) | working | NONE |
| CODE.shell.80 | Quota classification: `QuotaExceededError`/`NS_ERROR_DOM_QUOTA_REACHED` DOMExceptions → 'quota', anything else → 'error'; `loadState` catches ALL storage exceptions and behaves like a fresh browser — storage being unreadable never crashes the app | repository.ts:77-94,219-224; persistence.test.ts:170-176 (PASS) | working | NONE |
| CODE.shell.81 | `quarantinedBlobIds()`: regex scan (`img_[0-9a-fA-F-]{10,}`) over RAW quarantine payloads — deliberate, since quarantined payloads by definition may not parse — so the boot image sweep cannot delete a recovered project's captures | repository.ts:268-287 | working | NONE |
| CODE.shell.82 | Other-tab subscription: `storage` events (which never fire in the writing tab) deliver parsed+normalized project updates, project deletions (null newValue) and meta changes; unparseable external writes are ignored (the writer owns quarantine) | repository.ts:306-337; store.tsx:427-439 | working | NONE |
| CODE.shell.83 | `quarantinedIds()` (repository.ts:253) — standalone export that scans quarantine keys "for the dashboard recovery notice" — is imported NOWHERE; the dashboard banner actually reads `state.quarantinedIds` captured at load. Dead export; a quarantine that happens in another tab after boot is not reflected until reload | repository.ts:252-266; Dashboard.tsx:108 | working (unused export recorded; banner is boot-time only) | NONE |
| CODE.shell.84 | `normalizeProject` runs on EVERY project loaded from storage (and on external tab writes): fields added after launch get defaults, malformed sub-entities drop item-by-item (all-or-nothing per ITEM, never per project); pure, no storage I/O | normalize.ts:1-28,128-222 | working | NONE |
| CODE.shell.85 | Weather guard: persisted weather must pass `isValidSiteWeather` (s6) whole or is DROPPED whole — the app falls back to the irradiance estimate instead of rendering NaN energy bars; location itself is kept | normalize.ts:20-27,197-199; store.test.ts:20-49,309-325 (PASS) | working | NONE |
| CODE.shell.86 | Pricing repair: marginPct clamped 0–60 (default 12 when missing/NaN); discount repaired or dropped — kind coerced to percent unless 'amount', non-finite/≤0 values dropped ("a zero discount is no discount"), percent capped at 100, label kept only when present | normalize.ts:108-126; store.test.ts:223-232 (PASS) | working | NONE |
| CODE.shell.87 | `Exhaustive<T>` compile-time pattern: rebuilt objects (pricing, derived, calibration) must MENTION every field of their type — omitting a newly-added sibling field is a compile error, not silent data loss (the discount was once saved correctly and erased on load exactly this way) | normalize.ts:86-98,125,155,180 | working | NONE |
| CODE.shell.88 | Derived-state defaults: `solarAccessFp` null unless string (legacy loads as "provisional" so shading re-verifies on first open); legacy `sldParams` snapshot migrates ONCE into `derived.sldOverrides` as a DIFF vs freshly-derived defaults (untouched fields return to live-derived; only real edits survive) and `sldParams` is nulled; `sldIntroSeen` inferred; a structurally-unsound persisted healthSnapshot resets to null ("never scored") rather than crashing the delta panel | normalize.ts:73-84,144-156; persistence.test.ts:133 (PASS) | working | NONE |
| CODE.shell.89 | Calibration repair: scaleFactor must be a finite number > 0 else 1; northOffsetDeg finite else 0; reference defaults null | normalize.ts:167-180 | working | NONE |
| CODE.shell.90 | Segment/keepout filters: a segment needs id/polygon/removed/rows/racking, a keepout needs id/shape/heightM — bad items drop individually; a malformed `legPlan` drops to AUTO without taking the table with it, and the key is REMOVED (not undefined) so a plan-less segment serialises byte-identically to pre-feature days (lazy-field/fingerprint contract); an empty point list counts as no plan | normalize.ts:29-63,181-184; store.test.ts:234-259 (PASS) | working | NONE |
| CODE.shell.91 | Every entity array is coerced to an array (obstructions/panels/walkways/rails/arresters/inverterPlacements/strings/bomOverrides/roofs) — a stored non-array field would otherwise crash the recompute host on every route and brick the whole app | normalize.ts:186-195; store.test.ts:291-307 (PASS) | working | NONE |
| CODE.shell.92 | Roof defaults & legacy coercions: pitchDeg 0, slopeAzimuthDeg 180, perEdgeSetbacksM null; parapet {enabled false, heightM 1, widthM 0.3, suppressSharedEdges true}, direction FORCED 'inward' (renderers always drew inward regardless of the stored value — preserves what saved projects showed), perEdge coerced from legacy {enabled} objects to boolean[] | normalize.ts:200-220; store.test.ts:261-289 (PASS) | working | NONE |
| CODE.shell.93 | Persisted `bom` block validation: overrides need lineKey + fields object, custom must be an array, inputs (avgDcRunM/avgAcRunM) must be finite ≥ 0; an EMPTY block normalizes to absent so a project that never edited its BOM keeps a byte-identical fingerprint; legacy `bomOverrides` deliberately NOT migrated here (migration re-derives the whole BOM — the store does it lazily on first BOM edit instead) | normalize.ts:224-259 | working | NONE |
| CODE.shell.94 | IndexedDB image store `solar-studio-blobs`/`images`: values are data-URL strings (plain `<img src>`, no object-URL lifecycle); ids `img_<uuid>`; a failed DB open resets the cached promise so it cannot poison every later call | blobs.ts:12-55 | working | NONE |
| CODE.shell.95 | In-memory image cache: `putImage`/`getImage` populate it; `peekImage` is a synchronous peek so cache-warm images render on FIRST paint without flicker | blobs.ts:35,57-74 | working | NONE |
| CODE.shell.96 | Image GC: `deleteImages` is best-effort (failure only wastes space, never surfaced); `projectBlobIds` = capture blobs + cover blob; boot-only `pruneOrphanImages` (see CODE.shell.66) | blobs.ts:76-118 | working | NONE |
| CODE.shell.97 | `useImage(blobId)`: resolves an id to a data URL; cache-warm ids resolve in the initial render; a COLD id clears the previous image first — never show picture A labelled as picture B while B loads; a read failure resolves to null (placeholder), never an unhandled rejection | useImage.ts:10-39 | working | NONE |
| CODE.shell.98 | `isoProject`: standard 30° isometric (x/y rake ±30°, z vertical, screen-y down); chosen over perspective because equal lengths draw equal — a fabricator can measure the drawing; linear (midpoints project to midpoints) | drawing-project.ts:16-31; drawing-project.test.ts:12-43 (PASS) | working | NONE |
| CODE.shell.99 | `elevationProject`: side elevation along the row axis, south/down-tilt edge on the LEFT (drafting convention), height up the page, row axis ignored. In-code warning recorded verbatim: `components/StructurePreview` (s5) draws this MIRRORED (its comment claims front-left but its arithmetic puts south right) — the printed sheet follows convention, the thumbnail is inconsistent and left uncorrected | drawing-project.ts:33-46; drawing-project.test.ts:45-65 (PASS) | POC-DEFECT(inconsistency recorded in code: StructurePreview thumbnail mirrors the printed elevation) | NONE |
| CODE.shell.100 | `fitToBox`: fits projected points into a frame PRESERVING aspect (a stretched drawing cannot be measured), default pad 12, drawing centred, `unitsPerMetre = k` is exactly what a scale bar must draw; empty and degenerate point sets return finite transforms (no divide-by-zero) | drawing-project.ts:48-91; drawing-project.test.ts:67-125 (PASS) | working | NONE |
| CODE.shell.101 | `projectMembers`: maps a 3D member list through a projection keeping each member's `kind` for per-class styling on the sheet | drawing-project.ts:93-106; drawing-project.test.ts:127-137 (PASS) | working | NONE |
| CODE.shell.102 | Router route model: `/share/<id>` → share; `/wizard/<step>` → wizard with step coerced to a number and clamped 1–10 (non-numeric → 1); `/projects`, `/bom`, `/proposal`, `/login` literal; ANY unknown path → login | router.ts:26-54 | working | NONE |
| CODE.shell.103 | `navigate()`: same API as the former hash router — routes through the bound Next router push when mounted, else falls back to `window.location.assign`; `useRoute` binds the Next router on mount | router.ts:13-33 | working | NONE |
| CODE.shell.104 | Root route `/` redirects by session: user → /projects, else /login (replace, not push) | app/(studio)/page.tsx:1-16 | working | SC.10-1.01 |
| CODE.shell.105 | Route guards (after hydration only): no user → /login for every route except /share/*; a logged-in user at /login → /projects; /wizard/* or /proposal without an ACTIVE project → /projects. The share route is deliberately session-free | StudioClientLayout.tsx:117-134 | working | NONE |
| CODE.shell.106 | Hydration gate: stateful route content renders NOTHING until localStorage has loaded — server and first client render must be identical (Next hydration correctness) | StudioClientLayout.tsx:131-133; (studio)/layout.tsx (force-dynamic) | working | NONE |
| CODE.shell.107 | `DesignSync` host mounts `useDesignSync` + `useHealthSync` (s5) at the LAYOUT level, not inside the wizard — a stale project heals (shading + health stamps) no matter which route it is opened on: /proposal, /share, /projects, deep links | StudioClientLayout.tsx:10-19,141-147 | working | SC.10-1.20 |
| CODE.shell.108 | Persistent "Not saved" chip (`role="alert"`, fixed bottom-right, dark red): quota → "Not saved — browser storage is full. Delete old projects to free space."; error → "Not saved — browser storage is unavailable."; a Retry button triggers the full rewrite (CODE.shell.70); invisible while status is 'ok' | StudioClientLayout.tsx:21-72 | working | NONE |
| CODE.shell.109 | External-conflict banner (`role="status"`, fixed top-centre, blue): "This project was updated in another tab — showing the latest version." with a labelled dismiss ×; raised only by a REAL foreign edit to the open project (CODE.shell.64) | StudioClientLayout.tsx:74-115 | working | NONE |
| CODE.shell.110 | Wizard route page clamps the `[step]` URL param to 1–10 before rendering `<Wizard>`; login/projects/proposal pages are thin re-exports of the feature screens | app/(studio)/wizard/[step]/page.tsx:1-10 etc. | working | NONE |
| CODE.shell.111 | Root layout: metadata title "Solar App", description "Solar design and proposal platform", `lang="en"`; single stylesheet entry `@/design/index.css` which pulls the legacy studio CSS in as a named cascade layer so Tailwind utilities can win (layer order is load-bearing per repo CLAUDE.md) | app/layout.tsx:1-21 | working | NONE |
| CODE.shell.112 | `/design` — living design-system reference ("Instrument"): demonstrates primary actions (ink-on-brass 5.78:1, disabled, danger), surfaces & text tiers, status chips, the 12px-floor type scale, tabular-number quote table, data-colour namespace (good/mid/poor with legend), spacing/radius scale, elevation 1–3, dark canvas chrome, and token swatch ramps — the page Tailwind scans to prove every token resolves | app/design/page.tsx:1-241 | working | NONE |
| CODE.shell.113 | `useDesignSync`: recomputes per-panel solar access whenever `shadingFp` (s9) drifts from the stamped `derived.solarAccessFp`; 800 ms debounce; runs OFF the main thread via `requestSolarAccess` (s1, cancelPrevious) — Tier-2 casters grow with panel count squared; skips when no panels or already computed for exactly this geometry | useDesignSync.ts:14-42 | working | SC.10-1.20 |
| CODE.shell.114 | Sync correctness rules: stamps the fingerprint of the geometry it ACTUALLY computed against; applies results to the LATEST panels re-read at completion; unchanged values still stamp freshness (or the "recalculating" badge would never clear); never an undo step; a superseded request (`AnalysisSuperseded`) is the normal mid-drag case, not an error; the stamp is itself an effect dependency so a clobbered stamp self-heals | useDesignSync.ts:20-77 | working | SC.10-1.20 |
| CODE.shell.115 | `Sheet` (side sheet): backdrop click closes, Escape closes, focus trap active, `role="dialog"` `aria-modal="true"` labelled by its title, header with optional icon + optional `right` slot + labelled Close button | ui.tsx:6-15,70-105; focus.dom.test.tsx (PASS) | working | NONE |
| CODE.shell.116 | `Dialog` (modal): same dialog semantics + a required actions row; backdrop click closes only when an `onClose` is supplied (confirm dialogs without onClose cannot be dismissed by misclick); inner click stops propagation | ui.tsx:107-142 | working | NONE |
| CODE.shell.117 | Focus trap (Phase 22p): on open, focus moves to the first focusable control (fallback: the container); Tab/Shift+Tab wrap at the ends and can never escape into the page behind; on close focus RESTORES to whatever opened the surface — "the half people forget"; hidden elements excluded via offsetParent check | ui.tsx:17-68; focus.dom.test.tsx:44-114 (12 tests PASS) | working | NONE |
| CODE.shell.118 | Escape-to-close is global while a Sheet/Dialog with onClose is mounted (window keydown listener) | ui.tsx:6-15; focus.dom.test.tsx:116-140 (PASS) | working | NONE |
| CODE.shell.119 | `SliderRow`: − / + stepper buttons (labelled "Decrease/Increase {label}") stepping by `step` (default 0.1) with clamp to min/max and 3-dp rounding; a range input (labelled); value display with optional display-only `format` (stored units never change) + unit suffix; optional hint line | ui.tsx:144-208 | working | NONE |
| CODE.shell.120 | `Toggle` / `ToggleRow`: `role="switch"` + `aria-checked`, labelled; ToggleRow adds label + optional sub-caption layout | ui.tsx:210-260 | working | NONE |
| CODE.shell.121 | `Seg` segmented control: `role="radiogroup"` of `role="radio"` buttons with aria-checked | ui.tsx:262-286 | working | NONE |
| CODE.shell.122 | `OptionCard`: selectable card with `aria-pressed`/`aria-disabled`, selected state (info border/bg + check), disabled at 0.55 opacity with not-allowed cursor and onClick stripped, optional icon/badge/sub-line | ui.tsx:288-362 | working | NONE |
| CODE.shell.123 | `UnitToggle`: radiogroup labelled "Units" with m / ft radio chips (info-filled when active) — the single control every studio surface reuses for the global unit preference | ui.tsx:364-391 | working | SC.10-1.10 |
| CODE.shell.124 | `NumberField` commits ONCE, on blur or Enter (Enter blurs; blur commits — never double); while focused it owns its draft text so a half-typed value never round-trips through the store; Escape abandons the edit; emptying CLEARS (`undefined`) rather than committing 0 ("₹0 is a price you can quote; 'no value' is not"); values clamp to min/max; re-typing the same value is not an edit (no undo entry); `type="number" inputMode="decimal"`, required ariaLabel, optional suffix | ui.tsx:393-474; NumberField.dom.test.tsx (7 tests PASS) | working | NONE |
| CODE.shell.125 | Commit rules live in `commitNumber` (lib/bom/view, s8) so they are testable without a DOM: null draft → none; blank → clear only if a value existed; non-finite → none; clamp then no-op when equal to current | ui.tsx:431-437; bom/view.ts:25-39 (one hop, s8) | working | NONE |
| CODE.shell.126 | `TextField`: text counterpart — commits on blur/Enter only when the draft differs, Escape abandons; exists because per-keystroke patching made undoing a rename one keypress per letter | ui.tsx:476-527 | working | NONE |
| CODE.shell.127 | `DataTable`: accessible by construction — `caption` is a REQUIRED prop (screen-reader-only by default, `captionVisible` opts in) and every column header renders `scope="col"`; column width/alignment options | ui.tsx:529-584 | working | NONE |
| CODE.shell.128 | `SR_ONLY` style constant: visually hidden but reachable by assistive tech (clip pattern, never display:none) — exported for reuse (e.g. the BOM actions column header) | ui.tsx:586-597 | working | NONE |
| CODE.shell.129 | `EmptyState`: centred icon + text block used by list screens | ui.tsx:599-617 | working | NONE |
| CODE.shell.130 | Automated a11y gate: axe-core runs over what jsdom can render — BomRow inside a real DataTable, NumberField/TextField, Dialog, Sheet, StructureSheet, LegPlanEditor — asserting ZERO violations (colour-contrast rule disabled because jsdom fakes layout); scope stated plainly in-file: anything inside <Canvas> (R3F/WebGL) is out of automated reach and stays hand-verified | components/__tests__/axe.dom.test.tsx:1-138 (6 tests PASS) | working | NONE |
| CODE.shell.131 | Model-version gate (test owned here; behavior in s9 fingerprints + s5 structure): `layoutFp` carries a `\|sm:<structureModelVersion>` stamp so captures taken under an OLDER structure model read STALE even when stored bytes are identical (the lazy-field default-move problem — "the 9-tonne shed"); the stamp is the only unconditional append | lib/__tests__/model-version.test.ts:1-98 (6 tests PASS); fingerprints.ts/structure.ts (one hop, s9/s5) | working | NONE |

## CENSUS-UNMATCHED (Appendix A §A.10-1 entries with no carrying code, or with sub-behaviors absent from code — regression-risk list)

| Census entry | What has no code today |
|---|---|
| `SC.10-1.05` | Step states (not started / in progress / done / has errors) — NO per-step state model or display exists anywhere in the shell; the only step signals are the "Step n of 10" text, the progress bar, and the wizardStep high-water mark |
| `SC.10-1.06` (partial) | The compact indicator TEXT exists (CODE.shell.12) but it is inert: no "‹ ›" step arrows, no tap-to-open step-list sheet, no desktop step rail — the only step navigation is Back/Next and browser URL |
| `SC.10-1.01` (partial) | "Opens from lead 'Create design'" — the POC has no lead/CRM entity at all; entry is login → /projects dashboard → New Project (CODE.shell.39, CODE.shell.104) |
| `SC.10-1.02` (partial) | Per-variant generation, price and payback on the design list, one variant marked "recommended", and per-lead grouping of variants — none exist; the card shows capacity/customer/updated only, and duplication lineage is a fresh flat project (CODE.shell.49/50) |
| `SC.gate.01`–`SC.gate.04` | Process law (the census is the acceptance checklist / merge gate), not code behavior — no code counterpart is possible; recorded N/A, not regression risk |

## Summary

- rowCount: 131
- defectCount: 3 — CODE.shell.51 (project delete confirm-only, no undo, vs product-spec §8.5), CODE.shell.55 ("Forgot password?" dead control), CODE.shell.99 (StructurePreview thumbnail mirrors the printed elevation — inconsistency recorded in code itself)
- censusUnmatched: 1 full (SC.10-1.05) + 3 partial (SC.10-1.01, SC.10-1.02, SC.10-1.06) + 4 N/A process entries (SC.gate.01–04)
- newBeyondCensus: 88 rows carry census NONE — the whole persistence/migration/quarantine/quota system, multi-tab LWW + conflict banner, undo/redo model, autosave + flush-on-exit, blob store + useImage, the UI kit incl. focus trap and commit-once fields, the a11y test gates, routing/guards/hydration gate, the login screen, dashboard search/filter/sort/language, /design reference, and the drawing projections
- notes (≤10 lines):
  1. All 11 shell-area test files PASS (120 tests) — no failing-test DATA.
  2. Wizard shows 10 steps; census overlay (R7/M05-02) directs 9 visible with no step 5 — step 5 is already page-less in code but still named, counted and URL-reachable (CODE.shell.1/2).
  3. "Save & exit to lead" lands on /projects — no lead entity exists in the POC (CODE.shell.20).
  4. Health "what changed" delta is per background stamp (~600 ms), not literally "since last save" (CODE.shell.31).
  5. Undo/redo state lives here but its keyboard/toolbar triggers live in the canvas screens (s2/s3/s5) — split ownership recorded (CODE.shell.61).
  6. Brief-vs-file-claims discrepancies (useHealthSync, app/page.tsx, BlobImg.tsx) recorded in the preamble, unresolved.
  7. `quarantinedIds()` is a dead export; the dashboard quarantine banner is boot-time-only (CODE.shell.83).
  8. screens/__tests__/* contains only other sittings' tests (s5/s8); run as directed, all PASS, inventoried by their owners.
  9. `model-version.test.ts` and `step-help.test.ts` are shell-owned test gates over other sittings' libs; rows cite the split.
  10. `/bom` route parses in the router (CODE.shell.102) but no `app/(studio)/bom` page exists — the route name is legacy; navigation to BOM uses /wizard/9.
