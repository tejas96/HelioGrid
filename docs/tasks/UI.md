# Component gaps — `@heliogrid/ui`

**A register, not tasks.** No requirement rows sit behind these, so they carry no `T-` id and are
exempt from `docs/tasks/README.md`'s task anatomy. Each was found by drawing a real screen against
the component and hitting the wall.

**Fix when you touch the component, then delete the row.** Fix **both halves** — this repo *and* the
design system screens are drawn against — or the two silently drift.

**Owed to the design system, repo side already fixed.** A `.jsx` there can only be replaced whole,
and retyping a large file to change two lines is how a silent corruption gets in — so these two
wait until something else opens the same file. `Sheet.jsx`: `aria-labelledby` must fall back to
`labelId` when there is no `title`, or an icon-header sheet has no accessible name (the repo half
and `Modal` on both sides are done). `FilterPanel.jsx`: forward `modal` to its `EditorSurface`, or
the side-panel form keeps a backdrop over the list it is filtering.

| Component | Gap | Found by |
|---|---|---|
| `OtpInput` · `Input` | Helper is fixed `--text-tertiary` 12px, which `N4` forbids for anything load-bearing — so every screen with a load-bearing sentence beside a field draws its own `--text-secondary` line outside the component and the spacing becomes a per-screen decision. | `SCR-M01-08` · `SCR-M01-09` |
| `NumberField` | No unset value. `value` defaults to `0`, so an unanswered field shows a figure nobody declared and no tier can qualify it (`N7`). **Waits for a caller:** the fix threads `number \| undefined` through the draft state, both boxes, both platforms and `onCommit`'s signature, and what a stepper does from empty is knowable only from a screen that uses one. | `SCR-M01-04` |
| `NumberField` · `OptionCardGroup` | Field `error` is described, not announced. The `Text` primitive now carries `live` (`PhoneField` uses it); these two still need it wired to their own error props. | `SCR-M01-04` |
| `Provenance` | Its line is fixed `--text-tertiary` — wrong where the tier IS the screen's honesty contract. Wants an `emphasis` prop. | `SCR-M01-04` |
| `Banner` | No `kind` for a signed-out steer — `suggestion`'s spark reads as AI. One action pill where two full-size routes are wanted. | `SCR-M01-02` |
| `Input` | No loading form. `StatCard`, `DataTable`, `Timeline`, `Sheet` and `DetailPanel` all take `state="loading"`; `Input` takes none and no `Shimmer` is exported, so each screen hand-draws a differently-shaped waiting field. | `SCR-M01-09` |
| `Avatar` | No nameless state. Its fallback derives initials from `name`, so an empty name renders an empty tinted circle — a rendering fault, not "no name yet". | `SCR-M01-09` |
| 7 field hosts | **Every field label is weight 500** — `Input`, `NumberField`, `Select`, `Textarea`, `DatePicker`, `TimeField`, `PhoneField` — and the type scale permits 500 for buttons, tabs and table headers only. Not one component's bug: either the rule is too narrow for a field label or seven components are wrong together, and changing one alone is drift. Needs a ruling. | `SCR-M01-09` |
| `Input` | No provenance slot. It has `attribution` (which layer supplied a value) and nothing for how far to trust it. `NumberField` has one. | `SCR-M01-05` |
| `DetailPanel` | **No `bodyStyle` slot, and the workaround is three mechanisms.** Pinning a block flush to the panel's bottom needs a wrapper that takes the container's bottom padding as its own and hands it back as negative margin PLUS `bottom: calc(-1 * var(--sp-6))` — because the sticky constraint rectangle is the scrollport's CONTENT box, not its padding box. A negative margin alone only moves the flow position; a negative sticky offset alone gets clamped. None of it is discoverable from the component's surface. | `SCR-M01-13` |
| `Provenance` | **A tier word cannot be localised.** The source clause is the caller's and translates; the tier WORD comes from the component's own registry, so a Marathi interface reads *Derived*. An honesty line the reader cannot read is not one — the registry needs the market pack's language. **Four screens have now hit it**; it is the one word `F8-07` exists to keep beside every figure. | `SCR-M01-12` · `-14` · `-15` |
| `FieldOverride` | **Its three words are the component's, so an overridden field renders English inside a translated one.** `Edited` (the marker), `was` (`autoLabel`, the one a caller can replace) and `Reset to ₹15,240` — the reset's accessible name included, which is the half `N8` shares with WCAG 2.5.3. Same shape as the `Provenance` row above and the same fix: the words belong to the caller or to the pack. | `SCR-M01-15` |
| `DataTable` | **The stacked card has no `marks` slot**, though `RecordCard`, `ListRow`, `AccordionItem`, `MenuItem`, `Tabs`, `SegmentedControl` and `OptionCardGroup` all take one. Chips reach the card only through a labelled `wrap` column, which spends a 12px label line on a set that names itself. Every records screen that stacks will re-derive it. | `SCR-M01-15` |
| `DataTable` · `ChipGroup` | **A `ChipGroup`'s `+N` control is unreachable on a clickable stacked card.** With `onRowClick` set the card's content wrapper is `pointerEvents: none` and only a live cell (`editable`/`override`/`attribution`/`cellIssue`) gets it back, so a `ChipGroup` in a plain column draws an overflow pill nothing can tap. Either the card grants `pointerEvents: auto` to a cell with an interactive descendant, or `ChipGroup` publishes that it has one. | `SCR-M01-15` |
| `DataTable` | **The stacked card's trailing slot cannot hold a `FieldOverride`** — marker, superseded value and a 44px reset is ~140px against a `flexShrink: 0` slot beside a flexible title, which squeezes the item name to two words (measured). A trailing column that dropped its override and attribution lines BELOW the title row would not need the second column the screen had to add. | `SCR-M01-15` |
| `DataTable` | **`sort` re-sorts the rows, so a caller with a domain-fixed order cannot render a sorted header.** Passing `sort` to draw the header state also reorders, and the table then disagrees with its own toolbar. Worked around with `sortable: true, sort: null`. What is missing is a way to say *this is the current order and it is not one of your columns*. | `SCR-M01-15` |
| `DataTable` | A `wrap` column is a stacked-card treatment with **no table-form meaning** — in the table the flag does nothing and `width` alone governs. The prop reads as a cross-form promise and keeps half of it. | `SCR-M01-15` |
| `Avatar` · `PhotoField` | **Initials are computed from the name, and Devanagari has no initials.** The first character of two words in `प्रिया देशमुख` is two conjunct clusters reading as a syllable, not a monogram. Wants a caller-supplied monogram or a per-script rule. | `SCR-M01-12` |
| `DataTable` | Its loading state **drops the header row**. `Person · Roles · Status · Last active` are words the app knows before any record arrives, and the one thing a table can say that a card cannot. `TableSkeleton` takes only `stacked`, so it cannot draw them. | `SCR-M01-12` |
| `Chip` | **A docs fix, not an elevation one.** `Chip.css` is already right and argued — a static label is `--e1` and a pressable chip `--e2`, because raising both would make every decorative chip read as pressable (`Q77`). What is wrong is that the docs name `Chip` first for a label a record HAS, where the tinted `Badge` is the instrument that reads on a white `RecordCard`. | `SCR-M01-12` |
| `RecordCard` | `meta` is mono + tabular-nums, and one legitimate entry on it is not a number — `NamedGap` sets its own face there, which works and is undocumented. | `SCR-M01-12` |
| — | **Nothing says "you will not see this".** `ScopeNote` covers an act somebody else holds and `ComplianceFloor` an act nobody holds; a VISIBILITY limit — *your list is yours, the pipeline is the owner's view* — has no component, and `UnavailableNote` speaks only for the surface it is on. Hand-drawn, which is how two screens explain one scope two ways. | `SCR-M01-10` |
| `ScopeNote` | Documented as belonging to an action row, but its sentence is exactly right for a card that DESCRIBES permissions. Either the doctrine widens or an explainer variant exists. | `SCR-M01-10` |
| `AppRail` · `BottomNav` | Inactive destination labels are `--text-tertiary`, and the 1536 rail carries its words only in `aria-label`. A destination label is load-bearing (`N4`), and `F7-31` parity makes "visible at 375, invisible at 1536" a question. The redundant `title` tooltips are removed; the tertiary and the missing visible word are a design call. | `SCR-M01-10` |
| 5 elements | **`title` carries meaning nothing else does** — `AllocationMeter` span, `Breadcrumb`'s collapsed crumbs, `BarChart`'s bar value, `Stepper`'s dot name, `UsageMeter`'s threshold. A tooltip is hover-only (`N1`) and is not reliably announced either. Each needs a visible or named replacement. | sweep 2026-08-31 |
| 10 classes | **`--text-tertiary` on the thing itself, not on a caption beside it** — `BandedFigure.label`, `Derivation.part-label`, `DocumentPreview.slot-label`, `FilterBar.sort-pills-label`, `JobTray.panel-label`, `OperationProgress.stage`, `PagedDocument.annotation-label`, `Stepper.rail-heading`, `Timeline.label`. Found by sweeping all 95; each needs a design call on whether it is quiet furniture or information (`N4`). | sweep 2026-08-31 |
| — | **No pressable card carrying a title and a wrapping sentence — three screens have now hand-rolled one.** `ListRow` puts `nowrap` + ellipsis on both its lines, is 64px, and rests at `--e1`, so a raised two-line control is impossible; `OptionCardGroup`'s `content` is static by contract (the card IS the radio); `Card.interactive` is a hover lift with no press semantics and no accessible name; and `ListRow.onClick` sits on a plain container with no `role`, no focus stop and no `selected`. Every hand-roll re-derives the elevation, the target and the accessible name — and `SCR-M01-16`'s got the name wrong (WCAG 2.5.3). | `SCR-M01-05` · `-10` · `-15` · `-16` |
| `Select` | **The one field host with no `attribution` and no `provenance` slot.** `Input`, `NumberField`, `Textarea`, `ListRow`, `DateSet` and `DataTable` columns all take them. A pre-scoped kind (*inherited — from the picker you opened this from*) and an extracted choice (*read off page 2*) both have to go in `helper`, which is advice, not provenance. | `SCR-M01-16` |
| `EditorSurface` | **The sheet-or-panel switch latches a mount-time measurement.** `useLayerWidth` reads the probe once and keeps it; where the containing block's width resolves after first paint the wrong form is chosen and never corrects — a sheet inside a 1536 frame. Any editor in a lazily-laid-out region can hit it. The probe's `ResizeObserver` watches a node whose own box may not change while its containing block does. | `SCR-M01-16` |
| `EditorSurface` | **Nothing publishes how much room a persistent panel leaves its caller.** A non-modal panel makes its caller a narrower region and the caller must know the number; `useEditorForm` publishes `layerWidth` and `panel` but not the panel's width, so every screen with an editor beside a list hand-computes the same subtraction — and any of them can get it wrong in the direction that hides a column. | `SCR-M01-16` |
| `ReadAlongside` · `SourceDocument` | **The stacked form names a height and the document fits to width, so the two disagree.** A 595×842 page fitted to width at 343px is 485px tall — the reference alone fills a phone editor body and pushes every field, and every tier, below the fold. The stacked form should default to `fit="page"` or clamp the page to `stackedHeight`. | `SCR-M01-16` |
| `Banner` | **No kind names a FAILED ACT** — an extraction that could not read, an import that broke, a save that came back. Twelve kinds cover validation, lifecycle, review, dunning, caps, data integrity, bundles, overrides, below-cost, preliminary, suggestion and the superseded disclaimer, and none of them is *the thing you asked for did not happen*. `review-needed` is content waiting for a person; `tone` overrides are documented as rarely right. Two screens, two different kinds picked for one fact. | `SCR-M01-16` · `-20` |
| `Input` · `NumberField` · `Select` | **A field's min-content width is undocumented and silently overflows a grid.** `1fr` is `minmax(auto, 1fr)` and the fields' own min-content is 222px, so four in a `repeat(4, 1fr)` demand 888px inside an 852px track and the last column is clipped. The caller cannot know the number, and the failure is invisible until something is cut. Publish it, or make the fields safe at `1fr`. | `SCR-M01-16` |
| `SourceDocument` · `Dropzone` | **Component strings with no prop** — `Expand document`, the page words, and `Dropzone`'s generated upload copy render English inside a translated surface. Same class as the `FieldOverride` row above; visible precisely because every string that HAS a prop was passed through. | `SCR-M01-16` |
| `ReorderList` | **No declared slot for a row-level act.** A phase row that can be reordered often also needs deleting or opening, and the component publishes nowhere to put it — so every authored-order screen composes its own row and re-derives the arrow geometry beside it. | `SCR-M01-19` |
| `PagedDocument` | **A sheet's prescribed bands do not fit the sheet, and the overflow prints over the footer.** Content is not measured against the remaining box, so a caller cannot know it has overrun until it looks at the printed page. | `SCR-M01-19` |
| `PreviewFrame` | **The readable floor is derived from the subject's body copy**, so the subject's smaller roles — captions, table type, the tier line — fall under it while the frame reports itself legible. The floor has to come from the smallest role on the page, not the largest. | `SCR-M01-19` |
| `Sheet` · `DetailPanel` | **One sheet grammar, two typographic voices for the same string.** A title set by the sheet and the same title set by the panel do not match, so an editor that changes form by width changes voice with it. | `SCR-M01-19` |
| `DocumentPreview` | **Two `M01-51` values feed a customer document and neither can reach the preview** — the cover's covering note, and the bank block that prints under *Payment and bank details*. The requirement that asks for the preview is the same one that names them. A `coverNote` slot and a section-body slot close it. | `SCR-M01-19` |
| `RichText` | **A set of English strings no caller can reach** — the count's noun in `${chars}/${maxLength} characters`, the toolbar's `Add logo` / `Logo on`, the select's `Load template…`, the link field's `Link address`, and the accessible name of every toolbar button (`Bold`, `Italic`, `Heading`, `Bulleted list`, `Numbered list`, `Link`). Plus two English DEFAULTS, which the copy rule forbids outright: `placeholder` and `saveTemplateLabel`. The accessible names are the half a Hindi screen-reader user meets. | `SCR-M01-19` |
| `RichText` | **The toolbar's height is decided by tenant data.** Its template `select` sizes to its widest `option`, so three names a tenant typed set how many rows the toolbar wraps to inside a 311px field — measured 148px against 102px, a 46px swing that reads as a language difference and is not one. A truncating trigger, or `Menu` instead of a native select, makes the chrome's height the component's own again. | `SCR-M01-19` |
| `AllocationMeter` | **Refuses a provenance slot, and `N7` admits no exception.** Its boundary against the billing meter rests partly on *"an allocation is the user's own arithmetic in progress"* — but the REMAINDER is a computed figure a reader acts on, and `F8-01` covers every number a user can read. | `SCR-M01-20` |
| `AllocationMeter` | **It composes its own sentences and one cannot be reached at all.** `label`, `targetLabel` and the two state sentences are the caller's *if the caller knows to pass them*; without `remainderWords` / `enforcementNote` the component generates English. One string has no prop. | `SCR-M01-20` |
| `Select` · `Menu` | **No picker can be specified open**, so the moment a value is BOUND cannot be drawn — and a static spec board is exactly where that moment has to be shown. `Select` takes no `open` / `defaultOpen`; `Menu` keeps its open state internal. | `SCR-M01-20` |
| `DocumentTranche` | **`amount` is required, so a percentages-only schedule cannot be expressed** — and its money carries no tier slot. A settings screen with no quote to price against must either invent a basis or push a string into a money column. | `SCR-M01-20` |
| `DocumentTranche` | **The share is a pre-worded string, so the market pack does not own the percent.** Amounts in the same table are numbers the active pack formats; the share beside them is whatever the caller typed — so `33.33%` here and `33,33 %` in a market that writes it that way become the caller's problem. | `SCR-M01-20` |
| `PreviewFrame` | **Three defects in one block, none visible in the markup.** Its probe reads its own width through a component mount and gets it wrong; it is blind to a subject that scales itself; and it reserves an A4 footprint for a subject that is not A4. | `SCR-M01-20` |
| `Chip` | **`tone` colours only the dot**, so a warning chip is a warning *mark* on a white pill — measured: `--surface` at `--e1`, near-black words, one 6px `--warning-text` dot, while `--warning-bg` exists as a token and goes unused. `F7-11` asks for the word first and the tint second. | `SCR-M01-20` |
| `PagedDocument` | **A tranche band names itself in a `<p>`, so its table is anonymous to a screen reader.** Measured on all eight bands: a `<table>` with no `<caption>`, no `aria-label` and no `role`, its name in a paragraph immediately above. `F7-27` asks a customer-facing table to carry its own name. | `SCR-M01-20` |
| `Modal` · `Sheet` | **`labelId` is web-only in practice.** Both take it on the shared type and only the web half reads it; the native halves carry `accessibilityViewIsModal` and no name, so a native icon-header dialog is still unnamed — the defect the prop was added to close. An unread optional prop typechecks, so Law 7 does NOT catch this. Route it through `accessibilityLabel`, or state the platform reason on the type. | `arch review 2026-09-02` |
| `StatCard` · `Timeline` | **Their retry controls were raised to `--e2` on web and left flat on native.** `Q77` applies across both platforms and the overlay family did both halves; these two did not. | `arch review 2026-09-02` |
| `PhoneField` | **The native half composes its accessible name in code** — `` `${label}, ${phone(value)}` `` — while the web half names the input from a `<label>` and puts the number in a separate sr-only span. Two announcements from one component, no platform reason recorded, and the join runs against `Q80`: a composed sentence is a whole translated sentence with slots, never a concatenation. | `arch review 2026-09-02` |
| `PhoneField` · `PhotoField` | **No design-system contract mirror**, so both sit outside `ds:contract` — the only gate comparing repo props against the design system. `ds:contract` skips a component with no contract file, silently. | `arch review 2026-09-02` |
| — | No `Skeleton` component, and no duration token for the stated 1.4 s shimmer (nearest is `--dur-ambient`, 500 ms). | `SCR-M01-03` |

**Has an owner, so not listed above:** `F3-13`'s React Native half — components read
`theme.type.families.sans` and RN has no per-codepoint fallback, so Devanagari falls to the OS face.
`T-FPLAT-007` owns it with `F3-17`'s per-script line height. The web half is fixed.
