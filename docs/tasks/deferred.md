# Deferred — found during a task, outside its scope

One file. A row here is the NEXT task, never a parked fix (`CLAUDE.md` §8). Each row: the issue ·
why not now · what it depends on · what must happen · who picks it up. The row goes when it ships.

| issue | why not now | depends on | what must happen | picks it up |
|---|---|---|---|---|
| Two notification jobs have nothing to run them. (1) A push HELD by quiet hours never leaves: `T-FPLAT-018` sets `push_due_at` and `T-FPLAT-067` sends only what is due AT EMIT, so a row that falls due later keeps `push_sent_at` null. (2) The centre's horizon purge: `T-FPLAT-019` bounds every centre READ to its horizon and deletes nothing, so rows past it stay in the table, unseen. Nothing is lost either way — the inbox is the truth (`F6-06`) and every fact lives on its record's timeline (`F6-19`). | the owner ruled both LOW PRIORITY. Where they run is ruled: each is a Temporal Schedule in `apps/worker` (`architecture.md` apps/worker; `02-system-architecture.md` schedule table). The worker has no database pool yet, and the push sender lives inside `apps/api`'s notification module. | the worker's first database pool, built by the first task that lands a Temporal Schedule — at the latest `T-M02-015`'s dormant sweep (`M02-52`) · the `PushDelivery` sender reachable from the worker · a DELETE grant on `notification` for the purge alone | one ticket, two schedules: `notification-push-drain` claims rows whose `push_due_at` has passed and whose `push_sent_at` is null with `FOR UPDATE SKIP LOCKED`, sends them through the SAME `PushDelivery` port and marks them; `notification-horizon-purge` deletes rows older than the horizon — proven by a row held at night arriving after the window ends, by two drains at once sending it once, and by a row past the horizon gone while a row inside it stays | the next task after the worker's database pool lands |
| `MS_PER_DAY` is declared privately three times in `packages/domain/src/auth/` — `invitation-policy.ts`, `otp-policy.ts`, `session-policy.ts` — beside the one `format/zone.ts` now exports. Three correct copies of one fact still diverge (`CLAUDE.md` §8). | found at `T-FPLAT-019`, which took the exported one; changing `auth/` in a notification task hides an unrelated edit in its diff. | nothing | the three files import `MS_PER_DAY` from `../format/zone` and drop their own; the auth tests stay green | the next `packages/domain/src/auth/` task |
| `packages/theme/src/_generated/tokens/{colors,elevation,field-mode}.css` carry six comments naming a retired open-question id with a date; they are pulled from the design system and never hand-edited | the text lives in the design system's token descriptions, not in this repo | a Claude Design session editing those descriptions | edit the six descriptions to state the raised-control rule without the id, then `ds:pull`; the generated files then carry no id | the next `ds:pull` |
| The datasheet-PDF extraction (`M01-40`'s build half) has no engine task; T-M01-016 names it and a screen may not own it | found while splitting M01 | `T-FPLAT-035` and the worker's outbox | one engine ticket: an extraction job over the stored file returning typed fields with the page read, on the worker | the `/start` of `T-M01-016` |
| `F2-24` asks that platform-staff access to a tenant's data be **read-only**, and no task in the suite builds a back-office surface for staff to read a tenant through, so the enforcement half has nothing to enforce on | found at `T-FPLAT-004`'s `/start`; that task lands the RECORD the row needs — `actor_kind: platform_staff` on an entry with no foreign key — and a surface no requirement row describes cannot be invented inside it (`CLAUDE.md` §4) | `T-FPLAT-004` (the table, the writer and the export) | one ticket for the internal read path: how staff reach a tenant, that every such read is read-only by construction rather than by discipline, and the entry each read writes — then `F2-24`'s second proof lands there | the owner, who decides whether V1 ships a back-office surface at all |
| `packages/i18n`'s root entry is React-free BY DESIGN — `createTranslator(locale)` "for a server render or a job" — and `T-FPLAT-005` assigns the notification and document engines (`T-FPLAT-017`, M06, F5) to call it at emit time on the server; yet `architecture.md` §2 lists i18n among the frontend layers `apps/api` and `apps/worker` may never import, and the boundary gate holds that line. The two records contradict, and today's server-side text (the sign-in code, the invite) is routed around the gap into the market pack — which is right for carrier-registered SMS templates (`F1-38`) and would be wrong for anything else a server renders | found at `T-M01-028`'s review, when the owner asked why an SMS text lives in the pack and not the catalog; no server consumer of the catalog exists yet, so nothing is broken today | `T-FPLAT-017` (the first engine that renders a title and body in a reader's language on the server) | at that task's `/start`, one ruling: either the boundary admits the React-free root of `@heliogrid/i18n` (never `./react`, never `./rn`) into `apps/api` and `apps/worker` and §2 says so, or the reader-language seam moves; and the rule is written in one place — the pack keeps only what a market REGISTERS, the catalog keeps every word a person reads on a screen or in a notification | the `/start` of `T-FPLAT-017` |
| A handler can THROW a status its own contract never declares. Response validation then turns it into an opaque `INTERNAL`, so the wire says 500 where the code meant 404 — the rule is written in `apps/api/CLAUDE.md` and `.claude/landmines.md` and NOTHING enforces it | found at `T-FPLAT-004`'s `/ship` review: the audit-log route threw `NotFoundException` for a state its guard already refuses, on a route declaring only 401 and 403. Caught by reading, which is how the rule has been held so far | nothing | an owner ruling picks the mechanism, then it lands with its `mechanisms.md` row: (a) a lint rule refusing `throw new *Exception` inside a `tsRestHandler` block, so a handler answers only through its declared statuses; or (b) the session helpers become the ONLY way to reach a membership or a tenant, each throwing a plain `Error` for a state the guard already excludes, so no handler has a status to invent — `tenantIdOf` is the first of them | the owner, then the next `apps/api` task |
| The arc centre's verb for eight homes and the standing slots for nine presets are `null` in `packages/domain/src/shell/` — no PRD row names an act or a list for them, and every named act is DENIED to them | a cell no row names would be an invention; ruled at `T-FPLAT-025`'s `/start` | each home landing | the home's task names its cell in the two tables, with the row that rules it: Survey Engineer slots `T-M04-006` · Design Engineer `T-MS-375` · Project Manager `T-M08-001` · Field Technician `T-M09-002` · Installation Team Member `T-M08-005` · HR/Admin `T-M10-001` · Finance `T-M11-001` · Operations `T-M13-003` · Marketing `T-M03-001`; the law tests in `packages/domain/tests/shell/` refuse a verb the preset is denied and a slot it cannot see | the named home tasks |
| A deep link to a signed-in route while signed out lands on the home after sign-in, not where the person was going: the door sends everyone to `landingOf(user)` and carries no return-to | found reviewing the route-group gate (T-M01-033); whether a return-to is wanted is a product choice no PRD row makes | an owner ruling | the ruling in the M01 PRD, then a `next` carried from the gate to the door on both platforms | the owner, then the next door task |
| The switch case (`PendingSwitch`: this device had someone else signed in) needs the door open while signed in, and both gates send a settled signed-in person off `/login` — so the switch frame is unreachable from either navigator today | found reviewing the route-group gate (T-M01-033); how a signed-in person reaches the door is the shell's sign-out/switch entry | `T-SHELL-001`'s ruling on the switch entry | the door accepts a signed-in visitor with an explicit switch intent, on both platforms, proven by a switch drive | `T-SHELL-001` |
| `packages/ui` declares 22 exported numeric constants and 6 SCREAMING_CASE lookups (`TEXT_FLOOR = 4.5`, `MIN_TOUCH_TARGET = 44`, `TAB_COUNT_MAX = 99`, `DOCUMENT_DESIGN_WIDTH = 480`, `A4_RATIO`, `DRAG_DISMISS = 96`, the AppRail and page-geometry sizes, …) — some are policy (`domain`), most are visual values (`theme`), and check 10 does not scan packages, so none is held | each needs a ruling — a floor is product law, a geometry default may be a token the design system has not emitted; widening check 10 as written fires 155 times on `ui`'s own prop unions | the theme tokens for geometry (`ds:pull`), and an owner ruling per policy number | move each policy number to `domain`, each visual value to `theme`, then widen check 10's number pattern to `packages` | the next `ds:pull`, and `docs/tasks/UI.md` for the per-component ones |
| The phone's code step drops the language control its number step and the web's code step keep: the frame's trailing slot holds only "Change number" (`apps/mobile/src/screens/shared/CodeStep.tsx`), so a person mid-code cannot switch language on the phone; the design record calls the control a designer addition on the front-door frames and says nothing about the code frame | found by qa-parity at PR B; the code step's affordances are `T-M01-001`'s and `T-M01-002`'s, not the door-kit switch | a reading of the export's `m-code-*` frames: whether the 375 header has room for both ghosts | either the brief records the omission as the phone's ruling, or the phone's code step renders `LanguageControl` beside "Change number" as the web does — proven by qa-parity on the next door run | the next door task (`T-M01-035`) |
| Ten door parts still exist once per platform — `PhoneStep`, `CodeStep`, `LanguageControl`, `AccountCard`, `CompanyFacts`, `CompanyFields`, `CompanyStep`, `KnownNumber`, `SignupProgress`, and `SwitchPanel`/`SwitchSheet` — each drawn the same on both platforms from the same hooks and words | found by the architecture review (its H2); the frame pieces were lifted first (#63, #64) and these need prop contracts before they can move, because `packages/ui` may not import `data` or `i18n` (`.claude/rules/screen-parts.md`) | nothing | each part takes its facts as props — the hook's state and the words — and lands in `packages/ui` with one `<Name>.types.ts`; the switch keeps its platform adapter (a panel on the web, a sheet on the phone) around one shared body; `qa-parity` then reads one import list | the next door task (`T-M01-035`), one part per PR when the ceiling asks |
| No gate reads the colour pairings `packages/ui` actually draws: the contrast gate measures the pairs declared in `packages/theme/src/contrast.ts`, so a component that sets one token on another nobody declared is unmeasured, and a re-pulled colour can drop that pairing under its floor silently (`F7-11`, `N4`) | found by the architecture review (its H8); the maths got its one owner first, and the scan is a new gate, which needs an owner ruling (`CLAUDE.md` §8) | the design system's CSS being the one source of the pairs a component draws | a build step reads every `var(--x)` set as a colour on a `var(--y)` background across `packages/ui/**/*.css` and the native halves' style objects, reports each pairing that no declared pair covers, and FAILS on one under its role's floor — proven red by drawing a component in `--text-tertiary` on `--canvas-sunken` (≈4.48) | a gate task off `T-FPLAT-022`, when the owner asks for the script |
| The door's `968px` breakpoint and its `--door-measure` widths are invented in `packages/ui/src/components/DoorFrame/DoorFrame.css`, and `DateSet`'s `BREAK = 640` is copied into both of its halves, because `packages/theme` carries no breakpoint or measure token at all | found by the architecture review (its `L2` and `M3`); this half needs a design-system WRITE, which is the owner's call, and `packages/theme` is generated and never hand-edited. The port half of this row shipped at `T-FPLAT-046` | an owner ruling on adding a breakpoint scale to the design system, then a pull | the breakpoint and the two measures become theme tokens the door's stylesheet and `DateSet` both read, so a second screen at the same width cannot invent a different number | the next design-system pull, or the screen that needs the second breakpoint |
| The audit log is read by OFFSET like every other list, and it is the one list that only grows — page 500 counts past 12,500 rows to return 25 (`packages/contracts/src/common.ts`'s pagination convention, `apps/api/src/modules/audit/audit.repository.ts`) | found by the architecture review (its `M18`); the owner ruled at that convention that offset beats cursor because counts, jump-to-page and column sorting are product needs, and that a route goes cursor-based ONLY with a ruling — which was given again here: build it with the screen, not before it, because a cursor cannot serve a count or a jump-to-page and nobody yet knows whether the screen needs either | the audit screen's brief saying whether it pages or scrolls, and whether it shows a total | the brief decides; if it scrolls with no count, the pagination schema gains an optional cursor and that one repository a keyset branch, indexes and tiebreakers already being keyset-ready — otherwise offset stands and this row goes | the audit screen's own task |
| No gate checks that a mechanism row an instruction cites EXISTS: `/ship` cited `M127` before the row was written and every gate passed. A gate cannot simply be added, because the ids collide — `M10`, `M11`, `M12` and `M13` are both mechanism rows and modules (`M11` is the tenant transaction AND payments & collections), so a check could not tell a module citation from a row citation | found at `T-FPLAT-056`, whose own citation dangled for one edit; the only dangling mechanism citation in the repo that day was that one, and eleven apparent others were module ids | an owner ruling on telling the two apart — a distinct form for mechanism citations, or for module ones | the ruling, then a gate beside the dangling-task-id check that refuses a cited mechanism row not defined in `.claude/mechanisms.md`, proven red on an invented id | the owner, then the next task that touches `scripts/gates.py` |
| A live PRD row cites a requirement that no longer exists: `M02-38` (`docs/prd/modules/M02-crm-and-leads.md`) says its honesty obligations are consumed from `F8-23` and from a second `F8` row that has since been DELETED — the one `T-FPLAT-028` ruled is cited as `F8-12` from now on. Every other place that ruling listed was repointed; this one was not. The deleted-row check reads `docs/tasks/` and the briefs, never one PRD document citing another, so every gate passes while it stands — and it refused this very row until the deleted id was taken out of it | found at `T-FPLAT-056`'s close-out, while confirming block 0's order. `M02-38` is a LAW row with no task of its own — enforced through `M13`'s forecast surfaces — so it first matters in block 6 and nothing is blocked today; and editing the PRD inside a closing audit would hide a spec edit in an unrelated diff | nothing — the repointing is already ruled at `T-FPLAT-028` | `M02-38` cites `F8-12` in place of the deleted row, and the deleted-row check reads PRD module documents too — proven red on `M02-38`'s citation as it stood | the `/start` of `T-M13-007`, which owns the weighted-pipeline reading `M02-38` is enforced through |
| A route with a `:param` in its path need not declare that param's SHAPE, so the raw string reaches the query: only 6 of the 40 routes declare `pathParams`, and `POST /notifications/:id/read` answered **500** on `not-a-uuid` because the text went to the database, which rejected it — the wire body stayed clean but the server logged the SQL and a stack trace | found by `qa-api` at `T-FPLAT-017`, NOT by any gate: typecheck, lint, boundaries, 31 doc gates, the invariants, 1131 unit tests and `pnpm verify:clean` were all green on that 500. The one route was fixed in the task that found it; widening the rule to the other 34 is a repo-wide edit that would hide in a feature diff | nothing — it reads the contract files alone | a gate over `packages/contracts/src/*.ts`: every `:segment` in a route's `path` has a matching key in its `pathParams` object, proven red on a route whose param is undeclared. It is a SCRIPT, so `CLAUDE.md` §8's mechanism order needs the owner's ruling first — no type or lint rule can read a path string against a sibling property | the owner, then the next `packages/contracts` task |
| No route declares the statuses the shared filter actually produces: **zero of 40** declare `400`, `403` or `500`, yet `400 VALIDATION_FAILED` is returned for a bad query or path param, `403` by the guard when a signed-in person holds no membership, and `500 INTERNAL` for anything thrown. The committed OpenAPI therefore understates every route's real surface, and a generated client sees fewer outcomes than exist | found at `T-FPLAT-017`'s review, asking what the inbox route can answer. It is the house convention, not one route's defect, and declaring three statuses on 40 routes would restate in 40 places what the filter guarantees in one — the duplication `CLAUDE.md` §8 calls a defect — so which way it goes is a ruling, not a fix | the ruling; it neighbours the undeclared-throw row above, which is the same seam from the throw side | an owner ruling: either the spec is emitted with the filter's three statuses added to every route from ONE declaration, or `M27` records in words that a route's declared union is its route-SPECIFIC codes and never the filter's — and `/ship`'s review reads it that way | the owner, then the next `packages/contracts` task |
| `T-FPLAT-008` reads `planned` while the work it names — the four format implementations, `packages/domain/src/format/{money,number,datetime,measurement}.ts` with their tests — landed in #13 before task ids existed, so no commit subject names the task and `M106` cannot see the reverse of a false `shipped` (its own recorded blind spot); `T-FPLAT-005` already calls it "landed" | found at `T-FPLAT-006`'s `/start`, whose `F3-08` proofs are those tests; flipping a row to shipped needs its six done-when lines checked against the tree, which is that task's work and not this one's | nothing | the next session reads `T-FPLAT-008`'s six lines against `packages/domain/src/format/` and its tests: each met → flip to `shipped (#13)` in a commit whose subject names the task; any unmet → build it | the next task that touches `packages/domain/src/format/` |
| `Banner` in `packages/ui` lacks the two props the design system gave it — `actionBelow` (below this much of the banner's OWN width the action drops under the body, 44px target kept) and `onFormChange`. So `adherence.oxlintrc.json` and `contracts/feedback/Banner.d.ts.txt` are held back from the mirror (`T-FPLAT-068`): pulled, `ds:contract` fails `(b) DROPPED` on both props | a component port is proven by RUNNING both platforms (`.claude/rules/testing.md`), which is a build task with `/verify`, not a mirror pull | nothing | port both props on web and native, pull the two held-back files in the same change, and see `ds:contract` green | its own `T-FPLAT` task, before `T-SHELL-006` builds the billing strip, whose act stacks under the rows at 375 |
| `FactRow` / `FactRows` / `FactInk` exist in the design system and in the mirror (`T-FPLAT-068`) and nowhere in `packages/ui`: the one form for a label and its value — a pair on one line while every value fits, measured on the group's own box, else one flowing line; stacked groups cannot yet share a label column | new component on two platforms — the web half leans on subgrid and a fit measurement, the native half has neither and needs its own answer | nothing | build it with one `FactRow.types.ts` both halves implement (Law 7); settle how stacked groups share one value edge (context file §3) | its own `T-FPLAT` task, before the first billing screen builds (`T-M12-001`…`-004`) |
| `BM-41` gives the trial three caps — 25 detections · 15 voice minutes · 5 GB — and names none for proposals, while `M12-52` says the trial runs "within the trial caps". `SCR-M12-04`'s trialing frame draws the proposal allowance as a named gap, `Not in the price list`, rather than invent one | a number no PRD row carries is the owner's (`CLAUDE.md` §4) | the owner's ruling: does a trial cap proposals, and at what number? | write the number into `BM-41` (or say the trial has no proposal cap), then the usage screen shows it and the named gap goes | the owner, before `T-M12-004` builds the usage screen |
| `UsageMeter` in `packages/ui` types its own English words: the connector ` of ` (`UsageMeterMetered.tsx:58`, `UsageMeterMetered.native.tsx:65` and `:73`) and every status sentence in `usage-meter-model.ts` — `Cap reached. … before new creations pause.` (`:83`), `Paused — the cap was reached and the grace period has ended.` (`:102`), `… beyond the bundle — billed as overage this period.` (`:110`), `…% of the bundle used.` (`:120`), `Paused — new creations are blocked on this plan.` (`:131`). A Hindi or Marathi reader gets English inside the meter, user-visible copy sits outside its owner (`CLAUDE.md` §8), and two of the sentences speak the product's words — bundle, overage — which a frame never does (`F7-42`). The design system's copy has the same gap: `SCR-M12-04`'s two language frames had to switch the meter's sentence off, which also cost the paused bar its grey | found while checking `SCR-M12-04`'s language proof; no screen draws the meter yet, and a component change is proven by RUNNING both platforms (`.claude/rules/testing.md`), which is a build task with `/verify` | nothing | the meter takes the connector and each state's sentence from its caller, which reads them from `packages/i18n`, in the person's words, under the one `UsageMeter.types.ts` both halves implement (Law 7); then the design system's `UsageMeter` gains the same props in its next change | the `/start` of `T-M12-004`, the first screen that draws the meter |
| The code's stage labels are not M02-41's: `packages/ui/src/components/StatusChip/StatusChip.registry.ts:39-46` prints *New lead*, *Site visit*, *Designing*, *Proposal sent*, where the funnel is new → contacted → qualified → survey → design → proposal → negotiating → won. The design system's registry was relabelled to *New*, *Survey*, *Design*, *Proposal* (owner ruling at `SCR-M02-03`'s review), so every drawn board now shows M02-41's words and the built screens would not | found at `SCR-M02-03`'s design review; a component change is proven by RUNNING both platforms (`.claude/rules/testing.md`), which is a build task with `/verify`, not a design-record commit | nothing | relabel the four entries on both halves under the one registry, decide whether the keys follow (`new-lead` → `new`, `site-visit` → `survey`, `designing` → `design`, `proposal-sent` → `proposal`) and move every caller with them, and add the Hindi and Marathi stage words to `packages/i18n` (the board's proposal: नई · संपर्क हुआ · योग्य · सर्वे · डिज़ाइन · प्रस्ताव · मोलभाव · जीती; नवीन · संपर्क झाला · पात्र · सर्वे · डिझाइन · प्रस्ताव · वाटाघाटी · जिंकली — for a native reader to confirm) | the `/start` of the first M02 screen task to build (`T-M02-001`) |
| Per-language CONTENT on the wire still refuses a language it does not know: `perLanguage` in `packages/contracts/src/document-content.ts` is `z.record(uiLanguageSchema, …)`, so a phone built before a language was added refuses a whole template or settings response that carries that language's version — and one request/response shape means an older build that reads, edits and saves a template would drop the versions it cannot see | found at `T-FPLAT-009`, whose owner ruling (1A) opened the language VALUE fields; the content maps are the M01 settings and document-template contracts, one shape used by both reads and writes, and splitting it is that slice's change | nothing | a response variant that keeps a version in a language it does not know and passes it back untouched on save, with the request half staying closed; proven by a response naming a new language that an older build reads and saves without loss | the next task that touches `document-content.ts`, and before the first store build |
| The invite SMS is sent INSIDE the create transaction (`apps/api/src/modules/invitation/invitation.service.ts`, the `create` callback): when the commit fails after the carrier accepted the SMS, the person holds a link whose token hash was never stored, and a retry sends a second SMS with a new link | found at `/start` of `T-FPLAT-011`, which makes a retried create safe but cannot make an outside call part of a transaction | the worker's outbox (`docs/engineering/forward-compat.md`, orchestration handoff) | the SMS leaves from the outbox after the commit, keyed by the invitation id, so it is sent once and only for a stored invite | the task that lands the outbox's first product event |
| An ACCEPT sent twice answers the second time as a conflict: the join ran, the first answer was lost, and the retry finds the invite no longer pending (`apps/api/src/modules/invitation/invitation.admin.repository.ts`, `accept`) — the person is on the team but their app shows an error | found at `/start` of `T-FPLAT-011`, whose rule is that an act repeated on a record already in its end state answers success; accept also opens a session under the new company, so its repeat must adopt that session again, which is the auth module's flow, not a one-line state check | nothing — the rows exist | an accept by the SAME person of an invite they already accepted answers as the first did: the membership, adopted into the session; an accept of an invite accepted by another number stays refused | the next `M01` onboarding task that touches the join, or a `T-FPLAT` follow-up |
| A PUT or a role write sent twice writes a SECOND audit entry for a change that changed nothing: `assignRoles` with the set already held records `team.roles_changed` from == to and moves the authorization version again, and every settings save (`settings.tranche_template_changed` and its siblings) records each send | found at `/start` of `T-FPLAT-011`, which covers the creates and the state changes a retry answered with an error; a write that replaces a whole value applies twice harmlessly, and only its record repeats | nothing | a replace whose new value equals the stored one writes nothing and records nothing, decided once for every settings save and for the role write | a `T-FPLAT` follow-up after `T-FPLAT-011` |
| The signup's failure banner states "Nothing was created, so trying again cannot make two companies" (`packages/i18n/src/copy/company-signup.ts`, `nothingCreated`). When the answer was LOST rather than refused, the company WAS created — `/verify` of `T-FPLAT-011` saw the banner over a committed company (W1). The second half is now true because the retry carries its key; the first half can be false, and a provenance-honest screen does not assert what it cannot know | found by `/verify` of `T-FPLAT-011`; the words are a drawn screen's (`SCR-M01`, company step), so they change in its design and brief, not in an engine task | nothing | the banner says only what is known — trying again is safe and cannot make a second company — without claiming nothing was made; the `hi` and `mr` catalogs follow | the owner, in the company step's design; then the M01 task that owns the screen |
| `pnpm test:unit` fails at random on a machine with many cores: vitest runs one worker per core with no `maxWorkers` in `vitest.config.mts`, every api HTTP suite boots the app, whose pools default to `max: 10` (`packages/db/src/client.ts:10`), and together they pass Postgres' 100 connections — the suites then fail on `remaining connection slots are reserved`, as 500s and 5-second timeouts (18 failures in one run, 6 in the next, 0 with `--maxWorkers=2`, all 1,334 tests and the coverage bars green) | found by `check:all` in the harness slice that fixes `break-and-run.sh`, which touches no test or test config | nothing | cap what the suites can open together — `maxWorkers` for the api project in `vitest.config.mts`, or a small `max` for the pools a test boots — and prove it by running `pnpm test:unit` three times with no connection error | a `T-FPLAT` follow-up |
| Four placement facts are held only by review: the contract comes before the code and no English in a UI component (both review-only, with no row), a query only in `db` and a brand never cast outside its owner (`M59`, `M60`, partial) — and no row at all stops LOGIC being written inside `packages/contracts`. `case-reviewer` and `break-it-reviewer` check them by hand from each ticket's Placement table | found planning `chore/harness-subtract`; a guard for each is its own design, and that PR only moves reviewers | nothing | for each, the cheapest mechanism that decides it completely — a type, a dependency-cruiser or Biome rule, or an invariant — proven red, and its row raised; a fact no machine can decide stays review-only and says so | the next harness batch |
| On Android 7–8.1 (API 24–27) a Medium or SemiBold weight draws the Regular Geist face: React Native below API 28 picks a registered family's face by style only (`ReactFontManager.kt`, `Typeface.create(typeface, nearestStyle)`), so 500 and 600 fall to normal; bold still draws Bold. Found by `T-FPLAT-023`'s PR review | the fix names a family per weight (`Geist-Medium` …) in the theme and every native `fontWeight` in `packages/ui` (129 files) — out of proportion to that task | nothing | the theme emits a per-weight native family on Android below API 28, `Text.native` and its siblings pick it, `check:languages` checks it, and an API 26 emulator shows a SemiBold title | the next `packages/ui` task that touches native type |
| The market pack's stage and blocker labels (`F1-09`) are translated display of a closed canonical vocabulary, and nothing checks that no translation gives two stages one word (`F3-12`) | found at `T-FPLAT-026`'s `/start`; its test reads the Lingui catalogs, and pack labels are pack data the catalogs never hold | the first pack that carries stage labels in more than one language | the pack's validation refuses two members of one vocabulary whose labels read as one word in one language, with the rule `readerForm` applies (`packages/i18n/src/copy/closed-vocabularies.ts`), which then moves to `packages/domain/src/format/` so the pack's validator and the i18n test share one copy — `domain` cannot import `i18n` — or the labels move into the registry if they become catalog copy | the task that authors the India pack's stage labels |
| `packages/ui` prints money through the bare formatters — `createFormat().money` and `compactMoney` (`packages/ui/src/utils/format.ts:84`), used by `MoneySummary`, `CompareGrid` and `Charts` with an optional standing — so a surface can print a final price that carries no tier, no energy source and no freshness, around `QualifiedAmount` (`packages/domain/src/format/qualified.ts`) | found at `T-FPLAT-028`'s `/start`; changing the design system's money props is its own slice, and no money surface exists yet | `T-FPLAT-028`, `T-FPLAT-072` | the design system's money components take a `QualifiedAmount` rather than a number and a standing, so a figure reaches the screen only with its obligations | the first money screen's `/start`, or `T-FPLAT-072` if its owner folds it in |
| 18 files under `packages/ui/src/components/` render English JSX text that no prop carries — `ActivityStream/ActivityStream.native.tsx`, `BrandColorField/BrandColorSpecimen.native.tsx`, `BrandColorField/BrandColorSpecimen.tsx`, `Checklist/ChecklistRow.tsx`, `CompareGrid/CompareGridTable.tsx`, `CompareGrid/CompareValueCell.tsx`, `DataTable/DataTableHead.tsx`, `DocumentPreview/DocumentBands.native.tsx`, `DocumentPreview/DocumentBands.tsx`, `DocumentPreview/DocumentHeader.native.tsx`, `DocumentPreview/DocumentHeader.tsx`, `DrawingSheet/DrawingSheetParts.tsx`, `FilterBar/FacetChips.tsx`, `PagedDocument/DocumentSheet.tsx`, `RichText/RichTextToolbar.tsx`, `Stepper/StepperNumbered.tsx`, `Wordmark/Wordmark.native.tsx`, `Wordmark/Wordmark.tsx` — so a Hindi or Marathi reader meets English there; the grep that listed them was retired for review, so nothing counts them now | each literal becomes a required prop on the component's one `<Name>.types.ts`, a design-system change that alters both halves and every call site together (Law 7), sequenced with the design-system work | the `TranslatedText` brand (`docs/tasks/UI.md`), or the component's next port | the prop lands, the caller reads the words from `packages/i18n`, and the file leaves this list | `docs/tasks/UI.md`, one component per PR |
| The phone's plural-rule data in `packages/i18n/src/rn/index.ts` is a bare side-effect import per language, referenced by nothing, so a language added to `UI_LANGUAGES` compiles without it and Hermes applies English plural rules to it mid-sentence; `LANGUAGE_META` and the catalog loaders are held by `satisfies Record<UiLanguage, …>` (`M48`) and this third registration is not | the grep that checked the import line was retired; the fix edits phone code, which is a task of its own, never a harness change | nothing | the plural data is registered from one `Record<UiLanguage, () => void>` (or from `LANGUAGE_META`), so a missing language fails typecheck — proven red by adding a language with no entry | the next `packages/i18n` task, a LOW-risk change |
| `packages/config/unit-test-packages.json`'s comment still names the deleted write guard (`block-test-files.sh`) and "four readers"; three readers remain | the file sits under `packages/`, so any edit moves the runtime digest, which git's pre-commit lets through only under a ticket the branch names (`M113`) — a harness PR has none, and the commit was refused on exactly that | nothing | drop the write-guard clause and the count from the comment | the next ticketed task that touches `packages/config`, LOW risk |
