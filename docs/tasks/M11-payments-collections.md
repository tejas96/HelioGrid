# M11 · Payments & Collections — engineering tasks

This file carries every engineering task for the tenant's own collections: the tranche schedule inherited from the accepted proposal version, the connection of the tenant's own collections account, payment links on due tranches, hand-recorded payments, receipts and their confirmation states, corrections (reversals and waivers), and the collections surfaces. Task ids in this file use the prefix `T-M11-`. Source doc: `docs/prd/modules/M11-payments-and-collections.md` (rows `M11-01`–`M11-56`). Screen specifications live in `docs/ux/briefs/SCR-M11-01-finance-home.md`, `docs/ux/briefs/SCR-M11-02-payments-ledger.md`, `docs/ux/briefs/SCR-M11-03-record-payment.md` and `docs/ux/briefs/SCR-M11-04-collections-settings.md`; the screen register is `docs/prd/registers/screens.md`. Two rows are worked by two screens each (`M11-19` on the ledger and on collections settings, `M11-36` on the ledger and on the recording sheet) and appear on both tasks; the Disposition index names both. One row is worked by a screen and an engine (`M11-38` — the amount a person types is refused on the recording sheet, T-M11-003, and the same constraint is enforced on the ledger's entry write path, T-M11-007), and the Disposition index names both there too. Rows whose screen half lands on another module's surface (`M11-45`, `M11-53`, `M11-55`) are built here as the fact-supply half and are marked as such on their task.

### T-M11-001 · Finance Home (Money Due)
**Type:** screen · **Tier:** P1
**PRD rows:** M11-54 (P1)
**DESIGN:** SCR-M11-01 → PENDING
**Requirements (verbatim):** Verbatim rows live in `docs/ux/briefs/SCR-M11-01-finance-home.md`; they are the specification.
**DONE WHEN:**
- Given a Finance user, when their home renders, then it shows due, overdue, receipts awaiting recording and the period's collections, each obeying the freshness law (`M11-54`).
- three base states + brief-listed states present at 375px and 1536px with full parity; zero raw colour literals/off-scale values.

### T-M11-002 · Payments Ledger
**Type:** screen · **Tier:** P0
**PRD rows:** M11-14 (P0), M11-15 (P0), M11-16 (P0), M11-19 (P0), M11-24 (P0), M11-28 (P0), M11-30 (P1), M11-31 (P0), M11-36 (P0), M11-41 (P0), M11-42 (P0), M11-46 (P0), M11-47 (P0), M11-49 (P0), M11-52 (P0)
**DESIGN:** SCR-M11-02 → PENDING
**Requirements (verbatim):** Verbatim rows live in `docs/ux/briefs/SCR-M11-02-payments-ledger.md`; they are the specification. Every row of this task is carried verbatim in that brief; no row is quoted here.
**DONE WHEN:**
- Given a new accepted version with revised terms, when the schedule re-renders, then it follows the version in force, every earlier receipt is still present and readable, and the earlier schedule remains viewable (`M11-14`).
- Given an accepted version with no payment terms, when the money surface renders, then it states that there is no schedule and creates no rows (`M11-15`, `M11-16`).
- Given a credential that fails its probe, when a due tranche is opened, then the surface states the failure and offers manual recording instead of a link action (`M11-19`).
- Given a due tranche in a tenant with a connected account, when a holder opens it, then the due row's collect action mints on that tenant's account for the outstanding amount to the minor unit — sending the link and its request message from the tenant's connected transactional channel where one is connected, and offering the copy where none is — and the copy action is present on both paths (`M11-24`, `M11-25`, `M11-26`). *(Amended per owner ruling 2026-08-06, Q45; this line previously read "then a "Copy payment link" action is present and mints on that tenant's account for the outstanding amount to the minor unit (`M11-24`, `M11-25`)" — the pre-Q33 manual-only shape, retired with the cells it quoted.)*
- Given a minted link in a tenant with a connected transactional channel, when it goes out from the due row, then it sends from that channel under the transactional template class and that channel's delivery states are shown honestly on this surface — exactly as it reports them and no further (`M11-26`, `M03-03`, `F5-28`).
- Given a minted link in a tenant with no connected channel, when it is copied from the due row, then nothing is transmitted by the product and no delivery state appears anywhere on this surface (`M11-26`) — the copy fallback is the only path on which a delivery state is absent, and its absence is never generalized to the connected-channel path.
- Given a holder of project visibility alone — a read-only Sales Executive on their own won deal, or Operations at portfolio scope — when they open a due tranche, then the plain request message is available to them to compose, to copy **and to send from the tenant's connected official channel**, and every payment-link act is absent (no mint, no copy, no send), as is every act that writes money (`F2.M08.project-visibility` via `F2.M11.send-request-message`; `F2.M11.record-payments`; §M11.4's permissions block as amended). *(Added per owner ruling 2026-08-06, Q48. This task previously built the reader's act as compose-and-copy only, with the send affordance absent for readers, because the question was recorded as open at PRD §6 `M11-Q4`; the ruling settles it in project scope — the plain message carries no money instrument — and leaves the payment link's authority unchanged.)*
- Given the plain request message sent from this surface — by any holder, the project-visibility-only reader included — when the send completes, then an audit entry is written under the name of the person who sent it (`M11-07`, `F2-22`; `F2.M11.send-request-message`), and **nothing about that entry is rendered on this screen**: the audit log has no surface in this module (no screen in `docs/prd/registers/screens.md` renders it; tenants read it through `F2-23`'s export), so no "sent" chip, tick, delivery state or sender line may be drawn from it — the only delivery reporting on this surface stays the connected channel's own, exactly as it reports it and no further (`M11-26`, `F5-28`). *(Added per owner ruling 2026-08-06, Q52, which settled PRD §6 `M11-Q5` / `foundations/F2` §6 `F2-Q2`. This task previously made no audit claim for the send; the write-side half is T-M11-009's. The ruling changes no affordance on this screen — the reader's send is Q48's and stands.)*
- Given the request message composed and copied from this surface in a tenant with **no** connected channel, when the person sends it outside the product, then no audit entry is written for that act at all (`M11-07` as amended, `F2-22`; `M11-26`) and **this screen draws nothing that implies one exists** — no "chased" or "sent" marker, no chase counter, no timeline line, no sender attribution — exactly as it draws nothing from the connected path's audit entry either. *(Added per owner ruling 2026-08-06, Q57, which settled PRD §6 `M11-Q6` / `foundations/F2` §6 `F2-Q3`: the log records what the product performed, and on this path it performed no send. The ruling adds no compensating record and changes no affordance on this screen — the fallback stays compose-and-copy with no delivery state, as the brief's **Compose-only-fallback-no-delivery-state** state specifies.)*
- Given a link that has been paid but not yet confirmed, when the tranche renders, then it states that confirmation is awaited and does not render as received (`M11-28`).
- Given an outstanding payment link and a change to that tranche's outstanding amount — a part payment recorded (`M11-36`) or a newly accepted version revising the schedule (`M11-14`) — when the money surface re-renders, then the outstanding link is marked superseded, a fresh link for the new outstanding amount is offered, and two live links for the same tranche never sit in circulation at once (`M11-30`).
- (M11-30 carries no dedicated Given/When/Then line in the PRD's acceptance block; the row's own wording in the brief is the binding criterion, alongside §M11.4's two edge cases — "the superseded link is marked" where the customer pays part of the amount through the link, and "the link is superseded and a fresh one is offered" where a new version revises the amount while a link is outstanding.)
- Given a tenant with no connected account or an unavailable rail, when a due tranche is opened, then manual recording is offered in one tap with the reason stated (`M11-31`).
- Given a payment smaller than the tranche's outstanding amount, when it is saved, then the tranche is part-received and the remaining amount is stated (`M11-36`).
- Given a ledger entry, when its receipt renders on any surface, then it carries what was paid, against which tranche, when, its mode, its reference, its file where attached, and its confirmation state (`M11-41`).
- Given one confirmed and one hand-recorded entry of equal amount on the same tranche, when both render, then each states its own provenance and neither is presented as the other (`M11-42`).
- Given a recorded payment, when it is corrected, then a reversing entry pointing at it is added with a reason and an actor, and the original entry is unchanged (`M11-46`, `M11-51`).
- Given a reversed payment, when the tranche's history renders, then both entries are visible with the reason, and the totals reflect the reversal (`M11-47`).
- Given a tranche that will not be paid, when it is waived with a reason, then it leaves the due set, is never counted as collected, and the act is audited (`M11-49`).
- Given a project with a schedule, when the payments screen renders, then it shows every tranche with its state, amount, date and receipt, and the due row carries the collect actions (`M11-52`).
- three base states + brief-listed states present at 375px and 1536px with full parity; zero raw colour literals/off-scale values.

### T-M11-003 · Record Payment
**Type:** screen · **Tier:** P0
**PRD rows:** M11-33 (P0), M11-34 (P0), M11-35 (P0), M11-36 (P0), M11-37 (P0), M11-38 (P1), M11-39 (P0)
**DESIGN:** SCR-M11-03 → PENDING
**Requirements (verbatim):** Verbatim rows live in `docs/ux/briefs/SCR-M11-03-record-payment.md`; they are the specification. Every row of this task is carried verbatim in that brief; no row is quoted here.
**DONE WHEN:**
- Given a tenant with no connected account, when a holder opens any due tranche, then manual recording is available with no precondition (`M11-33`).
- Given a recorded payment, when the ledger entry is read, then it carries amount, mode, reference, received-on date, its receipt file where one was attached, and the recording person (`M11-34`).
- Given the recording sheet, when the mode field is opened, then the options are exactly the tenant market's declared payment modes, rendered with the pack's display names (`M11-35`).
- Given a payment smaller than the tranche's outstanding amount, when it is saved, then the tranche is part-received and the remaining amount is stated (`M11-36`).
- Given a receipt file still uploading, when the tranche renders, then the payment is present and the attachment shows as in progress (`M11-37`).
- Given the recording sheet with an amount of zero or less, when save is attempted, then the entry is refused with the constraint stated and the reversal path is named as the only way a negative row is produced — no person ever types a negative amount into the ledger (`M11-38`, `M11-46`).
- (M11-38 carries no dedicated Given/When/Then line in the PRD's acceptance block; the requirement text in the brief is the binding criterion, alongside §M11.5's edge case — *The amount recorded was wrong* → it is not edited; it is reversed and re-recorded (`M11-38`, `M11-46`).)
- Given no connection, when a payment is recorded, then it is refused with an honest reason and no entry exists anywhere afterwards (`M11-39`).
- three base states + brief-listed states present at 375px and 1536px with full parity; zero raw colour literals/off-scale values.

### T-M11-004 · Collections Settings
**Type:** screen · **Tier:** P0
**PRD rows:** M11-19 (P0), M11-23 (P1)
**DESIGN:** SCR-M11-04 → PENDING
**Requirements (verbatim):** Verbatim rows live in `docs/ux/briefs/SCR-M11-04-collections-settings.md`; they are the specification. Every row of this task is carried verbatim in that brief; no row is quoted here.
**DONE WHEN:**
- Given a credential that fails its probe, when a due tranche is opened, then the surface states the failure and offers manual recording instead of a link action (`M11-19`).
- Given a tenant who disconnects, when the collections settings re-render, then the state is "not connected", link-minting is no longer offered, existing receipts are intact, and the copy about already-minted links makes no claim of revocation (`M11-23`).
- three base states + brief-listed states present at 375px and 1536px with full parity; zero raw colour literals/off-scale values.

### T-M11-005 · Schedule derivation from the accepted version — one money path to the minor unit
**Type:** engine · **Tier:** P0
**PRD rows:** M11-08, M11-09, M11-13
**Requirements (verbatim):**
- **M11-08** (P0) — **One money path, reconciling end to end to the currency's minor unit.** The bill of materials, the proposal's money block, the tranche schedule and the payments recorded against it are one chain, not four records that resemble each other: the schedule's percentages sum to exactly 100.00 for a version, its amounts sum to that version's payable, and the receipts reconcile against those amounts — all to the minor unit the market pack declares (`F1-21`). A disagreement anywhere in that chain is a defect, never a rounding style.
- **M11-09** (P0) — **The accepted proposal version's payment terms *are* the collection schedule — the same rows, pinned to that version, never re-entered and never re-derived.** The project inherits them at Won by reference (`M08-04`, `M08-35`); the tranche amounts are computed from *that version's* payable and move only when the version in force moves (`M11-14`). A sent document keeps the figures it was sent with, so a later price change never rewrites a schedule a customer already agreed to (`F8-15`).
- **M11-13** (P0) — **A tranche's amount is the version's arithmetic, and a person never types one.** Percentages come from the terms; amounts come from the payable; the minor-unit remainder is allocated by the same single arithmetic everywhere rather than by whoever is looking (`F8-24`). A tenant who wants different money writes different terms on a new proposal version (`M11-14`) — the schedule is not a scratchpad.
**DONE WHEN:**
- Given an accepted proposal version, when its project's schedule renders, then the rows, their shares and their amounts are that version's, unentered by any person, and the amounts sum to that version's payable to the minor unit (`M11-08`, `M11-09`, `M11-13`).

### T-M11-006 · Tranche state derivation and due-ness transitions
**Type:** engine · **Tier:** P0
**PRD rows:** M11-10, M11-11, M11-12
**Requirements (verbatim):**
- **M11-10** (P0) — **Tranche state is derived from the ledger and can never be typed.** `upcoming → due → part-received → received`, with `waived` terminal: the received states are recomputed from the payment entries that exist, so no person sets "received" as a status and no status can disagree with the receipts behind it. What a person *can* do is add an entry (`M11.5`), reverse one (`M11.7`) or waive the tranche (`M11-49`) — each of which changes the state by changing the facts.
- **M11-11** (P0) — **Completing the stage a tranche is mapped to makes that tranche due.** The mapping is against the canonical project chain with market-neutral stage names (`R2`); the stage event is `modules/M08`'s (`M08-36`) and the transition it causes is this module's. Nothing else makes a tranche due — not a date, not a person's judgement, not the customer's link being opened.
- **M11-12** (P0) — **A tranche mapped to a stage the project skips becomes due when the project passes the point that stage occupied — recorded as a reading, not as source text.** Skippable stages are pack data (`F1-22`) and the source is silent on what happens to money mapped to one. `M08-36` states this reading first and this module reciprocates it so the two never diverge; the alternative reading — the tranche stays `upcoming` until a person releases it — is available to an owner ruling and would be a change to this rule, not to the schedule's structure. What is *not* available under any reading is stranding the money silently.
**DONE WHEN:**
- Given a tranche with recorded payments, when its state renders, then the state is the one the entries imply and no control exists that sets it directly (`M11-10`).
- Given a project whose coordinator completes the stage a tranche is mapped to, when the stage move saves, then that tranche is due (`M11-11`).
- Given a market pack that marks a mapped stage skippable and a project that skips it, when the project passes the point that stage occupied, then the tranche becomes due rather than remaining upcoming (`M11-12`).

### T-M11-007 · Append-only payments ledger and its entry write path
**Type:** engine · **Tier:** P0
**PRD rows:** M11-40, M11-38
**Requirements (verbatim):**
- **M11-40** (P0) — **The payments ledger is append-only: entries are added, never edited, never deleted.** Every payment — hand-recorded or confirmed by the tenant's account — is an entry in one ledger per project, and the schedule's states are readings of it. Nothing in the product exposes an "edit payment" control, and no correction path removes an entry (`M11.7`).
- **M11-38** (P1) — **A person never types a negative amount.** A recorded payment is money that arrived; a mistake is undone by reversing the entry (`M11.7`), which is the only path that produces a negative row and the only one that keeps the trail intact.
**DONE WHEN:**
- Given any payment, when it is recorded or confirmed, then a ledger entry exists and no control anywhere edits or deletes it (`M11-40`).
- Given a write reaching the entry path with a zero or negative amount from any origin other than a reversal, when it is applied, then it is refused — the reversal path (`M11.7`, built by T-M11-009 and T-M11-013) is the only producer of a negative row, and the screen-side refusal is T-M11-003's (`M11-38`).
- (M11-38 carries no dedicated Given/When/Then line in the PRD's acceptance block; the requirement text above is the binding criterion. The row's screen half — the amount a person types — is worked by T-M11-003 and carried verbatim in `docs/ux/briefs/SCR-M11-03-record-payment.md`.)

### T-M11-008 · The online-only money-mutation boundary
**Type:** engine · **Tier:** P0
**PRD rows:** M11-06
**Requirements (verbatim):**
- **M11-06** (P0) — **Every money mutation is online-only and is refused, never queued.** Recording a payment, attaching it to a tranche, waiving, reversing and minting a link all require the server; with no connection the act fails fast with an honest reason and leaves nothing pending. There is no offline money in this product, on any surface, at any tier.
**DONE WHEN:**
- Given no connection, when a payment is recorded, waived, reversed or a link is minted, then the act fails fast with an honest reason and no pending write exists (`M11-06`).

### T-M11-009 · Money-event audit trail with named actors
**Type:** engine · **Tier:** P0
**PRD rows:** M11-07, M11-51
**Requirements (verbatim):**
- **M11-07** (P0) — **Every money event is an audit entry, written with the change that caused it.** Tranche edits, payment recorded, reversal posted, waiver applied, link minted, and every credential lifecycle event and decrypt are covered events — actor, time, before and after — and audit rows can never be updated or deleted. **And one act that writes no money is a covered event too: the send of the plain payment-request message from the tenant's connected official channel is an audit entry recorded under the name of the person who sent it (owner ruling 2026-08-06, Q52)** — it leaves the tenant's own official channel and reaches a customer about money, so it is written to the same log with its actor, whatever preset that person holds, the project-visibility-only reader of `F2.M11.send-request-message` included. *(That last sentence is the ruling's; this row previously ended at "…and audit rows can never be updated or deleted." and named no message send, the question standing open at §6 `M11-Q5` and `foundations/F2` §6 `F2-Q2`. Every money event above, the actor/time/before-and-after discipline and the never-updated-never-deleted rule are unchanged, and the entry records the send act and its sender — it is not a delivery state and claims none, `M11-26`.)* **And that covered event is the send from the connected channel and nothing else (owner ruling 2026-08-06, Q57): on `M11-26`'s copy-paste fallback — no channel connected — the product composes the message and places it on the clipboard, a person sends it outside the product, and no audit entry is written at all, neither a compose record nor a copy record nor a send record.** The same chase is therefore attributable when the product sends it and unrecorded when a person does — a deliberate simplification, recorded as one rather than compensated for: this log holds what the product performed, and the product never claims what it did not do. *(Those last two sentences are that ruling's; this row previously stopped at the Q52 clause and stated no boundary for the fallback path, the question standing open at §6 `M11-Q6` and `foundations/F2` §6 `F2-Q3`. Q57 adds no covered event, adds no compensating record, counter or timeline entry, and changes nothing else here: every money event above, the actor/time/before-and-after discipline, the never-updated-never-deleted rule and the Q52 clause's own terms are unchanged.)* _(cell amended at source by owner rulings 2026-08-06, Q52 and Q57 — quoted here as amended; the send this clause covers is `F2.M11.send-request-message`'s, whose holder set is Q48's and is unchanged, and Q57 adds no covered event: it bounds the clause to the connected channel and leaves the copy-paste fallback writing nothing)_
- **M11-51** (P0) — **Reversal and waiver are money events with named actors, and both are audit entries.** Who reversed, who waived, when, why, and against what — recorded at the moment of the act, never reconstructed afterwards.
**DONE WHEN:**
- Given any money event, when the audit log is read, then the event appears with its actor, its time and the change that caused it (`M11-07`).
- Given a recorded payment, when it is corrected, then a reversing entry pointing at it is added with a reason and an actor, and the original entry is unchanged (`M11-46`, `M11-51`).
- Given the plain payment-request message sent from the tenant's connected official channel, when the audit log is read, then the send appears as its own append-only entry under the name of the person who sent it — including where that person holds project visibility alone and writes no money (`M11-07`, `F2-22`; `F2.M11.send-request-message`). Composing and copying write no entry; the send is the audited act, and the entry records the act and its sender — it is not a delivery state and no surface may render it as one (`M11-26`). *(Added per owner ruling 2026-08-06, Q52, which settled PRD §6 `M11-Q5` / `foundations/F2` §6 `F2-Q2`. This task previously carried no audit criterion for any message send, because the `M11-07` cell it quotes named none and the question was recorded as open; the send-side screen and engine halves are T-M11-002's and T-M11-016's.)*
- Given a tenant with **no** connected official channel, when the request message is composed and copied and a person sends it outside the product, then this write path writes nothing — no audit entry of any kind exists for that act, not a compose record, not a copy record, not a send record, and no counter or timeline entry stands in for one (`M11-07` as amended, `F2-22`; `M11-26`). The audited act is the product's send; where the product performed no send there is nothing to record, and the log is deliberately silent. *(Added per owner ruling 2026-08-06, Q57, which settled PRD §6 `M11-Q6` / `foundations/F2` §6 `F2-Q3`. The owner chose the simpler rule explicitly — no compensating record — and the accepted trade is stated in the `M11-07` cell quoted above: the same chase is attributable when the product sends it and unrecorded when a person does. **Review rejects any fallback-path record added back in**, under any name.)*

### T-M11-010 · Collections-account connection: vendor-neutral rail adapter and credential handling
**Type:** integration · **Tier:** P0
**PRD rows:** M11-05, M11-17, M11-18, M11-20, M11-22
**Requirements (verbatim):**
- **M11-05** (P0) — **The collection rail is a capability declared by the market pack, never a vendor requirement.** Which rails a market has — payment links, the payment modes a manual entry may carry, and any payment-data constraints — is `pack.payment-rails` data; the launch market's rail is a *reference implementation* and swapping it is an adapter change, never a product change. No requirement in this module names a payment provider, and no screen in it does either.
- **M11-17** (P0) — **Each tenant connects their own collections account, and that account is where everything happens.** Payment links are minted on it, confirmations come back from it, and settlement lands in the bank account behind it. The tenant is the account holder and the merchant of record for their own collections; the product is a client of their account, never a party to the transaction (`M11-01`).
- **M11-18** (P0) — **Connection is a capability, and its credential handling is stated at product level: write-only, last-4, never read back.** After entry a credential is never displayed again beyond its last four characters, never returned to any client, never logged and never carried in a customer-facing artefact; storage is encrypted and every decrypt is an audit entry. Credentials belong to the tenant and are the tenant's to rotate at any time.
- **M11-20** (P0) — **The link rail is optional per market, and the module is fully functional without it.** Whether a market has a payment-link rail at all is `pack.payment-rails` data; where it has none, or where a tenant simply has not connected an account, collections run entirely on manual recording and every capability of this module except link-minting still works.
- **M11-22** (P0) — **No credential ever reaches a client, and no collection call is made from one.** The rail is spoken to only from the server side, on the tenant's behalf; nothing in a browser or on a device holds, sees or transmits a tenant's collection credential.
**DONE WHEN:**
- Given the payment rail of the tenant's market, when the module is read end to end, then no provider name appears as a requirement and every rail fact resolves to a pack key (`M11-05`).
- Given a tenant with a connected account, when a payment link is minted, then it is minted on that tenant's account and settlement is to that tenant's bank (`M11-17`).
- Given a stored credential, when any surface in this module renders it, then at most its last four characters appear and no read-back exists (`M11-18`).
- Given a tenant with no connected account, when they work any collections surface, then every capability except link-minting is available and nothing is blocked (`M11-20`, `M11-21`).
- Given any collections flow, when its network activity is examined, then no tenant credential leaves the server and no call to the rail originates from a client (`M11-22`).

### T-M11-011 · Payment-link minting on the tenant's account
**Type:** integration · **Tier:** P0
**PRD rows:** M11-25
**Requirements (verbatim):**
- **M11-25** (P0) — **A link is minted for one tranche and for exactly what that tranche still owes**, in the tenant's currency, to the minor unit — never a rounded figure, never a "convenient" amount, never a lump that spans rows.
**DONE WHEN:**
- Given a due tranche in a tenant with a connected account, when a holder opens it, then the due row's collect action mints on that tenant's account for the outstanding amount to the minor unit — sending the link and its request message from the tenant's connected transactional channel where one is connected, and offering the copy where none is — and the copy action is present on both paths (`M11-24`, `M11-25`, `M11-26`). *(Amended per owner ruling 2026-08-06, Q45; this line previously read "then a "Copy payment link" action is present and mints on that tenant's account for the outstanding amount to the minor unit (`M11-24`, `M11-25`)".)*
- Given a minted link in a tenant with a connected transactional channel, when it goes out, then it is handed to that channel's transactional send under the transactional template class (`M03-03`, the `payment_reminder` key of T-FPLAT-021's registry) and the channel's delivery states ride it honestly; and given no connected channel, then it goes to the clipboard only and nothing anywhere claims a delivery (`M11-26` as amended by owner ruling 2026-08-06, Q45 — the requirement cell itself now carries both branches, so this criterion no longer rests on `docs/prd/modules/M11-payments-and-collections.md` §5's Q33 bullet alone; see the M11-26 law below).
- Given the minting path in any tenant, when the settlement is examined, then the send changed nothing about it: the link is minted on the tenant's own account and the money moves customer → tenant with no platform-held balance at any point (`M11-01`, unamended by Q45).

### T-M11-012 · Confirmation intake and reconciliation
**Type:** integration · **Tier:** P0
**PRD rows:** M11-27, M11-29
**Requirements (verbatim):**
- **M11-27** (P0) — **A tranche becomes received only on confirmation from the tenant's own account, and a repeated confirmation never counts twice.** Confirmation is verified as coming from that tenant's account, is safe to receive more than once, marks the tranche's receipt and attaches the receipt record. Nothing about "the customer said they paid" moves the state — only the account's confirmation, or a person's explicit recorded entry (`M11.5`), which is labelled as exactly that (`M11-42`).
- **M11-29** (P0) — **Missed confirmations are healed by reconciliation, not by a person chasing the product.** The tranche's status is re-checked against the tenant's account whenever a user views it, and a periodic sweep repairs anything nobody looked at; a repaired state is the same state a live confirmation would have produced, and the receipt it produces is identical.
**DONE WHEN:**
- Given a confirmation from the tenant's account, when it is received, then the tranche's receipt is written and its state follows the ledger; and given the same confirmation again, when it is received a second time, then no second receipt exists (`M11-27`).
- Given a confirmation that never arrived, when a user next views the tranche, then reconciliation runs and the state matches the tenant's account (`M11-29`).

### T-M11-013 · Reversal restatement and the cancelled-project money rule
**Type:** engine · **Tier:** P0
**PRD rows:** M11-48, M11-50
**Requirements (verbatim):**
- **M11-48** (P0) — **A reversal restates the tranche's state by restating its facts.** Because state is derived (`M11-10`), a reversal moves a received tranche back to part-received or due automatically; nobody re-types a status, and no state can survive a reversal that contradicts it.
- **M11-50** (P0) — **Cancelling a project does not unwind money, and the platform never performs a refund.** Receipts already taken stay readable on a cancelled project; any money returned to a customer is returned by the tenant, on the tenant's own account, and recorded here as a reversing entry with its reason. The product records refunds; it does not move them (`M11-01`).
**DONE WHEN:**
- Given a fully received tranche, when a payment against it is reversed, then its state returns to the one the remaining entries imply, with no status typed by anyone (`M11-48`).
- Given a cancelled project with receipts, when its money surface renders, then the receipts are readable and any refund appears as a reversing entry rather than a platform-performed movement of money (`M11-50`).

### T-M11-014 · The single collected-versus-due figure and its freshness law
**Type:** engine · **Tier:** P0
**PRD rows:** M11-43, M11-44
**Requirements (verbatim):**
- **M11-43** (P0) — **Collected-against-due is one computed figure, and every surface that shows it shows the same one.** The payments screen, the project's money block, the board card, the owner's dashboard, an export and the customer's rendering are renderings of one value — they never recompute independently, round differently or drop a qualifier the others carry.
- **M11-44** (P0) — **No money figure here renders as final while stale.** Figures are reconciled against the ledger before display; anything unreconciled is visibly provisional rather than presented as settled fact. There is no export, screen or speed optimisation exempt from this.
**DONE WHEN:**
- Given the payments screen and the project's money block open at once, when both render the same project, then collected and due are identical figures (`M11-43`).
- Given a money figure that cannot be reconciled at display time, when it renders, then it renders provisional and is not presented as final (`M11-44`).

### T-M11-015 · Publishing receipts and payment facts to the customer surface
**Type:** integration · **Tier:** P0
**PRD rows:** M11-45, M11-55
**Requirements (verbatim):**
- **M11-45** (P1) — **The receipt is one of the three things that carry the customer through their most anxious moment, and this module owns its half.** The source names them: *"an instant receipt, a named person to contact, and a clear statement of what happens next and when."* This module produces the receipt the instant the money is confirmed or recorded, and publishes it to the customer's surface; the named contact and the what-happens-next line are `foundations/F5`'s rendering of `modules/M08`'s facts.
- **M11-55** (P0) — **What this module publishes to the customer's surface, and nothing more:** the due tranche's label and amount, its payment link where one has been minted, the receipts with their confirmation states, and the fact that nothing here may gate the page. `foundations/F5` renders every word the customer reads, decides the page's shape, and owns the token and its lifecycle; this module supplies facts and never copy.
This task builds the fact-supply half only. The customer-facing rendering is SCR-F5-02, specified in `docs/ux/briefs/SCR-F5-02-link-progress.md` and owned by the F5 task file, not by this one.
**DONE WHEN:**
- Given a confirmed payment, when the customer's surface is opened, then the receipt is already there (`M11-45`).
- Given a confirmed or recorded payment, when the customer's surface renders, then the receipt and its confirmation state are available to it and no state in this module gates the page (`M11-55`, `M11-32`).

### T-M11-016 · Overdue facts and the chase prompt supplied to the project, board and dashboard
**Type:** engine · **Tier:** P0
**PRD rows:** M11-53
**Requirements (verbatim):**
- **M11-53** (P0) — **An unpaid due tranche is chased through a person, and the product's job is to make it impossible to miss.** It surfaces on the project, on the stage board and on the owner's dashboard (`modules/M08`, `modules/M13` own those surfaces; this module supplies the facts), and the rep is prompted to chase. The prompt leads to a message — sent from the tenant's connected transactional channel where one exists and composed for a person to send where none is (owner ruling 2026-08-06, Q45; `M11-26`) — never to a product-side sanction against the customer (`M11-32`). *This clause previously read "The prompt leads to a message a person sends — never to a product-side sanction against the customer (`M11-32`)"; the pre-Q33 manual-only half is retired and the never-sanction half is unchanged. The chase is still a person's decision: the product prompts and the person acts.* _(cell amended at source by owner ruling 2026-08-06, Q45 — quoted here as amended; the message's send path is `M11-26`'s, the never-sanction law is unchanged)_
This task builds the fact-supply half only. The rendering surfaces are SCR-M08-02 and the M13 dashboards, owned by those modules' task files, not by this one.
**DONE WHEN:**
- Given an overdue tranche, when the board, the project and the owner's dashboard render, then it is visible on each with what is owed and since when, and a chase prompt is offered (`M11-53`).
- Given the chase prompt in a tenant with a connected transactional channel, when it is taken, then the message sends from that channel (`M11-26`, `M03-03`) and the customer is subjected to nothing else — no gate, no lock, no withheld document, no revoked link (`M11-32`, `M11-53`); and given no connected channel, then it is composed for a person to send and no delivery state is claimed anywhere (`M11-26`). *(Added per owner ruling 2026-08-06, Q45. This task previously carried the M11-53 acceptance line alone, against a cell that read "The prompt leads to a message a person sends".)*
- Given the chase prompt taken by a holder of project visibility alone — the read-only Sales Executive on their own won deal — when the tenant has a connected official channel, then the send is theirs to take: the plain request message carries no money instrument and rides their own project scope (`F2.M11.send-request-message`, `F2.M08.project-visibility`), while every payment-link act stays with `F2.M11.record-payments`. *(Added per owner ruling 2026-08-06, Q48, which settled PRD §6 `M11-Q4`. This criterion did not exist while that question was open; the `M11-53` cell quoted above is unamended by Q48.)*
- Given the chase prompt taken by any holder — the read-only Sales Executive included — when the message sends from the tenant's connected official channel, then the send is written to the audit log as its own entry under that person's name (`M11-07` as amended, `F2-22`; `F2.M11.send-request-message`), and the customer is still subjected to nothing else (`M11-32`); where no channel is connected the message is composed for a person to send, the product performs no send, and no audit entry, delivery state or attribution is claimed for what that person does outside the product (`M11-26`). *(Added per owner ruling 2026-08-06, Q52, which settled PRD §6 `M11-Q5` / `foundations/F2` §6 `F2-Q2`. This task previously made no audit claim for the send; the `M11-53` cell quoted above is unamended by Q52 — the amended cell is `M11-07`, worked by T-M11-009, which builds the write path.)* *(**Confirmed and now ruled by owner ruling 2026-08-06, Q57**, which settled PRD §6 `M11-Q6` / `foundations/F2` §6 `F2-Q3`: the no-channel half of this criterion — the product performs no send and claims no audit entry, delivery state or attribution — is the ruled behaviour rather than a consequence read off an open question. **Nothing is recorded on that path at all**, and no compose record, copy record, counter or timeline entry may be added back; the criterion's text is unchanged by the ruling, and the `M11-53` cell quoted above is unamended by it — the amended cell is `M11-07`.)*

## Laws (enforced through screens and review, no standalone build)

- **M11-01** (P0) — **The platform never touches tenant funds — the load-bearing law of this module.** Every collection settles directly from the customer to the tenant's own account; links are minted on that account and money lands in that account's bank settlement. No product behaviour may hold, route, escrow, net, split, delay or take a share of a tenant's collection, and no future feature may quietly acquire the ability. This is a licensing boundary, not a design preference: a product that handled other people's money would need the money-handling licence its market requires, and the whole collections design exists to stay outside that (`F1-18`, `F1-43` for the market instance). — *Enforced by:* T-M11-010 minting only on the tenant's own account with settlement to that tenant's bank, T-M11-013 recording refunds rather than moving them, and review of every flow in this file; its acceptance line: Given any collection in the product, when it settles, then it settles from the customer to the tenant's own account and no platform-held balance exists at any point (`M11-01`).
- **M11-02** (P0) — **Two money systems that never mix, and this module is only one of them.** The tenant's customers paying the tenant is this module; the tenant paying the platform is `modules/M12`. No surface here shows a subscription figure, no total here includes one, no export here mixes them, and the two keep separate vocabularies — *collections, tranches, receipts* here; *subscription, invoice, plan* there. **The EPC's customer never pays the platform anything.** — *Enforced by:* content review of every surface in this file (T-M11-001…T-M11-004) against their briefs, and `docs/tasks/M12-platform-billing.md`'s reciprocal law `M12-01`; its acceptance line: Given a collections surface, when it renders, then no platform-subscription figure appears on it and no total on it includes one (`M11-02`).
- **M11-03** (P0) — **The product takes no share of any tenant collection, ever.** No percentage, no per-transaction fee, no float, no "powered by" surcharge on money the EPC collects. This is a deliberate competitive skip and it is recorded as one: the rival model that takes a cut of customer payments is exactly what the never-touch-funds law forecloses, and the product's revenue comes from subscription alone (`modules/M12`). — *Enforced by:* T-M11-011 minting for exactly what the tranche still owes and for nothing else, T-M11-005's single money path, and review that no fee, share or surcharge exists in any collections arithmetic; its acceptance line: Given a completed collection of any amount, when the tenant's settlement is examined, then the product has deducted nothing from it (`M11-03`).
- **M11-04** (P0) — **Split settlement with the platform as master merchant is rejected for v1, and the rejection carries its trigger.** The alternative — the platform as the account of record, splitting each collection — is documented as an alternate rail adapter and nothing more, because it would make the platform the licence-bearing merchant with the obligations that follow. Revisit only if a marketplace revenue model appears (§5). — *Enforced by:* T-M11-010's adapter offering connection to the tenant's own account as the only arrangement, and review of any proposed rail against `docs/prd/modules/M11-payments-and-collections.md` §5; its acceptance line: Given a tenant setting up collections, when the available arrangements are read, then no split-settlement or platform-as-master-merchant arrangement is offered anywhere in the product, and the alternative exists only as a recorded non-goal with its revisit trigger (`M11-04`, §5).
- **M11-21** (P0) — **The connected account is an accelerator, never a dependency — stated as law because the temptation runs the other way.** No collection, receipt, tranche state, document, handover or customer surface may be made conditional on a tenant having connected an account. *"Cash is still king"* in this trade, and the product that assumes otherwise is wrong about its market. — *Enforced by:* T-M11-003 offering manual recording with no precondition, T-M11-010's no-account path leaving every capability except link-minting available, T-M11-002 and T-M11-004 rendering the manual path with the reason stated, and review that no gate anywhere reads connection state; its acceptance line: Given a tenant with no connected account, when they work any collections surface, then every capability except link-minting is available and nothing is blocked (`M11-20`, `M11-21`).
- **M11-26** (P0) — **The payment link and its request message send from the tenant's connected transactional channel; composed copy-paste is the fallback where no channel is connected (owner ruling 2026-08-06, Q45, applying owner ruling 2026-08-04 Q33).** Where the tenant has a connected official channel — the same connection `modules/M03` establishes — the minted link and its composed request message go out from that channel under the transactional/utility template class (`M03-03`), and that channel's own delivery states ride the sent message honestly: exactly as it reports them and no further (`F5-28`). Where no channel is connected, the link is placed on the clipboard (with the request message where the person wants both) and the rep or coordinator sends it through whatever channel they already use — and **only that fallback path claims no delivery**: no delivery state appears anywhere on the surface for it, because the product will not claim knowledge of a delivery it did not perform. **Only the message is automated.** Money is untouched by this row: collections still settle directly from the customer to the tenant's own account and the platform touches no funds (`M11-01`, unamended). *This row previously read, in full: **The product composes and copies; a person sends.** There is no send capability here and no delivery state anywhere on the surface: the link is placed on the clipboard (with the request message where the person wants both) and the rep or coordinator sends it through whatever channel they already use. The product will not claim knowledge of a delivery it did not perform. That was the pre-Q33 manual-only rule stated unscoped; it is retired here and survives only as the fallback path's no-delivery-claim discipline. The divergence it created with this document's §5 is recorded at `docs/prd/registers/conflicts.md` row 10 and is closed by this amendment.* — **AMENDED AT SOURCE (owner ruling 2026-08-06, Q45), and the divergence this entry used to carry is closed.** This entry previously quoted the pre-Q33 cell — the exact wording it replaced is carried inside the amended cell above, which records it — and wrapped it in a **RECONCILED (owner ruling 2026-08-04, Q33)** annotation that read the send onto the build from `docs/prd/modules/M11-payments-and-collections.md` §5's Q33 bullet, because the cell itself had never been rewritten and the contradiction stood at `docs/prd/registers/conflicts.md` row 10. The owner has now rewritten the cell: the requirement text above **is** the amended cell, its two branches are the cell's own law rather than an annotation's reading, and row 10's contradiction is closed. The build does not change — it is what that annotation already required. The connected-channel send is the same connection `modules/M03` establishes, under the utility/transactional template class the pack's rules define (`M03-03`), drawing the `payment_reminder` template key from the registry T-FPLAT-021 builds (`F6-26` in `docs/tasks/F-platform.md`, which names "`M11`'s payment-request message" among that registry's consumers). `D32`'s manual-only rule is retired here as everywhere else; on this module's surfaces it survives solely as the fallback path's no-delivery-claim discipline. — *Enforced by:* T-M11-011 minting the link on the tenant's account and handing it to the transactional send where a channel is connected and to the clipboard where none is, T-M11-002's due-row collect actions carrying send-where-connected alongside copy-always per `docs/ux/briefs/SCR-M11-02-payments-ledger.md`, and review that no delivery state is claimed on the copy-paste fallback path on any surface in this file — **review rejects a missing send control wherever a channel is connected, and rejects any delivery state on the copy fallback**; its acceptance lines, both now carried by the PRD's own §M11.4 acceptance block: Given a minted link in a tenant with a connected transactional channel, when it goes out, then it sends from that channel under the transactional template class and that channel's delivery states are shown honestly — exactly as it reports them and no further (`M11-26`, `M03-03`, `F5-28`); and given a minted link in a tenant with no connected channel, when it is copied, then nothing is transmitted by the product and no delivery state appears anywhere on the surface (`M11-26`). The ruling automates the message only: `M11-01` is unamended, the platform touches no funds, and collections still settle customer → the tenant's own account. **RULED (owner ruling 2026-08-06, Q48) — the question this entry carried as open is settled and the build gains the reader's send.** The *send of the plain request message* from the tenant's connected official channel **stays in project scope** (`F2.M08.project-visibility`), now carried by `foundations/F2` §F2.5-M11's row `F2.M11.send-request-message`: the plain message carries no money instrument, and keeping it there preserves the case the PRD wanted — a read-only Sales Executive chasing their own won deal (`S8.wrong.3`, `M08-18`). **Minting and sending an actual payment LINK is unchanged and still requires `F2.M11.record-payments`**, a link being a live collection instrument. T-M11-002 therefore builds the reader's send alongside compose-and-copy, and its DONE WHEN carries the criterion. *(This passage previously read: "**New question, recorded and not decided (PRD §6 `M11-Q4`):** whose act the *send of the plain request message* is — the reader's project scope (`F2.M08.project-visibility`, which is what lets the read-only Sales Executive chase their own won deal) or its own holder set — is open; the payment link's own authority is unaffected (`F2.M11.record-payments` covers minting, sending and re-copying it), and T-M11-002 builds the reader's compose-and-copy act unchanged until it is ruled." PRD §6 `M11-Q4` is now RESOLVED. The requirement cell quoted above is **not** amended by Q48 — it stands exactly as Q45 left it — and money settlement is untouched.)* **RULED (owner ruling 2026-08-06, Q52) — the question this entry carried as open is settled and the build gains the audit write.** The plain message's *send* **is** an audit-covered event and the entry is recorded **under the name of the person who sent it**, for every holder of `F2.M11.send-request-message`, the project-visibility-only reader included: the message leaves the tenant's own official channel and reaches a customer about money, and the record answers "who messaged my customer?" instantly. `F2-22`'s checklist now names the send and `M11-07` is amended at source to reciprocate it (T-M11-009 quotes the amended cell and builds the write path; T-M11-002 and T-M11-016 carry the criterion on the surfaces the send is taken from). Composing and copying write no entry — the **send** is the audited act — and the entry records the act and its sender, never a delivery: `M11-26`'s no-delivery-claim discipline is untouched, the copy fallback still shows no delivery state anywhere, and **review rejects any surface that renders an audit entry as a delivery affordance**. *(This passage previously read: "**One new question is raised by applying Q48 and is recorded, not decided (PRD §6 `M11-Q5`, `foundations/F2` §6 `F2-Q2`):** whether the plain message's *send* is an audit-covered event and in whose name — `F2-22`'s checklist does not name it and this file claims no audit for it; nothing here decides it." PRD §6 `M11-Q5` and `foundations/F2` §6 `F2-Q2` are now RESOLVED. The requirement cell quoted at the head of this entry — `M11-26` — is **not** amended by Q52; it stands exactly as Q45 left it, the holder set stays Q48's, and money settlement is untouched: `M11-01` stands unamended.)* **RULED (owner ruling 2026-08-06, Q57) — the audit write stops at the connected channel, and the fallback path records nothing.** Q52's covered event is the send the *product* performs; on this cell's copy-paste fallback the product composes and places text on the clipboard and a person sends it outside the product, so **no entry is written at all — no compose record, no copy record, no send record — and no counter, timeline entry or chase marker stands in for one**. The owner chose the simpler rule in as many words (*"keep as much as simple. and dont do overengineering"*), and the trade is recorded rather than engineered around: the same chase is attributable when the product sends it and unrecorded when a person does, which is exactly this cell's own discipline applied to the log — the product never claims what it did not do. The build gains nothing and loses nothing: T-M11-009 writes on the connected path only, T-M11-002 and T-M11-016 carry the boundary as criteria, and **review rejects any fallback-path record added back under any name, as it already rejects any surface that renders an audit entry as a delivery affordance**. PRD §6 `M11-Q6` and `foundations/F2` §6 `F2-Q3` are now RESOLVED. The requirement cell quoted at the head of this entry — `M11-26` — is **not** amended by Q57 either; it stands exactly as Q45 left it, the holder set stays Q48's, the amended cell is `M11-07`, and money settlement is untouched: `M11-01` stands unamended.
- **M11-32** (P0) — **An unpaid tranche never gates the customer's own surface.** The payment link and the customer's progress link are different objects with different jobs: money owed is chased through people, and *"never block the customer's progress link over money — chase the person, do not punish the view"*. No state in this module may be wired to a customer-facing gate. — *Enforced by:* T-M11-015 publishing facts that gate nothing and T-M11-016 supplying the chase facts rather than a sanction, with the link's own lifecycle owned by `docs/tasks/F5-customer-link.md`; review rejects any wiring from a tranche state to a customer-facing gate; its acceptance line: Given a project with an overdue tranche, when the customer opens their own surface, then it works exactly as it would if nothing were owed (`M11-32`).
- **M11-56** (P0) — **No commercial figure from this module reaches a surface that must not carry one.** Nothing in the tranche schedule, the ledger or the collections views may appear on any surface the Installation Team Member preset reaches — a property of the surface, not of the viewer. — *Enforced by:* this module composing no money fact onto an installation surface — the crew surfaces of `docs/tasks/M09-field-workforce.md` and the installation-reachable surfaces of `docs/tasks/M08-projects.md` consume none — and review of every consumer of T-M11-014's figure; its acceptance line: Given any surface the Installation Team Member preset reaches, when it renders, then no tranche, receipt, price or other commercial figure appears (`M11-56`).

## Disposition index

| Row | Disposition |
|---|---|
| M11-01 | LAW |
| M11-02 | LAW |
| M11-03 | LAW |
| M11-04 | LAW |
| M11-05 | T-M11-010 |
| M11-06 | T-M11-008 |
| M11-07 | T-M11-009 |
| M11-08 | T-M11-005 |
| M11-09 | T-M11-005 |
| M11-10 | T-M11-006 |
| M11-11 | T-M11-006 |
| M11-12 | T-M11-006 |
| M11-13 | T-M11-005 |
| M11-14 | T-M11-002 |
| M11-15 | T-M11-002 |
| M11-16 | T-M11-002 |
| M11-17 | T-M11-010 |
| M11-18 | T-M11-010 |
| M11-19 | T-M11-002, T-M11-004 |
| M11-20 | T-M11-010 |
| M11-21 | LAW |
| M11-22 | T-M11-010 |
| M11-23 | T-M11-004 |
| M11-24 | T-M11-002 |
| M11-25 | T-M11-011 |
| M11-26 | LAW |
| M11-27 | T-M11-012 |
| M11-28 | T-M11-002 |
| M11-29 | T-M11-012 |
| M11-30 | T-M11-002 |
| M11-31 | T-M11-002 |
| M11-32 | LAW |
| M11-33 | T-M11-003 |
| M11-34 | T-M11-003 |
| M11-35 | T-M11-003 |
| M11-36 | T-M11-002, T-M11-003 |
| M11-37 | T-M11-003 |
| M11-38 | T-M11-003, T-M11-007 |
| M11-39 | T-M11-003 |
| M11-40 | T-M11-007 |
| M11-41 | T-M11-002 |
| M11-42 | T-M11-002 |
| M11-43 | T-M11-014 |
| M11-44 | T-M11-014 |
| M11-45 | T-M11-015 |
| M11-46 | T-M11-002 |
| M11-47 | T-M11-002 |
| M11-48 | T-M11-013 |
| M11-49 | T-M11-002 |
| M11-50 | T-M11-013 |
| M11-51 | T-M11-009 |
| M11-52 | T-M11-002 |
| M11-53 | T-M11-016 |
| M11-54 | T-M11-001 |
| M11-55 | T-M11-015 |
| M11-56 | LAW |
