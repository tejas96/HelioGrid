1. **Font family** · Repo (10-i18n §2/§8, research/design.md §3): Inter is the sans, loaded via `next/font`; `--font-inter` composed into `--font-sans`. · _ds: **Geist** is the default sans, **Geist Mono** for IDs/kWh/₹/coordinates (564 mono uses in mockups); Inter appears only as an unloaded stack fallback (`--font-sans:"Geist","Inter",-apple-system…`); two variable woff2 files (100–900) are the only fonts shipped. · **RESOLUTION: UX wins — Geist + Geist Mono, vendored from `_ds/assets/fonts/`.** Drop Inter loading entirely (do not repeat the POC's "declared Inter, never shipped it" bug in reverse by shipping a font the spec doesn't use). Add the mono-for-numerics rule to repo docs — it is currently absent and is one of the strongest visual signatures. RN caveat: only variable woff2 exists; RN needs static instances cut at the sanctioned weights.

2. **Devanagari gap** · Repo: Inter → Noto Sans Devanagari chain, four weights bundled, RN `<AppText>` run-splitting, no synthetic bold. · _ds: **no Devanagari font anywhere** — HI/MR mockups render via OS fallback (Kohinoor/Nirmala), and the CustomerProposal QA harness only tests that "layout survives Devanagari". · **RESOLUTION: product law stays — pair Geist → Noto Sans Devanagari** (web `next/font` chain; RN explicit run-splitting exactly as repo §8, s/Inter/Geist/). The _ds silence here is a gap, not a decision; system-font fallback is unacceptable for a pixel-perfect product. Keep the per-screen Hindi render check in DoD.

3. **Weight set** · Repo: 400/500/600/700, no 550. · _ds tokens/readme: **400/700 only; 500 restricted to buttons/tabs/table-headers**; no 600 token exists — but the mockups use 500 pervasively (1213×) and **600 ×210** (off-token, concentrated in dense desktop screens). · **RESOLUTION: adopt _ds tokens 400/500/700 as the base; treat the readme's "500 restricted" clause as dead (usage overrules it).** The 600s are internal drift within the UX package — either add a sanctioned `--fw-semibold:600` or normalise mockup 600→700 at implementation. Flagged for owner (see below).

4. **Type scale + body size + 11px overline vs 12px floor** · Repo: 8 rem steps `--text-2xs…3xl`, body 14px, **N3: no font below 12px ever**. · _ds: 12 px-denominated roles (display 40 → table 13), **body 15px**, tightly tracked headings (−0.01 to −0.03em, "essential to the look"), caption 12px, and the signature **overline at 11px/700/0.12em/uppercase**. · **RESOLUTION: UX scale wins verbatim (body 15px, tracking included).** N3 stays as law with **one named exception: the overline token** — 11px/700/uppercase micro-labels only, never body/data/anything interactive. Owner sign-off on the exception (below).

5. **Primary-action colour + brass existence** · Repo: primary fill is brass `#C8842A` with INK label (white-on-brass fails AA), accent **brightens** on hover, dark mode moves fill up to brass-300 — the three "most-broken rules" in ui.md are all brass rules. · _ds: **brass does not exist anywhere in the package**; primary action is near-black `#0A0A0B` / white text (~19:1, the ink-on-brass problem evaporates), hover `#26262A`, pressed `#000000`. "Primary action is near-black, never coloured — the strongest identity marker." · **RESOLUTION: UX wins — near-black primary; retire the entire brass rule cluster** (ink-on-brass, brighten-on-hover, `text-accent-text`) from ui.md/CLAUDE.md/10-i18n §1.3. Note the mockups' arc-bar centre FAB is `--action-primary` near-black — ui.md's "brass centre action" line is wrong twice over. Nothing brass-coloured survives; the only brass-ish hex in 80 mockups is a roof-render tint.

6. **Accent system** · Repo: one accent (brass) doing fills, hover, subtle, border, text via `-text`. · _ds: two separate systems — **interactive accent `#5A4BFF`** (focus, links, selected, active tab, control fills — never a button fill) and the **iridescent trio** `#7B5CFF/#3B82F6/#E85CBE` as gradient/glow/icon-wash/AI cues only ("colour is atmosphere"). `#5A4BFF` on white is ~5.4:1, so it works as link text directly — the `text-accent`-doesn't-exist rule is obsolete. · **RESOLUTION: UX wins.** Also promote the mockups' recurring hand-mixed violet washes (`#F4F1FF`, `#FCFBFF`, `#FBFAFF`…) into a proper `--brand-wash` token — the one recurring off-token colour in usage.

7. **Borders vs luminance hierarchy** · Repo: `--border/--border-strong/--border-subtle` tokens; N4 requires 3:1 UI boundaries. · _ds: **"hierarchy comes from luminance and softness, never from lines"; no structural 1px borders anywhere** (exceptions: dashed drop-zones, opt-in high-contrast field mode); only `--hairline rgba(10,10,11,0.06)`; table rows alternate `#FFFFFF/#FAFBFC`. · **RESOLUTION: UX wins — delete the border token family, keep `--hairline`.** N4's 3:1 boundary rule applies to *meaning-bearing* boundaries (focus ring, form-control state), which _ds satisfies via the 2px accent ring and elevation-on-focus inputs; it does not mandate decorating cards with lines. Keep the high-contrast field mode as the sanctioned escape hatch (good fit for sunlight-visibility on site).

8. **Focus ring** · Repo: two-tone ring (`--focus-inner` near-white + `--focus-outer` ink halo), a named brand rule. · _ds: **single `2px solid #5A4BFF`, offset 2px, never removed**; inputs focus via `--e2 + 0 0 0 2px surface + 0 0 0 4px accent` (which is itself two-tone in effect). · **RESOLUTION: UX wins** — single accent ring globally, the input treatment verbatim. RN still needs the bespoke ≥3:1 focus treatment from repo §4 (that contract survives).

9. **Overlay treatment** · Repo: unstated scrim, `--surface-overlay` token, elevations deepen in dark. · _ds: **overlays blur (0→8px) the layer behind and fade it toward white (0.35), never a dark scrim**; `backdrop-filter` reserved for overlay backdrops + glass credit chip. · **RESOLUTION: UX wins — blur+fade-to-white is law; no dark scrim exists in the system.**

10. **Dark mode** · Repo: hard requirement — one semantic system/two value sets, generated dark block, "light AND dark correct" in the DoD, `--surface-canvas` theme-invariant **dark** for satellite/3D. · _ds ground truth: **light-only**. `base.css` sets `color-scheme:light`; zero dark tokens, media queries or `data-theme` selectors exist (the readme's "colors.css (+ dark mode)" index line is false); no mockup renders dark; **even DesignStudio's canvas is light** (`--canvas`/`--canvas-sunken` wells) — the sole near-black background literal in 80 files is one small element. · **RESOLUTION: OWNER DECISION (below).** Default until decided: build light-only to match the spec, keep the semantic-alias indirection so a dark set is a later value-set drop, and strike "light AND dark correct" from the DoD interim. The dark-studio-canvas doctrine is also overturned by the mockups — flag both together.

11. **Density modes** · Repo: **entirely absent** — one radius/spacing scale, no density concept. · _ds: core doctrine — **Expressive vs Functional** twin token families (`--r-*`/`--rf-*`, screen-pad pairs, card 24 vs 12px, input 14 vs 10px), same colours/type/rules; default Expressive mobile, Functional desktop-data. Mockups never set `data-mode` — density is applied per-frame by convention, and the components carry a `density` prop. · **RESOLUTION: adopt the doctrine into repo docs; implement as the component-level `density` prop (what the _ds components and lint allowlists actually specify), not a global mode attribute** (the `:root[data-mode]` selector is a no-op placeholder).

12. **Elevation** · Repo: `--elev-1/2/3` only. · _ds: **six steps `--e0–e5`**, "felt not seen" (wide blur, low opacity), strict assignment rows→cards→hover→popover→modal, hover = exactly +1 step + translateY(−1px). · **RESOLUTION: UX wins — e0–e5 verbatim, including the hover law.**

13. **Radius** · Repo: sm/md/lg/full = 6/10/14/9999. · _ds: dual scales (expressive 8–40, functional 4–16), pill 999 for **all** buttons/chips, card 24/12, sheet-top 32, input 14/10, signature feature tile `999px 999px 32px 32px`. Mockups hard-code px (999 ×540, 12/16/14/24…) rather than using the vars. · **RESOLUTION: UX token set wins wholesale;** implementation uses the tokens even though the mockups inline px (the values match the token families).

14. **Spacing & layout constants** · Repo: space scale 0–24 (4px base) — compatible; ui.md says desktop sidebar **~240px**. · _ds: same 4px scale plus layout tokens — **sidebar 260px** (collapsed 68), header 64, mobile topbar 56, bottom nav 72, content-max 1440, screen-pad pairs per density. · **RESOLUTION: UX wins — adopt the layout token block; correct ui.md's 240px→260px** and the arc-bar description (curved white SVG bar, near-black centre FAB — the 5-slot/role-adaptive behaviour from repo docs is product design and can stay).

15. **Motion** · Repo: 120/200/320ms, ease-out/in. · _ds: adds **500ms ambient** and four easings incl. **spring `cubic-bezier(0.34,1.56,0.64,1)`** (sheets/FAB/segmented), signature motions (press scale 0.97, card fade+rise stagger 40ms max 6, skeleton 1.4s), reduced-motion collapses durations to 1ms. Beware: **the _ds manifest snapshotted the 1ms reduced-motion values as canonical — `motion.css` is the truth (120/200/320/500)**. · **RESOLUTION: UX wins — adopt the superset; never trust `_ds_manifest.json` for values.**

16. **Data colours (N6)** · Repo: `--data-roof-1…8`, `--data-string-1…12`, `--data-scale-0…10`, good/mid/poor — a full engineering-viz namespace. · _ds: **`--chart-1…8`** (colourblind-safe, sampled from the iridescence) + gridline; used in only 7/80 mockups. No roof/string/irradiance scales exist. · **RESOLUTION: N6 (UI colour ≠ data colour) stays as law; `--chart-1…8` becomes the categorical chart palette.** The studio's roof/string/irradiance namespaces are a genuine gap in the UX package — author them as new tokens harmonised with the _ds palette, keeping the deuteranopia + non-colour-channel requirements. "Never chart with brass" becomes "never chart with `--accent`/`--action-primary`".

17. **Token architecture & naming** · Repo: two-layer DTCG — `--c-*` primitive ramps + role-named semantic layer, hand-authored `*.tokens.json`. · _ds: flat kebab tokens (`--sp-*`, `--fs-*`, `--e*`…) + a thin semantic-alias layer (`--bg-page`, `--surface-card`, `--link`…); status uses `-bg` suffix (plus a fifth `neutral` tone), not repo's `-subtle`. · **RESOLUTION: the _ds names are canonical and must survive verbatim** — the mockups reference them; renaming to repo conventions breaks pixel-perfect traceability. The _ds raw values *are* the primitive layer; keep the alias layer; retire the `--c-*` ramp scheme and `-subtle` naming.

18. **Contrast verification** · Repo: build-verified `contrast.pairs.json`, fails the build below floor — but every listed pair is a brass pair. · _ds: no contrast metadata at all, and several shipped combinations computably fail AA as text: `--text-tertiary #A1A5AC` on white ≈ **2.5:1** (used for the signature overline), `--warning #E9A23B` as foreground ≈ **2.2:1**, `--text-secondary #74787E` on white ≈ **4.45:1** (borderline). · **RESOLUTION: the N4 build gate is product law and stays; regenerate the pairs file from _ds values.** Where a pair fails, constrain the role (tertiary = decorative/≥18px-equivalent only) or nudge the value — owner call on posture (below).

19. **Component contract list** · Repo: Radix-based `packages/ui` + **four ported contracts** (NumberField commit-on-blur, DataTable caption, mandatory ariaLabel, focus trap/restore). · _ds: a **21-component library with full prop/variant APIs** (Button primary/secondary/ghost/destructive · lg/md/sm, Card, Chip, StatusChip with the domain status→colour map + dot, Input with borderless focus spec, StatCard, ListRow, EmptyState, OfflineBanner, SegmentedControl, Tabs, Toast…) — but in usage only `Button` is consumed; everything else is hand-rolled to spec. Mockup bug: `variant="danger"` (×1) doesn't exist — canonical is `destructive`. · **RESOLUTION: merge, don't choose.** The _ds inventory + prop enums (as codified in the oxlint allowlists) become the public API of `packages/ui`; Radix stays underneath for a11y plumbing; the four repo contracts are behavioural product law layered on top (they don't conflict — _ds has no NumberField, so the contract fills a gap). Treat the JSX library as spec to implement, not code to import.

20. **Lint enforcement** · Repo: Biome + dependency-cruiser + review; contrast gate in tokens build. · _ds: ships `_adherence.oxlintrc.json` — bans raw hex, raw px, non-Geist fonts, deep component imports, unknown props per component — but everything is **warn-only**, the no-borders law is unenforced, `forbid-elements` is empty, and three exported components have no guards; run over its own `tokens/` it would flag itself. · **RESOLUTION: adopt the _ds rule *intents* at error severity in the repo's own toolchain** (Biome/GritQL plus, if useful, oxlint scoped to JSX source and excluding `packages/tokens`); keep dependency-cruiser and the contrast build gate. Do not inherit the config as-is.

21. **`packages/tokens` build source** · Repo plan: hand-authored DTCG JSON → Style Dictionary → outputs. · Ground truth: the vendored `_ds/tokens/*.css` **is** the pixel-perfect spec, and the package's own manifest already proves re-transcription drifts (1ms durations, miscategorised `kind`s, phantom dark mode). · **RESOLUTION: YES — vendor the `_ds` `tokens/` + `assets/fonts/` into `packages/tokens` and GENERATE from the CSS** (parse the vendored CSS as source of truth → emit `tokens.css` near-verbatim for web, `theme.ts` for RN, `tokens.json` for tooling). Never re-transcribe by hand; never read `_ds_manifest.json` for values. Additions applied at generation, clearly marked as extensions: Noto Devanagari font-face, `--brand-wash`, roof/string/irradiance namespaces, contrast-pairs metadata, and a dark set only if commissioned.

22. **Brand mark & iconography** · Repo: "monogram, not a sun". · _ds: **no logo exists — wordmark is plain Geist Bold; do not invent a mark**; icons are Lucide, outlined, 1.5px stroke, 24/20/28px, circular 6%-tint containers, filled only for active bottom-nav. · **RESOLUTION: UX wins — no monogram, adopt the Lucide spec into ui.md** (currently absent). Bundle Lucide locally; the CDN reference is mockup-only.

---

**ITEMS NEEDING OWNER DECISION** (genuine product calls — everything above defaults to UX-wins):

- **A. Dark mode in v1.** The _ds system is light-only by law and by fact (zero dark tokens; even the 3D studio canvas is light in the mockups, overturning the repo's "theme-invariant dark canvas" doctrine). Options: (1) ship light-only and strike dark from the DoD, (2) commission a dark value set from the UX side first, (3) keep dark as a post-v1 value-set drop. Engineering keeps the door open either way; the DoD and the studio-canvas rule need an explicit ruling now.
- **B. The 11px overline vs the N3 12px floor.** Approve the single named exception (11px/700/uppercase micro-labels, never data or interactive text) or hold the 12px line and render overlines at 12px.
- **C. AA-failing _ds text colours.** `--text-tertiary` (~2.5:1) — which happens to be the overline's specified colour — and `--warning` as foreground (~2.2:1) fail N4 as text; `--text-secondary` sits at ~4.45:1. Choose: restrict these tokens to non-text/large-text roles, or nudge the hex values (a visible deviation from the pixel-perfect spec). This is accessibility-vs-fidelity, not a visual fact.
- **D. Weight 600.** The tokens say 400/500/700; the mockups use 600 two hundred and ten times on dense screens. Sanction `--fw-semibold:600` as a token, or normalise mockups' 600s away at implementation.
- **E. Formal retirement of "Instrument".** Confirm the graphite+brass brand is dead everywhere — including 10-i18n §10's promise that "the operator app is Instrument graphite+brass for every tenant" (the per-tenant-branding *scope* — customer documents only — is unaffected and should stand).

Files compared: `/Volumes/works-space/heliogrid/docs/10-i18n-and-design-system.md`, `/Volumes/works-space/heliogrid/.claude/rules/ui.md`, `/Volumes/works-space/heliogrid/CLAUDE.md` (UI section), `/Volumes/works-space/heliogrid/docs/research/design.md` vs `/Users/devtejas/Downloads/HelioGrid UX/_ds/heliogrid-design-system-c8aa4326-21bf-453a-8d11-749cc81dee12/`.