# Open questions register

> **State as of 2026-09-06 — 83 questions, ZERO open.**
>
> | | |
> |---|---|
> | **`Q53`** | **Resolved 2026-08-26.** The research was done: TCCCPR states no hour, and its time band is a per-recipient preference binding promotional traffic only. The IN pack declares an empty messaging window and a 19:00 tenant-local send hour → `F1-62`. |
> | `Q1`–`Q52`, `Q54`–`Q64`, `Q67`–`Q71` | Resolved by owner ruling. |
> | `Q65`, `Q66` | Resolved by owner ruling 2026-08-26 → `F4-36` / `T-FPLAT-033` and `F4-37` / `T-FPLAT-034`. |
> | `Q68`, `Q70` | Answered by `docs/engineering/data-model.md`, 2026-08-26. |
> | `Q56`, `Q60` | Not owner decisions — implementation-time items for the designer of `SCR-M01-01`. **Both written 2026-08-27** into `docs/ux/briefs/SCR-M01-01-sign-in.md`: the confirmed-hard-failure copy, and a 1.2 s success dwell on both platforms. |
> | `Q72` | Deferred to the `modules/M01` catalog slice; carried verbatim in code meanwhile. |
> | **`Q73`** | **Resolved 2026-08-27 by owner ruling.** The 15-minute lock is the SMS channel, not the account — Google Login stays available → `M01-04`. |
> | **`Q77`** | **Resolved 2026-08-28.** A control is made findable by **RAISING** it, never by outlining it: a control is `--surface` at `--e2`, and whatever holds it steps **down** to `--surface-form` — the page below that at `--canvas`. Measured after the owner found an input on a card at 1.08:1 and a ghost button at 1.00:1 against WCAG's 3:1. `F7-15` stands at two exceptions and `--control-edge` stays `none` — an outline is never the answer here. **Applied 2026-08-31 to every control, not only form fields** — **45 components** across both platforms: fields, buttons, icon buttons, filter and facet chips, language pills, slider steppers, colour swatches, the stepper indicator, a range's end boxes, inline cell editors and **every `Try again` button in the package**. The last group was found by `SCR-M01-07`'s own self-audit, not by a sweep: a name-driven search missed a whole class, and a **name-agnostic** one — white ground, control-sized, `--e1` — is what proves the set is closed. A **disabled** control, a **decorative** chip and a **specimen or listed row** keep `--e1`. Hover lifts to `--e3` because a lift to the resting step is no lift, and focus ADDS a ring rather than replacing the elevation. |
> | **`Q83`** | **Resolved 2026-09-06.** **Tax rounds ONCE, at the currency's minor unit, half away from zero — per component, and every total above it is a sum.** `F1-07`, `F1-31` and `M11-08` fix the unit the money path reconciles to and name no rounding mode; the choice sits in one function, `applyRate` in `packages/domain`, so BOM, proposal, tranche and invoice cannot disagree by a paisa, and a credit carries exactly the negative of its charge. Raised by `T-FCORE-002`. |
> | **`Q82`** | **Resolved 2026-09-02.** **Terms and conditions are a FLOOR on a proposal, not a preference.** `M01-51` lists the sections a tenant configures and names no floor, so `SCR-M01-19` correctly drew Terms as switchable — and a settings screen is exactly where that gets switched off once and forgotten forever. A proposal is a commercial offer; one that carries no terms is the tenant's exposure, not their choice. The section therefore has **no toggle**: the row states the floor by name, permanently and neutrally, which is `ComplianceFloor`'s own case — the control is absent rather than disabled. What a tenant still controls is the WORDING, through the same standard-terms editor. |
> | **`Q81`** | **Resolved 2026-09-01.** A **date follows the MARKET, not the reader** — the pack owns the order, the calendar and the month name, so `12 Aug 2026` renders identically in every UI language. Same class as `BIS`, `ALMM`, `DISCOM` and `kWp`, which `F3-08` already carries in Latin on purpose. Two colleagues in one tenant reading different languages therefore quote a customer the same date, and an exported document does not change shape because of who opened it. The reader's language is never an input to a date. |
> | **`Q80`** | **Resolved 2026-08-31.** A composed sentence is **a whole translated sentence with slots, never a concatenation**: each language supplies its own template and its own list joiner, so word order belongs to the TRANSLATOR rather than to the code. And **a preset supplies a full CLAUSE, not a fragment** — `Viewer` reads *"can look but change nothing"* and joins cleanly, where a fragment could not join a *can-do* list at all. The original wording said the grant line is *"composed from localized capability phrases"*, and that is not enough to build it. Hindi and Marathi put the verb AFTER the list, so `name + "can" + phrases` ships broken Hindi; the joiner, the tail verb and the terminator are per-language data nothing owns — `F3` covers formats (`F3-19`) and catalog copy (`F3-07`), not sentence assembly. And `Viewer` ("look, and change nothing") will not join a *can-do* list in any language. Raised by `SCR-M01-13`'s Devanagari pass, which found it by drawing rather than by asserting. **`F3` gains the rule**: it owns formats (`F3-19`) and catalog copy (`F3-07`) and owned no sentence assembly, which is why the gap had no home. |
> | **`Q79`** | **Resolved 2026-08-31.** An invited employee gets **no language screen** — `M01 §M01.2`'s acceptance corridor stands (invite landing → name and photo → role card → home), and the switch is corridor chrome on `SCR-M01-09`. `F3-03` says every user can **REACH** a picker at first run, not that every user is given one, so a 44px control satisfies it; `SCR-M01-03`'s brief claimed more than the row supports and is amended. Inserting a screen would also spend the under-two-minutes bar (`M01-17`) on a setting the invitee can change in one tap. `SCR-M01-03` remains the signing-up owner's first-run step. |
> | **`Q78`** | **Resolved 2026-08-31.** Where an act cannot be undone by the person who did it, `N8` is satisfied by a confirm that carries the recovery route **in words**, on both the confirm and the after-state — `F7-23` amended. Raised by `SCR-M01-08`: declining an invite voids it and notifies the owner, so there is nothing left for the invitee to undo. A grace window was refused — it holds a destructive write in limbo and delays someone else's notification. |
> | **`Q76`** | **Resolved 2026-08-28.** `F8-01`'s "number" means a **quantity**; an identifier — a tax registration, a bank account, an invoice number, a phone number, a one-time code — carries no tier, and what was checked about it is stated as prose beside it per `F8-03`. Writes down what four screens had already practised. |
> | **`Q74`**, **`Q75`** | **Resolved 2026-08-28.** The typical system size is declared in **`kWp`** — market-neutral, and the 10–30% DC/AC gap that residential hides and C&I does not. Both declarations are edited afterwards in the **company profile, `SCR-M01-05`** → `M01-23`. |
>
> A row is open **only** if its contradiction cell begins with `**OPEN`. No row does. The rows
> stay for their trail, never deleted.

Purpose: decisions V2 still owes an owner ruling. Every "Open questions" section in every
module/foundation PRD is mirrored here so the full set of pending decisions is visible in one
place, independent of which document raised it — and since 2026-08-06 a decision raised as a
contradiction in `registers/conflicts.md` rather than as a module §6 question is mirrored here too,
so this register really is the whole set (see the update note below). **As of 2026-08-04 every question then in this
register — `Q1`–`Q43` — is resolved** — the owner ruled on all 39 open questions in the live
session recorded at `docs/prd/registers/open-questions.md`, and each of those rows now
carries the **"Decision recorded — not open"** label with its ruling, its decision date and any
revisit trigger the ruling names. The register remains the landing place for future questions: a
new question is appended in the same row format, and the raised-question trail below is kept
complete — rows are annotated, never deleted. *(Header updated by the rulings-application pass,
2026-08-04; Task 26's earlier clarifying line about mixed open/recorded rows is superseded — no
row of `Q1`–`Q43` is genuinely awaiting a ruling. That sentence was written unqualified — "no row
is genuinely awaiting a ruling" — and was true of the whole register when written; it is scoped to
`Q1`–`Q43` on 2026-08-06, when `Q44`–`Q46` were appended — and `Q44`–`Q46` are themselves resolved
later the same day, leaving `Q47`–`Q50`, raised by applying those rulings, as the only rows
genuinely awaiting an owner. **`Q47`–`Q50` are in turn resolved the same day** by owner rulings of
2026-08-06, so that last clause is overtaken too: the rows genuinely awaiting an owner are now
`Q51`–`Q55`, raised by applying `Q47`–`Q50`. **That clause is overtaken in its turn, the same
day:** the owner ruled on `Q51`, `Q52`, `Q54` and `Q55`; **`Q53` deliberately stays open** — *(`Q53` has since been closed — owner ruling 2026-08-26 → `F1-62`.)* it is
a regulatory-data gap, not a decision awaiting a preference — and applying those four rulings
raised `Q56`, `Q57` and `Q58`. That clause then read "**The rows genuinely awaiting an owner are
now `Q53`, `Q56`, `Q57` and `Q58` — four**", and it is **overtaken the same day, and this is the
end of the chain:** the owner ruled on `Q57` and `Q58`, and `Q56` is **reclassified** — it was
never an owner decision at all but **screen copy**, which is written when the screen is designed,
so it is retyped as an **implementation-time item** belonging to the designer who draws
`SCR-M01-01`, with the one constraint on it already fixed. That clause read "**What is left in
this register is `Q53` and nothing else — one research task with a named owner, and no owner
decision outstanding anywhere in this suite**", and it is **overtaken on 2026-08-07** by the
UX-readiness pass, which raised `Q59` and `Q60` from the briefs rather than from any module's §6.
Both are closed the same day: `Q59` is an owner ruling, `Q60` is **reclassified** as an
implementation-time item exactly as `Q56` was. **So the clause holds again in its original terms:
what is left in this register is `Q53` and nothing else — one research task with a named owner,
and no owner decision outstanding anywhere in this suite.** See the six update notes directly
below.)*

**Update 2026-08-06 — this register is not empty of owed decisions, and says so.** `00-README.md`'s
document map names this file as the suite's list of "Decisions V2 still owes an owner ruling", so a
reader who opens it must see **every** decision genuinely awaiting an owner, whichever register
first recorded it. Three do not come from a module's §6 Open questions section — they were raised as
contradictions, and `registers/conflicts.md` holds their full statement — so they were invisible
here while the header declared the set empty. They are appended below as **`Q44`–`Q46`** in the
same row format, each pointing at its conflicts row. The division of labour is unchanged: the
conflicts row remains the record of the contradiction; the row here is the pointer to the decision
it owes, so the two registers tell one story. **Nothing is resolved by being recorded** — per the
conflicts register's own header, entries are recorded "**and never silently resolved**" — and no
requirement cell is rewritten by any of these rows. *(That last clause records what the act of
recording did, which is all this note performed, and it stands as the discipline for every row
raised here. It stopped describing these three rows' subject matter later the same day: the owner
ruled on `Q44`–`Q46`, and an owner ruling is the one authority a requirement cell answers to, so
six cells were amended to it — see the second update note.)* Verified against `registers/conflicts.md` on
2026-08-06: rows 1–2 are permanent source gaps rather than owed decisions; rows 3–8 owe nothing
(3 and 4 resolved by design spec §2 `DD2`, and 4 and 8 additionally by the 2026-08-04 ruling
recorded at `Q33`; 5, 6 and 7 are recorded supersessions where the later text already wins); and
row 11's registry gap is closed by the registration `Q24`'s status note records —
`design_survey_superseded` is live in `foundations/F6-notifications-and-search.md` §F6.3 and in
`modules/M05-design-studio.md` §4 — with that row's own annotation the conflicts register's to
carry. What remains genuinely open is **rows 9, 10 and 12**, mirrored below as `Q44`, `Q45` and
`Q46` respectively. *(Superseded later the same day — see the second update note below: the owner
ruled on all three, `Q44`–`Q46` are closed, and no row of `registers/conflicts.md` is awaiting an
owner any more.)*

**Second update 2026-08-06 — the owner ruled on `Q44`, `Q45` and `Q46`; four new questions replace
them.** In a ruling recorded the same day the owner settled all three of the contradiction-raised
decisions this register had just mirrored: **`Q44`** — the OTP resend cooldown is **30 seconds**,
superseding docs/engineering/08's 45 s; **`Q45`** — payment links **send automatically** from the tenant's
connected channel, with copy-paste where none is connected, applying `Q33` to `modules/M11`'s own
rows (money settlement unchanged — the platform never touches tenant funds); **`Q46`** — the
evening-before crew message **rides the transactional lane** like its `F5` siblings. Each row below
now carries the **"Decision recorded — not open"** label with its decision, its date and the
documents it was applied to, and each states what the row previously said so the history survives.
**This is the one case in which a requirement cell may be amended** — an owner ruling is the
authority requirement cells answer to — and the cells amended are exactly the ones the rulings
settle: `M01-04`, `M11-24`, `M11-26`, `M11-52`, `M11-53` and `F5-68`, each recording its own
pre-ruling text. `registers/conflicts.md` rows 9, 10 and 12 carry the matching closure notes with
their contradiction text retained per that register's never-delete rule; **with rows 11, 9, 10 and
12 all closed, no row of the conflicts register is awaiting an owner ruling.** Applying the rulings
exposed **four questions none of them answered**, which are recorded and **not decided** — the
suite does not resolve a contradiction on its own even when it is one step from the answer: `Q47`
(what the user sees when the 30 s resend unlock and the 30 s channel-fallback land together),
`Q48` (whose act the *send* of the plain request message is), `Q49` (the crew message's
message-template registry row) and `Q50` (what "the evening before" resolves to, and whether a
statutory messaging window binds it). Those four are appended below in the same row format and are
**the whole of what this suite owes an owner** as of 2026-08-06. *(That last clause was true when
written and is overtaken later the same day — see the third update note directly below: the owner
ruled on all four, and the five questions raised by applying them, `Q51`–`Q55`, are now the whole
of what this suite owes an owner. **That replacement clause is itself overtaken the same day** —
see the fourth update note: four of those five are ruled (`Q51`, `Q52`, `Q54`, `Q55`), `Q53` stays
open as a regulatory-data gap, and `Q56`–`Q58` are raised by applying the four, so the set owed is
now `Q53`, `Q56`, `Q57` and `Q58`. **And that clause is overtaken in its turn** — see the fifth
update note: `Q57` and `Q58` are ruled, `Q56` is reclassified as screen copy an implementation
pass writes, and `Q53` — a research task, never a preference — is the whole of what is left.)*

**Third update 2026-08-06 — the owner ruled on `Q47`, `Q48`, `Q49` and `Q50`; five new questions
replace them.** In a ruling recorded the same day the owner settled all four of the questions the
`Q44`–`Q46` application had raised. **`Q47`** — there is **no automatic OTP channel fallback at
all**, so the timer collision the question described has no subject left: SMS delivers, the
**resend** control unlocks at 30 s and the user acts. A capability was **REMOVED** (the automatic
secondary messaging-channel OTP) and a different one was **RETAINED** (the user-initiated **"call
me instead"** voice OTP) — the two must not be read as one, and **the alternate-channel story does
not survive as a whole**: what survives is the user's own act, never a system fallback. **`Q48`** —
the **plain payment-request message stays in project scope**, so a read-only rep may send it, while
minting or sending a payment **LINK** still requires `F2.M11.record-payments`. **`Q49`** — the crew
message's message-template registry row is seeded as **`crew_arrival`** (an authoring act applying
`Q46`, not a new decision). **`Q50`** — the crew message's **send hour is market-pack data and
floor-bound**: a pack-declared default inside the market's statutory messaging window, which is a
floor; a slot outside lawful hours goes at the **last lawful moment before it, never after**; a
tenant may **narrow** the window and never widen it. Each row below now carries the **"Decision
recorded — not open"** label with its decision, its date and the documents it was applied to, and
each states what the row previously said so the history survives. **Requirement cells were amended
only where a ruling settles one, each recording its own pre-ruling text:** `M01-03` (and `M01-04`'s
cross-reference to it) by `Q47`, `F6-26` by `Q49`, `F5-68` by `Q49` and `Q50`; `Q48` amended **no**
requirement cell and instead added a capability row, `F2.M11.send-request-message`, to
`foundations/F2` §F2.5-M11. No `SRC` citation was deleted — `M01-03`'s `DOC07.otp-delivery` pointer
is kept with its automatic-fallback half marked superseded. `registers/conflicts.md` rows 9, 10 and
12 each name `Q47`–`Q50` as the new questions their own closures raised; those clauses are overtaken
by these rulings, and correcting them is that register's owner's act. Applying the four rulings
exposed **five further questions none of them answered**, recorded and **not decided** — the suite
still does not resolve a contradiction on its own: `Q51` (what the resend control does inside a hard
delivery-failure state before t=30 s), `Q52` (whether the plain request message's send is an
audit-covered event, and with whose name), `Q53` (the IN pack declares neither a statutory messaging
window nor a send hour, so `F5-68` has no IN value to resolve against), `Q54` (which clock the send
hour is read on) and `Q55` (whether `survey_complete` and any later-seeded key belong in `F6-26`'s
key list). Those five are appended below in the same row format and are **the whole of what this
suite owes an owner** as of 2026-08-06. `Q53` and `Q54` are the two halves of the single
`foundations/F1` §6 bullet `F1-Q3`, split here so each decision carries its own row — the `Q49`/`Q50`
precedent, where one `foundations/F5` §6 bullet became two rows. *(That "whole of what this suite
owes an owner" clause was true when written and is overtaken later the same day — see the fourth
update note directly below: the owner ruled on `Q51`, `Q52`, `Q54` and `Q55`, `Q53` stays open *(`Q53` has since been closed — owner ruling 2026-08-26 → `F1-62`.)* by
the owner's explicit direction, and `Q56`–`Q58` are raised by applying the four. **Overtaken again
by the fifth update note:** `Q57` and `Q58` are ruled, `Q56` is reclassified as an
implementation-time copy item that awaits no owner, and `Q53` alone remains.)*

**Fourth update 2026-08-06 — the owner ruled on `Q51`, `Q52`, `Q54` and `Q55`; `Q53` deliberately
stays open; three new questions are raised.** In a ruling recorded the same day the owner settled
four of the five questions the `Q47`–`Q50` application had raised. **`Q51`** — a confirmed **hard**
SMS delivery failure **releases the 30 s resend cooldown immediately**, with the user-initiated
voice OTP offered in that state; the `M01-04` anti-abuse caps still bind, and a cap already reached
governs instead. **`Q52`** — sending the plain payment-request message **is an audited event,
recorded under the sender's name**; no grant moves. **`Q54`** — the scheduled send hour resolves on
the **TENANT's** timezone (`F1-10`), not the customer's, superseding `Q50`'s *"customer's market
timezone"* wording **on the clock only**. **`Q55`** — `F6-26`'s template-key list is **exhaustive**;
`survey_complete` joins `crew_arrival` in it, and seeding a key is not complete until the list
carries it. Each row below now carries the **"Decision recorded — not open"** label with its
decision, its date and the documents it was applied to, and each states what the row previously
said so the history survives. **Requirement cells were amended only where a ruling settles one,
each recording its own pre-ruling text:** `F2-22` and `M11-07` by `Q52`; `F1-15` and `F5-68` (its
clock clause alone) by `Q54`; `F6-26` by `Q55`. **`Q51` amended no requirement cell** — `M01-03`
and `M01-04` stand exactly as `Q47` and `Q44` left them, and that ruling lands in `modules/M01`'s
§M01.1 acceptance block and edge list, its §5 non-goal bullet, `T-M01-001` and `SCR-M01-01`. No
`SRC` citation was deleted — `F1-15` keeps its `Q50` citation with the *"customer's market
timezone"* clock wording marked **superseded** rather than removed. **`Q53` is NOT closed, and by
the owner's explicit direction it stays open:** the India pack declares neither a statutory
messaging window nor a send hour, and that is a **DATA gap needing real regulatory input (the TRAI
transactional-messaging rules), not a design choice** — a research task with a named owner, never a
decision awaiting a preference. Its row below is sharpened to say exactly that, what data is
needed, and what is floorless without it: the send window of `F5-68`'s crew message, the product's
one scheduled automatic send. **`Q54` does not close it** — `Q54` settles *which clock* the hour is
read on, never *what the hour is*. Applying the four rulings exposed **three questions none of them
answered**, recorded and **not decided** — the suite still does not resolve a contradiction on its
own: `Q56` (what the confirmed-hard-failure message *says* once the retry is available at once),
`Q57` (whether the copy-paste fallback path leaves any audit record at all) and `Q58` (which clock
the statutory messaging **window** is evaluated on). `Q55` raised none, as `foundations/F6` §6
records. Those three are appended below in the same row format and, **with `Q53`, are the whole of
what this suite owes an owner as of 2026-08-06 — four decisions, no more.** Cross-checked against
`registers/conflicts.md` the same day: rows 1–2 are permanent source gaps rather than owed
decisions, rows 3–8 owe nothing, and rows 9, 10, 11 and 12 are all closed — **no row of the
conflicts register is awaiting an owner ruling**, so no open decision reaches this register from
that side. *(The "**four decisions, no more**" clause above was true when written and is overtaken
later the same day — see the fifth update note directly below: the owner ruled on `Q57` and `Q58`,
`Q56` is reclassified as an implementation-time copy item awaiting no owner, and `Q53` — a
research task, not a decision — is the whole of what is left. The conflicts cross-check in this
note is re-run in the fifth and still holds.)*

**Fifth update 2026-08-06 — the owner ruled on `Q57` and `Q58`; `Q56` is reclassified as screen
copy; the register is closed of owner decisions.** In a ruling recorded the same day the owner
settled two of the three questions the `Q51`/`Q52`/`Q54`/`Q55` application had raised, and the
third is reclassified rather than closed because it was never an owner's to decide. **`Q57`** —
the copy-paste **fallback** path leaves **no audit record at all, and none is added**: the log
records what the **product performed**, and what it performs is the send from the tenant's
connected channel (`Q52`); where no channel is connected the product composes the message and
places it on the clipboard while a **person** sends it outside the product, so there is no
compose record, no copy record, no send record — **and no compensating counter, timeline entry or
chase marker either**. **`Q58`** — **one clock**: the statutory messaging **window** is evaluated
on the **tenant's** timezone (`F1-10`), the same clock as the send hour (`Q54`), never the
customer's and never the market's. **The owner chose the simpler option in both, explicitly** —
*"hey keep as much as simple. and dont do overengineering."* — and **neither trade is compensated
for by a mechanism elsewhere; each is recorded honestly instead**, so a later reader sees a
deliberate choice rather than an oversight: for `Q57`, the same chase, to the same customer, about
the same tranche, is **attributable when the product sends it and unrecorded when a person does**;
for `Q58`, where an EPC ever serves a customer in another timezone **both** the hour and the
lawful-window check run on the **EPC's** clock rather than the recipient's, and where a tenant's
timezone differs from its market's default (`F1-21`) the market's statutory window is evaluated on
that tenant's clock. Each row below carries the **"Decision recorded — not open"** label with its
decision in full, its date, the owner's reasoning and the documents it was applied to, and each
states what the row previously said so the history survives. **Requirement cells were amended only
where a ruling settles one, each recording its own pre-ruling text:** `F2-22` and `M11-07` by
`Q57`; `F1-15` (its window clause alone) and `F5-68` (its window clause alone) by `Q58`. **No
`SRC` citation was deleted** — `F5-68`'s `SRC` keeps its `Q50` pointer with the *"customer's
market timezone"* wording marked **superseded** for the window as it already was for the hour.
**Nothing else moves:** no grant, no matrix cell and no covered event is added by `Q57` (it
**bounds** the clause `Q52` added rather than widening it), money settlement is untouched
(`M11-01` unamended, the platform never touches tenant funds), and `Q50`'s floor/default law is
untouched by `Q58` (a tenant may narrow the window and never widen it; a slot outside lawful hours
goes at the last lawful moment **before** it, never after). **`Q56` is RECLASSIFIED, not closed
and not deleted:** *what the confirmed-hard-failure message says* is **screen copy, and copy is
written when the screen is designed** — it is not an owner decision, it never was, and **nobody
should wait on an owner for it**. Its row is retyped as an **implementation-time item** whose
decision belongs to the designer drawing `SCR-M01-01`, with the one constraint on it already
fixed by a ruling that exists: **the message must not say "later" when the retry is available
now** — `M01-03`'s *"retry-later"* wording is **superseded in that state** by `Q51` — and the
brief already carries the annotation where the designer meets it. **`Q53` is NOT closed and stays
exactly as the fourth note sharpened it:** a regulatory-DATA gap needing real TRAI
transactional-messaging input, a **research task with a named owner**, never a decision awaiting a
preference; `Q58` does not close it — it settles which clock the window is read on, never what the
window is, and the IN pack still declares none. **Applying these two rulings raised no new
question** — `foundations/F2` §6, `modules/M11` §6, `foundations/F1` §6 and `foundations/F5` §6
each record the same, and the reclassification of `Q56` raises none by construction. **So, counted
and not estimated: `Q53` is the only row of this register still open, it is a research task, and *(`Q53` has since been closed — owner ruling 2026-08-26 → `F1-62`.)*
no owner decision is outstanding anywhere in this suite.** Cross-checked against
`registers/conflicts.md` the same day, end to end: rows 1–2 are permanent source gaps rather than
owed decisions, rows 3–8 owe nothing, and rows 9, 10, 11 and 12 all carry their closure notes —
**no row of the conflicts register is awaiting an owner ruling**, so no open decision reaches this
register from that side. **Still carrying pre-ruling text in files this register does not own —
recorded, not corrected here:** `modules/M01-onboarding-and-tenant-config.md` §6's Q56 bullet
still reads "**New question raised by the Q51 closure — OPEN, needs a register id**" (the id is
`Q56`, and its type is implementation-time, not open), and *retired: verification report*
carries a dated snapshot naming 42 questions and 9 conflict rows with one "awaiting an owner
value (OTP 30 s/45 s)" — a historical count from before `Q43`, superseded by `Q44`, not a live
open item; and `modules/M11-payments-and-collections.md` §6's `M11-Q2` still describes register
`Q7` as "already open", where `Q7` has been **Decision recorded — not open** since 2026-08-04
(India-first, zero-rated export of services) with a **revisit trigger** at real foreign revenue —
which is what that dependency actually rides, and it is not an owed decision. Each is its own
file's owner's act. **None of the three is an open decision, and correcting them adds none.**

**Update 2026-08-07 — two questions raised by the UX-readiness pass, both closed the same day.**
The pass that gated the suite for screen design read the 152 briefs as a designer would and found
two decisions owed that no module's §6 had raised, because neither is visible from inside a single
PRD document — each only appears when the briefs are read against each other.

- **`Q59` — do dates carry a provenance tier?** `F8-01` says *every* user-visible number carries
  exactly one tier, and `F8-02` fixes the four as canonical, but `F8` never says whether a date is
  such a number — and the briefs had split, some tiering a date and others declaring it an untiered
  record fact. **Ruled**: a recorded date is an untiered record fact and shows when it was
  recorded; a projected date carries a tier. Applied in `docs/ux/claude-design-context.md` N7, which is
  read at the top of every screen-design session, so the split cannot recur.
- **`Q60` — the success-dwell duration on sign-in.** `Q20`'s rebuild acceptance list names
  success-dwell timing as binding, but `SCR-M01-01` carried no post-verification state and no
  duration existed anywhere in the suite, so the moment would simply never have been drawn.
  **Reclassified**, not ruled: the beat's existence is binding and the brief now carries a
  `verified-success-dwell` state; the duration is a value the designer writes and records, under
  the completion contract's own "designer decides" rule. Same treatment as `Q56`.

Neither touches `Q53`, which stays open on its own terms as a regulatory-data gap. *(`Q53` has since been closed — owner ruling 2026-08-26 → `F1-62`.)* The readiness
pass also made `SCR-M03-04`'s `window-refused` state **conditional** on a pack declaring a window —
that is an authoring act applying `Q53`'s existing classification, not a new decision, so it takes
no register id.

**Update 2026-08-07 — `Q62`–`Q66`, and what they are.** Applying `Q61` (the offline removal) to
`docs/tasks/` found that the same-day sweep had cut **five rows whose law was never about
connectivity**. Each was written in offline-era language, which is why a sweep matching on that
language took them; each states something still true of a product that requires a connection, and
in each case **no live row now carries it**. They are recorded as `Q62`–`Q66` rather than
re-instated silently, because putting a deleted row back is an owner's call and not an editor's.
Three of them — `Q62`, `Q63`, `Q64` — **block screens that are already waiting to be designed**,
and their briefs carry dated `UNRESOLVED` notes rather than a citation to a dead row. `Q66` is
different in kind: it is not a row the sweep should have kept but a hole the photograph
**carve-out** created, and `F4-21` and tenant isolation now contradict each other on the same
device. With `Q53` still open on its own terms *(`Q53` has since been closed — owner ruling 2026-08-26 → `F1-62`.)*, the rows genuinely awaiting an owner are
**`Q53` and `Q62`–`Q66`**.

**Update 2026-08-15 — `Q62`, `Q63` and `Q64` are ruled; the rows are restored.** The owner ruled
all three the same way and for one reason: they are not three questions but one, and it was
already answered. **`F8-36` is live P0** — *"does not silently queue, partially apply, or display
an optimistic result"* — so every one of these was already the law in the abstract, with no module
row making it concrete at the surface where it bites. `M11-06` states it for money and `M05-09`
for designs; leaving lead triage and attendance as the two unstated exceptions would have been a
carve-out nobody had decided. Ruling the other way would not have been a simplification, it would
have required amending `F8-36`. The three restored rows are **`M02-66`**, **`M02-67`** and
**`M09-71`** — new ids, because `M02-04`, `M02-26` and `M09-36` genuinely *were* deleted on
2026-08-07 and the struck rows recording that must stay true; each new row names the one it
restores. `SCR-M02-01`, `SCR-M02-02`, `SCR-M02-04` and `SCR-M09-02` are unblocked. The rows
genuinely awaiting an owner are now **`Q53`, `Q56`, `Q60`, `Q65` and `Q66`** — and of those, none
blocks a screen.

Row format:

| Q id | area | question | decision owner |
|---|---|---|---|
| Q1 | Business model / market pricing (`04-business-model.md`, F1 India pack) | **Decision recorded — not open.** Base tiers re-validated and confirmed unchanged (₹1,999/₹3,999/₹9,999/Enterprise); the two V2 add-on slots now carry the owner's **draft** values — tracked seat ≈₹99/seat/mo beyond the tier's included allowance, marketing-send bundles Starter 500 / Growth 2,000 / Pro 10,000 sends/mo with overage ≈ WhatsApp ₹1.5 · SMS ₹0.35 · email ₹0.10 — recorded at `BM-41`/`F1-61` (`BM-22`/`BM-26` noted; `M09-16` presents the seat draft). **All add-on numbers are draft-pending-rate-card-verification** per `BM-17`/`BM-26`. Decision date 2026-08-04. **Revisit trigger: rate-card verification against worst-case unit COGS — the meters are not sellable until it passes.** | Resolved by owner ruling 2026-08-04; revisit at rate-card verification — Owner |
| Q2 | Roles (`foundations/F2`) — design sign-off preset (`02-personas.md` §6 **A**) | **Decision recorded — not open.** Sign-off is an **approval capability of the Design Engineer preset** (`F2-03`, matrix row `F2.M05.approve-designs`: EPC Owner + Design Engineer), not a thirteenth preset. The v1 reviewer-not-author guard is preserved on the capability itself (`F2-04`, with the one-person-tenant edge recorded — the v1 matrix always granted the Owner both). Rationale: DD3 sizes the preset set to the persona set; nothing a v1 `Engineer` could do is lost *(grammar corrected by Task 26)*; grantable stacking stays possible. Revisit trigger: a tenant demonstrably needing reviewer-only staffing — R16's precedent adds a preset in a later release without restructuring (roles are M:N). | Resolved by `foundations/F2` §F2.1 (Task 5); revisit on tenant evidence — Owner |
| Q3 | Roles (`foundations/F2`) — v1 `Manager` fan-out (`02-personas.md` §6 **B**) | **Decision recorded — not open.** **Sales Manager is the v1 `Manager` preset's direct successor** and inherits every v1 matrix grant unchanged (team visibility, add/edit/assign leads, capture surveys, create/send proposals, update stages, record payments + documents, agent performance, company reports). **Project Manager** re-grants the coordinator subset at single-project scope and takes over R16's coordinator checklist duty; **Operations** re-grants it at portfolio scope plus the DD11 catalog grant and field-workforce team visibility. No v1 grant dropped or narrowed (`F2-08`, `F2-26`). | Resolved by `foundations/F2` §F2.1 (Task 5), encoded in the §F2.5 matrices; M08 references, does not re-rule |
| Q4 | Roles (`foundations/F2`) — Installation Team Member login (`02-personas.md` §6 **C**) | **Decision recorded — not open.** R16 is honored verbatim for the v1-derived scope (crew has no login; coordinator runs the checklist; ticks attributed to the coordinator with optional free-text "done by"). V2 adds the **Installation Team Member preset** (`BRIEF` — owner brief names Installation Teams as primary users), exactly the addition R16's consequence pre-authorized ("v2 adds an Installer preset without schema change"). R16's constraint travels as surface law: **no commercial figures, ever, on any surface the preset grants** (`F2-05`–`F2-07`, `F2.M08.installation-checklist`); the coordinator fallback is retained for mixed crews. | Resolved by `foundations/F2` §F2.1 (Task 5); surface obligations land in `modules/M08` + `modules/M09` at authoring |
| Q5 | Multi-role home composition (`02-personas.md` §6 **D**, home half; F2 `F2-Q1`) | **Decision recorded — not open.** F2 resolved the *permission* half: visibility resolves **per domain**, widest-wins inside each domain, never leaking across domains (`F2-14`). The **home-screen** half is resolved by `modules/M13` `M13-10` (Task 23): one person, one home, chosen by a **fixed preset-precedence ladder** ordered by the breadth of the decision surface each home summarises, with `F2-14`'s domain lattice as the input — EPC Owner · Sales Manager · Operations · Project Manager · Marketing · Finance · HR/Admin · Sales Executive · Design Engineer · Survey Engineer · Field Technician · Installation Team Member. The home is the highest-ladder held preset's; every other held preset's today-work composes in as blocks (the source's rep + surveyor example holds — My Day with visits inside, `PS-05`); a switcher lists every held preset's home. The ladder is a product constant; its rationale and the worked `Q5` example are recorded in M13 §M13.2. Revisit trigger: owner preference on the ladder's order — a one-table change in M13, nothing else moves. | Resolved by `modules/M13` §M13.2 (Task 23), with `foundations/F2` `F2-14` as input; ladder-order revisit — Owner |
| Q6 | IN calling rules (`foundations/F1` §F1.3, `pack.calling-rules` — F1 §6 `F1-Q1`) | **Decision recorded — not open.** The ≤30 s disclosure floor is replaced by the **tiered disclosure law** (`F1-36`(d)): at IN launch the agent opens naturally ("I'm Asha from [company]") with **no proactive AI mention**; four hard floors are non-negotiable product law — never claims to be human · never denies being AI when asked (honest answer + immediate human offer) · instant human handoff · full transcription to the timeline. The proactive-disclosure line is **pack data**: IN ships OFF and **auto-flips ON with owner notification when TRAI's AI-caller identification rule binds**; EU-class packs ship ON (AI Act Art. 50). Applied at `M07-10`/`M07-24`/`M07-32` and the OV voice-agent row. Decision date 2026-08-04 (owner ruled after being advised of the regulatory trajectory and brand risk, twice). **Revisit trigger: TRAI finalization — the pack-data auto-flip.** | Resolved by owner ruling 2026-08-04; revisit at TRAI finalization (auto-flip is pack data, owner notified on flip) |
| Q7 | Market expansion (`foundations/F1` `F1-05`, `F1-26`) | **Decision recorded — not open.** India-first commercially; the first foreign subscribers are billed by the Indian entity as zero-rated **export of services** (international gateway mode, USD/AED) — posture at `BM-40`, note in F1 §4; `F1-05`'s per-market gate (privacy/residency determination) is unchanged. Decision date 2026-08-04. **Revisit trigger: real foreign revenue — the merchant-of-record / foreign-entity decision is retaken then.** | Resolved by owner ruling 2026-08-04; MoR revisit at real foreign revenue — Owner |
| Q8 | Data honesty (`foundations/F8` §F8.1 — `F8-Q1`) | **Decision recorded — not open.** The **two-flow simplicity rule**: roof geometry enters via exactly two flows — (1) studio/remote drawing, keeping the existing `derived`/`estimated` tags; (2) physical survey capture = **`measured`, including surveyor-typed instrument readings entered on site**. No phone-typed roof flow exists, no fifth tier, no context machinery; typed figures outside the survey flow keep the weakest applicable tier (`assumed` — `F8-21` now final). Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`F8-Q1`/`F8-21` closed) |
| Q9 | Data honesty (`foundations/F8` §F8.7 — `F8-Q2`) | **Decision recorded — not open.** The four-tier vocabulary does **not** extend to platform usage metering: usage and billing screens use plain **"actual usage"** language (`F8-33`, `M12-34`), and the provenance word "measured" is reserved for engineering/survey data; `F8-02`'s "(on site)" definition is unwidened. Decision date 2026-08-04 (resolved by the same simplicity direction as Q8). | Resolved by owner ruling 2026-08-04 (`F8-Q2` closed) |
| Q10 | Localization (`foundations/F3` §F3.2 — `F3-Q1`) | **Decision recorded — not open.** Tenant content with no version in the reader's language is shown in its **original language with a small note** saying so — a labelled fallback, never silent machine translation, never an unlabelled substitute — and the gap is surfaced to the author (`F3-10`). Decision date 2026-08-04 (BATCH-8). | Resolved by owner ruling 2026-08-04 (`F3-Q1` closed) |
| Q11 | Localization (`foundations/F3` §F3.2 — `F3-Q2`) | **Decision recorded — not open.** The identical Hindi/Marathi "Proposal" rendering is flagged a **likely source typo**: the **translator confirms the canonical renderings at catalog authoring** — closed as a translation-time task — and the per-string retain-English judgements are made in the same authoring pass by the named translation authority (`F3-11`/`F3-12` unchanged). Decision date 2026-08-04 (BATCH-8). | Resolved by owner ruling 2026-08-04; execution with the translation authority at catalog authoring |
| Q12 | Design language / brand assets (`foundations/F7` §6 — `F7-Q1`) | **Decision recorded — not open.** The launch **app icon is a typographic letter-tile** — a bold "H" in the brand face on the near-black brand background, **derived from the wordmark's style, nothing invented**; a **commissioned logo replaces it post-launch** via an ordinary update. This unblocks store submission on both platforms and unblocks Q13; `F7-05`'s no-invention law survives for every other surface, and the `UXG-26`/`UXG-27` dispositions note the resolution. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04; commissioned logo lands post-launch — Owner |
| Q13 | Design language / app boot surface (`foundations/F7` §6 — `F7-Q2`) | **Decision recorded — not open.** Boot/splash = **plain canvas + the HelioGrid wordmark until the Q12 commissioned logo exists**; the Android-12+ icon-on-splash follows the Q12 outcome (the letter-tile now, the commissioned mark later), so the accepted platform divergence stands against a product asset rather than a stock placeholder. One boot colour, no stock vendor branding, no flash across the handoff (`F7-05`, F7 §6). Decision date 2026-08-04 (BATCH-8, tied to Q12). | Resolved by owner ruling 2026-08-04 (with Q12) |
| Q14 | Design language / type system (`foundations/F7` §6 — `F7-Q3`; `foundations/F3` `F3-13`/`F3-14`) | **Decision recorded — not open.** The Devanagari face is **Noto Sans Devanagari (OFL, free)** for HI/MR UI and documents, paired with the brand face; the **design phase confirms weights and pairing** (`F7-14`'s obligations bind it at every sanctioned weight; `F7-41`/`F3-18`'s render check proves it). Closes `F3-13`/`F3-14`'s face obligation. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04; weights/pairing confirmed in design phase — design authority |
| Q15 | Offline boundary (`foundations/F4` §6 — `F4-Q1`) | **Decision recorded — not open.** The offline-capable set is **not extended** to the V2 brief-scoped capabilities: attendance day-start/day-end and geofence events stay **online-first** as final text (`F4-13` records the ruling; `M09-36`/`M09-52` final). The consequence is stated honestly — in a no-signal area the day and the tracking window (Q39) start when signal returns — and **workers are advised via a UI hint**. Site check-in/out and visit logging remain inside the ruled set (`F4-08` row 4). Decision date 2026-08-04. **SUPERSEDED 2026-08-07 by owner ruling `Q61`.** This ruling decided the *shape* of a boundary that no longer exists: with the offline capability removed there is no offline-capable set to extend or withhold, and `F4-13`, which recorded it, is deleted. **What survives is its consequence, now unconditional rather than a no-signal edge case:** an attendance day-start and the tracking window it opens happen when the server has them. **What was lost with it:** `M09-36`, the row that said a day mark reaches the server before it reads as recorded, was deleted in the same sweep and nothing live replaces it — recorded as open question `Q64`. The worker-facing UI hint this ruling required has no surviving carrier either. Nothing here is re-instated by an editor; `Q64` is the decision. | Resolved by owner ruling 2026-08-04 (`F4-Q1` closed; ruled set unchanged) |
| Q16 | Offline boundary / entitlement grace (`foundations/F4` §6 — `F4-Q2`; `foundations/F4` `F4-34`) | **Decision recorded — not open.** Field capture is **never cut off by elapsed offline time**: capture works through the **full billing grace** and pauses only at **`halted`** (a state the device learns on reconnect), with a mid-visit halt letting the **current visit complete and sync** — "never strand a surveyor on a roof". Reads, exports and queue drain are unchanged, always-on (`F4-34`, `M12-27`, `BM-36` all final). Decision date 2026-08-04. **PARTLY SUPERSEDED 2026-08-07 by owner ruling `Q61`.** The half that survives is the one that mattered: field capture is **never cut off by elapsed time**, works through the full billing grace and pauses only at `halted`, with a mid-visit halt letting the current visit finish — live and unchanged at `M12-27`, with the always-on read/export set at `M12-22` and `M12-24`. The half that dies is the mechanism: there is no cached entitlement, no 72-hour device grace, no state "the device learns on reconnect" and no queue to drain, and `F4-34` is deleted. *"Never strand a surveyor on a roof"* still holds as far as billing is concerned; it no longer holds against loss of signal, which is the accepted cost recorded in `Q61`. | Resolved by owner ruling 2026-08-04 (`F4-Q2`/`M12-27` closed) |
| Q17 | Business model / tracked-seat bundling (`04-business-model.md` §6 — BM-Q2) | **Decision recorded — not open.** Higher tiers bundle included tracked seats: **Starter 0 · Growth 3 · Pro 10 · Enterprise custom**, with the draft ≈₹99/seat/mo applying beyond the allowance (`BM-22`, `BM-41`, `M09-16`). The seat price and ladder ride Q1's draft-pending-rate-card-verification status. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04; draft pending the Q1 rate-card check — Owner |
| Q18 | Onboarding / authentication (`modules/M01` §6 — M01-Q1) | **Decision recorded — not open.** Google Login is a **convenience sign-in bound to the SAME phone-identity account**: the phone remains the identity, no duplicate accounts are ever created, and the **linking flow runs at the first Google sign-in** (`M01-02`); signup completes phone verification as part of becoming an account. Decision date 2026-08-04 (BATCH-8). | Resolved by owner ruling 2026-08-04 (`M01-Q1` closed) |
| Q19 | Market packs / demo & seed content (`modules/M01` §6 — M01-Q2; `foundations/F1` `F1-02`) | **Decision recorded — not open.** Every market pack ships **one demo project as pack content** (IN: the Pune-class rooftop, `M01-27`) — versioned with the pack per `F1-11`, beside the eight rules keys rather than as a ninth key (`F1-02` carries the note); the KB seed pack rides the same pack-content family. Decision date 2026-08-04 (BATCH-8). | Resolved by owner ruling 2026-08-04 (`M01-Q2` closed) |
| Q20 | Onboarding / auth-rebuild product laws (`modules/M01` §6 — M01-Q3; extraction ledger UXG parity appendix) | **Decision recorded — not open.** The auth-rebuild parity laws (`UXG-PAR-02`–`05`) are **carried as written into the rebuild acceptance list**: signed-out routing decided once for both platforms; the success-dwell timing; the explicit OTP-submission connectivity contract in `F4-01` vocabulary; visible resend feedback on every platform (`M01-Q3`). Decision date 2026-08-04 (BATCH-8). **PARTLY SUPERSEDED 2026-08-07 by owner ruling `Q61`.** Three of the four parity laws stand untouched: signed-out routing decided once for both platforms, the success-dwell timing (see `Q60`), and visible resend feedback on every platform. The fourth — *"the explicit OTP-submission connectivity contract in `F4-01` vocabulary"* — is superseded: `F4-01` defined the three-term vocabulary (offline-capable / online-first / online-only) and was deleted with the capability, so there is no vocabulary left to state the contract in. OTP submission is simply an online action that fails fast and honestly on failure (`F8-36`). | Resolved by owner ruling 2026-08-04 (parity laws carried into the rebuild acceptance list) |
| Q21 | CRM & leads (`modules/M02` §6 — M02-Q1; ruling `R9.lost`) | **Decision recorded — not open.** **"Not interested" is ADDED to the Lost reasons as the seventh** — price · chose competitor · postponed · not reachable · roof unsuitable · financing failed · not interested — and it carries the **six-month no-call suppression** exactly as R9 intends (`M02-54`; surface at `M07-63`). The disqualify six are unchanged (`M02-42`). The customer-side outcome — *"They should not then be called for six months"* — is delivered by the Lost state. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M02-Q1`/`M07-63` closed) |
| Q22 | CRM & leads / offline boundary (`modules/M02` §6 — M02-Q2; `foundations/F4` `F4-08` row 3) | **Decision recorded — not open.** An offline-sync duplicate the device could not see keeps **both records flagged "possible duplicate"**; the rep resolves via the **standard three-choice dedupe sheet on next open** — never auto-merged, never auto-logged (`M02-04` final). This is also the precedent for any future offline capture path (`modules/M03`, `modules/M09`). Decision date 2026-08-04. **SUPERSEDED 2026-08-07 by owner ruling `Q61`, and its substance is re-opened as `Q62`.** This ruling was framed as an *offline-sync* precedent and `M02-04`, the row it made final, was deleted with the offline sweep. **But the behaviour it ruled on is not an offline behaviour**: two reps entering the same phone number in the same second, both online, is the ordinary case, because `M02-07` runs the live duplicate check strictly *before* the save. So the ruling's answer — both records flagged "possible duplicate", resolved through the standard three-choice sheet on next open, never auto-merged — is still the right answer to a question the product still has, and it now has no live carrier. Re-instating a deleted row is an owner's call, not an editor's, so it is recorded as open question **`Q62`**, which blocks `SCR-M02-01` and `SCR-M02-04`. The sentence above naming this the precedent "for any future offline capture path" is void: there is no offline capture path. | Resolved by owner ruling 2026-08-04 (`M02-Q2` closed) |
| Q23 | CRM & leads / visibility scope (`modules/M02` §6 — M02-Q3; `foundations/F2` `F2-12`) | **Decision recorded — not open.** **Confirmed as specified**: the cross-scope dedupe sheet shows exactly three facts — owner name, stage, last-contact date — with no file access, and cross-scope Open-existing resolves to a request-to-owner (`M02-08` final). No permission widened; `F2-15` stands; binds all three entry points of the one shared sheet. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M02-Q3` closed) |
| Q24 | Survey / design freshness (`modules/M04` §6 — M04-Q1; `modules/M05`, `foundations/F8` `F8-13`–`F8-15`) | **Decision recorded — not open.** When a newer survey supersedes design inputs, the design is marked **"survey updated — review needed"** and the **designer is notified**; **draft proposals on it are blocked from SENDING until the review**; **sent proposals stay pinned** and never mutate (`F8-15`). The same self-stale pattern as catalog releases (`M05-13` final; `M04-66` the survey side; `F5-40` unchanged — the customer sees only ever a new version at the same URL). Decision date 2026-08-04. **Status note (2026-08-06) — the notification leg is now registered and buildable.** The ruling's third clause ("the designer is notified") was undeliverable as ruled: `F6-10` closes the notification registry — an event absent from a module's §4 contract *and* from §F6.3's matrix does not notify — and no event row existed for survey supersession, so the clause named a notification no channel could carry (recorded at `registers/conflicts.md` row 11). That row now exists — event **`design_survey_superseded`**, registering module **M05**, recipient **the design's author (own)** — in `foundations/F6-notifications-and-search.md` §F6.3, registered on the other side of `F6-10`'s cross-check in `modules/M05` §4's contract, emitted at `docs/tasks/MS-studio-a.md` `T-MS-117` and delivered by `docs/tasks/F-platform.md` `T-FPLAT-018`. **This is a registration status note, not a conflict and not a change to the ruling** — nothing owner-approved ever disagreed with the clause; the leg was simply unregistered. All three clauses (marker · designer notified · draft-send block) are now specified end to end. | Resolved by owner ruling 2026-08-04 (`M04-Q1`/`M05-Q2` closed); notification leg registered 2026-08-06 (`design_survey_superseded`) |
| Q25 | Survey / AI detection correction rights (`modules/M04` §6 — M04-Q2; `foundations/F5`) | **Decision recorded — not open.** The corrector of a wrong roof detection is **anyone who can run the remote survey — rep, surveyor or designer** — on the operator surface (`M04-15`), with studio re-verification and provenance labels as the safety net; the homeowner's route stays the link's question affordance, a question and never a mutation (`F5-56` final). The source's "customer" wording is confirmed as meaning the operator. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M04-Q2` closed) |
| Q26 | Design studio / pass two (`modules/M05` §6 — M05-Q1; design spec §2 DD13) | **Decision recorded — not open.** Sequence confirmed: the studio deep-dive (pass two, DD13) **starts when the owner supplies the studio repository**, as its own brainstorm → spec → plan cycle with the census-grounded M05 baseline and Appendix A as the cross-check; the census never shrinks and nothing blocks on the repo. Decision date 2026-08-04 (BATCH-8). | Resolved by owner ruling 2026-08-04 (`M05-Q1` closed); pass two starts on repo delivery — Owner supplies |
| Q27 | Design studio / customer 3D share link (`modules/M05` §6 — M05-Q3; `foundations/F5`) | **Decision recorded — not open.** The 3D live view ships **inside the proposal link** — a **"View in 3D" button on the customer proposal page** (`F5-33`); **no second link exists**; captures/pictures remain the fallback (`M05-55`, `M05-75`; `F5-80`'s one-framework law holds with nothing beside it). The unapproved-design gate binds unchanged (`M05-82`, `F8-29`). Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M05-Q3` closed) |
| Q28 | Design studio / entitlements vs the census's no-tier-gate rule (`modules/M05` §6 — M05-Q4; `modules/M12`) | **Decision recorded — not open.** **Confirmed: zero feature gates in the studio.** The tier kW ceiling is the **only** gate, enforced at Save/Generate — never mid-edit; over-ceiling designs stay readable forever; the census's "coming soon, never upgrade" copy rule stands as law (`M05-12`, `M05-50` final; `M12-20` and the M12 design-kW enforcement row marked confirmed). Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M05-Q4`/`M12-Q2` closed) |
| Q29 | Proposals / OPEX-PPA entitlement gating (`modules/M06` §6 — M06-Q1; `modules/M12`) | **Decision recorded — not open.** The OPEX/PPA proposal type is **UNGATED — available on every tier** (the never-gate-features law holds; the kW ceiling applies naturally); **no proposal-type entitlement key exists** (`M06-06` final; `M12-20`). The former interims are final text. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M06-Q1`/`M12-Q3` closed) |
| Q30 | Sales execution / calling-window scope (`modules/M07` §6 — M07-Q1; `foundations/F1` `pack.calling-rules`) | **Decision recorded — not open.** The **three-lane calling law** (owner-ruled after TRAI purpose-based analysis; `F1-36`(b)): (1) inbound AI answering 24/7; (2) unsolicited/promotional AI dials strictly 09:00–21:00 — statutory floor, no override; (3) the **requested-callback lane** — AI may dial outside the window only on an explicitly recorded, timestamped customer request for that time (transcript/message/rep note), the call opens by referencing the request, the consent trail is stored as evidence, a single "stop" ends the lane, and the lane is product law with per-tenant enable/disable only (`M07-33`). Human reps: **warning-then-proceed** with "customer requested" context logged (`M07-30`; `F5-11`/`F5-54` aligned). *Activation caveat: operator-side consent registration (DCA/DLT) may be required — rides the activation clocks.* Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M07-Q1` closed); lane-3 activation rides the DCA/DLT clocks |
| Q31 | Sales execution / agent-queue cancel asymmetry (`modules/M07` §6 — M07-Q2; `foundations/F2` §F2.5-M07) | **Decision recorded — not open.** Queued AI calls are cancellable by the **queuing rep for their own queued entries** and by the **Owner for anything**; every cancellation is logged to the lead timeline (`M07-35`; `F2.M07.control-agent-queue` widened). Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M07-Q2` closed) |
| Q32 | Projects / the money surface of an operating-expense or power-purchase project (`modules/M08` §6 — M08-Q1; `modules/M06`, `modules/M11`) | **Decision recorded — not open.** The PPA/OPEX project money section shows the **one-time payments from the accepted version** (deposit / connection fee) with the **full tranche toolset**, plus the honest note **"monthly energy billing is handled outside this platform"** — nothing fabricated, nothing hidden (`M08` §M08.6 and `M11-16` final, clause for clause; no recurring invoicing, no meter ingestion — `R17` unchanged). Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M08-Q1`/`M11-Q1` closed) |
| Q33 | Customer link / customer-message send channel (`foundations/F5` §6 — F5-Q1; `modules/M06`, `modules/M03`) | **Decision recorded — not open.** **AUTOMATIC transactional sending**: proposal link, payment link and status updates send from the **tenant's connected official WhatsApp/SMS channel** (the same connection as M03 campaigns), under the utility/transactional template class per pack rules — distinct from the marketing lane — with **composed copy-paste as the fallback** where no channel is connected (no delivery ever claimed there). Supersedes `D32`'s manual-only rule for transactional moments; `registers/conflicts.md` rows 4 and 8 carry the resolution notes; applied at `F5-16`/`F5-28`/`F5-48`, `M03-03`, `M06-53`/`M06-54`/`M06-57`, `M02-33`/`M02-47`/`M02-48`, `M08-38`, `F6-04`/`F6-26`. Decision date 2026-08-04. **Status note (2026-08-06) — the ruling stands; the two source cells that had not caught up now have.** Two `SRC` requirement cells written before the ruling still stated `D32`'s retired rule unscoped: `M11-24`/`M11-26` (the collections copy-link flow) and `F5-68` (the evening-before crew message). They were recorded as their own contradictions (`registers/conflicts.md` rows 10 and 12) and mirrored as open rows here (`Q45`, `Q46`) rather than rewritten, because amending a requirement cell is an owner act. **The owner performed that act on 2026-08-06.** `Q45` rules that payment links send automatically from the tenant's connected channel with copy-paste as the fallback — applying this ruling, whose own text already named "payment link", to `modules/M11`'s rows; `Q46` rules that the evening-before crew message rides the transactional lane like its `F5` siblings. Both cells are amended to those rulings, each recording its pre-ruling text, and conflicts rows 10 and 12 are closed. **`Q33` itself is unchanged by either** — nothing was re-ruled, only applied where the text had lagged. Money settlement remains untouched throughout: the platform never touches tenant funds (`M11-01`, unamended). *(This note previously read that the cells were not rewritten, that each was "mirrored as an open row in this register (`Q45`, `Q46`)", and that `F5-68`'s lane membership "is the owner's to settle" — all three are overtaken by the 2026-08-06 rulings.)* **This is a status note, not a change to the ruling.** | Resolved by owner ruling 2026-08-04 (`F5-Q1` closed; D32's manual rule retired); the two pre-ruling cells amended 2026-08-06 by owner rulings `Q45` and `Q46` |
| Q34 | Customer link / expiry and renewal (`foundations/F5` §6 — F5-Q2; `C.lifecycle.9`) | **Decision recorded — not open.** The customer link **lives forever**: view scopes never expire, and after handover the same URL becomes the customer's **permanent read-only "solar file"** — documents, warranties, referral CTA — for life (`F5-22`, `F5-25`, `F5-70`, `F5-74` final). No self-serve renewal is needed because the late-return failure mode no longer exists; revocation remains the operator's kill switch. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`F5-Q2` closed) |
| Q35 | Marketing / capture identity (`modules/M03` §6 — M03-Q1; `modules/M02` `M02-03`, `M02-13`) | **Decision recorded — not open.** The phone number is **MANDATORY on every connectable lead-capture form** — website, Facebook/Instagram lead forms, every form-bearing channel; a form without a required phone field **cannot be connected** (`M03-19`, `M03-33`, `M03-24`); phone-as-identity is unbroken and the "incomplete lead" path is **not built**. The owner chose data integrity over form-fill rate — deliberately, against the standing recommendation. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M03-Q1` closed; owner ruled against recommendation — deliberate) |
| Q36 | Marketing / messaging consent records (`modules/M03` §6 — M03-Q2; `foundations/F1` `F1-15`, `F1-58`) | **Decision recorded — not open.** The **consent ledger** is the record shape: per contact, per channel class — opt-in **source + timestamp auto-recorded** at the product's own capture points, with a **tenant declaration checkbox** as the recorded basis for imported/legacy lists; opt-outs honored suite-wide; campaign sends **auto-filtered by consent**; **proof one tap** from the contact's record (`M03-34`, `M03-46`; cross-ref at `M02-37`). Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M03-Q2` closed) |
| Q37 | Marketing / campaign audience reach (`modules/M03` §6 — M03-Q3; `foundations/F2` `F2.M02.lead-visibility`, `F2.M03.build-campaign-audience`) | **Decision recorded — not open.** **Segments-yes, files-no**: the Marketing preset gains the **whole-base audience-builder capability** — filters, counts and send-selection across the tenant's full lead base, aggregate-only — with **no individual lead-file read** (`M03-10` final; `F2.M03.build-campaign-audience` widened as a distinct aggregate-only scope; record-level visibility law `F2-12`–`F2-15` unchanged). Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M03-Q3` closed; F2 matrix updated) |
| Q38 | Field workforce / the `DD7` seat boundary (`modules/M09` §6 — M09-Q1; *retired: PRD design note* §2 `DD7`, `04-business-model.md` `BM-22`/`BM-23`) | **Decision recorded — not open.** **Confirmed free-for-all**: attendance, the team visibility board and the activity timeline are included for every worker on every tier — no seat needed; the paid seat is purely the **GPS bundle** (live location, route timeline, geofencing, movement history, activity playback). The `DD7` boundary is closed exactly as `M09-05`'s conservative reading placed it (`M09-05` marked final). Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M09-Q1` closed; M09-05 confirmed) |
| Q39 | Field workforce / the work-hours window (`modules/M09` §6 — M09-Q2; `modules/M10`, `foundations/F1` `pack.data-rights`) | **Decision recorded — not open.** The tracking window is the worker's **day-start tap → day-end tap**, worker-controlled, with a tenant **force-stop backstop — default 20:00, owner-set** — and an **always-visible tracking indicator**; no attendance = no tracking that day (`M09-44` concretized; `M09-13`). The definition stays in M09 — v1 ships no per-employee shift patterns (`M10-29` confirmed) — and `pack.data-rights` still wins where stricter. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M09-Q2`/`M10-Q1` closed) |
| Q40 | Field workforce / location-history retention (`modules/M09` §6 — M09-Q3; `foundations/F1` `pack.data-rights` `F1-23`/`F1-24`, `pack.tax` `F1-13`) | **Decision recorded — not open.** GPS movement-trail retention is **90 days rolling, auto-deleted after**; attendance, visit records and check-ins — the non-GPS business records — are retained unaffected (`M09-57` final). The retention fact is noted per market in `pack.data-rights`, which may set a stricter period. Decision date 2026-08-04. | Resolved by owner ruling 2026-08-04 (`M09-Q3` closed) |
| Q41 | Rulings / `EOD-7` disambiguation (*retired: rulings ledger*; traceability Task 25 block) | **Decision recorded — not open.** `EOD-7` is read as the **voice-agent behaviour rulebook** — the first-class deliverable reflecting the Q6/Q30/Q31 rulings across `modules/M07`'s agent surfaces, which carry the owner's top-priority weighting. **Engineering hand-off note:** the alternative coding-rules reading (the repo's AI-assisted-development rules) is handed to the engineering phase — process content, outside PRD scope. Traceability's `EOD-7` disposition notes the resolution. Decision date 2026-08-04 (BATCH-8, walked through with the owner). | Resolved by owner ruling 2026-08-04; coding-rules reading handed to engineering |
| Q42 | Customer link / acceptance-threshold default placement (`foundations/F5` §6; `foundations/F1` `F1-02`; `modules/M01`) | **Decision recorded — not open.** **OTP-at-accept ships DEFAULT OFF at launch** — no value threshold is set, the feature is a **per-tenant enable in settings**, and no pack key or product default figure is needed (`F5-44` final). The **C&I acceptance risk is recorded honestly** in F5, and **named-link open-attribution plus the acceptance record remain the acceptance evidence** (`F5-46`). Decision date 2026-08-04. **Revisit trigger: post-launch review of the default and any threshold.** | Resolved by owner ruling 2026-08-04; revisit post-launch — Owner |
| Q43 | Business model / grandfathering on lapse (`modules/M12` §6 — M12-Q4; `04-business-model.md` `BM-42`) | **Decision recorded — not open.** **Lapse forfeits**: cancellation or halt ends the 24-month launch-price protection, and reactivation — inside or after the original horizon — prices at the **current list book** (`BM-42` amended; `M12-57` replaced accordingly). Cancellation, dunning and win-back copy must state the forfeiture plainly — the no-surprise rule (`M12-39`, `M12-41`). Decision date 2026-08-04. *(Owner chose against the standing recommendation — deliberate.)* | Resolved by owner ruling 2026-08-04 (`M12-Q4` closed; owner ruled against recommendation — deliberate) |
| Q44 | Onboarding / OTP resend cooldown — 45 s vs 30 s (`registers/conflicts.md` row 9; `modules/M01` `M01-04`, `M01-03`; `docs/engineering/08-security-and-tenancy.md` `DOC08.otp-limits` · *retired: product journey* Stage 0 `S0.wrong.2`) | **Decision recorded — not open.** The OTP resend cooldown is **30 seconds** — the value the journey already promises the user (`S0.wrong.2`, "resend after 30s", then "call me instead") — and `DOC08.otp-limits`' **45 s** resend cooldown is **superseded**; it ships nowhere. The UI resend control respects the ruled 30 s. **Scope: the resend cooldown only.** Every other limit in docs/engineering/08's anti-abuse row is untouched — 3 requests per 15 min and 8 per day per phone, 5 failed verify attempts invalidating the OTP, 3 consecutive invalidations locking the number 15 min with an honest message, per-tenant invite caps — as is `M01-03`'s layered delivery path (SMS → fallback channel → voice OTP), which stood under either value. Applied at `modules/M01-onboarding-and-tenant-config.md` `M01-04` (cell amended, recording its pre-ruling text) and §M01.1's acceptance block (a resend-cooldown criterion added where the block previously asserted no length at all), `modules/M01` §6, `docs/tasks/M01-onboarding.md` `T-M01-001`, and `docs/ux/briefs/SCR-M01-01-sign-in.md` (quote re-synced; the `resend-cooldown` state names 30 s). `registers/conflicts.md` row 9 carries the closure note. Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** A source divergence this suite has deliberately not settled… **not resolved here; picking the number is the owner's act**, and neither source row is rewritten." The owner has now picked the number, and `M01-04` is amended to it.)* **New question raised by applying the ruling — recorded, not decided: `Q47`.** Still distinct from `Q20` (auth-rebuild parity laws) and `Q42` (OTP-at-accept), which are about other things. | Resolved by owner ruling 2026-08-06 (`registers/conflicts.md` row 9 closed; 30 s ruled, docs/engineering/08's 45 s superseded); new question at `Q47` |
| Q45 | Payments & collections / the pre-`Q33` wording surviving in `M11-24`/`M11-26` (`registers/conflicts.md` row 10; `modules/M11-payments-and-collections.md` requirement cells vs the same document's §5) | **Decision recorded — not open.** Payment links **send automatically** from the tenant's connected transactional channel — the same connection `modules/M03` establishes, under the utility/transactional template class per pack rules — with **composed copy-paste as the fallback** where no channel is connected, and **only that fallback path claims no delivery**. This applies owner ruling 2026-08-04 (`Q33`), whose text already named "payment link", to this module's own rows; it is an application, not a new rule. The cells are **amended** accordingly, each recording its pre-ruling text in full: `M11-26` now reads *"The payment link and its request message send from the tenant's connected transactional channel; composed copy-paste is the fallback where no channel is connected"*, with that channel's delivery states shown exactly as it reports them and no further (`F5-28`); `M11-24` now offers the link *"sent from the tenant's connected transactional channel where one exists, with 'Copy payment link' as the fallback where none is"*, copy present on both paths; `M11-52` and `M11-53` follow. **Money settlement is unchanged and was never in question: the platform never touches tenant funds** — `M11-01` stands unamended, collections settle directly from the customer to the tenant's own account, and §5's funds and no-cut bullets are untouched. **Only the message is automated.** Applied at `modules/M11-payments-and-collections.md` (`M11-24`, `M11-26`, `M11-52`, `M11-53`; §M11.4's acceptance block, edge cases and permissions; §1, §2 and §5; §6 `M11-Q3` RESOLVED), `docs/tasks/M11-payments-collections.md` `T-M11-002`, `T-M11-011` and `T-M11-016`, and `docs/ux/briefs/SCR-M11-02-payments-ledger.md` (quotes re-synced, states carrying both paths, the contradiction annotation rewritten as a closure), with a scope note at `docs/ux/briefs/SCR-M11-03-record-payment.md` that nothing on that sheet is amended and nothing there sends. `registers/conflicts.md` row 10 carries the closure note. Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … **Amending a requirement cell is an owner act**, so the cells are left exactly as written and the divergence stays visible; until the owner amends them, `M11-24`/`M11-26` must be read with conflicts row 10." The owner act has happened and the cells are amended.)* **New question raised by applying the ruling — recorded, not decided: `Q48`.** | Resolved by owner ruling 2026-08-06 (`registers/conflicts.md` row 10 closed; `M11-24`/`M11-26`/`M11-52`/`M11-53` amended; money settlement unchanged); new question at `Q48` |
| Q46 | Customer link / lane membership of the evening-before crew message, `F5-68` (`registers/conflicts.md` row 12; `foundations/F5-customer-link.md` `F5-68` + §F5.9, `docs/ux/briefs/SCR-F5-02-link-progress.md`, `registers/screens.md`) | **Decision recorded — not open.** The evening-before crew message **rides the transactional lane**, exactly as its F5 siblings `F5-13`, `F5-16` and `F5-48` do: it **sends automatically the evening before from the tenant's connected transactional channel** under the transactional template class (`M03-03`), with the composed copy-paste text a person sends where no channel is connected — the one path that claims no delivery. Conflicts row 12's reading **(a)** is chosen; reading (b) is rejected, so the brief that argued for the lane was right and is not the thing that had to change. `D32`'s manual-only rule, which the cell still stated unscoped, is retired for this moment too and survives only as the fallback path's no-delivery-claim discipline. The `F5-68` requirement cell is **amended** to the ruling, recording its pre-ruling clause *"the message is composed and sent by a person (`D32`)"*, and §F5.9's acceptance line — which previously tested the four facts only and directed the reader to conflicts row 12 before building — now tests the send on both branches. **The four facts the row makes mandatory — who is coming, when, how long it will take, what disturbance to expect — are untouched by the ruling and stand on the progress page whichever path carried the message.** Applied at `foundations/F5-customer-link.md` (`F5-68`, §F5.9, §6), `docs/tasks/F5-customer-link.md` `T-F5-002` (requirements note and a DONE WHEN criterion on both branches), and `docs/ux/briefs/SCR-F5-02-link-progress.md` (quote re-synced to the amended cell, the `installation-scheduled` state carrying both paths, the annotation rewritten to record the ruling rather than argue for it). `registers/conflicts.md` row 12 carries the closure note. Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … Choosing between them is an owner act — the same discipline row 10 applies to `M11-24`/`M11-26` — so neither the cell nor the brief is amended." The owner has chosen and the cell is amended.)* **Two new questions raised by applying the ruling — recorded, not decided: `Q49` and `Q50`.** **Still to catch up (not owner acts, their own files' owners':** `registers/screens.md`'s `F5-68` non-UI note and `docs/tasks/M03-marketing.md`'s `M03-03` complete-lane-membership block, neither of which had been updated when this row was closed.**)** | Resolved by owner ruling 2026-08-06 (`registers/conflicts.md` row 12 closed; `F5-68` on the transactional lane; cell + §F5.9 amended); new questions at `Q49`/`Q50` |
| Q47 | Onboarding / what the user sees when the resend unlock and the OTP channel-fallback land together at t=30 s (`modules/M01` §6; `M01-03`, `M01-04`; raised by the `Q44` closure) | **Decision recorded — not open.** There is **no automatic OTP channel fallback** — the capability is **REMOVED, not re-timed** — so the t=30 s collision this row described has no subject left: one timer remains, the 30 s resend unlock (`M01-04`, the value ruled at `Q44`). Owner's words: *"keep only resend feature after 30s that button get enabled, and the system should not use any fallback mechanism."* SMS is the delivery channel and nothing is retried behind the user's back; the user's **resend** control unlocks at 30 s and the user acts; a delivery failure fails **loudly** with a plain retry-later message. **A capability was REMOVED and a different one was RETAINED — the alternate-channel story does not survive as a whole.** What is gone is the automatic secondary messaging-channel OTP. What is **kept** is the user-initiated **"call me instead" voice OTP**: the ruling names what *the system* may not do, and a user whose SMS path is broken must still have a way in, so voice OTP stands as **the user's act, never a system fallback**. `M01-04`'s anti-abuse limits are untouched by this ruling (3 per 15 min, 8 per day per phone, 5-fail invalidation, 3-invalidation 15-min lock, per-tenant invite caps), as is `Q44`'s 30 s value. Applied at `modules/M01-onboarding-and-tenant-config.md` `M01-03` (**cell amended**, recording its pre-ruling text in full — *"SMS is the primary channel, with automatic fallback to a secondary messaging-channel OTP where the market's OTP rail provides one, on delivery failure or a 30 s timeout"* — and **keeping** its `SRC` `DOC07.otp-delivery` citation with the automatic-fallback half marked **superseded** rather than deleted; the row's surviving halves still rest on it), `M01-04` (its cross-reference to the layered path amended; no limit changed), §M01.1's acceptance block and edge cases, `modules/M01` §6, `docs/tasks/M01-onboarding.md` `T-M01-001`, and `docs/ux/briefs/SCR-M01-01-sign-in.md`. Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … With the resend cooldown ruled at **30 s** (`M01-04`), it now expires at the same instant as `M01-03`'s automatic channel-fallback timer — *'on delivery failure or a 30 s timeout'*. … **Not decided here** — `M01-03` and `M01-04` each stand exactly as written, and no surface, task or brief invents an ordering." The owner has ruled, and `M01-03` is amended; any annotation anywhere still describing this as open is false as of 2026-08-06.)* **Still carrying pre-ruling text in files this register does not own — recorded, not corrected here:** `registers/conflicts.md` row 9 (still states *"as is `M01-03`'s layered delivery path (SMS → fallback channel → voice OTP)"* and names `Q47` an open new question), `registers/screens.md`'s `M01-03` row, and the retired traceability register's `DOC07.otp-delivery` row. **New question raised by applying the ruling — recorded, not decided: `Q51`.** | Resolved by owner ruling 2026-08-06 (automatic channel fallback **REMOVED**; user-initiated voice OTP **RETAINED**; `M01-03` amended, `M01-04`'s cross-reference amended, no anti-abuse limit and no `Q44` value changed); new question at `Q51` |
| Q48 | Payments & collections / whose act is the **send** of the plain request message (`modules/M11` §6 `M11-Q4`; §M11.4's permissions block, `foundations/F2` §F2.5-M11; raised by the `Q45` closure) | **Decision recorded — not open.** The *send* of the **plain payment-request message** — the message alone, carrying **no** payment link — **stays in project scope**: it is available to anyone holding `F2.M08.project-visibility`, at that holder's own scope, because the plain message carries no money instrument and because keeping it there preserves the case the PRD explicitly wanted — **a read-only Sales Executive chasing their own won deal** (`S8.wrong.3`, `M08-18`). **Minting or sending an actual payment LINK is unchanged and still requires `F2.M11.record-payments`**, a link being a live collection instrument on the tenant's account. The row whose absence raised this question now exists: **`F2.M11.send-request-message`** in `foundations/F2` §F2.5-M11, its cells `F2.M08.project-visibility`'s holders at each holder's own scope (Owner all · Sales Manager team · Sales Executive own won deals · Project Manager own projects · Finance the money scope · Operations portfolio) — **with one cell struck, Installation Team Member `—`**, which is not a narrowing of the ruling but a consequence of a law it did not touch: no surface that preset reaches carries a tranche, an amount owed or any other commercial figure (`F2-06`, `M11-56`, `M08-43`), so the act has no surface to be taken on there. **No requirement cell of §M11.4 is amended by this ruling** — `M11-24`, `M11-26`, `M11-52` and `M11-53` stand exactly as `Q45` left them — and **money settlement is untouched**: `M11-01` stands unamended, the platform never touches tenant funds. Applied at `modules/M11-payments-and-collections.md` (§M11.4's permissions block and its acceptance block, §2's Sales Executive persona bullet, §M11.8's permissions paragraph, §4's `foundations/F2` expectation, §6 `M11-Q4` RESOLVED), `foundations/F2-roles-and-permissions.md` §F2.5-M11 (the new capability row), `docs/tasks/M11-payments-collections.md` (`T-M11-002`, `T-M11-016` and the `M11-26` law entry), and `docs/ux/briefs/SCR-M11-02-payments-ledger.md` (its annotation and its **reader-read-only** state). Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … `Q45` settled that the message **sends**; it did not state whether *sending from the tenant's own official channel* — as distinct from composing and copying it — stays inside that reader scope or needs its own holder set, and `foundations/F2` §F2.5-M11 carries no row for it. **Not decided here**". The owner has ruled and the row exists; both of those statements are now false.)* **New question raised by applying the ruling — recorded, not decided: `Q52`** (raised at `modules/M11` §6 `M11-Q5`, mirrored at `foundations/F2` §6 `F2-Q2`). | Resolved by owner ruling 2026-08-06 (plain request message stays in project scope — a read-only rep may send it; payment **LINK** still `F2.M11.record-payments`; `F2.M11.send-request-message` added to §F2.5-M11; no M11 requirement cell amended, money settlement unchanged); new question at `Q52` |
| Q49 | Customer link / the message-template registry row for the evening-before crew message (`foundations/F5` §6, part (a); `F5-68`, `F6-26`; raised by the `Q46` closure) | **Decision recorded — not open — and it is an authoring act applying `Q46`, not a new decision.** The evening-before crew message has its seeded row in the message-template registry, keyed **`crew_arrival`**, its seeded copy carrying the four facts `F5-68` fixes — who is coming, when, how long it will take, what disturbance to expect — **plus the crew lead's name and number**. The precedent the ruling names is `survey_complete`, seeded when `Q24` was applied; the same shape as the registry gap `registers/conflicts.md` row 11 recorded and closed by authoring the row. `F6-26`'s key list gains it beside `proposal_share` · `follow_up_nudge` · `payment_reminder` · `visit_confirmation` · `handover`, and `F5-68` now names the row. The **hour** the message goes out is never this registry's — it is market-pack data under the same day's `Q50` — so `F6-04`'s no-send-channel-of-its-own posture is untouched. Applied at `foundations/F6-notifications-and-search.md` `F6-26` (cell amended, recording its pre-ruling five-key list in full), §F6.3/§F6.6 and §4's `foundations/F5` contract line, `foundations/F5-customer-link.md` `F5-68` (cell amended and its `SRC` column naming the row) and §6 (`Q49` RESOLVED), `docs/tasks/F-platform.md` `T-FPLAT-021` (the registry's seed) and `docs/tasks/F5-customer-link.md` `T-F5-002` (the send), and `docs/ux/briefs/SCR-F5-02-link-progress.md`. Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … `Q46` named the **lane** and the template **class**; it did not name a registry row for the crew message, and no row for it exists. **Not decided here** — authoring the registration is a `foundations/F6`/owner act, and no default is invented in F5." Both of those statements are now false: the row exists and `F5-68` names it.)* **`F5-68`'s four facts and its two-branch send are unaffected, exactly as this row said they would be.** **New question raised by applying the ruling — recorded, not decided: `Q55`** (raised at `foundations/F6` §6 `F6-Q4`). | Resolved by owner ruling 2026-08-06 (`crew_arrival` seeded in `F6-26`, `F5-68` names it; an authoring act applying `Q46`, not a new decision); new question at `Q55` |
| Q50 | Customer link / what *"the evening before"* resolves to for an automatic send, and whether the market's statutory messaging window binds it (`foundations/F5` §6, part (b); `F5-68`, `F5-11`, `F1-15`, `F1-17`; raised by the `Q46` closure) | **Decision recorded — not open.** The send hour is **market-pack data, and it is floor-bound.** The pack declares the hour as a **default** — early evening in the customer's market timezone — sitting inside the market's **statutory messaging window, which is a floor** (`F1-15`, `F1-17`). Three consequences are ruled, not inferred: a configured slot falling **outside lawful hours sends at the last lawful moment BEFORE it, never after**; a **tenant may narrow the window and never widen it**; and **no tenant setting and no platform support action configures around a floor item**. So the answer to the second half of the question is yes — where a market declares a messaging window, it binds this transactional crew message. Applied at `foundations/F5-customer-link.md` `F5-68` (cell amended, recording that it previously named **no send hour** and left *"the evening before"* unresolved with no relationship stated to the compliance ruleset behind `F5-11`), §F5.9's acceptance line and edge cases, §5's non-goals, §4's `foundations/F1` contract line and §6 (`Q50` RESOLVED); `foundations/F1-global-market-framework.md` (`F1-15`'s floor/default classification; §6 `F1-Q3`); `foundations/F6-notifications-and-search.md` `F6-26` and §4's `foundations/F1` row (the hour is never the registry's); `docs/tasks/F5-customer-link.md` `T-F5-002` and `docs/tasks/F-platform.md`; and `docs/ux/briefs/SCR-F5-02-link-progress.md`. Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … An automatic send has to resolve **an hour and a timezone**, and its relationship to the market's communications-compliance ruleset behind `F5-11` (`F1-15`, `F1-17`) … Whether a statutory messaging window binds a transactional crew message at all is part of what is owed. **Not decided here**". The owner has ruled on the hour's home and on the window. **The timezone half is not fully answered** — the ruling reads the hour on the customer's market timezone while `F1-10` runs schedules on the tenant's — and that residue is recorded at `Q54`, not decided.)* **`F5-68`'s four facts and its two-branch send are unaffected, exactly as this row said they would be.** **Two new questions raised by applying the ruling — recorded, not decided: `Q53` and `Q54`** (both raised at `foundations/F1` §6 `F1-Q3`, mirrored at `foundations/F5` §6). | Resolved by owner ruling 2026-08-06 (send hour = pack data, a default inside a statutory-window **floor**; outside lawful hours → last lawful moment **before**, never after; tenants may narrow, never widen); new questions at `Q53`/`Q54` |
| Q51 | Onboarding / what the resend control does inside a hard SMS delivery-failure state before t=30 s (`modules/M01` §6; `M01-03`, `M01-04`; raised by the `Q47` closure) | **Decision recorded — not open.** A **confirmed hard SMS delivery failure releases the 30 s resend cooldown immediately** — the moment the SMS rail reports a hard failure the cooldown is released and the user may act at once, with nothing left to wait out — and the user-initiated **"call me instead" voice OTP is offered in that same state** (`M01-03`, `M01-04`). The rationale the owner accepted: the cooldown exists to stop resends while a message is still **in flight**, and once the network confirms failure nothing is in flight. **The release is of the cooldown only — every `M01-04` anti-abuse cap is unchanged and still binds** (3 requests per 15 min and 8 per day per phone, 5 failed verify attempts invalidating the OTP, 3 consecutive invalidations locking the number 15 min, per-tenant invite caps); where a cap is already reached **the cap governs the state instead** and the message says so honestly. `Q44`'s **30 s** value is untouched for the ordinary in-flight case, and `Q47`'s removal of the automatic channel fallback is untouched — the voice OTP offered here is still **the user's own act, never a system fallback**. **No requirement cell is amended by this ruling:** `M01-03` and `M01-04` stand exactly as `Q47` and `Q44` left them, and the ruling lands in the surfaces that state behaviour. Applied at `modules/M01-onboarding-and-tenant-config.md` §M01.1's acceptance block (a hard-failure criterion added where the block previously asserted nothing about the resend control inside the failure state) and its edge-case list, §5's no-silent-degradation non-goal bullet (extended with the release clause, having previously ended at "a loud, plain retry-later message"), and §6 (register `Q51` RESOLVED); `docs/tasks/M01-onboarding.md` `T-M01-001` (a DONE WHEN copied verbatim from the amended acceptance block, carrying the cap-already-reached branch); and `docs/ux/briefs/SCR-M01-01-sign-in.md` (the `resend-cooldown` state now stating that it does not render once a hard failure is confirmed, `call-me-instead` offered immediately in that state, `delivery-failed-retry-later` carrying the available resend control and the caps that still bind, and the removed-state note re-scoped). Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … It is not stated whether the loud-failure state **releases that cooldown early** — the user's only remaining recovery becoming usable at once — or whether the user waits out the 30 s before the retry-later message can be acted on; nor whether the user-initiated **'call me instead'** voice OTP is offered immediately in that state. … **Not decided here** — `Q47` settled that no fallback exists, not the cooldown's behaviour inside the failure state." The owner has ruled on both halves; any annotation anywhere still describing this as open is false as of 2026-08-06.)* **New question raised by applying the ruling — recorded, not decided: `Q56`** (raised at `modules/M01` §6). | Resolved by owner ruling 2026-08-06 (a confirmed hard delivery failure releases the 30 s cooldown at once, voice OTP offered in that state; anti-abuse caps unchanged and still binding; no requirement cell amended); new question at `Q56` |
| Q52 | Payments & collections / is the **send** of the plain request message an audit-covered event, and with whose name (`modules/M11` §6 `M11-Q5`; `foundations/F2` §6 `F2-Q2`, `F2-22`; §M11.4's permissions block; raised by the `Q48` closure) | **Decision recorded — not open.** The *send* of the **plain payment-request message** from the tenant's connected official channel **IS an audit-covered event, and the entry is recorded under the name of the person who sent it** — binding every holder of `F2.M11.send-request-message`, the project-visibility-only reader such as a Sales Executive chasing their own won deal (`Q48`) included. The rationale the owner accepted: the message leaves the company's official channel and reaches a customer about money, which is exactly what the audit log exists for, and the record answers *"who messaged my customer?"* instantly. **The entry records the act and its sender — it is not a delivery state and claims none** (`M11-26` and `modules/M11` §5's no-fabricated-delivery-state bullet both unchanged); composing and copying write no entry, the **send** being the audited act. **No permission cell moves and no grant changes** — `Q48`'s holder set stands, and this ruling settles only the audit obligation on an act `Q48` had already placed. **Money settlement is untouched and was never in question:** `M11-01` stands unamended, the platform never touches tenant funds. Applied at `foundations/F2-roles-and-permissions.md` `F2-22` (**cell amended**, recording what its covered-events checklist previously enumerated), §F2.4's acceptance block, §F2.5-M11's `F2.M11.send-request-message` row and the notes beneath it, and §6 `F2-Q2` (RESOLVED); `modules/M11-payments-and-collections.md` `M11-07` (**cell amended**, recording its pre-ruling text), §M11.1's and §M11.4's acceptance blocks, §M11.4's and §M11.8's permissions blocks, §4's `foundations/F2` expectation, §5's no-fabricated-delivery-state scope note and §6 `M11-Q5` (RESOLVED); `docs/tasks/M11-payments-collections.md` (`T-M11-002`, `T-M11-009`, `T-M11-016` and the `M11-26` law entry); and `docs/ux/briefs/SCR-M11-02-payments-ledger.md`. Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … So the product now performs an outbound act in the tenant's name for a holder who writes no money, and no document states whether that act is written to the audit log with its actor. The ruling did not address it. **Not decided here** … The answer lands as a cell edit to `F2-22`." The owner has ruled, and the answer landed exactly where that row said it would — at `F2-22`, reciprocated at `M11-07`.)* **New question raised by applying the ruling — recorded, not decided: `Q57`** (raised at `foundations/F2` §6 `F2-Q3`, mirrored at `modules/M11` §6 `M11-Q6`). | Resolved by owner ruling 2026-08-06 (the send IS audited, under the sender's name; `F2-22` and `M11-07` amended; no grant moves, no delivery claim, money settlement unchanged); new question at `Q57` |
| Q53 | Market packs / the IN pack declares neither a statutory messaging window nor a send hour — **a regulatory-DATA gap, not a design choice** (`foundations/F1` §6 `F1-Q3`(a); `F1-15`, `F1-17`, `F1-25`, `F1-36`, `F1-38`, F1 §5; `F5-68`; raised by the `Q50` closure) | **Decision recorded — not open.** The research this row asked for was done 2026-08-26 against the TCCCPR gazette amendment of 12 Feb 2025, and it answers the row by dissolving its premise: **TCCCPR states no hour anywhere**, and its "time band" is a preference each recipient registers with their own access provider, enforced by DLT and applying to **promotional traffic only** ("except service and transaction type of Commercial Communications"). The owner rules (2026-08-26): the IN pack declares an **empty messaging window** — an authored value, not a missing one — and a **send-hour default of 19:00 tenant-local**. Landed as `F1-62` (§F1.3), claimed by `T-FCORE-004`. Transactional sends (`F5-68`, `M12-39`) are unconstrained by time; promotional sends are constrained per recipient by DLT, which the product never models. **The research is not a legal opinion — confirm with counsel before relying on it for a compliance claim**; the product behaviour is safe either way, because it enforces nothing it cannot know. *Originally raised as a research task by the `Q50` closure.* *(Nature made explicit 2026-08-06 by the `Q51`/`Q52`/`Q54`/`Q55` closure pass, on the owner's direction that this row stay open and say what kind of open it is. The row's original statement is preserved below, unchanged.)* **Nobody settles this by choosing.** There is no product judgement to exercise and no preference to elicit: the answer is **external regulatory fact** that must be researched and then authored into the India pack. **What data is needed:** (i) the **statutory messaging window** for IN — the lawful hours in which a transactional customer message may be sent, the messaging counterpart of the voice floor `F1-36` already carries; and (ii) the pack's **default scheduled-send hour** for an automatic transactional message, a concrete time sitting inside that window. **Where the data comes from:** the **TRAI transactional/commercial-messaging rules** and the DLT sender/template-registration regime `F1-38` already names for IN — real regulatory input, verified, not inferred from the voice lanes and not read across from another market. **Named owner:** the **Owner**, as `foundations/F1` pack authoring under `F1-25`'s owner-authors-the-values law — the same authority that authored every other IN pack value. **What is floorless in the product without it:** `F5-68`'s **evening-before crew message** — the product's one scheduled automatic send at launch — has **no IN send hour to resolve against and no IN window to be floor-bound by**. `Q50`'s ruling that the hour is *"a pack-declared default inside the market's statutory messaging window, which is a floor"* and that a slot outside lawful hours goes at *"the last lawful moment before it, never after"* has, on the IN side, **nothing to evaluate**: no default to place and no floor to clamp to. `Q54`'s tenant clock says which clock to read, not what to read on it, and `Q58`'s window-clock question has no window to place. *(Reference kept current, status unchanged: `Q58` is itself ruled later the same day — the window is evaluated on the tenant's clock, `F1-10`, the same clock as the hour — so it is no longer a question; that changes nothing here, because it settles which clock the window is read on and never what the window IS, and there is still no IN window to read on any clock. **This row stays open, and stays a research task.**)* **The send itself is specified and built** (`docs/tasks/F5-customer-link.md` `T-F5-002`, `docs/tasks/F-platform.md` `T-FPLAT-021`, `docs/ux/briefs/SCR-F5-02-link-progress.md`) — it is only the *when* that is unfloored, and until the pack declares both values **nothing downstream may assume an IN send hour or an IN messaging window exists**. *(Original statement, unchanged:)* Raised by applying `Q50` and deliberately not decided by the pass that applied it; one of the two halves of the single `foundations/F1` §6 bullet `F1-Q3`, split here so each decision carries its own row (`Q54` is the other half) — the `Q49`/`Q50` precedent. `Q50` makes the scheduled-send hour **pack data** and the messaging window a **floor**, but §F1.3 carries only the IN **voice** floor (`F1-36`'s 09:00–21:00 lanes) and the IN messaging **registration** regime (`F1-38`): it declares **no statutory messaging window and no send hour**. *"Early evening"* is a default's description, not a time. Until the IN pack declares both, `F5-68`'s send hour has **no IN value to resolve against**. **Not decided here** — authoring pack values is an owner act, and choosing one is barred to this suite by `F1-25`'s owner-authors-the-values law and F1 §5's no-invented-numbers rule; no hour and no window is invented in `foundations/F1`, `foundations/F5` or anywhere downstream. `F5-68`'s four facts, its two send branches and `Q50`'s floor/default classification hold either way. Recorded 2026-08-06 by the `Q50` closure pass. **Explicitly NOT closed by the same day's `Q54`**, which settles *which clock* the hour is read on and never *what the hour is*; `foundations/F1` §6 `F1-Q3`(a) and `foundations/F5` §6 both say so. **SCOPE CORRECTED 2026-08-16 by the V1 readiness audit — this row reaches further than it says, and the reach is inside V1.** The statement above closes with `F5-68`'s crew message as the one thing left floorless. That was true when written and is not true now. **`modules/M12-platform-billing.md` is V1 build block 2 since the 2026-08-16 scope widening, and it sends automatic scheduled transactional messages on a day index:** `M12-39`'s dunning ladder fires at day 0, 2, 4, 6 and 7, and `M12-42`'s trial nudges at day 7, 12 and 14 — both, per `M12-40`, over **SMS on registered templates (`F1-38`)**, the same DLT regime this question is about. `F1-15`'s own wording already covers them: a pack declares *"the **statutory messaging window** together with the **scheduled-send hour** of each automatic transactional message the product sends on a schedule"*. A dunning rung is exactly that. **What this does NOT settle, and must not be read as settling:** `M12-40` states that *"D32 constrains tenant→customer messaging only — it does not restrict the platform messaging its own tenants"*. `D32` and the statutory window are different instruments, and whether India's transactional-messaging time restrictions bind a platform→tenant SMS is part of the research this row already asks for — not something an editor may infer. **The V1 design consequence, which holds either way:** the send hour of every dunning and trial-nudge rung is **pack data (`F1-15`, `F1-17`), never screen copy**, so `SCR-M12-02` and `SCR-M12-03` render it as a value the pack supplies and must not hard-code a time. That is drawable today. Only the lawful-window refusal, if one turns out to apply, waits on the research. | Resolved by owner ruling 2026-08-26 → `F1-62`, `T-FCORE-004`; confirm with counsel before any compliance claim — Owner |
| Q54 | Market packs / which clock the scheduled send hour is read on — the tenant's timezone (`F1-10`) or the customer's market timezone (the `Q50` wording) (`foundations/F1` §6 `F1-Q3`(b); `F1-10`, `F1-21`; `F5-68`; raised by the `Q50` closure) | **Decision recorded — not open.** The scheduled send hour is read on the **TENANT's timezone (`F1-10`), never the customer's** — so `Q50`'s *"early evening in the customer's market timezone"* wording is **superseded on the clock, and on the clock alone**. Everything else `Q50` ruled stands untouched: the hour is still pack data, the statutory messaging window is still a **floor**, a slot outside lawful hours still resolves to the **last lawful moment before it and never after**, and a tenant may still **narrow** the window and never widen it. **The owner's reasoning, recorded so a later reader sees a deliberate simplification rather than an oversight:** a solar EPC's customers are local to it, so the tenant's clock and the customer's are **the same clock in practice**. **The accepted consequence, stated honestly:** where an EPC does serve a customer in another timezone, that customer receives the message at the **EPC's** evening hour. There is no per-customer clock, and no setting for one. Applied at `foundations/F1-global-market-framework.md` `F1-15` (**cell amended** — its scheduled-send default clause now names the tenant's clock and carries the reasoning and the accepted consequence in the clause itself, recording the `Q50` wording it replaces; the `SRC` column keeps the `Q50` citation with its clock wording marked **superseded**, nothing deleted), §F1.2's edge cases and acceptance block, §4's `foundations/F5` row and §6 `F1-Q3`(b) (RESOLVED); `foundations/F5-customer-link.md` `F5-68` (**cell amended a third time — the clock clause only**; its four facts and its two send branches are untouched), §F5.9's acceptance line and edge cases, §5's non-goals, §4's `foundations/F1` contract line and §6 (register `Q54` RESOLVED); `foundations/F6-notifications-and-search.md` `F6-26`, §F6.6's behaviour detail and §4's `foundations/F1` row (the hour was never this registry's, and now nor is its clock); `docs/tasks/F5-customer-link.md` `T-F5-002` and `docs/tasks/F-platform.md` `T-FPLAT-021`; and `docs/ux/briefs/SCR-F5-02-link-progress.md`. Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … The ruling reads the hour on **the customer's market timezone**, while `F1-10` makes every user-facing schedule run on the **tenant's** timezone … Where a tenant's timezone and the customer's market timezone differ, the two point at **different clocks for the same send**, and no document chooses between them. **Not decided here**". The owner has chosen, and `F1-15` is amended to it.)* **This ruling does NOT close `Q53`** *(`Q53` has since been closed — owner ruling 2026-08-26 → `F1-62`.)* — it settles *which clock* the hour is read on, never *what the hour is*, and the IN pack still declares no send hour and no messaging window. **New question raised by applying the ruling — recorded, not decided: `Q58`** (raised at `foundations/F1` §6 `F1-Q4`, mirrored at `foundations/F5` §6). | Resolved by owner ruling 2026-08-06 (send hour read on the **tenant's** timezone `F1-10`, superseding `Q50`'s customer-market-timezone wording on the clock only; a deliberate simplification with its cross-timezone consequence accepted; `F1-15` and `F5-68`'s clock clause amended); `Q53` untouched; new question at `Q58` |
| Q55 | Notifications / whether `survey_complete` and any later-seeded key belong in `F6-26`'s key list (`foundations/F6` §6 `F6-Q4`; `F6-26`; `docs/tasks/F-platform.md` `T-FPLAT-021`; raised by the `Q49` closure) | **Decision recorded — not open.** `F6-26`'s template-key list is **EXHAUSTIVE, not illustrative**: every key the platform seeds appears in it, so that one row answers *"what messages does this product send to customers?"* — and **seeding a key anywhere, by any later ruling, task or module, is not complete until the key is added to the list**. `survey_complete` (seeded when `Q24` was applied; the post-survey promise-with-a-date of `F5-14`) and `crew_arrival` (seeded by `Q49`; the evening-before crew message of `F5-68`) are **both named in the list itself**, so the asymmetry this row recorded — two seeded keys recorded two different ways, one in `F6-26` and one only under `docs/tasks/` — no longer exists. **No key is removed and no seeded copy changes**, and the tenant-extendable clause, the `Q33` send-rail sentence with its fallback no-delivery-claim discipline, and the *"the hour is never this registry's"* sentence are all untouched. Applied at `foundations/F6-notifications-and-search.md` `F6-26` (**cell amended**, recording in full the six-key list it carried immediately before — `proposal_share` · `follow_up_nudge` · `payment_reminder` · `visit_confirmation` · `handover` · `crew_arrival` — and the undeclared status the row previously gave it), §F6.6's behaviour detail (a paragraph stating the list is the whole seeded key set) and its acceptance block (the seeded starter set tested against the list for identity), §5's matching non-goal, and §6 `F6-Q4` (RESOLVED); and `docs/tasks/F-platform.md` `T-FPLAT-021` (a DONE WHEN comparing the keys this registry seeds with `F6-26`'s list, `survey_complete` included). Decision date 2026-08-06. *(This row previously read "**OPEN — awaiting an owner ruling.** … With **`crew_arrival`** now named in that list, **two seeded keys are recorded two different ways**, and `F6-26` alone no longer tells a reader whether the list is the registry's whole key set or only its original five. **Not decided here** — whether `survey_complete`, and any key seeded later, belongs in the list is a `foundations/F6`/owner act." The owner has ruled that it does belong, and that the list is the whole set.)* **No new question is raised by applying this ruling** — `foundations/F6` §6 records the same. | Resolved by owner ruling 2026-08-06 (`F6-26`'s key list is exhaustive, not illustrative; `survey_complete` added beside `crew_arrival`; seeding a key is not complete until the list carries it); no new question |
| Q56 | Onboarding / what the confirmed-hard-failure message **says**, now that the retry is available immediately in it (`modules/M01` §6; `M01-03`, `M01-04`; `docs/ux/briefs/SCR-M01-01-sign-in.md`'s `delivery-failed-retry-later` state; raised by the `Q51` closure) | **NOT AN OWNER DECISION — RECLASSIFIED 2026-08-06 as an IMPLEMENTATION-TIME item. No owner ruling is owed on this row, none is pending, and nobody should wait on one.** *(Reclassified — not closed and not deleted — by the `Q57`/`Q58` closure pass, on the owner's direction. The row's original statement is preserved in full below, unchanged.)* **This is screen copy, and copy is written when the screen is designed.** The decision belongs to **the designer who draws `SCR-M01-01`** (`docs/ux/briefs/SCR-M01-01-sign-in.md`), in the ordinary course of designing the sign-in screen — there is no product judgement here for an owner to exercise and no preference to elicit, only the wording of one failure message. **The one constraint on it is already fixed, by a ruling that exists:** the message **must not say "later" when the retry is available now**. `Q51` makes the resend control available immediately in the confirmed-hard-failure state — nothing to wait out — and offers the user-initiated "call me instead" voice OTP in the same state, so `M01-03`'s *"retry-later"* wording is **superseded in that state by `Q51`** and cannot be the copy. The brief's state name `delivery-failed-retry-later` is a state id that **follows** the copy and never leads it. **The brief already carries the annotation where the designer will meet this:** `docs/ux/briefs/SCR-M01-01-sign-in.md`'s `delivery-failed-retry-later` state, as the `Q51` closure pass left it, states that on a confirmed hard delivery failure the resend control is available immediately — no cooldown countdown, nothing to wait out — that the voice OTP is offered there too, and that `M01-04`'s anti-abuse caps still bind so a cap already reached governs and the state says so honestly. That is the whole of what the copy must satisfy; the designer writes the words. **Nothing in the product waits on this and nothing is unspecified because of it:** the behaviour is ruled, built and testable on any wording (`M01-03`, `M01-04`, §M01.1's acceptance block and edge list, `docs/tasks/M01-onboarding.md` `T-M01-001`). **Still carrying the earlier typing in a file this register does not own — recorded, not corrected here:** `modules/M01-onboarding-and-tenant-config.md` §6 still heads this bullet "**New question raised by the Q51 closure — OPEN, needs a register id**"; the id is `Q56`, and its type is implementation-time, not open — that file's owner's act. *(Original statement, unchanged:)* **OPEN — awaiting an owner ruling.** Raised by applying `Q51` and deliberately not decided by the pass that applied it. The confirmed-hard-failure state now carries two things the PRD words in opposite directions: `M01-03` says that failure *"fails loudly with a plain **retry-later** message"*, while `Q51` makes the retry available **immediately** in that same state — the resend cooldown released and the user-initiated voice OTP offered. What the message actually **says** is therefore unstated: whether the loud-failure copy keeps its retry-*later* framing while a live resend control sits beside it, or whether the copy names the now-available act and the state's own name (`delivery-failed-retry-later`) follows it. **Not decided here** — `Q51` settled *when the user may act*, not *what the failure message says*; `M01-03`'s "retry-later" wording and the brief's state name each stand exactly as written, and no surface, task or brief invents a replacement. Nothing in the product depends on the answer today: the behaviour is specified and testable on both readings, only the copy is unsettled. Recorded 2026-08-06 by the `Q51` closure pass. *(That original statement's substance survives the reclassification intact — the copy is genuinely unsettled, and nothing depends on it. What changed on 2026-08-06 is its **type**: the row was typed "**OPEN — awaiting an owner ruling**" with decision owner "**Open** — Owner", and it is neither open nor the owner's. Reclassifying it is not a closure and settles no copy — the words are still to be written, by the designer, at design time.)* | **Not open — implementation-time item (screen copy); no owner action pending.** Written by the designer drawing `SCR-M01-01` when the sign-in screen is designed, inside the already-fixed `Q51` constraint (no "later" while the retry is live); the annotation the designer meets it at is `docs/ux/briefs/SCR-M01-01-sign-in.md`'s `delivery-failed-retry-later` state; full statement at `modules/M01-onboarding-and-tenant-config.md` §6; origin `Q51`; reclassified 2026-08-06 |
| Q57 | Payments & collections / does the copy-paste **fallback** path leave any audit record at all (`foundations/F2` §6 `F2-Q3`; `modules/M11` §6 `M11-Q6`; `F2-22`, `M11-07`, `M11-21`, `M11-26`, `modules/M11` §5; raised by the `Q52` closure) | **Decision recorded — not open.** The copy-paste **fallback** path leaves **no audit record at all, and none is added.** `F2-22` covers the send **from the tenant's connected official channel** and only that send, because **the log records what the PRODUCT performed** — and where no channel is connected the product performs only the composing and the placing on the clipboard, while a **person** sends the message outside the product. So on that path **no compose record, no copy record and no send record is written — and no compensating counter, timeline entry or chase marker either.** The owner took the second of the two readings this row set out, explicitly and on simplicity grounds: *"hey keep as much as simple. and dont do overengineering."* **The trade is accepted and recorded rather than engineered around** — it is the ruling's own condition, not a caveat on it: the same chase, to the same customer, about the same tranche, is **attributable when the product sends it and unrecorded when a person does.** That is consistent with the standing law that the product never claims what it did not do — the log is deliberately silent where the product did nothing — and `M11-26` and `modules/M11` §5's no-fabricated-delivery-state bullet are both unchanged, so no delivery, attribution or receipt claim is made on the fallback path either. **Nothing else moves:** no permission cell, no matrix cell and no grant changes, and **no covered event is added** — `Q57` **bounds** the clause `Q52` added rather than widening it, and `Q48`'s holder set and `Q52`'s connected-channel audit obligation both stand exactly as they were. **Money settlement is untouched and was never in question:** `M11-01` stands unamended, the platform never touches tenant funds. **Requirement cells amended, each recording its own pre-ruling text: `F2-22`** (the covered-events cell) **and `M11-07`** (the module's audit row) — the two cells this row said the answer would land in, and it landed in exactly those. Applied at `foundations/F2-roles-and-permissions.md` (`F2-22`, §F2.4's acceptance block, §F2.5-M11's `F2.M11.send-request-message` row and the notes beneath it, §6 `F2-Q3` RESOLVED); `modules/M11-payments-and-collections.md` (`M11-07`, §M11.1's and §M11.4's acceptance blocks, §M11.4's permissions block and its edge cases, §M11.8's permissions paragraph, §4's `foundations/F2` expectation, §5's no-fabricated-delivery-state scope note, §6 `M11-Q6` RESOLVED); `docs/tasks/M11-payments-collections.md` (`T-M11-002`, `T-M11-009`, `T-M11-016` and the `M11-26` law entry); and `docs/ux/briefs/SCR-M11-02-payments-ledger.md`. Decision date 2026-08-06. **No new question is raised by applying this ruling** — `foundations/F2` §6 and `modules/M11` §6 record the same. *(This row previously read, in full: "**OPEN — awaiting an owner ruling.** Raised by applying `Q52` and deliberately not decided by the pass that applied it. `Q52` makes the **send from the tenant's connected official channel** an audited event under the sender's name, and the reason the owner gave is that the log answers *"who messaged my customer?"* instantly — an answer that exists **only where a channel is connected**. Where none is (`M11-26`'s fallback, which `M11-21` keeps first-class because the connected account is an accelerator and never a dependency), the product composes and copies, the person sends outside the product, and — the **send** being the audited act — **no entry is written at all**. So the same chase, to the same customer, about the same tranche, is **attributable on one path and invisible on the other**. Whether the fallback's compose-or-copy act should itself be recorded — as an act record naming who took it and against which tranche, **never** a delivery claim, which `M11-26` and `modules/M11` §5 forbid absolutely — or whether the log is deliberately silent where the product performed nothing, `Q52` did not address. **Not decided here** — nothing in `foundations/F2`, `modules/M11`, their tasks or their briefs decides it; no record is written on the fallback path in the meantime, and **no delivery, attribution or receipt claim is made for it anywhere**. The answer lands as a cell edit to `F2-22` and to `M11-07`. Recorded 2026-08-06 by the `Q52` closure pass." Its decision owner read "**Open** — Owner (with `foundations/F2` and `modules/M11`)". The owner has ruled — for the second of the two readings, the silent one — and the answer landed exactly where that text said it would, at `F2-22` and `M11-07`; any annotation anywhere still describing this as open is false as of 2026-08-06.)* | Resolved by owner ruling 2026-08-06 (the fallback path writes **nothing** — no compose, copy or send record, and no compensating mechanism; the log records what the product performed; `F2-22` and `M11-07` amended; no grant moves, no covered event added, no delivery claim, money settlement unchanged). **Trade accepted and recorded, not compensated: attributable when the product sends, unrecorded when a person does.** No new question |
| Q58 | Market packs / which clock the statutory messaging **WINDOW** is evaluated on (`foundations/F1` §6 `F1-Q4`; `F1-10`, `F1-15`, `F1-17`, `F1-21`, `F1-48`; `F5-68`; raised by the `Q54` closure) | **Decision recorded — not open. ONE clock.** The statutory messaging **WINDOW is evaluated on the TENANT's timezone (`F1-10`) — the same clock as the send hour (`Q54`)** — never the customer's and never the market's. The frame this row said was undefined is now defined on both sides of the comparison: `Q50`'s *"the configured slot falls outside lawful hours"* and *"the last lawful moment before it"* are read on the tenant's clock, one clock for the hour and for the window it sits inside, never two. **The owner's reasoning is `Q54`'s, given again — recorded so a later reader sees a deliberate simplification and not an oversight:** a solar EPC's customers are local to it, so a second clock would add machinery with no real-world difference; the same explicit direction governs both of today's rulings, *"hey keep as much as simple. and dont do overengineering."* **The accepted consequences, stated honestly and deliberately not compensated for:** where an EPC does serve a customer in another timezone, **both** the hour and the lawful-window check are evaluated on the **EPC's** clock rather than the recipient's — the recipient's own statutory hours are never evaluated and there is no per-customer clock and no setting for one; and where a **tenant's** own timezone differs from its market's default (`F1-21`), the market's statutory window is evaluated on **that tenant's** clock rather than the market's, with no second, per-market evaluation running beside it. **Everything else `Q50` ruled is untouched:** the hour is still pack data, the window is still a **floor** a tenant may **narrow and never widen**, a slot outside lawful hours still sends at the **last lawful moment BEFORE it, never after**, and no tenant setting or platform support action configures around a floor item. **Requirement cells amended — the window clause alone in each, each recording its own pre-ruling text:** `F1-15` (whose floor clause previously named **no clock at all** for the window) and `F5-68` (amended again; its four facts and its two send branches untouched). **No `SRC` citation was deleted** — `F5-68`'s `SRC` keeps its `Q50` pointer with the *"customer's market timezone"* wording marked **superseded** for the window as `Q54` had already marked it for the hour. Applied at `foundations/F1-global-market-framework.md` (`F1-15`, `F1-17`'s clock sentence, §F1.2's edge cases and acceptance block, §4's `foundations/F5` row, §5's no-second-clock non-goal, §6 `F1-Q4` RESOLVED); `foundations/F5-customer-link.md` (`F5-68`, §F5.9's acceptance line and edge cases, §5's non-goals, §4's `foundations/F1` contract line, §6 register `Q58` RESOLVED); `docs/tasks/F5-customer-link.md` and `docs/tasks/F-platform.md`; and `docs/ux/briefs/SCR-F5-02-link-progress.md`. Decision date 2026-08-06. **This ruling does NOT close `Q53`** — it settles *which clock* the window is read on, never *what the window is*: the IN pack still declares no statutory messaging window and no send hour, so at IN launch there is still nothing to evaluate on this or any clock and nothing downstream assumes one. The row's own observation that **at IN launch the point is moot in practice** — one market, one authored pack, `Asia/Kolkata` the declared default (`F1-48`) — survives as an observation and was never the answer. **No new question is raised by applying this ruling** — `foundations/F1` §6 and `foundations/F5` §6 record the same. *(This row previously read, in full: "**OPEN — awaiting an owner ruling.** Raised by applying `Q54` and deliberately not decided by the pass that applied it. `Q54` settles the clock for the **hour** — the tenant's (`F1-10`). It says nothing about the clock the **window** that hour sits inside is evaluated on, and `F1-15` makes that window a **market** statutory floor. Before `Q54` the point could not arise: hour and window sat on one clock, the customer's market timezone. Now the hour is on the tenant's clock and the window's clock is unstated, so `Q50`'s *"the configured slot falls outside lawful hours"* and *"the last lawful moment before it"* have **no single defined frame** wherever a tenant's timezone differs from its market's default (`F1-21`, which `F1-10` permits — the market timezone being a default only). Reading a market's statutory window on an arbitrary tenant clock is a **compliance** question, not a formatting one, so no default is chosen: choosing would be exactly the floor-item resolution `F1-17` reserves to the owner. **At IN launch the point is moot in practice** — one market, one authored pack, `Asia/Kolkata` as the declared default (`F1-48`) — but "moot in practice" is not a declaration. **This question also depends on the still-open `Q53`:** the IN pack declares no statutory messaging window at all, so there is currently no window to read on any clock and nothing downstream assumes one. **Not decided here** — `foundations/F1` does not choose, `foundations/F5` does not choose, and no task or brief invents a clock. `F5-68`'s four facts, its two send branches, `Q50`'s floor/default classification and `Q54`'s tenant clock for the hour all hold either way. Recorded 2026-08-06 by the `Q54` closure pass." Its decision owner read "**Open** — Owner (with `foundations/F1`)". The owner has chosen — the tenant's clock, one clock — and `F1-15` is amended to it; any annotation anywhere still describing this as open is false as of 2026-08-06. Its dependency on `Q53` is unchanged in substance and does not reopen anything: `Q53` still supplies no IN window, and this ruling never needed one to fix the frame.)* | Resolved by owner ruling 2026-08-06 (the statutory messaging **window** is evaluated on the **tenant's** timezone `F1-10` — the same clock as the send hour, one clock and not two; `F1-15` and `F5-68`'s window clause amended, `Q50`'s citation kept with its customer-market-timezone wording marked superseded; `Q50`'s floor/default law untouched — narrow never widen, last lawful moment **before** and never after). **Consequence accepted and recorded: an out-of-timezone customer's own lawful hours are never evaluated, and a tenant whose timezone differs from its market's default reads the market's window on its own clock.** `Q53` untouched and still open *(`Q53` has since been closed — owner ruling 2026-08-26 → `F1-62`.)*; no new question |
| Q59 | Data honesty / does a **date** carry a provenance tier (`foundations/F8` `F8-01`, `F8-02`; `docs/ux/claude-design-context.md` N7; raised by the UX-readiness pass 2026-08-07, which found the briefs split — some tiering a date, others declaring it an untiered record fact — and `F8` silent) | **Decision recorded — not open. Dates split on whether the thing already happened.** A **recorded** date carries **no tier** and is a record fact: a payment's received-on date, a visit's check-in time, an audit timestamp. It shows **when it was recorded**, not a tier — `F8-02`'s four values all describe how a quantity was *arrived at* (`measured` on site · `derived` from a model · `estimated` by heuristic · `assumed` from catalog), and none of them describes a timestamp the system wrote down. A **projected** date carries a tier like any other projection: a payback month, an expected commissioning date, an estimated arrival window. **The reasoning, recorded so a later reader sees a deliberate line and not an oversight:** forcing a tier onto a recorded timestamp would either invent a fifth value — which `F8-02` forbids, its four being "verbatim and canonical" — or trip `F8-01`'s own clause that *"a number whose tier cannot be established is not rendered as a number"*, which would stop the product printing payment dates. Leaving projections untiered is the opposite failure and the one `F8` actually exists to prevent: an unlabelled *"payback Mar 2031"* is precisely the confident-looking number `OV-38` says competitors print and this product does not. **Accepted consequence:** two date treatments coexist on screens that show both, e.g. a payments ledger with recorded received-on dates beside a projected next-due date. That is the intended reading — the difference is the honest signal, not noise. **REFINED THE SAME DAY, and this is the operative form.** The first wording tested *"is the date a fact about something that already happened"*, which sent a **scheduled visit time** to the tiered side — where none of `F8-02`'s four values fits it, since a person picked the time and nothing estimated it. Labelling a firm appointment `assumed` reads as *"we guessed this time"* on a customer-facing visit confirmation, which is the opposite of what `F8` is for. **The test is now how the value was ARRIVED AT: recorded → no tier; computed or guessed → tier.** A booked appointment time and a user-set *"busy until 17:00"* are recorded — the booking happened even though the visit has not. Payback months, expected commissioning dates and estimated arrival windows are computed, and carry a tier. Raised by Claude Design during the `SCR-SHELL-01` session, 2026-08-07, and ruled by the owner the same day. | Resolved by owner ruling 2026-08-07, refined the same day (recorded values are untiered; computed or guessed values carry a tier; `F8-01`/`F8-02` unchanged — this rules on their **scope** over dates, which `F8` never stated; applied in `docs/ux/claude-design-context.md` N7 and `docs/ux/briefs/SCR-SHELL-01-app-shell.md`) |
| Q60 | Onboarding / the **success-dwell duration** between OTP acceptance and the post-auth handoff (`modules/M01` §6; the `Q20` rebuild acceptance list; `docs/ux/briefs/SCR-M01-01-sign-in.md`; raised by the UX-readiness pass 2026-08-07, which found `Q20` naming success-dwell timing as binding while no state carried it and no duration existed anywhere in the suite) | **NOT AN OWNER DECISION — reclassified 2026-08-07 as an implementation-time item, exactly as `Q56` was.** The *existence* of the confirmation beat is settled and binding — `Q20`'s acceptance list names it, and on a bad connection it is the only thing telling the user the sign-in worked — so `SCR-M01-01` now carries a **`verified-success-dwell`** state that must be drawn. The *duration* is a value written when the screen is designed, not a preference an owner holds: it is marked **"not pinned by PRD — designer decides"**, which is the completion contract's own mechanism (item 6), and the designer records the chosen value back into the brief so the second platform inherits it rather than choosing a different one. The one constraint already fixed: whatever is chosen applies to **both** platforms, which is what `Q20` means by "signed-out routing decided once for both platforms, success-dwell timing". | Reclassified 2026-08-07 — belongs to the designer who draws `SCR-M01-01`, under the "designer decides, and records it" rule; no owner decision outstanding |
| Q61 | Product scope / removing the offline & sync capability entirely (`foundations/F4`, deleted; `F7-23` `N10`; `F7-36`; `F7-43` item 2; `docs/ux/claude-design-context.md`) | **Decision recorded — not open.** The **offline capability is removed from HelioGrid V2.** The app requires a live connection: no cache, no local-first read, no sync engine, no queue, no staleness, no conflict resolution from deferred writes, no sync centre, no global connection indicator, no queued or unsynced marker, and **no `offline` state on any screen**. Losing the connection is an ordinary network error shown on **one shared full-screen state** owned by the design system. **The single carve-out:** field **photographs** are held on the device and upload when the connection returns — one queue, one direction, no conflicts, no merge — and its status appears on `SCR-M04-07` alone. **Owner's reasoning, recorded so a later reader sees a deliberate simplification and not an oversight:** *"the main goal of our product is not overengineer and keep things more simple way and fast."* **Consequences accepted explicitly:** `F4-offline-and-sync.md` deleted (25 of its 35 rows; the other ten were never about connectivity and survive in `F4-data-integrity.md` with their ids intact); `SCR-SHELL-04` and `SCR-SHELL-05` deleted, 152 → **150 screens**; `N10` amended from four base states to three **by ruling, not by edit**, since the N-set is carried under *"never renumber, never reword"* — the `R19-B`/`F7-10` precedent; `F7-36` (Principle 7) **struck in place, not renumbered**, so Principles 8–12 keep their citations; `OV-39`'s competitive-moat claim and `BM-05`'s public pricing-page feature line both deleted, because a product that requires a connection cannot advertise an offline field app. **The cost, stated plainly:** a surveyor on a roof with no signal cannot open their assigned work. The owner was shown this and chose it. | Resolved by owner ruling 2026-08-07 (offline removed entirely; photo-capture carve-out only; applied across `docs/prd/`, `docs/ux/` and `docs/tasks/` the same day) |
| Q62 | CRM / a duplicate the live check could not see (`modules/M02` §M02.2; deleted `M02-04`; `M02-07`, `M02-09`, `M02-12`, `M02-59`/`M02-60`; briefs `SCR-M02-01`, `SCR-M02-04`) | **Decision recorded — not open.** **The law is restored, as new row `M02-66` in §M02.2.** The owner's reasoning, recorded so a later reader sees a deliberate re-instatement rather than a reversal: the behaviour was never an offline behaviour. `M02-07` runs the duplicate check *before* the save, so two reps entering the same number in the same second — both online — is the ordinary case the rule governs, and `Q22` already ruled on it correctly on 2026-08-04. It was cut only because its wording was framed around a device that had saved alone. **`F8-36` (live, P0) already forbids the alternative** — an action must not "display an optimistic result" — so ruling the other way would have required carving lead capture out of a live honesty law. **The cost is small and was weighed:** `M02-09`'s three-choice sheet and `M02-12`'s both-records-each-shows-the-other linkage already exist, so this reuses machinery rather than building any. **A new id, not the old one:** `M02-04` stays struck in `registers/screens.md` and in every dated task record, because it *was* deleted on 2026-08-07 and those records must stay true; `M02-66` names it as the law it restores. Applied at `modules/M02-crm-and-leads.md` `M02-66` + §M02.2 acceptance block; briefs `SCR-M02-01` and `SCR-M02-04` (UNRESOLVED markers replaced by the verbatim row); `docs/tasks/M02-crm-leads.md`; both registers. | Resolved by owner ruling 2026-08-15 (law restored as `M02-66`; raised by the `Q61` application sweep, and previously ruled the same way at `Q22` 2026-08-04) |
| Q63 | CRM / triage actions are server-completed (`modules/M02` §M02.5–§M02.6; deleted `M02-26`; `M02-24`, `M02-28`, `M02-55`; brief `SCR-M02-02`) | **Decision recorded — not open.** **The law is restored, as new row `M02-67` in §M02.5.** **The owner's reasoning:** the rule is about optimistic UI, not connectivity — it forbids showing a lead as assigned before the server agrees, so two managers cannot both be told they got it. That is an online race and always was. **`F8-36` (live, P0) already forbids the alternative**, and `M11-06` (money) and `M05-09` (designs) are the same rule at their own surfaces; leaving lead triage as the one exception would have been an unstated carve-out. **The constraint the ruling carries, stated so no designer over-builds it:** `M02-24` requires triage under three seconds per lead and `M02-28` gives a one-tap assign with no confirm step. Both stand. The pending state is a light in-row treatment that leaves the row operable — **never a blocking overlay and never a spinner wall** (`F4-27`). Honest, not heavy. This also closes a gap the sweep left wider than it found it: junk — `M02-24`'s second single-tap decision, `M02-55` — was covered by nothing at all. **A new id, not the old one:** `M02-26` stays struck; `M02-67` names it. Applied at `modules/M02-crm-and-leads.md` `M02-67` + §M02.5 acceptance block; brief `SCR-M02-02`; `docs/tasks/M02-crm-leads.md`; both registers. | Resolved by owner ruling 2026-08-15 (law restored as `M02-67`; raised by the `Q61` application sweep) |
| Q64 | Field workforce / an attendance mark is a server write (`modules/M09` §M09.5; deleted `M09-36`; `M09-35`, `M09-37`, `M09-38`, `M09-39`; brief `SCR-M09-02`) | **Decision recorded — not open.** **The law is restored, as new row `M09-71` in §M09.5.** **The owner's reasoning:** of the three rulings taken together on 2026-08-15 this is the one that binds hardest, because attendance is read as a judgement about a person and feeds `M10-23`'s register and payroll. A mark that *looks* recorded and is not is a wrong answer about someone's day. **It is also required by a law already live, twice over.** `F8-36` forbids an optimistic result; and the `Q59` date rule holds that a recorded time carries **no** provenance tier precisely because something wrote it down — so an optimistic local tick would render an untiered record fact that is not yet a record, which `F8-01` forbids outright. Ruling the other way would have contradicted two live P0 laws, not one. **What it does not change:** `M09-37`'s proposal-not-derivation rule and `M09-38`'s correction-by-append are untouched; this row governs only the moment between the person's act and the server's acknowledgement. **A new id, not the old one:** `M09-36` stays struck; `M09-71` names it. Applied at `modules/M09-field-workforce.md` `M09-71` + §M09.5 acceptance block; brief `SCR-M09-02`; `docs/tasks/M09-field-workforce.md`; both registers. | Resolved by owner ruling 2026-08-15 (law restored as `M09-71`; raised by the `Q61` application sweep) |
| Q65 | Platform / a client too old to talk to the server (`foundations/F4` §F4.3 or `modules/M01`; deleted `F4-35` and its screen `SCR-SHELL-05`) | **Decision recorded — not open.** The owner rules the forced-upgrade screen (2026-08-26): below the server-declared minimum version the app shows a plain full-screen block naming the required version and routing to the store, never a bare error. Landed as `F4-36` (`foundations/F4` §F4.1), state `update-required` on `SCR-SHELL-01`, task `T-FPLAT-033`. Rationale: v1 keeps no local store, so an out-of-date client has no cached data being withheld — which makes a screen the whole answer and a degraded read mode meaningless. *Raised 2026-08-07 by the `Q61` application sweep.* Deleted `F4-35` stated: **an application too old to sync still reads** — a hard lockout that hides a field user's own data behind a version check is not an acceptable failure mode. It was deleted correctly *as written*: its trigger was a sync-contract change and its mitigation was that local reads keep working, and with no local store neither half is possible. **But the product still ships client versions and will still break compatibility**, and nothing now states what a person sees when their app is too old. A grep across every foundation, module and root document finds no row on client version skew, a minimum supported version, or forced upgrade. Every always-readable guarantee that survives (`M12-22`, `M12-24`, `BM-32`, `F1-24`) is conditioned on **billing** state, not on client version. Unlike `Q62`–`Q64` this blocks no screen — `SCR-SHELL-05` was deleted with the row, so there is no surface waiting on it. **The decision:** state a v1 rule for the too-old client (recommended: a plain forced-upgrade screen naming the version, since with no local store there is nothing to read anyway — which makes this cheap to close), or record explicitly that v1 has no version-skew policy. | Resolved by owner ruling 2026-08-26 → `F4-36`, `T-FPLAT-033` — Owner |
| Q66 | Field capture / a shared phone holding another user's unuploaded photographs (`foundations/F4` carve-out; deleted `F4-32`; `M04-55`, `F4-21`, `M01-07`, `F2-20`; task `T-FPLAT-016`) | **Decision recorded — not open.** The owner rules for tenant isolation (2026-08-26): a user switch on a shared device **discards work held for the previous user, after telling them what is lost and offering the chance to upload first**. Landed as `F4-37` (`foundations/F4` §F4.3) with `F4-21` explicitly carved out for this one case; state `switch-discards-held-work` on `SCR-M01-01`, task `T-FPLAT-034`. Rationale: a rep must never reach another rep's customer photographs, and shared field phones are normal in this market. *Raised 2026-08-07 by the `Q61` application sweep; the hole was created by the carve-out, not by the deletion.* Deleted `F4-32`'s second half stated the tenant-isolation wipe: when a different user signs in on a device holding another user's unuploaded work, **tenant isolation wins** — the held work is discarded before any new data loads, and the person whose work is discarded is told before it happens (*"tenant isolation beats convenience"*, the source's own rule). Its first half — local reads and writes continuing while a session token is expired — died correctly with the local-first store. **The second half did not become moot, because photographs still sit on the device.** Shared field phones are normal in this market. No live row carries it: `M04-55` governs the queue but says nothing about who is signed in; `F4-21` promises nothing captured is unrecoverable, which pulls the *other* way and would have the phone keep the photos; `M01-07` covers session lifetime and revocation but not device-held data; `F2-20` covers deactivation, not user switching. **`F4-21` and tenant isolation are in direct conflict here and the product currently states both**, so an implementer must choose, and either choice is defensible from the live PRD. **The decision:** re-state the isolation rule as a new row in `F4` §F4.3 with `F4-21` carved out for it (recommended), or rule that photographs survive a user switch. | Resolved by owner ruling 2026-08-26 → `F4-37`, `T-FPLAT-034` — Owner |
| Q67 | 3D Studio / what blocks a design start when the release has no survey (`modules/M05` `M05-21`; `M04` entirely V2 under the 2026-08-16 scope lock; brief `SCR-MS-01`) | **Decision recorded — not open.** **`M05-21`'s prerequisite set is what the release actually offers, not a fixed list.** Raised by the V1 readiness audit of 2026-08-16, which found that the scope lock had made **the entire 18-screen V1 studio unreachable**: `M05-21` (P0) blocks design start for a lead whose survey is not submitted, §M05.2 makes clear that *"not submitted"* includes *does not exist* — *"M05-21's block is the honest front door for one that is not"* — and all ten `M04` screens are V2, so no survey can exist in V1. **The owner's reasoning:** this is the same shape as `Q62`–`Q64` — a law written under an assumption a later scope decision invalidated, not a law that was wrong. Survey went to V2 precisely *because* the studio draws its own roof in Step 2; gating the studio on the thing it replaces inverts that. **What is unchanged:** the honesty half, which is the row's actual source (`S5.wrong.1`, *"show exactly what is missing and who to ask"*) — a block still names each missing prerequisite, its owner and the fastest route, never a bare refusal. In V1 the set is the site-type and connection fields Step 1 collects; from V2 the submitted survey rejoins it. **The accepted consequence, stated plainly:** a V1 design starts from less site data than `M05` was written to assume. Applied at `modules/M05-design-studio.md` `M05-21` (cell amended, original text preserved ahead of the amendment) and re-synced into `docs/ux/briefs/SCR-MS-01-design-list.md`. | Resolved by owner ruling 2026-08-16 (raised by the V1 readiness audit; unblocks all 18 studio screens) |
| Q68 | Data model / three modules have no schema at all (`modules/M03-marketing.md`, `modules/M09-field-workforce.md`, `modules/M10-hr-lite.md`; `docs/engineering/forward-compat.md` rows added 2026-08-25) | **Answered — not open.** `docs/engineering/data-model.md` (2026-08-26) rebuilds the logical model from this PRD and defines the entity shapes for all three: M03 (`campaign`, `campaign_template`, `campaign_send`, `campaign_link`, `lead_capture_record`, `suppression_entry`, `consent_record`), M09 and M10. That document is the answer this row asked for — logical only, no SQL, read beside `forward-compat.md`. *Raised 2026-08-25 by the docs truth-and-structure pass.* The retired repo-wide data-model document defined no tables for **marketing (M03)**, **field workforce (M09)** or **HR-lite (M10)**, and none exists elsewhere. `docs/engineering/forward-compat.md` now carries a row for each stating the constraints their first migration must satisfy — per-contact per-channel consent ledger and market-pack-validated `lead_source` for M03; online-first check-in/visit/attendance/geofence rows with server-assigned timestamps for M09; tenant-scoped deactivate-never-delete employee records sharing the one `files` table for M10 — but the **entity shapes themselves are undecided**. A ruling is owed before any of those three slices begins, and the data model is rebuilt from this PRD rather than restored. | Answered by `docs/engineering/data-model.md` 2026-08-26 |
| Q69 | Roles / preset count contradiction — six versus twelve (`foundations/F2-roles-and-permissions.md` `F2-08`; retired data-model `role_preset` enum) | **Decision recorded — not open. Twelve presets are the shipping launch set.** The retired six-value `role_preset` enum is superseded and must not be restored. The rebuilt contracts, relational membership-role model, capability map, role assignment surfaces and invariants use all twelve presets fixed by `foundations/F2`; every preset is assignable at launch. This does not create per-person grants or editable roles: stacking the fixed presets remains the only widening mechanism. | Resolved by owner ruling 2026-08-25; applied to the architecture-foundation roadmap, with contract/schema migration landing in the M01 implementation track |
| Q70 | Billing meters / the retired enums are narrower than M12 requires (`modules/M12-platform-billing.md` `M12-33`; `04-business-model.md` `BM-22`; retired `usage_metric` / `entitlement_key` enums) | **Answered — not open.** `docs/engineering/data-model.md` (2026-08-26) resolves the narrowness by making `entitlement_key` an **open string** keyed per tenant rather than a closed enum, and carries tracked seats and marketing sends as real metered facts per `M12-33`/`BM-22`. The retired enums are not restored; nothing closed-set constrains the meters. *Raised 2026-08-25 by the docs truth-and-structure pass.* `M12-33` makes **tracked seats** and **marketing sends** real metered facts, and `BM-22` makes seat counting the sanctioned exception to the no-per-seat-pricing rule. The retired data model's `usage_metric` and `entitlement_key` enums were closed sets carrying neither, and its text asserted seats are "always unlimited, no per-seat pricing". The rebuild must open both enums to cover them. What is owed is narrower than that mechanical fix: **are seats billed, or only counted?** `BM-22` sanctions counting; nothing rules on charging. The answer changes whether `seats` is an entitlement limit, a usage meter, or both. | Answered by `docs/engineering/data-model.md` 2026-08-26 |
| Q71 | Authentication / mobile session inactivity (`modules/M01` `M01-07`; architecture-foundation review 2026-08-25) | **Decision recorded — not open.** Mobile has **no fixed maximum session lifetime while the person remains active**. Seven full days without foreground authenticated use expires the underlying mobile session and requires sign-in again. Opening or using the signed-in app in the foreground resets the inactivity window; background refresh, push handling and scheduled work never reset it. Product API tokens remain short-lived (≤10 minutes) and renew silently while the session is valid, so token expiry never presents a login screen by itself. Deactivation and “sign out everywhere” still end access within ≤10 minutes. This supersedes `M01-07`'s prior fixed 90-day mobile lifetime; the web's 30-day rolling session is unchanged. | Resolved by owner ruling 2026-08-25; applied to `modules/M01` `M01-07` |
| Q72 | Roles / a phrase-cell is not a machine-checkable grant (`foundations/F2-roles-and-permissions.md` `F2-25`, §F2.5-M01 `F2.M01.manage-catalog`; architecture-foundation Track 5a, 2026-08-25) | **Not open — deferred to its owning slice, and already recorded there.** `packages/domain/src/authz/capabilities.ts` carries the `F2-25` phrase VERBATIM rather than inventing a grant the PRD does not state, so nothing is silently widened or narrowed today. The phrase becomes a machine-checkable grant when the **M01 catalog slice** is built and needs to enforce it — the decision belongs to that task, not to a register row. *Raised 2026-08-25 by authoring the capability policy in code.* F2-25 says "a phrase in a cell is a scoped grant (the scope word is the grant)", and §F2.5-M01 uses one: Finance holds `F2.M01.manage-catalog` as *"view prices & margins"*. That is neither a full grant nor one of the four visibility scopes (`all`/`team`/`own`/`assigned`, which F2-14 resolves per domain) — it is a **narrower act**, and a deny-by-default guard answering a boolean cannot enforce a sentence. Three readings exist and the PRD does not choose: (a) the row splits into two rows — `manage-catalog` (EPC Owner, Operations) and a new read-only pricing row that also grants Finance; (b) `manage-catalog` becomes scoped like a visibility row, with a fifth scope vocabulary for acts; (c) the phrase stays prose and the owning slice enforces it in module code, outside the matrix. **Nothing is decided here and no cell was moved.** `packages/domain/src/authz/capabilities.ts` carries the phrase VERBATIM on a `limited` grant so it is neither dropped nor silently widened, and `capabilityLimit()` surfaces it; the guard grants the act and the limit is the owning slice's to honour. Coining a `view-catalog-prices` row in code would have invented a requirement the PRD does not carry, which is why this is a register row instead. The answer lands as a cell edit in §F2.5-M01, and the catalog slice (`modules/M01` catalog area) is the first consumer that must not ship without it. | Deferred to the `modules/M01` catalog slice; carried verbatim in code meanwhile |
| Q73 | Authentication / does the 15-minute number lock close the Google door too? (`modules/M01-onboarding-and-tenant-config.md` `M01-04`, `M01-02`; `SCR-M01-01`) | **Decision recorded — not open.** Owner ruling 2026-08-27: **the lock is the channel, not the account** — reading (b). A number under its 15-minute lock cannot request or verify another SMS code; **Google Login stays available** onto the same phone-identity account throughout, and the message names what paused and what still works (`M12-31`'s pause-honesty). Applied to `M01-04` in `modules/M01-onboarding-and-tenant-config.md`, to the verbatim row and the `number-locked-15min` state in `docs/ux/briefs/SCR-M01-01-sign-in.md`. Reading (a), the locked account, was considered and rejected: reaching the Google door already requires holding that Google account, so it buys little, and it costs a legitimate person their last way in. Decision date 2026-08-27. **Raised 2026-08-27 by designing `SCR-M01-01`.** `M01-04` locks the **number** for 15 minutes after three consecutive OTP invalidations, and says nothing about the other sign-in method. `M01-02` binds Google to the **same phone-identity account** (`Q18`), so the two readings are not stylistic: **(a) locked account** — the lock is on the identity, and Google is closed for the 15 minutes too, or **(b) locked channel** — the lock is on the OTP rail, and Google remains a way in. (a) is the security reading: an attacker who has burned the OTP path must not simply walk through the other door. (b) is the recovery reading: a legitimate owner mistyping a code three times keeps a route in, which is what a convenience door is for. They are different products and the PRD chooses neither. **Nothing is blocked and nothing was guessed:** the `number-locked-15min` frame claims neither reading — it states the lock, the chain that caused it and the wait, and shows no Google control asserting either answer. The answer lands as a clause on `M01-04` and a line in `SCR-M01-01`'s `number-locked-15min` state; the first consumer that cannot ship without it is `T-M01-001`. | Resolved by owner ruling 2026-08-27 |
| Q74 | Units / does a tenant declare its typical system size in **kW** or **kWp**? (`modules/M01-onboarding-and-tenant-config.md` `M01-23`; `SCR-M01-04`) | **Decision recorded — not open.** Owner ruling 2026-08-28, delegated to the assistant on the product's global scope: **the unit is `kWp`.** The two are different quantities — `kWp` is the DC array at peak, `kW` the inverter's AC output, DC running 10–30% above AC. Residential uses them interchangeably and lands close; C&I does not, and `M01-23`'s own step asks Residential / C&I / **both**, so a `kW` label on a value that seeds a `kWp` target (`M05-39`) would be wrong by that margin for every C&I tenant. Decisive for a **global** product: `kW` names the sanctioned array in one market and the inverter rating in another, and a stored value whose meaning changes per market is what `F1`'s pack model exists to prevent — while EPCs price per `kWp` installed in every market, so it is their own unit rather than jargon. Applied to `M01-23`, to its verbatim copy and prose in `docs/ux/briefs/SCR-M01-04-setup-what-you-sell.md`, and carried into `SCR-M01-05`'s brief. The drawn screen needs a redraw of the field's unit and label. Decision date 2026-08-28. *Raised 2026-08-28 by designing `SCR-M01-04`, which drew the brief's `kW` verbatim and flagged it rather than silently correcting a P0 cell.* `M01-23` writes **kW** twice — *typical system size* and *stored as the tenant's segment + typical-kW declarations* — carried verbatim from `DOC04.tenant-onboarding-fields`. Every other surface in the product states a system's size in **kWp**: `F3-09`'s own worked example is *8.2 kWp सिस्टम*, and the drawn screens render `64 kWp estimated` and `180 kWp DC`. `F3-08` makes kW, kWh and kWp all valid never-translated units, so neither is a typo — they are different quantities (kW is output, kWp is peak DC), and the declaration seeds defaults that a design later reports in kWp. **Nothing is blocked and nothing was guessed:** `SCR-M01-04` draws the brief's word verbatim and flags it here rather than silently correcting a P0 cell. The answer is a one-word edit to `M01-23` and to `SCR-M01-04`'s brief; `T-M01-004` is the first consumer that cannot ship without it. | Resolved 2026-08-28 |
| Q75 | Onboarding / no screen owns the tenant's segment + typical-size declarations after onboarding (`modules/M01-onboarding-and-tenant-config.md` `M01-23`; `SCR-M01-04`) | **Decision recorded — not open.** Owner ruling 2026-08-28, delegated to the assistant: **the company profile, `SCR-M01-05` (`M01-24`), is where the segment and typical-size declarations are edited after onboarding.** Reading (a) of the three. It is already the tenant's one write-point that consumers read by reference and never re-ask (`M01-31`), it already exists three times over in the product's life (skippable onboarding step, in-context prompt-point, settings), and it carries the permission and the audit obligation these declarations need. Reading (c), a new company-settings screen, was rejected as a register row, a brief, a task and a design session for two fields; reading (b), catalog settings, was rejected because a segment is not a catalog. The cost is stated rather than hidden: `SCR-M01-05` widens from legal identity — logo, tax registration, address, bank details — to also hold two commercial defaults. Applied to `M01-23` and to both screens' briefs. Decision date 2026-08-28. *Raised 2026-08-28 by designing `SCR-M01-04`.* `M01-23` stores two tenant declarations and names no surface that edits them later. Searched: *segment* and *typical system size* appear in `SCR-M01-04`'s brief and the M01 PRD and **nowhere else** — `SCR-M01-05` Business Profile carries neither. Contrast `F3-03`, which pins the language picker's permanent home at `SCR-M01-11` in its own words. The gap has a drawn consequence: `SCR-M01-04` states under its primary that a later change re-seeds only what comes next, and its `error` state offers *Finish later* as the escape from a failed save — **both point at a screen that does not exist**, which is the completion contract's no-orphan rule with no surface to satisfy it. Three readings, and the PRD chooses none: (a) the two declarations join `SCR-M01-05` Business Profile; (b) they join `SCR-M01-15` Catalog Settings, since what they seed is catalog-shaped; (c) a company-settings screen is added, which would be a new register row rather than a brief edit. **Nothing is blocked and nothing was guessed:** the screen names the destination in words and does not invent an id. `T-M01-004` is the first consumer that cannot ship without it. | Resolved 2026-08-28 |
| Q76 | Data honesty / does `F8-01`'s "every user-visible number" cover an **identifier**? (`foundations/F8-data-honesty.md` `F8-01`, `F8-03`; `M01-25`; `SCR-M01-05`) | **Decision recorded — not open.** Owner ruling 2026-08-28, delegated to the assistant: **no. A number is a quantity; an identifier is not one and carries no tier.** Raised by designing `SCR-M01-05`, which had to render a tax registration and bank details and found that none of the four tiers can honestly describe either — `estimated` on a GSTIN claims a guess nobody made, `measured` claims a verification nobody performed. `F8-01`'s own remedy proves the scope: *a number whose tier cannot be established is not rendered as a number* would mean refusing to show a correct registration number and displaying *what is missing* instead, which is false. **The design session's proposed answer was rejected as a law violation, and the rejection is the useful half:** it drew a second axis — a standing of `reported` beside the four tiers — and `F8-03` forbids exactly that by example (*introduces a parallel vocabulary … alongside these four*). Its *content* was right and is kept: `F8-03`'s own sentence says that where a surface needs to say more than the tier says, **it says it as prose beside the tier**. So the tax registration renders with no tier and a plain sentence stating the format was checked and the issuing authority was not — which is `M01-25`'s live check described honestly. **This was already the product's practice and had never been written down:** four screens had ruled identifiers untiered in their own design records (`SCR-M01-01`'s OTP, `SCR-M01-02`'s code, `SCR-SHELL-02`'s phone numbers, `SCR-M01-04`'s), a load-bearing distinction held by review alone. Applied as a scope clause on `F8-01` and to its verbatim copy in `docs/tasks/F-platform.md`. No fifth tier was added and the set stays closed. Decision date 2026-08-28. | Resolved by owner ruling 2026-08-28 |
| Q77 | Design language / does `F7-15`'s no-borders law leave a **control** identifiable? (`foundations/F7-design-language.md` `F7-15`, `F7-16`; every screen) | **Decision recorded — not open.** Owner ruling 2026-08-28: **a control is made findable by RAISING it, never by outlining it.** The ladder, lightest last — page `--canvas` #EEF0F3 → a container that wraps controls `--surface-form` #F6F7F9 → the control `--surface` #FFFFFF at `--e2`. Each step is brighter than the one behind it, so a control is the brightest object on screen and `F7-15` keeps its **two** exceptions. **What this binds downstream.** *The page is the darkest ground:* a container darker than its own page reads as a hole rather than as depth. *#EEF0F3 is the darkest ground this palette's marks survive* — a `--success` dot measures 2.88 one step lower against the 3:1 non-text floor and the theme build refuses it, so `--canvas-sunken` carries the page's own value with a different role (below its *container*, not below the page), and `--text-tertiary` sits at #6A6D73 for the same reason (4.48 on this page fails the gate). *`--control-edge` stays `none` in the base scope:* seven controls read it BECAUSE it is none — both ghost variants, `Switch`'s track, `FieldModeToggle`, `AudioTransport`, `FindingList`, `PendingAction` — so giving it a resting value rings all seven and turns the ghost text button into an outlined one, collapsing fill / outline / text into two tiers. Its one job is the field-mode-only reach for a control whose inline `none` a stylesheet rule cannot beat. *An outline is not an alternative to re-propose:* a 1px `--mark-subtle` ring measures 3.57:1 and clears the floor, and it reads as an outlined form. **The cost is stated, not buried:** `--surface` on `--canvas-sunken` is **1.14:1**, below WCAG 1.4.11's 3:1 floor for information identifying a control. The owner chose elevation with that number in hand; `F7-16`'s field mode is the sanctioned answer for sunlight and rings every elevation, so a control at `--e2` is covered with no per-control token. Raised on `SCR-M01-07` — *"on card when input box or button render, those are very low visibility and look like normal text until you look closely"* — where an `Input` measured **1.08:1** and a ghost `Button` **1.00:1**. | Resolved by owner ruling 2026-08-28 |
