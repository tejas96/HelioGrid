# Sitting 8 rulings — Customer surfaces: proposal document + share link (2026-08-05)
S8-1 (.109/.92/.94/.44/.16, POC-DEFECTs): DOCUMENT IDENTITY — every proposal carries proposal number, issue date, version/revision, validity period, prepared-by (company identity per M01 branding), and the CUSTOMER-facing project name (never the internal design/variant name incl. "(copy)"); page numbers counted not hardcoded; no trailing blank page; share-viewer caption uses customer-facing naming. Owner ruled 2026-08-05.
S8-2 (.107/.108/.101/.98/.99/.14/.100, POC-DEFECTs + F5 gaps): CUSTOMER-FACING GATING — (a) issuance blocked unless the readiness review passes (electrical gate + review verdict), all entry points marked consistently (.107/.108/.14); (b) an issued proposal is PINNED — later design edits create a NEW version, never mutate what was sent (.101, M06 pinning law); (c) links follow F5: per-recipient named links, revocable, open-attributed, with the acceptance surface and OTP-at-accept per Q42 (default OFF) (.98/.99/.100). Owner ruled 2026-08-05.
S8-3 (.35/.62/.63/.80/.24/.59/.75/.83, POC-DEFECTs): HONESTY ON PAPER — (a) staleness warnings PRINT (never no-print) incl. per-capture badges + true capture sun state (.35/.62/.63); (b) structure disclaimer appears wherever structure/mounting is quoted, not only when a member model exists; share viewer carries it too (.80/.24); (c) "nothing is estimated" prints only when true — otherwise the estimate provenance line (.59, F8-09); (d) subsidy/scheme sentences print only where eligible (.75, F1 pack); (e) BOM quantities carry their confidence/provenance marker on the customer document (.83, F8). Owner ruled 2026-08-05.
S8-4 (privacy/robustness/polish batch, all remaining POC-DEFECTs): (1) share page loads ONLY that design's data — no operator store hydration (.106, privacy); (2) no operator-only alerts/instructions on customer surfaces (.8/.64); (3) distinct loading vs missing-image states with stable footprint (.27/.29); (4) no "undefined"/"− ₹0" leaks (.70/.73); (5) captures print in preset order (.61); (6) deep-link never crashes; location-less design gets an honest message, not "invalid link" (.5/.13); (7) accessible roles on labelled elements (.28/.54); (8) copy-link success/failure feedback (.41); (9) Back returns to the entry point (.37); (10) hydration shows a loading state, not blank (.4); (11) no background recompute/persisted writes from a read-only viewer (.7); (12) QR failure handled visibly (.53); (13) the two customer-facing files get automated tests incl. an accessibility pass (.97).
SITTING 8 CLOSED 2026-08-05 — 4 rulings covering all 35 POC-DEFECTs + the F5/Q42 alignment gaps, zero open items.
