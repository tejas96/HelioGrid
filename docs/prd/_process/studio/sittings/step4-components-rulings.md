# Sitting 4 rulings — Step 4 Components (2026-08-05)
S4-1 (CODE.step4-components.8/.73 + census "available-later" rows): FULL DD12 ALIGNMENT — picker reads the tenant's RESOLVED catalog (platform market slice + own SKUs + overrides, M01-32..46); all three entry paths real (browse · upload datasheet w/ PDF extraction + review-before-commit per M01-40 · manual specs) plus in-flow Excel import (M01-41); BATTERY becomes a first-class section (Panel → Capacity → Inverter → Battery, DD12/M05-37); per-entry provenance surfaces in the picker (catalogProvenance/effectiveFrom go live). Owner ruled 2026-08-05.
S4-2 (CODE.step4-components.20, POC-DEFECT): Panel swap with placed modules ALWAYS raises the same guard dialog the Compare→Apply path uses (what changes, module count, keep-or-refill choice); one undo step. Closes SC.10-5.40. Owner ruled 2026-08-05.
S4-3 (CODE.step4-components.37/.42, POC-DEFECTs): No-fit state EXPLAINED — when no inverter lands in the sizing band, show the reason + nearest fits (reuse compare's nearestInverterFit) + suggestions (multi-unit / phase change); phase-incompatible inverters carry a badge in the list on single-phase sites. Owner ruled 2026-08-05.
S4-4 (hygiene batch, all 7): (1) open section collapses (fixes .5, aria-expanded honest); (2) filter/search state persists across section switches (fixes .22); (3) watt bounds clamped, blank = no-bound never empty-catalog (fixes .13); (4) no-match state + Clear filters (fixes .21); (5) capacity input commit-on-blur, clamped >0, quantised 0.1, one undo entry (fixes .26); (6) inverter count max enforced (fixes .39); (7) disabled controls state the blocking REASON (fixes .28, SC.10-5.13/M05-39). Owner ruled 2026-08-05.
S4-5 (polish batch, all 4): (1) cross-check shows panels AND ≈kWp (fixes .29, SC.10-5.14); (2) suggestion banners = real buttons (Enter+Space, a11y) with market-grouped currency (fixes .32); (3) availability badge travels to picker + summary, not compare-only (fixes .42-note, M05-43); (4) inverter list sorted phase-suitability then kW ascending (fixes .42). Owner ruled 2026-08-05.
SITTING 4 CLOSED 2026-08-05 — 5 rulings (S4-1..S4-5) covering all 14 defects + full DD12 alignment, zero open items.
