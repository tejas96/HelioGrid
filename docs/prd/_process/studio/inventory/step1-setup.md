# Feature inventory — step1-setup (Step 1 · Site Setup: address/map/imagery/calibration/north)

Pass-two inventory, 2026-08-05. Sitting 1 primary files: `Step1Setup.tsx`, `lib/maps.ts`, `lib/calibration.ts`, `lib/geo.ts`, `lib/solarApi.ts`, `lib/analysis-client.ts`, `workers/analysis.worker.ts`, `components/SatCanvas.tsx` (all under `3d_design_studio/src/features/solar-studio/`).

Test run (read-only; deps installed from lockfile, no source touched):
`npx vitest run …/geo.test.ts …/calibration.test.ts …/maps.test.ts …/solarApi.test.ts …/analysis-client.test.ts` → **5 files, 33 tests, all PASS** (vitest 3.2.7).

One-hop libraries consulted but owned elsewhere: `data/discoms.ts` (s13), `lib/solar.ts` + `lib/weatherApi.ts` (s6), `lib/shading.ts` (s5), `store/store.tsx` + `lib/units.ts` (s10/s8), `screens/Step2Roof.tsx` calibration sheet (s2). Note: `SatCanvas.tsx` is a sitting-1 file but is consumed by Steps 2/3/6 canvases — Step 1 itself uses the live Google Map, not SatCanvas.

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step1-setup.1 | Tutorial banner: static 170 px gradient block with a play icon and caption "Tutorial: Getting Started"; decorative only — no click handler, no video wired | Step1Setup.tsx:43-71 | working | NONE |
| CODE.step1-setup.2 | "Project Information" section heading with info icon (15 px heading style shared by all Step-1 sections) | Step1Setup.tsx:25-30,73-75 | working | NONE |
| CODE.step1-setup.3 | Every Step-1 info field writes straight into `project.info` on each keystroke via a non-undoable patch (`undoable=false` default) — Step-1 form edits never enter the undo stack | Step1Setup.tsx:37-39; store.tsx:471-475 (one hop) | working | NONE |
| CODE.step1-setup.4 | Project Name text field, placeholder "e.g., Smith Residence Solar", aria-label "Project name" | Step1Setup.tsx:77-85 | working | SC.10-2.02 |
| CODE.step1-setup.5 | Customer Name text field, placeholder "e.g., John Smith" | Step1Setup.tsx:87-95 | working | SC.10-2.03 |
| CODE.step1-setup.6 | Customer Phone field labeled "(for proposal)", placeholder "98765 43210"; free text — no phone-format validation of any kind | Step1Setup.tsx:96-104 | working | SC.10-2.04 |
| CODE.step1-setup.7 | Country select hard-locked to "India": single option, value fixed, onChange is a no-op | Step1Setup.tsx:106-111 | working | SC.10-2.05 |
| CODE.step1-setup.8 | State select: 37 states/UTs (INDIAN_STATES) plus placeholder "Select State"; choosing a state clears DISCOM to '' and re-derives the tariff via `tariffFor(state,'',siteType)` | Step1Setup.tsx:112-131; discoms.ts:5-14 (one hop) | working | SC.10-2.06 |
| CODE.step1-setup.9 | DISCOM select: disabled until a state is chosen; placeholder reads "Select Provider" (state set) / "Select State First" (not set); options come per state, unknown state → single generic "State DISCOM"; choosing re-derives tariff | Step1Setup.tsx:132-151; discoms.ts:16-44 (one hop) | working | SC.10-2.07 |
| CODE.step1-setup.10 | Tariff auto-fill precedence (₹/kWh, representative mock rates, not a live feed): per-DISCOM override → per-state residential/commercial pair → rules default (commercial = default × 1.4 rounded to 0.1) | Step1Setup.tsx:122,141,194; discoms.ts:46-92 (one hop) | working | SC.10-2.07 |
| CODE.step1-setup.11 | "Ground Mount Project" card: rendered at 0.55 opacity with "BETA" badge, "PRO" badge with lock icon, subtitle "Open access / PPA — no sanctioned load required", and an inert `<div class="toggle"/>` with no handler — the option cannot be enabled at all | Step1Setup.tsx:154-170 | POC-DEFECT(docs-contradict: census SC.10-2.14/D38 requires a normal option with NO PRO/tier gate, marked "coming soon" if non-functional; code shows a PRO lock and a dead toggle) | SC.10-2.14 |
| CODE.step1-setup.12 | Site Type segmented control: Residential (home icon) / Commercial (building icon); switching re-derives tariff for the new type | Step1Setup.tsx:172-196 | working | SC.10-2.08 |
| CODE.step1-setup.13 | Connection Type segmented control: Single Phase / Three Phase | Step1Setup.tsx:197-208 | working | SC.10-2.09 |
| CODE.step1-setup.14 | Sanctioned Load (kW) number input, min 0; value 0 renders as blank; stores `Number(input)` (blank → 0) | Step1Setup.tsx:209-219 | working | SC.10-2.10 |
| CODE.step1-setup.15 | Electricity Tariff (₹/kWh) number input, min 0, step 0.1, placeholder "e.g., 8.5"; manual edits always override the auto-fill | Step1Setup.tsx:222-232 | working | SC.10-2.11 |
| CODE.step1-setup.16 | Tariff provenance hint: "Auto-filled from {DISCOM \|\| state \|\| 'your DISCOM'} ({siteType}) — representative rate; edit to match your actual bill." | Step1Setup.tsx:233-237 | working | SC.10-2.11 |
| CODE.step1-setup.17 | Avg Monthly Electricity Bill (₹) input "— for sizing suggestion": min 0, nullable (blank stores null, not 0); hint explains it recommends system size from consumption at the current tariff/state; consumed by the Step-4 size suggestion | Step1Setup.tsx:240-256; phase-10-prompts.md:636 | working | SC.10-2.12 |
| CODE.step1-setup.18 | Company Logo section: heading + "This logo will appear on generated proposals" | Step1Setup.tsx:278-283 | working | SC.10-2.13 |
| CODE.step1-setup.19 | Logo preview: uploaded logo drawn at max 70 px high × 200 px wide; empty state shows a placeholder image icon | Step1Setup.tsx:284-291 | working | SC.10-2.13 |
| CODE.step1-setup.20 | Upload/Change Logo: button label switches ("Upload Logo" ↔ "Change Logo"); hidden file input `accept="image/png,image/jpeg"`; file is read via FileReader and stored as a base64 data-URL (`info.logoDataUrl`) inside the project record | Step1Setup.tsx:292-314 | working | SC.10-2.13 |
| CODE.step1-setup.21 | Logo size cap: UI text promises "Max size: 5 MB \| Formats: PNG, JPG" but NO size check exists anywhere — any file the picker accepts becomes a data-URL of unbounded size in the project (the `accept` attribute is a picker hint, not validation) | Step1Setup.tsx:298-300,307-313 | POC-DEFECT(docs-contradict: own UI copy and census SC.10-2.13 state a 5 MB cap the code never enforces) | SC.10-2.13 |
| CODE.step1-setup.22 | "Installation Location" section heading with pin icon | Step1Setup.tsx:486-488 | working | NONE |
| CODE.step1-setup.23 | Location Input Method select: "Search Address" (default) / "Enter Coordinates" | Step1Setup.tsx:328,490-500 | working | SC.10-2.15 |
| CODE.step1-setup.24 | Address search: Google Places Autocomplete bound to the input (fields restricted to `geometry`,`formatted_address`); picking a place sets the pending pin {lat,lng,address}; a place without geometry is silently ignored; the listener is detached on method switch/unmount | Step1Setup.tsx:353-369,502-512 | working | SC.10-2.15 |
| CODE.step1-setup.25 | Search input disabled until the Maps SDK loads; SDK load failure shows red hint "Google Maps could not load — check the API key / network." | Step1Setup.tsx:346-350,509-511 | working | SC.10-2.15 |
| CODE.step1-setup.26 | Maps-loader failure is memoized: `loadGoogleMaps` caches the rejected promise, so one transient failure disables address search for the whole session — no retry path short of a page reload | maps.ts:2-7,21; Step1Setup.tsx:346-350 | uncertain (code-analysis; permanent in-session degradation after a transient blip, unobserved in browser) | NONE |
| CODE.step1-setup.27 | Coordinates entry: text field "lat, lng" (placeholder "16.8524, 74.5815") + "Locate" button; accepts exactly 2 comma-separated finite numbers; sets the pending pin with address label = coords at 5 dp | Step1Setup.tsx:514-536 | working | SC.10-2.15 |
| CODE.step1-setup.28 | Coordinate bounds are NOT validated: lat=999 or lng=500 passes `Number.isFinite` and becomes a pending pin (map/tile then fails visually) | Step1Setup.tsx:527-530 | uncertain (edge case, no bounds check; downstream behavior unobserved) | NONE |
| CODE.step1-setup.29 | Map viewport: 300 px-tall rounded panel; first pending location creates the Google Map at zoom 20, `mapTypeId:'satellite'`, all default UI disabled except zoom control, tilt 0; subsequent pendings re-centre the existing map (user zoom preserved) | Step1Setup.tsx:372-386,539-551 | working | SC.10-2.16 |
| CODE.step1-setup.30 | Pin pattern: FIXED red centre pin (MapPin 34 px, #dc2626/#fecaca, drop-shadow, anchored bottom-centre, pointer-events none) + top pill "Drag map to adjust" — you drag the MAP under the pin | Step1Setup.tsx:572-601 | working (census records this exact pattern as confusing-on-touch, to be redesigned) | SC.10-2.16 |
| CODE.step1-setup.31 | Map empty state: centred pin icon + "Search for an address to see satellite view" | Step1Setup.tsx:603-619 | working | SC.10-2.16 |
| CODE.step1-setup.32 | Map error overlay (pin pending but SDK failed): "Satellite view unavailable ({error}) — you can still confirm the typed coordinates." | Step1Setup.tsx:552-571 | working | SC.10-2.16 |
| CODE.step1-setup.33 | "Confirm Location" primary block button, disabled until a pending pin exists | Step1Setup.tsx:650-656 | working | SC.10-2.17 |
| CODE.step1-setup.34 | Confirm source of truth: the draggable map's live centre wins when the SDK loaded; without it the typed pending coords are used — a Maps failure never dead-ends Step 1 | Step1Setup.tsx:388-395 | working | SC.10-2.17 |
| CODE.step1-setup.35 | On confirm the location stores: address, latLng, confirmed:true, irradiance = peakSunHours = built-in latitude model `clamp(4.6…5.9, 5.6 − \|lat−24\|·0.045)` rounded 2 dp (kWh/m²/day, peaks ~23–27°N), dataSource "Built-in irradiance model (latitude fit, ±10%) — verify with site data" | Step1Setup.tsx:396,438-448; solar.ts:20-24 (one hop) | working | SC.10-2.17 |
| CODE.step1-setup.36 | Relocation guard: distance from the previously confirmed pin computed equirectangularly (Δlat×111320 m/°, Δlng×111320·cos lat), wipe triggers only above **25 m** — threshold deliberately generous because `getCenter` jitters metres on map re-layout; ≤25 m re-confirms keep the whole design | Step1Setup.tsx:397-409 | working | SC.10-2.20 |
| CODE.step1-setup.37 | Wipe contents on >25 m relocation: roofs, obstructions, panels, segments, keepouts, walkways, rails, arresters, inverterPlacements, strings, captures, sldParams→null, bomOverrides, coverImageBlobId/coverForLayoutFp→null, derived stamps reset (solarAccessFp/sldOverrides/sldIntroSeen/healthSnapshot), calibration reset to {scaleFactor 1, northOffsetDeg 0, reference null} ("new location = new imagery"), wizardStep → 1 | Step1Setup.tsx:410-433 | working | SC.10-2.20 |
| CODE.step1-setup.38 | Wipe recoverability: the confirm patch is pushed as UNDOABLE only when moved >25 m (one mis-click recoverable with Ctrl+Z; undo stack keeps 25 entries); there is NO confirmation dialog before the wipe — census directs adding a clear confirm | Step1Setup.tsx:434-449; store.tsx:204-207 (one hop) | working (census overlay directs a confirm dialog on top of the kept guard) | SC.10-2.20 |
| CODE.step1-setup.39 | Post-confirm async enhancement 1 — Google Solar Building Insights fetch: decoupled, never blocks the manual flow; result patched onto the LATEST location when it arrives; discarded if the location is no longer confirmed or the pin moved >1e-6° (~0.11 m) since the request | Step1Setup.tsx:450-467; solar.ts:68-70 (one hop) | working | SC.10-2.18 |
| CODE.step1-setup.40 | Post-confirm async enhancement 2 — PVGIS weather fetch (independent of the Solar call; each write spreads the latest location so a slow Solar call cannot drop weather): on success stores `weather`, overwrites irradiance & peak sun hours with measured annual GHI rounded 2 dp, dataSource → "Real irradiance — PVGIS {raddatabase ?? '(measured)'}" | Step1Setup.tsx:468-481; weatherApi.ts:32-40 (one hop) | working | SC.10-2.17 |
| CODE.step1-setup.41 | Confirmed state: disabled green "Location Confirmed" button replaces Confirm | Step1Setup.tsx:623-627 | working | SC.10-2.17 |
| CODE.step1-setup.42 | "Solar Data Fetched" card: Irradiance **{n} kWh/m²/day** · Peak Sun Hours **{n} hrs** · "Source: {dataSource}" provenance line | Step1Setup.tsx:628-639 | working | SC.10-2.17 |
| CODE.step1-setup.43 | "Change Location" secondary button: sets `confirmed:false` only — design, pin and previously fetched data preserved; the map stays on the prior pin (pending state initialized from the saved location on re-entry) | Step1Setup.tsx:331-339,641-647 | working | SC.10-2.15 |
| CODE.step1-setup.44 | Site Intelligence loading state (insights not yet arrived): satellite icon + "Checking Google Solar coverage…" | Step1Setup.tsx:668-677 | working | SC.10-2.18 |
| CODE.step1-setup.45 | Site Intelligence non-ok states: 'unavailable' → "Google Solar has no coverage here — manual design mode"; 'error' → "Google Solar unreachable — manual design mode"; optional detail `message` line below | Step1Setup.tsx:678-692 | working | SC.10-2.18 |
| CODE.step1-setup.46 | Site Intelligence ok header: "Site Intelligence" + "Google Solar" badge + imagery-quality badge (HIGH → good colours, MEDIUM/BASE → warn colours) with tooltip "Imagery quality — confidence indicator" | Step1Setup.tsx:697-715 | working | SC.10-2.19 |
| CODE.step1-setup.47 | Site Intelligence ok stats grid (each item hidden when its field is absent): Max panels (+ "~{kWp} kWp" = round(maxPanels×panelWatts/100)/10), Roof area via `fmtArea` (honours the global m/ft preference), Sunshine "{n} h/yr", Roof faces count | Step1Setup.tsx:693-747 | working | SC.10-2.19 |
| CODE.step1-setup.48 | Site Intelligence ok footer: "Imagery {yyyy-mm-dd \| 'date unknown'} · independent cross-check for your traced design — Google's panel model may differ from your selection" | Step1Setup.tsx:748-752 | working | SC.10-2.19 |
| CODE.step1-setup.49 | Solar API access is proxy-only: browser calls `/api/solar/building-insights?lat&lng` (coords at 6 dp); the Solar-scoped key lives server-side (GOOGLE_SOLAR_API_KEY) | solarApi.ts:1-10,104-107 | working | SC.10-2.18 |
| CODE.step1-setup.50 | Solar client timeout: AbortController at **10 s** (bound chosen to exceed the proxy's 8 s upstream timeout); abort → status 'error' message "Solar API request timed out"; other fetch failure → "Solar API request failed" | solarApi.ts:100-133 | working | SC.10-2.18 |
| CODE.step1-setup.51 | Non-2xx proxy response → status 'error' message "Solar proxy HTTP {status}" | solarApi.ts:108-110 | working | SC.10-2.18 |
| CODE.step1-setup.52 | `fetchBuildingInsights` never throws: always resolves to SolarInsights with status ok/unavailable/error + fetchedAt, so the UI can always explain the data situation | solarApi.ts:84-134; solarApi.test.ts "an empty body maps to all-undefined fields (never throws)" | working | SC.10-2.18 |
| CODE.step1-setup.53 | Insights memo keyed on coords at 6 dp: re-confirming the same pin never re-bills Google; ok/unavailable are cached as facts about the location, 'error' results are NOT cached (retryable); memo hits get a fresh fetchedAt | solarApi.ts:81-97,122 | working | NONE |
| CODE.step1-setup.54 | `mapBuildingInsights` mapping (pure, shared by proxy and tests so the stored shape cannot drift): imageryDate → zero-padded "yyyy-mm-dd"; imageryQuality passthrough (HIGH/MEDIUM/BASE); maxPanels & panelCapacityWatts raw; maxArrayAreaM2, maxSunshineHoursPerYear, roofAreaM2, carbonOffsetFactorKgPerMwh rounded to whole numbers | solarApi.ts:39-79; solarApi.test.ts "rounds area/sunshine/carbon; passes counts and watts raw", "formats the imagery date…" | working | SC.10-2.19 |
| CODE.step1-setup.55 | Roof-segment mapping: only segments with BOTH pitch and azimuth kept (unusable ones filtered, never zero-filled); pitch/azimuth rounded to whole degrees (Google azimuth is already 0=N clockwise — app convention, no conversion); area 1 dp; optional centre lat/lng; `roofSegmentCount` = USABLE count (matches the Step-2 suggestion chips), falling back to raw count only when the mapped list is absent | solarApi.ts:44-54,72-74; solarApi.test.ts "keeps ONLY segments with pitch AND azimuth; azimuth passes unconverted" | working | SC.10-2.19 |
| CODE.step1-setup.56 | `loadGoogleMaps`: single script-tag injection (`libraries=places&loading=async` + global callback `__solarStudioMapsReady`), promise memoized module-wide; resolves immediately if `google.maps` already present; script error → reject "Google Maps failed to load" | maps.ts:1-22 | working | SC.10-2.15 |
| CODE.step1-setup.57 | `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` read client-side for both JS API and static tiles; a missing key is substituted with '' (requests still fire and fail → the UI error states above carry the degradation) | maps.ts:6,16,32-33 | working | NONE |
| CODE.step1-setup.58 | `staticSatelliteUrl`: Google Static Maps satellite tile centred on lat/lng; defaults zoom 19, 640×640, scale 1 (callers actually use zoom 20; the 3D ground texture requests scale 2 = 1280 px) | maps.ts:25-34; SatCanvas.tsx:227; Scene3D.tsx:1162 (one hop) | working | NONE |
| CODE.step1-setup.59 | `metersPerStaticMap`: ground metres covered = 156543.03392·cos(lat)/2^zoom × sizePx (Web-Mercator m/px at equator) | maps.ts:37-45; maps.test.ts regression test | working | NONE |
| CODE.step1-setup.60 | `pickScaleBar`: candidates 1/2/5/10/20/50 m; picks the smallest drawing >56 px at the given px/m, falls back to 50 m; bar px ≡ m × pxPerM exactly (the old inline formula multiplied by a spurious viewport factor and drew ~20% short) | maps.ts:47-61; maps.test.ts "bar px is EXACTLY meters × pxPerM…", "regression: the old viewport-height factor made a 20% error at 800px" | working | NONE |
| CODE.step1-setup.61 | `rescaleProjectGeometry(project,k)`: scales every image-traced coordinate by k about the local origin — roof polygons; obstruction centres + lengthM/widthM/diameterM; panel centres; segment polygons; keepout shapes; walkway endpoints; rail endpoints; arrester positions. Non-finite or ≤0 k → empty patch (no-op) | calibration.ts:23-54; calibration.test.ts "rescales every traced coordinate by k…", "rejects degenerate input" | working | SC.10-3.39 |
| CODE.step1-setup.62 | What calibration does NOT scale (physical, user-entered): heights (roof/obstruction/parapet), regulatory setbacks, walkway widths, panel spec sizes — so capacity (count × watts) and panel count are unchanged; areas scale by k² and energy/BOM recompute consistently | calibration.ts:9-14; calibration.test.ts "capacity is unchanged…", "BOM quantities re-derive…" | working | SC.10-3.39 |
| CODE.step1-setup.63 | Rescale coverage gap: `gridConnection.pos` (user-placed plan-metre point) and `cableRoutes` paths are absent from the rescale list (fields post-date calibration.ts; routes may re-derive, but a placed grid-connection point would stay at its old coordinates and drift off the rescaled plan) | calibration.ts:23-54 vs types.ts:936-946 | uncertain (code-analysis, no test either way; recorded, not resolved) | NONE |
| CODE.step1-setup.64 | `applyKnownDistance(a,b,knownM)`: rejects measured ≤0.05 m or knownM ≤0.05 m (returns null); k = knownM/measured; `calibration.scaleFactor` multiplies CUMULATIVELY (a second calibration composes: k₁·k₂); the reference measurement is stored RESCALED (its stored length round-trips to exactly knownM) — provenance for the report | calibration.ts:60-75; calibration.test.ts "a second calibration composes: factors multiply", "rejects degenerate input" | working | SC.10-3.39 |
| CODE.step1-setup.65 | scaleFactor drives the imagery projector: satellite world span = metersPerStaticMap × scaleFactor in both SatCanvas and the 3D ground plane, so corrected geometry still sits exactly on the imagery | SatCanvas.tsx:97-99; Scene3D.tsx:1161 (one hop); types.ts:858-864 | working | SC.10-3.39 |
| CODE.step1-setup.66 | northOffsetDeg (degrees TRUE north lies clockwise of image-up; 0 = north-up tile default): rotates every engine sun sample by exactly the offset about vertical (altitude and weight preserved; offset 0 is bit-identical to the uncalibrated engine) and rotates the SatCanvas north badge; entered in the Step-2 calibration sheet with a \|offset\| ≤ 45° UI clamp (sheet owned by s2) | types.ts:865-870; calibration.test.ts "rotates every engine sun sample by exactly the offset…", "offset 0 is exactly the uncalibrated engine"; SatCanvas.tsx:327-338; Step2Roof.tsx:3097-3105 (one hop) | working | SC.10-3.39 |
| CODE.step1-setup.67 | Calibration invalidates the geometry fingerprint, so everything downstream (solar access, energy, BOM) recalculates rather than serving stale numbers | calibration.test.ts "calibration invalidates geometryFp (everything downstream recalculates)" | working | SC.10-3.39 |
| CODE.step1-setup.68 | `makeProjector`: equirectangular lat/lng ↔ local-metre projection about an origin; Earth radius 6 378 137 m; m/°lng = m/°lat × cos(origin lat) — "accurate enough at roof scale" | geo.ts:5-26 | working | NONE |
| CODE.step1-setup.69 | Vector helpers: `dist` (Euclidean), `add`, `sub`, `rotate` (degrees, CCW math convention) | geo.ts:28-30,453-465 | working | NONE |
| CODE.step1-setup.70 | `polygonArea`: absolute shoelace area (m²) | geo.ts:32-40 | working | NONE |
| CODE.step1-setup.71 | `polygonPerimeter`: closed-ring perimeter including the closing edge (fencing/earthing-ring quantities); <2 vertices → 0; orientation-independent | geo.ts:42-48; geo.test.ts polygonPerimeter ×3 | working | NONE |
| CODE.step1-setup.72 | `polygonCentroid`: area-weighted centroid; degenerate ring (\|area\| < 1e-9) falls back to the vertex mean | geo.ts:50-71 | working | NONE |
| CODE.step1-setup.73 | `pointInPolygon`: ray-cast even-odd test | geo.ts:73-86; exercised by geo.test.ts inset assertions | working | NONE |
| CODE.step1-setup.74 | `isCCW`: signed-area orientation test | geo.ts:88-97; geo.test.ts "handles clockwise input the same as counter-clockwise" | working | NONE |
| CODE.step1-setup.75 | `insetPolygonRobust` (setback band removal, concave-safe): boolean-subtracts per-edge inward strips (0.02 m outward overhang) + 12-gon vertex disks instead of miter offset; per-edge widths (missing → insets[0] → 0); consecutive vertices <1e-6 m apart deduped and a closing duplicate dropped; coords snapped to a 0.1 mm grid for sweep-line stability; may legitimately return several disjoint regions; regions with <3 vertices or area <0.5 m² discarded; holes discarded ("can't hold panels anyway"); zero/max-inset <1e-4 → copy of source; <3 points → [] | geo.ts:99-221; geo.test.ts insetPolygonRobust ×5 (symmetric shrink, vertices strictly inside, reflex-corner band excluded, zero/consumed, CW==CCW) | working | NONE |
| CODE.step1-setup.76 | Inset crash recovery: if polygon-clipping's sweep line throws, strips are carved ONE AT A TIME, skipping any single strip that still crashes — that edge's setback momentarily relaxes instead of the whole roof reading as "fully consumed" and being hard-blocked | geo.ts:178-204 | working | NONE |
| CODE.step1-setup.77 | `outsetPolygonRobust` (dilation, mirror of the inset via union): per-edge outward strips + vertex disks; same dedupe/snap/12-gon constants; outward parapet bands = outset ring with the original as hole; no minimum-area output filter (rings ≥3 vertices kept); same strip-at-a-time crash recovery (welds) | geo.ts:235-335 | working | NONE |
| CODE.step1-setup.78 | `collinearOverlap` (shared-wall detection between adjacent roofs): segments must be within 2° angular and 0.05 m perpendicular tolerance (defaults); returns overlap length + covered parameter interval on segment A, else null | geo.ts:337-370 | working | NONE |
| CODE.step1-setup.79 | `rectIntersectsPolygon`: convex quad vs polygon — corner-in-polygon, polygon-vertex-in-quad, or any edge-pair intersection (eps 1e-9) | geo.ts:372-385 | working | NONE |
| CODE.step1-setup.80 | `polygonsUnionArea`: total footprint without double-counting overlaps (union; hole rings subtracted); clipper crash → plain per-polygon sum as the "least-wrong" fallback (can over-count overlaps) | geo.ts:387-406 | working | NONE |
| CODE.step1-setup.81 | `differencePolygons`: subject − clips returned as clean outer+holes polygons (holes guaranteed not to touch the boundary → always triangulates without the parapet-band spanning-face artifact); crash → raw subject with clips as holes (still renders) | geo.ts:408-435 | working | NONE |
| CODE.step1-setup.82 | `intersectPolygons`: intersection of two polygons, outer rings only; crash → [] | geo.ts:437-451 | working | NONE |
| CODE.step1-setup.83 | `rectCorners`: rectangle footprint corners from centre/lengthM/widthM/rotationDeg (used for panels & obstructions) | geo.ts:467-482 | working | NONE |
| CODE.step1-setup.84 | `rectsOverlap`: separating-axis test for convex quads | geo.ts:484-511 | working | NONE |
| CODE.step1-setup.85 | `pointSegDist`: point-to-segment distance with closest parameter t clamped to [0,1] (hit-testing) | geo.ts:513-527 | working | NONE |
| CODE.step1-setup.86 | `validateRoofPolygon` (gate before setbacks/layout/3D): ≥3 corners ("A roof needs at least 3 corners."); all coordinates finite ("Every roof point must have a valid coordinate."); every edge ≥ **0.15 m** default ("Roof edges must be at least 0.15 m long."); no crossing or touching edges, eps 1e-8 ("Roof edges cannot cross or touch each other."); area ≥ **0.5 m²** default ("Roof area must be at least 0.5 m².") — plain-language reasons returned for the UI | geo.ts:529-598; geo.test.ts validateRoofPolygon ×4 | working | SC.10-3.18 (rule source; drawing UI that surfaces it is s2's) |
| CODE.step1-setup.87 | `dominantEdgeAngle`: angle of the polygon's longest edge for panel alignment — doc comment says "deg from north" but the formula is `atan2(dy,dx)` (math angle from +x/east); consumers treat it as a grid rotation so behavior is self-consistent; comment/formula discrepancy recorded, not resolved | geo.ts:600-614 | uncertain (in-code comment contradicts formula; product behavior consistent) | NONE |
| CODE.step1-setup.88 | `fmtM`: metres formatted to 2 dp + " m" | geo.ts:616-618 | working | NONE |
| CODE.step1-setup.89 | `genId`: collision-safe entity ids — `{prefix}_{crypto.randomUUID()}` when available; non-secure-context fallback `{prefix}_{epoch base36}_{counter base36}` (old scheme collided across tabs once storage-event merges existed) | geo.ts:620-633 | working | NONE |
| CODE.step1-setup.90 | Analysis worker client: ONE long-lived worker created lazily on first request (per-request spawning would pay module-init on every keystroke of the shading pass) | analysis-client.ts:1-9,19-52 | working | NONE |
| CODE.step1-setup.91 | Request/reply protocol is id-keyed: replies whose id was retired or unknown are ignored — a superseded result is never applied to geometry the user moved past | analysis-client.ts:30-37; analysis-client.test.ts "resolves with the worker's answer, keyed by request id" | working | NONE |
| CODE.step1-setup.92 | `cancelPrevious` option: all in-flight waiters are rejected with `AnalysisSuperseded` (name 'AnalysisSuperseded', message 'superseded by a newer analysis request'); supersession is re-thrown, NOT retried inline (the caller has moved on) | analysis-client.ts:59-89; analysis-client.test.ts "cancelPrevious retires the in-flight request instead of applying stale geometry" | working | NONE |
| CODE.step1-setup.93 | Worker crash (onerror): `workerBroken` latch — worker terminated, all pending waiters rejected, every LATER call runs inline; the rejected callers themselves fall back to inline compute, so results degrade to slower-but-fresh, never silently stale | analysis-client.ts:22-23,38-46,75-80; analysis-client.test.ts "a crashed worker degrades to INLINE compute — never to silent staleness" | working | NONE |
| CODE.step1-setup.94 | No `Worker` global (SSR prerender, unit tests, locked-down browser) or a throwing Worker constructor: `computeSolarAccess` runs inline with the IDENTICAL pure engine — client result equals the direct engine result exactly | analysis-client.ts:25-29,48-51,63-64; analysis-client.test.ts "computes INLINE and returns exactly the engine's own numbers", "an unusable Worker constructor falls back inline without throwing" | working | NONE |
| CODE.step1-setup.95 | `resetAnalysisWorker` test/HMR hook: terminates the worker, clears the broken latch and pending map so the next call re-creates cleanly | analysis-client.ts:91-97 | working | NONE |
| CODE.step1-setup.96 | Worker protocol: request `{id, kind:'access', project}` (whole project via structured clone; worker holds NO state between messages) → response `{id, ok:true, kind:'access', access:[panelId,fraction][]}` — three.js raycast shading engine, no WebGL/DOM | analysis.worker.ts:1-33 | working | NONE |
| CODE.step1-setup.97 | Worker error handling: unknown kind → `{ok:false, error:'unknown kind: {kind}'}`; engine exception → `{ok:false, error: message \| 'analysis failed'}` — errors are per-request; the worker survives | analysis.worker.ts:34-42 | working | NONE |
| CODE.step1-setup.98 | SatCanvas viewport (serves Steps 2/3/6 canvases; Step 1 uses the live Google map): 1000 px logical square canvas over a 640 px zoom-20 static satellite tile; world span = metersPerStaticMap(lat, 20, 640) × calibration scaleFactor; centre-origin metre coordinates, +y = up/north on the image | SatCanvas.tsx:39,96-113 | working | NONE |
| CODE.step1-setup.99 | `SAT_ZOOM = 20` exported constant — single zoom level shared with the 3D ground texture and AI-detect tile math so all surfaces agree on m/px | SatCanvas.tsx:39; Scene3D.tsx:1161, gemini-client.ts:54 (one hop) | working | NONE |
| CODE.step1-setup.100 | `CanvasFrame` context (spanM, sizePx, zoom, pxPerM, toPx, toM) provided to all SVG overlay children; `useCanvasFrame` outside a SatCanvas throws | SatCanvas.tsx:20-37,195 | working | NONE |
| CODE.step1-setup.101 | Initial view: zoom 1.5 (shows as "150%"), pan (0,0) | SatCanvas.tsx:87-88 | working | NONE |
| CODE.step1-setup.102 | Wheel zoom: ×1.12 per notch in, ÷1.12 out; clamped **0.55–5.0**; the world point under the cursor stays fixed (pan compensated exactly) | SatCanvas.tsx:172-187 | working | NONE |
| CODE.step1-setup.103 | Zoom buttons: + is ×1.25, − is ÷1.25 (rounded to 2 dp), same 0.55–5 clamps; live percentage readout in tabular numerals between them; tooltips "Zoom in (scroll wheel)" / "Zoom out" | SatCanvas.tsx:249-283 | working | NONE |
| CODE.step1-setup.104 | Fit view button (Maximize icon, tooltip "Fit view"): resets zoom to 1.5 and pan to 0 | SatCanvas.tsx:285-296 | working | NONE |
| CODE.step1-setup.105 | Panning: middle-button drag ALWAYS pans; left-drag pans when `panEnabled` and the gesture wasn't captured by a tool; ≥3 px cumulative movement marks the gesture as a drag | SatCanvas.tsx:139-162 | working | NONE |
| CODE.step1-setup.106 | Click vs drag separation: pointer-up dispatches `onCanvasClick` only for button 0, gesture not custom, and movement <3 px — a pan never fires a stray click | SatCanvas.tsx:164-170 | working | NONE |
| CODE.step1-setup.107 | Tool capture: `onCanvasDown` returning true switches the gesture to 'custom' mode — panning disabled and no click for that gesture (how draw/drag tools own the pointer); middle-button bypasses capture | SatCanvas.tsx:53-57,139-149 | working | NONE |
| CODE.step1-setup.108 | Pointer capture on down (drags continue outside the element); `touchAction:'none'` — single-pointer interaction only, NO pinch-zoom or two-finger pan exists | SatCanvas.tsx:149,223 | working (census SC.10-3.42 records pinch-zoom/two-finger pan as missing, to add) | SC.10-3.42 |
| CODE.step1-setup.109 | `eventToMeters`: pointer position → metre coords via the overlay svg's bounding rect (correct under the CSS transform); missing svg → (0,0) | SatCanvas.tsx:130-137 | working | NONE |
| CODE.step1-setup.110 | Base imagery presentation: brightness 0.94 normally; `dim` mode 0.6 brightness + 0.9 saturate (focus modes); `imageRendering:'pixelated'` above zoom 2.2 (honest pixels instead of smear); drag-disabled, aria-hidden | SatCanvas.tsx:226-240 | working | NONE |
| CODE.step1-setup.111 | Scale bar (bottom-left): end-capped bar + "{m} m" label computed from the SAME frame.pxPerM used for hit-testing, so the drawn bar can never disagree with the geometry | SatCanvas.tsx:189-192,340-354; maps.test.ts regression | working | NONE |
| CODE.step1-setup.112 | North badge (bottom-left "N" pill): arrow SVG rotated by calibration northOffsetDeg — points at TRUE north, not image-up | SatCanvas.tsx:299-339 | working | SC.10-3.39 |
| CODE.step1-setup.113 | `centerOn(p, targetZoom?)` imperative handle: pans (and optionally zooms) so a world point lands dead-centre — pan = −(basePx − sizePx/2)·zoom, exact at any zoom (used to jump the view to entities) | SatCanvas.tsx:41-45,116-128 | working | NONE |
| CODE.step1-setup.114 | Accessibility & chrome: container role="application" aria-label "Satellite roof drawing canvas"; zoom cluster role="group" aria-label "Zoom controls"; `hud` slot renders extra chrome (rails, pills) above the canvas | SatCanvas.tsx:194-209,249-255,357 | working | NONE |
| CODE.step1-setup.115 | `polyPath`: polygon → SVG path string in px coords (1 dp), optional close — shared by every overlay renderer | SatCanvas.tsx:363-372 | working | NONE |

## CENSUS-UNMATCHED (entries from A.10-1 / A.10-2 with no code row in this area — regression-risk list)

| Census entry | Entry (abridged) | Note |
|---|---|---|
| SC.10-2.01 | Pre-fill from survey (Phase 5): address/location/site details carried over, editable | NO code exists anywhere in the POC — no survey module; Step 1 always starts from the blank/saved project. v2-planned behavior with zero POC implementation. |
| SC.10-1.01 | Entry/design list: studio opens from lead "Create design" | Not in step1-setup files; expected owner: shell/design-list sitting (Dashboard/Wizard/router, s10). Cross-check that sitting's inventory. |
| SC.10-1.02 | A lead holds several variants | Not in step1-setup files; expected s10/s13 (§M05.13). |
| SC.10-1.03 | Design-list actions: new/duplicate/open | Not in step1-setup files; expected s9/s10. |
| SC.10-1.04 | 10-step wizard structure | Not in step1-setup files; Wizard.tsx (s10). |
| SC.10-1.05 | Step states not-started/in-progress/done/has-errors | Not in step1-setup files; Wizard.tsx (s10). |
| SC.10-1.06 | Compact step indicator / step rail | Not in step1-setup files; Wizard.tsx (s10). |
| SC.10-1.07 | Header: back | Not in step1-setup files; Wizard.tsx (s10). |
| SC.10-1.08 | Header: step title | Not in step1-setup files; Wizard.tsx (s10). |
| SC.10-1.09 | Header: Design Health chip | Not in step1-setup files; s5/s10. |
| SC.10-1.10 | Header: units toggle (m/ft global) | Not in step1-setup files; units.ts (s8) / Wizard (s10). Step 1 honours it only via fmtArea in the Site Intelligence card (row 47). |
| SC.10-1.11 | Header: Save | Not in step1-setup files; s10. |
| SC.10-1.12 | Header: Save & exit | Not in step1-setup files; s10. |
| SC.10-1.13 | Header: Help (per-step) | Not in step1-setup files; step-help (s10). |
| SC.10-1.14 | Header: Next/Done | Not in step1-setup files; Wizard.tsx (s10) — includes the Step-1 Next gate (location confirmed), which lives in Wizard, not Step1Setup. |
| SC.10-1.15 | Per-step Next gate with plain reason | Not in step1-setup files; Wizard.tsx (s10). |
| SC.10-1.16 | Hard electrical gate on layout step | Not in step1-setup files; s7/s10. |
| SC.10-1.17 | Design Health score/band | Not in step1-setup files; health.ts (s5). |
| SC.10-1.18 | Health sheet per-category deductions | Not in step1-setup files; s5. |
| SC.10-1.19 | Health "what changed since last save" delta | Not in step1-setup files; s5. |
| SC.10-1.20 | Design Health provisional while shading recalculates | Not in step1-setup files; s5 (useHealthSync). The recompute MECHANISM (worker + supersede protocol) is rows 90-97 here, but the provisional UI state is s5's. |

---

rowCount: 115
defectCount: 2 (rows 11, 21 — both docs-contradict)
censusUnmatched: 21 (1 genuinely unimplemented: SC.10-2.01; 20 are A.10-1 shell/health entries owned by other sittings' files — cross-check needed, not step1 regressions)
newBeyondCensus: 57 (rows with census NONE)
notes:
- All 33 area tests pass; no failing-test defects. Deps installed from lockfile only; no source modified.
- Defect 1: Ground-mount card is PRO-locked with a dead toggle — census SC.10-2.14/D38 forbids the tier gate.
- Defect 2: "Max size: 5 MB" logo cap is advertised but never enforced; unbounded data-URL enters the project record.
- 4 uncertain rows: memoized maps-load failure (no in-session retry, row 26); no lat/lng bounds check (row 28); calibration rescale misses gridConnection.pos/cableRoutes (row 63); dominantEdgeAngle comment/formula mismatch (row 87).
- Census rows SC.10-2.16 (fixed pin) and SC.10-2.20 (silent >25 m wipe, undo-only) match code exactly; both carry census redesign directives recorded in-row.
- Calibration/north rows cite SC.10-3.39 and SatCanvas touch gap cites SC.10-3.42 (outside my 10-1/10-2 diff duty, cited for accuracy; 10-3 diff belongs to sitting 2).
