# Feature inventory — step3-obstructions (Step 3 · Obstructions: placing/editing roof obstacles that shade or block panels)

Pass-two inventory, 2026-08-05 (Sitting 3). Primary files (under `3d_design_studio/src/features/solar-studio/`): `screens/Step3Obstructions.tsx` (1,103 ln) · `three/ObstructionMesh.tsx` (707 ln).

Test run (read-only, deps from lockfile): `npx vitest run` on the 3 obstruction test files → **3 files, 60 tests, all PASS** (vitest 3.2.7, 1.0 s): obstruction-grounding (27) · obstruction-assets (8) · capabilities (25). One defect (dead Casts-shadow toggle) additionally reproduced live with `npx tsx` against the real modules.

One-hop libraries consulted but owned elsewhere: `lib/roof-factory.ts` OBSTRUCTION_PRESETS/makeObstruction/obstructionToPlatform (s2 owns the file; the obstruction behaviors are inventoried HERE because Step 3 is their only UI), `lib/capabilities.ts` + `lib/structure-edit.ts` reconcileBridgedPanels + `lib/shading.ts` (s5), `lib/ground.ts` + `lib/geo.ts` + `components/SatCanvas.tsx` (s1), `components/MeasureTool.tsx` + `components/EdgeLabels.tsx` (s2), `components/ui.tsx` Sheet/SliderRow/ToggleRow/OptionCard/UnitToggle (s8/shared), `lib/units.ts` (s8), `store/store.tsx` (s10), `three/Scene3D.tsx` (s6 3D sitting).

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step3-obstructions.1 | Full-screen `SatCanvas` at the project pin with calibration `scaleFactor` + `northOffsetDeg`; cursor semantics: `copy` while placing, `crosshair` while measuring (measure wins if both), `grabbing` mid-drag, else default; **map pan/zoom disabled while placing or mid-drag** (`panEnabled={!placing && !drag}`) | Step3Obstructions.tsx:291-297 | working | SC.10-4.07 |
| CODE.step3-obstructions.2 | Roofs render as a READ-ONLY backdrop: fill rgba(59,130,246,0.3), stroke #3b82f6 1.5px, plus each roof's red-dashed setback inset (#f87171, 1.2px, dash 6/4) from `insetPolygonRobust` over per-edge or uniform setbacks; no roof edit affordance exists on this screen | Step3Obstructions.tsx:327-328,765-805 | working | SC.10-4.01 |
| CODE.step3-obstructions.3 | Every obstruction drawn to scale: rect = rotated polygon via `rectCorners`, circle = radius diameterM/2·pxPerM; selected fill rgba(250,204,21,0.5)/stroke #facc15, idle fill rgba(30,41,59,0.75)/stroke #e2e8f0, width 1.5 | Step3Obstructions.tsx:1059-1069,1092-1103 | working | SC.10-4.02 |
| CODE.step3-obstructions.4 | ID chip above each object: 30×13px dark rect (#0f172a) with the label (e.g. WT1) at 8.5px bold white, counter-scaled `scale(1/zoom)` so it stays constant screen size at any zoom | Step3Obstructions.tsx:1071-1084 | working | SC.10-4.02 |
| CODE.step3-obstructions.5 | Red-dashed setback ring per BLOCKING object only (`o.blocksPlacement`): CIRCLE of radius (max(L,W) or diameter)/2 + setbackM, fill rgba(248,113,113,0.12), stroke #f87171 0.8px dash 4/3; ring resizes live with setback/size edits. Note: the ring is a circular approximation even for rectangles (the true keep-out is computed in layout.ts, s5) | Step3Obstructions.tsx:1033-1036,1048-1058 | working | SC.10-4.03 |
| CODE.step3-obstructions.6 | North badge (arrow rotated by `northOffsetDeg` to true north) and graphic scale bar (borrowed px/m, always labeled in **m** regardless of unit preference) — both provided by SatCanvas (s1) and present on this screen | SatCanvas.tsx:299-355 (one hop) | working | SC.10-4.04, SC.10-4.05 |
| CODE.step3-obstructions.7 | Zoom cluster (SatCanvas, s1): zoom in/out ×1.25 clamped 0.55–5, live % readout, Fit view (resets zoom 1.5, pan 0,0), wheel-zoom keeping cursor point fixed | SatCanvas.tsx:172-188,249-297 (one hop) | working | SC.10-4.06 |
| CODE.step3-obstructions.8 | Undo/redo: every edit records correctly (place/duplicate/delete/sheet-change = 1 undoable patch; drag gesture = exactly 1 via `histRef` first-change latch; 25-snapshot cap) but **no undo/redo button or keyboard shortcut exists anywhere on Step 3** — only Step2Roof and Step6Editor dispatch `{type:'undo'}`; an Esc-cancelled or mistaken drag cannot be undone without leaving the step | Step3Obstructions.tsx:123,149-157; store.tsx:204-206; grep: dispatch 'undo' only in Step2Roof.tsx:873,1014 and Step6Editor.tsx:1026,1567 | POC-DEFECT(no undo/redo trigger on this screen; census requires it always-on-screen) | SC.10-4.08 |
| CODE.step3-obstructions.9 | Units m/ft: global `useUnits` preference; UnitToggle in the Size sheet header (and the Wizard footer, s10); dims stored in meters, inputs show/accept the display unit (`uf = M_TO_FT`), displayed at 2dp; all readouts via `fmtLen`/`lenValue` | Step3Obstructions.tsx:112-116,533-538; Wizard.tsx:290-291 (one hop) | working | SC.10-4.09 |
| CODE.step3-obstructions.10 | Tool rail (bottom-left, dark, role=group "Obstruction tools") — exactly 5 tools. Tool 1 Add obstruction: opens the type picker sheet; carries a count badge when obstructions > 0; button styles `accent` while placing | Step3Obstructions.tsx:393-411,456 | working | SC.10-4.10 |
| CODE.step3-obstructions.11 | Tool 2 Show/Hide obstructions (Eye/EyeOff, aria-pressed): toggling clears the selection; while hidden the obstruction layer AND selection overlay unmount, canvas clicks only deselect; choosing a type in the picker force-unhides (`setHidden(false)`) | Step3Obstructions.tsx:305-308,329,349,412-424,477 | working | SC.10-4.11 |
| CODE.step3-obstructions.12 | Tool 3 Show all measurements: edge-length labels on every ROOF edge only (`EdgeLabels` inside RoofBackdrop) — census/doc also require rectangular-OBSTRUCTION edge labels, which are not rendered anywhere | Step3Obstructions.tsx:425-434,799; phase-10-prompts.md:422-423 | POC-DEFECT(rect-obstruction edge labels missing; roof edges only) | SC.10-4.12 |
| CODE.step3-obstructions.13 | Tool 4 Measure distance: toggle (aria-pressed), two-point ruler with 2dp formatted overlay; measure clicks take precedence over placement clicks; restart clears prior, toggle-off clears (MeasureTool, s2) | Step3Obstructions.tsx:110,299,370,436-444; MeasureTool.tsx (one hop) | working | SC.10-4.13 |
| CODE.step3-obstructions.14 | Tool 5 Open 3D view: replaces the whole step with `Scene3D` (initialViewMode "mesh") until closed, then returns to Step 3 with state intact | Step3Obstructions.tsx:111,285-287,445-453 | working | SC.10-4.14 |
| CODE.step3-obstructions.15 | Type picker sheet ("Add obstruction", Plus icon): grid auto-fill minmax(110px,1fr) of 11 cards, each with icon, name, default size caption "L×W×H m" from OBSTRUCTION_PRESETS — Tank WT 2×1.5×1.2 (Cylinder) · Dish DS 1×1×1.2 (Satellite) · Chimney CH 0.8×0.8×2 (Factory) · Tree TR 3×3×5 (TreePine) · Elevated EL 3×2.5×2.6 (Layers) · Building BL 8×6×9 (Building2) · Solar WH SW 2×1.2×1.5 (Flame) · Ladder LD 0.6×1.5×3 (ArrowUpFromLine, **BETA badge**) · Windmill WM 1.8×1.8×3.5 (Wind) · Turbine Vent TV 0.4×0.4×0.5 (Fan) · Other OB 1.5×1.5×1 (Package) — all values exactly per census | Step3Obstructions.tsx:59-87,456-507; roof-factory.ts:196-211 | working | SC.10-4.15 |
| CODE.step3-obstructions.16 | Placement mode: picking a card closes the sheet, un-hides obstructions, shows hint bar (role=status) "Click to place {label} · Esc to cancel"; the next canvas click drops ONE object at default size, exits placement, and auto-selects it; Esc cancels without placing; Add button stays visually active | Step3Obstructions.tsx:159-171,211-214,374-391,475-479 | working | SC.10-4.16 |
| CODE.step3-obstructions.17 | Construction defaults via the shared factory (identical for hand-placed and AI-imported): roofId auto-resolved from the drop position (`resolveAnchorRoofId` → owning roof or null = "on ground"); label = type code + (count of same type + 1); shape circle for tree/windmill else rect; diameterM initialised to L; L/W floored at 0.3, height at 0.1; rotation 0; setbackM 0.5; castsShadow + blocksPlacement true; FULL capability preset written explicitly. **Label is count-based, not "next free number"**: delete WT1 while WT2 exists → the next tank is labelled WT2 again (duplicate chip) | Step3Obstructions.tsx:159-171; roof-factory.ts:213,220-255; test capabilities "factory obstructions carry the FULL preset explicitly" | POC-DEFECT(label numbering collides after deletions; doc 10.4 §3 says "next free number") | SC.10-4.17 |
| CODE.step3-obstructions.18 | Per-type capability presets (defaults, all per-instance editable): tank & solar_wh panelsMayCross + maintenanceAccess 'top' + requiresEngineerConfirmation; dish panelsMayCross + removable; ladder panelsMayCross + minVerticalClearance 0.5 + removable; chimney/turbine_vent/windmill mustRemainOpenToSky; tree maintenanceAccess 'none'; elevated supportsStructuralLoad; building/other base (no bridging). BASE: clearance 0.3 m, legsAllowedNearby, perimeter access, castsAnalyticalShadow | capabilities.ts:12-48 (one hop, surfaced only through this screen's Settings); test capabilities 25 PASS | working | NONE (type-specific bridging defaults beyond census) |
| CODE.step3-obstructions.19 | Click-select: topmost (most recently added first — array reversed) object whose center is within max(L,W,diameter)/2 + 0.3 m of the click; empty click deselects; pointerdown directly on a shape also selects (with capture). Quirk: the radial test under-covers large rectangles (8×6 building: corners at 5 m > 4.3 m radius), so the click that follows a corner pointerdown can hit-test to nothing and immediately deselect | Step3Obstructions.tsx:298-314,1040-1046 | uncertain (code-analysis: corner select/deselect race depends on React flush order between pointerup and click) | NONE |
| CODE.step3-obstructions.20 | Move: pointerdown on the body begins a move-drag keeping the grab point under the cursor (offset = center − last pointer pos); every live move **re-resolves the owning roof** in `update()` (stronger than census's "on release") and re-reconciles bridged panels in the same patch; release, click, or pointer-leaving-with-buttons-up ends it; Esc cancels the drag WITHOUT reverting the applied moves (and no undo is reachable on this screen — see row 8) | Step3Obstructions.tsx:117-146,252-258,315-325,333-346 | working | SC.10-4.18 |
| CODE.step3-obstructions.21 | Resize rectangle: 4 corner handles (9px/zoom white squares, yellow stroke, cursor alternates nwse/nesw by corner parity); drag sets lengthM = 2·abs(local.x), widthM = 2·abs(local.y) in the object's rotated frame — symmetric about the center, floored at 0.3 m, rounded 2dp | Step3Obstructions.tsx:275-279,846-848,888-902 | working | SC.10-4.19, SC.10-4.22 |
| CODE.step3-obstructions.22 | Resize circle: single east handle (5.5px/zoom circle, cursor ew-resize, corner index −1); diameter = 2 × distance(cursor, center), floored at 0.3 m, 2dp | Step3Obstructions.tsx:269-274,849-850,903-914 | working | SC.10-4.20, SC.10-4.22 |
| CODE.step3-obstructions.23 | Rotate: white circular handle on a yellow dashed stem 1.2 m above the object's half-height edge (rotates with the object); drag sets rotation = atan2 of the handle vector, Shift = 15° steps, free = 1° rounding, normalised 0–359; 0.05 m dead zone around center. The 15° snap is modifier-key-only (doc §4 requires on-screen equivalents; exact degrees ARE available via the Size sheet slider) | Step3Obstructions.tsx:260-267,851-887 | working (touch directive gap recorded: snap is Shift-held only) | SC.10-4.21 |
| CODE.step3-obstructions.24 | Locked object: all handles hidden (`!locked` gate), body drag refused at pointerdown, Delete/Backspace and arrow nudges refused, Shape/Size/Delete buttons disabled (opacity 0.35) with tooltips "Unlock to edit shape"/"Unlock to resize"/"Unlock to delete"; Duplicate and Settings stay available; lock is session-only React state (Set) — not persisted, not undoable, copy from Duplicate starts unlocked | Step3Obstructions.tsx:118,126,196-204,224-245,335,867,952-999 | working | SC.10-4.23 |
| CODE.step3-obstructions.25 | Keyboard: Esc priority chain (cancel placing → let Sheet self-close → cancel drag → deselect); Delete/Backspace deletes the selection unless locked; Arrow keys nudge 0.1 m (Shift = 1 m) with 2dp rounding, each nudge = one undo entry and re-resolves the roof; all suppressed when focus is in input/textarea/select/contenteditable or a sheet is open; handler re-registered every render for fresh state. Nudge is keyboard-only — no on-screen nudge control exists (doc §4 directive) | Step3Obstructions.tsx:206-250 | working (touch directive gap recorded: no on-screen nudge) | SC.10-4.31 |
| CODE.step3-obstructions.26 | Floating context bar: counter-scaled foreignObject centered below the object, offset past max(body half-diagonal, setback ring) + 16px so it clears both; role=toolbar labelled "{Type} {label} actions"; leads with "{Type} · {label}" text; contains **exactly seven actions** (Duplicate · Shape · Size & rotation · Settings · Lock/Unlock · Delete · Deselect) with separators | Step3Obstructions.tsx:856-863,918-1012 | working | SC.10-4.24–SC.10-4.30 |
| CODE.step3-obstructions.27 | Duplicate: copy offset (+2, +2) m, labelled `{label}c` (repeat duplicates give "WT1cc"), roof anchor re-resolved for the new position, copy selected, one undo step; available while locked | Step3Obstructions.tsx:173-188,944-951 | working | SC.10-4.24 |
| CODE.step3-obstructions.28 | Shape sheet: two OptionCards — Rectangle ("Rectangular shape with length and width", Square icon) / Circle ("Circular shape with diameter", CircleIcon) — switchable any time; the stored L/W and diameter both persist so switching back restores prior dims; each switch = one undoable patch + bridged-panel reconcile | Step3Obstructions.tsx:509-526 | working | SC.10-4.25, SC.10-4.32 |
| CODE.step3-obstructions.29 | Size & rotation sheet: typed inputs in the display unit (UnitToggle in header) — rect: Length/Width/Height; circle: Diameter/Height; step 0.1; Rotation slider 0–359° in 1° steps. **Typed values bypass the 0.3 m floor entirely** (no clamp on this path — an emptied field commits 0, negatives accepted, heightM 0 reaches the shading engine), and the raw controlled `<input type=number>` pushes **one undo entry per keystroke** (commit-on-blur `NumberField` exists in ui.tsx:394 for exactly this but is not used here) | Step3Obstructions.tsx:528-601 | POC-DEFECT(typed dims unclamped below 0.3 m/0, violating SC.10-4.22; keystroke-grain undo) | SC.10-4.26, SC.10-4.33, SC.10-4.22 |
| CODE.step3-obstructions.30 | Sheet control undo granularity: SliderRow fires `onChange` per native input event and every Step-3 `update()` defaults undoable — dragging the Setback/Rotation/Clearance slider emits dozens of undo entries and can flush the whole 25-snapshot stack in one gesture (census: one gesture = one step) | ui.tsx:144-208 (one hop); Step3Obstructions.tsx:129-146,592-616,653-663; store.tsx:204-206 | POC-DEFECT(slider drag = N undo steps, not 1) | SC.10-4.08 |
| CODE.step3-obstructions.31 | Settings — Setback slider: 0–3 m in 0.1 steps (values metric; display converted via `lenValue(v,1)` + lenUnit), hint "Buffer zone where panels cannot be placed"; ring redraws live | Step3Obstructions.tsx:606-616 | working | SC.10-4.35 |
| CODE.step3-obstructions.32 | Settings — Casts shadow toggle writes only the legacy `castsShadow` boolean, but every factory-created obstruction carries `capabilities.castsAnalyticalShadow: true` EXPLICITLY, and the resolver prefers the explicit field — so **for every object placed in Step 3 (or AI-imported) the toggle is dead**: shading engine and 3D `castShadow` both keep casting via `castsAnalyticalShadow()`. Reproduced live: factory tank with castsShadow=false still resolves true; only legacy objects (capabilities undefined) respond. UI reads back `selected.castsShadow` so the switch even displays the ineffective state | Step3Obstructions.tsx:617-621; capabilities.ts:55-71,84-86; roof-factory.ts:250; ObstructionMesh.tsx:159; tsx repro: `castsAnalyticalShadow({...factoryTank, castsShadow:false}) === true` | POC-DEFECT(toggle has no effect on shading/3D for factory-created obstructions — explicit capability preset wins over the boolean the toggle writes) | SC.10-4.36 |
| CODE.step3-obstructions.33 | Settings — Blocks panel placement toggle: writes `blocksPlacement` (this one WORKS — it is read raw as the bridging master switch and by layout, and controls the ring); switching ON reveals the nested BRIDGING card ("BRIDGING (§ structure may span above)") | Step3Obstructions.tsx:622-641,1048; capabilities.ts:98-104 | working | SC.10-4.37 |
| CODE.step3-obstructions.34 | Bridging chain — "Panels may bridge above" toggle writes `capabilities.panelsMayCross` explicitly (effective; verified by reconcile test "turning bridging OFF disables the spanning panels"); when ON reveals "Must remain open to sky" | Step3Obstructions.tsx:639-650; test capabilities:336-344 | working | SC.10-4.38 |
| CODE.step3-obstructions.35 | Bridging chain — "Must remain open to sky" ON blocks bridging regardless of clearance; when OFF reveals "Clearance above it" slider 0–1 m in 0.05 steps (2dp) with live hint "Bridgeable when the array structure clears {heightM+clearance} m (its {heightM} m + margin)"; engineer warning "Bridging over this is flagged for engineer confirmation." shows when `requiresEngineerConfirmation` resolves true (preset: tank & solar_wh; dish/ladder bridge without the warning — capability-driven, slightly narrower than the doc's unconditional wording) | Step3Obstructions.tsx:645-672; capabilities.ts:89-104; tests capabilities:103-116,185-187 | working | SC.10-4.39 |
| CODE.step3-obstructions.36 | Height drives shadow + bridging maths end-to-end: every height/size/position/capability edit runs `reconcileBridgedPanels` in the SAME patch — raising a bridged tank past the structure clearance disables the spanning panels, lowering re-enables (symmetric); required clearance = heightM + minVerticalClearanceM | Step3Obstructions.tsx:142-145; capabilities.ts:89-91; tests capabilities:324-334 ("raising the obstruction HEIGHT past the clearance disables (Step3 path)"), 306-312 | working | SC.10-4.34 |
| CODE.step3-obstructions.37 | Convert to rooftop platform: one-tap replaces the obstruction with a stacked roof named "{label} platform" at (base roof height + obstruction height), parented to the base roof; circles become a 12-gon; polygon must pass the roof sanitiser else the button is disabled with reason "Top surface is too small to become a usable roof."; enabled hint states the height and "Structural adequacy of the platform needs engineer verification."; one undoable patch, sheet closes, selection clears; panels then mount on it via the normal roof pipeline | Step3Obstructions.tsx:676-703; roof-factory.ts:269-313; tests capabilities:253-281 (rect + circle-12-gon + too-small null) | working | SC.10-4.40 |
| CODE.step3-obstructions.38 | HEIGHT INFO card (read-only): Placement = anchored roof's name or "On ground"; Base surface = fmtLen(roof.heightM or 0, 1dp); Top from ground = fmtLen(base + heightM) bold. Note: base uses the anchored roof's eave `heightM`, not the local pitched-surface height the 3D scene grounds with (`obstructionBaseY`) — numbers can differ on a pitched roof | Step3Obstructions.tsx:282,704-727 | working (pitched-roof base readout uses eave height; recorded) | SC.10-4.41 |
| CODE.step3-obstructions.39 | Settings footer: type icon + "{Type label} · {id label}" identity line | Step3Obstructions.tsx:728-742 | working | NONE |
| CODE.step3-obstructions.40 | Everything derived recomputes live per render from the store: owning roof (re-resolved on every center change), base/top heights, required bridging clearance in the hint, setback ring radius, Add-button count badge | Step3Obstructions.tsx:129-146,408-410,662,704-727,1033-1036 | working | SC.10-4.42 |
| CODE.step3-obstructions.41 | Empty state: no dedicated guidance when 0 obstructions — just the bare canvas + tool rail (doc's STATES list names "empty — no obstructions yet" as a design frame; census has no numbered entry) | Step3Obstructions.tsx:289-454 (absence); phase-10-prompts.md:550-551 | working (no empty-state prompt; not census-listed) | NONE |
| CODE.step3-obstructions.42 | `ObstructionLayer` is exported and reused read-only (no `onDown`) by Step6Editor so the panel-layout screen shows the same shapes/chips/rings | Step3Obstructions.tsx:1017-1090; Step6Editor.tsx:51 (one hop) | working | NONE |
| CODE.step3-obstructions.43 | 3D placement: each obstruction renders at [x, baseY, −y] with yaw −rotationDeg; baseY comes from `obstructionBaseY` which resolves the surface from POSITION (stale roofId healed, explicit ground kept, deleted-roof fallback, higher roof wins) — pinned by 27 grounding tests incl. "the SHADING caster is grounded identically to the visual mesh (§A0)" | ObstructionMesh.tsx:157-273; Scene3D.tsx:1298-1300 (one hop); test obstruction-grounding 27 PASS | working | NONE |
| CODE.step3-obstructions.44 | Visual/analytic shadow parity: mesh `castShadow` uses the same `castsAnalyticalShadow(o)` predicate as the shading engine (userData.shadowCaster carried onto every part), so the 3D shadow and the energy math can't drift | ObstructionMesh.tsx:158-161,27; capabilities.ts:84-86 | working | NONE |
| CODE.step3-obstructions.45 | Lazy GLB pipeline: 6 types have models (tree, tank, chimney, dish, solar_wh, turbine_vent; ~169 MB total); nothing preloads at module scope — `useWarmObstructionAssets` prefetches only the types the CURRENT project contains, once the 3D scene mounts; first render of a type suspends into its procedural fallback while streaming | ObstructionMesh.tsx:1-11,50-81; Scene3D.tsx:1086 (one hop) | working | NONE |
| CODE.step3-obstructions.46 | `AssetBoundary` error boundary: a 404/corrupt GLB throws from the loader — caught and rendered as the same procedural fallback used for the Suspense loading path, so a missing asset never crashes the scene | ObstructionMesh.tsx:29-48 | working | NONE |
| CODE.step3-obstructions.47 | GLB scaling convention: models normalised to 1 m tall origin-at-base, so scaleY = heightM directly; footprint scaled by target/measured-ref per axis, all axes clamped ≥ 0.05; per-type target: tank & dish use diameter (circle) or **min(L,W)** (rect); chimney, solar_wh, turbine_vent use L×W (or diameter); tree & windmill use diameter or **max(L,W)**; measured refs (TANK 0.73888/0.73929 · CHIMNEY 0.89661/0.76548 · DISH 0.62657/0.60935 · SOLAR_WH 2.18336/2.33074 · TURBINE_VENT 1.05162/1.05168 · TREE 1.2623/1.89677/1.21922) are pinned against the real GLB binary accessors by obstruction-assets.test.ts (8 PASS, incl. "every shipped model is covered by this table") | ObstructionMesh.tsx:143-155,501,592-598,600-706; test obstruction-assets 8 PASS | working | NONE |
| CODE.step3-obstructions.48 | `GroundedClone`: measures the real post-scale bounding box at runtime and lifts the model by −min.y so its lowest point rests at the surface — regression armour for the "levitating/buried tree" (tree.glb is authored 1.9 m tall, origin at bbox CENTRE, minY −0.9508) | ObstructionMesh.tsx:83-133,489-501; test obstruction-assets "tree: authored height and origin are unchanged" | working | NONE |
| CODE.step3-obstructions.49 | Tree: GLB with continuous sway animation (rotation.z ±0.02 rad at 0.6 Hz, rotation.x ±0.015 at 0.5 Hz, about the BASE); procedural fallback = tapered trunk (height max(0.6, 0.4·h)) + two green spheres (crown radius max(0.5, r, 0.55·(h−trunk))) | ObstructionMesh.tsx:175-185,327-354,503-538 | working | NONE |
| CODE.step3-obstructions.50 | Windmill: GLB deliberately NOT shipped/preloaded (would 404) — always renders the procedural mesh: tapered tower 86% of height, nacelle box, hub + 3 blades spinning at 4.2 rad/s; blades castShadow=false (excluded from shadows); `WindmillAsset` + placeholder ref {1,1} kept as dead code awaiting the asset | ObstructionMesh.tsx:236-244,275-325,547-598 | working | NONE |
| CODE.step3-obstructions.51 | Turbine vent: GLB variant spins the ENTIRE model (base included) at 3.5 rad/s — the group ref wraps the whole Clone; procedural variant spins only the 8-blade dome above a static base cylinder | ObstructionMesh.tsx:246-253,452-487,682-706 | working (whole-body spin on the GLB path recorded as a visual quirk) | NONE |
| CODE.step3-obstructions.52 | Building: no GLB — box (L×H×W) or 20-segment cylinder with a deterministic tint hashed from the obstruction id over 3 muted tones (#8d8579/#9a9287/#7f7a70), roughness 0.95 | ObstructionMesh.tsx:135-141,187-205 | working | NONE |
| CODE.step3-obstructions.53 | Default mesh for elevated/ladder/other (and any future type): gray #7d8590 box or cylinder at heightM; cylinder radius for rect-shaped objects = min(L,W)/2 clamped ≥ 0.2 across all procedural paths | ObstructionMesh.tsx:162-163,255-272 | working | NONE |
| CODE.step3-obstructions.54 | Tank/chimney/dish/solar_wh procedural fallbacks: tank = cylinder + protruding lid disc (r×1.05, 0.07 thick); chimney = shape-aware box/cylinder in brick tone; dish = pole (60% of height) + flattened sphere (scale y 0.32) tilted −0.6 rad; solar_wh = gray box/cylinder | ObstructionMesh.tsx:356-450 | working | NONE |

## CENSUS-UNMATCHED (Appendix A §10-4 entries with no code behavior)

None — all 42 entries `SC.10-4.01`–`SC.10-4.42` are matched above (SC.10-4.08 and SC.10-4.12 matched as POC-DEFECT; SC.10-4.22 partially defective on the typed path; SC.10-4.36 matched as POC-DEFECT).

## Summary

- rowCount: 54
- defectCount: 6 (rows 8 undo/redo missing on screen · 12 no rect-obstruction edge labels · 17 label collision after deletion · 29 typed dims unclamped below 0.3 m + keystroke undo · 30 slider undo spam · 32 dead Casts-shadow toggle)
- censusUnmatched: 0 of 42
- newBeyondCensus: 17 (rows 18, 19, 39, 41–54 — capability presets, select mechanics, identity footer, empty state, Step6 reuse, and all 12 3D-mesh/GLB behaviors)
- notes: The headline defect is row 32 — the Casts-shadow toggle silently does nothing for every obstruction created in this build (verified by live repro), the exact mirror of the bug capabilities.ts says it fixed. Row 8 is the second structural gap: Step 3 records perfect undo history it gives the user no way to invoke. All 60 obstruction tests pass; none cover the two defective UI paths (toggle, typed-size clamp). The bridging chain, platform conversion, grounding/stale-anchor healing and GLB contracts are all strongly test-pinned and census-faithful. Direct manipulation is complete but desktop-biased (Shift-snap, arrow-nudge) against the doc's touch-first directive.
