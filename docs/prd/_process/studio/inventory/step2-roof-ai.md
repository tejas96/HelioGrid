# Feature inventory — step2-roof-ai (Step 2 · Roof: AI detection pipeline, artifact doorway, server proxies)

Pass-two inventory, 2026-08-05. Sitting 2 (AI half) primary files: `lib/roof-ai/` (all 9: `artifact.ts`, `detect-client.ts`, `detect.worker.ts`, `gemini-client.ts`, `geotiff-decode.ts`, `pipeline.ts`, `plane-fit.ts`, `utm.ts`, `vectorize.ts`) under `3d_design_studio/src/features/solar-studio/`, plus `src/app/api/gemini/route.ts`, `src/app/api/solar/building-insights/route.ts`, `src/app/api/solar/data-layers/route.ts`, `src/app/api/solar/geotiff/route.ts`, `src/app/api/solar/key.ts`.

Test run (read-only, deps from lockfile):
`npx vitest run …/roof-artifact.test.ts …/roof-pipeline.test.ts …/gemini-client.test.ts` → **3 files, 27 tests, all PASS** (vitest 3.2.7). Pipeline tests run against REAL captured Google Solar fixtures: `fixtures/datalayers-pune-dense` (city block, 30.7% mask coverage, 180,924 building pixels, EPSG:32643/UTM 43N) and `fixtures/datalayers-pune` (mask legitimately empty — 0 pixels), both captured live 2026-07-16.

One-hop files consulted but owned elsewhere: `screens/Step2Roof.tsx` (s2 drawing agent — fallback-ladder call sites + ghost-review UI), `lib/roof-factory.ts`, `lib/roof-topology.ts` (s2 drawing agent), `lib/geo.ts`, `lib/maps.ts`, `lib/solarApi.ts`, `components/SatCanvas.tsx` (s1), `lib/__tests__/gemini-client.test.ts` (ledger claims s6, but it exclusively tests my `roof-ai/gemini-client.ts` — ran it as data; discrepancy noted, not resolved).

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step2-roof-ai.1 | "Detect roofs (AI)" toolbar entry triggers the detection ladder; while a ghost review is open the same button acts as cancel (aria-pressed reflects review-open state); button disabled + dimmed to 0.5 opacity while detection is running (aiBusy) | detect-client.ts:32; Step2Roof.tsx:1444-1451,164 (one hop) | working | SC.10-3.03 |
| CODE.step2-roof-ai.2 | Fallback ladder, rung order fixed: (1) aerial geometric detection (Google dataLayers DSM+mask); if it yields ≥1 roof or obstruction → ghost review; (2) only when rung 1 returns zero entities or errors → hint "Aerial mask empty — trying photo analysis…" then Gemini photo analysis of the same satellite tile; (3) both empty/failed → combined hint "<aerial reason> Also, <gemini reason> — draw the roof with the pen tool." Manual drawing always remains available | Step2Roof.tsx:176-227 (one hop); pipeline.ts:57-62 | working | SC.10-3.10 |
| CODE.step2-roof-ai.3 | Rung-2 failure reasons are specific: 'photo analysis is not configured' (unconfigured) / 'photo analysis: <message>' (error) / 'photo analysis found nothing definite' (ok but no roofs) | Step2Roof.tsx:217-224 (one hop); gemini-client.ts:147-150 | working | SC.10-3.11 |
| CODE.step2-roof-ai.4 | Geometric detection requests an AOI of radius 40 m around the project pin (client default); the proxy clamps any requested radius to 10–100 m and defaults to 30 m when absent/NaN | detect-client.ts:32,37; data-layers/route.ts:17,36 | working | SC.10-3.10 |
| CODE.step2-roof-ai.5 | Detection client outcome envelope: `ok` (validated artifact + imageryDate/Quality) / `unavailable` (no aerial data, message) / `error` (message); proxy unreachable or non-JSON reply → error 'Could not reach the detection service' | detect-client.ts:11-14,36-47 | working | SC.10-3.10 |
| CODE.step2-roof-ai.6 | Mask+DSM rasters are fetched immediately and in parallel through the relay (upstream URLs expire in ~1 h — never stored); a failed raster download surfaces 'raster fetch HTTP <status>' | detect-client.ts:26-30,49-62 | working | NONE |
| CODE.step2-roof-ai.7 | The pipeline runs in a throwaway Web Worker: buffers transferred (not copied), worker terminated after every run; 30 s timeout → error 'Roof detection timed out'; worker crash → 'Roof detection crashed'; in-worker exceptions (e.g. decode errors) surface their message text | detect-client.ts:24,64-92; detect.worker.ts:13-23 | working | NONE |
| CODE.step2-roof-ai.8 | The geotiff dependency (~113 KB) is imported only inside the worker chunk — never in the main client bundle; GeoTIFF.Pool deliberately unused | detect.worker.ts:1-5; geotiff-decode.ts:8-10 | working | NONE |
| CODE.step2-roof-ai.9 | Every detection result — geometric AND Gemini — passes `validateArtifact` against the CURRENT pin before anyone sees a ghost; a rejected artifact becomes an error message, never a ghost | detect-client.ts:95-103; Step2Roof.tsx:204 (one hop); test 'the artifact passes the SAME validation doorway as the geometric path' | working | SC.10-3.12 |
| CODE.step2-roof-ai.10 | GET /api/solar/data-layers always answers HTTP 200 with `{status: ok\|unavailable\|error, message?}`; invalid/non-finite lat/lng (\|lat\|>90, \|lng\|>180) → error 'Invalid lat/lng'; no server key → error 'No Solar API key configured on the server' | data-layers/route.ts:32-46 | working | NONE |
| CODE.step2-roof-ai.11 | Scope rule: dataLayers requested with view=IMAGERY_LAYERS and requiredQuality=LOW only — DSM+RGB+mask for geometry; flux/irradiance layers are NEVER requested (PVGIS remains sole energy source) | data-layers/route.ts:1-4,48-51 | working | SC.10-3.10 |
| CODE.step2-roof-ai.12 | dataLayers upstream timeout 8 s → error 'dataLayers request timed out'; upstream 404 → unavailable 'No aerial data layers for this location'; other upstream errors surface Google's error.message or 'dataLayers HTTP <status>' | data-layers/route.ts:16,53-69,94-99 | working | NONE |
| CODE.step2-roof-ai.13 | An upstream reply missing maskUrl or dsmUrl → unavailable 'dataLayers response missing mask/DSM' (RGB alone is not enough to detect) | data-layers/route.ts:71-76 | working | NONE |
| CODE.step2-roof-ai.14 | Provenance passthrough: imageryDate formatted `YYYY-MM-DD` (zero-padded) + imageryQuality string returned in the envelope, carried into the artifact and shown in the ghost-review pill | data-layers/route.ts:77-84; pipeline.ts:44-45; Step2Roof.tsx:1309-1310 (one hop) | working | SC.10-3.12 |
| CODE.step2-roof-ai.15 | Raster URLs are rewritten to the `/api/solar/geotiff?src=…` relay before reaching the client — the paid key never leaves the server; the rgb layer URL is relayed too but no client code ever fetches it (mask+DSM only) | data-layers/route.ts:29-30,85-90; detect-client.ts:43,53-56 | working | NONE |
| CODE.step2-roof-ai.16 | GeoTIFF relay SSRF guard: only URLs starting exactly `https://solar.googleapis.com/v1/geoTiff:get` are relayable; anything else → HTTP 400 before any fetch | geotiff/route.ts:11,17-19 | working | NONE |
| CODE.step2-roof-ai.17 | Relay appends the key server-side and streams bytes as `image/tiff`; upstream 404 (expired ~1 h URL) → HTTP 410 (client-retryable), other upstream failures → 502; 20 s timeout → 504; no key → 503 | geotiff/route.ts:12,20-48 | working | NONE |
| CODE.step2-roof-ai.18 | Relay caches raster bytes `Cache-Control: private, max-age=86400` — a re-detect on the same pin within a day can serve from browser cache | geotiff/route.ts:40-42 | working | NONE |
| CODE.step2-roof-ai.19 | Solar key resolution (product-visible): detection/insights work when either GOOGLE_SOLAR_API_KEY (preferred) or the public Maps key is configured; neither → the 'No Solar API key configured' error path | key.ts:7-9 | working | NONE |
| CODE.step2-roof-ai.20 | GeoTIFF decode accepts only UTM-georeferenced rasters (EPSG 32601–32660 north / 32701–32760 south); any other CRS throws 'Unsupported raster CRS EPSG:<n> — expected a UTM zone' (surfaces as a detection error) | geotiff-decode.ts:35-40; utm.ts:15-19 | working | NONE |
| CODE.step2-roof-ai.21 | Decode validates raster size (data length = width×height, else 'Raster size mismatch') and georeferences at pixel CENTERS (+0.5 px, PixelIsArea), rows advancing southward from the top-left UTM origin | geotiff-decode.ts:32-34,42-50; test 'decodes the mask with the verified grid + coverage' | working | NONE |
| CODE.step2-roof-ai.22 | UTM→WGS84 inverse is a built-in Snyder Transverse-Mercator series (WGS84 ellipsoid, ~1 mm accuracy, no proj4 dep); each traced vertex goes UTM → lat/lng → project east-north, handling grid convergence exactly | utm.ts:1-65; test 'utmToLatLng round-trips a known UTM 43N point' | working | NONE |
| CODE.step2-roof-ai.23 | Coordinate-alignment gate ≤0.5 m over 50 m against the app's own projector frame (tested: east 49.99 m, north 50.2–50.45 m — the meridian-scale residual is what known-distance calibration corrects); orientation convergence ≤0.4° | roof-pipeline.test.ts:42-85 tests 'raster center maps back to the request pin', 'UTM distances survive the utm→latLng→projector chain within 0.5 m over 50 m' | working | NONE |
| CODE.step2-roof-ai.24 | Building-mask components are 4-connected (mask value ===1), labeled once and shared by tracing and plane fitting; deterministic scan order, no recursion | vectorize.ts:17-46 | working | NONE |
| CODE.step2-roof-ai.25 | Minimum roof component area 2000 px (≈20 m² at 0.1 m/px); smaller blobs are silently ignored | pipeline.ts:28,54-55 | working | NONE |
| CODE.step2-roof-ai.26 | Empty-mask outcome: zero roofs/obstructions plus the exact warning 'The aerial building mask is empty here — no roofs auto-detected. Draw the roof manually, or try the photo-based detection.' — this warning drives ladder rung 2 | pipeline.ts:57-62; test 'returns zero roofs plus the honest warning that drives the fallback' | working | SC.10-3.10 |
| CODE.step2-roof-ai.27 | Roof cap: at most 8 roofs imported, largest area first, with warning 'Detected N roof surfaces; importing the largest 8.' | pipeline.ts:29,97-100 | working | NONE |
| CODE.step2-roof-ai.28 | Urban-blob splitting: each mask component is first segmented into height-coherent sub-buildings by DSM region-growing (neighbour joins when step ≤0.75 m AND deviation from running region mean ≤1.25 m — the band stops ramp-chaining); sub-regions under 2000 px are dropped; if height data segments everything away, the whole component is used as-is | vectorize.ts:219-271; pipeline.ts:79-96 | working | NONE |
| CODE.step2-roof-ai.29 | Boundary: Moore-neighbour trace of the outer contour (Jacob's stopping criterion, bounded at 4·W·H steps); components tracing to fewer than 4 ring points are skipped | vectorize.ts:52-101; pipeline.ts:109-110 | working | NONE |
| CODE.step2-roof-ai.30 | Footprint simplification: Douglas-Peucker in METERS with 0.4 m tolerance, applied after every vertex is converted through the raster's own georeferencing into the project frame | pipeline.ts:30,111-112; vectorize.ts:104-129 | working | NONE |
| CODE.step2-roof-ai.31 | Gated orthogonalization: footprints are squared only when ≥80% of total edge length lies within 15° of the two dominant axes (length-weighted 4θ circular mean); otherwise returned untouched so non-rectilinear roofs are never mangled; degenerate merge results also fall back untouched | vectorize.ts:140-209 | working | NONE |
| CODE.step2-roof-ai.32 | DSM/mask grid mismatch is verified, not assumed: differing grids → warning 'DSM and mask grids differ — heights/pitch skipped.' and footprint-only roofs (confidence 0.4) | pipeline.ts:67-69,122-123 | working | NONE |
| CODE.step2-roof-ai.33 | Ground level = median DSM over non-building pixels (mask 0), sampling every 7th pixel, requiring ≥50 samples with elevation >1 m; failure → warning 'Could not estimate ground level — roof heights use the default.' | plane-fit.ts:70-83; pipeline.ts:70-73 | working | NONE |
| CODE.step2-roof-ai.34 | Plane fit per roof: centroid-centered least-squares z=ax+by+c over ≤~5000 interior DSM samples (grid step = max(1, ⌊√(areaPx/5000)⌋)); needs ≥12 samples and a non-degenerate footprint or the roof stays footprint-only; edge-ramp pixels excluded via an interior test (all pixels within Chebyshev radius 3 share the label) — smoothed DSM edges would fake 10–25° pitch on flat roofs | pipeline.ts:31,123-144; plane-fit.ts:28-64; vectorize.ts:278-296 | working | SC.10-3.10 |
| CODE.step2-roof-ai.35 | DSM samples with elevation ≤1 m are excluded (nodata guard) — both in plane sampling and residuals; sites genuinely at ≤1 m ASL would lose plane fits/heights | pipeline.ts:135-136; plane-fit.ts:117; vectorize.ts:233 | uncertain (code-analysis; coastal ≤1 m ASL sites unobserved — Pune fixtures are ~560 m) | NONE |
| CODE.step2-roof-ai.36 | Detected values are reported at fixed resolutions: pitch rounded to 0.1°, slope azimuth to 1° (compass, 0=N CW; null i.e. "flat" when pitch <3°), RMSE to 0.01 m, eave height to 0.1 m | pipeline.ts:145-147,152; plane-fit.ts:57-62; test 'plane fits produce finite pitch and plausible heights where the DSM supports them' | working | SC.10-3.10 |
| CODE.step2-roof-ai.37 | Eave height = plane height at centroid − ground level, kept only when 1 < h < 60 m, else null (→ manual 3 m default on accept) | pipeline.ts:150-153; artifact.ts:29 | working | SC.10-3.10 |
| CODE.step2-roof-ai.38 | Per-roof confidence formula: clamp(1 − RMSE/0.6, 0.2, 0.95) — RMSE 0.3 m ⇒ 0.5; footprint-only roofs (no DSM support) get fixed 0.4 | pipeline.ts:122,154-155 | working | SC.10-3.10 |
| CODE.step2-roof-ai.39 | Honest complexity warning when fit RMSE >0.3 m: 'Roof N: uneven surface (fit RMSE X.XX m) — likely multiple planes or clutter; review by hand.' | pipeline.ts:156-160 | working | SC.10-3.12 |
| CODE.step2-roof-ai.40 | Obstruction candidates = clusters of DSM pixels rising ≥0.35 m ABOVE the fitted plane, min cluster 25 px, gated on fit quality (skipped entirely when RMSE >0.5 m — residuals of a bad fit are noise); negative residuals ignored; only clusters with peak height ≥0.5 m kept; top 5 by area per roof | plane-fit.ts:101-148; pipeline.ts:162-171 | working | SC.10-3.10 |
| CODE.step2-roof-ai.41 | Detected obstruction shape: axis-aligned bbox in project frame — center at bbox middle, length/width = bbox extents floored at 0.3 m, height = peak residual rounded 0.1 m, rotation 0, type always 'other' (the DSM sees a lump; the USER classifies), fixed confidence 0.5 | pipeline.ts:172-186 | working | SC.10-3.10 |
| CODE.step2-roof-ai.42 | Pipeline is fully deterministic: no RNG, no clock (generatedAt supplied by caller), stable ids ar_N/ao_N ordered by descending component area; repeat runs byte-identical | pipeline.ts:6-8; test 'is fully deterministic' | working | NONE |
| CODE.step2-roof-ai.43 | RoofArtifact v1 is the ONLY doorway from AI output into the project: versioned (version=1), source 'dataLayers'\|'gemini', pin-stamped, with per-entity confidence and honest warnings[]; AI never touches the store or emits prose | artifact.ts:1-62 | working | SC.10-3.12 |
| CODE.step2-roof-ai.44 | Whole-artifact rejections (nothing shown): not an object / version ≠1 ('Unsupported artifact version') / unknown source / missing pin / malformed entity lists | artifact.ts:102-118; test 'rejects non-objects, wrong versions and unknown sources' | working | NONE |
| CODE.step2-roof-ai.45 | Stale-pin guard: artifact pin must be within 1e-3° (~110 m) of the CURRENT pin or the whole artifact is rejected: 'Artifact was generated for a different location' — guards reuse after a pin move | artifact.ts:68,113-115; test 'rejects an artifact generated for a DIFFERENT location (pin guard)' | working | NONE |
| CODE.step2-roof-ai.46 | Per-entity failures DROP that entity with a stated reason and keep siblings — one bad polygon never discards a good detection; dropped list is surfaced in the review pill as 'Skipped one shape: <reason>' | artifact.ts:92-97,120-152; Step2Roof.tsx:1348-1353 (one hop); test 'drops a self-intersecting roof with a reason; the good roof survives' | working | SC.10-3.12 |
| CODE.step2-roof-ai.47 | Roof polygon sanitation on validation: near-duplicate vertices (<0.15 m) merged, closing duplicate dropped, near-collinear vertices collapsed (preCleanRing), then the same sanitizer manual drawing uses (sanitizeRoofPolygon — rejects self-intersections, degenerate shapes) | artifact.ts:130-135; roof-factory.ts (one hop); tests 'preCleanRing (AI ring pre-clean)' ×2 | working | NONE |
| CODE.step2-roof-ai.48 | Site-extent gate: every roof vertex and obstruction center must lie within 150 m of the pin (dataLayers max AOI is 100 m); violators dropped 'outside the 150 m site extent' | artifact.ts:66-67,121,136-139,157-160; test 'drops out-of-extent and implausible entities' | working | NONE |
| CODE.step2-roof-ai.49 | Roof value gates (out-of-range → null, not dropped): heightM kept only in (0,60); pitchDeg only in [0,60]; slopeAzimuthDeg only in [0,360); confidence clamped to 0..1; rmseM optional passthrough | artifact.ts:143-150; test 'clamps confidence, coerces unknown obstruction types to other, nulls implausible heights' | working | NONE |
| CODE.step2-roof-ai.50 | Obstruction gates: center required + in-extent; length/width must be >0 and ≤50 m; height >0 and ≤60 m ('implausible dimensions' drop); unknown type coerced to 'other' (closed 11-type list); rotation normalized to [0,360); confidence clamped 0..1 | artifact.ts:87-90,154-181 | working | NONE |
| CODE.step2-roof-ai.51 | Validation output hygiene: warnings filtered to strings only; generatedAt defaults to 0 when non-numeric; imageryDate/Quality kept only when strings | artifact.ts:183-197 | working | NONE |
| CODE.step2-roof-ai.52 | Ghost review model: all detected roofs+obstructions arrive PRE-SELECTED; tapping a ghost toggles include/exclude; nothing enters the project until "Add selected" | Step2Roof.tsx:183-192,1258-1262,3172 (one hop) | working | SC.10-3.12 |
| CODE.step2-roof-ai.53 | Review pill content: '<n>/<N> roofs · <m>/<M> objects', imagery quality + date when known, artifact warnings + dropped reasons in amber, Cancel; "Add selected" disabled when zero entities selected | Step2Roof.tsx:1283-1355 (one hop) | working | SC.10-3.12 |
| CODE.step2-roof-ai.54 | Align nudge: four arrow chips shift ALL ghosts by 0.5 m per tap (E/W/N/S, aria-label 'Nudge ghosts <dir>'), compensating dataLayers↔tile georegistration offset; the offset is baked into the accepted geometry (roof vertices + obstruction centers) on accept | Step2Roof.tsx:172-173,1313-1336,231-243 (one hop) | working | SC.10-3.12 |
| CODE.step2-roof-ai.55 | "Add selected" applies the whole import as ONE undoable patch (applyArtifact returns a single Partial<Project>; caller applies with undoable=true), then hints 'Imported N roof(s) + M obstruction(s) — review heights and edit like any drawn roof' | artifact.ts:200-264; Step2Roof.tsx:244-256 (one hop); test 'imports only the accepted subset as one composable patch' | working | SC.10-3.12 |
| CODE.step2-roof-ai.56 | Accepted entities are created via the SAME factory as manual tools: naming continues the existing counter (e.g. 'Roof 2', 'OB1'), roofType default rcc_flat, roof setback 0.3 m, obstruction setback 0.5 m + castsShadow true, null height → 3 m manual default | artifact.ts:216-256; roof-factory.ts:115-138 (one hop); tests 'imports only the accepted subset…', 'obstructions get factory labels…' | working | NONE |
| CODE.step2-roof-ai.57 | Mumty rule on accept: an artifact roof fully inside an existing or just-accepted roof (every vertex + centroid contained) is parented — becomes 'Mumty N' at parent height + 2.2 m when its own height is unknown | artifact.ts:219-225; roof-factory.ts:138 (one hop); test 'a roof fully inside an existing roof becomes a Mumty on top of it' | working | NONE |
| CODE.step2-roof-ai.58 | Obstructions accept AFTER roofs so parenting sees new roofs: each accepted obstruction is attached to the roof under its center (pickRoofAt), or roofId null when over no roof | artifact.ts:239-256; test 'obstructions get factory labels continuing the count and parent to the roof under them' | working | NONE |
| CODE.step2-roof-ai.59 | Every accepted entity is stamped with provenance `{source: 'dataLayers'\|'gemini', confidence}` — the artifact's per-entity confidence survives into the project record | artifact.ts:214,234,253; test asserts `provenance == {source:'dataLayers', confidence:0.8}` | working | SC.10-3.41 |
| CODE.step2-roof-ai.60 | POST /api/gemini without GEMINI_API_KEY → status 'unconfigured' with message 'Photo-based detection is not configured (GEMINI_API_KEY)' (HTTP 200, graceful degradation — the ladder reports it, the entry point is NOT hidden) | gemini/route.ts:89-96; Step2Roof.tsx:217-219 (one hop) | working | SC.10-3.11 |
| CODE.step2-roof-ai.61 | Satellite mode: the SERVER fetches exactly the SatCanvas tile — zoom 20, 640×640, scale 1, maptype satellite, centered on the pin — guaranteeing the 1:1 pixel-mapping contract the client conversion assumes (CORS-proof); needs the Maps key ('No Maps key for tile fetch' otherwise); tile fetch failures → 'Tile fetch HTTP <n>' / 'Tile fetch failed'; invalid lat/lng rejected | gemini/route.ts:116-145; SatCanvas.tsx:39 (one hop, SAT_ZOOM=20) | working | SC.10-3.11 |
| CODE.step2-roof-ai.62 | Photo mode (kind:'photo', caller-supplied imageBase64/mimeType/dimensions, prompt says "possibly oblique") exists in the route but NO app code ever calls it — the only client always sends kind:'satellite'; survey-photo detection has no UI | gemini/route.ts:98-114,24-29; gemini-client.ts:165-169 | POC-DEFECT(not wired: endpoint capability with no in-app caller) | NONE |
| CODE.step2-roof-ai.63 | Image size cap: base64 payload >4,000,000 chars (≈3 MB binary) → error 'Image too large — downscale before sending'; missing image/mime/dimensions → 'Missing image data' | gemini/route.ts:14,147-155 | working | NONE |
| CODE.step2-roof-ai.64 | Model+prompt provenance: model = GEMINI_MODEL env or default 'gemini-2.5-flash'; every ok response echoes `promptVersion: 'roof-detect-v1'` + model, which the client records in the artifact as imageryQuality 'Gemini <model> (<version>)' | gemini/route.ts:17,157,211-214; gemini-client.ts:94,179-185 | working | SC.10-3.11 |
| CODE.step2-roof-ai.65 | Structured-only output: temperature 0, responseMimeType application/json, responseSchema-enforced (roofs[{polygonPx[{x,y}],confidence}], objects[{centerPx,widthPx,heightPx,kind?,confidence}]) — no prose can leak through | gemini/route.ts:44-86,177-181 | working | SC.10-3.11 |
| CODE.step2-roof-ai.66 | Prompt contract (precision beats recall, verbatim rules): trace only edges actually SEEN, never guess occluded/shadowed edges — omit the roof instead; pixel coords inside the image; polygons 3–24 ordered vertices, no self-intersections, no closing duplicate; per-entity confidence 0..1 with instruction to omit below 0.3; no neighbouring roofs unless physically touching; "Empty arrays are a VALID and correct answer"; JSON only | gemini/route.ts:24-42 | working | SC.10-3.11 |
| CODE.step2-roof-ai.67 | Gemini failure envelope (all HTTP 200): 25 s timeout → 'Gemini request timed out'; upstream non-ok surfaces Google's error message or 'Gemini HTTP <n>'; empty candidates → 'Gemini returned no content'; unparseable text → 'Gemini output was not valid JSON'; unreachable proxy → 'Could not reach the photo-analysis service' (client) | gemini/route.ts:13,185-220; gemini-client.ts:170-177 | working | NONE |
| CODE.step2-roof-ai.68 | Pixel→meter conversion: mx=(px/640−0.5)·span, my=(0.5−py/640)·span where span = metersPerStaticMap(lat, zoom 20, 640 px) × calibration.scaleFactor (156543.03392·cos(lat)/2²⁰ m per px) — a calibrated project would otherwise misalign by (scaleFactor−1)·distance | gemini-client.ts:17,47-59; maps.ts:37-45 (one hop); tests 'maps the tile centre to the origin…', 'applies calibration.scaleFactor…' | working | SC.10-3.11 |
| CODE.step2-roof-ai.69 | Gemini roofs: polygons need ≥3 points else filtered; heightM/pitchDeg/slopeAzimuthDeg all null — a photo cannot measure them (manual defaults apply on accept); missing confidence → 0; ids ar_gN/ao_gN | gemini-client.ts:61-70 | working | SC.10-3.11 |
| CODE.step2-roof-ai.70 | Gemini objects: need center + both pixel dims >0; kind mapped through a closed table (tank/water_tank→tank, dish/antenna→dish, chimney→chimney, solar_water_heater/solar_wh→solar_wh, tree→tree, staircase/staircase_room→elevated, unknown→other); footprint from pixel dims; height from the TYPE PRESET (e.g. tank 1.2 m, chimney 2 m) since imagery can't measure it; rotation 0 | gemini-client.ts:30-41,72-87; roof-factory.ts:196-210 (one hop); test 'imagery cannot measure heights: roofs get null, objects get type presets' | working | SC.10-3.11 |
| CODE.step2-roof-ai.71 | Every Gemini artifact carries the mandatory warning 'Photo-based detection: heights/pitch are NOT measurable from imagery — defaults applied; verify on site.' shown in the review pill | gemini-client.ts:96-99 | working | SC.10-3.11 |
| CODE.step2-roof-ai.72 | Geometry cross-check (plan §23b): where geometric reference roofs exist, a Gemini roof overlapping them by <20% of its own area gets confidence floored to ≤0.25 plus warning 'One AI-suggested roof disagrees with the aerial building mask (only N% overlap) — verify before accepting.' — never silently deleted; no reference or clipping failure ⇒ no judgement | gemini-client.ts:103-145; tests 'agreement…keeps confidence', 'disagreement floors confidence and adds a warning — never deletes', 'no geometric reference ⇒ no judgement' | POC-DEFECT(not wired: exported+tested but zero app call sites; the ladder only runs Gemini when the geometric rung returned nothing, so the both-sources case is unreachable in-product — conflicts main-suite M04-20) | SC.10-3.11 |
| CODE.step2-roof-ai.73 | GET /api/solar/building-insights: findClosest with requiredQuality=BASE; envelope always HTTP 200 {status, insights?, message?}; invalid lat/lng and missing key handled as errors; 8 s upstream timeout ('Solar API request timed out') | building-insights/route.ts:16-41,63-68 | working | NONE |
| CODE.step2-roof-ai.74 | Coverage honesty: upstream 404 → unavailable 'Google Solar has no coverage for this location' (India coverage is partial — enhancement, never a dependency); other errors surface Google's message | building-insights/route.ts:42-56 | working | NONE |
| CODE.step2-roof-ai.75 | Insights response mapping (product values): per-face roofSegments only where BOTH pitch+azimuth exist — pitch/azimuth rounded to whole degrees, area to 0.1 m², optional center lat/lng; roofSegmentCount counts USABLE segments so the "roof faces" stat matches the number of Step-2 suggestion chips; maxPanels, maxArrayArea/sunshine/carbonOffset rounded to integers; imageryDate YYYY-MM-DD + quality HIGH/MEDIUM/BASE | building-insights/route.ts:57-62; solarApi.ts:39-81 (one hop, owned s1) | working | SC.10-3.34 |
| CODE.step2-roof-ai.76 | Per-coordinate insights result cached a day: response header Cache-Control public, max-age=86400 (same policy as /api/pvgis) — a Confirm on the same pin must not re-bill Google | building-insights/route.ts:59-62; solarApi.ts:84-100 (one hop, client memo) | working | NONE |
| CODE.step2-roof-ai.77 | The "Google detected N° facing DIR — apply/dismiss" pitch/azimuth suggestion in Step 2's Height & Parapet sheet is fed by these roofSegments — non-destructive, surfaced as a dismissible suggestion, never auto-applied (manual pipeline stays authoritative) | Step2Roof.tsx:261-270 (one hop, roofHint); solarApi.ts:39-55 (one hop) | working | SC.10-3.34 |
| CODE.step2-roof-ai.78 | No metering/allowance hook exists anywhere in the detection paths: /api/gemini and /api/solar/* perform no pre-call allowance check, no billing event, no per-tenant counting; detection is unmetered in the POC (main-suite M04-23/BM-19 is contract-only) | gemini/route.ts:88-224; data-layers/route.ts:32-103; geotiff/route.ts:14-52 (absence — full-file reads) | working (absence is code-faithful; contract gap noted, not a code bug) | NONE |

## CENSUS-UNMATCHED (AI/detection half of A.10-3)

My half of the 42-entry census (detection/imagery rows): SC.10-3.03, SC.10-3.10, SC.10-3.11, SC.10-3.12, SC.10-3.34, SC.10-3.41.

| census entry | status |
|---|---|
| — | none unmatched: all 6 detection/imagery entries are matched by rows above (SC.10-3.12's tap-ghost/nudge/add-selected UI half lives in Step2Roof.tsx — one-hop evidence here; that file's full UI inventory belongs to the s2 drawing agent) |

All remaining A.10-3 entries (SC.10-3.01–.02, .04–.09, .13–.33, .35–.40, .42) are drawing/editing/measure rows owned by the DRAWING agent.

## Footer

- **rowCount:** 78
- **defectCount:** 2 (CODE.step2-roof-ai.62 photo-mode endpoint unwired; .72 crossCheckWithGeometry unwired — both code-vs-product-surface discrepancies, all 27 tests green)
- **censusUnmatched:** 0 (of my 6-entry half)
- **newBeyondCensus:** 44 rows carry census NONE — proxy envelopes/timeouts/caching, SSRF guard, decode/UTM math + alignment gate, all pipeline constants (2000 px min, 8-roof cap, 0.4 m simplify, 80%/15° ortho gate, 0.75/1.25 m height segmentation, RMSE→confidence formula, 0.35 m/25 px/top-5 obstruction clustering), artifact gates (pin 1e-3°, 150 m extent, value ranges), mumty/parenting on accept, determinism.
- **notes (≤10 lines):**
  1. Fallback ladder is strictly sequential and mutually exclusive — Gemini runs ONLY when the aerial rung yields zero entities; both-source cross-check (M04-20 contract) is therefore dead code (row 72).
  2. M04 consistency: M04-15/16/17/18/19/24 are satisfied in spirit by rows 43–55, 61, 64–66 (per-entity confidence, no-guessing prompt, empty-result-valid, same-tile 1:1, versioned prompt provenance, validated-artifact doorway with dropped reasons).
  3. M04 conflicts: M04-21 wants missing capability HIDDEN — POC always shows Detect and reports "not configured" in the ladder hint (rows 1, 3, 60); M04-23 metering absent entirely (row 78); M04-14's three-step progress copy absent — POC shows one busy state + one mid-ladder hint.
  4. Gemini detects against a live re-fetch of the same static-tile URL, not a STORED pinned tile (M04-10/19 contract nuance) — the POC has no tile persistence.
  5. Gemini artifacts carry no imageryDate (only 'Gemini <model> (<version>)' as quality); dataLayers artifacts carry both.
  6. Test-claim discrepancy: file-claims.md assigns gemini-client.test.ts to sitting 6, but it tests only my gemini-client.ts; ran it (8/8 pass), recorded here.
  7. detect-client radius default 40 m vs proxy default 30 m — both exist; app-effective value is 40 m (row 4).
  8. Confidence values at product level: plane-fit clamp(1−RMSE/0.6, 0.2, 0.95); footprint-only 0.4; DSM obstruction 0.5; Gemini as-returned (missing→0), floored ≤0.25 on cross-check disagreement (unreachable in-app).
