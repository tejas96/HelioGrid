# Feature inventory — step6-layout (Sitting 5 · Step 6: the layout Editor — fill/selection/keepouts/walkways/toolbars/table-sheet/validation surface)

Pass-two inventory, 2026-08-05. Primary files (all under `3d_design_studio/src/features/solar-studio/`):
`screens/Step6Editor.tsx` (3,511 ln) · `screens/step6-erase.ts` (67 ln) · `lib/layout.ts` (697) · `lib/panel-move.ts` (132) · `lib/panel-pose.ts` (107) · `lib/spacing.ts` (49) · `lib/auto-design.ts` (282) · `lib/health.ts` (378).

Test run (read-only, deps from lockfile): `npx vitest run` on layout.test, health.test, health-coverage.test, step6-erase.test, panel-inspector.test, panel-move.test → **6 files, 94 tests, all PASS**; plus one-frame.test, frame-parity.test, spacing.test, auto-design.test, mms-customize.test → **5 files, 60 tests, all PASS** (vitest 3.2.7). Combined: **11 files, 154 tests, 0 failures**.

One-hop libraries consulted, owned elsewhere (cited, not inventoried): `lib/segment-ops.ts` (s2 — group/grow/duplicate/respace/reindex/setSegment*), `lib/stringing.ts` + `lib/electrical/autostring.ts` + `lib/electrical/gate.ts` + `lib/routing.ts` + `lib/drc.ts` + `lib/cascade.ts` (s7), `lib/insights/*` (s6), `lib/solar-heatmap.ts` + `lib/structure.ts` + `lib/structure-edit.ts` (sitting-5 files owned by agents B/C of this sitting), `components/SatCanvas.tsx` + `components/MeasureTool.tsx` (s1/s2), `store/store.tsx` (s10), `screens/Wizard.tsx` (s10 — cited only for the Step-6 hard gate).

**Census scope (agent A of sitting 5):** A.10-6 rows that are layout/fill/segment-surface — i.e. all 66 **except** `SC.10-6.04` (heatmap → agent B) and `SC.10-6.42/.43/.48/.49/.50` (structure/ground → agent C) = **60 rows owned**. My share of A.10-7 is **zero rows** (all 43 are 3D-view/shadow/heatmap → B, or structure/foundation edit cards → C). Rows below that touch B/C-owned census entries are marked "(B)" / "(C)" — the code lives in my primary file, the census ownership does not.

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step6-layout.1 | Arrival with 0 panels and ≥1 roof opens the auto-place dialog exactly once (mount effect); "Place manually" is the dismiss action — closes the dialog, empty roof stays | Step6Editor.tsx:299-304,1876-1904 | working | SC.10-6.01 |
| CODE.step6-layout.2 | Dialog action "Auto-fill panels" runs `autoDesign(project,'target_kwp')`: panel budget = `floor(targetKwp·1000/watt)`, roofs filled best-first until the budget is spent | Step6Editor.tsx:409-425,1886-1889; auto-design.ts:136-140,173-193; auto-design.test.ts (11 PASS) | working | SC.10-6.02 |
| CODE.step6-layout.3 | Dialog action "Use max roof capacity" runs `autoDesign(project,'max_roof')` — budget ∞, every valid position on every ranked roof used | Step6Editor.tsx:1890-1892; auto-design.ts:136-140,249-251 | working | SC.10-6.03 |
| CODE.step6-layout.4 | Auto-place REPLACES panels+segments in one undoable patch, clears strings, stores `designLog`, flashes each warning (2.6 s), and auto-opens the "Why this layout?" sheet when decisions exist | Step6Editor.tsx:409-425 | working | SC.10-6.02 |
| CODE.step6-layout.5 | Roof ranking = measured beam access (≤12 evenly-sampled probe panels through the real raycast shading engine) × POA orientation factor (plane-of-array/GHI for the probe panels' own tilt+azimuth); score ties broken by roof order; no location ⇒ POA 1 | auto-design.ts:51,67-118; auto-design.test.ts | working | SC.10-6.02 |
| CODE.step6-layout.6 | Decision log records: objective, per-roof rank (capacity · access % · orientation %), row spacing per roof (winter shadow-free, 09:00–15:00 solar), every §26c bridged obstruction (clearance numbers + engineer-confirmation warning), achieved capacity, and a sanctioned-load soft-cap entry (warn, never block) when kWp > sanctionedLoadKw | auto-design.ts:142-274 | working | SC.10-6.06 |
| CODE.step6-layout.7 | `estimateMaxCapacityKwp`: raw roof capacity through the same `fillRoofAsSegment` door (bridging honoured, `avoidPanels:[]`), kWp rounded UP to 0.1 so budget reproduction is exact — consumed by Step 4's "Auto" | layout.ts:677-697; layout.test.ts:175-183 | working | NONE (Step-4 census A.10-5 carries the Auto button) |
| CODE.step6-layout.8 | `autoFillRoof` grid fill: per-edge setback inset (robust inset, multiple regions), obstruction buffers = max(setbackM, capability minHorizontalClearanceM) with §26c bridge skip, walkway strips, keepouts (kind≠'shade'), higher-roof footprints, and existing panels (default `avoidPanels = project.panels`); lattice anchored to the ROOF frame (never the drag box), 1 µm grid epsilon, `cellIndex = row·1000+col` | layout.ts:94,302-428; layout.test.ts (21 PASS: collision-aware, monotonic area fill, keepout-aware) | working | SC.10-6.09 |
| CODE.step6-layout.9 | Row pitch inside a fill: tilted rows on a flat roof step by the winter-solstice shadow-free pitch (expert `rowPitchM` override allowed, floored at footprint+1 cm); flush rows step footprint+gap (+0.4 m every row when `grouped:false`) | layout.ts:235-253,354-364; spacing.ts:21-49; spacing.test.ts (6 PASS) | working | SC.10-6.09 |
| CODE.step6-layout.10 | `defaultPanelPose` by roof type: sloped→flush slope pose; metal_shed→0°/180°; flat tile→0°/180° (hooks, not tilt legs); ground→rules `groundTiltDeg` (20°)/180°; RCC flat→10°/180° | layout.ts:67-81; india.ts:521; layout.test.ts:104-117,218-231 | working | SC.10-6.09 |
| CODE.step6-layout.11 | `fillRoofAsSegment` wraps a fill as an ArraySegment: fixed_tilt racking records the pitch actually used, backLegM = 0.3 + h·sin(tilt), DEFAULT_PROFILE; bridging clearance defaults chain max(0.3, roof.structureOverride ?? project.structureDefaults ?? 0); label assigned by caller via `nextSegmentLabel` ('A{n+1}') | layout.ts:441-516; layout.test.ts:75-118 | working | SC.10-6.09 |
| CODE.step6-layout.12 | `panelCornersOnRoof` is THE canonical plan footprint (placement, DRC, render, DXF, SLD all share it): flat+tilted → rectCorners(w, h·cos tilt, −azimuth); sloped → h·cos(pitch) foreshortened down-slope, w across; flat flush → grid-aligned w×h | layout.ts:546-577; frame-parity.test.ts (11 PASS); one-frame.test.ts (12 PASS) | working | SC.10-6.09 |
| CODE.step6-layout.13 | `snapPanelCenter`: grid anchored to the first existing panel on the roof (align to current layout) else the setback-inset corner; pitch = plan cell + 0.05 m; idempotent | layout.ts:584-624; layout.test.ts:251-268 | working | SC.10-6.09 |
| CODE.step6-layout.14 | `panelFitsAt` validates the candidate at its ACTUAL pose (caller override or roof default): inside inset + clear of obstruction/walkway/panel blockers + keepouts + covered footprints; optional bridge clearance | layout.ts:631-674; layout.test.ts:232-249 | working | SC.10-6.09 |
| CODE.step6-layout.15 | Left tool rail: four labelled groups View / Build / Safety / Electrical; every button has aria-label, tooltip with the keyboard key, active/pressed states; count badges on Walkway, No-build zone, Safety rail, Lightning arrester, Mount inverter (live from project arrays) | Step6Editor.tsx:166-208,1413-1552 | working | SC.10-6.11–15 (counts) |
| CODE.step6-layout.16 | Irradiance heatmap toggle (H): real rasterised `computeHeatmap` (same engine as 3D), cached by shading fingerprint, recomputes only on geometry change; cells projected to 2D; panels recoloured by solarAccess (green >0.95 / yellow >0.85 / red); legend = month name + "N% avg access" + gradient bar + Poor/Moderate/Good/Excellent + Jan–Dec range slider. MISSING vs census text: 0/50/100% scale marks, avg sun-hrs/day, kWh/m² for the month, and "Computing solar access… X%" progress (while computing the roofs show a static orange radial gradient, no %) | Step6Editor.tsx:219-222,310-330,1363-1411,3003-3010,3034-3036,3069-3075,3112-3124; solar-heatmap.ts (B) | POC-DEFECT(2D legend lacks 0/50/100% marks, sun-hrs/day, kWh/m², compute-progress % required by census; 3D legend is agent B's to verify) | SC.10-6.04 (B) |
| CODE.step6-layout.17 | Show strings toggle (S), default ON: dashed series path through each string's panels + solid routed home-run cables (black underlay + string colour); hand-routed runs carry a "hand-routed (auto-string will not change it)" tooltip | Step6Editor.tsx:223,1431-1438,3157-3229 | working | SC.10-6.05 |
| CODE.step6-layout.18 | "Why this layout?" sheet: renders the decision log verbatim (topic/choice/reason/inputs mono line) + "COPILOT SUGGESTIONS" (severity-tinted cards, title+detail); empty state "No automatic design has run yet". NO accept/ignore control exists anywhere: `project.insightState` ('accepted'\|'ignored') is honoured by the insights registry and health scoring but no UI ever writes it (only init/normalize) | Step6Editor.tsx:1439-1446,1906-1937; registry.ts:58 (s6); types.ts:982; store.tsx:128; normalize.ts:166 | POC-DEFECT(census requires accept/ignore per improvement suggestion; the model supports it, no UI writes insightState) | SC.10-6.06 |
| CODE.step6-layout.19 | Measure distance: two-point ruler via `useMeasure`/`MeasureOverlay`, label "X.XX m"; measure owns clicks while active (tools suspended), cursor crosshair | Step6Editor.tsx:255,715-716,1164,1360,1447-1454; MeasureTool.tsx (s2) | working | SC.10-6.07 |
| CODE.step6-layout.20 | Tool activation: re-tapping the active tool toggles back to Select; any activation cancels manual string/drag/marquee and clears selection; mutating tools (panels/walkway/rail/arrester/inverter/keepout/erase) blocked while locked with a lock flash | Step6Editor.tsx:130-150,377-388 | working | SC.10-6.08 |
| CODE.step6-layout.21 | Panels tool TAP (drag <0.4 m): ONE portrait panel snapped to the roof grid, placed only if `panelFitsAt` (refusal is silent — the ghost already showed red); pose = roof default; patch clears all strings. Live hover ghost: green solid when it fits, red dashed when overlap/setback breach | Step6Editor.tsx:1260-1289,2950-2970,3499-3508 | working | SC.10-6.09 |
| CODE.step6-layout.22 | Panels tool DRAG: fills the rectangle as a collision-aware SEGMENT (avoids existing panels, obstructions, setbacks, keepouts, walkways), geometric reindex, auto label, strings cleared; live preview = green zone + ghost of the exact panels that will land (blocker-aware while dragging); lattice monotonic (growing the box only adds) | Step6Editor.tsx:1290-1322,2923-2946,3468-3497; layout.test.ts:185-216 | working | SC.10-6.09 |
| CODE.step6-layout.23 | Erase tool: shared resolver `findEraseTargetAt` — priority panel (1.3 m) → arrester → inverter → meter (0.8 m) → walkway (½width+0.3 m) → rail (0.4 m); hover highlights in red EXACTLY what a click removes; each removal is one undoable patch; panel erase cascades (tables reindexed/dropped, strings pruned not wiped); a miss does nothing by design | Step6Editor.tsx:718-762,2975-2978,3383-3450; step6-erase.ts:37-67; step6-erase.test.ts (10 PASS); cascade.ts (s7) | working | SC.10-6.10 |
| CODE.step6-layout.24 | Walkway tool: drag (>0.5 m) draws a lane; width chips 600/800/1000 mm + custom number input 100–3000 step 50 (clamped to 100–3000 on commit); heightMm fixed 100; live true-width strip preview; amber render at real width | Step6Editor.tsx:162,846-859,1638-1684,3077-3090,3362-3381 | working | SC.10-6.11 |
| CODE.step6-layout.25 | No-build zone tool: drag an axis-aligned rectangle (each side ≥0.3 m, else ignored as a stray click); stored kind 'fire_setback', heightM 0 = blocks placement but casts no shadow; CLICK (<0.4 m drag) on an existing zone removes the TOPMOST one under the point; hatched orange render drawn ABOVE panels; live rect preview | Step6Editor.tsx:860-881,1246-1259,3142-3155,3348-3360; layout.ts:113-119 | working | SC.10-6.12 |
| CODE.step6-layout.26 | Safety rail tool: drag (>0.5 m) along an edge → rail heightMm 1100, red dashed line render; true-width strip preview (0.12 m) | Step6Editor.tsx:882-891,3091-3103,3362-3381 | working | SC.10-6.13 |
| CODE.step6-layout.27 | Lightning arrester tool: tap over a roof places a 2.0 m arrester; ghost marker only over a roof; constant-screen-size amber marker with bolt icon | Step6Editor.tsx:157,763-774,2996-3000,3234-3251,3452-3459 | working | SC.10-6.14 |
| CODE.step6-layout.28 | Mount inverter tool, two sub-modes via Inverter/Meter toggle in the bottom hint bar (aria-pressed, per-mode instruction text): INVERTER snaps to the nearest roof edge within 4 m (live ghost), stored as edgeIndex+t at 1.5 m height, then tool auto-returns to Select with "Inverter mounted — now string the panels"; METER is a free point (`gridConnection`), re-tap moves it, copy explains it makes the AC cable a measured length. NOTE: mounting REPLACES the whole `inverterPlacements` array — only one inverter placement can ever exist | Step6Editor.tsx:243-247,775-811,1694-1738,2981-2994,3253-3291,3460-3466 | working | SC.10-6.15 |
| CODE.step6-layout.29 | Stringing button (G) opens the stringing sheet: Auto string / Manual string / Clear strings (Clear only rendered when strings exist); blocked with lock flash while locked. NO running count badge on the Stringing or String-connections rail buttons (census marks all ELECTRICAL tools "with count") | Step6Editor.tsx:401-407,1538-1551,2375-2408 | POC-DEFECT(no running count on Stringing / String connections buttons; census requires each ELECTRICAL tool carry one) | SC.10-6.16 |
| CODE.step6-layout.30 | Auto string: `autoStringPlan` groups panels into MPPT-valid strings at the site design temps (unstrung-over-illegal — planner leaves panels unstrung rather than build an illegal string, reported via validateSystem); ONE patch writes strings + DC home-run routes + AC route so undo restores both; showStrings forced on | Step6Editor.tsx:917-939; autostring.ts (s7); routing.ts (s7); autostring.test.ts (s7's) | working | SC.10-6.38 |
| CODE.step6-layout.31 | Manual string: tap-to-wire in Select mode; live bar "Tap panels in order — N of min–max" from `stringSizing` + running cold Voc coloured green/amber/red with explicit "over {maxDcV} V limit" and "below MPPT floor when hot"; refuses a panel already in this string (silent no-op), a disabled panel ("enable it before wiring"), a panel owned by another string (names the owner); Esc/Cancel discards; "Save string" creates String {n} with round-robin inverter/MPPT index and a 5-colour palette | Step6Editor.tsx:242,285-296,813-835,1740-1793,1001-1017 | working | SC.10-6.39 |
| CODE.step6-layout.32 | Clear strings: patches `strings: []` ONLY — `cableRoutes` survive, so `dcCableFromRoutes` keeps returning routed=true home-run metres for strings that no longer exist; the status pill then shows a "routed" DC length with zero strings | Step6Editor.tsx:2394-2406; routing.ts:505-530 (s7) | POC-DEFECT(clear-strings orphans cable routes; DC read-out stays "routed" for deleted strings) | SC.10-6.40 |
| CODE.step6-layout.33 | String connections sheet: read-only card per string — colour dot, name, "INV n · MPPT n", inverter icon → numbered chain 1…N in wiring order, panel count, string Voc (STC); empty state points at the Stringing tool | Step6Editor.tsx:1546-1551,2410-2474 | working | SC.10-6.17 |
| CODE.step6-layout.34 | Canvas pan/zoom enabled only in Select mode and not while manual-stringing (`panEnabled` prop); draw-tool pointer-downs return true to claim the gesture (pan suspended mid-gesture — mechanism in SatCanvas, s1) | Step6Editor.tsx:1165-1204; SatCanvas.tsx (s1) | working | SC.10-6.21 |
| CODE.step6-layout.35 | Tap select: hit = centre within 1.3 m (`findPanelAt` — a radius test, not a footprint test); plain tap toggles single selection, Shift-tap adds/removes; a press that travels <0.3 m resolves as a click | Step6Editor.tsx:463-470,709-713,836-839,1219-1227 | working | SC.10-6.22 |
| CODE.step6-layout.36 | Marquee: drag on empty canvas draws a dashed box; release selects every panel whose CENTRE is inside; Shift unions with the existing selection; a <0.3 m box is an empty click (clears selection unless Shift) | Step6Editor.tsx:894-913,1201-1202,3293-3305 | working | SC.10-6.23 |
| CODE.step6-layout.37 | Panel drag move: pressing a panel starts a move (unselected panel ⇒ moves just that one); live outline ghost shows the WHOLE moving set including full segment closure; validated once on release, all-or-nothing; a refused move flashes "That move doesn't fit — setback, obstruction or another array is in the way"; strings survive moves | Step6Editor.tsx:1194-1199,1219-1236,3307-3346; panel-move.ts:98 | working | SC.10-6.24 |
| CODE.step6-layout.38 | `movePanels` rules: (1) a touched segment moves in full and its polygon travels with it; (2) validation runs against the layout minus the moving set (no self-collision), each moved panel re-validated at its OWN pose with the segment's bridge clearance; refusal returns the original layout untouched | panel-move.ts:47-111; panel-move.test.ts (15 PASS) | working | SC.10-6.24 |
| CODE.step6-layout.39 | Keyboard nudge: arrows move the selection 0.1 m, Shift+arrows 0.5 m (screen-up = −y = north); same all-or-nothing validation, refusal reason flashed once | Step6Editor.tsx:472-487,1042-1049; panel-move.ts:113-132; panel-move.test.ts:188+ | working | NONE (visible-label equivalent is a redesign duty; POC keyboard-only) |
| CODE.step6-layout.40 | Cable-route editing: interior waypoints draggable (0.9 m grab tolerance); pressing ON a straight segment (0.6 m) inserts a corner and drags it in the same gesture; committed on release as ONE undo step and the route is marked `manual: true` so auto-routing never overwrites it; ends (panel/inverter) not grabbable; an inserted corner moved <0.25 m is dropped as collinear noise | Step6Editor.tsx:251-253,946-999,1179-1193,3186-3227 | working | SC.10-6.25 |
| CODE.step6-layout.41 | Every draw tool previews live: walkway/rail true-width strips, keepout hatched rect, panel-fill zone + blocker-aware panel ghosts, single-panel fit ghost, grow ghost, inverter/arrester placement ghosts, erase red highlight | Step6Editor.tsx:3307-3508 | working | SC.10-6.26 |
| CODE.step6-layout.42 | Selection context bar floats above the selection centroid at constant screen size (counter-scaled foreignObject); shows "N panel(s)" | Step6Editor.tsx:2479-2618 | working | SC.10-6.27 |
| CODE.step6-layout.43 | Group: shown only when 2+ loose panels (none already in a segment) all on one roof; `groupIntoTable` (s2) creates a parametric table, strings cleared, new table selected | Step6Editor.tsx:539-566,2619-2632; segment-ops.ts (s2); segment-ops.test.ts (s2's) | working | SC.10-6.28 |
| CODE.step6-layout.44 | Quick grow "+Row"/"+Col": one tap adds one row/column on whichever side has room (probed via live preview: bottom→top, right→left); button disabled with reason when no side has room ("No room for another row on this roof"); a failed add flashes inline next to the button | Step6Editor.tsx:574-597,2524-2560,2637-2666 | working | SC.10-6.29 |
| CODE.step6-layout.45 | "…" grow popover: axis Row/Column, count stepper 1–20, side buttons (top/bottom or left/right), live green ghost of the EXACT panels a grow would add, "Add N row(s)/column(s)"; failure flashes "No room to add a {axis} there" without closing | Step6Editor.tsx:601-610,2796-2874,2580-2589 | working | SC.10-6.30 |
| CODE.step6-layout.46 | Rotate: ±90° buttons update each selected panel's azimuth (mod 360); value chip shows the shared azimuth or "mixed". NOTE: acts on panels only — `segment.azimuthDeg` is NOT updated, so after rotating a table from the context bar the Table-settings azimuth read-out (seg.azimuthDeg + compass) disagrees with the panels' actual facing (footprints/energy follow the panels; the sheet's setSegmentAzimuth path keeps both in step) | Step6Editor.tsx:446-461,2568-2569,2692-2717 | POC-DEFECT(context-bar rotate leaves segment.azimuthDeg stale; table sheet then shows the old facing) | SC.10-6.31 |
| CODE.step6-layout.47 | Tilt ±5°: selection is PARTITIONED — every represented non-flush segment tilts as a unit via `setSegmentTilt` (racking/legs/member model stay coupled, bridged panels reconciled), loose panels tilt individually clamped 0–35°, flush segments skipped; value chip shows the MIN tilt of the selection (never says "mixed") | Step6Editor.tsx:489-519,2718-2744 | working | SC.10-6.32 |
| CODE.step6-layout.48 | "Table…" button appears only when the selection classifies as one segment (`classifySelection`); opens the Table-settings sheet | Step6Editor.tsx:533-536,613-617,2746-2756 | working | SC.10-6.33 |
| CODE.step6-layout.49 | Enable/Disable: label flips by state ("Disable — panels stay placed but produce nothing"); toggling ALSO wipes ALL strings project-wide (`strings: []`), even strings not touching the selection | Step6Editor.tsx:429-444,2757-2773 | working | SC.10-6.34 |
| CODE.step6-layout.50 | Delete selected (button or Del/Backspace): `cascadeDeletePanels` reindexes/drops affected tables and PRUNES strings (not wipes); selection cleared | Step6Editor.tsx:521-530,1050-1056,2774-2784; cascade.ts (s7) | working | SC.10-6.35 |
| CODE.step6-layout.51 | Clear selection: X button on the bar, or Esc (which also cancels tool/gesture back to Select) | Step6Editor.tsx:1034-1041,2785-2793 | working | SC.10-6.36 |
| CODE.step6-layout.52 | Lock discipline on selection actions: Rotate/Tilt/Enable/Delete render dimmed (opacity .4 + aria-disabled) but stay clickable — the handler flashes "Layout locked — unlock to make changes"; Grow/Group/table paths refuse with the reason in the tooltip ("Layout is locked"); mutating rail tools are disabled at activation | Step6Editor.tsx:373-388,2561-2570,2637-2784 | working | SC.10-6.37 |
| CODE.step6-layout.53 | Lock/Unlock layout button (top-right rail): locking resets to Select and cancels manual string/drag/marquee. NOTE: `locked` is component state — not persisted, resets when leaving the step or reloading | Step6Editor.tsx:235,390-399,1586-1595 | working | SC.10-6.19 |
| CODE.step6-layout.54 | Undo/Redo buttons (disabled at empty stacks) + Ctrl/Cmd+Z and Ctrl/Cmd+Shift+Z; every gesture (drag-move, route drag, draw) commits exactly ONE undoable patch on release | Step6Editor.tsx:973-999 (route), 1219-1236 (move), 1026-1030,1561-1576 | working | SC.10-6.18 |
| CODE.step6-layout.55 | Clear all panels: button disabled when locked or 0 panels; confirm dialog "removes all N panels and their strings … You can undo"; clears panels+segments+strings in one patch | Step6Editor.tsx:1596-1609,1939-1967 | working | SC.10-6.20 |
| CODE.step6-layout.56 | Table-settings header: "{label} · rows×cols · N panels · kWp" (kWp rounded to 0.1) | Step6Editor.tsx:1969-1990 | working | SC.10-6.41 |
| CODE.step6-layout.57 | Structure preset trio Flush / Standard 10° / Walk-under 2.2 m with `StructurePreview` cross-sections; match is DERIVED from resolved racking (std: tilt 10 & frontLeg<1 m; walk: frontLeg≥2.2 m), "· custom" suffix when none matches; presets write only the fields they own (`applyStructChoice`, s5-C lib) | Step6Editor.tsx:665-670,1992-2091; structure-edit.ts; mms-customize.test.ts (20 PASS) | working | SC.10-6.42 (C) |
| CODE.step6-layout.58 | Ground roof (roofType 'ground') swaps the preset row for Foundation: Driven pile / Ballasted, with "Embedment depth and pull-out capacity depend on the soil — a site survey and engineer sign-off are required" | Step6Editor.tsx:2041-2072 | working | SC.10-6.43 (C) |
| CODE.step6-layout.59 | Racking trio Flush / Fixed tilt / Dual tilt (`setSegmentRacking`, s2); racking/tilt/respace edits reconcile obstruction-bridging panels in the SAME undoable patch, and every segment edit clears strings | Step6Editor.tsx:619-641,2093-2100; structure-edit.ts:reconcileBridgedPanels | working | SC.10-6.44 |
| CODE.step6-layout.60 | Panel tilt (non-flush): −/+ 1° buttons + 0–35 range slider (aria-label "Panel tilt"); the 5°-step control lives on the selection bar (row 47) | Step6Editor.tsx:2102-2116 | working | SC.10-6.45 |
| CODE.step6-layout.61 | Inter-row shading card (non-flush + located): recommended winter shadow-free row pitch (live `shadowFreePitchM` for the segment's collector length + azimuth), resulting GCR, and one-tap "Apply shadow-free spacing" → `respaceSegment` (s2); no-room refusal flashes "No room to apply that spacing" | Step6Editor.tsx:671-678,2118-2153; spacing.ts:9-49; spacing.test.ts | working | SC.10-6.46 |
| CODE.step6-layout.62 | Azimuth row: −/+5° steps, "Due S" preset (hardcoded 180 — northern-hemisphere assumption; PRD's hemisphere-correct rendering is a pass-two addition, not in the POC), "Roof slope" preset (roof.slopeAzimuthDeg ?? 180), live "az° {8-point compass}" read-out; sheet path `setSegmentAzimuth` keeps segment+panels in step | Step6Editor.tsx:638-641,1976,2155-2166 | working | SC.10-6.47 |
| CODE.step6-layout.63 | Structure profile buttons (non-flush): each steel section with its kg/m; `setSegmentProfile` (s2) | Step6Editor.tsx:642-654,2168-2181 | working | SC.10-6.48 (C) |
| CODE.step6-layout.64 | Structure member model (non-flush): cross-section preview + counted bill — Legs (front+back) / Rafters / Purlins / Braces with count · metres, total steel kg for the profile; editable Leg spacing 0.5–4 m and Clearance 0–3 m (clamped; clearance change reconciles bridged panels), Anchored/Ballasted; first DRC message + "(+N more)" and every structure warning listed | Step6Editor.tsx:655-664,2183-2252; structure.ts (s5-C) | working | SC.10-6.49 (C) |
| CODE.step6-layout.65 | STRUCTURE_DISCLAIMER is rendered ONLY inside the non-flush member-model branch — a FLUSH table's sheet shows no disclaimer at all; census says "always" | Step6Editor.tsx:2168 (branch guard `!isFlush && seg2.kind !== 'flush'`), 2253-2255; structure.ts:STRUCTURE_DISCLAIMER | POC-DEFECT(disclaimer absent for flush tables; census requires it always) | SC.10-6.50 (C) |
| CODE.step6-layout.66 | Duplicate table: `duplicateSegment` (s2) + fresh label, strings cleared, new table selected, sheet closes; "No room to duplicate this table" flash on failure. Delete table: cascade-deletes all its panels | Step6Editor.tsx:679-705,2262-2277 | working | SC.10-6.51, SC.10-6.52 |
| CODE.step6-layout.67 | Live validation banner: `layoutIssues + structureIssues + routeIssues + validateSystem` merged, errors sorted before warnings; banner shows the FIRST message + "+N more · View all"; error styling when any error, else warning; click opens the full sheet | Step6Editor.tsx:332-356,1127-1155 | working | SC.10-6.53 |
| CODE.step6-layout.68 | Validation item — string cold Voc above inverter max ('voc_high', error) | stringing.ts:74 (s7); surfaced Step6Editor.tsx:332-356 | working (emitter owned s7) | SC.10-6.54 |
| CODE.step6-layout.69 | Validation item — hot Vmp below the MPPT floor ('vmp_low') | stringing.ts:81 (s7) | working (emitter owned s7) | SC.10-6.55 |
| CODE.step6-layout.70 | Validation item — panel/string current above MPPT input ('imp_high' per string; 'isc_high' from the auto-string planner) | stringing.ts:89; autostring.ts:204 (s7) | working (emitter owned s7) | SC.10-6.56 |
| CODE.step6-layout.71 | Validation item — DC/AC ratio too high / too low ('dc_ac_high' >; 'dc_ac_low') | stringing.ts:103,112 (s7) | working (emitter owned s7) | SC.10-6.57 |
| CODE.step6-layout.72 | Validation item — panels left unstrung ('unstrung_panels', error; enabled-panel ids passed so the check runs) | stringing.ts:127 (s7); Step6Editor.tsx:338-346 | working (emitter owned s7) | SC.10-6.58 |
| CODE.step6-layout.73 | Validation item — more strings than MPPT inputs ('mppt_overflow'; planner variant 'mppt_capacity') | stringing.ts:139; autostring.ts:274 (s7) | working (emitter owned s7) | SC.10-6.59 |
| CODE.step6-layout.74 | Validation item — no valid string length for this panel/inverter pair ('string_window_empty' — an empty voltage window is an explicit fault, never silently passed) | stringing.ts:63 (s7) | working (emitter owned s7) | SC.10-6.60 |
| CODE.step6-layout.75 | Tap-to-locate: every issue card with live `focusPanelIds` is a button labelled "tap to locate →" — switches to Select, selects the faulty panels, closes the sheet and centres the canvas on their centroid | Step6Editor.tsx:2291-2349 | working | SC.10-6.61 |
| CODE.step6-layout.76 | Inline "Auto-string now" button ON the 'unstrung_panels' and 'mppt_capacity' cards (stopPropagation so it doesn't also locate); runs the full auto-string | Step6Editor.tsx:2354-2366 | working | SC.10-6.62 |
| CODE.step6-layout.77 | THE HARD GATE: Wizard step-6 `nextBlocker` — 0 enabled panels blocks with "Place at least one panel to continue"; any electrical ERROR blocks Next via `electricalGate` with the lead message ("(and N more electrical errors)" when several) and " — use Stringing → Auto string" appended when auto-stringable; the gate also caps `allowedStep`, so a later-invalidated design cannot sit on a downstream step | Wizard.tsx:109-138 (s10, cited); electrical/gate.ts:29-58 (s7); electrical-gate.test.ts (s7's) | working | SC.10-6.63 |
| CODE.step6-layout.78 | Status pill: enabled-panel count · "X.X / target kWp" (green; red only when over the PLAN limit — over-TARGET alone does not recolour, the cue is the visible X/target comparison) · numbered string colour dots · "DC cable ≈ N m" with "(est.)" suffix when no routed runs exist (routed metres from `dcCableFromRoutes`, else the labelled legacy `estimateDcCableM`) | Step6Editor.tsx:272-281,1795-1841 | working | SC.10-6.64 |
| CODE.step6-layout.79 | "3D" pill button (bottom-left, offset to left:76 so it can never cover the tool rail — a documented past bug made Mount-inverter unreachable); opens Scene3D REPLACING the editor; on close, focus returns to the 3D button (Phase 22p a11y) | Step6Editor.tsx:224-234,1101-1103,1843-1874 | working | SC.10-6.65 |
| CODE.step6-layout.80 | PLAN-CAPACITY LIMIT EXISTS: `PLAN_LIMIT_KW = resolveRules().defaults.planLimitKw` (=10); kWp above it shows a persistent error banner "Total capacity X kWp exceeds the 10 kW plan limit — remove panels or upgrade" with an "Upgrade" button (demo flash "Plan upgrades are not available in this demo") and turns the status-pill kWp red. Census/D38 and M05-12/Q28 say NO subscription/plan-capacity limit anywhere in the layout | Step6Editor.tsx:140,363-365,1108-1126,1810; india.ts:529 | POC-DEFECT(census forbids any plan-capacity limit in the editor; code enforces a 10 kW freemium banner + Upgrade CTA) | SC.10-6.66 |
| CODE.step6-layout.81 | Keyboard shortcuts (desktop bonus): H heatmap · S strings · V select · T/P panels · E erase · W walkway · R rail · K keepout · L arrester · I inverter · G stringing · Esc cancel-to-Select · Del/Backspace delete · arrows nudge · Ctrl/Cmd+Z(+Shift); suppressed while an input has focus or a sheet/dialog is open | Step6Editor.tsx:1019-1099 | working | NONE (POC shortcut layer; census 10-2/redesign folds these into visible controls) |
| CODE.step6-layout.82 | Transient notice system: 2.6 s hint-bar flash with lock/ok/info icon, `role="status"` aria-live polite (lock refusals, meter/inverter confirmations, auto-design warnings) | Step6Editor.tsx:164,269-270,367-375,1626-1637 | working | NONE |
| CODE.step6-layout.83 | Per-tool instruction hint bar (top centre) whenever a non-Select tool is active: exact copy per tool (e.g. Panels "Click to add one panel · drag to fill an area (obstacles auto-avoided)") | Step6Editor.tsx:152-160,1685-1691 | working | NONE |
| CODE.step6-layout.84 | Banner stacking: plan-limit and issue banners push the tool rails and hint bar down by 38 px each (`chromeTop`) so chrome never hides under banners | Step6Editor.tsx:363-365,1416,1557 | working | NONE |
| CODE.step6-layout.85 | Off-roof draws fall back to the FIRST roof: walkway/rail/keepout and the drag-fill use `pickRoofAt(start) ?? project.roofs[0]` — an object drawn entirely off any roof is still attributed a roofId (the first roof's) | Step6Editor.tsx:846-849,1301 | working (attribution fallback noted — objects drawn off-roof belong to roofs[0]) | NONE |
| CODE.step6-layout.86 | String-invalidation policy is asymmetric: adding a panel, drag-fill, group, grow, duplicate table, enable/disable and every segment edit wipe ALL strings; move/nudge preserves strings; rotate/tilt preserve strings although they change the electrical geometry; erase/delete PRUNE via cascade | Step6Editor.tsx:441,486,562,591,627,691,1282,1316; cascade.ts (s7) | working (policy recorded as-is; rotate/tilt keeping strings while enable wipes them is the code's own inconsistency, not resolved here) | NONE |
| CODE.step6-layout.87 | `panel-pose.ts` — THE canonical 3D pose of a module (mesh, shadow slab and ray origin share one plate): flush-on-slope +0.06 m standoff; structured = frontLeg + rise/2 + module standoff; loose flat = 0.18 m; yaw = down-slope on pitched, −azimuth when tilted on flat, roof-grid when flush-flat; `panelSampleHeightM` = real plane + 0.05 m epsilon (fixes walk-under-over-tank false shading) | panel-pose.ts:17-19,43-87,89-107; one-frame.test.ts, frame-parity.test.ts, panel-inspector.test.ts (all PASS) | working | NONE (3D consumption censused under A.10-7, agent B) |
| CODE.step6-layout.88 | Design Health scorer (`computeHealth`): three categories energy/electrical/utilization, each 100 − fixed per-code deductions (rule config), deduped by code (imp_high per-string collapse ⇒ monotonicity), N/A categories null; total = weighted mean of applicable categories; bands good/fair/poor from rule thresholds | health.ts:74-96,179-295; health.test.ts (28 PASS) | working | NONE (Design Health censused at A.10-1, shell sitting) |
| CODE.step6-layout.89 | Health exclusions are deliberate and documented: dc_ac_high/low (charged once via the Copilot insight's 0.90–1.35 band), temp_coeff_estimated (provenance note), foundation_dead_load (deducting would assert a structural verdict §F forbids); every emitted code must appear in VALIDATION_CATEGORY or EXCLUDED_VALIDATION — enforced by test so a new DRC code cannot be silently unscored again | health.ts:98-115; health-coverage.test.ts (10 PASS) | working | NONE |
| CODE.step6-layout.90 | Health key & snapshots: composite key = designFp + solarAccessFp + insightState + registered analyzers; `provisional: true` while the shading recompute is stale; `explainDelta` names the exact codes added/removed per category; `nextHealthSnapshot` never chains provisional intermediates (deltas always compare settled states) and is write-loop-free | health.ts:157-170,287-357; health.test.ts:212-303 | working | NONE (A.10-1: health sheet deductions + "what changed" delta) |
| CODE.step6-layout.91 | Health context lines (shown, never scored): irradiance provenance ("PVGIS-measured for this pin" vs "built-in estimate (±10%)"), AI-detected entity count ("dimensions are detector estimates"), and calibration state | health.ts:273-285; health.test.ts:316-320 | working | NONE |
| CODE.step6-layout.92 | `shadowFreePitchM`: winter-solstice (Dec 21 north / Jun 21 south) shadow-free window 09:00–15:00 solar time in 0.5 h steps via the app's ONE sun engine; pitch = collector·cos(tilt) + max back-edge shadow along the inter-row axis; flush returns collector length (GCR≈1); `gcr` = collector/pitch (pvlib convention) | spacing.ts:9-49; spacing.test.ts (6 PASS) | working | SC.10-6.46 (supporting solver) |
| CODE.step6-layout.93 | Heatmap compute is cancellable and cached: fingerprint-keyed cache ref; stale async results discarded via an aborted flag; toggling the heatmap off/on without a geometry change re-uses the cached raster | Step6Editor.tsx:222,310-330 | working | NONE |

## CENSUS-UNMATCHED — agent A's half (A.10-6 layout/fill/segment-surface rows; A.10-7 share = none)

| Census entry | Finding |
|---|---|
| *(none)* | All 60 owned rows of A.10-6 have a code surface in the rows above. Two are matched-with-defect, not unmatched: `SC.10-6.06` (decision log + suggestions exist; the accept/ignore half has no UI — row 18) and `SC.10-6.66` (code CONTRADICTS the census: a 10 kW plan-limit banner + Upgrade CTA exists — row 80). Partial-content gap on `SC.10-6.04` recorded at row 16 for agent B's cross-check. |

rowCount: 93
defectCount: 7 (rows 16, 18, 29, 32, 46, 65, 80; of these, 16 and 65 sit on B/C-owned census rows — my-half defects are 18, 29, 32, 46, 80 = 5)
censusUnmatched: 0 (2 matched-with-defect: SC.10-6.06 partial, SC.10-6.66 contradicted)
newBeyondCensus: 15 (rows 7, 39, 81–91, 93 — Step-4 capacity solver, keyboard layer, notices/hints/chrome, off-roof fallback, string-invalidation policy, pose engine, health engine ×4, heatmap cache)
notes:
- All 154 tests across 11 files PASS read-only; no failing test = no test-derived defects.
- The plan-limit banner (row 80) is the census's single hard contradiction (D38/Q28); left in-row, unresolved per the honesty rule.
- Stringing/validation UI is inventoried here (it lives in Step6Editor.tsx); every emitter (stringing/autostring/routing/drc/cascade/gate) is sitting 7's — cited one-hop, never claimed.
- Structure/table-sheet rows 57/58/63/64/65 sit on agent C's census rows; heatmap row 16 on agent B's — recorded because the UI is in my primary file, ownership marked in the census column.
- Layout lock (row 53) is unpersisted component state; census does not specify persistence — noted, not judged.
- My A.10-7 census share is zero rows; agents B/C carry all 43.
