# Feature inventory — step6-structures (Sitting 5 agent C · Step 6: the parametric mounting structure, profiles, foundations, leg-plan editor, ground tables, and the F8-25 honesty rule)

Pass-two inventory, 2026-08-05 (Sitting 5, agent C). Primary files (under `3d_design_studio/src/features/solar-studio/`): `lib/structure.ts` (1,078 ln) · `lib/structure-edit.ts` (257) · `lib/structure-view.ts` (191) · `lib/leg-plan-edit.ts` (207) · `lib/foundation.ts` (208) · `lib/ground.ts` (55) · `three/StructureInstanced.tsx` (181) · `three/StructureNodesInstanced.tsx` (145) · `three/StructEditPanel.tsx` (627) · `three/LegPlanEditor.tsx` (321) · `components/StructurePreview.tsx` (164) · `three/profile-geometry.ts` (209).

Test run (read-only, deps from lockfile, vitest 3.2.7): **21 files, 398 tests, all PASS** — structure (22) · structure-golden (8, snapshot) · structure-parametrics (27) · structure-view (27) · structure-edit (18) · monorail (23) · mms-customize (20) · mms-bom (17) · leg-plan (28) · leg-plan-edit (19) · LegPlanEditor.dom (10) · foundation-grounding (15) · foundations (28) · foundation-clamp (11) · ground-mount (22) · live-structure-geometry (5) · drc-structure (14) · dxf-structure (14) · frame-parity (11) · profiles (32) · obstruction-grounding (27).

One-hop files consulted but owned elsewhere: `lib/segment-ops.ts` (setSegmentRacking/Tilt/Profile/StructureFields, segmentFrameAngle — s2), `lib/layout.ts` panelFootprintM/COL_STRIDE (s5-A), `lib/panel-pose.ts` (s5-A), `data/profiles.ts` (s4 file; section catalog inventoried here because structure is its only consumer), `data/rules/india.ts` foundations/sheet/wind/defaults (s8 file), `lib/hardware.ts` nodeHardware (s7 file; rendered only by my StructureNodesInstanced), `lib/drc.ts` structureIssues (s7 file; structure-side checks inventoried here per the drc-structure test claim), `lib/capabilities.ts` (s4), `screens/Step6Editor.tsx` + `three/Scene3D.tsx` (s5 shared screens; only their structure blocks cited here), `lib/fingerprints.ts` (s9), `lib/persistence/normalize.ts` (s10), `screens/Step8Sld.tsx`/`Step9Bom`/`ProposalView.tsx`/`components/drawing/StructureSheet.tsx` (s8/s2 — cited as disclaimer consumers only), `components/SatCanvas.tsx`/`lib/geo.ts` (s1).

Census split assumption (recorded, not resolved): agent C owns the structure/ground rows — `SC.10-6.42/.43/.44/.48/.49/.50` and `SC.10-7.20`–`SC.10-7.31`. `SC.10-6.44` (racking triple) is claimed here because racking kind IS the structure-topology switch; the op itself (`setSegmentRacking`) is s2's. `SC.10-6.41/.45/.46/.47/.51/.52` (header, tilt, inter-row, azimuth, duplicate/delete) left to agent A; `SC.10-7.12` (shadow casting) to agent B — with a structure-side note at row 40.

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step6-structures.1 | `resolveRacking` lazy chain — segment.racking → roof.structureOverride → project.structureDefaults → built-ins (legSpacing 2.0 m); returns null for flush racking AND for any pitched roof (`roof.pitchDeg > 0`, v1 flat-deck topology); `frontLegM = max(stored, clearanceM)`; `backLegM` is ALWAYS front + module rise `h·sin(tilt)` — repairs legacy fill segments that persisted backLegM=0.3 at READ time, never rewriting stored bytes | structure.ts:167-234; test structure.test:68-114 ("repairs a persisted backLegM=0.3 at READ time", "most specific wins") | working | NONE |
| CODE.step6-structures.2 | Surface-conditional foundation DEFAULTS: rooftop → `concrete` cast PCC pedestal (D12: waterproofing/ponding/cost), ground → `pile` (driven post), metal shed → `anchor` (E1: cannot cast on corrugated steel); foundationShape falls through to the rule config's shape for that kind | structure.ts:121-132,186-192,208-214; tests monorail.test:220-231 ("DEFAULTS to an anchor, not the rooftop pedestal", "a flat RCC roof still defaults to the pedestal"), ground-mount.test:147-163 | working | NONE |
| CODE.step6-structures.3 | READ-TIME foundation clamp: a persisted kind the surface cannot carry (e.g. `concrete` written on a metal shed before 22h) resolves to `allowed[0]` instead — nothing rewritten, so changing the roof back restores the stored preference; the pedestal BOM line and dead-load warning go with it | structure.ts:193-199; test foundation-clamp (11 PASS: "a PERSISTED concrete pedestal on a shed is corrected, not honoured", "the correction is READ-TIME, never a rewrite") | working | NONE |
| CODE.step6-structures.4 | `allowedFoundations` — the single source for picker AND resolution: ground `[pile, concrete, ballast]`; metal_shed `[anchor]`; other rooftops `[concrete, anchor, ballast]` (no pile into a slab); `[]` for every non-elevated topology | structure.ts:762-778; tests foundation-clamp:110-128 ("every offered option is an allowed option, on every surface"), structure-view.test:235 | working | SC.10-7.25 |
| CODE.step6-structures.5 | `topologyOf` dispatch: flush + metal_shed → `sheet_monorail`; flush elsewhere → `flush` (no member model); sloped roof → `none`; else `elevated_table`. Builder and UI gate on the SAME predicate so offered vs built cannot disagree | structure.ts:780-787,986-996; tests monorail.test:88-104, structure.test:301-307 ("no member model on pitched roofs") | working | SC.10-6.44 |
| CODE.step6-structures.6 | `buildStructure` member emission: panels grouped by grid row (`cellIndex/COL_STRIDE`), rows split into contiguous runs at holes (no member under an absent module); per run — front+back legs at stations (AUTO: `ceil(runLen/legSpacingM)` bays + 1), a tilted rafter per station, `purlinCount` purlins spanning the run, longitudinal braces between adjacent stations at half back-leg height | structure.ts:463-682; tests structure.test:124-155 ("expected member set for a 3-panel row"), 177-187 ("holes split runs") | working | SC.10-6.49 |
| CODE.step6-structures.7 | Node graph per run: `roof_anchor` per leg base ON the deck (z = roof.heightM, not the leg base after D15), `leg_rafter` (bolts:2) at each leg top, `rafter_purlin` (bolts:1) per purlin per station, `brace_bolt` (bolts:1) at brace ends, `panel_clamp_end` at purlin ends + `panel_clamp_mid` between adjacent modules | structure.ts:565-587,634-680; test structure.test:157-167 ("counts fasteners from the node graph (hand-computed)") | working | SC.10-6.49 |
| CODE.step6-structures.8 | Determinism contract: ids are STRUCTURAL (`${seg.id}/m/rafter/2`, `${seg.id}/n/<kind>/<idx>`), the graph is a pure function of (segment, panels, roof, racking), Class-derived and never persisted; 8 golden snapshots pin byte-identity incl. single panel, holes, two rows, rotated azimuth 90 | structure.ts:284-298,437-461; tests structure-golden (8 PASS + `__snapshots__`), structure.test:169-175 | working | NONE |
| CODE.step6-structures.9 | D15 height chain — the foundation CONSUMES clearance: steel leg spans `dz+foundH → dz+frontLegM`; switching foundation kind NEVER moves the module plane (shading/energy/captures stable) and "walk-under 2.2 m" keeps meaning 2.2 m of clearance; `anchor` (height 0) is a no-op | structure.ts:538-563,419; foundation.ts:13-25,99-101; tests foundation-grounding (15 PASS: "module plane does NOT move when the foundation kind changes"), foundations.test:67-97 | working | SC.10-7.25 |
| CODE.step6-structures.10 | `anchorSpec` — ONE foundation per leg base, counted not estimated: ballast `{plates:1,ballast:1}` · pile `{piles:1}` · concrete `{pedestals:1,plates:1,anchors:2}` · anchor `{anchors:2,plates:1}`; a ground table's pile count IS its leg count | structure.ts:721-734; test ground-mount.test:171-196 ("exactly ONE foundation per leg base — never an estimate") | working | NONE |
| CODE.step6-structures.11 | `fastenerTotals` Σ over node graphs: anchors/plates/bolts/clamps/ballast/piles/pedestals/standoffs/sealingWashers + the 22j clamp split (`clampsMid`/`clampsEnd` — an end clamp is a separate part and price; `clamps` stays the total) | structure.ts:1000-1047; tests structure.test:157-167, monorail.test:191-196 | working | NONE |
| CODE.step6-structures.12 | `validateStructure` unsupported-member DRC: REQUIRED node kinds per member kind (legs need roof_anchor+leg_rafter; rafter leg_rafter; purlin rafter_purlin+panel_clamp_end; brace brace_bolt; rail sheet_standoff+panel_clamp_end); generated graphs always pass; surfaced in the 2D member card as first issue "+N more" | structure.ts:1053-1078; Step6Editor.tsx:2245-2249; tests structure.test:199-218, structure-parametrics:232-251 | working | SC.10-6.49 |
| CODE.step6-structures.13 | `steelKg` = Σ per member × its OWN class section's kgPerM (a table mixing heavy legs and light purlins cannot be priced off one kg/m); `memberSummary` = count + totalM per member kind (all 6 kinds always present), lengths rounded to 3 dp | structure.ts:331,694-707; tests structure-parametrics:207-219 ("mass is summed PER MEMBER against its own section") | working | SC.10-6.49 |
| CODE.step6-structures.14 | Phase-22g parametrics in the builder: `purlinCount` (default 2 = front/back module edges; extras interpolated in XY AND Z so they land on the tilted rafter, each with its own support+clamp nodes) · `rafterCount`/`rafterMultiplier` (default 1 rafter per leg station, emitted inside the station loop so ids never move; other counts interpolate independently with own joint nodes) · `endBufferM` (extends purlins only — legs stand where they stand) · `bracing:false` removes braces AND their bolts and still validates | structure.ts:519-531,592-661,627-631; test structure-parametrics (27 PASS) | working | SC.10-7.30 |
| CODE.step6-structures.15 | Per-member-class profiles: `racking.profileFor(kind)` — `profiles.legs/rafters/purlins` override the table profile per class (brace follows the base section); unset classes fall back; `defaultStructureParams` exports the load-bearing defaults so previews/tests share one copy | structure.ts:216-232,245-258; test structure-parametrics:195-229 | working | NONE (beyond census — census models one profile per table) |
| CODE.step6-structures.16 | Leg-plan integration (22i): saved `legPlan.points` live in the segment's LOCAL frame, rotated to world by `segmentFrameAngle` (the SAME derivation the panel lattice uses); a point counts for a run within ±(half+0.05 m); planned stations replace AUTO per run, sorted along the run so ids stay deterministic; a run the plan misses keeps AUTO with a per-run warning, and a mixed table adds the E6 summary "N of M runs use your leg plan… Open Legs (2D)" | structure.ts:344-380,492-516,688-692; test leg-plan (28 PASS: rotated azimuth 60/135/247/300 alignment, "braces span the PLANNED bays", E6 mix warning) | working | SC.10-7.31 |
| CODE.step6-structures.17 | Dual-tilt honesty: `kind:'dual_tilt'` builds the fixed-tilt topology and pushes the warning "Dual-tilt is modeled with fixed-tilt structure topology in v1 — tonnage is approximate." | structure.ts:397-401; test structure.test:189-197 ("honestly labeled") | working | SC.10-6.44 |
| CODE.step6-structures.18 | `buildMonorail` (22h, flush-on-metal-shed): two `rail` members per run (top_hat profile) at the module edges, z = roof.heightM + 0.1 m standoff height; standoffs per rail = `max(minStandoffsPerRail=2, ceil(runLen/purlinPitchM)+1)` — never a one-foot lever (E15); each standoff node `{standoffs:1, sealingWashers:1, bolts:2}` (one EPDM washer per sheet penetration); clamps as on a purlin; holes split runs; purlinPitch overridable roof → project → rule 1.4 m | structure.ts:790,807-953; data/rules/india.ts:517; test monorail (23 PASS) | working | NONE (metal-shed monorail is beyond the 10-6/10-7 census) |
| CODE.step6-structures.19 | Monorail honesty warning names BOTH assumed figures: "N sheet fixings assume 1.4 m purlin centres and a 0.2 m rib pitch — ASSUMED, not measured. Confirm both at survey…"; `foundation` field stamped `'anchor'` as nearest truth while the foundation card is hidden entirely (foundationOptionsFor → []) | structure.ts:929-948; tests monorail.test:198-231 ("warns that BOTH… are assumed", "offered no foundation at all") | working | NONE |
| CODE.step6-structures.20 | `projectStructures` — the BOM/3D entry point: dispatches per segment on topology (monorail vs elevated), skips flush/pitched/missing-roof/empty, drops structures with zero members | structure.ts:974-998; test structure.test:231-253 ("one graph per elevated segment, none for flush") | working | NONE |
| CODE.step6-structures.21 | Capture-stale guard: `STRUCTURE_MODEL_VERSION='sm2'` joins `layoutFp` (built-in default changes re-key untouched projects); `legPlan` points serialise into layoutFp; absent structure defaults change NO fingerprint | structure.ts:96-119; fingerprints.ts:112,127; tests leg-plan:253-266, structure.test:348-359, mms-customize:87-92 | working | NONE |
| CODE.step6-structures.22 | `MODULE_STANDOFF_M = 0.08` — members render at true axes; the MODULE centre plane is lifted 0.08 m (rafter half-section 0.03 + clamp stack ≈0.05) in panel-pose so member tops never pierce the glass | structure.ts:33-39; panel-pose.ts:9,71 (one hop, s5-A); StructureInstanced.tsx:118-120 | working | NONE |
| CODE.step6-structures.23 | F8-25 honesty core: `STRUCTURE_DISCLAIMER` ("Structural design subject to certified engineer verification (IS 875 wind load, roof capacity, anchoring).") + `engineeringStatus` — approved:false with label "PRELIMINARY — pending engineer verification" unless `project.structuralVerification.status === 'engineer_approved'` (a HUMAN flag set in Step8Sld's chip UI, s8). No wind-load/uplift/roof-capacity computation exists anywhere in the module | structure.ts:41-52,74-75; Step8Sld.tsx:132-140 (one hop); test structure.test:309-321 ("defaults to PRELIMINARY and flips only on explicit sign-off") | working | SC.10-6.50 |
| CODE.step6-structures.24 | `windZoneInfo`: IS 875-3 representative basic wind speed per state (display-only table, e.g. Gujarat 50 m/s), `high` at ≥47 m/s with label "High-wind zone (IS 875 basic wind speed N m/s) — engineer verification mandatory"; null/silent for unknown states; NEVER a calculation. Consumed by Step8Sld, Step9Bom, ProposalView (s8 screens) | structure.ts:54-71; data/rules/india.ts:471-486; test structure.test:323-329 | working | NONE (10-9/10-11 census rows carry the surfaces) |
| CODE.step6-structures.25 | Disclaimer travel (F8-28 side of F8-25): STRUCTURE_DISCLAIMER renders in the Step6 member card, joins every structural BOM line formula + a BOM note + the CSV export, heads the structural drawing sheet, and prints in the proposal with the engineering status and wind flag | Step6Editor.tsx:2253-2255; bom.ts:231-233; Step9Bom/index.tsx:299-305; StructureSheet.tsx:16,214; ProposalView.tsx:413-419; test structure.test:331-346 ("CSV export carries the disclaimer whenever structure lines exist") | working | SC.10-6.50 |
| CODE.step6-structures.26 | `applyStructChoice` (§H): ONE pure function computes the patch a caller renders or commits — never mutates the input project, deterministic (preview===commit by construction); null for unknown segment/profile/missing spec and for every member choice on flush racking (the model refuses independently of the UI) | structure-edit.ts:72-175; test structure-edit (18 PASS: "never mutates the input project", "the hovered preview IS the committed patch") | working | SC.10-7.20 |
| CODE.step6-structures.27 | Presets: `flush` → setSegmentRacking flush; `standard` → fixed_tilt 10° + clearanceM CLEARED back to the lazy chain; `walkunder` → fixed_tilt 10° + clearanceM 2.2 owned on the segment AND remembered on `roof.structureOverride.clearanceM` so the NEXT fill bridges again after a clear-all (standard deletes the roof key) | structure-edit.ts:92-132; test structure-edit:40-72 ("walk-under… owned explicitly", "standard preset clears an earlier walk-under clearance") | working | SC.10-6.42, SC.10-7.21 |
| CODE.step6-structures.28 | Ground presets: `ground_pile`/`ground_ballast` → fixed_tilt at the rule ground tilt (20°), clearanceM 0.5 (ASSUMED vegetation/splash floor, comment says drainage governs the real value), foundation pile or ballast | structure-edit.ts:29-31,102-109; data/rules/india.ts:521; test foundation-clamp:93-97 ("every ground kind the app offers as a preset survives resolution") | working | SC.10-6.43 |
| CODE.step6-structures.29 | Remaining choices: `profile` (by catalog key), `tilt`, `clearance` (>0 writes, else clears the key), `foundation`, `foundationShape`, `mms` — all routed through s2's segment setters, all refused on flush; every choice runs `reconcileBridgedPanels` in the SAME patch so clearance changes re-enable/disable panels bridging an obstruction (walk-under → flush drops the array INTO the tank) | structure-edit.ts:133-173,86-87,206-250; tests structure-edit:73-231, mms-customize:124-140 | working | SC.10-7.24, SC.10-7.25, SC.10-7.26, SC.10-7.28, SC.10-7.29 |
| CODE.step6-structures.30 | MMS default-clears-key discipline: `value === default` is passed as `undefined` and `setSegmentStructureFields` DELETES the key — a table returned to standard serialises byte-identically to one never touched, so no capture stales for a no-op (pinned per field: set→clear ⇒ byte-identical layoutFp, key REMOVED not stored-undefined, graph identical) | structure-edit.ts:42-56; StructEditPanel.tsx:521-530,566-573; test mms-customize:94-122 | working | SC.10-7.30 |
| CODE.step6-structures.31 | Structure-view pure state: `PanelVisibility` show/ghost/hide × `TableScope` all/isolate; default ghost+all ("the point of entering the mode is to see the structure"); NOTHING persisted or fingerprinted; `partitionPanels` — no selection ⇒ everything normal (stale view cannot leak), isolate hides other tables AND loose panels, every panel lands in exactly one bucket; `visibleStructureIds` drops non-selected structures under isolate | structure-view.ts:22-94; test structure-view (27 PASS) | working | SC.10-7.22, SC.10-7.23 |
| CODE.step6-structures.32 | Capture override (E8): `effectiveView` forces `{show, all}` in capture mode so a proposal hero shot is never a bare-structure frame; outside capture the user's choice stands | structure-view.ts:40-46; Scene3D.tsx:1046; test structure-view:163-175 | working | SC.10-7.22 |
| CODE.step6-structures.33 | `foundationOptionsFor` (what the picker OFFERS ⊆ allowed): ground drops `ballast` from the leading options (pile/concrete led with; ballast reachable via presets); `shapeOptionsFor` — only `concrete` offers `['square','circular']` (ballast precast, pile driven round, anchor is a plate); the containment is test-asserted | structure-view.ts:117-128; tests structure-view:112-124, foundation-clamp:110-128 | working | SC.10-7.25, SC.10-7.26 |
| CODE.step6-structures.34 | `structureEditorState` — inert WITH a reason, never blank cards or a throw (E18): no spec → "Select a panel in Step 4…"; missing segment/roof → "…no longer exists"; flush → "no structure model to edit"; monorail → "mounts on standoffs through the sheet, so there is no foundation to choose"; pitched → "Pitched roofs do not carry the elevated member model yet."; `canEditMembers` only for elevated_table+racking | structure-view.ts:147-191; test structure-view:136-233 | working | NONE |
| CODE.step6-structures.35 | `ruleFor` shape override: only a CAST pedestal honours it; square→circular carries plan size over as diameter (and vice versa — "same size, round shuttering"); ballast/pile/anchor ignore the override rather than producing an unbuildable shape | foundation.ts:63-71; test foundations.test:157-208 ("ballast and pile IGNORE a shape override") | working | SC.10-7.26 |
| CODE.step6-structures.36 | Foundation quantities (ALL ASSUMED, rule config): `foundationVolumeM3` shape-correct (circular = π(d/2)²h — the 21% concrete difference is the point), `foundationDeadLoadKg` = volume × 2400 kg/m³, 0 for pile (founded in ground) and anchor (a plate); default pedestal 300×300×150 ≈ 32 kg so 30 legs ≈ 1 t | foundation.ts:80-96; data/rules/india.ts:501-513; test foundations (28 PASS: "~32 kg, so 30 legs is ~1 tonne") | working | SC.10-7.27 |
| CODE.step6-structures.37 | `foundationAssembly` per-kind parts (leg-local frame, y=0 at deck): concrete = block + 12 mm grout bed + 160 mm plate + 4 cast-in M12 bolts; anchor = plate + 4 drilled bolts, nothing cast; ballast = block + plate, NO bolts (no penetration); pile = one Ø90 cylinder spanning 1.2 m embedment below grade to the plate; `heightM` = plateY + plate thickness feeds D15 | foundation.ts:108-191; test foundations.test:99-155 ("nothing in the assembly dips below the deck except a pile") | working | NONE |
| CODE.step6-structures.38 | `foundationTooTall` / `legAboveFoundationM`: flags (never clamps) a foundation leaving < 0.05 m of buildable steel leg inside the clearance; `foundationKindOfSpec` reads the kind back from the node's own hardware so renderer/DRC/BOM share one answer (§A0) | foundation.ts:99-106,199-208; data/rules/india.ts:503; tests foundations.test:91-97, drc-structure:147-175 ("flagged, not clamped", "the rule config is what decides") | working | SC.10-7.25 |
| CODE.step6-structures.39 | `lib/ground.ts` — one answer to "what is under this point": `resolveAnchorRoofId` (roof id or null = open ground), `groundHeightAt` (position-resolved, grade fallback), `obstructionBaseY` (null roofId = "stands on ground" PRESERVED; a non-null claim is VERIFIED and healed from position when stale — cures pre-sync saves with no migration). Behaviors surfaced through Step 3 / the 3D scene — cross-cited to step3-obstructions rows 43-44 | ground.ts:17-55; Scene3D.tsx:1300 (one hop); test obstruction-grounding (27 PASS incl. "the SHADING caster is grounded identically to the visual mesh (§A0)") | working | NONE (10-4 rows carry the UI; lib inventoried here) |
| CODE.step6-structures.40 | `StructureInstanced`: members drawn with their REAL extruded cross-section, one cached geometry per profileKey bucket, stretched by Y-scale ONLY (a C-channel must not thin its web); legacy profiles without `dims` fall back to a unit box at 0.04 m (brace)/0.06 m half-size; EN→three mapping (x, z, −y); `castShadow` is VISUAL only — members deliberately never join `buildShadowCasters` (documented containment argument: every member sits under its own overhanging module; expires if members ever extend beyond modules). Structure-side note for agent B's SC.10-7.12: "shadows cast by structure" is true visually, false analytically, by design | StructureInstanced.tsx:11-35,44-46,81-146; test live-structure-geometry (5 PASS: "no member extends beyond the array", pre-22a stored profile resolves by KEY) | working | SC.10-7.20 |
| CODE.step6-structures.41 | Member click → `onMemberClick(segmentId, memberId)` with drag guard (`e.delta > 4` ignored); Scene3D opens the struct edit card from it. DISCREPANCY recorded: Scene3D drops the reported memberId (`(segId) => onStructOpen(segId)`) and neither renderer consumer passes `highlightIds`, so the Phase-22n member/node highlight tint (#ffb454) is unreachable dead capability in the app | StructureInstanced.tsx:63-70,137,163-177; StructureNodesInstanced.tsx:39-42,119; Scene3D.tsx:1306-1312 (sole mount, no highlightIds prop; grep-verified no other consumer) | POC-DEFECT(22n BOM↔3D focus tint unwired — no consumer passes highlightIds; Scene3D discards memberId) | NONE (10-10's agent should cross-check any BOM→3D locate row) |
| CODE.step6-structures.42 | `StructureNodesInstanced`: draws what every joint IS — foundation assembly per `roof_anchor` (kind from the node's own fastenerSpec, shape from the structure's resolved `foundationShape` — neither re-derived) + `nodeHardware` for every other node (clamps 32/40 mm castings, joint plates+bolts, 100 mm L-foot standoffs; roof_anchor returns [] there so nothing draws twice); concrete parts get a matte non-metallic material (0x9a9791, roughness .95) vs cloned galvanised steel; ≤6 instanced buckets | StructureNodesInstanced.tsx:44-58,71-127; hardware.ts:24-104 (one hop, s7 file); test foundation-grounding:84-136 ("the pedestal's underside lands exactly on the deck") | working | SC.10-7.25 |
| CODE.step6-structures.43 | Renderer hygiene: per-instance GPU buffers disposed on unmount; profile geometries cached per key and NEVER disposed by renderers (`disposeProfileGeometryCache` for full teardown only); shared panel-leg material CLONED before per-instance colour; `frustumCulled=false` (instances spread beyond geometry bounds) | StructureInstanced.tsx:73-79,143,151-155; StructureNodesInstanced.tsx:60-68,130-135; profile-geometry.ts:193-209 | working | NONE |
| CODE.step6-structures.44 | `profile-geometry`: exact procedural outlines for 7 section shapes (c lipped, u, l, z, hat, rhs, chs Ø48-segment) — `bevelEnabled:false` (a bevel would shrink a section whose mass is quoted), extruded to UNIT length along +Y and centred on its axis (`translate(0,-0.5,0)` — pinned after purlins visibly shot past the array end); `sectionSvgPath` derives the picker glyph from the SAME outline (§A0 — glyph can never show a different section) | profile-geometry.ts:44-176,179-191; test profiles.test (32 PASS) | working | SC.10-7.24 |
| CODE.step6-structures.45 | Section catalog (data/profiles.ts, s4 file — inventoried here as structure's only consumer): 8 profiles — c_channel 2.2 · u_channel 2.4 · l_angle 1.8 · z_purlin 2.6 · rhs 3.4 · chs 3.0 · top_hat 2.01 · c_channel_80 3.73 kg/m; kgPerM audited by shape family (open = developed coil-blank width × t × 7850, hollow = true area — the ~5% bend allowance that makes the heavy 80×40×15×2.5 channel 3.73 not the polygon's 450 mm²); order LOAD-BEARING ([0] is the default at 4 call sites); every profile carries derived `sectionMm` label + IS 2062 grade + HDG ≥80 µm coating | profiles.ts:24-111; test profiles.test (every entry asserted against its mass rule) | working | SC.10-6.48 |
| CODE.step6-structures.46 | `StructEditPanel` open/close lifecycle: opens anchored AT the table (drei `Html`, translate(64%,−10%), 252 px card) on tap of any member or any segmented module (flush tables stay re-elevatable from 3D); commits are select-only — hover preview was trialled and removed by user decision 2026-07-16; Esc closes; outside-close pairs pointerdown/up with 6 px drag tolerance and a `[data-struct-edit-card]` inside-guard (the "steppers close the panel" bug); auto-closes when the segment vanishes (undo/delete in 2D) | StructEditPanel.tsx:1-17,209-237; Scene3D.tsx:189-231,1073-1080,1306-1330 | working | SC.10-7.20 |
| CODE.step6-structures.47 | Every design click commits ONE undoable patch via `applyStructChoice` (`patchProject(r, true)`); VIEW buttons (modules/scope) call `onViewChange` only and stopPropagation on pointerdown — they never touch the project | Scene3D.tsx:189-193; StructEditPanel.tsx:12-17,332-382; test structure-edit:131-137 ("it is ONE patch") | working | SC.10-7.20, SC.10-7.22 |
| CODE.step6-structures.48 | THIS PANEL block (when a module was tapped): access % tinted green >95 / amber >85 / red (same thresholds as the module tint), ≈N kWh/yr share ("Estimated share of the system total"), "Sun lost to — click to look" — top-3 blockers with −loss% that swing the camera via `onFocusBlocker`, or "Nothing blocks this module"; recomputed fresh on open (~250 rays), no persistence/staleness badge needed. Shade math is agent B's `computePanelShadeDetail`/`panelEnergyShares` (cited) | StructEditPanel.tsx:140-152,239-296; Scene3D.tsx:454,1325 | working | SC.10-7.20 |
| CODE.step6-structures.49 | 3D preset row: Flush / Std 10° / Walk 2.2m, each with a live `StructurePreview` section thumbnail (64×38); active detection DERIVED never stored — std = tilt 10 ∧ frontLeg < 1, walk = frontLeg ≥ 2.2, flush = racking kind; no "custom" marker in the 3D card (census puts that on the 2D table settings, where it exists) | StructEditPanel.tsx:163-177,301-327 | working | SC.10-7.21 |
| CODE.step6-structures.50 | MODULES show/ghost/hide + "All tables"/"Isolate this" as accessible radiogroups (role=radio, aria-checked), amber active border; drives `partitionPanels`/`visibleStructureIds` through Scene3D state that is never persisted | StructEditPanel.tsx:329-382; Scene3D.tsx:145,1040-1054,1331-1345 | working | SC.10-7.22, SC.10-7.23 |
| CODE.step6-structures.51 | Profile picker: one button per catalog profile with `SectionGlyph` drawn from the SAME `sectionSvgPath` outline the 3D member extrudes, aria-pressed active state. DISCREPANCY: section size (mm) and kg/m are present ONLY in `title` tooltip and aria-label — census/doc say the card "shows its… section size in mm and its weight per metre", and a title attribute never shows on touch | StructEditPanel.tsx:77-91,386-412; phase-10-prompts.md:1024-1025 | POC-DEFECT(size mm + kg/m not visibly rendered in the 3D card — tooltip/aria only; the 2D card does show kg/m inline) | SC.10-7.24 |
| CODE.step6-structures.52 | Foundation card: options from `foundationOptionsFor` (card hidden entirely when [], e.g. monorail/pitched), labels PCC pedestal · Chemical anchor · Ballast block · Driven pile with per-kind hints; too-tall options (foundationTooTall) render dimmed (opacity .45) with reason title "…taller than this table's clearance". DISCREPANCY: no `disabled` attribute — a dimmed too-tall option STILL COMMITS on click (DRC then errors `foundation_too_tall`; flag-not-clamp is deliberate downstream, but census says "unavailable") | StructEditPanel.tsx:43-55,417-446; census SC.10-7.25 "options taller… unavailable with the reason shown" | POC-DEFECT(too-tall foundation options look unavailable but remain clickable and commit) | SC.10-7.25 |
| CODE.step6-structures.53 | Shuttering form (cast pedestal only): Square ("Plank shuttering — the common rooftop pedestal") / Circular ("Sono-tube / pipe shuttering — ~21% less concrete") radiogroup; only rendered when `shapeOptionsFor` is non-empty | StructEditPanel.tsx:450-479; test structure-edit:169+ (shape reaches the structure) | working | SC.10-7.26 |
| CODE.step6-structures.54 | Foundation honesty note under the card: "Nominal size — assumed, engineer to confirm." + for mass-adding kinds "Adds ~N kg to this roof; roof capacity is not checked." (N = per-foundation dead load × roof_anchor count of THIS table, shape-aware) | StructEditPanel.tsx:57-74,158-161,481-483 | working | SC.10-7.27 |
| CODE.step6-structures.55 | Tilt stepper ±5° clamped 0–35 (buttons null out at the rails, dimmed+disabled); Clearance stepper ±0.3 m clamped 0–3 (minus gated below 0.05 m), value shown at 1 dp with unit | StructEditPanel.tsx:179-207,486-502 | working | SC.10-7.28, SC.10-7.29 |
| CODE.step6-structures.56 | CUSTOMIZE MMS: Purlins/row 1–6 (±1) · Rafter density 1–3× (±0.5) · End overhang 0–1 m (±0.1, unit-formatted via fmtLen) · Bracing Braced/None toggle; each control passes `undefined` at the default so the key is DELETED; closing note "Rafter density is a MATERIAL allowance, not a safety factor — no structural analysis is performed here." (F8-25) | StructEditPanel.tsx:504-587; tests mms-customize (20 PASS: controls reach graph AND BOM kg together, defaults byte-identical) | working | SC.10-7.30 |
| CODE.step6-structures.57 | "Edit legs (2D)" toggle (aria-expanded) embeds `LegPlanEditor` (300×190) in the card; commits a complete already-validated `segments` patch via `onPatch` — still ONE undoable patch (§H); rendered only when resolved+onPatch+fmtLen present; `fmtLen` passed as a PROP because drei `Html` is a separate React root where `useStore()` throws (E19 — the "open button closes the panel" bug) | StructEditPanel.tsx:589-621,127-133; LegPlanEditor.tsx:49-59 | working | SC.10-7.31 |
| CODE.step6-structures.58 | LegPlanEditor canvas: the segment's own LOCAL frame (rotated tables draw square, E3), 1 m grid, dashed buildable region (roof minus per-edge/uniform setbacks — same inset the fill uses), panels in blue, AUTO stations as dimmed (0.34) non-interactive dashed crosshairs ("these are placed for you", not "your legs are gone"), planned legs as focusable crosshair buttons (tabIndex 0, per-leg aria "Leg i of N, X along the table…"), AUTO/CUSTOM·N-legs badge | LegPlanEditor.tsx:72-113,184-313; leg-plan-edit.ts:26-33,85-115; test LegPlanEditor.dom:84-121 ("AUTO shows the automatic stations rather than an empty roof") | working | SC.10-7.31 |
| CODE.step6-structures.59 | Leg-plan ops — every decision in lib, the component decides nothing: Add leg (button places at the panel centroid; FIRST add seeds from `autoSeedPoints`, which mirrors buildStructure's AUTO exactly — footprint run, not centre span, so taking over never makes legs jump); Remove (Delete/Backspace; last removal returns to AUTO "key and all"); Reset to auto (only shown in CUSTOM); drag commits ONE patch on pointerup (nothing mid-gesture — no per-frame undo spam), <1 mm treated as a click; arrows nudge 0.25 m / Shift 0.05 m — keyboard and mouse produce IDENTICAL patches because both call `moveLeg` | leg-plan-edit.ts:17-190,193-207; LegPlanEditor.tsx:131-181,220-237; tests leg-plan-edit (19 PASS: "arrow-nudge and drag produce the SAME patch"), LegPlanEditor.dom:123-165 | working | SC.10-7.31 |
| CODE.step6-structures.60 | E4 leg validation: checked in WORLD space against the inset polygon (a rotated table is not a loophole); refusal `{ok:false, reason:'Outside the roof setback — a leg has to land on buildable roof.'}`; every action AND refusal lands in a `role=status aria-live=polite` announcement channel — never a silent no-op | leg-plan-edit.ts:43-52; LegPlanEditor.tsx:115-123,315-318; tests leg-plan-edit:139-231, LegPlanEditor.dom:167-184 ("announces WHY and commits nothing") | working | SC.10-7.31 |
| CODE.step6-structures.61 | Leg-plan editor gaps recorded (in-row, unresolved): no live ghost mid-drag — the crosshair stays at the committed spot until pointerup (deliberate one-patch design, but zero visual feedback during the gesture); the lib comment "Enter on the plan itself adds a leg at the centre" is stale — no Enter handler exists, addAtCentre is button-only | LegPlanEditor.tsx:139-146,172-181 (absence of position state mid-drag; no onKeyDown on the svg) | uncertain (drag-feedback absence is design-adjacent; census asks draggable, which works) | SC.10-7.31 |
| CODE.step6-structures.62 | Persistence armour: `normalizeProject` drops a malformed persisted `legPlan` (non-finite/empty/wrong shape) by REMOVING the key — table falls back to AUTO, never crashes, layoutFp identical to never-planned | persistence/normalize.ts:46-62 (one hop, s10 file); test leg-plan:210-251 ("a bad plan drops to AUTO, never taking the table with it") | working | NONE |
| CODE.step6-structures.63 | Step6Editor (2D table settings) preset block: Flush / Standard 10° / Walk-under 2.2 m as cards with live `StructurePreview` thumbnails (96×56); header "Structure preset · custom" when none matches (derived: std = tilt 10 ∧ frontLeg < 1, walk = frontLeg ≥ 2.2); commits via the same `applyStructChoice` | Step6Editor.tsx:666-669,1992-2090 | working | SC.10-6.42 |
| CODE.step6-structures.64 | Step6Editor ground branch: a ground-surface table shows ONLY "Foundation" preset cards Driven pile / Ballasted (active = resolved foundation) + soil note "Embedment depth and pull-out capacity depend on the soil — a site survey and engineer sign-off are required."; rooftop presets hidden on ground and vice versa. In-row notes: both thumbnails render the identical member section (preview draws members only, tilt overridden to 20°, leg heights not recomputed); the " · custom" suffix logic is rooftop-based so a fresh ground preset (tilt 20) always reads "Foundation · custom" | Step6Editor.tsx:2040-2073; test ground-mount (22 PASS) | working (two cosmetic notes recorded) | SC.10-6.43 |
| CODE.step6-structures.65 | Step6Editor racking triple Flush / Fixed tilt / Dual tilt → `applyRacking` → s2's `setSegmentRacking`; dual_tilt builds fixed-tilt topology with the honesty warning (row 17) | Step6Editor.tsx:2093-2101; segment-ops.ts (one hop, s2) | working | SC.10-6.44 |
| CODE.step6-structures.66 | Step6Editor profile row: one button per catalog profile showing label + kg/m inline (the census wording "weight per metre" is met HERE, unlike the 3D card) | Step6Editor.tsx:2170-2183 | working | SC.10-6.48 |
| CODE.step6-structures.67 | Step6Editor "Structure (member model)" card: live `StructurePreview` (120×80) + counted bill — Legs (front+back combined) / Rafters / Purlins / Braces each as "count · N.N m", Steel ({profile label}) total kg; Leg spacing number input clamped 0.5–4 m; Clearance clamped 0–3 m (clearance edits re-run `reconcileBridgedPanels` in the same patch); `validateStructure` first issue "+N more" in red; builder warnings in amber; STRUCTURE_DISCLAIMER footer. Input quirk recorded: emptied fields coerce (`|| 2` spacing, `|| 0` clearance) and commit immediately | Step6Editor.tsx:655-663,2185-2256 | working | SC.10-6.49, SC.10-6.50 |
| CODE.step6-structures.68 | Step6Editor Anchored / Ballasted buttons write `foundation` raw with NO `allowedFoundations` gate: on a GROUND table "Anchored" writes `anchor`, which the read-time clamp silently corrects to `pile` — the button can never show active and clicking it does nothing visible; on a tilted METAL-SHED table "Ballasted" writes `ballast`, clamped to `anchor` — same dead option. (On RCC flat both work; census pins exactly this anchored/ballasted pair, so the offer is census-conformant — the defect is the ungated dead options on ground/shed) | Step6Editor.tsx:2237-2243; structure.ts:193-199,762-778 | POC-DEFECT(2D foundation toggle offers kinds the surface rejects; the write is silently corrected at read time with no explanation) | SC.10-6.49 |
| CODE.step6-structures.69 | `StructurePreview`: side-elevation SVG PROJECTED FROM THE REAL MEMBER GRAPH (synthetic 1-panel segment at origin through `buildStructure` — never a static picture that can drift); legs grey 2.5px, rafters blue 4px, purlins as section dots, roof line, clearance callout "N.Nm"; south/down-tilt edge on the LEFT (drafting convention — an earlier mirror bug is documented and fixed in-file); flush renders an honest flat glyph; zero WebGL cost | StructurePreview.tsx:14-68,88-163 | working | SC.10-6.42, SC.10-7.21 |
| CODE.step6-structures.70 | Structure DRC (in s7's drc.ts; structure side inventoried here): (1) `foundation_dead_load` WARN per roof — "Foundations add ~N kg… roof capacity is NOT checked. Have a structural engineer confirm…" — skips ground (no slab) and zero-mass kinds, shape-aware; (2) `foundation_clash` ERROR — foundation FOOTPRINT (not point) intersecting a no-build zone/walkway/obstruction: "…A panel may span above an obstruction; a footing cannot be cast in one."; (3) `foundation_too_tall` ERROR — flag, never clamp. Fed into Step6Editor's issue list | drc.ts:236-340; Step6Editor.tsx:336; test drc-structure (14 PASS) | working | NONE (beyond the 10-6/10-7 census rows) |
| CODE.step6-structures.71 | Structure reaches the drawings: the DXF export declares structural layers and draws one setting-out cross + one TRUE-footprint footing per leg base (square pedestal = closed polyline, circular = CIRCLE at rule radius, anchored plate still marked "so there is something to set out"); legs are points in plan, not members. Surface is Step 8's (s8/s2 screens); the graph source is this module's | export-dxf.ts + StructureSheet.tsx (one hop); test dxf-structure (14 PASS) | working | NONE (10-9 census rows carry the drawing surface) |
| CODE.step6-structures.72 | Ground-mount table = the same elevated_table model standing at grade: ground surface has heightM 0/no parapet (s2's makeGroundSurface), pile assembly reaches 1.2 m below grade, ground arrays are exempt from the dead-load DRC, BOM prints "Ground Mount Structure" never the rooftop-RCC line and flags the foundation ASSUMED/site-dependent (s8's emitters) | ground-mount.test (22 PASS: "founds on a driven pile, not a chemical anchor into soil", "flags the foundation as assumed"); drc-structure:91-97 | working | SC.10-6.43 |

## CENSUS-UNMATCHED (agent C's half of Appendix A §10-6/§10-7: structure/ground rows with no code behavior)

None. All 18 owned entries matched: SC.10-6.42 (rows 27,63,69) · .43 (28,64,72) · .44 (5,17,65) · .48 (45,66) · .49 (6,7,12,13,67,68) · .50 (23,25,67) · SC.10-7.20 (26,40,46-48) · .21 (49,69) · .22 (31,32,47,50) · .23 (31,50) · .24 (44,51) · .25 (4,9,38,42,52) · .26 (33,35,53) · .27 (36,54) · .28 (29,55) · .29 (29,55) · .30 (14,30,56) · .31 (16,57-61).

- rowCount: 72
- defectCount: 4 (row 41 22n highlight tint unwired · row 51 3D profile card hides mm/kg-per-m in tooltip only · row 52 too-tall foundation options clickable despite census "unavailable" · row 68 2D anchored/ballasted ungated by surface — dead, silently-corrected options on ground/shed)
- censusUnmatched: 0 of 18 (my half; split assumption recorded above — SC.10-6.44 claimed here, SC.10-6.41/.45/.46/.47/.51/.52 left to agent A, SC.10-7.01–19/32–43 to agent B)
- newBeyondCensus: 25 (rows 1-3, 8-11, 15, 18-22, 24, 34, 37, 39, 41, 43, 61, 62, 70-72 — resolution chain/clamp, determinism+fingerprint armour, monorail model, wind-zone flag, hardware/foundation assemblies, grounding lib, renderer internals, normalization, structure DRC, DXF, ground BOM side)
- notes (≤10 lines):
  1. 398/398 tests green across 21 files — no failing-test DATA to report.
  2. F8-25 holds everywhere inspected: no wind/uplift/roof-capacity computation exists; every assumed number (foundation sizes, purlin/rib pitch, ground clearance) is labelled ASSUMED in code and UI, and the disclaimer travels to BOM/CSV/SLD/drawing/proposal (rows 23-25).
  3. The two clickability defects (rows 52, 68) share one root: the 3D picker is gated by foundationOptionsFor/allowedFoundations, the 2D card and the too-tall dimming are not.
  4. `lib/ground.ts` is grounding (surface height), not earthing — the electrical earthing lib is s7's; obstruction-grounding overlap cross-cited to step3-obstructions rows 43-44.
  5. Hover preview for struct choices was deliberately removed (user decision 2026-07-16, in-file); `applyStructChoice` purity is still test-pinned, so preview could be re-enabled without drift.
  6. `hardware.ts` (s7 file) and `data/profiles.ts` (s4 file) behaviors are inventoried here because my renderers/model are their only consumers — flagged for the file-claim reconciler.
