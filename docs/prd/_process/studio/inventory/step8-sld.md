# Feature inventory — step8-sld (Step 8 · SLD & the electrical engine)

Pass-two inventory, 2026-08-05. Primary files, all under `3d_design_studio/src/features/solar-studio/`: `screens/Step8Sld.tsx` (934 ln) · `lib/sld.ts` (105) · `lib/electrical/` all six — `autostring.ts` (292), `combiner.ts` (69), `gate.ts` (57), `grouping.ts` (340), `temps.ts` (96), `window.ts` (74) · `lib/electrical-sizing.ts` (162) · `lib/stringing.ts` (165) · `lib/routing.ts` (530) · `lib/drc.ts` (340) · `lib/cascade.ts` (87) · `lib/hardware.ts` (104).

**Test run** (read-only, vitest 3.2.7): `npx vitest run` over the 12 claimed test files → **12 files, 171 tests, ALL PASS** (778 ms): `routing` 34 · `ac-cable-sizing` 26 · `autostring` 25 · `grouping-plane` 15 · `drc-structure` 14 · `electrical-temps` 13 · `drc` 12 · `electrical-sizing` 9 · `stringing` 7 · `electrical-gate` 7 · `mlpe` 5 · `cascade` 4. **No failing test ⇒ no test-derived DATA finding.** All numeric ladders/factors below verified against `data/rules/india.ts` (the market rule pack, read as evidence, owned per its consumers).

**One-hop files consulted but owned elsewhere** (cited, never re-inventoried): `components/drawing/index.tsx` (`Sheet`/`TitleBlock`) and `components/drawing/StructureSheet.tsx` — structure drawings, sitting 6 (structures) · `lib/structure.ts` (`engineeringStatus`, `STRUCTURE_DISCLAIMER`, `windZoneInfo`, `resolveRacking`) — sitting 6 · `lib/layout.ts` (`panelCornersOnRoof`, `roofGridAngle`) — sitting 5 · `lib/export-dxf.ts` (`layoutToDxf`) — export path, sitting 5/10 per claims · `lib/solar.ts` `computeEnergyReport` — sitting 6 (step7) · `screens/Wizard.tsx` (`nextBlocker`, `allowedStep`) — sitting 10, cited here because it is the gate's one consumer · `screens/Step6Editor.tsx` (auto-string / route buttons, issues banner) — sittings 2/5 · `lib/bom/*` + `lib/health.ts` (consume sizing/DRC/combiner) — sitting 8 · `lib/capabilities.ts` + `lib/foundation.ts` (obstruction capabilities, foundation geometry) — sittings 3/6 · catalog specs (`PanelSpec`/`InverterSpec` fields incl. the single `mppt.maxCurrentA`) — sitting 4.

Census target: `docs/prd/modules/M05-design-studio.md` Appendix A §A.10-9, `SC.10-9.01`–`SC.10-9.37` (37 entries); baseline rows M05-62…M05-67.

---

## §A — Step shell, tabs & header controls (`Step8Sld.tsx`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.1 | Four drawing tabs in fixed order — **SLD · PV Layout · String Route · Structure** — as `chip` buttons with `aria-pressed`, icons Zap/Grid3x3/Cable/Grid3x3; switching swaps the sheet inside one scrollable host. SLD and String Route render only when `project.strings.length > 0`, else the shared `UnstrungState`; PV Layout and Structure render regardless | Step8Sld.tsx:19,32,84-100,210-215 | working | SC.10-9.01 |
| CODE.step8-sld.2 | The sheet host is a plain `overflow:auto` div (`background #f0f1f3, padding 18`) and each sheet an SVG at `width:100%, minWidth:900`. The header pill reads **"NTS · A2 Landscape · Scroll to zoom"** — but no zoom handler exists anywhere on the screen: scrolling scrolls (pans), nothing zooms. The census's "zoom and pan within its own area" is half-implemented | Step8Sld.tsx:155-157,205-216; components/drawing/index.tsx:42-50 (one hop — no zoom there either) | POC-DEFECT(header advertises "Scroll to zoom"; no zoom is implemented — pan-by-scroll only) | SC.10-9.02 |
| CODE.step8-sld.3 | SLD parameters are **derived live on every render** — `effectiveSld(project)!` (derive-from-components + user overrides merged on top), replacing the old frozen `sldParams` snapshot. The non-null `!` is safe only because the wizard's `allowedStep` clamp makes Step 8 unreachable without an inverter (Step 4 gate) | Step8Sld.tsx:32-37; sld.ts:62-66; Wizard.tsx:118-121,163-171 (one hop) | working | SC.10-9.18 (substrate) |
| CODE.step8-sld.4 | First-visit explainer dialog "Single Line Diagram" ("Required for DISCOM approval") with the four colour-legend rows — DC Side #b91c1c · Inverter #1d4ed8 · AC Side #047857 · Earthing #b45309 — shown while `!project.derived.sldIntroSeen`; "Got it" patches `sldIntroSeen: true` (persists, never shows again) | Step8Sld.tsx:25,218-246,332-344 | working | SC.10-9.37 |
| CODE.step8-sld.5 | **Edit Ratings** chip appears on the SLD tab only; when overrides exist it carries a violet (#7c3aed) count badge with tooltip "N rating(s) manually edited — the rest stay auto-derived". Count = `Object.keys(project.derived.sldOverrides ?? {}).length` | Step8Sld.tsx:37,101-121 | working | SC.10-9.18 |
| CODE.step8-sld.6 | **Structural verification** is a two-chip toggle — `pending` ("Pending verification", ShieldAlert) ⇄ `engineer_approved` ("Engineer approved", ShieldCheck) — patching `project.structuralVerification.status` while preserving `notes`; default state `pending`. Tooltip states the law: "Engineering sign-off is recorded by a HUMAN engineer — the app never calculates structural adequacy" | Step8Sld.tsx:123-146 | working | SC.10-9.30 |
| CODE.step8-sld.7 | **HIGH WIND** badge shown when `windZoneInfo(project.info.state).high` (state's IS 875-3 representative basic wind speed ≥ 47 m/s per the rules pack), tooltip = the zone label; display-only, and the same label is appended to every sheet's disclaimer footnote | Step8Sld.tsx:147-154; india.ts:471-486; lib/structure.ts windZoneInfo (one hop) | working | SC.10-9.31 |
| CODE.step8-sld.8 | **3-line** toggle (SLD tab only, `aria-pressed`, tooltip "Show every conductor (L/N/PE)… required by some DISCOMs") flips local state `threeLine` — presentation only, same derived model | Step8Sld.tsx:28-29,158-167 | working | SC.10-9.17 |
| CODE.step8-sld.9 | **SVG (CAD) export**: serialises the currently visible sheet's `<svg>` via `XMLSerializer`, downloads as `{tab}-{project name}.svg` (`image/svg+xml`) | Step8Sld.tsx:39-45,168-170,346-351 | working | SC.10-9.32 |
| CODE.step8-sld.10 | **PNG export**: rasterises the same SVG through an `Image` + canvas at **2× the image's natural size**, white background fill, `canvas.toBlob` → `{tab}-{project name}.png`. Silent no-op if the SVG blob fails to load (no error surface) | Step8Sld.tsx:46-68,171-173 | working | SC.10-9.33 |
| CODE.step8-sld.11 | **DXF (layout) export**: `layoutToDxf(project)` → `dxfFileName(project)` download, tooltip "Array layout as a true-metre DXF (layered: roofs, modules, obstructions, strings) — opens in any CAD". Always exports the LAYOUT regardless of the active tab (SLD itself has no DXF form) | Step8Sld.tsx:174-182; lib/export-dxf.ts (one hop) | working | SC.10-9.34 |
| CODE.step8-sld.12 | **Print / Save-as-PDF** path is a scoped `@media print` stylesheet: `@page A3 landscape margin 8mm`, everything hidden except `[data-sheet-host]`, sheet forced white/full-width. Deliberately browser-print, no PDF library ("zero dependency, and the ₹ glyph renders correctly") | Step8Sld.tsx:186-204 | working | SC.10-9.35 |
| CODE.step8-sld.13 | Paper-size labels disagree across the surface: header pill "**A2** Landscape" (row 2), SLD title block "SLD-01 · NTS · **A2**", String Route title block "2 OF 2 · NTS · **A1**", print CSS "**A3** landscape", and the Sheet component's own comment "1 unit ≈ 1 mm at **A3**". Whatever paper the drawing actually lands on, at least two of its own labels are wrong | Step8Sld.tsx:156,592,788,193; drawing/index.tsx:14 | POC-DEFECT(four contradictory paper-size claims on one drawing set) | SC.10-9.02 (divergence) |

## §B — Empty state (nothing strung)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.14 | `UnstrungState` (shown for SLD + String Route tabs when `strings.length === 0`): card "No strings configured yet", copy explaining the SLD is drawn from the real string configuration, two ways out — **Auto-string now** (primary) and **String manually in the editor** (→ `navigate('/wizard/6')`). The refusal to draw is deliberate: "A drawing fabricated from unstrung panels would show an electrically impossible configuration" | Step8Sld.tsx:210,212,270-330 | working | SC.10-9.36, SC.10-9.14 |
| CODE.step8-sld.15 | Auto-string is disabled until `panel && inverter && enabledCount > 0`, with the reason printed underneath: "Select a panel and inverter (Step 4) and place at least one panel (Step 6) first." | Step8Sld.tsx:279-281,302-306,323-327 | working | SC.10-9.36 |
| CODE.step8-sld.16 | Step 8's Auto-string calls the **LEGACY `autoString` shim**, which wraps the real planner in a **fabricated project** `{ panels, segments: [], roofs: [] }` — so (a) co-planar face merging is impossible (`computePlaneIds` gets no roofs; every `roofId` is its own plane), (b) MLPE mode is lost (`components` is undefined ⇒ `mlpe = 'none'`), (c) serpentine falls back to group azimuth instead of the roof-grid frame, and (d) the plan's `issues` and `unstrungPanelIds` are **discarded** — refusals are silent here. Step 6's own button uses the full `autoStringPlan(project, …)` and surfaces every issue. Same click, two different stringers | Step8Sld.tsx:306-315; stringing.ts:22-42; grouping.ts:222-243,296-315; Step6Editor.tsx:920 (one hop) | POC-DEFECT(Step 8's auto-string runs the degraded shim and swallows refusal issues; unstrung leftovers surface only later via validateSystem/the gate) | SC.10-9.36 (divergence) |
| CODE.step8-sld.17 | Auto-string commits `patch({ strings }, true)` — a single undoable patch (the strings arrive atomically; undo removes them all) | Step8Sld.tsx:314 | working | SC.10-9.36 |

## §C — The SLD sheet itself (duty d: symbols, layout, title block, drawn vs assumed)

All coordinates are in the fixed 980×640 `Sheet` viewBox; the whole diagram is a parametric SVG in monospace, no drawing engine.

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.18 | Sheet primitive (`Sheet`): `viewBox 0 0 980 640`, white ground, 6-unit inner border rect at stroke 1.4 — shared with all four tabs (Phase 22o extraction, gated byte-identical to the old inline markup) | drawing/index.tsx:14-51 (one hop) | working | SC.10-9.02 |
| CODE.step8-sld.19 | Sheet heading: `{PROJECT NAME uppercased} — {capacityKwp} kWp SOLAR PV SYSTEM · SINGLE LINE DIAGRAM`, centred at y=30; capacity from `computeEnergyReport(project)` (live, same model as Step 7) | Step8Sld.tsx:382,390-392 | working | SC.10-9.02 |
| CODE.step8-sld.20 | **Every real string is drawn, only real strings are drawn** (`project.strings`; the caller guarantees ≥1). Each string row: exactly **3 module symbols** (28×28 rects with a diagonal, DC red #b91c1c) as shorthand regardless of actual count, plus label `{name} · {n} × {watt}Wp · {round(n × vocV)}V`. Row pitch adapts: `y = 120 + i·min(90, 380/nStrings)` so any string count fits the sheet | Step8Sld.tsx:378-419 | working — note: the per-string voltage label is **STC Voc** (`n × spec.vocV`), unqualified; only the compliance box (row 23) is temperature-corrected | SC.10-9.03 |
| CODE.step8-sld.21 | Per-string DC conductor is drawn to the collection bus with the label `{sld.dcCableSizeMm2} mm² Cu` (hidden in central topology where the SCB block carries the ratings); bus x = 252 (DCDB) or 188 (SCB) | Step8Sld.tsx:374,409-416 | working | SC.10-9.03 |
| CODE.step8-sld.22 | **String Combiner Boxes** drawn ONLY when `inverterTopology === 'central'` AND `combinerPlan(...).ok` — one block reading `SCB × {nBoxes}`, `≤{first box inputCount}-in`, `Fuse {stringFuseA}A/str`, `{totalStrings} strings`. Drawn strictly from the combiner model, "never invented"; a not-ok plan silently falls back to the plain DCDB drawing | Step8Sld.tsx:369-374,421-439 | working | SC.10-9.04 |
| CODE.step8-sld.23 | **MAXIMUM SYSTEM VOLTAGE compliance box** over the DC bus: green (#f0fdf4/#16a34a) when within, red (#fef2f2/#dc2626) when over; text `MAX SYSTEM VOLTAGE {maxSystemVdc} Vdc ≤/> {inverterMaxDcV} V` plus the basis line `{maxStringLength}-module string · cold Voc @ {minAmbientC}°C (assumed)` and, when over, ` · OVER LIMIT — shorten the string`. Provenance suffix "(assumed)" appears whenever `temps.source === 'assumed'` (today: always — row 56) | Step8Sld.tsx:376-377,441-464 | working | SC.10-9.16 |
| CODE.step8-sld.24 | `maxSystemVdc` = `round(vocAt(panel, temps.minCellC) × longest real string length)` — the **longest actual string**, never an all-panels fiction; 0 when unstrung. `voltageWithinLimit` is **strictly `<`** ("equal is already a design-margin problem"), and vacuously true at 0 | sld.ts:26-39; electrical-temps.test.ts ("cold Voc rises above STC — hand-check 49.5·(1+0.0027·20) @5 °C") | working | SC.10-9.16 |
| CODE.step8-sld.25 | **DCDB block** (110×70 black-stroke box): `Fuse {dcFuseA}A / str` · `SPD {dcSpdType}` · `Isolator {dcIsolatorA}A`, DC-red feed onward to the inverter | Step8Sld.tsx:466-474 | working | SC.10-9.05 |
| CODE.step8-sld.26 | **Inverter block** (blue #eff6ff/#1d4ed8): "INVERTER", label truncated to 24 chars, `{acRatingKw} kW · {phases}φ`, `{mppt.count} MPPT · {mppt.minV}–{mppt.maxV}V`, and `× {inverterCount} units` when >1 | Step8Sld.tsx:476-487 | working | SC.10-9.06 |
| CODE.step8-sld.27 | AC run: single-line = one green (#047857) conductor labelled `{acCableSizeMm2} mm² {acCableType}`; **3-line mode** draws every conductor individually — labels from `acConductorLabels(phases)`: 3φ → `L1 L2 L3 N PE` (5), 1φ → `L N PE` (3), PE always present and drawn in earth-brown #b45309 — with the core count printed (`{n}-core`). The conductor rule lives in lib/sld.ts so it cannot drift from the inverter spec | Step8Sld.tsx:375,488-510; sld.ts:88-105 | working | SC.10-9.17 |
| CODE.step8-sld.28 | **ACDB block**: `MCCB {mccbA}A` · `SPD {acSpdType}` · `Isolator {acIsolatorA}A` | Step8Sld.tsx:513-521 | working | SC.10-9.07 |
| CODE.step8-sld.29 | **Meters + grid**: circle "GM" (GEN METER) → circle "NM" (NET METER) → triangle grid symbol labelled "GRID", in series on the AC run | Step8Sld.tsx:523-536 | working | SC.10-9.08 |
| CODE.step8-sld.30 | **Earthing**: three IEC ground symbols (3 shrinking bars) dropped from DCDB, inverter and ACDB, labelled `EARTHING — DC / INVERTER / AC (3 pits, {standard})`. The **"3 pits" is hardcoded**; the BOM derives pits from the rules pack as `pitsForSystem (2) + (arresters present ? pitsForLps (1) : 0)` — so a no-arrester design prints **3 pits on the SLD and quotes 2 in the BOM** | Step8Sld.tsx:538-553; india.ts:372-378; lib/bom/emitters/safety.ts:80-98 (one hop, sitting 8) | POC-DEFECT(SLD earth-pit count hardcoded at 3; disagrees with the BOM's rules-derived 2 when no lightning arrester exists) | SC.10-9.09 |
| CODE.step8-sld.31 | **STRING / MPPT SCHEDULE** table, height grows with string count: per row `{name} → INV {i+1} · MPPT {m+1} · {n} panels · {round(n × vocV)}V · {impA}A` — again STC values (V at STC Voc, current as module Imp) | Step8Sld.tsx:555-566 | working | SC.10-9.10 |
| CODE.step8-sld.32 | **PLANT DETAILS** table, six lines: DC kWp · AC kW · `{count} × {watt}Wp {tech}` modules · `Annual Gen {annualMwh} MWh · PR {performanceRatio}%` · `DISCOM {name} ({3φ/1φ from connectionType})` · `Standard {sld.standard}` | Step8Sld.tsx:568-583 | working | SC.10-9.11 |
| CODE.step8-sld.33 | SLD title block (own inline table, not the shared `TitleBlock`): PROJECT / CLIENT / DRAWING "SINGLE LINE DIAGRAM" / SHEET "SLD-01 · NTS · A2" / DATE / BY "Solar Studio (auto)". **DATE = `new Date()` at render time** — a re-opened drawing silently re-dates itself; there is no stored issue date | Step8Sld.tsx:585-601 | working (recorded: render-date, not issue-date) | SC.10-9.02 |
| CODE.step8-sld.34 | The **structural disclaimer footnote travels on every sheet**: `STRUCTURE_DISCLAIMER + engineeringStatus(project).label` + the high-wind zone label when applicable, at y=632 on all four tabs (SLD, Layout, String Route via `drawingFootnote`; Structure sheet via its own component) | Step8Sld.tsx:355-363,602-604,731-733,793-795 | working | SC.10-9.12 |
| CODE.step8-sld.35 | `deriveSldDefaults` AC cable size is the **fixed pair `phases === 3 ? 10 : 6` mm²** — exactly the constant `lib/electrical-sizing.ts` was written to kill ("every three-phase system past roughly 30 kW was quoted a cable that could not carry its own breaker"). The BOM's ACDB cable line uses `sizeAcCable` (ampacity + voltage-drop); the SLD sheet still prints the constant. The two documents can state different AC conductor sizes for the same system | sld.ts:49; electrical-sizing.ts:59-81,140-162; lib/bom/emitters/electrical.ts:46 (one hop) | POC-DEFECT(SLD AC-cable default is the legacy 10/6 constant while the BOM sizes properly — cross-document rating divergence above ~30 kW 3φ / ~7 kW 1φ long runs) | SC.10-9.24 (divergence) |

## §D — PV Layout & String Route sheets

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.36 | Shared plan transform (`useLayoutTransform`): bounding box of all roof polygons +3 m margin, scaled to fit 560×420, y-flipped (plan north-up); memoised on `project.roofs`. Null (blank sheet) when no roofs | Step8Sld.tsx:611-629 | working | SC.10-9.13 |
| CODE.step8-sld.37 | Layout heading claims a numeric scale — `SCALE 1:{round(100/scale)·10}` — while the header pill and title blocks say NTS. The figure is only meaningful at one particular on-screen rendering width; exported SVG/PNG/print sizes change it silently | Step8Sld.tsx:640-642 | POC-DEFECT(printed scale claim is not tied to any physical output size; contradicts the sheet's own NTS labels) | SC.10-9.13 (divergence) |
| CODE.step8-sld.38 | Roofs drawn as outlines with **every edge dimensioned** (`{len.toFixed(2)}m` at each edge midpoint, DC-red); every enabled panel drawn from `panelCornersOnRoof` — the same canonical footprint DRC and the editor use (blue fill #dbeafe / stroke #1d4ed8) | Step8Sld.tsx:644-673; lib/layout.ts (one hop) | working | SC.10-9.13 |
| CODE.step8-sld.39 | Obstructions are drawn as dashed circles `r = max(4, diameterM/2 · scale)` with their label — **for every obstruction, including rectangular ones** (`lengthM`/`widthM` ignored; `diameterM` is the circle-only field, populated with whatever default the rect carries). A 6×2 m water tank renders at its default circle diameter | Step8Sld.tsx:675-684 vs routing.ts:86 (the same file's correct shape switch); types.ts:211-213 | POC-DEFECT(rect obstructions plotted as circles from the wrong field — footprint on the layout drawing is wrong) | SC.10-9.13 (divergence) |
| CODE.step8-sld.40 | North arrow (circle + solid pointer + "N") at fixed (640, 90) — orientation fixed to plan-north, correct because the transform is north-up | Step8Sld.tsx:686-691 | working | SC.10-9.13 |
| CODE.step8-sld.41 | LEGEND box lists six symbols — PV Module, Roof boundary, Obstruction + buffer, **Walkway, Safety rail, Lightning arrester** — but the sheet never draws walkways, rails or arresters (no render path exists for them on this sheet). Half the legend advertises symbols that cannot appear | Step8Sld.tsx:693-707 vs 644-691 (the full draw list) | POC-DEFECT(legend lists three entity types the sheet never renders) | SC.10-9.13 (divergence) |
| CODE.step8-sld.42 | "PV TABLE SECTION (**10° TILT**)" detail is a **fully static drawing** with the caption "HDG steel legs · chemical anchor" — hardcoded tilt, hardcoded foundation type, regardless of the design's actual segment tilts or chosen foundation (pedestal/ballast/anchor/pile all print "chemical anchor") | Step8Sld.tsx:709-718 | POC-DEFECT(static detail presents assumed tilt/foundation as if from the design) | SC.10-9.13 (divergence) |
| CODE.step8-sld.43 | Layout sheet numbering says "SHEET 1 OF 2" (heading) / DRAWING "PV ARRAY LAYOUT · 1 OF 2"; String Route says "SHEET 2 OF 2". The drawing set actually has four sheets (SLD "SLD-01", Structure unnumbered) — the of-2 pairing predates the other tabs | Step8Sld.tsx:641,728,747,788 | POC-DEFECT(sheet numbering inconsistent with the four-sheet set) | SC.10-9.02 (divergence) |
| CODE.step8-sld.44 | Layout title block (shared `TitleBlock`): PROJECT / CLIENT / SITE (address, 30 chars) / MODULE (`{brand} {watt}Wp × {count}`) / CAPACITY / DRAWING | Step8Sld.tsx:720-730 | working | SC.10-9.13 |
| CODE.step8-sld.45 | **String Route sheet**: roof outlines + each string's series path drawn as a dashed polyline through its panel centres in the string's own colour, name labelled at the first panel; STRING SCHEDULE box (`{name}: {n} modules → INV{i}/MPPT{m}` per row, colour-matched); title block + disclaimer. Note: this draws the SERIES WALK, not the routed home-run cables (`cableRoutes` are Step 6's overlay and the BOM's input — they are not printed on any sheet) | Step8Sld.tsx:738-798 | working — recorded gap: routed home runs exist in the model but no drawing prints them | SC.10-9.14 |
| CODE.step8-sld.46 | The in-sheet fallback "No strings — run Auto String in the editor (Step 6)" at StringSheet:769-773 is **dead code**: the tab renders `UnstrungState` instead whenever `strings.length === 0` | Step8Sld.tsx:212 vs 769-773 | working (dead branch, recorded) | SC.10-9.14 |
| CODE.step8-sld.47 | **Structure tab** renders `<StructureSheet project={project}/>` — the Phase 22o printable structural drawing (owned by sitting 6/structures; behavior not re-inventoried here). "The structure has been modelled since 22a and priced throughout, and nothing printed it until now" | Step8Sld.tsx:213-215; components/drawing/StructureSheet.tsx (one hop) | working | SC.10-9.15 |

## §E — SLD parameter layer (`lib/sld.ts`) & the Edit-Ratings dialog

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.48 | `deriveSldDefaults(project)` is pure, Class-S derived data: returns null without an inverter; otherwise the full `SldParams` — `acRatingKw = inv.acKw × inverterCount`, `mccbA = acBreakerA(acKw, phases)` (the SAME function the BOM uses — "the sheet and the quote can never print two different ratings"), `acIsolatorA = mccbA`, DC values from the selected module (`dcCableSizeMm2/dcFuseA/dcIsolatorA`, fallbacks 4 mm²/20 A/32 A when no panel), SPDs `'Type-II'` both sides, `acCableType 'PVC Cu'`, `standard 'IS/IEC 62548 · CEA (India)'` | sld.ts:18-56; electrical-sizing.test.ts "the SLD sheet and the BOM ACDB line print the SAME rating" | working | SC.10-9.18 (substrate) |
| CODE.step8-sld.49 | Override model: `effectiveSld = derived ⊕ project.derived.sldOverrides` — the mergedBom pattern. `diffSldOverrides(stored, derived)` keeps ONLY fields differing from fresh defaults (null when none), so untouched fields keep tracking component changes forever; also used by the one-time legacy `sldParams` migration | sld.ts:58-86 | working | SC.10-9.18, SC.10-9.29 |
| CODE.step8-sld.50 | Edit-Ratings dialog: form seeded from the effective params; live `editedCount` = fields differing from freshly-derived defaults; **Reset to auto** (disabled at 0 edits, tooltip "Discard all manual edits…") sets the form to derived; **Cancel** closes; **Save** persists `diffSldOverrides(form, derived)` only | Step8Sld.tsx:248-265,806-849 | working | SC.10-9.29 |
| CODE.step8-sld.51 | Field inventory, exactly as censused: Inverter name/model (text) + AC rating kW (number) · DC cable mm² `[2.5, 4, 6, 10]` · DC fuse A `[15, 20, 25, 32]` · DC SPD `[Type-I, Type-II, Type I+II]` · DC isolator A `[25, 32, 40, 63]` · AC cable mm² `[4, 6, 10, 16, 25, 35, 50, 95]` · AC cable type `[PVC Cu, XLPE Cu, XLPE Al]` · MCCB A from the **full rules breaker ladder** (selection also sets `acIsolatorA`) · AC SPD · Standard `[IS/IEC 62548 · CEA (India), IEC 60364-7-712, NEC 690 (US)]` | Step8Sld.tsx:851-931 | working | SC.10-9.19–SC.10-9.28 |
| CODE.step8-sld.52 | Only the MCCB select honours the "derived value must always be representable" rule (full ladder from `resolveRules()`). The other selects are hardcoded subsets that the DERIVATION can escape: DC fuse ladder emits 10/12/30/40/50/63 A (dialog offers 15–32 only — an 18.5 A-Isc module derives 30 A, unrepresentable), DC isolator ladder emits 16/80/100/125 A (dialog 25–63), AC cable sizing emits 70/120/150/185/240/300/400 mm² for large systems (dialog tops at 95). React renders such a select with no matching option — the shown value lies | Step8Sld.tsx:866-895,904-915 vs india.ts:381-411 | POC-DEFECT(non-MCCB dialog ladders are subsets; legally derived ratings can be unrepresentable/undisplayable in the edit form) | SC.10-9.20–SC.10-9.24 (divergence) |
| CODE.step8-sld.53 | The three standards options are **hardcoded strings in the dialog**, not read from any market/rules pack. M05-65's overlay expects "grid & standards family from the market pack's engineering-standards labels" — the POC has no such pack surface; `standard` is just a free string on SldParams | Step8Sld.tsx:923-931; india.ts (no standards-label field exists) | working (recorded divergence vs M05-65 overlay/F1-20) | SC.10-9.28 (note) |

## §F — Design temperatures & the string window (special duty a: the string-sizing math)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.54 | `resolveDesignTemps(project)`: picks the first latitude band with `|lat| ≤ maxAbsLat` from the rules pack (lat defaults to **20** when the project has no location — no crash). Bands (design min/max ambient °C): ≤15° "deep south" **15/40** · ≤20° "Deccan" **8/42** · ≤26° "central" **4/45** · ≤32° "north plains" **0/47** · ≤90° "himalayan" **−10/38** | temps.ts:49-68; india.ts:351-358; electrical-temps.test.ts (Pune 18.5°⇒Deccan, Delhi 28.6°⇒0 °C, unlocated resolves) | working | NONE |
| CODE.step8-sld.55 | The two design cell temperatures: **hot** `maxCellC = maxAmbientC + cellRiseC (30)` — NOCT-style full-irradiance adder; **cold** `minCellC = minAmbientC` — ambient, deliberately NOT a warmed cell ("worst-case Voc happens at first light… adding a cell rise here would quietly shrink the safety margin") | temps.ts:57-67; india.ts:359; electrical-temps.test.ts "cold Voc is checked at AMBIENT, not at a warmed cell" | working | NONE |
| CODE.step8-sld.56 | Provenance travels: `source: 'measured' | 'assumed'` + a plain-language `note` ("{band label} climate band (X–Y °C ambient, +30 °C cell rise) — assumed, confirm for this site"). **Today the resolver always returns 'assumed'** — no weather source supplies ambient extremes (PVGIS MRcalc is irradiance-only); the measured branch is pre-wired for later TMY data. The label reaches the SLD compliance box (row 23) and the autostring warning (row 76). Flagged in-code ENGINEER VALIDATION REQUIRED (record-low vs TMY-min policy unsettled) | temps.ts:28-47,65-67 | working | NONE |
| CODE.step8-sld.57 | Module voltage at temperature: `vocAt = vocV · (1 + tempCoeffVocPct/100 · (T − 25))` (cold ⇒ higher) and `vmpAt = vmpV · (1 + pmaxCoeff/100 · (T − 25))` (hot ⇒ lower). **Hot Vmp uses the Pmax coefficient, never Voc's** (Voc ~−0.25 %/°C vs Pmax ~−0.35 %/°C — the old engine's mix-up understated the MPPT-floor minimum). Fallback Pmax coeff **−0.35 %/°C** when the datasheet omits it, returned with `estimated: true` | temps.ts:70-96; india.ts:360; electrical-temps.test.ts "hot Vmp uses the Pmax coeff", "falls back… and says it is ESTIMATED" | working | NONE |
| CODE.step8-sld.58 | **The string window** `stringSizing(panel, inverter, temps)`: `vocCold = vocAt(panel, minCellC)`; `vmpHot = vmpAt(panel, maxCellC)`; `maxByDc = ⌊maxDcV / vocCold⌋`; `maxByMppt = ⌊mppt.maxV / vocCold⌋`; `minByMppt = ⌈mppt.minV / max(1, vmpHot)⌉`; `minPanels = max(1, minByMppt)`; `maxPanels = max(1, min(maxByDc, maxByMppt))`. Result carries the temps (provenance) and `pmaxEstimated` | window.ts:44-73; stringing.test.ts "max string length respects BOTH inverter maxDcV and MPPT ceiling" | working | NONE |
| CODE.step8-sld.59 | An **empty window is stated, not returned as min>max nonsense**: when `minPanels > maxPanels` the result carries `impossible`: "No series length fits this pair at {min}–{maxCell} °C: {min} modules needed to hold the {mpptMinV} V MPPT floor when hot, but only {max} fit under {min(maxDcV, mpptMaxV)} V when cold" | window.ts:59-72; electrical-temps.test.ts "an IMPOSSIBLE pairing is stated" | working | NONE |
| CODE.step8-sld.60 | `STRING_COLORS`: a fixed 10-colour palette cycled per string index — shared by the planner, the 3D editor overlay and the String Route sheet, so string identity is one colour everywhere | window.ts:8-20; Step8Sld.tsx:762 | working | NONE |
| CODE.step8-sld.61 | **Parallel strings per MPPT** `parallelPerMppt`: `min(⌊mppt.maxCurrentA / max(0.1, iscA)⌋, max(1, stringsPerMppt))`, plus `limitedByCurrent`. Compares **Isc** (not Imp — the old engine's audit-finding-7 error) against the input rating **WITHOUT the 1.25 duty factor** — deliberately, since 1.25×1.25 already lives in the fuse and double-counting "REFUSES ordinary hardware" (13.85 A module on a 16 A MPPT is standard). KNOWN CATALOG GAP flagged in-code: `mppt.maxCurrentA` is one field where datasheets publish two (operating vs short-circuit) — holding Isc to it is the conservative reading | autostring.ts:32-64; autostring.test.ts "does NOT double-apply the 1.25 wiring factor", "Isc caps parallel strings BELOW the datasheet claim" | working | NONE |

## §G — Grouping: what may share a series string (`lib/electrical/grouping.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.62 | Shade tiers reuse the 3D access-tint thresholds exactly: `clear > 0.95 ≥ light > 0.85 ≥ heavy` — "the grouping a user can SEE is the grouping the stringer applies" | grouping.ts:25-36; autostring.test.ts "shade tiers reuse the SAME thresholds as the 3D access tint" | working | NONE |
| CODE.step8-sld.63 | Group identity = `planeId | bucketed azimuth | bucketed tilt | shade tier`. Tolerances: **azimuth ±5°, tilt ±3°**, via deterministic BUCKETING (not clustering) — order-independent, reproducible; `bucketAzimuth` normalises into [0,360) so 359° and 1° share a bucket (a due-north roof is one group, not two half-strings) | grouping.ts:59-93,244-247; grouping-plane.test.ts §(c) wraparound tests | working | NONE |
| CODE.step8-sld.64 | **Plane identity** (`computePlaneIds`): two Roof polygons are one plane iff **co-planar** (pitch Δ ≤ 0.5°, azimuth Δ ≤ 1° unless both flat, plane-datum Δ ≤ 0.15 m — datum = surface height extrapolated to plan origin, separating parallel-but-stepped surfaces) **AND edge-adjacent** (any collinear wall overlap, tol 0.1 m). Identity = union-find connected component, root = smallest roof id (deterministic, order-free). Cross-building rule: co-planar but detached faces stay SEPARATE (else strings understate cable/drop/conduit) | grouping.ts:95-207; grouping-plane.test.ts (co-planar siblings merge; azimuth/pitch/height split; detached stay separate; order-independent) | working | NONE |
| CODE.step8-sld.65 | `groupPanels`: enabled panels only (disabled never occupy a string); a panel whose roof is missing from the project keeps its own plane ("unknown stays isolated rather than merged on faith"); groups sorted biggest-first, key-tiebreak (big groups claim MPPT slots first; runs reproducible) | grouping.ts:214-266; autostring.test.ts "excludes disabled panels entirely", "is deterministic" | working | NONE |
| CODE.step8-sld.66 | **MLPE (per-module optimisers)**: when `components.mlpe === 'optimizer'`, the whole enabled array is ONE mega-group (`key 'mlpe|all'`) — orientation/tilt/shade stop constraining stringing because every module tracks its own MPP; the hip-roof faces plain wiring strands can now share strings. Cable distance is not gated here — "the router prices the real runs" | grouping.ts:216-234; mlpe.test.ts "WITH optimisers, the whole array is ONE group" | working | NONE |
| CODE.step8-sld.67 | Serpentine order: rows clustered in the **ROOF GRID frame** (`roofGridAngle`, the frame auto-fill placed panels in — audit finding 11: any other frame fragments rows on a ~1°-rotated roof), row tolerance **0.5 m**, alternating direction per row. `orderGroup` walks a multi-roof (MLPE) group **face-by-face** — each roof its own grid frame and serpentine, concatenated in project roof order — so the walk never hops between planes | grouping.ts:268-340; mlpe.test.ts "walks face-by-face"; autostring.test.ts "walks rows alternately" | working | NONE |

## §H — Auto-stringing planner (`lib/electrical/autostring.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.68 | `autoStringPlan` contract: pure — returns `{ strings, issues, unstrungPanelIds }`; the caller commits strings as one patch. **The founding rule: NEVER emit a string that cannot be built** — panels a legal string can't take are left UNSTRUNG (visible, reportable, gate-blocking) instead of the old engine's silent 30-in-series over-voltage strings (audit finding 7, the Phase-4 KNOWN LIMIT, closed) | autostring.ts:1-30,171-292; autostring.test.ts "THE FIX: too few MPPT slots ⇒ unstrung panels + an explicit error (never over-long strings)" | working | NONE |
| CODE.step8-sld.69 | Empty-window refusal: an impossible panel/inverter pair strings NOTHING — one `error string_window_empty` "{panel brand model} + {inverter brand model}: {impossible text}" and every enabled panel returned unstrung | autostring.ts:182-195; autostring.test.ts "an impossible panel/inverter pair strings nothing and says why" | working | NONE |
| CODE.step8-sld.70 | Isc refusal: `parallelPerMppt.allowed === 0` ⇒ `error isc_high` "One string of {model} can present {Isc} A (Isc) — above the {maxCurrentA} A MPPT input limit of {inverter model}", everything unstrung | autostring.ts:197-210; autostring.test.ts "a module whose Isc alone exceeds the MPPT input is refused, not wired" | working | NONE |
| CODE.step8-sld.71 | `mergeUndersizedGroups`: a shade-tier group **smaller than minPanels** folds into an electrically identical host — **same PLANE, same azimuth, same tilt** (shade tier alone differs; different planes never merge). Emits `warn shade_mismatch`: "N {tier}ly-shaded panel(s) (down to X% sun) joined a string of clearer modules — too few to form their own string. In series the shadiest module limits the string, so consider removing it/them instead." A genuinely shaded full ROW keeps its own string (tiering still works). Kills the dead-end where one 82.7 %-access module was permanently unstrung with no action available | autostring.ts:66-127; autostring.test.ts "a lone off-tier panel joins its electrical twins", "a genuinely shaded ROW keeps its own string" | working | NONE |
| CODE.step8-sld.72 | `splitGroup`: balanced first — `nStrings = ⌈n/maxPanels⌉`, sizes differ by ≤1 (parallel strings sharing an MPPT must be equal-length) when `⌊n/nStrings⌋ ≥ minPanels`; otherwise fills **full max-length strings** and reports the remainder honestly — a sub-min tail is returned as `tail`, never shipped as a short string that drops out of tracking | autostring.ts:129-165; autostring.test.ts "balances strings when the window allows (≤1)", "a narrow window keeps full strings and reports the tail" | working | NONE |
| CODE.step8-sld.73 | Tail refusal: `warn group_too_small` — "N panel(s) can't form a string: {minPanels} in series are needed to hold the {mpptMinV} V MPPT floor at {maxCellC} °C. Remove them, or add panels alongside them." with `focusPanelIds` (the issues system's click-to-locate) | autostring.ts:222-230 | working | NONE |
| CODE.step8-sld.74 | MPPT slot assignment: slots = `inverterCount × mppt.count`; a planned string joins an occupied slot only when **same group AND same length** and below `par.allowed` parallel ("unequal strings in parallel fight each other"); otherwise takes an empty slot; no slot ⇒ overflow. Strings never span groups (the global-Y-sort bug, closed) | autostring.ts:233-267; autostring.test.ts "every emitted string fits the window and the slots it claims", "strings never span groups" | working | NONE |
| CODE.step8-sld.75 | Overflow refusal: `error mppt_capacity` — "Insufficient MPPT capacity: N panel(s) left unstrung. {inverterCount} × {model} offers {slots} MPPT input(s)[ × k parallel][ (current-limited to k, below the datasheet's S)] — add an inverter or reduce panels." | autostring.ts:269-283 | working | NONE |
| CODE.step8-sld.76 | Provenance warning: when the Pmax coeff was assumed, `warn temp_coeff_estimated` — "String lengths use an assumed Pmax coefficient ({model} datasheet value missing) and {temps.note}". Minted strings: `genId('str')`, `name 'String N'`, `inverterIndex`/`mpptIndex` from the slot, colour = `STRING_COLORS[i % 10]` | autostring.ts:257-266,284-290 | working | NONE |

## §I — Live validation & the legacy shims (`lib/stringing.ts`) — duty b, electrical half

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.77 | `validateSystem` (runs live on every edit; consumed by Step 6's banner, health score and the gate). Empty window ⇒ ONE `error string_window_empty` naming both components — "a component-pairing fault, not a per-string one… don't bury it under N derived string errors" | stringing.ts:45-66 | working | NONE |
| CODE.step8-sld.78 | Per string, `n > maxPanels` ⇒ **`error voc_high`**: "{name}: {n} panels → {round(vocAt(minCellC)·n)}V cold Voc exceeds max DC {maxDcV}V (max {maxPanels}/string)" — catches HAND-authored over-voltage too, however the strings were made | stringing.ts:68-77; stringing.test.ts "validateSystem still catches an over-voltage string authored BY HAND" | working | NONE |
| CODE.step8-sld.79 | Per string, `n < minPanels` ⇒ **`warn vmp_low`**: "{name}: {n} panels may fall below MPPT window {mpptMinV}V at {maxCellC}°C cell (min {minPanels}/string)" — a warning, not an error (it degrades, it does not destroy) | stringing.ts:78-85 | working | NONE |
| CODE.step8-sld.80 | `panel.impA > mppt.maxCurrentA` ⇒ **`warn imp_high`** "{name}: panel Imp {imp}A exceeds MPPT input limit {max}A" — the condition is string-independent but is emitted once PER STRING (N identical warnings for N strings, each with its own focus list) | stringing.ts:86-93 | working (repetition recorded; each instance carries its string's focusPanelIds) | NONE |
| CODE.step8-sld.81 | DC/AC ratio band: `> 1.35` ⇒ **`warn dc_ac_high`** "…expect clipping losses"; `< 0.90` ⇒ **`warn dc_ac_low`** "…inverter is oversized". The 0.90 floor is deliberately ONE band across every surface (Step-4 recommender, comparison, Copilot, health) — a looser floor here once made Step 6 read "all checks pass" while health deducted | stringing.ts:96-115 | working | NONE |
| CODE.step8-sld.82 | **`error unstrung_panels`**: enabled panels no string covers — "N enabled panel(s) is/are not wired into any string — they add cost and capacity but generate nothing". The check that makes autostring's refusals VISIBLE, and it holds for manual authoring too | stringing.ts:117-132; autostring.test.ts §"unstrung panels are an ERROR that outlives autostring" | working | NONE |
| CODE.step8-sld.83 | **`error mppt_overflow`**: `strings.length > mppt.count × inverterCount` — "N strings exceed available M MPPT inputs". (Counts SLOTS, not parallel capacity; parallel legality is the planner's job — a hand-built design using 2-parallel legally could false-positive here. Not observed; the planner never produces it) | stringing.ts:134-141 | working — uncertain(edge: hand-authored parallel strings on one MPPT would trip this even when `stringsPerMppt` allows them) | NONE |
| CODE.step8-sld.84 | Legacy exports still live: `autoString` shim (§B row 16 — fabricated project, issues discarded; also used by the comparison engine for candidate stringing) · `vocAtTemp` re-export · `estimateDcCableM` — the OLD serpentine+15 m-homerun ×2 estimator, still the BOM's DC-cable fallback (`max(30, estimate)`) when nothing is routed | stringing.ts:15-42,146-165; lib/bom/context.ts:141, lib/comparison.ts:123 (one hop) | working | NONE |

## §J — THE HARD GATE (`lib/electrical/gate.ts` + its one consumer) — special duty e

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.85 | `electricalGate(project)` is pure and single-sourced — "the wizard, the tests and any future engineering-pack export all ask the same function, so 'is this design issuable?' has exactly one answer". Returns **null (no block)** when panel, inverter or enabled panels are missing — "earlier steps own these" (their own nextBlocker cases hold the user there instead) | gate.ts:27-31; electrical-gate.test.ts "stays SILENT before the design exists" | working | NONE (census home is §A.10-6/shell; phase-10-prompts.md:79 "Electrical has a hard gate", :175-176) |
| CODE.step8-sld.86 | The gate blocks on **ERROR-level issues only** from `validateSystem` (warns never block). The blockable set is therefore exactly: `string_window_empty` · `voc_high` · `unstrung_panels` · `mppt_overflow`. Message = the lead error verbatim, `+ " (and N more electrical error(s))"` when several. Lead preference: `unstrung_panels` first ("the most common, the most fixable, the one with a one-click answer"); over-voltage/MPPT overflow need a human decision so they are quoted as-is | gate.ts:33-56; electrical-gate.test.ts (blocks unstrung, blocks hand-authored over-voltage, blocks MPPT overflow, passes a legal design, names hidden error count) | working | NONE |
| CODE.step8-sld.87 | `autoStringable: !!unstrung || errors.every(e => e.code === 'unstrung_panels')` — true when "Auto-string now" plausibly resolves it. (The second clause is redundant — with ≥1 error, `every(unstrung)` implies `!!unstrung` — but harmless.) The wizard appends " — use Stringing → Auto string" when set | gate.ts:48-56; Wizard.tsx:131-134 | working | NONE |
| CODE.step8-sld.88 | **Where the gate bites — the studio's ONE hard gate**: Wizard `nextBlocker` case 6 returns the gate message, which (a) blocks Step 6's **Next** with a 2.6 s toast, and (b) feeds `allowedStep` (first step with a blocker), which **clamps deep links and navigates the user back** — so a design that becomes electrically invalid can never sit on Steps 7–10 "showing numbers derived from strings that can't exist". A disabled panel outside every string does NOT block (it generates nothing) | Wizard.tsx:110-138,163-171,183-189; electrical-gate.test.ts "a disabled panel outside every string does NOT block" | working | NONE |
| CODE.step8-sld.89 | Placement of the block is doctrine, in-code: "Deliberately a BLOCK, not a warning… placed on Step 6's Next, so the user is held IN the editor — the one screen where the problem is fixable — rather than bounced from a downstream screen with nowhere to go." Root cause on record: the audit's impossible 2794 V all-panels string printed on a DISCOM sheet with no warning | gate.ts:1-13 | working | NONE |

## §K — DC protection & AC sizing (`lib/electrical-sizing.ts` + rules) — duty c, sizing half

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.90 | **String fuse** `dcFuseA`: next standard gPV rating ≥ **1.56 × Isc** (1.25×1.25 continuous+safety; ≥1.5 per IEC 62548), ladder `[10, 12, 15, 20, 25, 30, 32, 40, 50, 63]` A, top rung as floor. e.g. 15.7 A module ⇒ 25 A (not the old 20 A default) | electrical-sizing.ts:11-19; india.ts:379-382; electrical-sizing.test.ts "a 15.7A module needs a 25A fuse", "every catalog panel gets a fuse ≥ 1.5×Isc" | working | NONE |
| CODE.step8-sld.91 | **DC isolator**: next rung of `[16, 25, 32, 40, 63, 80, 100, 125]` A ≥ the fuse. **DC cable**: smallest of `[4→32 A, 6→42 A, 10→57 A]` mm² whose free-air ampacity ≥ the fuse — conductor sized against the protecting device (coordination rule) | electrical-sizing.ts:21-32; india.ts:383-388 | working | NONE |
| CODE.step8-sld.92 | **AC full-load amps**, exact, no premature rounding: 3φ `kW·1000/(1.732·415)`, 1φ `kW·1000/230` | electrical-sizing.ts:42-44; electrical-sizing.test.ts "sizes from exact amps… (72 kW 3φ boundary case)" | working | NONE |
| CODE.step8-sld.93 | **AC breaker** `acBreakerA`: next rung ≥ **1.25 × FLA** on the ONE ladder `[16, 25, 32, 40, 63, 80, 100, 125, 160, 200, 250, 320, 400, 500, 630]` A shared by BOM and SLD — replacing "two divergent inline ladders… which could print two contradictory breaker ratings", and past whose end a rating below load current was possible. 630 A tops every catalog combination | electrical-sizing.ts:34-56; india.ts:389-391; electrical-sizing.test.ts "never rates a breaker below the load current", "the SLD sheet and the BOM ACDB line print the SAME rating" | working | NONE |
| CODE.step8-sld.94 | **AC cable (ampacity)** `acCableSizeMm2`: smallest mm² whose **40 °C in-conduit/tray derated** ampacity carries the BREAKER — ladder 4→32 A up to 400→500 A (15 rungs; ascending-in-both-columns is load-bearing and tested) | electrical-sizing.ts:59-81; india.ts:392-411; ac-cable-sizing.test.ts "the conductor carries its breaker", "is ascending in BOTH columns" | working | NONE |
| CODE.step8-sld.95 | **AC voltage drop** `acVoltDropPct`: `drop = √3·I·L·R` (3φ) or `2·I·L·R` (1φ, return conductor drops too), R from the ~70 °C operating-temperature table (6.7 Ω/km @4 mm² … 0.073 @400), as % of 415/230 V nominal. Limit **3 %** (ordinary Indian LV final-circuit practice, rules-configurable) | electrical-sizing.ts:83-106; india.ts:412-429; ac-cable-sizing.test.ts "drop rises with length and falls with size", "single phase drops more than three" | working | NONE |
| CODE.step8-sld.96 | **`sizeAcCable`** = the governing size of BOTH criteria: `mm2 = max(ampacityMm2, voltDropMm2)`, with `governedBy: 'ampacity'|'voltage-drop'`, the achieved `voltDropPct`, and `singleRunAdequate: false` when NO rung carries the breaker (parallel runs/busbar needed — "reported rather than silently returning the top rung"). Rationale in-code: ampacity alone puts 15 kW 3φ on 4 mm², which dies over 60 m — "under-built the common small system while looking more rigorous than the constant it replaced" | electrical-sizing.ts:108-162; ac-cable-sizing.test.ts "a long run needs more copper than ampacity alone", "a breaker no single cable can carry is flagged, not fudged", "governedBy names the criterion" | working | NONE |
| CODE.step8-sld.97 | Honesty boundary, stated at both layers: ampacity+drop only — grouping factors, non-40 °C ambient, buried-vs-tray, harmonics NOT modelled; "callers must present the result as a starting point" (the BOM line's formula admits it — tested). Consumers: BOM electrical emitter (`sizeAcCable`, `acBreakerA`, `acFullLoadA`, `dcCableSizeMm2`) — sitting 8; SLD derives (rows 48, 35) | electrical-sizing.ts:70-75,136-139; ac-cable-sizing.test.ts "the formula admits what it did NOT model"; lib/bom/emitters/electrical.ts (one hop) | working | NONE |

## §L — String combiner boxes (`lib/electrical/combiner.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.98 | `combinerPlan(strings, panel)` (central/C&I topology): `nBoxes = ⌈total / maxStringsPerBox (12)⌉`, strings spread evenly (first `total % nBoxes` boxes get one extra), each box `SCB-{n}` with its string ids, per-string gPV fuse = `dcFuseA(panel)` (the SAME 1.56×Isc ladder as the DCDB), output current = `round(nStrings × Isc × 1.25, 0.1)` A | combiner.ts:34-60; india.ts:487-490 | working | SC.10-9.04 (model side) |
| CODE.step8-sld.99 | Reconciliation gates: zero strings ⇒ `ok:false` "No strings to combine — string the array first."; Σ inputs ≠ total ⇒ `ok:false` planning error; any box > 12 inputs ⇒ `ok:false`. Every string assigned exactly once | combiner.ts:38-68 | working | SC.10-9.04 |
| CODE.step8-sld.100 | Consumers: the SLD SCB block (row 22, drawn only when `ok`) and the BOM combiner context (sitting 8). Topology flag `components.inverterTopology` (`'string'` default / `'central'`) is Step 4's control — cited, owned there | Step8Sld.tsx:369-374; lib/bom/context.ts:162 (one hop); types.ts:328 | working | SC.10-9.04 |

## §M — Cable routing (`lib/routing.ts`) — duty c, routing half

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.101 | What is bought, and what is not (the doctrine that replaced audit finding 6's guess — "summed every module-to-module distance, added a flat 15 m per string, doubled the lot, floored it at 30 m, and printed '(+15% slack incl.)' … a slack it never applied"): module→module inside a row costs NOTHING (factory leads); only hops beyond lead reach cost their EXCESS; the two HOME RUNS per string (+ and −) are the bulk; plus the vertical drop and installation slack | routing.ts:1-20 | working | NONE |
| CODE.step8-sld.102 | **Vertical drops come from the model, not a constant**: DC = `max(0, roofHeight − inverterHeight)`, AC = `max(0, inverterHeight)` (inverter down to the meter at grade); fallback `defaultVerticalDropM (3)` only when no inverter placement exists | routing.ts:35-49; india.ts:370; routing.test.ts "a taller roof drops further (the flat 3 m under-counted it)" | working | NONE |
| CODE.step8-sld.103 | `inverterWorldPos`: wall-mounted inverter resolved to plan XY by interpolating along its roof edge (`edgeIndex`, `t`); null (⇒ nothing routes) with no placement | routing.ts:52-60; routing.test.ts "no placement ⇒ no position (and the router simply routes nothing)" | working | NONE |
| CODE.step8-sld.104 | Route length law: `routeLengthM = (polyline + verticalDrop) × (1 + slackPct)`, slack **10 %** from rules | routing.ts:63-74; india.ts:363; routing.test.ts "route length = path + drop, then slack" | working | NONE |
| CODE.step8-sld.105 | `routeBlockers`: only footprints a cable may not cross — obstructions with capability `mustRemainOpenToSky` (chimney flue, opening hatch) + non-degenerate keepouts. **Bridgeable objects do NOT block** ("cable runs under a raised array happily") | routing.ts:76-98; lib/capabilities.ts (one hop); routing.test.ts "a bridgeable tank does not" | working | NONE |
| CODE.step8-sld.106 | **`routePath`**: straight line when clear (exact, no fabricated doglegs); otherwise Dijkstra over a **visibility graph** of blocker corners padded outward 0.4 m plus corridor vertices. Chosen over grid A* in-code: a grid "quantises the length (you would quote cable to the cell size)". History pinned: single-corner detours were wrong — a straddling box needs two corners, and the first version silently fell back to a line THROUGH the obstacle. Fully-enclosed target ⇒ returns the direct line "honestly". Collinear waypoints deduped | routing.ts:104-237; routing.test.ts "detours AROUND a blocker… never enters it", "a clear roof gets a straight line" | working | NONE |
| CODE.step8-sld.107 | **Cost ≠ length**: corridor edges (consecutive vertices of one corridor, rings closing) are free; every other edge pays `crossFieldPenalty (6×)` **only on the metres actually inside the array footprint** (`crossingLengthM`, 0.4 m sampling). "The penalty steers the path, it never inflates the metres quoted — those are re-measured from the returned polyline" | routing.ts:169-199,339-351; india.ts:364; routing.test.ts "prefers the perimeter corridor", "still takes the direct line when the corridor would be absurd" | working | NONE |
| CODE.step8-sld.108 | Corridors: `roofCorridor` — the roof polygon pulled inboard `corridorInsetM (0.4)`, closed ring; `arrayCorridors` — each panel ROW (roof-grid frame, same 0.5 m clustering as the stringer) as a rail-run corridor plus the rectangular ring around the array ("how a run crosses between rows without walking over modules"). Without these, measured: 26/26 routes went straight across live modules | routing.ts:239-311; routing.test.ts "routes AROUND the array footprint rather than across module glass" | working | NONE |
| CODE.step8-sld.109 | `arrayFootprint`: the enabled-panel bounding box in the roof-grid frame with 0.6 m outward margin (edge-hugging runs don't count as crossing); the region cable "must NOT be dragged across (running a conductor over module glass is not done)" | routing.ts:313-337 | working | NONE |
| CODE.step8-sld.110 | `intraStringExtraM`: within a string, only hops whose centre-to-centre distance exceeds `moduleLeadReachM (1.4)` are charged, and only the excess. "On a tidy row this is zero" | routing.ts:365-381; india.ts:369; routing.test.ts "modules within lead reach cost nothing", "a long jump charges only the EXCESS" | working | NONE |
| CODE.step8-sld.111 | **`autoRouteStrings`**: per string, routes BOTH conductors home — + from one end, − from the other (`{stringId}/hr/0` and `/hr/1`), each with blockers, corridors, footprint, model drop and 10 % slack. Hand-edited (`manual`) home runs are preserved **only while their string still exists** — re-stringing mints new ids, so orphaned manual routes are dropped rather than surviving as dead copper in the BOM (measured live: 27→28 routes on a second Auto string before the fix) | routing.ts:383-429; routing.test.ts "routes BOTH conductors home", "drops a hand-routed run whose string no longer exists", "never stomps a hand-edited route" | working | NONE |
| CODE.step8-sld.112 | **`autoRouteAc`**: one `ac/main` route inverter → `gridConnection.pos`; returns **[] when no service entry is placed** — "the length is then genuinely unknown, and the BOM says so instead of guessing". `acCableFromRoutes` reports `{meters, routed}` accordingly (BOM: assumed allowance vs measured length — tested) | routing.ts:431-461; routing.test.ts "with NO meter placed the AC run is an ASSUMED allowance", "placing the meter turns the AC run into a MEASURED length" | working | NONE |
| CODE.step8-sld.113 | **`routeIssues` — the DC voltage-drop check** (`warn dc_voltage_drop`): per string, worst conductor of the pair, `Vdrop = 2·L·Imp·ρ / A` with ρ = 0.0175 Ω·mm²/m (Cu ~20 °C) and A = the quoted 4 mm², against `Vmp × n` at STC; warns above `maxDcDropPct (2 %)`: "{name}: X% DC voltage drop over its Y m run (4 sq.mm Cu) — above the 2% design limit. Shorten the run, move the inverter closer, or size the cable up." Limit flagged ENGINEER VALIDATION REQUIRED (policy, DISCOM-adopted) | routing.ts:463-502; india.ts:366-368; routing.test.ts "warns when a long thin run exceeds the design limit" | working | NONE |
| CODE.step8-sld.114 | **`dcCableFromRoutes` — the BOM quantity**: `meters = round(Σ routeLengthM) + round(Σ intraStringExtra × slack)` — "Round the PARTS, then sum — never round both independently… if they disagree by a metre the line stops being auditable"; `ductM` = distinct path length (Σ polyline / 2 — a string's + and − share one duct); `routed:false` before routing has run | routing.ts:504-530; routing.test.ts "sums home runs + long hops, with slack", "the formula states what was actually summed (it used to claim slack it never applied)" | working | NONE |
| CODE.step8-sld.115 | Wiring proof: routes **re-key the design fingerprint** (they move cable metres = money) while a project with NO routes fingerprints byte-identically (no quote goes stale on upgrade). Route auto-generation is triggered from Step 6's "Route cables" (`[...autoRouteStrings, ...autoRouteAc]` in one patch) — screen side owned by sittings 2/5 | routing.test.ts "wiring: fingerprints + cascade" describe; Step6Editor.tsx:933 (one hop); lib/fingerprints.ts (one hop, sitting 9) | working | NONE |

## §N — Cascade deletion (`lib/cascade.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.116 | `cascadeDeleteRoof`: ONE atomic `Partial<Project>` patch removing the roof and everything living on it — panels, segments (reindexed via `reindexAll`, emptied tables dropped), on-roof obstructions, walkways, rails, arresters, wall-mounted inverter placements, keepouts — with strings pruned of the dead panels. "No entity may outlive the object it depends on": previously deleted roofs left panels counting in capacity/energy/BOM and obstructions as ground-level shadow casters | cascade.ts:44-70; cascade.test.ts "removes every entity living on the roof and prunes strings", "energy report and BOM no longer count the deleted roof panels" | working | NONE |
| CODE.step8-sld.117 | `pruneStrings`: dead panel ids dropped from every string; a string emptied of panels is removed entirely; strings keep their surviving panels (never wholesale-cleared) | cascade.ts:27-31; cascade.test.ts "strings keep panels from surviving roofs", "removes only the dead ids… drops emptied strings" | working | NONE |
| CODE.step8-sld.118 | `pruneRoutes`: home runs whose string died go with it ("dead copper… still counted, priced and quoted" otherwise); deliberately **conditional** — a project without `cableRoutes` is returned untouched so its fingerprint stays byte-identical | cascade.ts:11-25; routing.test.ts "deleting the panels kills the string AND its home runs (no dead copper in the quote)" | working | NONE |
| CODE.step8-sld.119 | `cascadeDeletePanels`: same contract for a panel set — strings pruned of exactly those ids, affected segment grids reindexed, routes pruned | cascade.ts:72-87 | working | NONE |

## §O — Layout & structure DRC (`lib/drc.ts`) — duty b, layout half

Consumed by Step 6's live issues banner and `lib/health.ts` (both one-hop); every issue code carries a fixed health-score deduction in the rules pack (india.ts:439-470).

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.120 | `layoutIssues` frame law: panel footprints via `panelCornersOnRoof` — "the SAME footprint placement validated (down-slope grid + cos(pitch) foreshortening), so DRC can never contradict the fill". Quads shrunk 10 % toward centre so exact edge-adjacency ≠ overlap. Returns [] without a spec or panels; disabled panels excluded throughout | drc.ts:26-50; lib/layout.ts (one hop) | working | NONE |
| CODE.step8-sld.121 | **`error panel_overlap`**: pairwise within each roof — "N overlapping panel pair(s) — clear or nudge them" (health −30) | drc.ts:74-96; drc.test.ts "flags overlapping panels as an error, pointing at the panels" | working | NONE |
| CODE.step8-sld.122 | **`warn setback_breach`**: a panel not fully inside any region of the roof polygon inset by `perEdgeSetbacksM` (fallback uniform `setbackM`) — "N panel(s) breach the roof setback" (health −12) | drc.ts:60-73,97-103 | working | NONE |
| CODE.step8-sld.123 | **`warn shaded`**: solar access < `SHADE_ACCESS_MIN (0.70)` — "N panel(s) below 70% solar access — shading loss" (health −15). Note this 0.70 flag threshold is a DIFFERENT dial from the stringer's 0.95/0.85 grouping tiers — three shade thresholds exist by design | drc.ts:24,86-88,104-113 | working | NONE |
| CODE.step8-sld.124 | **`error panel_in_keepout`**: panels intersecting a non-`shade` keepout (`roofId null` = project-wide zone) — "N panel(s) inside a no-build zone — remove or disable them". Exists because a zone drawn AFTER placement moves nothing, "which is exactly the silent-wrong-output case"; same block set as the fill (shade-only keepouts mark shade, they don't forbid) (health −30) | drc.ts:115-141; drc.test.ts §"panels inside a drawn no-build zone" (project-wide, other-roof, shade-only, disabled cases) | working | NONE |
| CODE.step8-sld.125 | **`error panel_over_obstruction`** (§26c bridging): a panel overlapping a `blocksPlacement` obstruction whose capabilities say `!panelsMayCross` or `mustRemainOpenToSky` — "N panel(s) over {label} — it must remain open to sky / it cannot be bridged" (health −30) | drc.ts:144-169 | working | NONE |
| CODE.step8-sld.126 | **`error bridge_clearance`**: a legally bridgeable obstruction needs under-structure clearance ≥ `obstruction height + minVerticalClearanceM`; panels whose segment racking `frontLegM` falls short — "…needs ≥ X m under-structure (obstruction H m + M m margin)" (health −25) | drc.ts:170-184; lib/capabilities.ts `requiredBridgeClearanceM`, lib/structure.ts `resolveRacking` (one hop) | working | NONE |
| CODE.step8-sld.127 | **`warn bridge_engineer`**: clearance OK but the capability demands confirmation — "Array bridges {label} — flagged for engineer confirmation (load path over the obstruction)" (health −8) | drc.ts:185-192 | working | NONE |
| CODE.step8-sld.128 | `structureIssues` 1 — **`warn foundation_dead_load`**, per roof (never on `ground` roofs): Σ `foundationDeadLoadKg` over roof_anchor nodes (shape-aware: circular ≈ −21 % concrete vs square) — "Foundations add ~N kg to {roof} (X t) — roof capacity is NOT checked. Have a structural engineer confirm the slab can carry it." Always warns when mass > 0 — a tonne of concrete is never added silently (health −via unknown table: not in penalties list → −10 unknownValidationPenalty) | drc.ts:236-271; lib/foundation.ts (one hop); drc-structure.test.ts "warns, and states the total", "does NOT fire on a ground array" | working — note: `foundation_dead_load` has no entry in `validationPenalties`, so it costs the generic 10 | NONE |
| CODE.step8-sld.129 | `structureIssues` 2 — **`error foundation_clash`**: a foundation FOOTPRINT (real plan size from the rules geometry, not a point) intersecting a keepout, walkway corridor or obstruction — deliberately ignoring the §26c bridging exemption: "A panel may span above an obstruction; a footing cannot be cast in one." (health −25) | drc.ts:273-318; drc-structure.test.ts "errors on an obstruction even though a panel may legally bridge it", "a walkway laid BETWEEN the leg lines is fine" | working | NONE |
| CODE.step8-sld.130 | `structureIssues` 3 — **`error foundation_too_tall`**: foundation height ≥ segment clearance − min steel leg (`minLegAboveFoundationM 0.05`) — "a {H} mm foundation leaves no buildable steel leg inside {C} mm of clearance. Raise the clearance or choose a shallower foundation." Flagged, never silently clamped; the rule config decides, not a magic number (tested) (health −20) | drc.ts:320-337; drc-structure.test.ts "errors when the clearance leaves no buildable steel leg", "the rule config is what decides" | working | NONE |
| CODE.step8-sld.131 | **The complete DRC/electrical rule set at a glance** (severity → code): ERRORS: panel_overlap · panel_in_keepout · panel_over_obstruction · bridge_clearance · foundation_clash · foundation_too_tall · string_window_empty · voc_high · unstrung_panels · mppt_overflow · isc_high · mppt_capacity. WARNINGS: setback_breach · shaded · bridge_engineer · foundation_dead_load · vmp_low · imp_high · dc_ac_high · dc_ac_low · shade_mismatch · group_too_small · temp_coeff_estimated · dc_voltage_drop. Only the electrical ERRORS from `validateSystem` gate the wizard (row 86); everything else informs (banner + health) | §§H/I/M/O above; india.ts:439-470 | working | NONE |

## §P — Node hardware visuals (`lib/hardware.ts`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step8-sld.132 | `nodeHardware(kind)`: nominal-size visual parts per structure node so ghosted-module inspection shows what holds things together — mid clamp (32×34×46 mm box straddling the rail), end clamp (40×34×52 — "the wider casting — the same distinction the BOM prices separately"), leg→rafter (70×70×8 plate + M12×28 bolt), rafter→purlin (M10×26 bolt), brace bolt (M10×22), sheet standoff (50×100×8 L-foot + 60×60×6 plate) | hardware.ts:24-104 | working | NONE |
| CODE.step8-sld.133 | Two laws: `roof_anchor` deliberately returns [] — `foundationAssembly` owns it, "keeps the two from ever drawing the same thing twice"; and dimensions are "representation, not engineering — no quantity is ever read back from them" (BOM counts come from the node graph, not these meshes) | hardware.ts:17-23,101-103 | working | NONE |

---

## Census diff — §A.10-9 (37 entries)

All 37 census entries are matched by code rows above. Matches with a recorded divergence:

| Census | Verdict |
|---|---|
| SC.10-9.01, .03–.12, .14–.19, .25–.27, .29–.34, .36, .37 | **Matched clean** (rows 1, 4–12, 14–15, 17, 20–34, 45–47, 50–51, 98–100) |
| SC.10-9.02 | Matched with THREE divergences: "zoom and pan" is pan-only (row 2, DEFECT); paper-size labels contradict each other (row 13, DEFECT); sheet numbering says "of 2" in a 4-sheet set (row 43, DEFECT). Title blocks + NTS notes themselves: present (rows 18, 33, 44) |
| SC.10-9.13 | Matched with divergences: rect obstructions drawn as circles (row 39), legend advertises undrawn symbols (row 41), static 10°-tilt/chemical-anchor detail (row 42), unreliable scale claim (row 37) — all DEFECTS on an otherwise census-complete sheet (rows 36, 38, 40, 44) |
| SC.10-9.20–.24 | Field lists match the census exactly (row 51) — but the census's own option lists are subsets of what derivation can produce (row 52, DEFECT: unrepresentable derived ratings) |
| SC.10-9.24 | Additional divergence: SLD AC-cable *default* is the legacy 10/6 constant, not the sized value the BOM prints (row 35, DEFECT) |
| SC.10-9.28 | Matched; standards are 3 hardcoded strings, not the market pack's engineering-standards labels expected by M05-65's overlay (row 53) |
| SC.10-9.35 | Matched (row 12); note the census/M05-67 "online-only, fail fast" qualifier is a PDF-render-service property — the POC's print path is pure browser CSS, nothing to fail |
| SC.10-9.09 | Matched; earth-pit count hardcoded at 3 vs the BOM's rules-derived 2(+1) (row 30, DEFECT) |
| SC.10-9.36 | Matched; Step 8's Auto-string runs the degraded legacy shim, unlike Step 6's (row 16, DEFECT) |

**CENSUS-UNMATCHED: none** — 0 of 37 entries lack a code row.

**M05 baseline consistency (duty f):** M05-62 ✓ (rows 1, 18); M05-63 ✓ (rows 20–34, 36–47); M05-64 ✓ (rows 23–24); M05-65 ✓ with the standards-pack divergence (row 53) and the option-subset defect (row 52); M05-66 ✓ (rows 6–7); M05-67 ✓ (rows 9–17). The HARD GATE (§J) has **no home in §A.10-9** — its census home is the Step-6/shell sections (phase-10-prompts.md:79 "Electrical has a hard gate: an invalid string design blocks 'Next' in the layout step"; :175-176 "Keep the hard electrical gate on the layout step") — inventoried here because the gate function and every rule feeding it are this area's files. The entire electrical ENGINE (§§F–O: temps, window, grouping, autostring, validation, sizing, routing, cascade, DRC) is likewise beyond §A.10-9's screen-scoped census — recorded as newBeyondCensus, not unmatched.

---

**rowCount:** 133
**defectCount:** 11 POC-DEFECT (rows 2, 13, 16, 30, 35, 37, 39, 41, 42, 43, 52) + 1 uncertain (row 83)
**censusUnmatched:** 0 of 37 (all matched; 9 entries matched with recorded divergences — table above)
**newBeyondCensus:** 77 rows (census = NONE: §§F–K engine rows 54–97 minus §L's 98–100, §§M–P rows 101–133 — the string-sizing math, grouping/plane engine, autostring planner, validation set, hard gate, DC/AC sizing, routing engine, cascade, structure DRC, node hardware; none of it screen-census territory)

**notes** (≤10 lines)
1. Test evidence: 12 files / 171 tests, **all pass** (778 ms) — no test-derived DATA finding; every ladder/factor cross-checked against `data/rules/india.ts`.
2. The engine is the strongest part of the studio: refusal-over-fabrication is enforced end-to-end (empty window → unstrung → gate → UnstrungState), and the audit's 2794 V sin is structurally impossible now (real strings only, cold-Voc box, hard gate).
3. The HARD GATE law, precisely: error-level electrical issues (`string_window_empty`, `voc_high`, `unstrung_panels`, `mppt_overflow`) block Step 6's Next AND clamp `allowedStep`, so Steps 7–10 are unreachable while any exists; warnings never block; missing panel/inverter/panels are earlier steps' gates.
4. Worst cross-document defect: the SLD prints the legacy 10/6 mm² AC-cable constant while the BOM sizes via ampacity+drop (row 35) — the exact two-answers problem the shared breaker path was built to kill, surviving one field over.
5. Second engine/screen split: Step 8's "Auto-string now" runs the degraded legacy shim (no plane merge, no MLPE, refusals discarded) while Step 6 runs the real planner (row 16).
6. Design temps are ALWAYS 'assumed' today (lat-band table; PVGIS extremes unwired) — correctly labelled everywhere it prints; three separate shade thresholds coexist by design (0.70 DRC flag, 0.95/0.85 string tiers).
7. The four ENGINEER-VALIDATION-REQUIRED flags in code: design-temperature policy, DC sizing vs DISCOM-adopted IS/IEC, cable slack/lead-reach/drop limits, earthing pit convention — all rule-pack data, none settled.
8. Drawing-sheet defects are cosmetic-but-customer-facing: paper-size contradictions, of-2 numbering, fake zoom claim, circle-rendered rect obstructions, undrawn legend symbols, static 10° detail (rows 2, 13, 37, 39, 41–43).
9. Routed home runs exist in the model and price the BOM but no drawing prints them (row 45) — the "String Route" sheet draws the series walk only.
10. `foundation_dead_load` is missing from the health penalty table (falls to the generic −10) — minor rules-pack gap noted in row 128.

