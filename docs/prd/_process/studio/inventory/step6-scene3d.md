# Feature inventory — step6-scene3d (Sitting 5 agent B · the 3D scene: sun/shadow sim, heatmap, view modes, camera, per-panel solar access)

Pass-two inventory, 2026-08-05. Primary files (under `3d_design_studio/src/features/solar-studio/`): `three/Scene3D.tsx` (1,655 ln) · `three/PanelsInstanced.tsx` (191) · `three/HeatmapLayer.tsx` (63) · `three/textures.ts` (77) · `three/profile-geometry.ts` (210) · `three/__tests__/*` · `lib/sun.ts` (91) · `lib/sim-time.ts` (19) · `lib/solar-heatmap.ts` (331) · `lib/shading.ts` (339) · `lib/scene-model.ts` (307) · `lib/scene-frame.ts` (88).

Test run (read-only, deps from lockfile, vitest 3.2.7) — **13 files, 139 tests, all PASS**: shading (8) · shading-beam-availability (8) · row-shading (7) · sim-time (4) · solar-heatmap (15) · scene-model (7) · one-frame (12) · frame-parity (11) · panel-inspector (10) · solar (13) · obstruction-assets (8) · profiles (32) · eave-ref-plane (4). No direct DOM/render test of Scene3D.tsx itself exists; its UI behavior is pinned only through the lib-level tests above.

One-hop files consulted, owned elsewhere: `lib/solar.ts` + `lib/poa.ts` + `lib/pvgis.ts` + `components/EnergyReportSheet.tsx` (sitting 6) · `lib/maps.ts` + `components/SatCanvas.tsx` + `lib/geo.ts` (sitting 1) · `lib/roof-plane.ts`, `lib/layout.ts`, `lib/segment-ops.ts`, roof factories (sitting 2) · `three/ObstructionMesh.tsx` (sitting 3) · `screens/Step6Editor.tsx`, `lib/structure*.ts`, `three/StructEditPanel.tsx`, `three/StructureInstanced.tsx`, `three/LegPlanEditor.tsx`, `lib/panel-pose.ts`, `lib/ground.ts` (sitting 5 agents A/C) · `store/useDesignSync.ts`, `store/store.tsx` (sitting 10) · `screens/ShareViewer.tsx` (sitting 11) · `screens/Step7Proposal.tsx` (sitting 7).

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.step6-scene3d.1 | 3D view is a full-screen overlay REPLACING the 2D editor (absolute inset 0, zIndex 50); Canvas: PCFSoftShadowMap, ACESFilmic tone mapping exposure 1.05, SRGB output, `preserveDrawingBuffer` (for captures), antialias, fov 45, initial camera at iso [30,42,42]; wrapper takes focus on mount so keys are live immediately (Phase 22p) | Scene3D.tsx:413-441,401-403 | working | SC.10-7.19 |
| CODE.step6-scene3d.2 | Orbit/pan/zoom via drei OrbitControls: damping 0.08, `zoomToCursor`, screenSpacePanning off, zoomSpeed 1.15 / panSpeed 0.9 / rotateSpeed 0.9, distance clamp 8–170 m, `maxPolarAngle` π/2.05 → camera never goes below the horizon; stable pivot [0,3,0] (fresh-array would snap pivot back and defeat zoom-to-cursor); rotation disabled while heatmap is on | Scene3D.tsx:467-482,102-104 | working | SC.10-7.01 |
| CODE.step6-scene3d.3 | Three one-tap view presets Top/Isometric/Front (rail bottom-left, hidden in heatmap mode): camera jumps to [0.01,80,0.01] / [30,42,42] / [0,10,58], target reset to (0,3,0) | Scene3D.tsx:94-100,316-322,580-593 | working | SC.10-7.02 |
| CODE.step6-scene3d.4 | Close → 2D: X button "Back to 2D editor" (top of left rail) and global Escape; focus hand-back is the parent's job (Step6Editor.tsx:225-230 restores it, one hop) | Scene3D.tsx:487-494,387-390,393-403 | working | SC.10-7.03 |
| CODE.step6-scene3d.5 | Keyboard camera (Phase 22p): arrows orbit ±0.12 rad (Shift = fine 0.04), +/=/− dolly ×0.9/×1.1, 1/2/3 jump to presets, Escape exits; guard so keys inside inputs/sliders/buttons never move the camera; scene is a tab stop (`tabIndex=0`, `role="application"`, instruction aria-label). Keyboard clamps (phi ≤ π/2−0.02, radius 4–320) differ slightly from drag's (π/2.05, 8–170) — OrbitControls re-clamps radius on update; discrepancy recorded, not resolved | Scene3D.tsx:336-391,414-421 | working | SC.10-7.04 |
| CODE.step6-scene3d.6 | On-screen equivalents: presets and close have buttons; orbit/zoom have pointer+touch gestures only — NO visible on-screen orbit or zoom in/out controls exist anywhere in the scene (POC doc 10.7 §1 and the census demand a visible equivalent for every keyboard-only control) | Scene3D.tsx:486-593 (absence — only rails shown); phase-10-prompts.md:968-970 | POC-DEFECT(no visible orbit/zoom widgets; gestures only) | SC.10-7.04 |
| CODE.step6-scene3d.7 | Season presets Winter/Summer/Equinox/Today as chips (aria-pressed, brand highlight): Dec 21 / Jun 21 / Mar 20 of the current year at 12:00, Today = now | Scene3D.tsx:75-85,233,706-731 | working | SC.10-7.05 |
| CODE.step6-scene3d.8 | Date picker (native `input type=date`, dark colorScheme): picking sets the date at LOCAL noon. Displayed value = `date.toISOString().slice(0,10)` — a UTC slice, so in a timezone ahead of UTC (IST) the field shows the PREVIOUS day when opened before ~05:30 local | Scene3D.tsx:732-748 | POC-DEFECT(minor: value rendered from UTC date, can lag the local calendar day) | SC.10-7.06 |
| CODE.step6-scene3d.9 | Play/Pause sun animation: +0.12 h every 60 ms (~2 sim-hours/s), loops 19h → 5h; shadows move because the directional light position derives from the animated hour | Scene3D.tsx:236,256-262,751-759,1239-1252 | working | SC.10-7.07 |
| CODE.step6-scene3d.10 | Time-of-day slider 5 AM–7 PM (min 5, max 19, step 0.05, amber accent), flanked by "5 AM"/"7 PM", current time shown bold via `fmtHour` (12-hour h:mm AM/PM) | Scene3D.tsx:760-772,784; solar.ts:11-16 | working | SC.10-7.08 |
| CODE.step6-scene3d.11 | Sunrise/sunset readouts for the chosen date+site (Sunrise/Sunset icons): 2-minute horizon-crossing scan in LOCAL SOLAR hours, TZ-independent | Scene3D.tsx:251-254,785-790; sun.ts:69-89; test solar.test.ts 13 PASS | working | SC.10-7.09 |
| CODE.step6-scene3d.12 | Sun-position widget (hidden in mesh & heatmap): SVG compass with N label + 4 ticks, amber needle at azimuth, `Az X° · Alt Y°` (altitude display floored at 0), date (en-IN) + time; badge shows TRUE compass azimuth while the SCENE sun uses the image-frame azimuth (true + calibration northOffsetDeg) — same convention as the shading engine | Scene3D.tsx:271-277,596-647 | working | SC.10-7.10 |
| CODE.step6-scene3d.13 | Sun-path arc toggle: dashed golden arc (#d4a017) of the day's sun positions (solar hours 5→19 step 0.25, below-horizon culled), HTML hour chips at whole hours at 1.05× radius (drei `<Html>` deliberately, not `<Text>` — troika suspense once blanked sibling panels); north offset applied; hidden in mesh mode | Scene3D.tsx:241,545-554,1461-1469,1594-1654; test sim-time.test.ts:53 "arc crosses the horizon where the sunrise/sunset labels say" | working | SC.10-7.11 |
| CODE.step6-scene3d.14 | ONE simulation-time basis: every sim instant is a local MEAN-SOLAR hour at the SITE's longitude → UTC via `solarHourDate` (`simTimeDate`); the visual sun, sun path, heatmap, engine and sunrise/sunset labels share it (old wall-clock basis was ~34 min off even in-timezone) | sim-time.ts:16-18; sun.ts:59-67; Scene3D.tsx:264-269,1615; 4 sim-time tests PASS | working | NONE (new-beyond-census correctness guarantee) |
| CODE.step6-scene3d.15 | Sun position from a NOAA-style astronomical algorithm (declination + hour angle, sidereal time), azimuth 0=N clockwise; drives light dir vector (x=sinAz·cosAlt, y=sinAlt, z=−cosAz·cosAlt) | sun.ts:25-51; Scene3D.tsx:1148-1153; solar.test.ts PASS | working | SC.10-7.10 |
| CODE.step6-scene3d.16 | Atmosphere & day/night: drei `<Sky>` when sun is up (turbidity 6, rayleigh 2.2), night = dark #0a0f1c background, exponential fog; dusk ramp `duskFactor = clamp(altitude/0.25)` scales ambient 0.14+0.38d, hemisphere 0.12+0.28d and the warm sun light 0.4+1.5d (#fff4e0, 2048² shadow map, bounds ±60, bias −0.0004); below the horizon there is no directional light → no shadows at night; glowing sun sphere (r 2.1) drawn at 0.75R along the sun direction | Scene3D.tsx:1173-1174,1218-1254,1470-1475 | working | NONE (new-beyond-census: dusk/night states + sun marker) |
| CODE.step6-scene3d.17 | Visible shadow casters: roofs & parapets (`castShadow` RoofMesh), obstructions (ObstructionMesh, castShadow == `castsAnalyticalShadow` parity — s3), panels (all 3 instanced meshes castShadow), structure members + foundation nodes (castShadow, visual-only by documented design — members excluded from the engine, modules overhang them) | Scene3D.tsx:1536,1547; PanelsInstanced.tsx:152-158; StructureInstanced.tsx:22,141-142 (one hop, agent C); shading.ts:34-35 | working | SC.10-7.12 |
| CODE.step6-scene3d.18 | Walkways, safety rails, lightning arresters and wall inverters render in 3D on the correct surface height (golden walkway slab #d9a410; red rails w/ posts every ~1.5 m; arrester mast + gold tip; red inverter box + "INV 1" HTML chip on its wall edge) — and set `castShadow`, so they throw VISIBLE shadows that the analytic engine never prices (engine casters are only roofs/parapets/casting-obstructions/panels); `userData.shadowCaster:false` marks them decor | Scene3D.tsx:1355-1455; scene-model.ts:198-263 | POC-DEFECT(visible shadow from site furniture contradicts the engine's exclusion — same class as audit R6's decor rule) | SC.10-7.12 |
| CODE.step6-scene3d.19 | Neighbour buildings toggle (map mode only): deterministic decor seeded from latitude (mulberry32; 14 candidates 18–44 m out, skipped within 14 m of any roof centroid, 4 tints + parapet lip); they cast NO shadows (no castShadow, `shadowCaster:false`) and are excluded from the engine — but NO user-facing text anywhere states they do not cast shadows (only code comments say it); the census/POC doc requires the statement to be made plainly | Scene3D.tsx:240,533-543,1457-1459,1555-1587; scene-model.ts:139-178; grep: no such UI string | POC-DEFECT(the "decor does not cast shadows" statement is never shown to the user) | SC.10-7.17 |
| CODE.step6-scene3d.20 | Map ⇄ Mesh toggle: MAP = model on the calibrated satellite tile (span = `metersPerStaticMap(lat, SAT_ZOOM, 640) × calibration.scaleFactor`, texture at scale 2, SRGB, anisotropy 8, disposed on URL change/unmount) over a 300 m base plane; MESH = isolated studio render — radial-vignette dark background, sun-INDEPENDENT product lighting (key 1.15 + fill 0.35 + hemisphere), 120 m grid, buildings shifted so their collective centroid sits at origin, optional `focusRoofId` isolates one building; sun widget, time bar, buildings toggle hidden in mesh (sun controls are unavailable there — recorded) | Scene3D.tsx:239,411,426-428,521-530,1090-1146,1160-1171,1194-1217,1257-1277 | working | SC.10-7.16 |
| CODE.step6-scene3d.21 | Solar-access view toggle: every panel tinted by its computed access via per-instance color on a flat unlit material lifted 0.02 m (frame hidden so gray doesn't wash the tint). Tint is DISCRETE 3-band: >0.95 green #16a34a, >0.85 amber #ca8a04, else red #dc2626 — while the legend shows a CONTINUOUS red→amber→green gradient labeled ≤85% / 95% / 100%; band-vs-ramp mismatch recorded, not resolved | Scene3D.tsx:237,507-515,650-680,1338-1342; PanelsInstanced.tsx:30-32,64-67,101-112 | working | SC.10-7.14 |
| CODE.step6-scene3d.22 | Per-panel access values come from the HEADLESS engine, not the renderer: `computeSolarAccess` (worker or sync fallback) stamps `panel.solarAccess` after any 2D edit (useDesignSync, s10); the scene only visualizes `p.solarAccess ?? 1` | Scene3D.tsx:1127,1155-1156; shading.ts:153-220; analysis-client.ts:64,79 + workers/analysis.worker.ts:29 (one hop); 23 shading/row-shading tests PASS | working | SC.10-7.14 |
| CODE.step6-scene3d.23 | Headless shading engine (SOLE shading authority): Tier-2 raycast of 12 months × solar hours 4–20 @ 0.5 h (beam-weighted, below-horizon culled), 3 sample points along module depth on the REAL §A0 pose, modules themselves cast (row-on-row loss), self-exclusion by panelId, beam-availability denominator (hours behind the module's plane excluded — orientation is poaBeamRatio's job, not shade); carries the ENGINEER-VALIDATION-REQUIRED banner + documented model limits (linear power, no bypass-diode cliff → partial-shade losses optimistic) | shading.ts:1-146,153-220; tests: shading 8, beam-availability 8, row-shading 7 PASS | working | SC.10-7.12 |
| CODE.step6-scene3d.24 | ONE geometry source for renderer AND engine: `buildShadowCasters` = roof solids (sloped tops), parapet bands (robust per-edge offsets, shared-wall suppression), obstructions per `castsAnalyticalShadow` capability, optional panel slabs (`includePanels`, opt-in per consumer: engine ON / roof heatmap OFF by design); DoubleSide material (backface hits register); userData identity (kind/id/label) on every caster for attribution | scene-model.ts:198-297; 7 scene-model tests PASS incl. "contains EXACTLY roofs + parapet bands + casting obstructions" | working | SC.10-7.12 |
| CODE.step6-scene3d.25 | Per-panel shade attribution: `computePanelShadeDetail` re-runs one panel's rays on demand, attributes each blocked sample to the NEAREST hit (the thing physically in the way), sorted worst-first; parity-tested to equal `computeSolarAccess` exactly; feeds the "SUN LOST TO — tap to look" list in the edit card (StructEditPanel.tsx:144-148,271, agent C's card) | shading.ts:249-327; 10 panel-inspector tests PASS incl. "access matches computeSolarAccess exactly" | working | SC.10-7.20 (engine side; card UI is agent C's row) |
| CODE.step6-scene3d.26 | Blocker → camera swing: `focusBlocker(kind,id)` re-targets OrbitControls at the obstruction (mid-height), panel (roof+0.5) or roof centroid so "shaded by WT1" is a place to look at | Scene3D.tsx:164-188,454; StructEditPanel.tsx:271 (one hop) | working | SC.10-7.20 (camera half) |
| CODE.step6-scene3d.27 | Tap a module → edit card opens for its TABLE (anchor above the panel centroid at roof+2.2 m, panelId carried so the card adds the per-panel sun/energy scope); click filtered from drags (`delta>4` ignored); ONLY panels with a `segmentId` open it — a loose ungrouped panel gets no card (comment: a flush table must stay re-elevatable; recorded); structure editing disabled in readOnly/capture/override surfaces | Scene3D.tsx:142,153-162,1076-1082; PanelsInstanced.tsx:175-188 | working | SC.10-7.20 (open path; card content is agent C's) |
| CODE.step6-scene3d.28 | Edit-card dismissal contract: Escape closes; a TRUE outside click closes (pointer down/up pair, ≤6 px movement, ignores anything touching the card — fixes "steppers close the panel" and orbit-drag kills); card auto-closes if its segment vanishes (undo / delete in 2D); a choice applies INSTANTLY as ONE undoable patch (hover-preview removed by user decision 2026-07-16) | Scene3D.tsx:136-141,189-231 | working | SC.10-7.20 |
| CODE.step6-scene3d.29 | Structure-inspection view state is VIEW-ONLY (never persisted, never fingerprinted — cannot stale a capture): Show/Ghost/Hide + All/Isolate from `structView` partitions the panel set (`partitionPanels`) and filters structures (`visibleStructureIds`); capture mode forces everything visible; ghosting can never move a module (one pose source) | Scene3D.tsx:143-145,1041-1056,1103-1132; structure-view.ts (one hop, agent C's rows SC.10-7.22/23) | working | SC.10-7.22 (render side; option UI is agent C's) |
| CODE.step6-scene3d.30 | Ghost rendering: second `PanelsInstanced` pass with own translucent material (0x8fb8e8, opacity 0.22, no depthWrite, renderOrder 2), frame + heuristic legs suppressed while ghosting; `hidden` bucket simply not drawn | PanelsInstanced.tsx:57,70-90,106,117-134; Scene3D.tsx:1330-1352 | working | SC.10-7.22 |
| CODE.step6-scene3d.31 | Irradiance heatmap toggle (left rail, accent when on): switching on forces map view + top preset and DISABLES rotation (pan/zoom still live); all other rail tools, sun widget, time bar, presets rail and share button hide while it's on; scene collapses to flat satellite + unlit colored cells (no 3D model, no lighting drama — cells read true ramp colors) | Scene3D.tsx:243,281-288,481,495-505,557,580,596,693,1178-1190 | working | SC.10-7.13 |
| CODE.step6-scene3d.32 | Heatmap compute: roof-aligned plan grid (target step 0.5 m, ≤1800 pts, corrective re-step on overshoot), 12 months × solar hours 4–20 @ 0.5 h beam-weighted raycast against the REAL casters (panels deliberately EXCLUDED — it answers a placement question); chunked 150 cells/frame with rAF yield, progress overlay "Computing solar access… X%", abort on toggle-off, result cached by `shadingFingerprint` (instant reopen while the design is unchanged) | solar-heatmap.ts:81-130,144-179,191-330; Scene3D.tsx:246-247,280,289-314,941-962; 15 solar-heatmap tests PASS | working | SC.10-7.13 |
| CODE.step6-scene3d.33 | Heatmap cell metric: colour/% = GEOMETRIC solar access `0.35 + 0.65 × beamFrac` on the FIXED diffuse floor (stable shadow contrast; real monsoon diffuse would wash shadows out — deliberate, test-pinned); continuous red→amber→green ramp (#dc2626→#ca8a04→#16a34a); bands Poor <50% / Moderate <75% / Good <90% / Excellent ≥90% | solar-heatmap.ts:56-73,281-287; poa.ts:14 (one hop); tests "keeps the colour/% metric on the FIXED floor", accessLabel, heatColor PASS | working | SC.10-7.13 |
| CODE.step6-scene3d.34 | Heatmap legend (bottom-right): title "Solar access", gradient bar, qualitative row Poor/Moderate/Good/Excellent, scale row 0%/50%/100%, then current-month summary: bold month + **N% {band} access** + "X.X sun-hours/day avg · geometric"; the kWh/m²·mo received line appears ONLY with fetched weather and alone carries the green "Real · PVGIS" badge — the geometric numbers deliberately carry NO marker (marker law implemented as specified) | Scene3D.tsx:799-905; solar-heatmap.ts:26,36-37 | working | SC.10-7.13 |
| CODE.step6-scene3d.35 | Month track bar (replaces the time bar in heatmap mode): full month name header, Jan–Dec slider 0–11 step 1; recolour is INSTANT — one instanced-color buffer write, no geometry rebuild (HeatmapLayer: one flat 0.98-step quad per cell, roof-aligned yaw, unlit white×instanceColor) | Scene3D.tsx:244,907-937; HeatmapLayer.tsx:18-61 | working | SC.10-7.13 |
| CODE.step6-scene3d.36 | Heatmap kWh layer (only with PVGIS weather): received = `GHI·days × (kd + (1−kd)·beamRatio·beamFrac)` per cell — real monthly diffuse kd (clamped ≤0.9), POA beam-orientation gain memoized per roof orientation, single decomposition (no double-blend); roof-average per month surfaces in the legend | solar-heatmap.ts:232-260,267-295,312-329; tests "no kWh layer without weather…", "≈ the horizontal monthly total (no double-count)" PASS | working | SC.10-7.13 |
| CODE.step6-scene3d.37 | 2D-canvas heatmap parity (the layout screen's VIEW tool, button "Irradiance heatmap", shortcut H): Step6Editor (agent A's file) runs the SAME `computeHeatmap` engine and rasterises the same cells + legend + month scrubber over the 2D canvas | Step6Editor.tsx:219,308-330,1363-1428,3002-3122 (one hop); engine = my solar-heatmap.ts | working | SC.10-6.04 |
| CODE.step6-scene3d.38 | 3D entry point from the layout: "3D view" button in Step6Editor renders `<Scene3D onClose>` (boundary row — the button lives on agent A's screen; possible double-claim recorded, not resolved) | Step6Editor.tsx:1102,1843 (one hop) | working | SC.10-6.65 |
| CODE.step6-scene3d.39 | Copy customer share link: rail button copies `{origin}/share/{shareId}` to the clipboard, tooltip flips to "Link copied" + Check icon for 1.8 s; hidden in readOnly mode AND while the heatmap is on | Scene3D.tsx:242,557-576 | working | SC.10-7.18 |
| CODE.step6-scene3d.40 | Read-only (customer share) surface: `<Scene3D readOnly projectOverride>` — share button hidden, `structInteractive` false so no edit card can open (clicks dead), report still viewable; ShareViewer mounts it exactly so | Scene3D.tsx:111,142,557; ShareViewer.tsx:34 (one hop, s11) | working | SC.10-7.43 |
| CODE.step6-scene3d.41 | Energy report trigger: BarChart3 rail button (hidden in heatmap) opens `EnergyReportSheet` over the scene; report INTERNALS (freshness note, provenance line, monsoon months, losses, financing, Customize/Quick-Generate) live in sitting-6's file — verified present at cite level only | Scene3D.tsx:238,517-519,795-797; EnergyReportSheet.tsx:49-243 (one hop, s6) | working | SC.10-7.15 |
| CODE.step6-scene3d.42 | Capture mode (proposal hero shots, census section 10-8): `captureMode` shows a Capture button → `gl.domElement.toDataURL('image/jpeg', 0.85)` labeled "{preset} {fmtHour(hour)}"; `initial` props preset date/hour/solarAccess and `initialViewMode`/`focusRoofId` stage the shot; capture forces all modules visible (never a bare frame) | Scene3D.tsx:107-124,405-409,682-690; structure-view.ts effectiveView (one hop); Step7Proposal.tsx:86-88 (one hop, s7) | working | NONE (10-8 rows are step-7's sitting) |
| CODE.step6-scene3d.43 | Roof rendering: sloped-solid BufferGeometry (world convention (x, up, −y); flat roof = old flat box), explicit top-ring outline (not EdgesGeometry — walls would all highlight), parapet bands extruded at eave height (pitched roofs skip parapets), photoreal concrete #ddd8cf in map view vs per-roof soft tint in mesh view; all geometries disposed on rebuild | Scene3D.tsx:1481-1553; scene-model.ts:40-127; eave-ref-plane 4 PASS | working | SC.10-7.12 (caster geometry; roof-shape rows are sitting 2's) |
| CODE.step6-scene3d.44 | Panels render as ≤3 instanced draws for the whole site (glass/frame/legs — was ~3 draw calls PER panel): unit box scaled per instance, matrix = T(center)·Ry(yaw)·Rx(−tilt) exactly; heuristic 2-leg stands only for elevated non-structured modules (parametric tables draw real legs instead, `legs:false`); frustumCulled off (unit bounds wrong); GPU buffers disposed on rebuild | PanelsInstanced.tsx:1-190; Scene3D.tsx:1107-1132,1303-1352 | working | NONE (performance architecture beyond census) |
| CODE.step6-scene3d.45 | ONE frame for mesh/engine/2D: `panelInstanceMatrix` (T·Ry·[Rx(−tilt)]·T·S) is shared by the renderer, the analytic shadow slabs and the one-frame gate; `panelPlanCorners` projects the drawn module back to plan (y=−z) with cos(tilt) foreshortening for parity with `panelCornersOnRoof` | scene-frame.ts:40-87; PanelsInstanced.tsx:37; scene-model.ts:278-283; one-frame 12 + frame-parity 11 PASS | working | NONE (§A0 gate, internal guarantee) |
| CODE.step6-scene3d.46 | Shared panel materials/texture: one 256×384 CanvasTexture (navy 6×10 cell grid + sheen + vignette) + 3 singleton materials (physical glass w/ clearcoat 0.7, aluminum frame, leg) for every panel — module-level cache, never disposed by design | textures.ts:9-76 | working | NONE (visual asset detail) |
| CODE.step6-scene3d.47 | Procedural steel-section geometry: exact extruded cross-sections for c/u/l/z/hat/rhs/chs from `SectionDims` (mm→m), `bevelEnabled:false` (a bevel would shrink a quoted mass), UNIT length extruded +Y and centred on its axis (uncentred members visibly overshot — now test-pinned), stretch by Y-scale ONLY (cross-section scale would thin a C-channel's web); per-profile cache + SVG glyph path from the SAME outline | profile-geometry.ts:1-209; profiles.test.ts 32 PASS | working | SC.10-7.24 (glyph/geometry side; picker UI is agent C's row) |
| CODE.step6-scene3d.48 | Structure graph re-derivation keyed on `layoutFp` (geometry+racking only) not the project object — unrelated patches (price, note) no longer rebuild every structure; racking resolved for ALL structured segments (isolating one table cannot move another's modules) | Scene3D.tsx:1029-1072 | working | NONE (performance/correctness beyond census) |
| CODE.step6-scene3d.49 | Obstruction placement in 3D delegates grounding to `obstructionBaseY` (position-resolved, stale-anchor healed — the turbine-vent-hanging bug) and streams only the GLB types the project uses (`useWarmObstructionAssets`) | Scene3D.tsx:1084-1086,1292-1301; ObstructionMesh.tsx + ground.ts (one hop, s3/s5-C); obstruction-assets 8 PASS | working | NONE (s3's census rows; cited) |
| CODE.step6-scene3d.50 | GPU hygiene: satellite texture, roof/top-ring/parapet geometries, instanced meshes, heatmap quads and ghost/access materials all disposed on change or unmount; engine caster groups disposed in `finally` | Scene3D.tsx:1169-1171,1518-1527; PanelsInstanced.tsx:82-90,164-173; HeatmapLayer.tsx:24; shading.ts:216-218,324-326; scene-model.ts:299-307 | working | NONE (resource discipline) |

## CENSUS-UNMATCHED (agent B half: 3D-view / shadow / heatmap rows of SC.10-6 + SC.10-7)

My half = `SC.10-6.04`, `SC.10-6.65`, `SC.10-7.01`–`SC.10-7.19`, `SC.10-7.43` (rows `SC.10-7.20`–`.31` are agent C's structure/edit-card half; `SC.10-6.01`–`.03`, `.05`–`.64`, `.66` are agent A's layout half).

| Census entry | Status |
|---|---|
| — | none: every row of my half has a code match above (two carry POC-DEFECTs in-row: SC.10-7.04 visible-equivalents gap, SC.10-7.17 missing decor-shadow statement) |

Boundary records (not resolved here): `SC.10-7.32`–`SC.10-7.41` + `SC.10-7.42` (energy-report internals & chart colours) sit in `components/EnergyReportSheet.tsx` — file-claims ledger assigns it to **sitting 6**, and it is outside the A/B/C split of this sitting; verified present at cite level (row 41). `SC.10-6.65` may be double-claimed by agent A (button lives on their screen). `SC.10-6.04` matched here because the heatmap ENGINE is mine even though the 2D raster lives in agent A's Step6Editor.

## Summary

- **rowCount:** 50
- **defectCount:** 4 — CODE.step6-scene3d.6 (SC.10-7.04: no visible on-screen orbit/zoom equivalents, gestures only) · .8 (date picker shows UTC-sliced date, lags local day before ~05:30 in TZ>UTC) · .18 (walkways/rails/arresters/inverters cast visible shadows the engine never prices) · .19 (SC.10-7.17: "decor buildings don't cast shadows" never stated in the UI)
- **censusUnmatched:** 0 of my half (22 rows: SC.10-6.04, SC.10-6.65, SC.10-7.01–.19, SC.10-7.43 — all matched; 2 matched-with-defect)
- **newBeyondCensus:** 7 product-relevant (rows 14 solar-time basis · 16 dusk/night/sun-marker · 42 capture staging · 44 instancing · 45 one-frame gate · 46 textures · 48 layoutFp keying) + 2 hygiene rows (49, 50)
- **notes:**
  1. All 139 tests across the 13 suites I ran PASS (read-only run); no failing test = no test-derived defects.
  2. Scene3D.tsx has NO direct component/DOM test — every UI behavior above is evidenced by code + lib tests only.
  3. Scene3D.tsx carries 8 dead imports (computePanelShadeDetail, panelEnergyShares, STRUCTURE_PROFILES, panelFootprintM, StructurePreview, roofGridAngle, foundationOptionsFor, isSloped) — the first two now live in StructEditPanel; hygiene only.
  4. Turning the heatmap off does NOT restore the previous view mode/camera (stays map + top); census is silent on restore.
  5. Keyboard-vs-drag camera clamps differ slightly (row 5); OrbitControls re-clamps radius, elevation floor differs by ~0.018 rad.
  6. Solar-access legend implies a continuous ramp; panel tint is 3-band discrete (row 21) — recorded, not resolved.
  7. Heatmap % can never read below 35% (fixed diffuse floor) though the legend scale starts at 0% — deliberate and test-pinned (row 33).
  8. Loose (unsegmented) panels open no edit card in 3D (row 27) — relevant to agent C's SC.10-7.20 verdict.
  9. Structure members/nodes shade visually but not analytically — documented deliberate with a geometric argument (StructureInstanced.tsx:22, s5-C's file).
  10. Mesh (studio) view has no sun controls at all — its lighting is sun-independent by design (row 20).
