# Feature inventory — share (the customer-facing proposal document + the read-only 3D share viewer)

Pass-two inventory, 2026-08-05 (Sitting 11). Primary files: `src/features/solar-studio/screens/ProposalView.tsx` (591 ln) · `screens/ShareViewer.tsx` (50) · `components/BlobImg.tsx` (36) · the two route files `src/app/(studio)/proposal/page.tsx` (3) and `src/app/(studio)/share/[shareId]/page.tsx` (9). `src/app/(studio)/StudioClientLayout.tsx` (149) and `src/features/solar-studio/router.ts` (54) are **shared shell** — the parts that decide who may reach these two routes are inventoried here (§A) because no other sitting owns the share/proposal half of the guard; the wizard/projects half is sitting 10's.

**Test run** (read-only, deps from lockfile, vitest 3.2.7, node 22.22.3): there is **no test anywhere in the repo that renders `ProposalView`, `ShareViewer` or `BlobImg`** — verified by `grep -rn "ProposalView\|ShareViewer\|BlobImg" src/` (only the three source files and Step 7's own use of `BlobImg` match). The closest covering suites were run: `npx vitest run lib/__tests__/{proposal-narrative,finance,financing,sld,fingerprints,project-duplicate,bom-money,persistence}.test.ts store/store.test.ts` → **9 files, 130 tests, ALL PASS** (770 ms). **No failing test ⇒ no test-derived DATA finding.** The zero-coverage fact is itself recorded (row 97); every behavioural claim below is read off the source, not off a test.

**One-hop files consulted but owned elsewhere** (cited, never re-inventoried): `three/Scene3D.tsx` (`readOnly` / `projectOverride`, the copy-link tool button, the whole viewer chrome the share link inherits) — sitting 5 · `components/EnergyReportSheet.tsx`, `lib/solar.ts` `computeEnergyReport`, `lib/finance.ts` `computeFinancials`, `lib/financing.ts` `computeFinancing`, `lib/proposal-narrative.ts` — sitting 6 · `lib/bom.ts` `mergedBom`/`bomMoney` + `lib/bom/money.ts` + `data/pricebook.ts` `DEFAULT_MARGIN_PCT` — sitting 8 · `lib/sld.ts` `effectiveSld` — sitting 7 · `lib/structure.ts` `engineeringStatus`/`projectStructures`/`STRUCTURE_DISCLAIMER`/`windZoneInfo` — sitting 5 (structures) · `lib/fingerprints.ts` `isShadingFresh`/`capturesFresh`, `lib/review.ts`, `screens/Step10Done.tsx`, `lib/project-duplicate.ts` — sitting 9 · `lib/persistence/{useImage,blobs}.ts` + `store/store.tsx` + `store/useDesignSync.ts` — sitting 10 · `lib/units.ts` `useUnits`/`fmtArea` — sitting 1 · `screens/Step7Proposal.tsx` (`Generate proposal` → `/proposal`) — sitting 6.

**Census scope note.** The ledger assigns this sitting "10-8 share rows, 10-11 link rows". The census's §10.8 is Step 7's *capture + readiness* screen (sitting 6) and §10.11 is the Done screen + engineer sign-off (sitting 9); **the proposal DOCUMENT itself is not a studio-census subject at all** — it belongs to Phase 3 / the proposal builder (`SC.10-8.19` hands numbers to it, `docs/phase-10-prompts.md:91-92`). Consequently most rows below are legitimately `NONE` and counted as new-beyond-census, not as census misses. The census rows this sitting actually matches are: the link surfaces `SC.10-7.18` (10 rows), `SC.10-11.05` (7) and `SC.10-7.41` (2); the shadow set `SC.10-8.16` (6); the staleness law `SC.10-8.20` (5); the cover preview `SC.10-8.15` (2); the system summary `SC.10-8.17` (1); the readiness verdict `SC.10-8.13` (1); the Generate-proposal hand-off `SC.10-8.19` (2); the Done "View proposal" action `SC.10-11.02` (1); plus three cross-refs whose owning rows sit elsewhere — `SC.10-11.18` (unapproved design never shown, sitting 9), `SC.10-6.50` (structure disclaimer, sitting 5), `SC.10-9.35` (Print / Save as PDF, sitting 8).

**Area-level style note** (recorded once, not per row): both screens are 100 % inline `style={{}}` with raw hex and px (`#fffbeb`, `#f59e0b`, `#92400e`, `#1d4ed8`, `#2563eb`, `#fff`, `#f0fdf4`, `#fef2f2`, `#15803d`, `#b91c1c`, `rgba(0,0,0,0.8)`) — the legacy studio path, not the `.ds` token contract of `docs/DESIGN-SYSTEM.md` / CLAUDE.md rule 1.

---

## §A — Routes, guards and who may reach these two surfaces

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.1 | Two distinct routes exist and they are **not the same surface**: `/proposal` renders the printable proposal document (`ProposalView`, no params), and `/share/{shareId}` renders the 3D viewer (`ShareViewer`). Both are `'use client'` pages under the `(studio)` route group; the proposal page is a bare re-export (`export { ProposalView as default }`) and takes no props at all | `src/app/(studio)/proposal/page.tsx:1-3`; `src/app/(studio)/share/[shareId]/page.tsx:1-9` | working | NONE |
| CODE.share.2 | **`/share/*` is the ONE route exempt from the login guard.** `RouteGuards` returns early — `if (route.name === 'share') return;` — before any `state.user` check, so an anonymous visitor reaches the 3D viewer with no login, no OTP, no token exchange and no account | `StudioClientLayout.tsx:118-120` | working (matches `F5-01` "the customer never logs in", and `Q42`'s default-OFF posture by accident: **no challenge of any kind exists to be turned on**) | SC.10-7.18, SC.10-11.05 |
| CODE.share.3 | **`/proposal` is NOT a customer surface.** It is guarded twice: `!state.user` ⇒ redirect `/login`, and `route.name === 'proposal' && !project` ⇒ redirect `/projects`. The customer-facing proposal page of `F5-33`/`Q27` does not exist in the POC — the document is an operator screen the operator prints | `StudioClientLayout.tsx:118-126` | working (divergence: `Q27` puts a **"View in 3D" button inside the proposal link with no second link**; the POC has the inverse — a login-gated document that *points at* a second, separate anonymous URL) | NONE |
| CODE.share.4 | Both routes render nothing until `state.hydrated` (`return state.hydrated ? <>{children}</> : null`), because project state lives in `localStorage` and server/first-client render must match. Consequence: the share link's first paint is a **blank page with no spinner, no skeleton and no "loading" text** for the duration of the async `loadState()` (v1→v2 migration + image hoisting) | `StudioClientLayout.tsx:128-131`; `store/store.tsx:324-337` | POC-DEFECT(anonymous share link paints blank with no loading state while storage hydrates — the customer's first impression of the differentiating 3D moment is an empty screen) | NONE |
| CODE.share.5 | The `/proposal` guard is an **effect**, and `ProposalView` dereferences the project during **render** (`useActiveProject()!` then `computeEnergyReport(project)` → `project.panels`). On the commit where `hydrated` flips true with `activeProjectId` unset or pointing at a deleted project, the child renders *before* the redirect effect fires and throws `TypeError: Cannot read properties of null`. **There is no `error.tsx`, no `global-error.tsx` and no `ErrorBoundary` anywhere in the app**, so the failure is a white screen | `ProposalView.tsx:47-48`; `lib/solar.ts:115-116` (one hop); `StudioClientLayout.tsx:118-131`; `find src/app -name "error.tsx" -o -name "global-error.tsx"` → empty; `grep -rn "ErrorBoundary\|componentDidCatch" src/` → empty | POC-DEFECT(deep-linking `/proposal` with no active project crashes the render before the guard can redirect; no error boundary catches it) | NONE |
| CODE.share.6 | `useRoute()` classifies `/share/{id}` as `{name:'share', shareId}` **only when the id is non-empty**; an empty id falls through the whole chain and returns `{name:'login'}`, which re-arms the login guard. In practice Next's dynamic segment guarantees a non-empty value, so the fallback is unreachable from a real URL | `router.ts:36-53` | working (dead branch recorded) | NONE |
| CODE.share.7 | `DesignSync` (per-panel shading recompute) and `useHealthSync` are mounted at the **layout** level, explicitly so a stale project heals "on EVERY studio route — wizard, proposal, share, dashboard". They operate on `useActiveProject()`, i.e. **the operator's active project, not the project the share link addresses**. Opening a share link on the owner's device therefore silently runs the shading engine against, and `patch()`es, a *different* project | `StudioClientLayout.tsx:10-19`; `store/useDesignSync.ts:1-13,15-16,25-60` (one hop) | POC-DEFECT(a read-only viewer route triggers background recompute + persisted writes on an unrelated project) | NONE |
| CODE.share.8 | The `PersistStatusChip` ("Not saved — browser storage is full/unavailable" + Retry) and the `ExternalConflictBanner` ("This project was updated in another tab — showing the latest version.") are rendered by the layout on **every** route including `/share`, so an anonymous customer can be shown the operator's storage-failure alert and multi-tab conflict notice | `StudioClientLayout.tsx:26-113,140-146` | POC-DEFECT(operator-only infrastructure alerts leak onto the anonymous customer link) | NONE |

## §B — `ShareViewer` · the read-only 3D share target (the QR/link destination)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.9 | Resolution is a **linear scan of the local store**: `state.projects.find(p => p.shareId === shareId)`. There is no fetch, no token verification, no server and no backend of any kind — the link resolves only on a browser whose `localStorage` already holds that project | ShareViewer.tsx:7-8 | working (POC limitation, stated in-product — see row 12) | SC.10-7.18, SC.10-11.05 |
| CODE.share.10 | The share token is **12 characters**: `crypto.randomUUID().replace(/-/g,'').slice(0,12)` (48 bits of hex), falling back to `Math.random().toString(36).slice(2,12)` where `crypto.randomUUID` is unavailable — a **non-cryptographic** fallback. It is minted once at project creation and stored on the project | store/store.tsx:79-83,129 (one hop); types.ts:995 | working (recorded: truncated-UUID token, non-CSPRNG fallback path) | NONE |
| CODE.share.11 | A share id is **never rotated, never revoked and never expires**. The only path that changes one is `duplicateProject`, which mints a fresh `shareId` for the copy precisely so "the copy [does not] silently overwrite what a customer already received" | lib/project-duplicate.ts:5-8,44 (one hop) + project-duplicate.test.ts:35-36 "copy.shareId !== src.shareId" | working (absence recorded: no revoke, no re-mint, no expiry, no per-recipient naming — see §G) | NONE |
| CODE.share.12 | **Not-found state**: a full-viewport centred column (`100vh`, gap 10, `var(--ink-3)`) with a 40 px `SearchX` glyph (`strokeWidth 1.5`, `aria-hidden`), the bold line **"Shared design not found"**, and a 13 px explanation **"This link is invalid or the design isn't on this device (POC stores locally)."** — names the cause honestly and names the POC as the reason | ShareViewer.tsx:10-30 | working | NONE |
| CODE.share.13 | The not-found guard is `!project \|\| !project.location` — a real, correctly-addressed design that has not yet had a location set renders the **same "link is invalid"** message as a wrong id. Two different situations, one wrong answer | ShareViewer.tsx:10 | POC-DEFECT(a valid link to a location-less design is reported to the customer as an invalid link) | NONE |
| CODE.share.14 | **No status, sign-off or readiness gate whatsoever.** Any project with a `location` is served — `status: 'in_progress'` designs, designs with zero panels, designs whose electrical gate fails, designs with no engineer verification. `project.status` and `project.structuralVerification` are never read by this screen | ShareViewer.tsx:8-10 (no other reads); types.ts:20,994 | POC-DEFECT(an unapproved / half-finished design is served to the customer link unchanged — the direct counterpart of `SC.10-11.18` "an unapproved design is never shown to the customer") | SC.10-11.18 (cross-ref; the rule's studio row is sitting 9's) |
| CODE.share.15 | The found state is `position:absolute; inset:0` holding exactly two things: `<Scene3D readOnly projectOverride={project} />` and one overlay caption block at `top:14 left:70 z:60` | ShareViewer.tsx:32-48 | working | SC.10-7.18 |
| CODE.share.16 | The caption prints **two lines in white with `textShadow 0 1px 3px rgba(0,0,0,0.8)`**: `project.info.name` (15 px / weight 800) and `project.info.customerName` (12 px, opacity 0.85). `info.name` is the **internal design name** (default `'New Solar Project'`, and `'… (copy)'` after a duplicate) — the operator's own label, shown verbatim to the customer. When `customerName` is blank an empty 12 px div renders | ShareViewer.tsx:44-47; store/store.tsx:88 + lib/project-duplicate.ts:22-30 (one hop) | POC-DEFECT(the operator's internal design/variant name, including "(copy)", is printed on the customer-facing viewer) | NONE |
| CODE.share.17 | `left: 70` is a hard-coded clearance for the left tool rail (`left: 14`). The caption is not in a layout relationship with the rail — a rail width change silently overlaps the customer's name | ShareViewer.tsx:39; Scene3D.tsx:485 (one hop) | working (brittleness recorded) | NONE |
| CODE.share.18 | There is **no branding on the viewer**: `project.info.logoDataUrl` is never rendered here (only the proposal document renders it), and no tenant colour, footer, contact or call-to-action exists. The 3D viewer is unbranded and terminal — it links nowhere | ShareViewer.tsx (whole file); cf. ProposalView.tsx:188-190 | working (divergence: `F7-07` puts tenant branding on customer-link pages; `F5-33` makes the 3D an element of the proposal page rather than a dead-end) | NONE |
| CODE.share.19 | `readOnly` reaches only **three** things inside `Scene3D`: it forces `structInteractive = false` (no on-object structure edit card, no `patchProject`), it hides the **"Copy customer share link"** tool button, and it is forwarded to `EnergyReportSheet` (which then hides its two navigation buttons). **Nothing else in the viewer chrome is gated by it** | Scene3D.tsx:111-112,120-121,127,142,449,557-575,796; EnergyReportSheet.tsx:16,20,233 (one hop) | working | SC.10-7.18, SC.10-7.41 |
| CODE.share.20 | Consequently the anonymous customer link inherits the **full editor viewer chrome**, live: solar-access heatmap toggle · per-panel solar-access tint toggle · **Energy report** button · map/mesh (studio render) toggle · neighbour-buildings toggle · sun-path toggle · Top/Isometric/Front view presets · the sun compass widget (azimuth/altitude, date, hour) · the solar-access legend (≤85 % / 95 % / 100 %) · the whole bottom time bar (Winter/Summer/Equinox/Today chips, a `type="date"` picker, play/pause sun animation, a 5 AM–7 PM hour slider at 0.05 steps, sunrise/sunset readouts) · full orbit/zoom/pan (`minDistance 8`, `maxDistance 170`) | Scene3D.tsx:484-575,578-592,594-648,650-672,689-793 (one hop) | working (matches `docs/phase-10-prompts.md:1080` "READ-ONLY … no edit controls, no share button" literally; recorded because the surface handed to a customer is an engineering instrument, not a customer artefact) | SC.10-7.18 |
| CODE.share.21 | The **X "Back to 2D editor"** button is absent on the share link — not because of `readOnly` but because `ShareViewer` passes no `onClose`, and that button is rendered `{onClose && …}` | ShareViewer.tsx:34; Scene3D.tsx:487-494 (one hop) | working | NONE |
| CODE.share.22 | **The customer can open the Energy report from the share link**, and it shows money: `Net Cost ₹Nk` with `after ₹Nk subsidy`, `Yearly Savings ₹Nk @₹{tariff}/kWh`, `Payback N.N years`, and — whenever `systemCostInr > 0` — the four **FINANCING OPTIONS** cards (Cash / Loan / Lease / PPA) with headline, note, `yr-1 net ±₹Nk`, and the line "every option finances the same ₹Nk system". `readOnly` removes only the two action buttons at the foot (**Customize Proposal**, **Quick Generate**) | Scene3D.tsx:796; EnergyReportSheet.tsx:190-232,233-245 (one hop); lib/financing.ts:61,78,92,106 (one hop) | working (recorded: an ungated anonymous URL renders priced figures. No installer subtotal or margin % is exposed — those live only in `ProposalView`'s engineering line, row 47) | SC.10-7.41 |
| CODE.share.23 | The share viewer carries **no staleness signal of any kind**. `isShadingFresh` / `capturesFresh` are never called here; a design whose per-panel solar access was computed for an older geometry renders its provisional numbers (the optimistic `1.0` default, or an older scene's values) through the solar-access tint, the heatmap and the energy report with nothing to say so | ShareViewer.tsx (no import); cf. ProposalView.tsx:18,75-76; lib/fingerprints.ts:301-303 (one hop) | POC-DEFECT(the customer-facing surface is the only one in the studio with no staleness marker — direct conflict with `SC.10-8.20` and the F8 honesty posture) | SC.10-8.20 |
| CODE.share.24 | The share viewer carries **no structure disclaimer**. `STRUCTURE_DISCLAIMER` ("Structural design subject to certified engineer verification (IS 875 wind load, roof capacity, anchoring).") reaches the proposal document (row 49) but not the 3D view, which renders mounting tables, legs and foundations as finished geometry | ShareViewer.tsx (no import); lib/structure.ts:74-75 (one hop) | POC-DEFECT(structure-bearing output shown to a customer without the disclaimer that CLAUDE.md's domain rules say travels with every such output) | NONE |

## §C — `BlobImg` · the shared image primitive of both customer surfaces

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.25 | Props: `blobId: string \| null \| undefined`, `alt: string`, optional `style`, optional `placeholderHeight` defaulting to **110**. It resolves the id through `useImage(blobId)`, which peeks a synchronous in-memory cache first (no flicker on remount), else clears the previous URL and awaits `getImage`, resolving a read failure to `null` rather than an unhandled rejection | BlobImg.tsx:10-35; lib/persistence/useImage.ts:9-40 (one hop); blobs.ts:58-74 (one hop) | working | NONE |
| CODE.share.26 | With no resolved source it renders a **`<div>`**, never a broken `<img>`: `height: placeholderHeight`, `borderRadius: 8`, `background: var(--paper-3)`, `aria-label = "{alt} (image unavailable)"`, with the caller's `style` spread **after** the defaults (so a caller-supplied height wins) | BlobImg.tsx:22-33 | working | SC.10-8.15, SC.10-8.16 |
| CODE.share.27 | **Loading and permanently-missing are the same visual.** A blob still being read from IndexedDB and a blob that does not exist on this device both render the identical grey box; nothing ever tells the reader which it is, and there is no retry | BlobImg.tsx:22-33; useImage.ts:20-38 (one hop) | POC-DEFECT(on a share/proposal opened where the blobs do not exist — the file's own stated case — the customer document is silently full of grey rectangles with no explanation) | NONE |
| CODE.share.28 | The `aria-label` sits on a bare `<div>` with no `role` and no text content. A `div` without a role has no accessible name in most AT mappings, so "image unavailable" is not reliably announced | BlobImg.tsx:23 | POC-DEFECT(the only accessible signal for a missing image is attached to a role-less element) | NONE |
| CODE.share.29 | The file's docstring claims the placeholder keeps "a neutral placeholder box of **the same footprint** so print/layout stay stable". It is not the same footprint: `placeholderHeight` applies **only** to the placeholder branch, and the `<img>` branch carries no `height`, no `width`/`height` attributes and no `aspect-ratio` — so the page reflows the moment a real image resolves | BlobImg.tsx:5-9 vs 22-35 | POC-DEFECT(documented layout-stability guarantee is not implemented; print pagination shifts between placeholder and image) | NONE |
| CODE.share.30 | The `<img>` branch sets `src`, `alt` and `style` only — no `loading`, no `decoding`, no dimensions. Sources are data URLs, so no network fetch is involved | BlobImg.tsx:35 | working | NONE |

## §D — `ProposalView` shell · staleness banner, toolbar, audience gate, print contract

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.31 | Page root: `background: var(--paper-2)`, `minHeight: 100vh`. The document body is a `maxWidth: 820` centred column, `margin '20px auto 60px'`, `padding '0 14px'`, `display:flex; flex-direction:column; gap:20` — pages are stacked cards, scrolled, with no pager, no table of contents and no page count | ProposalView.tsx:79,175 | working | NONE |
| CODE.share.32 | Everything is recomputed **on every render, unconditionally and unmemoised**: `computeEnergyReport` → `computeFinancials` → `mergedBom` → `bomMoney` → `computeFinancing` → `effectiveSld` → `proposalNarrative` (which internally re-runs `computeEnergyReport` **and** `computeFinancials` a second time) → `projectStructures`/`engineeringStatus`/`windZoneInfo` (the latter two called **3× and 2×** inline in the same JSX block). Because everything derives from the current project at render time, the document can never print a stale *money* figure | ProposalView.tsx:48-52,55-57,68,70; lib/proposal-narrative.ts:30,33 (one hop); ProposalView.tsx:413-419 | working (redundant recompute recorded; the honesty property is a side effect of it) | NONE |
| CODE.share.33 | **Staleness banner** — rendered when `!isShadingFresh(project) \|\| !capturesFresh(project)`. `className="no-print"`, `role="alert"`, centred flex, `background #fffbeb`, `border-bottom 1px solid #f59e0b`, `color #92400e`, `padding 9px 20px`, `font-size 12.5`. Exactly one of two messages, shading first: (a) `<RefreshCw size=14 className="spin">` + *"Shading is recalculating after recent edits — wait a moment before printing so the energy numbers reflect the final design."*; (b) `<Camera size=14>` + *"The design changed after the 3D images were captured — the pictures no longer match the numbers."* + a **Retake captures** `btn-secondary` (`padding 3px 10px`, 12 px) that navigates to `/wizard/7` | ProposalView.tsx:75-76,81-118; lib/fingerprints.ts:301-303,311-316 (one hop); theme.css:429 (`.spin`) | working | SC.10-8.20 |
| CODE.share.34 | The banner is **advisory only** — no control is disabled, Print is never blocked, and printing while shading is recalculating is permitted with one click | ProposalView.tsx:81-118 vs 159-161 | working (the in-file comment names it "soft block") | SC.10-8.20 |
| CODE.share.35 | **The staleness banner is `no-print`.** The printed PDF — the artefact that actually reaches the customer — therefore carries **no** indication that its energy figures are provisional or that its pictures predate the design. The only honesty marker in the whole document is on the screen the customer never sees | ProposalView.tsx:82,169 (`.no-print { display:none !important }`) | POC-DEFECT(the staleness warning is deleted from the only output that leaves the building — a direct failure of `SC.10-8.20` "must say so, not silently show an out-of-date picture") | SC.10-8.20 |
| CODE.share.36 | Toolbar: `className="no-print"`, `position: sticky; top: 0; z-index: 10`, `justify-content: space-between`, `padding 10px 20px`, `background var(--paper)`, `border-bottom 1px solid var(--line)`. Left: **Back** (`btn-ghost`, `ArrowLeft` 15). Right group (`gap 10`): audience toggle · SLD toggle (conditional) · **Copy 3D link** · **Print / Save PDF** | ProposalView.tsx:119-163 | working | NONE |
| CODE.share.37 | **Back always goes to `/wizard/7`**, a literal — regardless of where the reader arrived from. The four entry points are Step 7's "Generate proposal", Step 10 Done's "View Proposal", the Dashboard row menu's "Show Proposal", and the Energy report's "Quick Generate"; three of the four are returned to a wizard step they did not come from | ProposalView.tsx:133-135; Step7Proposal.tsx:371, Step10Done.tsx:61, Dashboard.tsx:277, EnergyReportSheet.tsx:241 (one hop) | POC-DEFECT(Back is hard-wired to Step 7 and lands three of four entry paths somewhere they were not) | NONE |
| CODE.share.38 | **Audience toggle** (`btn-secondary`): a two-state switch whose label prints the **current** state — `'Customer doc'` when `audience === 'customer'`, `'Engineering doc'` otherwise — with `title="Customer doc hides your subtotal & margin; engineering doc shows the full breakdown"`. It is component-local `useState` initialised to `'customer'`, so **every mount and every reload returns to the customer view** — the in-file comment calls this "the safe leak" | ProposalView.tsx:64,137-143 | working (a toggle labelled with its state rather than its action is ambiguous; the default is deliberately the safe one) | NONE |
| CODE.share.39 | The audience switch changes **exactly one line of the document** — the money line under the BOM table (row 47). The BOM table itself, the financing table, the energy report and every other page are byte-identical in both audiences, so the tooltip's promise that the engineering doc "shows the full breakdown" overstates what the switch does | ProposalView.tsx:139-140 vs 444-474 (the only `audience` read in the file) | working (divergence: control copy promises more than the control delivers) | NONE |
| CODE.share.40 | **SLD page toggle** is offered only when `canSld = !!effectiveSld(project) && project.strings.length > 0`. Label and style flip with state: `btn-primary` + `'SLD page: on'` when included, `btn-secondary` + `'Add SLD page'` when not; `title="Add an electrical single-line-diagram page (for banks / DISCOM)"`. Default **off**, local state, resets on reload | ProposalView.tsx:67-69,144-152 | working | NONE |
| CODE.share.41 | **Copy 3D link** (`btn-secondary`, `Link2` 15) calls `navigator.clipboard.writeText(shareUrl)` with **no `.then`, no `.catch`, no confirmation and no state change** — the button gives the operator no feedback that anything happened, and a rejected promise (permission denied) is an unhandled rejection. In a non-secure context `navigator.clipboard` is `undefined` and the click throws a `TypeError` into an app with no error boundary | ProposalView.tsx:153-158; cf. Scene3D.tsx:563-572 (one hop), which *does* confirm with a `Check` icon and a "Link copied" tip for 1800 ms | POC-DEFECT(silent copy: no success feedback, no failure handling, and the sibling implementation in Scene3D does both) | SC.10-11.05 |
| CODE.share.42 | Three separate places build the share URL by the same string concatenation — `` `${location.origin}/share/${project.shareId}` `` — in `ProposalView`, `Step10Done` and `Scene3D`. There is no shared helper, so the link format is duplicated three ways | ProposalView.tsx:73; Step10Done.tsx:23, Scene3D.tsx:566 (one hop) | working (duplication recorded) | SC.10-7.18, SC.10-11.05 |
| CODE.share.43 | **Print / Save PDF** (`btn-primary`, `Printer` 15) calls `window.print()` directly — no dialog, no filename control, no server render, no queue. The print stylesheet is injected inline: `-webkit-print-color-adjust: exact !important` on `*` (so the white-on-blue hero does not print white-on-white), `@page { size: A4; margin: 10mm }`, `.no-print { display:none !important }`, `.page { box-shadow:none; margin:0 auto; page-break-after: always }`, `break-inside: avoid` on page images/tables/cards, `body { background: #fff }` | ProposalView.tsx:159-161,165-173 | working | NONE |
| CODE.share.44 | `page-break-after: always` is applied to **every** `.page` including the last, which produces a trailing blank sheet in browsers that honour it | ProposalView.tsx:170 | POC-DEFECT(the printed proposal ends on an empty page) | NONE |

## §E — Page 1 · cover, branding and the 3D-link block (the Q27 surface)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.45 | Headline: **`{r.capacityKwp}kW On-grid Solar Proposal`** at 26 px / weight 800. The unit is **`kW`** here and **`kWp`** for the same number in the System Summary two pages later; `On-grid` is a hard-coded literal — the product has no off-grid/hybrid concept to contradict it | ProposalView.tsx:180-182 vs 337 | working (unit inconsistency inside one document recorded) | NONE |
| CODE.share.46 | Sub-line, 13 px `var(--ink-3)`: **`Prepared for {customerName or 'Customer'} · {location?.address}`**. A blank customer name falls back to the literal word **"Customer"**; a project with no location prints the separator followed by nothing | ProposalView.tsx:183-186 | working (the dangling " · " on a location-less project recorded) | NONE |
| CODE.share.47 | Tenant branding is **one image**: `project.info.logoDataUrl` rendered `maxHeight: 46` with `alt="logo"` when set, nothing when not. No brand colour, no letterhead, no footer identity — the page footer says "Generated by Solar Studio" instead (row 59). `alt="logo"` names the element type rather than the organisation | ProposalView.tsx:188-190 | working (divergence: `F7-07` puts a tenant logo **and** a primary brand colour on customer-facing documents) | NONE |
| CODE.share.48 | Cover image: rendered **only if `project.coverImageBlobId` is set** — `BlobImg` at `width:100%`, `borderRadius:12`, `margin:'18px 0'`, `placeholderHeight: 260`. Unlike Step 7's review, there is **no "no cover captured yet" empty state**: a design with no cover simply has a missing block, unremarked | ProposalView.tsx:192-199; cf. Step7Proposal.tsx:231-255 (one hop, sitting 6) | working (missing empty state recorded) | SC.10-8.15 |
| CODE.share.49 | **3D DESIGN MODEL card** — `display:flex; gap:16; align-items:center`, `1px solid var(--line)`, radius 12, padding 16. Left column: bold 14 px title `3D DESIGN MODEL`; 12 px `var(--ink-3)` body *"Explore an interactive 3D model of your solar rooftop with panel layout and shadow simulation."*; the share URL as a **bare `<a href>` printing its own href** at 12 px / weight 700; then 10.5 px `var(--ink-3)` **"Viewable on the device this design was created on."** Right column: the QR at 96 px | ProposalView.tsx:200-226 | working | SC.10-7.18, SC.10-11.05 |
| CODE.share.50 | The printed caveat **"Viewable on the device this design was created on."** is an honest, customer-visible statement of the POC's device-local storage limit — the same fact the not-found screen states (row 12). It is also, read plainly, an admission printed on a sales document that the link will not work for the reader | ProposalView.tsx:219-221 | working (honesty preserved; recorded as the POC's own disclosure, not a defect) | NONE |
| CODE.share.51 | The link is a plain `<a href={shareUrl}>` with **no `target`, no `rel`, no click handler and no copy affordance** — on screen it navigates away from the proposal in the same tab, from which there is no route back | ProposalView.tsx:216-218 | working (one-way navigation recorded) | NONE |
| CODE.share.52 | **`QrCode`** renders a genuine scannable QR as **inline SVG**, self-contained (no external image service, prints crisply): `QRCode.toString(url, { type:'svg', margin:1, errorCorrectionLevel:'M' }, cb)`. The in-file claim that "qrcode's callback is synchronous for SVG" is **correct** — the library's callback branch calls `cb(null, render(...))` inline. Wrapped in a `useMemo` keyed on `url`, injected with `dangerouslySetInnerHTML`, sized `96 × 96`, `flex:'none'`, `aria-label="Scan to open the 3D model"` | ProposalView.tsx:25-43,225; `node_modules/qrcode/lib/browser.js:61-66,74-76` | working | SC.10-7.18 |
| CODE.share.53 | On a QR generation error the callback receives `err`, `out` stays `''`, and the component `return`s `null` — **the QR silently vanishes** with no placeholder and no message. The printed URL above it remains, so the page degrades rather than breaks, but nothing signals that a code was meant to be there | ProposalView.tsx:29-35 | POC-DEFECT(silent failure of a print-only affordance) | NONE |
| CODE.share.54 | The QR's `aria-label` sits on a bare `<div>` with no `role="img"` — the same role-less-label pattern as `BlobImg` (row 28), so screen readers are not reliably told what the graphic is | ProposalView.tsx:36-41 | POC-DEFECT(accessible name attached to a role-less element) | NONE |
| CODE.share.55 | **This is the whole of the POC's answer to Q27.** The customer's 3D moment is delivered as a *printed URL plus a QR pointing at a second, separate, anonymous route* — not as a "View in 3D" button inside a customer proposal link. There is no proposal link at all (row 3), so the ruling's "no second link" property is inverted: the second link is the only link | ProposalView.tsx:73,216-225; ShareViewer.tsx (whole file) | working (divergence recorded against `Q27` / `F5-33`; the POC predates the ruling) | SC.10-7.18, SC.10-11.05 |

## §F — Page 2a · "YOUR SOLAR STORY" (narrative)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.56 | Rendered only when `proposalNarrative(project, fmtArea).length > 0`; the generator returns `[]` when no panel spec is chosen or `panelCount === 0`, so the page disappears entirely rather than printing an empty section | ProposalView.tsx:70,230-231; lib/proposal-narrative.ts:29-32 (one hop) | working | NONE |
| CODE.share.57 | Layout: `PageTitle` **YOUR SOLAR STORY**, then per section a 13 px / 800 title with `marginBottom 4`, then each beat as a 12.5 px `line-height 1.5` `var(--ink-2)` paragraph with `marginBottom 3`. Beats are keyed by array index; sections by title | ProposalView.tsx:232-242 | working | NONE |
| CODE.share.58 | Each `NarrativeBeat` carries a `facts` array (the raw numbers the sentence was built from) which this screen **never renders** — the provenance data exists in the model and is discarded at the presentation layer | ProposalView.tsx:236-240 vs lib/proposal-narrative.ts:41,46,58 (one hop) | working (unused provenance recorded) | NONE |
| CODE.share.59 | Closing 10 px `var(--ink-3)` line: *"Every figure above is drawn directly from your design — nothing is estimated beyond the labelled climate and cost assumptions."* — a blanket honesty claim printed **regardless of the actual irradiance source**, while the energy page two sheets later may say the monthly shape is a "built-in seasonal model (estimate, ±10%)" (row 65) | ProposalView.tsx:243-246 vs 317-321 | POC-DEFECT(a fixed "nothing is estimated" sentence can contradict the estimate disclosure the same document prints) | NONE |

## §G — Page 2b · SHADOW ANALYSIS (the captures as the customer receives them)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.60 | A 2-column grid (`1fr 1fr`, gap 14) over `project.captures.filter(c => c.imageBlobId)` — i.e. only captures whose blob actually saved. Each tile: `BlobImg` (`width:100%`, `borderRadius:10`, `1px solid var(--line)`, `placeholderHeight:180`), then the capture label centred at 12 px / 700, then centred 10.5 px `var(--ink-3)` reading **`{c.hour}:00 · {dateIso as en-IN "d MMM yyyy"}`** | ProposalView.tsx:251-271 | working | SC.10-8.16 |
| CODE.share.61 | Tiles are rendered in **`project.captures` array order**, which is insertion order — a design captured out of sequence (or with shot 3 retaken last) prints its shadow study in a different order from Step 7's fixed 2×2 preset grid | ProposalView.tsx:254-256 vs Step7Proposal.tsx:257-317 (one hop, sitting 6) | POC-DEFECT(shadow-study order on the customer document is not the four-preset order) | SC.10-8.16 |
| CODE.share.62 | **No per-capture staleness badge exists on this page.** Step 7's review grid marks a stale tile "Outdated — retake"; the printed document marks nothing. Combined with row 35 (the banner is `no-print`), a stale picture reaches the customer with no marker anywhere in the artefact | ProposalView.tsx:251-278 (no `isCaptureFresh` call); Step7Proposal.tsx:262-289 (one hop) | POC-DEFECT(the staleness law is enforced on the internal screen and dropped on the customer-facing one) | SC.10-8.20, SC.10-8.16 |
| CODE.share.63 | The printed caption is the **preset's** `dateIso`/`hour`, not the sun state the picture was actually taken at (the capture record stores the preset — sitting 6's `CODE.step7-proposal.9`). This document is where that mislabel is delivered to the customer | ProposalView.tsx:267-269; Step7Proposal.tsx:57-66 (one hop, sitting 6) | POC-DEFECT(consequence row: the mislabelled capture is printed as fact on the customer artefact) | SC.10-8.16 |
| CODE.share.64 | Empty state, spanning both columns (`gridColumn:'1/-1'`, `var(--ink-3)`, 13 px): **"No shadow captures — go back to Step 7 to capture them."** — an instruction addressed to the operator, printed inside the customer's document | ProposalView.tsx:272-276 | POC-DEFECT(operator-facing instruction rendered on, and printed into, the customer document) | SC.10-8.16 |

## §H — Page 3 · ENERGY REPORT

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.65 | Hero band: `linear-gradient(120deg,#1d4ed8,#2563eb)`, white text, radius 12, padding 18. **`{r.annualMwh} MWh`** at 28 px / 800; **"per year (Year 1)"** at 12 px / opacity .85; then a `gap:26` row at 12 px: **Specific Yield `{r.specificYield} kWh/kWp`** and **Performance Ratio `{r.performanceRatio}%`** | ProposalView.tsx:283-302 | working | NONE |
| CODE.share.66 | Monthly chart: twelve bars in a 100 px-tall flex row (`gap:4`), each `height = (kwh / max(...monthlyKwh, 1)) * 82` px, `borderRadius '3px 3px 0 0'`, coloured **`#f59e0b` for a monsoon month** (`r.monsoonMonths`, Jun–Sep) and `#2563eb` otherwise, with an 8.5 px `Jan…Dec` label beneath. There is **no y-axis, no value labels, no gridline and no legend** — nothing on the page says why four bars are amber | ProposalView.tsx:23,74,303-316 | working (unexplained colour encoding recorded) | NONE |
| CODE.share.67 | Provenance line, 9 px `var(--ink-3)`, switched on `r.irradianceSource`: **"Monthly shape from measured PVGIS climate (real monsoon variation)."** vs **"Monthly shape from built-in seasonal model (estimate, ±10%)."** — the document's one genuine provenance disclosure, and it is the smallest type on the page | ProposalView.tsx:317-321 | working | NONE |
| CODE.share.68 | Two-column footer grid (gap 16). Left, **LOSSES BREAKDOWN** (12.5 px / 700): one `label … pct%` row per `r.losses` entry at 11.5 px, then a bold `border-top` row **Total System Loss `{r.totalLossPct}%`** | ProposalView.tsx:322-334 | working | NONE |
| CODE.share.69 | Right, **SYSTEM SUMMARY** — six `KV` rows in fixed order: Capacity `{kWp} kWp` · Panels `{count} ({watt}W)` · Roof Area `fmtArea(roofAreaM2)` · Solar Access `{avgSolarAccessPct}%` · 25-yr Generation `{lifetimeMwh25} MWh` · Year 25 Output `{year25Mwh} MWh ({round(year25/max(0.01,annual)*100)}% of Y1)` | ProposalView.tsx:335-343 | working | SC.10-8.17 (Step 7's equivalent summary; this is the document's copy) |
| CODE.share.70 | The Panels row interpolates `project.components.panel?.watt` into a **template string**, so with no panel selected it prints the literal text **`0 (undefinedW)`** on the customer's document. The route is reachable — the Dashboard's "Show Proposal" opens `/proposal` for any project, including one still at Step 1 | ProposalView.tsx:338; Dashboard.tsx:277 (one hop) | POC-DEFECT(optional-chain result interpolated into a string renders the word "undefined" to the customer) | NONE |
| CODE.share.71 | Roof Area is the **only unit-sensitive figure in the whole document** — everything else is kWp/MWh/₹/%. `useUnits()` falls back to metric when no user is logged in, but `/proposal` is login-gated (row 3) so it always renders the operator's preference | ProposalView.tsx:59,339; lib/units.ts:44-48,50-66 (one hop) | working | NONE |

## §I — Page 4 · INVESTMENT & SAVINGS · HOW TO PAY · structure note · BOM summary · the money line

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.72 | Six `Big` tiles in two 3-column rows (gap 12). Row 1: **System Cost `₹{systemCostInr}`** · **Govt. Subsidy `− ₹{subsidyInr}`** (accent `var(--good)`) · **Net Investment `₹{netCostInr}`**. Row 2: **Annual Savings `₹{annualSavingsInr}`** · **Payback `{paybackYears} yrs`** · **25-yr Savings `₹{round(savings25YrInr/100000)} L`** (accent `var(--good)`). All rupee figures use `toLocaleString('en-IN')` (lakh/crore grouping); the 25-year figure is converted to **lakhs** and rounded to a whole `L` | ProposalView.tsx:348-359,584-591 | working | NONE |
| CODE.share.73 | When the subsidy is zero the tile still prints **"− ₹0"** — a minus sign applied to nothing | ProposalView.tsx:352 | POC-DEFECT(cosmetic but customer-facing: "− ₹0" on a priced document) | NONE |
| CODE.share.74 | Assumption line, 11.5 px `var(--ink-3)`: **"Assumes tariff ₹{tariffInrPerKwh}/kWh escalating {tariffEscalationPct}%/yr · Subsidy per PM Surya Ghar slab (residential, DCR modules; ₹78,000 cap from 3 kW)."** `tariffEscalationPct` is the hard-coded `3` from `computeFinancials`; the subsidy sentence is a **fixed literal** | ProposalView.tsx:360-363; lib/finance.ts:43,79 (one hop) | working | NONE |
| CODE.share.75 | That subsidy sentence prints **unconditionally**, including on a commercial/industrial project or one with non-DCR modules — exactly the cases where `subsidyInr()` returns `0`. The customer reads a residential-scheme entitlement beside a "− ₹0" tile, with nothing saying which condition failed | ProposalView.tsx:360-363; lib/finance.ts:14-25 (one hop) | POC-DEFECT(a scheme-eligibility sentence printed where the scheme does not apply, with no reason given) | NONE |
| CODE.share.76 | **HOW TO PAY** renders only when `fin.systemCostInr > 0` (else `financing` is `null` and the whole block, heading included, is absent). It is a 5-column table at 11 px — headers **Option · Upfront · Monthly · Year-1 net · Over the term** on a `var(--paper-2)` row — with one row per financing option: **Cash · Loan · Lease · PPA** | ProposalView.tsx:55-57,370-404; lib/financing.ts:61,78,92,106 (one hop) | working | NONE |
| CODE.share.77 | Row rendering: bold option label; `₹{upfrontInr}`; `₹{monthlyInr}` **or an em-dash `—` when zero**; Year-1 net as `+₹{abs}` in `var(--good)` when ≥ 0 and `−₹{abs}` in `var(--ink-2)` when negative (a negative year-1 position is styled as ordinary body text, **not** as `var(--bad)`); `₹{lifetimeCostInr}` | ProposalView.tsx:384-402 | working (the neutral styling of a negative outcome recorded) | NONE |
| CODE.share.78 | Footnote, 10.5 px: **"Representative terms — every option finances the same ₹{systemCostInr} system. Confirm actual lender / PPA offers before signing."** The in-file comment records why this table replaced a single hard-coded EMI line: the old line "quoted a loan on the FULL net cost while the model assumes a down payment" | ProposalView.tsx:365-369,405-409 | working | NONE |
| CODE.share.79 | **Structure note** — rendered only when `projectStructures(project).length > 0`. 11 px `var(--ink-3)` with a `3px solid var(--warn)` left border: **`Mounting structure — {engineeringStatus(project).label}.`** (default label **"PRELIMINARY — pending engineer verification"**, or **"Engineer approved"**) followed by `STRUCTURE_DISCLAIMER` = *"Structural design subject to certified engineer verification (IS 875 wind load, roof capacity, anchoring)."*, then the wind-zone label when `windZoneInfo(state).high`, then `Engineer notes: {notes}` when approved and non-empty | ProposalView.tsx:412-421; lib/structure.ts:43-52,74-75 (one hop) | working | SC.10-6.50 (cross-ref: the disclaimer's studio rows are sitting 5's) |
| CODE.share.80 | The disclaimer is **conditional on structures existing**. A design with panels but no generated mounting structures prints a priced proposal with **no structural disclaimer at all**, against CLAUDE.md's rule that "the disclaimer travels with every structure-bearing output" | ProposalView.tsx:412 | POC-DEFECT(the structure disclaimer can be absent from a proposal that still quotes a rooftop installation) | SC.10-6.50 |
| CODE.share.81 | `engineeringStatus(project)` and `windZoneInfo(project.info.state)` are each **called repeatedly inline** in the same JSX expression (3× and 2×) rather than computed once | ProposalView.tsx:414-419 | working (recorded) | NONE |
| CODE.share.82 | **BILL OF MATERIALS (summary)** — a 4-column table at 11 px over `mergedBom(project)`: **Item · Spec · Qty · Unit**, header row on `var(--paper-2)`, `Spec` in `var(--ink-3)`. **Identical in both audiences.** No rate, no amount, no GST, no line total appears anywhere in the document's BOM | ProposalView.tsx:422-443 | working | NONE |
| CODE.share.83 | The BOM prints quantities with **no provenance/confidence marker**, although `bomConfidence(mergedBom(project))` exists and Step 7's readiness card uses it to say which lines are "estimated / assumed". The customer receives measured, derived and assumed quantities rendered identically | ProposalView.tsx:434-441; lib/review.ts:98-120 (one hop, sitting 9) vs CLAUDE.md "every user-visible number carries a provenance tier" | POC-DEFECT(the commercial document is the one surface that drops the provenance tier) | NONE |
| CODE.share.84 | **The money line** (right-aligned, 12.5 px / 800) is the entire audience gate. **Customer:** `System Cost ₹{fin.systemCostInr}` — the one final price, margin already inside it. **Engineering:** the full chain `Cost ₹{money.subtotal} + {project.pricing?.marginPct ?? 12}% margin = [₹{money.taxableBeforeDiscount} − discount ₹{money.discount} = ] taxable ₹{money.taxable} + GST ₹{money.gst} = ₹{fin.systemCostInr}`, where the discount clause appears only when `money.discount > 0` | ProposalView.tsx:444-474; data/pricebook.ts:137 `DEFAULT_MARGIN_PCT = 12` (one hop) | working | NONE |
| CODE.share.85 | The two documents cannot disagree with Step 9: the headline `fin.systemCostInr` is `bomTotal(mergedBom(project))` and the breakdown is `bomMoney(mergedBom(project), project)` — the **same money path**, computed from the same lines. The in-file comment records that the short engineering form was arithmetically false until per-line GST landed (it was "short by exactly the tax — ₹15,690 on the 8-panel fixture"), and that the discount had to move *into* the chain for the equation to hold | ProposalView.tsx:50-52,445-469; lib/finance.ts:32-33 + lib/bom/money.ts:136-176 (one hop, sitting 8) | working | NONE |

## §J — Optional page · ELECTRICAL — SINGLE LINE SUMMARY

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.86 | Rendered only when `includeSld && sld`. Opens with a status banner: `1px solid var(--good)` on `#f0fdf4` in `#15803d` when `sld.voltageWithinLimit`, else `var(--bad)` on `#fef2f2` in `#b91c1c`; radius 10, `padding 10px 14px`, 12.5 px / 700. Text: **`Max system voltage {maxSystemVdc} Vdc {≤ or >} {inverterMaxDcV} V inverter limit · {maxStringLength}-module string, cold Voc`**, with **` · OVER LIMIT`** appended when the limit is exceeded | ProposalView.tsx:480-499 | working | NONE |
| CODE.share.87 | An **over-limit electrical design is printed to the customer in red saying "OVER LIMIT"** — nothing prevents adding the page, and the audience toggle does not affect it | ProposalView.tsx:144-152,480-499 | working (recorded: honest, but it means a customer-facing document can carry a failing engineering state on request) | NONE |
| CODE.share.88 | An 11-row two-column table at 12 px, fixed order: **Inverter** (`{label}` + ` × {count}` only when count > 1) · **AC rating** `{acRatingKw} kW` · **DC string fuse** `{dcFuseA} A` · **DC isolator** `{dcIsolatorA} A` · **DC cable** `{dcCableSizeMm2} mm² Cu` · **DC SPD** `{dcSpdType}` · **AC MCCB** `{mccbA} A` · **AC isolator** `{acIsolatorA} A` · **AC cable** `{acCableSizeMm2} mm² {acCableType}` · **AC SPD** `{acSpdType}` · **Standard** `{standard}`. Key column is `var(--ink-3)` at 40 % width | ProposalView.tsx:500-521 | working | NONE |
| CODE.share.89 | Footnote, 10.5 px: **"Full single-line diagram available in the engineering pack (Step 8)."** — a reference to a wizard step, printed on the customer document | ProposalView.tsx:522-524 | working (operator-vocabulary leak recorded, milder than row 64) | NONE |
| CODE.share.90 | The page reads `effectiveSld(project)` — the same derived-plus-overrides object the Step 8 sheet reads — so the proposal and the SLD sheet cannot print two different ratings | ProposalView.tsx:68,480; lib/sld.ts:62-66 (one hop, sitting 7) | working | NONE |

## §K — Page chrome (`Page`, `PageTitle`, `KV`, `Big`)

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.91 | `Page`: `className="page"`, `background:#fff`, radius 12, `boxShadow: var(--shadow-1)`, `padding '34px 38px 50px'`, `position:relative`, `minHeight:640`. Footer pinned `bottom:14, left:38, right:38`, `border-top 1px solid var(--line)`, `paddingTop 8`, 10 px `var(--ink-3)`, `justify-content: space-between` | ProposalView.tsx:532-565 | working | NONE |
| CODE.share.92 | The footer prints **`{project?.info.name}`** on the left — the operator's internal design name, including `"(copy)"` after a duplicate — on every page of the customer's document, the same leak as the share viewer's caption (row 16) | ProposalView.tsx:560; lib/project-duplicate.ts:22-30 (one hop) | POC-DEFECT(internal design/variant name printed on every page of the customer artefact) | NONE |
| CODE.share.93 | The footer prints **`PAGE {num} · Generated by Solar Studio`** on the right. The product name is the POC's, not the tenant's, and it appears on every page of a document that is otherwise meant to carry the EPC's branding | ProposalView.tsx:561 | working (divergence against `F7-07` tenant branding recorded) | NONE |
| CODE.share.94 | **Page numbers are hard-coded props, not a count.** The six possible pages pass `num =` 1 · **2** (narrative) · **2** (shadow analysis) · 3 · **4** (investment) · **4** (SLD). Two pairs of duplicates always print, and when the narrative page is suppressed the sequence becomes 1, 2, 3, 4, 4 — so the number is neither unique nor sequential, and no "of N" total exists | ProposalView.tsx:177,231,251,281,348,481 | POC-DEFECT(duplicate and non-sequential page numbers on a printed commercial document) | NONE |
| CODE.share.95 | `PageTitle`: 15 px / weight 800, `letter-spacing 1`, `marginBottom 14`; its `children` prop is typed `string`, so a title can never be composed of elements. `KV`: `space-between` row, 11.5 px, `padding '3px 0'`, key in `var(--ink-3)`, value bold. `Big`: `1px solid var(--line)`, radius 10, padding 14, 10.5 px `var(--ink-3)` label over a 17 px / 800 value coloured `accent ?? var(--ink)` | ProposalView.tsx:567-591 | working | NONE |
| CODE.share.96 | `Page` types its `project` prop as `ReturnType<typeof useActiveProject>` (i.e. `Project \| null`) and guards with `project?.info.name`, while the screen that passes it has already asserted the value non-null with `!`. The two halves of the same file disagree about whether the project can be missing — and §A row 5 shows the optimistic half is the one that is wrong | ProposalView.tsx:47,532,560 | working (type inconsistency recorded; the consequence is row 5) | NONE |

## §L — Link laws: what these two surfaces do NOT have (F5 / Q27 / Q42)

Absences are recorded as rows because "what is hidden and how the link behaves" is this sitting's subject; an absent control is a link behaviour. None of these is scored as a POC-DEFECT — the POC predates the F5 framework and the 2026-08-04 rulings — but each is a gap the PRD must state rather than inherit.

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.97 | **No test in the repository renders any of the three primary files.** `ProposalView`, `ShareViewer` and `BlobImg` have zero direct coverage: no DOM test, no snapshot, no axe pass (the axe suite covers `BomRow`, the `ui` primitives, `StructureSheet` and `LegPlanEditor` only). The customer-facing surfaces are the least tested in the studio | `grep -rn "ProposalView\|ShareViewer\|BlobImg" src/` → source files only; components/__tests__/axe.dom.test.tsx:15-21 | POC-DEFECT(the two surfaces that reach the customer carry no automated coverage of any kind) | NONE |
| CODE.share.98 | **There is no link lifecycle.** A `shareId` is minted once at project creation and lives forever: no revoke, no re-mint, no expiry, no scope, no status, and no manager surface listing issued links. `duplicateProject` is the only code path that ever produces a different one, and only for a copy | store/store.tsx:79-83,129; lib/project-duplicate.ts:5-8,44 (one hop); no other writer of `shareId` in `src/` | working (divergence: `F5` mint / re-mint / revoke / regenerate-with-revoke, and the `F2.F5.*` capabilities, have no POC counterpart) | SC.10-7.18, SC.10-11.05 |
| CODE.share.99 | **The link is not named and not attributable.** There is one id per project, not one per recipient; no label, no contact, no open event, no view log, no audit entry. Nothing in the POC can answer *who* opened the design | store/store.tsx:79-83; ShareViewer.tsx:7-8 (a pure local lookup, no side effect) | working (divergence: `F5-27` per-link open attribution, `F5-29` open events, `F5-31` audit coverage) | NONE |
| CODE.share.100 | **There is no acceptance surface and therefore no challenge to default off.** No Accept, no Negotiate, no Decline, no acceptance record, no OTP of any kind anywhere in the studio — `Q42`'s "default OFF, per-tenant enable" has no POC control to describe, and `F5-46`'s attribution record has no POC data source | ShareViewer.tsx (whole file); ProposalView.tsx (whole file); `grep -rn "shareId" src/` → 8 sites, none transactional | working (divergence recorded against `Q42` / `F5-44` / `F5-46`) | NONE |
| CODE.share.101 | **There is no version pinning.** The share viewer renders `state.projects.find(...)` live, so every edit to the design changes what an already-sent link shows, retroactively and silently. The printed proposal is a snapshot only because paper is; nothing in the app records what a customer was shown | ShareViewer.tsx:8; StudioClientLayout.tsx:10-19 (the layout even recomputes on this route) | working (divergence: `F5-40` "a version already shared never changes underneath the customer") | NONE |
| CODE.share.102 | **The web/document relationship is inverted.** `F5-39` makes the web page the path to the number and the PDF an artefact; in the POC the *page* is login-gated (row 3) and the customer's only route to the numbers is a PDF the operator produces with `window.print()` — a browser-side render with no retry, no failure notification and no server path | ProposalView.tsx:159-161; StudioClientLayout.tsx:121-126 | working (divergence recorded against `F5-39`) | SC.10-9.35 (cross-ref: the BOM's Print/Save-as-PDF row is sitting 8's) |
| CODE.share.103 | **The share link carries only the 3D view.** No stage/progress display, no documents, no contact, no money summary, no call to action — the surfaces `F5` builds on the customer link (stage chain, pack, permanence after close) have no POC counterpart, and the link therefore cannot be degraded over money because there is nothing on it to degrade | ShareViewer.tsx:32-48 | working (divergence recorded; `F5-24`/`F5-62`/`F5-74` are vacuously satisfied) | NONE |
| CODE.share.104 | **Minting and opening are fully offline operations.** The id comes from `crypto.randomUUID()` and the copy is a string concatenation into the clipboard; resolution is a local array scan. `F4-09` classes customer-link issue as online-only-fail-fast — the POC has no network step that could fail | store/store.tsx:79-83; ProposalView.tsx:73,155; ShareViewer.tsx:8 | working (divergence recorded against `F4-09`) | NONE |
| CODE.share.105 | The public share page **loads no third-party script, font or analytics** — the root layout renders `<html lang="en"><body>` with a single local stylesheet entry, no `next/font` call, no external `@import`, no tag manager. `F5-29`'s no-third-party requirement holds, incidentally | src/app/layout.tsx:1-21; `grep -rn "fonts.googleapis\|next/font\|analytics\|gtag" src/design src/app` → no match | working | NONE |
| CODE.share.106 | Opening the anonymous `/share/{id}` route hydrates the operator's **entire local project store** — every project, every customer name, every price — into the page's JavaScript memory, because `StoreProvider` wraps the whole `(studio)` group and `loadState()` is unconditional. Only the matched project is rendered, and the store is device-local, so nothing crosses a network; the exposure is to whoever holds the device | StudioClientLayout.tsx:136-148; store/store.tsx:324-337 (one hop); ShareViewer.tsx:7-8 | uncertain (no rendered leak; recorded because the "read-only public page" and the operator's full book of business share one memory space, and a future server-backed share must not inherit this shape) | NONE |

## §M — Readiness, issue gating and document identity

| id | behavior | evidence | status | census |
|---|---|---|---|---|
| CODE.share.107 | **The document has no readiness gate.** `ProposalView` imports nothing from `lib/review.ts` and never calls `preProposalReview`, `electricalGate` or `bomConfidence`. Step 7's "Before you issue" card can read **"Not ready to issue"** (`overall === 'blocked'`) and the full priced proposal still renders, prints and hands out a share link — the verdict is advisory on its own screen and invisible here | ProposalView.tsx (no `review` import, verified by grep); lib/review.ts:155-161 (one hop, sitting 9) | POC-DEFECT(the blocking readiness verdict does not gate the document it exists to gate) | SC.10-8.13, SC.10-8.19 |
| CODE.share.108 | **Only two of the four entry points mark the design proposal-ready.** Step 7's *Generate Proposal* patches `status: 'proposal_ready'` before navigating, and Step 10 Done patches it on mount; the Dashboard row menu's *Show Proposal* and the Energy report's *Quick Generate* navigate to `/proposal` with no status write. The same document is therefore routinely produced and printed for a project the app still lists as **"In progress"** on the Dashboard | Step7Proposal.tsx:369-371, Step10Done.tsx:13-18 vs Dashboard.tsx:277, EnergyReportSheet.tsx:241 (one hop); `grep -rn "proposal_ready" src/` → no writer in `ProposalView.tsx` | POC-DEFECT(two of four routes to the customer document leave the design's status untouched) | SC.10-8.19, SC.10-11.02 |
| CODE.share.109 | **The document has no identity.** No issue date, no proposal number, no revision/version, no validity period and no "prepared by" appear anywhere on any page — the only date printed in the whole artefact is the *capture* date under each shadow image. Two proposals for the same customer, printed a month apart at different prices, are indistinguishable as objects | ProposalView.tsx (only `toLocaleDateString` call is 268); footer 560-561 | POC-DEFECT(a priced commercial document carries no date, number or version) | NONE |
