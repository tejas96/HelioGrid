# Extraction ledger — Competitive verdicts (`CG-*`)

**Source:** `docs/12-competitive-gaps.md` (Status: approved · its own source: `./research/market.md`, missing from the corpus — recorded gap).
**Overlay applied:** `docs/15-spec-resolutions.md` read first; every row records the POST-overlay state, with what the overlay superseded/amended named in `notes`.
**Keys:** `CG-<n>` = the 19 numbered gaps · `CG-reslink.<i>` = Reslink tier-coverage rows · `CG-moat.<1..6>` = "What nobody else has".
**Row counts:** 19 CG + 19 CG-reslink + 6 CG-moat = 44.

**Corpus-wide note that applies to every row below (recorded once, not resolved):** docs/12's framing line reads "'HG' = HelioGrid as specified for v1 (the single 20-day build, docs/14)". Post-overlay that calendar is dead — docs/15 §4 directive 5: "**Timeline: SUPERSEDED.** … What survives is the *track dependency structure*, not the calendar … The scope commitment (no Launch-2, no 'later' buckets) stands; the day numbers do not." Read every "v1 / in the build" verdict below as scope commitment, never as schedule (also required by Global Constraint §14/DD4: no timelines or build phases in the PRD).

## 1 · Every gap, with a verdict (CG-1 … CG-19)

Verdict vocabulary is the source's own: **ADOPT-NOW** (in v1 spec, build it) · **DESIGN-FOR** (port/seam exists in v1; adapter or module lands later without rework) · **SKIP-DELIBERATELY** (named non-goal; do not build it by accident).

| key | gap + who has it | verdict | why (faithful) | target_doc | notes |
|---|---|---|---|---|---|
| CG-1 | **ALMM/DCR compliance checking** (Reslink, sole rival) | ADOPT-NOW | "Elevated from data flags to a checked feature: platform catalog items carry MNRE ALMM list references + DCR flags; a DRC rule fails the BOM/proposal when the subsidy path requires DCR and a component isn't compliant — prevents the costliest rework in Indian EPC, and only one competitor has it." | `foundations/F1-global-market-framework.md` + `modules/M01-onboarding-and-tenant-config.md` + `modules/M06-proposals.md` | Post-overlay AMENDED by R13 (2026-08-02 global-backend ruling): "ALMM/DCR are the IN entries of a scheme-keyed `certifications` structure on catalog specs; the market pack declares which schemes a market requires. The two-tier resolution is unchanged." So the rule is market-neutral in the module and India-keyed in the F1 pack (Global Constraint §6). Mapping correction vs. the initial table ("catalog → M01"): the source text puts the *check* on the BOM/proposal, so M06 carries the Generate-time failure; M01 carries the catalog flags; F1 carries the scheme data. Verdict unchanged. Source typo recorded, not fixed: "a **DRC** rule" where DCR is meant. |
| CG-2 | **PM Surya Ghar subsidy automation** (Reslink ✔; ARKA/OpenSolar manual slabs) | ADOPT-NOW | "Already in the ported domain (`subsidyInr(kwp, residential, dcrEligible)`); slabs ship as versioned injected market config so slab revisions are data updates, not releases." | `foundations/F1-global-market-framework.md` + `modules/M06-proposals.md` | Post-overlay: consistent with F1 market packs ("subsidy/compliance/calling rules; India = first pack") and with R2's amendment that the incentive stage is market-neutral (`INCENTIVE_CLAIMED`, "Subsidy claimed" being the IN pack's label). PM Surya Ghar is IN-pack data; the module states the capability. `subsidyInr(...)` is an implementation signature — the PRD must state it as a capability, not the function (§14/DD4). |
| CG-3 | **DISCOM selection + state-specific application packets** (Reslink partial; nobody does packets) | DESIGN-FOR | "v1: Site carries DISCOM, blockers attribute the wait, document checklist tracks the application. Packets are a `DocumentRenderPort` template family added post-launch — the render pipeline (Playwright, Devanagari-correct) already exists." | `modules/M08-projects.md` + `foundations/F1-global-market-framework.md` | Post-overlay AMENDED by R2 (2026-08-02): the blocker party is `utility` (was `discom`) and the stage is `UTILITY_INSPECTION` (was `DISCOM_INSPECTION`); "DISCOM inspection" is the IN pack's LABEL. So M08 says utility selection + utility-blocker attribution; F1's India pack supplies the DISCOM label and state packet list. Mapping derived from "S8 → M08" (blockers/checklist are Stage 8 material); F1 added because the source content is India-specific. |
| CG-4 | **Financing marketplace** (OpenSolar lender referral; Aurora US financing) | DESIGN-FOR | "v1 ships the EMI calculator only. A `FinancingPort` (eligibility → application → status webhook) is the India analog of OpenSolar's model — PSU bank/NBFC adapters post-launch. Referral revenue is upside, never load-bearing for margin." | `modules/M06-proposals.md` | EMI calculator is proposal-financials surface, so M06; the marketplace itself is a v1 non-goal to record in M06 §5 (and the margin statement belongs to `04-business-model.md` if the business-model author wants it). No overlay ruling touches this gap. "PSU bank/NBFC" is India-specific — the capability is market-neutral, the adapter list is F1 pack colour. |
| CG-5 | **CashFlow-style platform payments cut** (OpenSolar) | SKIP-DELIBERATELY | "Taking a cut of customer payments makes us a regulated money-mover (RBI PA licence). BYO-Razorpay keeps funds EPC-direct and us out of scope; Razorpay Route stays a documented alternate adapter only." | `modules/M11-payments-and-collections.md` | Post-overlay REINFORCED by R4: "tenant customer-collections = BYO-Razorpay payment links per tenant via `PaymentLinkPort` — platform never touches tenant funds (no PA licence needed)"; R4 also flags "Selling outside India needs a supplier-of-record decision — owner-blocked" (open question, not resolved here). Mapping from "money path/tranches → M11". Vendor name Razorpay becomes a capability requirement with Razorpay as the v1 reference implementation (Global Constraint §6). |
| CG-6 | **Monitoring / O&M / end-customer app** (Enact ENGAGE, $150–5,000/yr per asset) | SKIP-DELIBERATELY | "D9: v1 is the selling engine. Commissioning artefacts are retained at handover so an O&M module can attach post-v1 — but no monitoring code, telemetry ingestion or customer app in v1." | `modules/M08-projects.md` + `01-product-overview.md` | Post-overlay: D9 is **PARTIAL** — "scope honored (no inventory/PO/O&M); its 5-state shorthand deprecated by R2". The half this gap leans on (scope) survives; do not cite D9's 5-state chain. Also reinforced by R17: "A PPA billing/metering engine is an explicit non-goal … no recurring invoicing, no meter ingestion." M08 carries handover artefact retention; 01-product-overview carries the scope statement (docs/00 records the non-goal). |
| CG-7 | **LiDAR / imagery-derived measurement** (Aurora) | SKIP-DELIBERATELY | "D35: photos are reference, never measurement. Indian LiDAR/high-res DSM coverage is too thin to be honest about; our remote survey already uses Google Solar DSM where it exists, with confidence shown and a physical-survey fallback." | `modules/M04-survey.md` | Post-overlay: D35 **HONORED** — "no LiDAR/AR/auto-measure; drone-as-imagery ok". Confidence display and the measured/derived/estimated split are F8's provenance system (R18, D30) — M04 references F8 rather than restating tiers. Mapping from "S4 → M04". |
| CG-8 | **Utility-scale terrain + AutoCAD workflows** (PVcase, HelioScope) | DESIGN-FOR | "The scale program is committed v1 ramp: blocks/tables/zones model, GPU shadow-map shading, single-axis trackers with GCR backtracking, GLO-30 DEM import. Terrain-following articulation and utility autorouting defer; AutoCAD-native is never — we are browser-native by identity." | `modules/M05-design-studio.md` | Mapping from "S5 + census + docs/11 → M05" (docs/11 is the scale program). Reinforced by §4 directive 9: "The scale program is investment INTO the studio moat, never a reason to cut studio capability." Note the three-way split inside one row for the author: committed ramp / deferred / never. |
| CG-9 | **PVsyst / SketchUp / CAD export** (ARKA; PVcase) | DESIGN-FOR | "C&I lenders ask for PVsyst-bankable files. The BOM engine's emitter architecture takes new emitters cheaply; PVsyst-compatible + DXF exports are post-launch emitters, not re-architecture." | `modules/M05-design-studio.md` + `modules/M06-proposals.md` | Split because the source names two homes: DXF/drawing export is studio output (see CG-reslink.3, "3D Export & SLD … every tier") while the "BOM engine emitter" is the BOM/proposal path. Contrast to hold, not resolve: CG-9 says CAD export is post-launch, while the matrix row "CAD / PVsyst / SketchUp export" is marked "✖ v1" for HG — the ✖ is the v1 state, DESIGN-FOR is the seam. |
| CG-10 | **Bankable P50/P90 yield reports** (HelioScope) | DESIGN-FOR | "PVGIS TMY + PR ladder are in place; uncertainty bands and exceedance statistics are an additive reporting layer on the same energy model, needed when Enterprise/utility tenants arrive." | `modules/M05-design-studio.md` | Post-overlay: the energy source of record is fixed by R5 — "PVGIS is the energy source of record (SARAH3 → ERA5 ladder, labelled 'Real · PVGIS'); built-in latitude-fit fallback always labelled '±10%'. Google Solar is an enhancement … never a dependency." Labelling is F8 (R18). "Enterprise tier" trigger touches `04-business-model.md`. |
| CG-11 | **Per-design pay-as-you-go pricing** (Pylon $4–10/design) | SKIP-DELIBERATELY | "Capacity tiers align our price to EPC revenue and keep ARPU predictable; PAYG fragments revenue and complicates entitlements. The ₹1,999/mo Starter tier (01) already serves seasonal micro-installers; revisit only if they churn even on the Starter floor." | `04-business-model.md` | Mapping from "docs/16 + docs/01 → 04-business-model + M12". Post-overlay the pricing frame is live, not deferred: D38 **SUPERSEDED** — "payments, subscriptions, billing, entitlements, usage tracking and gateway integration are IN v1". The ₹ figure is India-pack price-book data under the global-backend ruling ("one currency per tenant … global-safe billing schema, India-only rails"); 04-business-model carries the India book as source-derived baseline. |
| CG-12 | **Free tier** (OpenSolar) | SKIP-DELIBERATELY | "User decision: trial-only. OpenSolar's free is funded by distributor placement + payments cut (gaps 5, 13) — without that engine a free tier is pure metered COGS." | `04-business-model.md` + `modules/M12-platform-billing.md` | Post-overlay CONFIRMED twice: §4 directive 4 "Trial-only, no free tier" and the user-decisions log "pricing: trial-only, no free tier". D11's "billing deferred" half and D38 are both superseded — billing is in v1, trial-only. M12 carries trial lifecycle/entitlements; the pre-commitment "read + export always work regardless of billing state" (D38 row) is the soft-block law for 04-business-model. |
| CG-13 | **Partner-funded distributor placement in catalog** (OpenSolar) | SKIP-DELIBERATELY | "The two-tier catalog's neutrality is a trust wedge with EPCs. If ever revisited, only as clearly-labelled sponsored listings — never silent ranking." | `modules/M01-onboarding-and-tenant-config.md` + `04-business-model.md` | Mapping from "tenant-config + catalog → M01"; the revenue-model half is business-model. Catalog structure per R13 (two-tier, resolution tenant-override → tenant-item → platform-item) and the earlier-owner-decisions line "two-tier catalog (platform master + tenant own + tenant overrides)". Neutrality is a product law worth stating in M01 §5 non-goals. |
| CG-14 | **WhatsApp-native proposal sending** (no incumbent has it natively — market gap, not competitor feature) | DESIGN-FOR | "D32 stands for v1: ManualCopyAdapter behind `MessagingPort`. v2 = BYO-WABA via Meta Embedded Signup (tenant owns the number and reputation). The port boundary means zero rework." | `modules/M06-proposals.md` + `foundations/F5-customer-link.md` | Post-overlay: D32 **HONORED** ("`MessagingPort` ManualCopyAdapter; v2 BYO-WABA documented"), and D4 ("WhatsApp primary channel") is **SUPERSEDED by D32**. **Contradiction to record, not resolve:** the V2 brief adds `modules/M03-marketing.md` as `[BRIEF — new scope]` with campaigns "across Email, WhatsApp, Facebook, Instagram, SMS" — a WhatsApp channel the source corpus rules out for v1. Two origins (SRC D32 vs BRIEF M03) disagree on WhatsApp; flag in `registers/conflicts.md` and let M03/M06 authors carry both tags separately. |
| CG-15 | **MLPE (micro-inverter/optimiser) electrical design** (ARKA, Aurora, HelioScope) | DESIGN-FOR | "Catalog holds the components in v1; the string-sizing ladder gains an MLPE branch later. Indian residential is string-inverter-dominated, so this follows demand, not launch." | `modules/M05-design-studio.md` + `modules/M01-onboarding-and-tenant-config.md` | Matrix row for HG reads "◐ (catalog, no MLPE electrical model)" — the partial is deliberate. M01 carries the catalog holding the components; M05 carries the string-sizing ladder and the absent MLPE electrical model as an explicit non-goal. The studio's electrical hard gate stays per R12 ("an invalid string design still blocks the layout step's Next"). |
| CG-16 | **Public API with keys/quotas** (ARKA, Aurora, OpenSolar) | DESIGN-FOR | "ts-rest emits OpenAPI 3.1 from day one (customer-link endpoints + webhooks already public); tenant API keys and quotas are a thin layer post-launch." | `04-business-model.md` | Product-level residue only: tenant API keys/quotas as a tier-packaged capability (see CG-19, which the source calls "the same engineering; this row is its Enterprise packaging"). The ts-rest/OpenAPI half is implementation and is excluded from the PRD by Global Constraint §14/DD4 — record as excluded in traceability rather than writing it into a module. If a tenant-facing key-management surface is later specified it belongs to M01 tenant config; the source does not say so, so that is not asserted here. |
| CG-17 | **CRM as included, not add-on** (ARKA charges extra) | ADOPT-NOW | "Already our pricing stance (`01-business-model.md`): every feature in every tier; CRM inclusion is a stated selling line against ARKA." | `04-business-model.md` + `modules/M02-crm-and-leads.md` | Mapping: docs/01 → 04-business-model (packaging conviction "every feature in every tier"); M02 is the capability itself. Matrix row: HG "✔" vs ARKA "✖ paid add-on", HelioScope/PVcase "✖", Aurora "◐ sales mode". |
| CG-18 | **White-label options** (Reslink Enterprise) | DESIGN-FOR | "Tenant branding on customer documents is all-tiers already (docs/10); full white-label (custom domain for customer links + unbranded portal) is an Enterprise option (owner-confirmed 2026-07-24). Custom-domain routing designed at the customer-link module; built when the first Enterprise deal asks." | `foundations/F5-customer-link.md` + `04-business-model.md` + `foundations/F7-design-language.md` | **Mapping correction, stated by the source text itself:** "Custom-domain routing designed at the customer-link module" → F5 (the initial table routes docs/10 to F3 + F7 only). Tenant branding on documents stays with docs/10 → F7/F3; tier placement is business-model. Owner-confirmed 2026-07-24 — same date as the design-system supersession, so branding tokens are ds-source-based (D3 superseded, R19-E "'Instrument' is formally retired"). |
| CG-19 | **Custom integrations / partner API** (Reslink Enterprise; ARKA API) | DESIGN-FOR | "Owner-confirmed Enterprise differentiator: tenant API keys + webhook subscriptions over the already-emitted OpenAPI 3.1 surface (gap 16 is the same engineering; this row is its Enterprise packaging)." | `04-business-model.md` + `modules/M12-platform-billing.md` | Packaging/entitlement statement, so business-model + M12 (entitlements are "the ONLY runtime gating" per §4 directive 8). API mechanics excluded as implementation (§14/DD4) — same treatment as CG-16. |

## 2 · Reslink feature-list coverage check (CG-reslink.1 … CG-reslink.19)

Source framing, kept intact: "Every feature Reslink lists on its tier cards, mapped to HelioGrid — **all available on every HelioGrid tier** (they gate most into higher tiers; that contrast is a selling line)". Owner-supplied pricing page, 2026-07-24. The "every tier" claim itself is a packaging conviction for `04-business-model.md`; the capability rows below land in their modules.

| key | Reslink lists (tier they gate it to) | HelioGrid equivalent (tier) | target_doc | notes |
|---|---|---|---|---|
| CG-reslink.1 | CRM & Project Management (Basic+) | "Full CRM + 9-stage projects module — every tier" | `modules/M02-crm-and-leads.md` + `modules/M08-projects.md` | "9-stage" is post-overlay correct: R2 fixes the FULL 9-stage chain as canonical and deprecates D9's 5-state shorthand. Two market-neutral stage names apply (`UTILITY_INSPECTION`, `INCENTIVE_CLAIMED`); labels are pack data. |
| CG-reslink.2 | Proposal Editing (Basic+) | "11-step builder, versions, Path A/B — every tier (counts per 01)" | `modules/M06-proposals.md` + `04-business-model.md` | "counts per 01" = proposal counts are a metered tier dimension → business-model. Path A/B per D21 (HONORED, "provenance differs (R18)"). Free navigation + validate-at-Generate per R12; Quick mode is committed per R11. |
| CG-reslink.3 | 3D Export & SLD (Basic+) | "SLD + drawing sheets, DXF/SVG/PNG/PDF export — every tier" | `modules/M05-design-studio.md` | Contrast to hold (not resolve): the matrix marks HG "✖ v1" for "CAD / PVsyst / SketchUp export" while this row commits DXF export on every tier. Read together with CG-9: DXF-from-studio ships; PVsyst/SketchUp/AutoCAD-native do not. |
| CG-reslink.4 | PV Report (Basic+) | "PVGIS energy report (monthly, losses, 25-yr, provenance-labelled) — every tier" | `modules/M05-design-studio.md` + `foundations/F8-data-honesty.md` | Provenance labelling is F8 (R18 + R5's source labelling: "Real · PVGIS ({database})" vs "Built-in estimate ±10%"). |
| CG-reslink.5 | AC/DC & Earthing Layout (Basic+) | "SLD with DCDB/ACDB/SPD/isolators/earthing pits + cable routing + IS/IEC ladders — every tier" | `modules/M05-design-studio.md` + `foundations/F1-global-market-framework.md` | "IS/IEC ladders": IS is the Indian standard set — the standards ladder is pack-declared per the market-neutrality constraint (§6); IEC 62548 is named in the matrix row for electrical sizing. |
| CG-reslink.6 | Tin Shed Support (Basic+) | "`metal_shed` roof type with monorail structure — every tier" | `modules/M05-design-studio.md` | Roof-type enum value quoted from source; M05's census baseline (`docs/prd/modules/M05-studio/studio-census.md`) is the acceptance list and never shrinks (Global Constraint §3.2). |
| CG-reslink.7 | Detailed Structure Analysis (Pro+) | "Parametric member/steel model + foundations (honest: material estimate + engineer sign-off, never a computed safety verdict) — every tier" | `modules/M05-design-studio.md` + `foundations/F8-data-honesty.md` | The parenthetical is a product law, not a caveat: "never a computed safety verdict". Same honesty rule appears in CG-moat.2 ("engineer sign-off instead of computed structural claims"). |
| CG-reslink.8 | Advanced Shadow Analysis (Pro+) | "Per-panel raycast solar access, monthly heatmap, sun-path simulation — every tier" | `modules/M05-design-studio.md` | Matrix row "3D rooftop design + shading" marks HG "✔ flagship" against Aurora "✔ best-in-class" — the flagship claim is §4 directive 9's. |
| CG-reslink.9 | Detailed Energy Reports (Pro+) | "Full loss breakdown, specific yield, PR, degradation — every tier" | `modules/M05-design-studio.md` | Energy source of record is PVGIS per R5; CG-10's P50/P90 uncertainty bands are the additive layer on top of this, post-launch. |
| CG-reslink.10 | Standard / All Obstruction Types (Pro/Premium) | "All 11 obstruction types incl. bridging + convert-to-platform — every tier" | `modules/M05-design-studio.md` | "11 obstruction types" is a census-checkable count — cross-check against `SC.*` rows in the studio census checklist before authoring M05. |
| CG-reslink.11 | Design Location Editing (Pro+) | "Pin placement + relocation guard + calibration + north offset — every tier" | `modules/M05-design-studio.md` | — |
| CG-reslink.12 | Industrial Drawings (Premium+) | "Title-blocked drawing sheets (SLD, PV layout, string route, structure) — every tier" | `modules/M05-design-studio.md` | Rendered output shares the document render pipeline referenced in CG-3 ("Playwright, Devanagari-correct") — Devanagari correctness is F3's law. |
| CG-reslink.13 | Ground Mount Design (Premium+) | "Ground arrays (flat v1; trackers/terrain in scale program) — every tier" | `modules/M05-design-studio.md` | The "flat v1 / trackers+terrain later" split matches CG-8's committed-ramp wording; docs/11 scale program is M05's material. |
| CG-reslink.14 | Advanced Structures (Premium+) | "Structure presets + full parametric customisation — every tier" | `modules/M05-design-studio.md` | — |
| CG-reslink.15 | Priority Support (Premium+) | "Pro+ (support is the one ladder we also climb)" | `04-business-model.md` | **The single exception to "all available on every HelioGrid tier"** — support is explicitly tiered. Record as an exception so the "every feature in every tier" conviction (CG-17) is not over-stated. |
| CG-reslink.16 | Unlimited Projects (Enterprise) | "Growth+ (Starter capped at 10 active — owner directive)" | `04-business-model.md` + `modules/M12-platform-billing.md` | **Second exception to the every-tier claim**: an entitlement cap (10 active projects on Starter), owner directive. Entitlements are the only runtime gating (§4 directive 8) → M12. |
| CG-reslink.17 | White-Label Options (Enterprise) | "Enterprise (gap 18)" | `04-business-model.md` + `foundations/F5-customer-link.md` | **Third exception**: Enterprise-only. Same content as CG-18; custom-domain routing designed at the customer-link module. |
| CG-reslink.18 | Custom Integrations (Enterprise) | "Enterprise (gap 19)" | `04-business-model.md` | **Fourth exception**: Enterprise-only. Same content as CG-19. |
| CG-reslink.19 | SLA / Dedicated Account Manager (Enterprise) | "Enterprise: dedicated manager; contractual SLA at sales discretion (not pre-committed)" | `04-business-model.md` | **Fifth exception**, and note the deliberate non-commitment: the SLA is "at sales discretion (not pre-committed)" — do not write a service-level guarantee into the PRD. |

## 3 · What nobody else has — the moat (CG-moat.1 … CG-moat.6)

Source framing: "The moat, stated once and defended everywhere." Text below is verbatim-faithful to docs/12 §"What nobody else has".

| key | moat item (verbatim-faithful) | target_doc | notes |
|---|---|---|---|
| CG-moat.1 | **The AI voice agent** — "outbound follow-up + inbound answering in Hindi/Marathi/Gujarati/Tamil/Telugu/English, TRAI-compliant by construction (`ComplianceGate`: daily DND scrub, 9am–9pm, disclosure ≤30 s), every call transcribed to the lead timeline, per-tenant numbers and IVR. No competitor at any price has this." | `modules/M07-sales-execution.md` + `foundations/F1-global-market-framework.md` | Mapping from "S7 + voice + D36 → M07". Post-overlay AMENDED by D36 (2026-08-02): "the gate's MECHANISM is non-swappable; its statutory RULESET is per-market data from the market pack (TRAI/DLT — DND scrub, 9am–9pm, DLT series, opt-out, recording retention — is the IN ruleset). A market with no voice ruleset cannot enable outbound voice." So the 6 agent languages + the gate mechanism are M07; the TRAI ruleset is F1's India pack. R3 fixes the vendors behind ports (Exotel + Sarvam) and keeps "agent languages (6) … independent of UI languages (3)". §4 directive 7 (AMENDED): DTMF-send is NOT provided by Exotel AgentStream, "so IVR traversal degrades honestly until a capable adapter exists" — the "per-tenant numbers and IVR" claim must be stated with that honest degradation. |
| CG-moat.2 | **The provenance/honesty system** — "measured/derived/estimated/assumed on every number, money-never-stale, 'Indicative proposal' labelling, engineer sign-off instead of computed structural claims, correlation-not-attribution on agent impact. Competitors print confident numbers; we print defensible ones." | `foundations/F8-data-honesty.md` | Mapping from "provenance/honesty (R18, journey honesty rules) → F8". Post-overlay R18 is canonical and adds the fifth-tier ban: "No screen invents a fifth tier", plus energy source labelling per R5. Money-never-stale is R14's rule ("money figures shown from cache carry the provisional label"). Correlation-not-attribution is D37 (HONORED) → also M13. |
| CG-moat.3 | **Offline-first field app** — "PowerSync-backed surveys, photos and My Day that work in a basement DB room or a village with no signal, then sync. Mobile-first rivals still assume connectivity." | `foundations/F4-data-integrity.md` | Mapping from "docs/06 → F4". Post-overlay bounded by R14: offline-full = survey capture, survey photos, quick-add lead, activity/visit logging, task ticks, My Day + assigned leads/read cache; online-only = proposal generate/share, any money mutation, design mutations, billing, agent/tenant config, imports/exports, PDF render. §4 directive 10 moves offline to the last build phase — "a move in TIME only — never in scope or quality" (and no timelines enter the PRD, §14/DD4), with "Until offline lands, mobile is online-first behind a repository abstraction". |
| CG-moat.4 | **Vernacular UI** — "EN/HI/MR interface, per-user language, Devanagari-correct PDFs, ₹ lakh/crore grouping everywhere. The field workforce is not English-first; no rival acknowledges this." | `foundations/F3-localization.md` + `foundations/F7-design-language.md` | Mapping from "docs/10 → F3 + F7". Post-overlay: D12 ("App UI English-only") is **SUPERSEDED** by D25 (EN/HI/MR, "Lingui v5, per-user re-render, Devanagari chain"). Amendment to record: "₹ lakh/crore grouping **everywhere**" is now market-pack behaviour, not universal — global-backend ruling 2026-08-02, "one currency per tenant", global-ready with India-only launch; so the lakh/crore grouping is the IN pack's number format (F1) while F3 states the capability. |
| CG-moat.5 | **Two-tier catalog with tenant price overrides** — "platform master catalog (ALMM-referenced) + tenant catalog + tenant overrides, with versioned price books so sent quotes never mutate. Closest rival (Reslink's local DB) has no override/versioning layer." | `modules/M01-onboarding-and-tenant-config.md` | Mapping from "tenant-config + catalog → M01". Post-overlay per R13: resolution order tenant-override → tenant-item → platform-item; "Catalog rate history is the versioned rate history on tenant items/overrides; `price_book_versions` exists as a separate table for non-catalog rates"; "sent proposals keep the rate version they were built with". AMENDED 2026-08-02: ALMM is one scheme entry in a scheme-keyed `certifications` structure (F1 declares which schemes a market requires). Vocabulary correction required by R1: "sent **quotes**" must be written as *proposals* — "quote"/"quotation" are banned from UI strings and identifiers (search aliases excepted). |
| CG-moat.6 | **Org pricing with unlimited seats under incumbent prices** — "a business-model feature: whole-company adoption is what makes the single-record pipeline (the #1 pain point) actually work." | `04-business-model.md` | Post-overlay CONFIRMED by the earlier-owner-decisions line: "benchmark pricing below Reslink/ARKA/Aurora with healthy margin (final tiers: docs/01)". Tension to hold with CG-reslink.16: seats are unlimited, but *projects* are capped on Starter (10 active) — unlimited seats is not unlimited usage. |

## Feature matrix (CG-matrix)

One row per capability line of the docs/12 "Feature matrix — HelioGrid v1 spec vs the field" (source lines 13–37; line 11 is the table header, line 12 the separator — the matrix holds **25 capability lines**, not the 26 the earlier coverage note claimed; counted twice, `awk 'NR>=13 && NR<=37'` = 25). Legend is the source's own: ✔ full · ◐ partial · ✖ absent. The HG cell is quoted **verbatim**; the six competitor cells (ARKA 360, Reslink, Aurora, OpenSolar, HelioScope, PVcase) are compressed, keeping every qualifier. The corpus-wide header note above applies: "HG = as specified for v1 (the single 20-day build)" is scope commitment, never schedule (§4 directive 5).

| key | capability + HG value (verbatim) | competitors (compressed) | target_doc |
|---|---|---|---|
| CG-matrix.1 | **3D rooftop design + shading** — HG: "✔ flagship" | ARKA ✔ · Reslink ✔ · Aurora ✔ best-in-class · OpenSolar ✔ · HelioScope ◐ C&I-oriented · PVcase ✖ (CAD utility) | M05 |
| CG-matrix.2 | **Full design parity on mobile (375 px, touch)** — HG: "✔" | Reslink ✔ mobile-first · OpenSolar ◐ · ARKA/Aurora/HelioScope/PVcase ✖ | M05 (+F7) |
| CG-matrix.3 | **Offline field capture (survey, photos)** — HG: "✔ PowerSync" | Reslink ◐ · all five others ✖ | F4 (+M04) |
| CG-matrix.4 | **AI roof detection w/ confidence + review** — HG: "✔ Google Solar + Gemini fallback" | Aurora ✔ · OpenSolar ✔ OS 3.0 · ARKA ◐ · Reslink ◐ tracing · HelioScope/PVcase ✖ | M05 (+M04, +F8) |
| CG-matrix.5 | **LiDAR/DSM measurement** — HG: "✖ (D35)" | Aurora ✔ · PVcase ◐ terrain · ARKA/Reslink/OpenSolar/HelioScope ✖ | M04 (§Non-goals) |
| CG-matrix.6 | **Energy simulation w/ provenance labels** — HG: "✔ PVGIS SARAH3→ERA5, labelled" | all six ✔ — Reslink NASA data · Aurora NREL-grade · HelioScope bankable · PVcase via RatedPower · ARKA/OpenSolar plain ✔ | M05 (+F8) |
| CG-matrix.7 | **Electrical sizing + autostring + SLD** — HG: "✔ IEC 62548 ladders" | ARKA ✔ manual stringing · Aurora ✔ · HelioScope ✔ · PVcase ✔ cabling · Reslink ◐ · OpenSolar ◐ | M05 |
| CG-matrix.8 | **Micro-inverter / optimiser design** — HG: "◐ (catalog, no MLPE electrical model)" | ARKA ✔ · Aurora ✔ · HelioScope ✔ · OpenSolar ◐ · Reslink/PVcase ✖ | M05 (+M01) |
| CG-matrix.9 | **Auto-BOM with per-field overrides + provenance** — HG: "✔ 6 emitters" | Reslink ✔ · ARKA ◐ · Aurora ◐ · OpenSolar ◐ · PVcase ◐ · HelioScope ✖ | M05 (+F8) |
| CG-matrix.10 | **Two-tier catalog + versioned price books** — HG: "✔" | Reslink ◐ local DB · OpenSolar ◐ · ARKA/Aurora/HelioScope/PVcase ✖ | M01 |
| CG-matrix.11 | **CRM + pipeline included in base price** — HG: "✔" | Reslink ✔ · OpenSolar ✔ · Aurora ◐ sales mode · ARKA ✖ paid add-on · HelioScope/PVcase ✖ | M02 (+04-business-model) |
| CG-matrix.12 | **Proposal + no-login customer accept link** — HG: "✔ tokenised lifecycle" | ARKA ✔ · Reslink ✔ · Aurora ✔ · OpenSolar ✔ · HelioScope/PVcase ✖ | M06 (+F5) |
| CG-matrix.13 | **ALMM/DCR compliance checking** — HG: "✔ (elevated — see gap 1)" | Reslink ✔ only rival · all five others ✖ | F1 (+M01, +M06) |
| CG-matrix.14 | **Subsidy computation (PM Surya Ghar slabs)** — HG: "✔ computed in domain" | Reslink ✔ · ARKA ◐ manual config · OpenSolar ◐ manual config · Aurora/HelioScope/PVcase ✖ | F1 (+M06) |
| CG-matrix.15 | **DISCOM selection / paperwork** — HG: "◐ selection + blocker attribution" | Reslink ◐ selection · all five others ✖ | M08 (+F1) |
| CG-matrix.16 | **GST-native money path (BOM↔proposal↔tranches)** — HG: "✔ to the paisa" | ARKA ◐ · Reslink ◐ · Aurora/OpenSolar/HelioScope/PVcase ✖ | M11 (+F1) |
| CG-matrix.17 | **Financing marketplace / lender integration** — HG: "✖ EMI calc only" | Aurora ✔ US lenders · OpenSolar ✔ · ARKA/Reslink/HelioScope/PVcase ✖ | M06 (§Non-goals; CG-4 seam) |
| CG-matrix.18 | **Payments / collections** — HG: "✔ BYO-Razorpay tranche links" | OpenSolar ✔ CashFlow (takes cut) · all five others ✖ | M11 |
| CG-matrix.19 | **Post-sale monitoring / O&M** — HG: "✖ (D9)" | all six ✖ | M08 (§Non-goals; +01-product-overview) |
| CG-matrix.20 | **Utility-scale: blocks/tables, trackers, terrain** — HG: "◐ scale program (committed ramp)" | PVcase ✔ category leader · HelioScope ✔ large arrays · Reslink ◐ ≤5 MW · ARKA/Aurora/OpenSolar ✖ | M05 |
| CG-matrix.21 | **CAD / PVsyst / SketchUp export** — HG: "✖ v1" | ARKA ✔ · PVcase ✔ AutoCAD-native · Aurora ◐ · HelioScope ◐ · Reslink/OpenSolar ✖ | M05 (§Non-goals; CG-9 seam — DXF-from-studio per CG-reslink.3 is the held contrast) |
| CG-matrix.22 | **Public API** — HG: "◐ OpenAPI 3.1 emitted; keys later" | ARKA ✔ · Aurora ✔ · OpenSolar ✔ · HelioScope ✔ · Reslink/PVcase ✖ | 04-business-model (mechanics excluded per §14/DD4, as CG-16) |
| CG-matrix.23 | **AI voice agent (outbound + inbound, 6 languages)** — HG: "✔ unique" | all six ✖ | M07 (+F1) |
| CG-matrix.24 | **Vernacular UI (EN/HI/MR) + Devanagari-correct PDFs** — HG: "✔" | all six ✖ | F3 (+F7) |
| CG-matrix.25 | **Provenance tiers + money-staleness honesty system** — HG: "✔ unique" | all six ✖ | F8 |
