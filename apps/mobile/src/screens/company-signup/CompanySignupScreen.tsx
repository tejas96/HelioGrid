import { PlaceholderScaffold } from '../shared/PlaceholderScaffold';

/** Placeholder — `SCR-M01-02` Company Signup lands here (`T-M01-002`); the door hands off to this route. */
export function CompanySignupScreen() {
  return <PlaceholderScaffold name="Company signup" />;
}
