/**
 * The health module's entire export surface (CLAUDE.md §Structure) — the module class only.
 * The liveness/readiness shapes are contract types, so nothing else needs exporting.
 */
export { HealthModule } from './health.module';
