/**
 * The auth module's export surface (apps/api/CLAUDE.md §Local conventions): the module, and the
 * service the company signup binds a session through. The projection is a contract type.
 */
export { AuthModule } from './auth.module';
export { AuthService } from './auth.service';
