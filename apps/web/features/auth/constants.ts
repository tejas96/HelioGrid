/** The routes the door sends people between — `/login` and where a signed-in person belongs. */
export const LOGIN_ROUTE = '/login';
export const HOME_ROUTE = '/home';
/** A signed-in person with no company yet (`M01-10`) — a placeholder until `T-M01-002`. */
export const COMPANY_SIGNUP_ROUTE = '/company-signup';
