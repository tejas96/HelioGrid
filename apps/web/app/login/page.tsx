'use client';
import { LoginScreen } from '../../features/auth';

/** Route only — the screen and its logic live in features/auth/login. */
export default function LoginPage() {
  return <LoginScreen />;
}
