import { DesignScreen } from '../../features/design-reference';

/** Route only — the screen lives in features/design-reference (ADR-0022). */
export default function DesignPage() {
  return <DesignScreen />;
}
