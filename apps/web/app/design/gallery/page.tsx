'use client';
import { GalleryScreen } from '../../../features/design-reference/gallery';

/** Route only — the screen lives in features/design-reference/gallery. */
export default function DesignGalleryPage() {
  return <GalleryScreen />;
}
