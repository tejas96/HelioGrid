import { HomeScreen } from '../../features/app/home';

/** Route entry for /home — routing only; all composition is in features/app/home. */
export default function HomePage() {
  return <HomeScreen />;
}
