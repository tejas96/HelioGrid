{
  "name": "heliogrid",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "prepare": "simple-git-hooks",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "bash scripts/lint-all.sh",
    "lint:fix": "biome check --write .",
    "boundaries": "turbo boundaries",
    "verify": "pnpm turbo build && pnpm lint && pnpm boundaries && pnpm turbo typecheck && pnpm check:adherence && pnpm check:dupes && pnpm ds:contract && pnpm check:openapi && pnpm check:catalogs && pnpm check:docs && pnpm turbo test",
    "verify:ci": "pnpm verify && pnpm check:env",
    "precommit": "pnpm exec biome check --error-on-warnings --no-errors-on-unmatched --staged . && pnpm turbo typecheck",
    "check:adherence": "bash scripts/check-adherence.sh",
    "check:catalogs": "c() { find packages/i18n/src/locales -type f -print0 | sort -z | xargs -0 git hash-object; }; before=$(c); pnpm --filter @heliogrid/i18n extract >/dev/null; if [ \"$before\" = \"$(c)\" ]; then echo \"catalogs OK - extraction produced no change\"; else echo \"catalogs STALE - copy changed without re-running extract:\"; git diff --stat packages/i18n/src/locales; exit 1; fi",
    "check:openapi": "node scripts/check-openapi-breaking.mjs",
    "check:dupes": "jscpd --config .jscpd.json apps packages",
    "check:unused": "knip",
    "check:env": "node scripts/check-env-access.mjs",
    "ds:contract": "node scripts/ds-contract.mjs",
    "check:docs": "python3 scripts/gates.py",
    "check:all": "pnpm lint:fix && pnpm turbo typecheck && pnpm boundaries && pnpm check:adherence && pnpm check:dupes && pnpm ds:contract && pnpm check:openapi && pnpm check:catalogs && pnpm check:env && pnpm check:docs"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm run precommit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "dependency-cruiser": "16.10.4",
    "jscpd": "4.0.5",
    "knip": "5.88.1",
    "sherif": "1.13.0",
    "simple-git-hooks": "2.13.1",
    "turbo": "2.10.7",
    "typescript": "5.8.3"
  }
}
