{
  "name": "@heliogrid/invariants",
  "version": "0.0.1",
  "private": true,
  "description": "The locked invariant set (CLAUDE.md §Testing) — additions require explicit owner approval",
  "scripts": {
    "test": "tsx src/run.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "@heliogrid/config": "workspace:*",
    "@types/node": "22.20.1",
    "tsx": "4.23.1",
    "typescript": "5.8.3"
  },
  "turbo": {
    "tags": [
      "app"
    ]
  }
}
