# Exact versions in every package.json — the lockfile is the single authority (docs/engineering/03 pin policy)
save-prefix=
engine-strict=true
# pnpm auto-switches to the packageManager-pinned version
manage-package-manager-versions=true
