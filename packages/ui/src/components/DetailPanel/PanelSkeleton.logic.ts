/**
 * The skeleton's row keys, declared once so the two halves cannot draw a different number of
 * placeholder lines — the kind of drift nobody notices, because a skeleton is on screen for a
 * moment and never compared side by side.
 */
export const PANEL_SKELETON_ROWS = ['row-1', 'row-2', 'row-3', 'row-4'] as const;
