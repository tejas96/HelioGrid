{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tags": ["theme"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "dependsOn": ["build"]
    }
  }
}
