import React from "react";

export interface EmptyStateProps {
  /** line-drawing icon or large glyph */
  icon?: React.ReactNode;
  title: string;
  /** centred, text-secondary, max ~2 lines */
  description?: string;
  /** primary pill button */
  action?: React.ReactNode;
  /** soft brand glow bloom behind the icon (default on) */
  glow?: boolean;
  style?: React.CSSProperties;
}

/**
 * Centred empty/error state — soft brand bloom behind a circular icon, headline, description, primary action.
 * @startingPoint section="Feedback" subtitle="Empty state with brand-glow bloom" viewport="700x340"
 */
export function EmptyState(props: EmptyStateProps): JSX.Element;
