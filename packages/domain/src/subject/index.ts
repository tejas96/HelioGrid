/**
 * The suite's one polymorphic pointer (`F2-22`, `F6-02`): a subject kind beside a subject id.
 * It belongs to no single table, so it lives beside none of them.
 */

export type { SubjectKind } from './kinds';
export { SUBJECT_KINDS } from './kinds';
